# SOLAR code deviations from Figma

Generated by `npm run solar:codegen` from `spec/tokens.json`, `spec/icons.json` and `spec/components/`. Foundations Figma file version `2026-09-25`; the `icon.*` and `logo.*` rows come from the SOLAR Icons file and the `component.*` rows from SOLAR Web, whose versions are recorded in their spec files.

Each row is a place where the generated code deliberately differs from what the Figma data says.
The mirror in `docs/` is never edited to accommodate these; take them to SOLAR governance.

| Token                                                                                                                            | Figma value                                                                                                                                                              | Why we differ                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Action                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| -------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `color.border.inverse`                                                                                                           | border/inverse                                                                                                                                                           | color.border.inverse is used in Figma both as a standalone value and as the namespace for finer variants (e.g. color.border.inverse.subtle). A DTCG tree node cannot cleanly be both, so it is emitted with its own $value and the variants nested beneath it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Ask SOLAR to give color.border.inverse an explicit base/default sibling name in Figma.                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `component.accordion.iconChevronDown.width#unbound`                                                                              | 16, bound to no variable                                                                                                                                                 | width on /Icon/ChevronDown is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (bind):** Same value, governed token; the chevron's height is already bound to it. Ask SOLAR to bind Accordion /Icon/ChevronDown width to icon.sm, which has the same value.                                                                                                                                                                                                                                                                                                                   |
| `component.accordion.root.gap#unbound`                                                                                           | 0, bound to no variable                                                                                                                                                  | gap on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Accordion / gap to inset.none or stack.none, which has the same value.                                 |
| `component.accordion.root.paddingBottom#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Accordion / paddingBottom to inset.none, which has the same value.                                                   |
| `component.accordion.root.paddingLeft#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Accordion / paddingLeft to inset.none, which has the same value.                                                     |
| `component.accordion.root.paddingRight#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Accordion / paddingRight to inset.none, which has the same value.                                                    |
| `component.accordion.root.paddingTop#unbound`                                                                                    | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Accordion / paddingTop to inset.none, which has the same value.                                                      |
| `component.accordion.root.width#unbound`                                                                                         | 400, bound to no variable                                                                                                                                                | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** An item spans the accordion it is in: Figma draws it 400 wide, a sample. The caller sizes it. SOLAR has no control width token; ask SOLAR for one, or confirm 400 is intended.                                                                                                                                                                                                                                                                                                        |
| `component.action card.button.height#unbound`                                                                                    | 32, bound to no variable                                                                                                                                                 | height on /Button is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (set):** As the primary Button's, the Button's own height. Ask SOLAR to bind Action Card /Button height to icon.2xl, which has the same value.                                                                                                                                                                                                                                                                                                                                                 |
| `component.action card.content.paddingBottom#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingBottom on /content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Action Card /content paddingBottom to inset.none, which has the same value.                                          |
| `component.action card.content.paddingLeft#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingLeft on /content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Action Card /content paddingLeft to inset.none, which has the same value.                                            |
| `component.action card.content.paddingRight#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingRight on /content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Action Card /content paddingRight to inset.none, which has the same value.                                           |
| `component.action card.content.paddingTop#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingTop on /content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Action Card /content paddingTop to inset.none, which has the same value.                                             |
| `component.action card.cta.paddingBottom#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingBottom on /CTA is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Action Card /CTA paddingBottom to inset.none, which has the same value.                                              |
| `component.action card.cta.paddingLeft#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingLeft on /CTA is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Action Card /CTA paddingLeft to inset.none, which has the same value.                                                |
| `component.action card.cta.paddingRight#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingRight on /CTA is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Action Card /CTA paddingRight to inset.none, which has the same value.                                               |
| `component.action card.cta.paddingTop#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingTop on /CTA is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Action Card /CTA paddingTop to inset.none, which has the same value.                                                 |
| `component.action card.icon.width#unbound`                                                                                       | 20, bound to no variable                                                                                                                                                 | width on /Title/Icon/None is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind):** Same value, governed token; the icon's height is already bound to it. Ask SOLAR to bind Action Card /Title/Icon/None width to icon.md, which has the same value.                                                                                                                                                                                                                                                                                                                     |
| `component.action card.iconMore.width#unbound`                                                                                   | 20, bound to no variable                                                                                                                                                 | width on /Title/Icon/More is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind):** Same value, governed token; the More icon's height is already bound to it. Ask SOLAR to bind Action Card /Title/Icon/More width to icon.md, which has the same value.                                                                                                                                                                                                                                                                                                                |
| `component.action card.primaryCTA.height#unbound`                                                                                | 32, bound to no variable                                                                                                                                                 | height on /CTA/Button is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (set):** The calls to action are the caller's Buttons, sm as Figma draws them, whose height is their own: the card places them and never sizes them. Ask SOLAR to bind Action Card /CTA/Button height to icon.2xl, which has the same value.                                                                                                                                                                                                                                                   |
| `component.action card.root.width#unbound`                                                                                       | 300, bound to no variable                                                                                                                                                | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** A card spans the space it is put in: Figma draws it 300 wide, a sample. The caller sizes it. SOLAR has no control width token; ask SOLAR for one, or confirm 300 is intended.                                                                                                                                                                                                                                                                                                         |
| `component.action card.secondaryCTA.height#unbound`                                                                              | 32, bound to no variable                                                                                                                                                 | height on /CTA/Button#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (set):** As the primary Button's, the Button's own height. Ask SOLAR to bind Action Card /CTA/Button#2 height to icon.2xl, which has the same value.                                                                                                                                                                                                                                                                                                                                           |
| `component.action card.title.paddingBottom#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Title is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Action Card /Title paddingBottom to inset.none, which has the same value.                                            |
| `component.action card.title.paddingLeft#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Title is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Action Card /Title paddingLeft to inset.none, which has the same value.                                              |
| `component.action card.title.paddingRight#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingRight on /Title is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Action Card /Title paddingRight to inset.none, which has the same value.                                             |
| `component.action card.title.paddingTop#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingTop on /Title is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Action Card /Title paddingTop to inset.none, which has the same value.                                               |
| `component.agenda row.attendee.height#unbound`                                                                                   | 32, bound to no variable                                                                                                                                                 | height on /Attendee is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (set):** The attendee is a SOLAR Avatar (md), whose 32px box is its own: the row places it, and never sizes it. Ask SOLAR to bind Agenda Row /Attendee height to icon.2xl, which has the same value.                                                                                                                                                                                                                                                                                           |
| `component.agenda row.attendee.width#unbound`                                                                                    | 32, bound to no variable                                                                                                                                                 | width on /Attendee is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (set):** The attendee is a SOLAR Avatar (md), whose 32px box is its own: the row places it, and never sizes it. Ask SOLAR to bind Agenda Row /Attendee width to icon.2xl, which has the same value.                                                                                                                                                                                                                                                                                            |
| `component.agenda row.body.paddingBottom#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Body is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Agenda Row /Body paddingBottom to inset.none, which has the same value.                                              |
| `component.agenda row.body.paddingLeft#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Body is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Agenda Row /Body paddingLeft to inset.none, which has the same value.                                                |
| `component.agenda row.body.paddingRight#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingRight on /Body is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Agenda Row /Body paddingRight to inset.none, which has the same value.                                               |
| `component.agenda row.body.paddingTop#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingTop on /Body is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Agenda Row /Body paddingTop to inset.none, which has the same value.                                                 |
| `component.agenda row.dot.height#unbound`                                                                                        | 8, bound to no variable                                                                                                                                                  | height on /Dot is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (allowLiteral):** The event's dot is 8px, as StatusIndicator's md dot, bound to nothing: SOLAR has no marker size. SOLAR has no control height token; ask SOLAR for one, or confirm 8 is intended.                                                                                                                                                                                                                                                                                             |
| `component.agenda row.dot.width#unbound`                                                                                         | 8, bound to no variable                                                                                                                                                  | width on /Dot is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (allowLiteral):** The event's dot is 8px, as StatusIndicator's md dot, bound to nothing: SOLAR has no marker size. SOLAR has no control width token; ask SOLAR for one, or confirm 8 is intended.                                                                                                                                                                                                                                                                                              |
| `component.agenda row.root.height#unbound`                                                                                       | 64, 32, bound to no variable                                                                                                                                             | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (set):** A row is as tall as its lines and padding. Figma fixes it at 64 (comfortable) and 32 (compact), room for its lines trimmed to their cap height (Figma's leading trim, on every SOLAR text); a line drawn at its full height (20 and 16) does not fit them, and neither platform trims a line as Figma does. The design review asks. SOLAR has no control height token; ask SOLAR for one, or confirm 64, 32 is intended.                                                              |
| `component.agenda row.root.height@state=default, density=compact`                                                                | 1 variant at state=default, density=compact: 32 where 64                                                                                                                 | height on / should follow no axis only, but at state=default, density=compact it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** A row is as tall as its lines and padding. Figma fixes it at 64 (comfortable) and 32 (compact), room for its lines trimmed to their cap height (Figma's leading trim, on every SOLAR text); a line drawn at its full height (20 and 16) does not fit them, and neither platform trims a line as Figma does. The design review asks. Ask SOLAR whether Agenda Row / height is meant to change at state=default, density=compact.                                                    |
| `component.agenda row.root.height@state=hover, density=compact`                                                                  | 1 variant at state=hover, density=compact: 32 where 64                                                                                                                   | height on / should follow no axis only, but at state=hover, density=compact it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** A row is as tall as its lines and padding. Figma fixes it at 64 (comfortable) and 32 (compact), room for its lines trimmed to their cap height (Figma's leading trim, on every SOLAR text); a line drawn at its full height (20 and 16) does not fit them, and neither platform trims a line as Figma does. The design review asks. Ask SOLAR whether Agenda Row / height is meant to change at state=hover, density=compact.                                                      |
| `component.agenda row.root.height@state=selected, density=compact`                                                               | 1 variant at state=selected, density=compact: 32 where 64                                                                                                                | height on / should follow no axis only, but at state=selected, density=compact it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** A row is as tall as its lines and padding. Figma fixes it at 64 (comfortable) and 32 (compact), room for its lines trimmed to their cap height (Figma's leading trim, on every SOLAR text); a line drawn at its full height (20 and 16) does not fit them, and neither platform trims a line as Figma does. The design review asks. Ask SOLAR whether Agenda Row / height is meant to change at state=selected, density=compact.                                                   |
| `component.agenda row.root.width#unbound`                                                                                        | 560, bound to no variable                                                                                                                                                | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** A row fills its agenda list: Figma draws a 560 sample. SOLAR has no control width token; ask SOLAR for one, or confirm 560 is intended.                                                                                                                                                                                                                                                                                                                                               |
| `component.agenda row.time.paddingBottom#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Time is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Agenda Row /Time paddingBottom to inset.none, which has the same value.                                              |
| `component.agenda row.time.paddingLeft#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Time is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Agenda Row /Time paddingLeft to inset.none, which has the same value.                                                |
| `component.agenda row.time.paddingRight#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingRight on /Time is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Agenda Row /Time paddingRight to inset.none, which has the same value.                                               |
| `component.agenda row.time.paddingTop#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingTop on /Time is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Agenda Row /Time paddingTop to inset.none, which has the same value.                                                 |
| `component.agenda row.time.width#unbound`                                                                                        | 36, 76, bound to no variable                                                                                                                                             | width on /Time is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (allowLiteral):** The time column is 36 or 76 wide, bound to nothing: SOLAR has no size for it, one of the sizes with no variable. SOLAR has no control width token; ask SOLAR for one, or confirm 36, 76 is intended.                                                                                                                                                                                                                                                                         |
| `component.alert small.content.paddingLeft#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingLeft on /content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Alert Small /content paddingLeft to inset.none, which has the same value.                                            |
| `component.alert small.content.paddingRight#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingRight on /content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Alert Small /content paddingRight to inset.none, which has the same value.                                           |
| `component.alert small.root.width#unbound`                                                                                       | 466, bound to no variable                                                                                                                                                | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** A callout spans the space it is put in: Figma draws it 466 wide, a sample. The caller sizes it. SOLAR has no control width token; ask SOLAR for one, or confirm 466 is intended.                                                                                                                                                                                                                                                                                                      |
| `component.alert small.statusIndicator.height#unbound`                                                                           | 16, bound to no variable                                                                                                                                                 | height on /StatusIndicator is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** As its width, the StatusIndicator's own. Ask SOLAR to bind Alert Small /StatusIndicator height to icon.sm, which has the same value.                                                                                                                                                                                                                                                                                                                                                  |
| `component.alert small.statusIndicator.width#unbound`                                                                            | 16, bound to no variable                                                                                                                                                 | width on /StatusIndicator is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (set):** The mark is a StatusIndicator, whose sm, 16px is its own: the callout places it, and never sizes it. Ask SOLAR to bind Alert Small /StatusIndicator width to icon.sm, which has the same value.                                                                                                                                                                                                                                                                                       |
| `component.alert.content.paddingLeft#unbound`                                                                                    | 0, bound to no variable                                                                                                                                                  | paddingLeft on /content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Alert /content paddingLeft to inset.none, which has the same value.                                                  |
| `component.alert.content.paddingRight#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingRight on /content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Alert /content paddingRight to inset.none, which has the same value.                                                 |
| `component.alert.root.width#unbound`                                                                                             | 466, bound to no variable                                                                                                                                                | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** A callout spans the space it is put in: Figma draws it 466 wide, a sample. The caller sizes it. SOLAR has no control width token; ask SOLAR for one, or confirm 466 is intended.                                                                                                                                                                                                                                                                                                      |
| `component.alert.statusIndicator.height#unbound`                                                                                 | 20, bound to no variable                                                                                                                                                 | height on /StatusIndicator is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** As its width, the StatusIndicator's own. Ask SOLAR to bind Alert /StatusIndicator height to icon.md, which has the same value.                                                                                                                                                                                                                                                                                                                                                        |
| `component.alert.statusIndicator.width#unbound`                                                                                  | 20, bound to no variable                                                                                                                                                 | width on /StatusIndicator is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (set):** The mark is a StatusIndicator, whose md, 20px is its own: the callout places it, and never sizes it. Ask SOLAR to bind Alert /StatusIndicator width to icon.md, which has the same value.                                                                                                                                                                                                                                                                                             |
| `component.all-day bar.root.gap#unbound`                                                                                         | 0, bound to no variable                                                                                                                                                  | gap on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind All-Day Bar / gap to inset.none or stack.none, which has the same value.                               |
| `component.all-day bar.root.height#unbound`                                                                                      | 22, bound to no variable                                                                                                                                                 | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (allowLiteral):** A bar is 22 tall, as an Event Chip is, bound to nothing: SOLAR has no size for it, one of the sizes with no variable (a calendar's row). SOLAR has no control height token; ask SOLAR for one, or confirm 22 is intended.                                                                                                                                                                                                                                                    |
| `component.all-day bar.root.paddingBottom#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind All-Day Bar / paddingBottom to inset.none, which has the same value.                                                 |
| `component.all-day bar.root.paddingLeft#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind All-Day Bar / paddingLeft to inset.none, which has the same value.                                                   |
| `component.all-day bar.root.paddingRight#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind All-Day Bar / paddingRight to inset.none, which has the same value.                                                  |
| `component.all-day bar.root.paddingTop#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind All-Day Bar / paddingTop to inset.none, which has the same value.                                                    |
| `component.all-day bar.root.width#unbound`                                                                                       | 160, 200, 240, bound to no variable                                                                                                                                      | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** A bar fills the columns its event spans: Figma draws each span at a sample width (160, 200, 240). SOLAR has no control width token; ask SOLAR for one, or confirm 160, 200, 240 is intended.                                                                                                                                                                                                                                                                                          |
| `component.all-day bar.root.width@style=solid, span=end`                                                                         | 1 variant at style=solid, span=end: 200 where 160                                                                                                                        | width on / should follow no axis only, but at style=solid, span=end it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** A bar fills the columns its event spans: Figma draws each span at a sample width (160, 200, 240). Ask SOLAR whether All-Day Bar / width is meant to change at style=solid, span=end.                                                                                                                                                                                                                                                                                               |
| `component.all-day bar.root.width@style=solid, span=middle`                                                                      | 1 variant at style=solid, span=middle: 240 where 160                                                                                                                     | width on / should follow no axis only, but at style=solid, span=middle it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (accept):** A bar fills the columns its event spans: Figma draws each span at a sample width (160, 200, 240). Ask SOLAR whether All-Day Bar / width is meant to change at style=solid, span=middle.                                                                                                                                                                                                                                                                                            |
| `component.all-day bar.root.width@style=solid, span=start`                                                                       | 1 variant at style=solid, span=start: 200 where 160                                                                                                                      | width on / should follow no axis only, but at style=solid, span=start it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (accept):** A bar fills the columns its event spans: Figma draws each span at a sample width (160, 200, 240). Ask SOLAR whether All-Day Bar / width is meant to change at style=solid, span=start.                                                                                                                                                                                                                                                                                             |
| `component.all-day bar.root.width@style=subtle, span=end`                                                                        | 1 variant at style=subtle, span=end: 200 where 160                                                                                                                       | width on / should follow no axis only, but at style=subtle, span=end it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (accept):** A bar fills the columns its event spans: Figma draws each span at a sample width (160, 200, 240). Ask SOLAR whether All-Day Bar / width is meant to change at style=subtle, span=end.                                                                                                                                                                                                                                                                                              |
| `component.all-day bar.root.width@style=subtle, span=middle`                                                                     | 1 variant at style=subtle, span=middle: 240 where 160                                                                                                                    | width on / should follow no axis only, but at style=subtle, span=middle it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** A bar fills the columns its event spans: Figma draws each span at a sample width (160, 200, 240). Ask SOLAR whether All-Day Bar / width is meant to change at style=subtle, span=middle.                                                                                                                                                                                                                                                                                           |
| `component.all-day bar.root.width@style=subtle, span=start`                                                                      | 1 variant at style=subtle, span=start: 200 where 160                                                                                                                     | width on / should follow no axis only, but at style=subtle, span=start it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (accept):** A bar fills the columns its event spans: Figma draws each span at a sample width (160, 200, 240). Ask SOLAR whether All-Day Bar / width is meant to change at style=subtle, span=start.                                                                                                                                                                                                                                                                                            |
| `component.all-day bar.stripe.height#unbound`                                                                                    | 25, bound to no variable                                                                                                                                                 | height on /Stripe is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (allowLiteral):** The 25 Figma keeps on the stripe where a solid bar hides it, never drawn (follows stripe.height); where it is drawn it fills the bar. SOLAR has no control height token; ask SOLAR for one, or confirm 25 is intended.                                                                                                                                                                                                                                                       |
| `component.all-day bar.stripe.width#unbound`                                                                                     | 3, bound to no variable                                                                                                                                                  | width on /Stripe is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (allowLiteral):** The stripe is 3 wide, as an Event Chip's, bound to nothing: no border width is 3, and a stripe is a fill, not an edge. SOLAR has no control width token; ask SOLAR for one, or confirm 3 is intended.                                                                                                                                                                                                                                                                        |
| `component.autocomplete open.dropdownMenu.width#unbound`                                                                         | 320, bound to no variable                                                                                                                                                | width on /Dropdown Menu is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (set):** The suggestions are as wide as the field, as MUI's and Flutter's autocompletes draw them: Figma draws them 320, the field's width. SOLAR has no control width token; ask SOLAR for one, or confirm 320 is intended.                                                                                                                                                                                                                                                                   |
| `component.autocomplete open.root.paddingBottom#unbound`                                                                         | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Autocomplete Open / paddingBottom to inset.none, which has the same value.                                           |
| `component.autocomplete open.root.paddingLeft#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Autocomplete Open / paddingLeft to inset.none, which has the same value.                                             |
| `component.autocomplete open.root.paddingRight#unbound`                                                                          | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Autocomplete Open / paddingRight to inset.none, which has the same value.                                            |
| `component.autocomplete open.root.paddingTop#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Autocomplete Open / paddingTop to inset.none, which has the same value.                                              |
| `component.autocomplete.field.height#unbound`                                                                                    | 40, 32, bound to no variable                                                                                                                                             | height on /Field is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind):** As Button's height: size.control, one step per size, which SOLAR added on 2026-09-25; Figma leaves the height unbound, which the design review asks SOLAR to bind. SOLAR has no control height token; ask SOLAR for one, or confirm 40, 32 is intended.                                                                                                                                                                                                                              |
| `component.autocomplete.label.paddingBottom#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Autocomplete /Label paddingBottom to inset.none, which has the same value.                                           |
| `component.autocomplete.label.paddingLeft#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Autocomplete /Label paddingLeft to inset.none, which has the same value.                                             |
| `component.autocomplete.label.paddingRight#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingRight on /Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Autocomplete /Label paddingRight to inset.none, which has the same value.                                            |
| `component.autocomplete.label.paddingTop#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingTop on /Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Autocomplete /Label paddingTop to inset.none, which has the same value.                                              |
| `component.autocomplete.leadingIcon.width#unbound`                                                                               | 16, bound to no variable                                                                                                                                                 | width on /Field/Icon/None is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind):** Same value, governed token; the icon's height is already bound to it. Ask SOLAR to bind Autocomplete /Field/Icon/None width to icon.sm, which has the same value.                                                                                                                                                                                                                                                                                                                    |
| `component.autocomplete.root.paddingBottom#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Autocomplete / paddingBottom to inset.none, which has the same value.                                                |
| `component.autocomplete.root.paddingLeft#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Autocomplete / paddingLeft to inset.none, which has the same value.                                                  |
| `component.autocomplete.root.paddingRight#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Autocomplete / paddingRight to inset.none, which has the same value.                                                 |
| `component.autocomplete.root.paddingTop#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Autocomplete / paddingTop to inset.none, which has the same value.                                                   |
| `component.autocomplete.root.width#unbound`                                                                                      | 240, 160, bound to no variable                                                                                                                                           | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** As md's, a sample. SOLAR has no control width token; ask SOLAR for one, or confirm 240, 160 is intended.                                                                                                                                                                                                                                                                                                                                                                              |
| `component.autocomplete.trailingIcon.width#unbound`                                                                              | 16, bound to no variable                                                                                                                                                 | width on /Field/Icon/None#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind):** As the leading icon's. Ask SOLAR to bind Autocomplete /Field/Icon/None#2 width to icon.sm, which has the same value.                                                                                                                                                                                                                                                                                                                                                                 |
| `component.avatar.root.gap#unbound`                                                                                              | 0, bound to no variable                                                                                                                                                  | gap on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Avatar / gap to inset.none or stack.none, which has the same value.                                    |
| `component.avatar.root.height#unbound`                                                                                           | 44, 32, 24, 16, bound to no variable                                                                                                                                     | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (allowLiteral):** As the width, the avatar is square. SOLAR has no control height token; ask SOLAR for one, or confirm 44, 32, 24, 16 is intended.                                                                                                                                                                                                                                                                                                                                             |
| `component.avatar.root.paddingBottom#unbound`                                                                                    | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Avatar / paddingBottom to inset.none, which has the same value.                                                      |
| `component.avatar.root.paddingLeft#unbound`                                                                                      | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Avatar / paddingLeft to inset.none, which has the same value.                                                        |
| `component.avatar.root.paddingRight#unbound`                                                                                     | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Avatar / paddingRight to inset.none, which has the same value.                                                       |
| `component.avatar.root.paddingTop#unbound`                                                                                       | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Avatar / paddingTop to inset.none, which has the same value.                                                         |
| `component.avatar.root.radius#unbound`                                                                                           | 11, bound to no variable                                                                                                                                                 | radius on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (set):** The lg logo avatar Figma added on 2026-09-25 is rounded 11, bound to nothing, where the md and sm logos take radius.container: drawn as theirs at every size, a slip the design review lists. SOLAR has no radius token; ask SOLAR for one, or confirm 11 is intended.                                                                                                                                                                                                                |
| `component.avatar.root.radius@size=md`                                                                                           | 1 variant at size=md: radius.container where 11                                                                                                                          | radius on / should follow type only, but at size=md it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (set):** The lg logo avatar Figma added on 2026-09-25 is rounded 11, bound to nothing, where the md and sm logos take radius.container: drawn as theirs at every size, a slip the design review lists. Ask SOLAR whether Avatar / radius is meant to change at size=md.                                                                                                                                                                                                                        |
| `component.avatar.root.radius@size=sm`                                                                                           | 1 variant at size=sm: radius.container where 11                                                                                                                          | radius on / should follow type only, but at size=sm it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (set):** The lg logo avatar Figma added on 2026-09-25 is rounded 11, bound to nothing, where the md and sm logos take radius.container: drawn as theirs at every size, a slip the design review lists. Ask SOLAR whether Avatar / radius is meant to change at size=sm.                                                                                                                                                                                                                        |
| `component.avatar.root.width#unbound`                                                                                            | 44, 32, 24, 16, bound to no variable                                                                                                                                     | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (allowLiteral):** The avatar's sizes, 44, 32, 24 and 16, bound to nothing: SOLAR has no avatar size. Raised as a governance gap. SOLAR has no control width token; ask SOLAR for one, or confirm 44, 32, 24, 16 is intended.                                                                                                                                                                                                                                                                   |
| `component.backbutton.iconArrowLeft.width#unbound`                                                                               | 20, 16, bound to no variable                                                                                                                                             | width on /Icon/ArrowLeft is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind):** Same values, governed tokens, a step of the icon ladder per size; the arrow's height is already bound to them. Ask SOLAR to bind BackButton /Icon/ArrowLeft width to icon.md or icon.sm, which has the same value.                                                                                                                                                                                                                                                                   |
| `component.backbutton.root.height#unbound`                                                                                       | 40, 32, bound to no variable                                                                                                                                             | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind):** As Button's height: size.control, one step per size, which SOLAR added on 2026-09-25; Figma leaves the height unbound, which the design review asks SOLAR to bind. SOLAR has no control height token; ask SOLAR for one, or confirm 40, 32 is intended.                                                                                                                                                                                                                              |
| `component.backbutton.root.paddingBottom#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind BackButton / paddingBottom to inset.none, which has the same value.                                                  |
| `component.backbutton.root.paddingTop#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind BackButton / paddingTop to inset.none, which has the same value.                                                     |
| `component.banner.actionGroup.paddingBottom#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingBottom on /action group is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Banner /action group paddingBottom to inset.none, which has the same value.                                          |
| `component.banner.actionGroup.paddingLeft#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingLeft on /action group is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Banner /action group paddingLeft to inset.none, which has the same value.                                            |
| `component.banner.actionGroup.paddingRight#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingRight on /action group is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Banner /action group paddingRight to inset.none, which has the same value.                                           |
| `component.banner.actionGroup.paddingTop#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingTop on /action group is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Banner /action group paddingTop to inset.none, which has the same value.                                             |
| `component.banner.close.height#unbound`                                                                                          | 20, bound to no variable                                                                                                                                                 | height on /Icon/Close is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind):** As the info icon's; the close button's width is already bound to it. Ask SOLAR to bind Banner /Icon/Close height to icon.md, which has the same value.                                                                                                                                                                                                                                                                                                                               |
| `component.banner.iconDanger.height#unbound`                                                                                     | 20, bound to no variable                                                                                                                                                 | height on /Icon/Danger is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind):** As the info icon's. Ask SOLAR to bind Banner /Icon/Danger height to icon.md, which has the same value.                                                                                                                                                                                                                                                                                                                                                                               |
| `component.banner.iconInfo.height#unbound`                                                                                       | 20, bound to no variable                                                                                                                                                 | height on /Icon/Info is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind):** Same value, governed token; the icon's width is already bound to it. Ask SOLAR to bind Banner /Icon/Info height to icon.md, which has the same value.                                                                                                                                                                                                                                                                                                                                |
| `component.banner.iconSuccess.height#unbound`                                                                                    | 20, bound to no variable                                                                                                                                                 | height on /Icon/Success is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind):** As the info icon's. Ask SOLAR to bind Banner /Icon/Success height to icon.md, which has the same value.                                                                                                                                                                                                                                                                                                                                                                              |
| `component.banner.iconWarning.height#unbound`                                                                                    | 20, bound to no variable                                                                                                                                                 | height on /Icon/Warning is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind):** As the info icon's. Ask SOLAR to bind Banner /Icon/Warning height to icon.md, which has the same value.                                                                                                                                                                                                                                                                                                                                                                              |
| `component.banner.primaryButton.height#unbound`                                                                                  | 32, bound to no variable                                                                                                                                                 | height on /action group/Button is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (set):** The Buttons are the caller's, sm as Figma draws them, whose height is their own: the banner places them and never sizes them. Ask SOLAR to bind Banner /action group/Button height to icon.2xl, which has the same value.                                                                                                                                                                                                                                                             |
| `component.banner.root.height#unbound`                                                                                           | 44, bound to no variable                                                                                                                                                 | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (allowLiteral):** A banner is 44 tall, bound to nothing, and on none of SOLAR's control heights (size.control: 32, 40, 48); carried as drawn, the design review asks. SOLAR has no control height token; ask SOLAR for one, or confirm 44 is intended.                                                                                                                                                                                                                                         |
| `component.banner.root.width#unbound`                                                                                            | 458, bound to no variable                                                                                                                                                | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** A banner spans the page or app it speaks for ("full-width", says the description): Figma draws it 458 wide, a sample. The caller places it. SOLAR has no control width token; ask SOLAR for one, or confirm 458 is intended.                                                                                                                                                                                                                                                          |
| `component.banner.secondaryButton.height#unbound`                                                                                | 32, bound to no variable                                                                                                                                                 | height on /action group/Button#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (set):** As the primary Button's, the Button's own height. Ask SOLAR to bind Banner /action group/Button#2 height to icon.2xl, which has the same value.                                                                                                                                                                                                                                                                                                                                       |
| `component.bar chart.bars.gap#unbound`                                                                                           | 10, bound to no variable                                                                                                                                                 | gap on /Plot/Bars is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no gap token; ask SOLAR for one, or confirm 10 is intended.                                                                                                                                                     |
| `component.bar chart.bars.gap@type=grouped, orientation=vertical, breakpoint=desktop`                                            | 1 variant at type=grouped, orientation=vertical, breakpoint=desktop: inset.xl where inset.md                                                                             | gap on /Plot/Bars should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars gap is meant to change at type=grouped, orientation=vertical, breakpoint=desktop.                                                                                                  |
| `component.bar chart.bars.gap@type=grouped, orientation=vertical, breakpoint=mobile`                                             | 1 variant at type=grouped, orientation=vertical, breakpoint=mobile: inset.sm where inset.md                                                                              | gap on /Plot/Bars should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars gap is meant to change at type=grouped, orientation=vertical, breakpoint=mobile.                                                                                                   |
| `component.bar chart.bars.gap@type=simple, orientation=vertical, breakpoint=mobile`                                              | 1 variant at type=simple, orientation=vertical, breakpoint=mobile: 10 where inset.md                                                                                     | gap on /Plot/Bars should follow no axis only, but at type=simple, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars gap is meant to change at type=simple, orientation=vertical, breakpoint=mobile.                                                                                                    |
| `component.bar chart.bars.gap@type=stacked, orientation=vertical, breakpoint=mobile`                                             | 1 variant at type=stacked, orientation=vertical, breakpoint=mobile: 10 where inset.md                                                                                    | gap on /Plot/Bars should follow no axis only, but at type=stacked, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars gap is meant to change at type=stacked, orientation=vertical, breakpoint=mobile.                                                                                                   |
| `component.bar chart.bars.paddingBottom#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Bars is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars paddingBottom to inset.none, which has the same value.                                                                                                                             |
| `component.bar chart.bars.paddingLeft#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Bars is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars paddingLeft to inset.none, which has the same value.                                                                                                                               |
| `component.bar chart.bars.paddingRight#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Bars is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars paddingRight to inset.none, which has the same value.                                                                                                                              |
| `component.bar chart.bars.paddingTop#unbound`                                                                                    | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Bars is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars paddingTop to inset.none, which has the same value.                                                                                                                                |
| `component.bar chart.col0.align@type=grouped, orientation=vertical, breakpoint=desktop`                                          | 1 variant at type=grouped, orientation=vertical, breakpoint=desktop: CENTER/MAX where MAX/CENTER                                                                         | align on /Plot/Bars/Col-0 should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-0 align is meant to change at type=grouped, orientation=vertical, breakpoint=desktop.                                                                                          |
| `component.bar chart.col0.align@type=grouped, orientation=vertical, breakpoint=mobile`                                           | 1 variant at type=grouped, orientation=vertical, breakpoint=mobile: CENTER/MAX where MAX/CENTER                                                                          | align on /Plot/Bars/Col-0 should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-0 align is meant to change at type=grouped, orientation=vertical, breakpoint=mobile.                                                                                           |
| `component.bar chart.col0.direction@type=grouped, orientation=vertical, breakpoint=desktop`                                      | 1 variant at type=grouped, orientation=vertical, breakpoint=desktop: HORIZONTAL where VERTICAL                                                                           | direction on /Plot/Bars/Col-0 should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-0 direction is meant to change at type=grouped, orientation=vertical, breakpoint=desktop.                                                                                      |
| `component.bar chart.col0.direction@type=grouped, orientation=vertical, breakpoint=mobile`                                       | 1 variant at type=grouped, orientation=vertical, breakpoint=mobile: HORIZONTAL where VERTICAL                                                                            | direction on /Plot/Bars/Col-0 should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-0 direction is meant to change at type=grouped, orientation=vertical, breakpoint=mobile.                                                                                       |
| `component.bar chart.col0.gap#unbound`                                                                                           | 0, bound to no variable                                                                                                                                                  | gap on /Plot/Bars/Col-0 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-0 gap to inset.none or stack.none, which has the same value.                                                                                                                   |
| `component.bar chart.col0.gap@type=grouped, orientation=vertical, breakpoint=desktop`                                            | 1 variant at type=grouped, orientation=vertical, breakpoint=desktop: inset.2xs where 0                                                                                   | gap on /Plot/Bars/Col-0 should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-0 gap is meant to change at type=grouped, orientation=vertical, breakpoint=desktop.                                                                                            |
| `component.bar chart.col0.gap@type=grouped, orientation=vertical, breakpoint=mobile`                                             | 1 variant at type=grouped, orientation=vertical, breakpoint=mobile: inset.2xs where 0                                                                                    | gap on /Plot/Bars/Col-0 should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-0 gap is meant to change at type=grouped, orientation=vertical, breakpoint=mobile.                                                                                             |
| `component.bar chart.col0.paddingBottom#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Bars/Col-0 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-0 paddingBottom to inset.none, which has the same value.                                                                                                                       |
| `component.bar chart.col0.paddingLeft#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Bars/Col-0 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-0 paddingLeft to inset.none, which has the same value.                                                                                                                         |
| `component.bar chart.col0.paddingRight#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Bars/Col-0 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-0 paddingRight to inset.none, which has the same value.                                                                                                                        |
| `component.bar chart.col0.paddingTop#unbound`                                                                                    | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Bars/Col-0 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-0 paddingTop to inset.none, which has the same value.                                                                                                                          |
| `component.bar chart.col0BarStack.gap#unbound`                                                                                   | 2, bound to no variable                                                                                                                                                  | gap on /Plot/Bars/Col-0/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no gap token; ask SOLAR for one, or confirm 2 is intended.                                                                                                                                                      |
| `component.bar chart.col0BarStack.height#unbound`                                                                                | 149, 101, bound to no variable                                                                                                                                           | height on /Plot/Bars/Col-0/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 149, 101 is intended.                                                                                                                                    |
| `component.bar chart.col0BarStack.height@type=stacked, orientation=vertical, breakpoint=mobile`                                  | 1 variant at type=stacked, orientation=vertical, breakpoint=mobile: 101 where 149                                                                                        | height on /Plot/Bars/Col-0/Bar Stack should follow no axis only, but at type=stacked, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-0/Bar Stack height is meant to change at type=stacked, orientation=vertical, breakpoint=mobile.                                                                                |
| `component.bar chart.col0BarStack.paddingBottom#unbound`                                                                         | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Bars/Col-0/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-0/Bar Stack paddingBottom to inset.none, which has the same value.                                                                                                             |
| `component.bar chart.col0BarStack.paddingLeft#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Bars/Col-0/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-0/Bar Stack paddingLeft to inset.none, which has the same value.                                                                                                               |
| `component.bar chart.col0BarStack.paddingRight#unbound`                                                                          | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Bars/Col-0/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-0/Bar Stack paddingRight to inset.none, which has the same value.                                                                                                              |
| `component.bar chart.col0BarStack.paddingTop#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Bars/Col-0/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-0/Bar Stack paddingTop to inset.none, which has the same value.                                                                                                                |
| `component.bar chart.col0BarStack.width#unbound`                                                                                 | 40, 32, bound to no variable                                                                                                                                             | width on /Plot/Bars/Col-0/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 40, 32 is intended.                                                                                                                                       |
| `component.bar chart.col0BarStack.width@type=stacked, orientation=vertical, breakpoint=mobile`                                   | 1 variant at type=stacked, orientation=vertical, breakpoint=mobile: 32 where 40                                                                                          | width on /Plot/Bars/Col-0/Bar Stack should follow no axis only, but at type=stacked, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-0/Bar Stack width is meant to change at type=stacked, orientation=vertical, breakpoint=mobile.                                                                                 |
| `component.bar chart.col0BarStackColorFeedbackDangerStrong.height#unbound`                                                       | 51, 34, bound to no variable                                                                                                                                             | height on /Plot/Bars/Col-0/Bar Stack/color=feedback-danger-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 51, 34 is intended.                                                                                                                                      |
| `component.bar chart.col0BarStackColorFeedbackDangerStrong.height@type=stacked, orientation=vertical, breakpoint=mobile`         | 1 variant at type=stacked, orientation=vertical, breakpoint=mobile: 34 where 51                                                                                          | height on /Plot/Bars/Col-0/Bar Stack/color=feedback-danger-strong should follow no axis only, but at type=stacked, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-0/Bar Stack/color=feedback-danger-strong height is meant to change at type=stacked, orientation=vertical, breakpoint=mobile.                                                   |
| `component.bar chart.col0BarStackColorFeedbackNeutralSubtle.height#unbound`                                                      | 58, 39, bound to no variable                                                                                                                                             | height on /Plot/Bars/Col-0/Bar Stack/color=feedback-neutral-subtle is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 58, 39 is intended.                                                                                                                                      |
| `component.bar chart.col0BarStackColorFeedbackNeutralSubtle.height@type=stacked, orientation=vertical, breakpoint=mobile`        | 1 variant at type=stacked, orientation=vertical, breakpoint=mobile: 39 where 58                                                                                          | height on /Plot/Bars/Col-0/Bar Stack/color=feedback-neutral-subtle should follow no axis only, but at type=stacked, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-0/Bar Stack/color=feedback-neutral-subtle height is meant to change at type=stacked, orientation=vertical, breakpoint=mobile.                                                  |
| `component.bar chart.col0BarStackColorFeedbackWarningMedium.height#unbound`                                                      | 36, 24, bound to no variable                                                                                                                                             | height on /Plot/Bars/Col-0/Bar Stack/color=feedback-warning-medium is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-0/Bar Stack/color=feedback-warning-medium height to icon.lg, which has the same value.                                                                                         |
| `component.bar chart.col0BarStackColorFeedbackWarningMedium.height@type=stacked, orientation=vertical, breakpoint=mobile`        | 1 variant at type=stacked, orientation=vertical, breakpoint=mobile: 24 where 36                                                                                          | height on /Plot/Bars/Col-0/Bar Stack/color=feedback-warning-medium should follow no axis only, but at type=stacked, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-0/Bar Stack/color=feedback-warning-medium height is meant to change at type=stacked, orientation=vertical, breakpoint=mobile.                                                  |
| `component.bar chart.col0ColorCategory02Strong.height#unbound`                                                                   | 86, 54, bound to no variable                                                                                                                                             | height on /Plot/Bars/Col-0/color=category-02-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 86, 54 is intended.                                                                                                                                      |
| `component.bar chart.col0ColorCategory02Strong.height@type=grouped, orientation=vertical, breakpoint=mobile`                     | 1 variant at type=grouped, orientation=vertical, breakpoint=mobile: 54 where 86                                                                                          | height on /Plot/Bars/Col-0/color=category-02-strong should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-0/color=category-02-strong height is meant to change at type=grouped, orientation=vertical, breakpoint=mobile.                                                                 |
| `component.bar chart.col0ColorCategory02Strong.width#unbound`                                                                    | 20, 14, bound to no variable                                                                                                                                             | width on /Plot/Bars/Col-0/color=category-02-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-0/color=category-02-strong width to icon.md, which has the same value.                                                                                                         |
| `component.bar chart.col0ColorCategory02Strong.width@type=grouped, orientation=vertical, breakpoint=mobile`                      | 1 variant at type=grouped, orientation=vertical, breakpoint=mobile: 14 where 20                                                                                          | width on /Plot/Bars/Col-0/color=category-02-strong should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-0/color=category-02-strong width is meant to change at type=grouped, orientation=vertical, breakpoint=mobile.                                                                  |
| `component.bar chart.col0ColorCategory06Strong.height#unbound`                                                                   | 146, 98, 130, 82, bound to no variable                                                                                                                                   | height on /Plot/Bars/Col-0/color=category-06-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 146, 98, 130, 82 is intended.                                                                                                                            |
| `component.bar chart.col0ColorCategory06Strong.height@type=grouped, orientation=vertical, breakpoint=desktop`                    | 1 variant at type=grouped, orientation=vertical, breakpoint=desktop: 130 where 146                                                                                       | height on /Plot/Bars/Col-0/color=category-06-strong should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-0/color=category-06-strong height is meant to change at type=grouped, orientation=vertical, breakpoint=desktop.                                                                |
| `component.bar chart.col0ColorCategory06Strong.height@type=grouped, orientation=vertical, breakpoint=mobile`                     | 1 variant at type=grouped, orientation=vertical, breakpoint=mobile: 82 where 146                                                                                         | height on /Plot/Bars/Col-0/color=category-06-strong should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-0/color=category-06-strong height is meant to change at type=grouped, orientation=vertical, breakpoint=mobile.                                                                 |
| `component.bar chart.col0ColorCategory06Strong.height@type=simple, orientation=vertical, breakpoint=mobile`                      | 1 variant at type=simple, orientation=vertical, breakpoint=mobile: 98 where 146                                                                                          | height on /Plot/Bars/Col-0/color=category-06-strong should follow no axis only, but at type=simple, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-0/color=category-06-strong height is meant to change at type=simple, orientation=vertical, breakpoint=mobile.                                                                  |
| `component.bar chart.col0ColorCategory06Strong.width#unbound`                                                                    | 40, 32, 20, 14, bound to no variable                                                                                                                                     | width on /Plot/Bars/Col-0/color=category-06-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-0/color=category-06-strong width to icon.2xl or icon.md, which has the same value.                                                                                             |
| `component.bar chart.col0ColorCategory06Strong.width@type=grouped, orientation=vertical, breakpoint=desktop`                     | 1 variant at type=grouped, orientation=vertical, breakpoint=desktop: 20 where 40                                                                                         | width on /Plot/Bars/Col-0/color=category-06-strong should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-0/color=category-06-strong width is meant to change at type=grouped, orientation=vertical, breakpoint=desktop.                                                                 |
| `component.bar chart.col0ColorCategory06Strong.width@type=grouped, orientation=vertical, breakpoint=mobile`                      | 1 variant at type=grouped, orientation=vertical, breakpoint=mobile: 14 where 40                                                                                          | width on /Plot/Bars/Col-0/color=category-06-strong should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-0/color=category-06-strong width is meant to change at type=grouped, orientation=vertical, breakpoint=mobile.                                                                  |
| `component.bar chart.col0ColorCategory06Strong.width@type=simple, orientation=vertical, breakpoint=mobile`                       | 1 variant at type=simple, orientation=vertical, breakpoint=mobile: 32 where 40                                                                                           | width on /Plot/Bars/Col-0/color=category-06-strong should follow no axis only, but at type=simple, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-0/color=category-06-strong width is meant to change at type=simple, orientation=vertical, breakpoint=mobile.                                                                   |
| `component.bar chart.col1.align@type=grouped, orientation=vertical, breakpoint=desktop`                                          | 1 variant at type=grouped, orientation=vertical, breakpoint=desktop: CENTER/MAX where MAX/CENTER                                                                         | align on /Plot/Bars/Col-1 should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-1 align is meant to change at type=grouped, orientation=vertical, breakpoint=desktop.                                                                                          |
| `component.bar chart.col1.align@type=grouped, orientation=vertical, breakpoint=mobile`                                           | 1 variant at type=grouped, orientation=vertical, breakpoint=mobile: CENTER/MAX where MAX/CENTER                                                                          | align on /Plot/Bars/Col-1 should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-1 align is meant to change at type=grouped, orientation=vertical, breakpoint=mobile.                                                                                           |
| `component.bar chart.col1.direction@type=grouped, orientation=vertical, breakpoint=desktop`                                      | 1 variant at type=grouped, orientation=vertical, breakpoint=desktop: HORIZONTAL where VERTICAL                                                                           | direction on /Plot/Bars/Col-1 should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-1 direction is meant to change at type=grouped, orientation=vertical, breakpoint=desktop.                                                                                      |
| `component.bar chart.col1.direction@type=grouped, orientation=vertical, breakpoint=mobile`                                       | 1 variant at type=grouped, orientation=vertical, breakpoint=mobile: HORIZONTAL where VERTICAL                                                                            | direction on /Plot/Bars/Col-1 should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-1 direction is meant to change at type=grouped, orientation=vertical, breakpoint=mobile.                                                                                       |
| `component.bar chart.col1.gap#unbound`                                                                                           | 0, bound to no variable                                                                                                                                                  | gap on /Plot/Bars/Col-1 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-1 gap to inset.none or stack.none, which has the same value.                                                                                                                   |
| `component.bar chart.col1.gap@type=grouped, orientation=vertical, breakpoint=desktop`                                            | 1 variant at type=grouped, orientation=vertical, breakpoint=desktop: inset.2xs where 0                                                                                   | gap on /Plot/Bars/Col-1 should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-1 gap is meant to change at type=grouped, orientation=vertical, breakpoint=desktop.                                                                                            |
| `component.bar chart.col1.gap@type=grouped, orientation=vertical, breakpoint=mobile`                                             | 1 variant at type=grouped, orientation=vertical, breakpoint=mobile: inset.2xs where 0                                                                                    | gap on /Plot/Bars/Col-1 should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-1 gap is meant to change at type=grouped, orientation=vertical, breakpoint=mobile.                                                                                             |
| `component.bar chart.col1.paddingBottom#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Bars/Col-1 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-1 paddingBottom to inset.none, which has the same value.                                                                                                                       |
| `component.bar chart.col1.paddingLeft#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Bars/Col-1 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-1 paddingLeft to inset.none, which has the same value.                                                                                                                         |
| `component.bar chart.col1.paddingRight#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Bars/Col-1 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-1 paddingRight to inset.none, which has the same value.                                                                                                                        |
| `component.bar chart.col1.paddingTop#unbound`                                                                                    | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Bars/Col-1 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-1 paddingTop to inset.none, which has the same value.                                                                                                                          |
| `component.bar chart.col1BarStack.gap#unbound`                                                                                   | 2, bound to no variable                                                                                                                                                  | gap on /Plot/Bars/Col-1/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no gap token; ask SOLAR for one, or confirm 2 is intended.                                                                                                                                                      |
| `component.bar chart.col1BarStack.height#unbound`                                                                                | 193, 131, bound to no variable                                                                                                                                           | height on /Plot/Bars/Col-1/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 193, 131 is intended.                                                                                                                                    |
| `component.bar chart.col1BarStack.height@type=stacked, orientation=vertical, breakpoint=mobile`                                  | 1 variant at type=stacked, orientation=vertical, breakpoint=mobile: 131 where 193                                                                                        | height on /Plot/Bars/Col-1/Bar Stack should follow no axis only, but at type=stacked, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-1/Bar Stack height is meant to change at type=stacked, orientation=vertical, breakpoint=mobile.                                                                                |
| `component.bar chart.col1BarStack.paddingBottom#unbound`                                                                         | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Bars/Col-1/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-1/Bar Stack paddingBottom to inset.none, which has the same value.                                                                                                             |
| `component.bar chart.col1BarStack.paddingLeft#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Bars/Col-1/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-1/Bar Stack paddingLeft to inset.none, which has the same value.                                                                                                               |
| `component.bar chart.col1BarStack.paddingRight#unbound`                                                                          | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Bars/Col-1/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-1/Bar Stack paddingRight to inset.none, which has the same value.                                                                                                              |
| `component.bar chart.col1BarStack.paddingTop#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Bars/Col-1/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-1/Bar Stack paddingTop to inset.none, which has the same value.                                                                                                                |
| `component.bar chart.col1BarStack.width#unbound`                                                                                 | 40, 32, bound to no variable                                                                                                                                             | width on /Plot/Bars/Col-1/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 40, 32 is intended.                                                                                                                                       |
| `component.bar chart.col1BarStack.width@type=stacked, orientation=vertical, breakpoint=mobile`                                   | 1 variant at type=stacked, orientation=vertical, breakpoint=mobile: 32 where 40                                                                                          | width on /Plot/Bars/Col-1/Bar Stack should follow no axis only, but at type=stacked, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-1/Bar Stack width is meant to change at type=stacked, orientation=vertical, breakpoint=mobile.                                                                                 |
| `component.bar chart.col1BarStackColorFeedbackDangerStrong.height#unbound`                                                       | 19, 13, bound to no variable                                                                                                                                             | height on /Plot/Bars/Col-1/Bar Stack/color=feedback-danger-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 19, 13 is intended.                                                                                                                                      |
| `component.bar chart.col1BarStackColorFeedbackDangerStrong.height@type=stacked, orientation=vertical, breakpoint=mobile`         | 1 variant at type=stacked, orientation=vertical, breakpoint=mobile: 13 where 19                                                                                          | height on /Plot/Bars/Col-1/Bar Stack/color=feedback-danger-strong should follow no axis only, but at type=stacked, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-1/Bar Stack/color=feedback-danger-strong height is meant to change at type=stacked, orientation=vertical, breakpoint=mobile.                                                   |
| `component.bar chart.col1BarStackColorFeedbackNeutralSubtle.height#unbound`                                                      | 95, 64, bound to no variable                                                                                                                                             | height on /Plot/Bars/Col-1/Bar Stack/color=feedback-neutral-subtle is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 95, 64 is intended.                                                                                                                                      |
| `component.bar chart.col1BarStackColorFeedbackNeutralSubtle.height@type=stacked, orientation=vertical, breakpoint=mobile`        | 1 variant at type=stacked, orientation=vertical, breakpoint=mobile: 64 where 95                                                                                          | height on /Plot/Bars/Col-1/Bar Stack/color=feedback-neutral-subtle should follow no axis only, but at type=stacked, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-1/Bar Stack/color=feedback-neutral-subtle height is meant to change at type=stacked, orientation=vertical, breakpoint=mobile.                                                  |
| `component.bar chart.col1BarStackColorFeedbackWarningMedium.height#unbound`                                                      | 76, 51, bound to no variable                                                                                                                                             | height on /Plot/Bars/Col-1/Bar Stack/color=feedback-warning-medium is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 76, 51 is intended.                                                                                                                                      |
| `component.bar chart.col1BarStackColorFeedbackWarningMedium.height@type=stacked, orientation=vertical, breakpoint=mobile`        | 1 variant at type=stacked, orientation=vertical, breakpoint=mobile: 51 where 76                                                                                          | height on /Plot/Bars/Col-1/Bar Stack/color=feedback-warning-medium should follow no axis only, but at type=stacked, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-1/Bar Stack/color=feedback-warning-medium height is meant to change at type=stacked, orientation=vertical, breakpoint=mobile.                                                  |
| `component.bar chart.col1ColorCategory02Strong.height#unbound`                                                                   | 119, 75, bound to no variable                                                                                                                                            | height on /Plot/Bars/Col-1/color=category-02-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 119, 75 is intended.                                                                                                                                     |
| `component.bar chart.col1ColorCategory02Strong.height@type=grouped, orientation=vertical, breakpoint=mobile`                     | 1 variant at type=grouped, orientation=vertical, breakpoint=mobile: 75 where 119                                                                                         | height on /Plot/Bars/Col-1/color=category-02-strong should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-1/color=category-02-strong height is meant to change at type=grouped, orientation=vertical, breakpoint=mobile.                                                                 |
| `component.bar chart.col1ColorCategory02Strong.width#unbound`                                                                    | 20, 14, bound to no variable                                                                                                                                             | width on /Plot/Bars/Col-1/color=category-02-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-1/color=category-02-strong width to icon.md, which has the same value.                                                                                                         |
| `component.bar chart.col1ColorCategory02Strong.width@type=grouped, orientation=vertical, breakpoint=mobile`                      | 1 variant at type=grouped, orientation=vertical, breakpoint=mobile: 14 where 20                                                                                          | width on /Plot/Bars/Col-1/color=category-02-strong should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-1/color=category-02-strong width is meant to change at type=grouped, orientation=vertical, breakpoint=mobile.                                                                  |
| `component.bar chart.col1ColorCategory06Strong.height#unbound`                                                                   | 190, 128, 168, 106, bound to no variable                                                                                                                                 | height on /Plot/Bars/Col-1/color=category-06-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 190, 128, 168, 106 is intended.                                                                                                                          |
| `component.bar chart.col1ColorCategory06Strong.height@type=grouped, orientation=vertical, breakpoint=desktop`                    | 1 variant at type=grouped, orientation=vertical, breakpoint=desktop: 168 where 190                                                                                       | height on /Plot/Bars/Col-1/color=category-06-strong should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-1/color=category-06-strong height is meant to change at type=grouped, orientation=vertical, breakpoint=desktop.                                                                |
| `component.bar chart.col1ColorCategory06Strong.height@type=grouped, orientation=vertical, breakpoint=mobile`                     | 1 variant at type=grouped, orientation=vertical, breakpoint=mobile: 106 where 190                                                                                        | height on /Plot/Bars/Col-1/color=category-06-strong should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-1/color=category-06-strong height is meant to change at type=grouped, orientation=vertical, breakpoint=mobile.                                                                 |
| `component.bar chart.col1ColorCategory06Strong.height@type=simple, orientation=vertical, breakpoint=mobile`                      | 1 variant at type=simple, orientation=vertical, breakpoint=mobile: 128 where 190                                                                                         | height on /Plot/Bars/Col-1/color=category-06-strong should follow no axis only, but at type=simple, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-1/color=category-06-strong height is meant to change at type=simple, orientation=vertical, breakpoint=mobile.                                                                  |
| `component.bar chart.col1ColorCategory06Strong.width#unbound`                                                                    | 40, 32, 20, 14, bound to no variable                                                                                                                                     | width on /Plot/Bars/Col-1/color=category-06-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-1/color=category-06-strong width to icon.2xl or icon.md, which has the same value.                                                                                             |
| `component.bar chart.col1ColorCategory06Strong.width@type=grouped, orientation=vertical, breakpoint=desktop`                     | 1 variant at type=grouped, orientation=vertical, breakpoint=desktop: 20 where 40                                                                                         | width on /Plot/Bars/Col-1/color=category-06-strong should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-1/color=category-06-strong width is meant to change at type=grouped, orientation=vertical, breakpoint=desktop.                                                                 |
| `component.bar chart.col1ColorCategory06Strong.width@type=grouped, orientation=vertical, breakpoint=mobile`                      | 1 variant at type=grouped, orientation=vertical, breakpoint=mobile: 14 where 40                                                                                          | width on /Plot/Bars/Col-1/color=category-06-strong should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-1/color=category-06-strong width is meant to change at type=grouped, orientation=vertical, breakpoint=mobile.                                                                  |
| `component.bar chart.col1ColorCategory06Strong.width@type=simple, orientation=vertical, breakpoint=mobile`                       | 1 variant at type=simple, orientation=vertical, breakpoint=mobile: 32 where 40                                                                                           | width on /Plot/Bars/Col-1/color=category-06-strong should follow no axis only, but at type=simple, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-1/color=category-06-strong width is meant to change at type=simple, orientation=vertical, breakpoint=mobile.                                                                   |
| `component.bar chart.col2.align@type=grouped, orientation=vertical, breakpoint=desktop`                                          | 1 variant at type=grouped, orientation=vertical, breakpoint=desktop: CENTER/MAX where MAX/CENTER                                                                         | align on /Plot/Bars/Col-2 should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-2 align is meant to change at type=grouped, orientation=vertical, breakpoint=desktop.                                                                                          |
| `component.bar chart.col2.align@type=grouped, orientation=vertical, breakpoint=mobile`                                           | 1 variant at type=grouped, orientation=vertical, breakpoint=mobile: CENTER/MAX where MAX/CENTER                                                                          | align on /Plot/Bars/Col-2 should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-2 align is meant to change at type=grouped, orientation=vertical, breakpoint=mobile.                                                                                           |
| `component.bar chart.col2.direction@type=grouped, orientation=vertical, breakpoint=desktop`                                      | 1 variant at type=grouped, orientation=vertical, breakpoint=desktop: HORIZONTAL where VERTICAL                                                                           | direction on /Plot/Bars/Col-2 should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-2 direction is meant to change at type=grouped, orientation=vertical, breakpoint=desktop.                                                                                      |
| `component.bar chart.col2.direction@type=grouped, orientation=vertical, breakpoint=mobile`                                       | 1 variant at type=grouped, orientation=vertical, breakpoint=mobile: HORIZONTAL where VERTICAL                                                                            | direction on /Plot/Bars/Col-2 should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-2 direction is meant to change at type=grouped, orientation=vertical, breakpoint=mobile.                                                                                       |
| `component.bar chart.col2.gap#unbound`                                                                                           | 0, bound to no variable                                                                                                                                                  | gap on /Plot/Bars/Col-2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-2 gap to inset.none or stack.none, which has the same value.                                                                                                                   |
| `component.bar chart.col2.gap@type=grouped, orientation=vertical, breakpoint=desktop`                                            | 1 variant at type=grouped, orientation=vertical, breakpoint=desktop: inset.2xs where 0                                                                                   | gap on /Plot/Bars/Col-2 should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-2 gap is meant to change at type=grouped, orientation=vertical, breakpoint=desktop.                                                                                            |
| `component.bar chart.col2.gap@type=grouped, orientation=vertical, breakpoint=mobile`                                             | 1 variant at type=grouped, orientation=vertical, breakpoint=mobile: inset.2xs where 0                                                                                    | gap on /Plot/Bars/Col-2 should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-2 gap is meant to change at type=grouped, orientation=vertical, breakpoint=mobile.                                                                                             |
| `component.bar chart.col2.paddingBottom#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Bars/Col-2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-2 paddingBottom to inset.none, which has the same value.                                                                                                                       |
| `component.bar chart.col2.paddingLeft#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Bars/Col-2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-2 paddingLeft to inset.none, which has the same value.                                                                                                                         |
| `component.bar chart.col2.paddingRight#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Bars/Col-2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-2 paddingRight to inset.none, which has the same value.                                                                                                                        |
| `component.bar chart.col2.paddingTop#unbound`                                                                                    | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Bars/Col-2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-2 paddingTop to inset.none, which has the same value.                                                                                                                          |
| `component.bar chart.col2BarStack.gap#unbound`                                                                                   | 2, bound to no variable                                                                                                                                                  | gap on /Plot/Bars/Col-2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no gap token; ask SOLAR for one, or confirm 2 is intended.                                                                                                                                                      |
| `component.bar chart.col2BarStack.height#unbound`                                                                                | 112, 76, bound to no variable                                                                                                                                            | height on /Plot/Bars/Col-2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 112, 76 is intended.                                                                                                                                     |
| `component.bar chart.col2BarStack.height@type=stacked, orientation=vertical, breakpoint=mobile`                                  | 1 variant at type=stacked, orientation=vertical, breakpoint=mobile: 76 where 112                                                                                         | height on /Plot/Bars/Col-2/Bar Stack should follow no axis only, but at type=stacked, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-2/Bar Stack height is meant to change at type=stacked, orientation=vertical, breakpoint=mobile.                                                                                |
| `component.bar chart.col2BarStack.paddingBottom#unbound`                                                                         | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Bars/Col-2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-2/Bar Stack paddingBottom to inset.none, which has the same value.                                                                                                             |
| `component.bar chart.col2BarStack.paddingLeft#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Bars/Col-2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-2/Bar Stack paddingLeft to inset.none, which has the same value.                                                                                                               |
| `component.bar chart.col2BarStack.paddingRight#unbound`                                                                          | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Bars/Col-2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-2/Bar Stack paddingRight to inset.none, which has the same value.                                                                                                              |
| `component.bar chart.col2BarStack.paddingTop#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Bars/Col-2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-2/Bar Stack paddingTop to inset.none, which has the same value.                                                                                                                |
| `component.bar chart.col2BarStack.width#unbound`                                                                                 | 40, 32, bound to no variable                                                                                                                                             | width on /Plot/Bars/Col-2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 40, 32 is intended.                                                                                                                                       |
| `component.bar chart.col2BarStack.width@type=stacked, orientation=vertical, breakpoint=mobile`                                   | 1 variant at type=stacked, orientation=vertical, breakpoint=mobile: 32 where 40                                                                                          | width on /Plot/Bars/Col-2/Bar Stack should follow no axis only, but at type=stacked, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-2/Bar Stack width is meant to change at type=stacked, orientation=vertical, breakpoint=mobile.                                                                                 |
| `component.bar chart.col2BarStackColorFeedbackDangerStrong.height#unbound`                                                       | 54, 36, bound to no variable                                                                                                                                             | height on /Plot/Bars/Col-2/Bar Stack/color=feedback-danger-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 54, 36 is intended.                                                                                                                                      |
| `component.bar chart.col2BarStackColorFeedbackDangerStrong.height@type=stacked, orientation=vertical, breakpoint=mobile`         | 1 variant at type=stacked, orientation=vertical, breakpoint=mobile: 36 where 54                                                                                          | height on /Plot/Bars/Col-2/Bar Stack/color=feedback-danger-strong should follow no axis only, but at type=stacked, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-2/Bar Stack/color=feedback-danger-strong height is meant to change at type=stacked, orientation=vertical, breakpoint=mobile.                                                   |
| `component.bar chart.col2BarStackColorFeedbackNeutralSubtle.height#unbound`                                                      | 22, 14, bound to no variable                                                                                                                                             | height on /Plot/Bars/Col-2/Bar Stack/color=feedback-neutral-subtle is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 22, 14 is intended.                                                                                                                                      |
| `component.bar chart.col2BarStackColorFeedbackNeutralSubtle.height@type=stacked, orientation=vertical, breakpoint=mobile`        | 1 variant at type=stacked, orientation=vertical, breakpoint=mobile: 14 where 22                                                                                          | height on /Plot/Bars/Col-2/Bar Stack/color=feedback-neutral-subtle should follow no axis only, but at type=stacked, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-2/Bar Stack/color=feedback-neutral-subtle height is meant to change at type=stacked, orientation=vertical, breakpoint=mobile.                                                  |
| `component.bar chart.col2BarStackColorFeedbackWarningMedium.height#unbound`                                                      | 32, 22, bound to no variable                                                                                                                                             | height on /Plot/Bars/Col-2/Bar Stack/color=feedback-warning-medium is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-2/Bar Stack/color=feedback-warning-medium height to icon.2xl, which has the same value.                                                                                        |
| `component.bar chart.col2BarStackColorFeedbackWarningMedium.height@type=stacked, orientation=vertical, breakpoint=mobile`        | 1 variant at type=stacked, orientation=vertical, breakpoint=mobile: 22 where 32                                                                                          | height on /Plot/Bars/Col-2/Bar Stack/color=feedback-warning-medium should follow no axis only, but at type=stacked, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-2/Bar Stack/color=feedback-warning-medium height is meant to change at type=stacked, orientation=vertical, breakpoint=mobile.                                                  |
| `component.bar chart.col2ColorCategory02Strong.height#unbound`                                                                   | 151, 95, bound to no variable                                                                                                                                            | height on /Plot/Bars/Col-2/color=category-02-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 151, 95 is intended.                                                                                                                                     |
| `component.bar chart.col2ColorCategory02Strong.height@type=grouped, orientation=vertical, breakpoint=mobile`                     | 1 variant at type=grouped, orientation=vertical, breakpoint=mobile: 95 where 151                                                                                         | height on /Plot/Bars/Col-2/color=category-02-strong should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-2/color=category-02-strong height is meant to change at type=grouped, orientation=vertical, breakpoint=mobile.                                                                 |
| `component.bar chart.col2ColorCategory02Strong.width#unbound`                                                                    | 20, 14, bound to no variable                                                                                                                                             | width on /Plot/Bars/Col-2/color=category-02-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-2/color=category-02-strong width to icon.md, which has the same value.                                                                                                         |
| `component.bar chart.col2ColorCategory02Strong.width@type=grouped, orientation=vertical, breakpoint=mobile`                      | 1 variant at type=grouped, orientation=vertical, breakpoint=mobile: 14 where 20                                                                                          | width on /Plot/Bars/Col-2/color=category-02-strong should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-2/color=category-02-strong width is meant to change at type=grouped, orientation=vertical, breakpoint=mobile.                                                                  |
| `component.bar chart.col2ColorCategory06Strong.height#unbound`                                                                   | 110, 74, 97, 61, bound to no variable                                                                                                                                    | height on /Plot/Bars/Col-2/color=category-06-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 110, 74, 97, 61 is intended.                                                                                                                             |
| `component.bar chart.col2ColorCategory06Strong.height@type=grouped, orientation=vertical, breakpoint=desktop`                    | 1 variant at type=grouped, orientation=vertical, breakpoint=desktop: 97 where 110                                                                                        | height on /Plot/Bars/Col-2/color=category-06-strong should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-2/color=category-06-strong height is meant to change at type=grouped, orientation=vertical, breakpoint=desktop.                                                                |
| `component.bar chart.col2ColorCategory06Strong.height@type=grouped, orientation=vertical, breakpoint=mobile`                     | 1 variant at type=grouped, orientation=vertical, breakpoint=mobile: 61 where 110                                                                                         | height on /Plot/Bars/Col-2/color=category-06-strong should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-2/color=category-06-strong height is meant to change at type=grouped, orientation=vertical, breakpoint=mobile.                                                                 |
| `component.bar chart.col2ColorCategory06Strong.height@type=simple, orientation=vertical, breakpoint=mobile`                      | 1 variant at type=simple, orientation=vertical, breakpoint=mobile: 74 where 110                                                                                          | height on /Plot/Bars/Col-2/color=category-06-strong should follow no axis only, but at type=simple, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-2/color=category-06-strong height is meant to change at type=simple, orientation=vertical, breakpoint=mobile.                                                                  |
| `component.bar chart.col2ColorCategory06Strong.width#unbound`                                                                    | 40, 32, 20, 14, bound to no variable                                                                                                                                     | width on /Plot/Bars/Col-2/color=category-06-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-2/color=category-06-strong width to icon.2xl or icon.md, which has the same value.                                                                                             |
| `component.bar chart.col2ColorCategory06Strong.width@type=grouped, orientation=vertical, breakpoint=desktop`                     | 1 variant at type=grouped, orientation=vertical, breakpoint=desktop: 20 where 40                                                                                         | width on /Plot/Bars/Col-2/color=category-06-strong should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-2/color=category-06-strong width is meant to change at type=grouped, orientation=vertical, breakpoint=desktop.                                                                 |
| `component.bar chart.col2ColorCategory06Strong.width@type=grouped, orientation=vertical, breakpoint=mobile`                      | 1 variant at type=grouped, orientation=vertical, breakpoint=mobile: 14 where 40                                                                                          | width on /Plot/Bars/Col-2/color=category-06-strong should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-2/color=category-06-strong width is meant to change at type=grouped, orientation=vertical, breakpoint=mobile.                                                                  |
| `component.bar chart.col2ColorCategory06Strong.width@type=simple, orientation=vertical, breakpoint=mobile`                       | 1 variant at type=simple, orientation=vertical, breakpoint=mobile: 32 where 40                                                                                           | width on /Plot/Bars/Col-2/color=category-06-strong should follow no axis only, but at type=simple, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-2/color=category-06-strong width is meant to change at type=simple, orientation=vertical, breakpoint=mobile.                                                                   |
| `component.bar chart.col3.align@type=grouped, orientation=vertical, breakpoint=desktop`                                          | 1 variant at type=grouped, orientation=vertical, breakpoint=desktop: CENTER/MAX where MAX/CENTER                                                                         | align on /Plot/Bars/Col-3 should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-3 align is meant to change at type=grouped, orientation=vertical, breakpoint=desktop.                                                                                          |
| `component.bar chart.col3.align@type=grouped, orientation=vertical, breakpoint=mobile`                                           | 1 variant at type=grouped, orientation=vertical, breakpoint=mobile: CENTER/MAX where MAX/CENTER                                                                          | align on /Plot/Bars/Col-3 should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-3 align is meant to change at type=grouped, orientation=vertical, breakpoint=mobile.                                                                                           |
| `component.bar chart.col3.direction@type=grouped, orientation=vertical, breakpoint=desktop`                                      | 1 variant at type=grouped, orientation=vertical, breakpoint=desktop: HORIZONTAL where VERTICAL                                                                           | direction on /Plot/Bars/Col-3 should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-3 direction is meant to change at type=grouped, orientation=vertical, breakpoint=desktop.                                                                                      |
| `component.bar chart.col3.direction@type=grouped, orientation=vertical, breakpoint=mobile`                                       | 1 variant at type=grouped, orientation=vertical, breakpoint=mobile: HORIZONTAL where VERTICAL                                                                            | direction on /Plot/Bars/Col-3 should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-3 direction is meant to change at type=grouped, orientation=vertical, breakpoint=mobile.                                                                                       |
| `component.bar chart.col3.gap#unbound`                                                                                           | 0, bound to no variable                                                                                                                                                  | gap on /Plot/Bars/Col-3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-3 gap to inset.none or stack.none, which has the same value.                                                                                                                   |
| `component.bar chart.col3.gap@type=grouped, orientation=vertical, breakpoint=desktop`                                            | 1 variant at type=grouped, orientation=vertical, breakpoint=desktop: inset.2xs where 0                                                                                   | gap on /Plot/Bars/Col-3 should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-3 gap is meant to change at type=grouped, orientation=vertical, breakpoint=desktop.                                                                                            |
| `component.bar chart.col3.gap@type=grouped, orientation=vertical, breakpoint=mobile`                                             | 1 variant at type=grouped, orientation=vertical, breakpoint=mobile: inset.2xs where 0                                                                                    | gap on /Plot/Bars/Col-3 should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-3 gap is meant to change at type=grouped, orientation=vertical, breakpoint=mobile.                                                                                             |
| `component.bar chart.col3.paddingBottom#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Bars/Col-3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-3 paddingBottom to inset.none, which has the same value.                                                                                                                       |
| `component.bar chart.col3.paddingLeft#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Bars/Col-3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-3 paddingLeft to inset.none, which has the same value.                                                                                                                         |
| `component.bar chart.col3.paddingRight#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Bars/Col-3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-3 paddingRight to inset.none, which has the same value.                                                                                                                        |
| `component.bar chart.col3.paddingTop#unbound`                                                                                    | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Bars/Col-3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-3 paddingTop to inset.none, which has the same value.                                                                                                                          |
| `component.bar chart.col3BarStack.gap#unbound`                                                                                   | 2, bound to no variable                                                                                                                                                  | gap on /Plot/Bars/Col-3/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no gap token; ask SOLAR for one, or confirm 2 is intended.                                                                                                                                                      |
| `component.bar chart.col3BarStack.height#unbound`                                                                                | 223, 151, bound to no variable                                                                                                                                           | height on /Plot/Bars/Col-3/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 223, 151 is intended.                                                                                                                                    |
| `component.bar chart.col3BarStack.height@type=stacked, orientation=vertical, breakpoint=mobile`                                  | 1 variant at type=stacked, orientation=vertical, breakpoint=mobile: 151 where 223                                                                                        | height on /Plot/Bars/Col-3/Bar Stack should follow no axis only, but at type=stacked, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-3/Bar Stack height is meant to change at type=stacked, orientation=vertical, breakpoint=mobile.                                                                                |
| `component.bar chart.col3BarStack.paddingBottom#unbound`                                                                         | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Bars/Col-3/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-3/Bar Stack paddingBottom to inset.none, which has the same value.                                                                                                             |
| `component.bar chart.col3BarStack.paddingLeft#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Bars/Col-3/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-3/Bar Stack paddingLeft to inset.none, which has the same value.                                                                                                               |
| `component.bar chart.col3BarStack.paddingRight#unbound`                                                                          | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Bars/Col-3/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-3/Bar Stack paddingRight to inset.none, which has the same value.                                                                                                              |
| `component.bar chart.col3BarStack.paddingTop#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Bars/Col-3/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-3/Bar Stack paddingTop to inset.none, which has the same value.                                                                                                                |
| `component.bar chart.col3BarStack.width#unbound`                                                                                 | 40, 32, bound to no variable                                                                                                                                             | width on /Plot/Bars/Col-3/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 40, 32 is intended.                                                                                                                                       |
| `component.bar chart.col3BarStack.width@type=stacked, orientation=vertical, breakpoint=mobile`                                   | 1 variant at type=stacked, orientation=vertical, breakpoint=mobile: 32 where 40                                                                                          | width on /Plot/Bars/Col-3/Bar Stack should follow no axis only, but at type=stacked, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-3/Bar Stack width is meant to change at type=stacked, orientation=vertical, breakpoint=mobile.                                                                                 |
| `component.bar chart.col3BarStackColorFeedbackDangerStrong.height#unbound`                                                       | 33, 22, bound to no variable                                                                                                                                             | height on /Plot/Bars/Col-3/Bar Stack/color=feedback-danger-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 33, 22 is intended.                                                                                                                                      |
| `component.bar chart.col3BarStackColorFeedbackDangerStrong.height@type=stacked, orientation=vertical, breakpoint=mobile`         | 1 variant at type=stacked, orientation=vertical, breakpoint=mobile: 22 where 33                                                                                          | height on /Plot/Bars/Col-3/Bar Stack/color=feedback-danger-strong should follow no axis only, but at type=stacked, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-3/Bar Stack/color=feedback-danger-strong height is meant to change at type=stacked, orientation=vertical, breakpoint=mobile.                                                   |
| `component.bar chart.col3BarStackColorFeedbackNeutralSubtle.height#unbound`                                                      | 110, 74, bound to no variable                                                                                                                                            | height on /Plot/Bars/Col-3/Bar Stack/color=feedback-neutral-subtle is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 110, 74 is intended.                                                                                                                                     |
| `component.bar chart.col3BarStackColorFeedbackNeutralSubtle.height@type=stacked, orientation=vertical, breakpoint=mobile`        | 1 variant at type=stacked, orientation=vertical, breakpoint=mobile: 74 where 110                                                                                         | height on /Plot/Bars/Col-3/Bar Stack/color=feedback-neutral-subtle should follow no axis only, but at type=stacked, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-3/Bar Stack/color=feedback-neutral-subtle height is meant to change at type=stacked, orientation=vertical, breakpoint=mobile.                                                  |
| `component.bar chart.col3BarStackColorFeedbackWarningMedium.height#unbound`                                                      | 77, 51, bound to no variable                                                                                                                                             | height on /Plot/Bars/Col-3/Bar Stack/color=feedback-warning-medium is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 77, 51 is intended.                                                                                                                                      |
| `component.bar chart.col3BarStackColorFeedbackWarningMedium.height@type=stacked, orientation=vertical, breakpoint=mobile`        | 1 variant at type=stacked, orientation=vertical, breakpoint=mobile: 51 where 77                                                                                          | height on /Plot/Bars/Col-3/Bar Stack/color=feedback-warning-medium should follow no axis only, but at type=stacked, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-3/Bar Stack/color=feedback-warning-medium height is meant to change at type=stacked, orientation=vertical, breakpoint=mobile.                                                  |
| `component.bar chart.col3ColorCategory02Strong.height#unbound`                                                                   | 108, 68, bound to no variable                                                                                                                                            | height on /Plot/Bars/Col-3/color=category-02-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 108, 68 is intended.                                                                                                                                     |
| `component.bar chart.col3ColorCategory02Strong.height@type=grouped, orientation=vertical, breakpoint=mobile`                     | 1 variant at type=grouped, orientation=vertical, breakpoint=mobile: 68 where 108                                                                                         | height on /Plot/Bars/Col-3/color=category-02-strong should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-3/color=category-02-strong height is meant to change at type=grouped, orientation=vertical, breakpoint=mobile.                                                                 |
| `component.bar chart.col3ColorCategory02Strong.width#unbound`                                                                    | 20, 14, bound to no variable                                                                                                                                             | width on /Plot/Bars/Col-3/color=category-02-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-3/color=category-02-strong width to icon.md, which has the same value.                                                                                                         |
| `component.bar chart.col3ColorCategory02Strong.width@type=grouped, orientation=vertical, breakpoint=mobile`                      | 1 variant at type=grouped, orientation=vertical, breakpoint=mobile: 14 where 20                                                                                          | width on /Plot/Bars/Col-3/color=category-02-strong should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-3/color=category-02-strong width is meant to change at type=grouped, orientation=vertical, breakpoint=mobile.                                                                  |
| `component.bar chart.col3ColorCategory06Strong.height#unbound`                                                                   | 220, 148, 194, 122, bound to no variable                                                                                                                                 | height on /Plot/Bars/Col-3/color=category-06-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 220, 148, 194, 122 is intended.                                                                                                                          |
| `component.bar chart.col3ColorCategory06Strong.height@type=grouped, orientation=vertical, breakpoint=desktop`                    | 1 variant at type=grouped, orientation=vertical, breakpoint=desktop: 194 where 220                                                                                       | height on /Plot/Bars/Col-3/color=category-06-strong should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-3/color=category-06-strong height is meant to change at type=grouped, orientation=vertical, breakpoint=desktop.                                                                |
| `component.bar chart.col3ColorCategory06Strong.height@type=grouped, orientation=vertical, breakpoint=mobile`                     | 1 variant at type=grouped, orientation=vertical, breakpoint=mobile: 122 where 220                                                                                        | height on /Plot/Bars/Col-3/color=category-06-strong should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-3/color=category-06-strong height is meant to change at type=grouped, orientation=vertical, breakpoint=mobile.                                                                 |
| `component.bar chart.col3ColorCategory06Strong.height@type=simple, orientation=vertical, breakpoint=mobile`                      | 1 variant at type=simple, orientation=vertical, breakpoint=mobile: 148 where 220                                                                                         | height on /Plot/Bars/Col-3/color=category-06-strong should follow no axis only, but at type=simple, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-3/color=category-06-strong height is meant to change at type=simple, orientation=vertical, breakpoint=mobile.                                                                  |
| `component.bar chart.col3ColorCategory06Strong.width#unbound`                                                                    | 40, 32, 20, 14, bound to no variable                                                                                                                                     | width on /Plot/Bars/Col-3/color=category-06-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-3/color=category-06-strong width to icon.2xl or icon.md, which has the same value.                                                                                             |
| `component.bar chart.col3ColorCategory06Strong.width@type=grouped, orientation=vertical, breakpoint=desktop`                     | 1 variant at type=grouped, orientation=vertical, breakpoint=desktop: 20 where 40                                                                                         | width on /Plot/Bars/Col-3/color=category-06-strong should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-3/color=category-06-strong width is meant to change at type=grouped, orientation=vertical, breakpoint=desktop.                                                                 |
| `component.bar chart.col3ColorCategory06Strong.width@type=grouped, orientation=vertical, breakpoint=mobile`                      | 1 variant at type=grouped, orientation=vertical, breakpoint=mobile: 14 where 40                                                                                          | width on /Plot/Bars/Col-3/color=category-06-strong should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-3/color=category-06-strong width is meant to change at type=grouped, orientation=vertical, breakpoint=mobile.                                                                  |
| `component.bar chart.col3ColorCategory06Strong.width@type=simple, orientation=vertical, breakpoint=mobile`                       | 1 variant at type=simple, orientation=vertical, breakpoint=mobile: 32 where 40                                                                                           | width on /Plot/Bars/Col-3/color=category-06-strong should follow no axis only, but at type=simple, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-3/color=category-06-strong width is meant to change at type=simple, orientation=vertical, breakpoint=mobile.                                                                   |
| `component.bar chart.col4.align@type=grouped, orientation=vertical, breakpoint=desktop`                                          | 1 variant at type=grouped, orientation=vertical, breakpoint=desktop: CENTER/MAX where MAX/CENTER                                                                         | align on /Plot/Bars/Col-4 should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-4 align is meant to change at type=grouped, orientation=vertical, breakpoint=desktop.                                                                                          |
| `component.bar chart.col4.direction@type=grouped, orientation=vertical, breakpoint=desktop`                                      | 1 variant at type=grouped, orientation=vertical, breakpoint=desktop: HORIZONTAL where VERTICAL                                                                           | direction on /Plot/Bars/Col-4 should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-4 direction is meant to change at type=grouped, orientation=vertical, breakpoint=desktop.                                                                                      |
| `component.bar chart.col4.gap#unbound`                                                                                           | 0, bound to no variable                                                                                                                                                  | gap on /Plot/Bars/Col-4 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-4 gap to inset.none or stack.none, which has the same value.                                                                                                                   |
| `component.bar chart.col4.gap@type=grouped, orientation=vertical, breakpoint=desktop`                                            | 1 variant at type=grouped, orientation=vertical, breakpoint=desktop: inset.2xs where 0                                                                                   | gap on /Plot/Bars/Col-4 should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-4 gap is meant to change at type=grouped, orientation=vertical, breakpoint=desktop.                                                                                            |
| `component.bar chart.col4.paddingBottom#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Bars/Col-4 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-4 paddingBottom to inset.none, which has the same value.                                                                                                                       |
| `component.bar chart.col4.paddingLeft#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Bars/Col-4 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-4 paddingLeft to inset.none, which has the same value.                                                                                                                         |
| `component.bar chart.col4.paddingRight#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Bars/Col-4 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-4 paddingRight to inset.none, which has the same value.                                                                                                                        |
| `component.bar chart.col4.paddingTop#unbound`                                                                                    | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Bars/Col-4 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-4 paddingTop to inset.none, which has the same value.                                                                                                                          |
| `component.bar chart.col4BarStack.gap#unbound`                                                                                   | 2, bound to no variable                                                                                                                                                  | gap on /Plot/Bars/Col-4/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no gap token; ask SOLAR for one, or confirm 2 is intended.                                                                                                                                                      |
| `component.bar chart.col4BarStack.height#unbound`                                                                                | 166, bound to no variable                                                                                                                                                | height on /Plot/Bars/Col-4/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 166 is intended.                                                                                                                                         |
| `component.bar chart.col4BarStack.paddingBottom#unbound`                                                                         | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Bars/Col-4/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-4/Bar Stack paddingBottom to inset.none, which has the same value.                                                                                                             |
| `component.bar chart.col4BarStack.paddingLeft#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Bars/Col-4/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-4/Bar Stack paddingLeft to inset.none, which has the same value.                                                                                                               |
| `component.bar chart.col4BarStack.paddingRight#unbound`                                                                          | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Bars/Col-4/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-4/Bar Stack paddingRight to inset.none, which has the same value.                                                                                                              |
| `component.bar chart.col4BarStack.paddingTop#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Bars/Col-4/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-4/Bar Stack paddingTop to inset.none, which has the same value.                                                                                                                |
| `component.bar chart.col4BarStack.width#unbound`                                                                                 | 40, bound to no variable                                                                                                                                                 | width on /Plot/Bars/Col-4/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                           |
| `component.bar chart.col4BarStackColorFeedbackDangerStrong.height#unbound`                                                       | 41, bound to no variable                                                                                                                                                 | height on /Plot/Bars/Col-4/Bar Stack/color=feedback-danger-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 41 is intended.                                                                                                                                          |
| `component.bar chart.col4BarStackColorFeedbackNeutralSubtle.height#unbound`                                                      | 81, bound to no variable                                                                                                                                                 | height on /Plot/Bars/Col-4/Bar Stack/color=feedback-neutral-subtle is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 81 is intended.                                                                                                                                          |
| `component.bar chart.col4BarStackColorFeedbackWarningMedium.height#unbound`                                                      | 41, bound to no variable                                                                                                                                                 | height on /Plot/Bars/Col-4/Bar Stack/color=feedback-warning-medium is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 41 is intended.                                                                                                                                          |
| `component.bar chart.col4ColorCategory02Strong.height#unbound`                                                                   | 173, bound to no variable                                                                                                                                                | height on /Plot/Bars/Col-4/color=category-02-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 173 is intended.                                                                                                                                         |
| `component.bar chart.col4ColorCategory02Strong.width#unbound`                                                                    | 20, bound to no variable                                                                                                                                                 | width on /Plot/Bars/Col-4/color=category-02-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-4/color=category-02-strong width to icon.md, which has the same value.                                                                                                         |
| `component.bar chart.col4ColorCategory06Strong.height#unbound`                                                                   | 163, 145, bound to no variable                                                                                                                                           | height on /Plot/Bars/Col-4/color=category-06-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 163, 145 is intended.                                                                                                                                    |
| `component.bar chart.col4ColorCategory06Strong.height@type=grouped, orientation=vertical, breakpoint=desktop`                    | 1 variant at type=grouped, orientation=vertical, breakpoint=desktop: 145 where 163                                                                                       | height on /Plot/Bars/Col-4/color=category-06-strong should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-4/color=category-06-strong height is meant to change at type=grouped, orientation=vertical, breakpoint=desktop.                                                                |
| `component.bar chart.col4ColorCategory06Strong.width#unbound`                                                                    | 40, 20, bound to no variable                                                                                                                                             | width on /Plot/Bars/Col-4/color=category-06-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-4/color=category-06-strong width to icon.md, which has the same value.                                                                                                         |
| `component.bar chart.col4ColorCategory06Strong.width@type=grouped, orientation=vertical, breakpoint=desktop`                     | 1 variant at type=grouped, orientation=vertical, breakpoint=desktop: 20 where 40                                                                                         | width on /Plot/Bars/Col-4/color=category-06-strong should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-4/color=category-06-strong width is meant to change at type=grouped, orientation=vertical, breakpoint=desktop.                                                                 |
| `component.bar chart.col5.align@type=grouped, orientation=vertical, breakpoint=desktop`                                          | 1 variant at type=grouped, orientation=vertical, breakpoint=desktop: CENTER/MAX where MAX/CENTER                                                                         | align on /Plot/Bars/Col-5 should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-5 align is meant to change at type=grouped, orientation=vertical, breakpoint=desktop.                                                                                          |
| `component.bar chart.col5.direction@type=grouped, orientation=vertical, breakpoint=desktop`                                      | 1 variant at type=grouped, orientation=vertical, breakpoint=desktop: HORIZONTAL where VERTICAL                                                                           | direction on /Plot/Bars/Col-5 should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-5 direction is meant to change at type=grouped, orientation=vertical, breakpoint=desktop.                                                                                      |
| `component.bar chart.col5.gap#unbound`                                                                                           | 0, bound to no variable                                                                                                                                                  | gap on /Plot/Bars/Col-5 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-5 gap to inset.none or stack.none, which has the same value.                                                                                                                   |
| `component.bar chart.col5.gap@type=grouped, orientation=vertical, breakpoint=desktop`                                            | 1 variant at type=grouped, orientation=vertical, breakpoint=desktop: inset.2xs where 0                                                                                   | gap on /Plot/Bars/Col-5 should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-5 gap is meant to change at type=grouped, orientation=vertical, breakpoint=desktop.                                                                                            |
| `component.bar chart.col5.paddingBottom#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Bars/Col-5 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-5 paddingBottom to inset.none, which has the same value.                                                                                                                       |
| `component.bar chart.col5.paddingLeft#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Bars/Col-5 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-5 paddingLeft to inset.none, which has the same value.                                                                                                                         |
| `component.bar chart.col5.paddingRight#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Bars/Col-5 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-5 paddingRight to inset.none, which has the same value.                                                                                                                        |
| `component.bar chart.col5.paddingTop#unbound`                                                                                    | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Bars/Col-5 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-5 paddingTop to inset.none, which has the same value.                                                                                                                          |
| `component.bar chart.col5BarStack.gap#unbound`                                                                                   | 2, bound to no variable                                                                                                                                                  | gap on /Plot/Bars/Col-5/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no gap token; ask SOLAR for one, or confirm 2 is intended.                                                                                                                                                      |
| `component.bar chart.col5BarStack.height#unbound`                                                                                | 136, bound to no variable                                                                                                                                                | height on /Plot/Bars/Col-5/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 136 is intended.                                                                                                                                         |
| `component.bar chart.col5BarStack.paddingBottom#unbound`                                                                         | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Bars/Col-5/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-5/Bar Stack paddingBottom to inset.none, which has the same value.                                                                                                             |
| `component.bar chart.col5BarStack.paddingLeft#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Bars/Col-5/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-5/Bar Stack paddingLeft to inset.none, which has the same value.                                                                                                               |
| `component.bar chart.col5BarStack.paddingRight#unbound`                                                                          | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Bars/Col-5/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-5/Bar Stack paddingRight to inset.none, which has the same value.                                                                                                              |
| `component.bar chart.col5BarStack.paddingTop#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Bars/Col-5/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-5/Bar Stack paddingTop to inset.none, which has the same value.                                                                                                                |
| `component.bar chart.col5BarStack.width#unbound`                                                                                 | 40, bound to no variable                                                                                                                                                 | width on /Plot/Bars/Col-5/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                           |
| `component.bar chart.col5BarStackColorFeedbackDangerStrong.height#unbound`                                                       | 53, bound to no variable                                                                                                                                                 | height on /Plot/Bars/Col-5/Bar Stack/color=feedback-danger-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 53 is intended.                                                                                                                                          |
| `component.bar chart.col5BarStackColorFeedbackNeutralSubtle.height#unbound`                                                      | 40, bound to no variable                                                                                                                                                 | height on /Plot/Bars/Col-5/Bar Stack/color=feedback-neutral-subtle is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                          |
| `component.bar chart.col5BarStackColorFeedbackWarningMedium.height#unbound`                                                      | 40, bound to no variable                                                                                                                                                 | height on /Plot/Bars/Col-5/Bar Stack/color=feedback-warning-medium is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                          |
| `component.bar chart.col5ColorCategory02Strong.height#unbound`                                                                   | 76, bound to no variable                                                                                                                                                 | height on /Plot/Bars/Col-5/color=category-02-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 76 is intended.                                                                                                                                          |
| `component.bar chart.col5ColorCategory02Strong.width#unbound`                                                                    | 20, bound to no variable                                                                                                                                                 | width on /Plot/Bars/Col-5/color=category-02-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-5/color=category-02-strong width to icon.md, which has the same value.                                                                                                         |
| `component.bar chart.col5ColorCategory06Strong.height#unbound`                                                                   | 134, 119, bound to no variable                                                                                                                                           | height on /Plot/Bars/Col-5/color=category-06-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 134, 119 is intended.                                                                                                                                    |
| `component.bar chart.col5ColorCategory06Strong.height@type=grouped, orientation=vertical, breakpoint=desktop`                    | 1 variant at type=grouped, orientation=vertical, breakpoint=desktop: 119 where 134                                                                                       | height on /Plot/Bars/Col-5/color=category-06-strong should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-5/color=category-06-strong height is meant to change at type=grouped, orientation=vertical, breakpoint=desktop.                                                                |
| `component.bar chart.col5ColorCategory06Strong.width#unbound`                                                                    | 40, 20, bound to no variable                                                                                                                                             | width on /Plot/Bars/Col-5/color=category-06-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Bars/Col-5/color=category-06-strong width to icon.md, which has the same value.                                                                                                         |
| `component.bar chart.col5ColorCategory06Strong.width@type=grouped, orientation=vertical, breakpoint=desktop`                     | 1 variant at type=grouped, orientation=vertical, breakpoint=desktop: 20 where 40                                                                                         | width on /Plot/Bars/Col-5/color=category-06-strong should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Bars/Col-5/color=category-06-strong width is meant to change at type=grouped, orientation=vertical, breakpoint=desktop.                                                                 |
| `component.bar chart.labelCell.gap#unbound`                                                                                      | 0, bound to no variable                                                                                                                                                  | gap on /Plot/Labels/LabelCell is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/LabelCell gap to inset.none or stack.none, which has the same value.                                                                                                             |
| `component.bar chart.labelCell.paddingBottom#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Labels/LabelCell is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/LabelCell paddingBottom to inset.none, which has the same value.                                                                                                                 |
| `component.bar chart.labelCell.paddingLeft#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Labels/LabelCell is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/LabelCell paddingLeft to inset.none, which has the same value.                                                                                                                   |
| `component.bar chart.labelCell.paddingRight#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Labels/LabelCell is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/LabelCell paddingRight to inset.none, which has the same value.                                                                                                                  |
| `component.bar chart.labelCell.paddingTop#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Labels/LabelCell is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/LabelCell paddingTop to inset.none, which has the same value.                                                                                                                    |
| `component.bar chart.labelCell2.gap#unbound`                                                                                     | 0, bound to no variable                                                                                                                                                  | gap on /Plot/Labels/LabelCell#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/LabelCell#2 gap to inset.none or stack.none, which has the same value.                                                                                                           |
| `component.bar chart.labelCell2.paddingBottom#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Labels/LabelCell#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/LabelCell#2 paddingBottom to inset.none, which has the same value.                                                                                                               |
| `component.bar chart.labelCell2.paddingLeft#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Labels/LabelCell#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/LabelCell#2 paddingLeft to inset.none, which has the same value.                                                                                                                 |
| `component.bar chart.labelCell2.paddingRight#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Labels/LabelCell#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/LabelCell#2 paddingRight to inset.none, which has the same value.                                                                                                                |
| `component.bar chart.labelCell2.paddingTop#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Labels/LabelCell#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/LabelCell#2 paddingTop to inset.none, which has the same value.                                                                                                                  |
| `component.bar chart.labelCell3.gap#unbound`                                                                                     | 0, bound to no variable                                                                                                                                                  | gap on /Plot/Labels/LabelCell#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/LabelCell#3 gap to inset.none or stack.none, which has the same value.                                                                                                           |
| `component.bar chart.labelCell3.paddingBottom#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Labels/LabelCell#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/LabelCell#3 paddingBottom to inset.none, which has the same value.                                                                                                               |
| `component.bar chart.labelCell3.paddingLeft#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Labels/LabelCell#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/LabelCell#3 paddingLeft to inset.none, which has the same value.                                                                                                                 |
| `component.bar chart.labelCell3.paddingRight#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Labels/LabelCell#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/LabelCell#3 paddingRight to inset.none, which has the same value.                                                                                                                |
| `component.bar chart.labelCell3.paddingTop#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Labels/LabelCell#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/LabelCell#3 paddingTop to inset.none, which has the same value.                                                                                                                  |
| `component.bar chart.labelCell4.gap#unbound`                                                                                     | 0, bound to no variable                                                                                                                                                  | gap on /Plot/Labels/LabelCell#4 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/LabelCell#4 gap to inset.none or stack.none, which has the same value.                                                                                                           |
| `component.bar chart.labelCell4.paddingBottom#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Labels/LabelCell#4 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/LabelCell#4 paddingBottom to inset.none, which has the same value.                                                                                                               |
| `component.bar chart.labelCell4.paddingLeft#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Labels/LabelCell#4 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/LabelCell#4 paddingLeft to inset.none, which has the same value.                                                                                                                 |
| `component.bar chart.labelCell4.paddingRight#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Labels/LabelCell#4 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/LabelCell#4 paddingRight to inset.none, which has the same value.                                                                                                                |
| `component.bar chart.labelCell4.paddingTop#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Labels/LabelCell#4 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/LabelCell#4 paddingTop to inset.none, which has the same value.                                                                                                                  |
| `component.bar chart.labelCell5.gap#unbound`                                                                                     | 0, bound to no variable                                                                                                                                                  | gap on /Plot/Labels/LabelCell#5 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/LabelCell#5 gap to inset.none or stack.none, which has the same value.                                                                                                           |
| `component.bar chart.labelCell5.paddingBottom#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Labels/LabelCell#5 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/LabelCell#5 paddingBottom to inset.none, which has the same value.                                                                                                               |
| `component.bar chart.labelCell5.paddingLeft#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Labels/LabelCell#5 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/LabelCell#5 paddingLeft to inset.none, which has the same value.                                                                                                                 |
| `component.bar chart.labelCell5.paddingRight#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Labels/LabelCell#5 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/LabelCell#5 paddingRight to inset.none, which has the same value.                                                                                                                |
| `component.bar chart.labelCell5.paddingTop#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Labels/LabelCell#5 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/LabelCell#5 paddingTop to inset.none, which has the same value.                                                                                                                  |
| `component.bar chart.labelCell6.gap#unbound`                                                                                     | 0, bound to no variable                                                                                                                                                  | gap on /Plot/Labels/LabelCell#6 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/LabelCell#6 gap to inset.none or stack.none, which has the same value.                                                                                                           |
| `component.bar chart.labelCell6.paddingBottom#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Labels/LabelCell#6 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/LabelCell#6 paddingBottom to inset.none, which has the same value.                                                                                                               |
| `component.bar chart.labelCell6.paddingLeft#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Labels/LabelCell#6 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/LabelCell#6 paddingLeft to inset.none, which has the same value.                                                                                                                 |
| `component.bar chart.labelCell6.paddingRight#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Labels/LabelCell#6 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/LabelCell#6 paddingRight to inset.none, which has the same value.                                                                                                                |
| `component.bar chart.labelCell6.paddingTop#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Labels/LabelCell#6 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/LabelCell#6 paddingTop to inset.none, which has the same value.                                                                                                                  |
| `component.bar chart.labels.gap#unbound`                                                                                         | 10, bound to no variable                                                                                                                                                 | gap on /Plot/Labels is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no gap token; ask SOLAR for one, or confirm 10 is intended.                                                                                                                                                     |
| `component.bar chart.labels.gap@type=grouped, orientation=vertical, breakpoint=desktop`                                          | 1 variant at type=grouped, orientation=vertical, breakpoint=desktop: inset.xl where inset.md                                                                             | gap on /Plot/Labels should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Labels gap is meant to change at type=grouped, orientation=vertical, breakpoint=desktop.                                                                                                |
| `component.bar chart.labels.gap@type=grouped, orientation=vertical, breakpoint=mobile`                                           | 1 variant at type=grouped, orientation=vertical, breakpoint=mobile: inset.sm where inset.md                                                                              | gap on /Plot/Labels should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Labels gap is meant to change at type=grouped, orientation=vertical, breakpoint=mobile.                                                                                                 |
| `component.bar chart.labels.gap@type=simple, orientation=vertical, breakpoint=mobile`                                            | 1 variant at type=simple, orientation=vertical, breakpoint=mobile: 10 where inset.md                                                                                     | gap on /Plot/Labels should follow no axis only, but at type=simple, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Labels gap is meant to change at type=simple, orientation=vertical, breakpoint=mobile.                                                                                                  |
| `component.bar chart.labels.gap@type=stacked, orientation=vertical, breakpoint=mobile`                                           | 1 variant at type=stacked, orientation=vertical, breakpoint=mobile: 10 where inset.md                                                                                    | gap on /Plot/Labels should follow no axis only, but at type=stacked, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Labels gap is meant to change at type=stacked, orientation=vertical, breakpoint=mobile.                                                                                                 |
| `component.bar chart.labels.paddingBottom#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Labels is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels paddingBottom to inset.none, which has the same value.                                                                                                                           |
| `component.bar chart.labels.paddingLeft#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Labels is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels paddingLeft to inset.none, which has the same value.                                                                                                                             |
| `component.bar chart.labels.paddingRight#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Labels is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels paddingRight to inset.none, which has the same value.                                                                                                                            |
| `component.bar chart.labels.paddingTop#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Labels is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels paddingTop to inset.none, which has the same value.                                                                                                                              |
| `component.bar chart.labelsFrame.gap#unbound`                                                                                    | 0, bound to no variable                                                                                                                                                  | gap on /Plot/Labels/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/Frame gap to inset.none or stack.none, which has the same value.                                                                                                                 |
| `component.bar chart.labelsFrame.paddingBottom#unbound`                                                                          | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Labels/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/Frame paddingBottom to inset.none, which has the same value.                                                                                                                     |
| `component.bar chart.labelsFrame.paddingLeft#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Labels/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/Frame paddingLeft to inset.none, which has the same value.                                                                                                                       |
| `component.bar chart.labelsFrame.paddingRight#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Labels/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/Frame paddingRight to inset.none, which has the same value.                                                                                                                      |
| `component.bar chart.labelsFrame.paddingTop#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Labels/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/Frame paddingTop to inset.none, which has the same value.                                                                                                                        |
| `component.bar chart.labelsFrame2.gap#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | gap on /Plot/Labels/Frame#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/Frame#2 gap to inset.none or stack.none, which has the same value.                                                                                                               |
| `component.bar chart.labelsFrame2.paddingBottom#unbound`                                                                         | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Labels/Frame#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/Frame#2 paddingBottom to inset.none, which has the same value.                                                                                                                   |
| `component.bar chart.labelsFrame2.paddingLeft#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Labels/Frame#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/Frame#2 paddingLeft to inset.none, which has the same value.                                                                                                                     |
| `component.bar chart.labelsFrame2.paddingRight#unbound`                                                                          | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Labels/Frame#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/Frame#2 paddingRight to inset.none, which has the same value.                                                                                                                    |
| `component.bar chart.labelsFrame2.paddingTop#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Labels/Frame#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/Frame#2 paddingTop to inset.none, which has the same value.                                                                                                                      |
| `component.bar chart.labelsFrame3.gap#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | gap on /Plot/Labels/Frame#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/Frame#3 gap to inset.none or stack.none, which has the same value.                                                                                                               |
| `component.bar chart.labelsFrame3.paddingBottom#unbound`                                                                         | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Labels/Frame#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/Frame#3 paddingBottom to inset.none, which has the same value.                                                                                                                   |
| `component.bar chart.labelsFrame3.paddingLeft#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Labels/Frame#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/Frame#3 paddingLeft to inset.none, which has the same value.                                                                                                                     |
| `component.bar chart.labelsFrame3.paddingRight#unbound`                                                                          | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Labels/Frame#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/Frame#3 paddingRight to inset.none, which has the same value.                                                                                                                    |
| `component.bar chart.labelsFrame3.paddingTop#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Labels/Frame#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/Frame#3 paddingTop to inset.none, which has the same value.                                                                                                                      |
| `component.bar chart.labelsFrame4.gap#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | gap on /Plot/Labels/Frame#4 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/Frame#4 gap to inset.none or stack.none, which has the same value.                                                                                                               |
| `component.bar chart.labelsFrame4.paddingBottom#unbound`                                                                         | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Labels/Frame#4 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/Frame#4 paddingBottom to inset.none, which has the same value.                                                                                                                   |
| `component.bar chart.labelsFrame4.paddingLeft#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Labels/Frame#4 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/Frame#4 paddingLeft to inset.none, which has the same value.                                                                                                                     |
| `component.bar chart.labelsFrame4.paddingRight#unbound`                                                                          | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Labels/Frame#4 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/Frame#4 paddingRight to inset.none, which has the same value.                                                                                                                    |
| `component.bar chart.labelsFrame4.paddingTop#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Labels/Frame#4 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/Frame#4 paddingTop to inset.none, which has the same value.                                                                                                                      |
| `component.bar chart.labelsFrame5.gap#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | gap on /Plot/Labels/Frame#5 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/Frame#5 gap to inset.none or stack.none, which has the same value.                                                                                                               |
| `component.bar chart.labelsFrame5.paddingBottom#unbound`                                                                         | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Labels/Frame#5 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/Frame#5 paddingBottom to inset.none, which has the same value.                                                                                                                   |
| `component.bar chart.labelsFrame5.paddingLeft#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Labels/Frame#5 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/Frame#5 paddingLeft to inset.none, which has the same value.                                                                                                                     |
| `component.bar chart.labelsFrame5.paddingRight#unbound`                                                                          | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Labels/Frame#5 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/Frame#5 paddingRight to inset.none, which has the same value.                                                                                                                    |
| `component.bar chart.labelsFrame5.paddingTop#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Labels/Frame#5 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/Frame#5 paddingTop to inset.none, which has the same value.                                                                                                                      |
| `component.bar chart.labelsFrame6.gap#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | gap on /Plot/Labels/Frame#6 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/Frame#6 gap to inset.none or stack.none, which has the same value.                                                                                                               |
| `component.bar chart.labelsFrame6.paddingBottom#unbound`                                                                         | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Labels/Frame#6 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/Frame#6 paddingBottom to inset.none, which has the same value.                                                                                                                   |
| `component.bar chart.labelsFrame6.paddingLeft#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Labels/Frame#6 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/Frame#6 paddingLeft to inset.none, which has the same value.                                                                                                                     |
| `component.bar chart.labelsFrame6.paddingRight#unbound`                                                                          | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Labels/Frame#6 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/Frame#6 paddingRight to inset.none, which has the same value.                                                                                                                    |
| `component.bar chart.labelsFrame6.paddingTop#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Labels/Frame#6 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Labels/Frame#6 paddingTop to inset.none, which has the same value.                                                                                                                      |
| `component.bar chart.legend.paddingBottom#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Legend is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Legend paddingBottom to inset.none, which has the same value.                                                                                                                                |
| `component.bar chart.legend.paddingLeft#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Legend is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Legend paddingLeft to inset.none, which has the same value.                                                                                                                                  |
| `component.bar chart.legend.paddingRight#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingRight on /Legend is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Legend paddingRight to inset.none, which has the same value.                                                                                                                                 |
| `component.bar chart.legend.paddingTop#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingTop on /Legend is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Legend paddingTop to inset.none, which has the same value.                                                                                                                                   |
| `component.bar chart.legendItem.gap#unbound`                                                                                     | 6, bound to no variable                                                                                                                                                  | gap on /Legend/LegendItem is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no gap token; ask SOLAR for one, or confirm 6 is intended.                                                                                                                                                      |
| `component.bar chart.legendItem.paddingBottom#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Legend/LegendItem is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Legend/LegendItem paddingBottom to inset.none, which has the same value.                                                                                                                     |
| `component.bar chart.legendItem.paddingLeft#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Legend/LegendItem is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Legend/LegendItem paddingLeft to inset.none, which has the same value.                                                                                                                       |
| `component.bar chart.legendItem.paddingRight#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingRight on /Legend/LegendItem is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Legend/LegendItem paddingRight to inset.none, which has the same value.                                                                                                                      |
| `component.bar chart.legendItem.paddingTop#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingTop on /Legend/LegendItem is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Legend/LegendItem paddingTop to inset.none, which has the same value.                                                                                                                        |
| `component.bar chart.legendItem2.gap#unbound`                                                                                    | 6, bound to no variable                                                                                                                                                  | gap on /Legend/LegendItem#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no gap token; ask SOLAR for one, or confirm 6 is intended.                                                                                                                                                      |
| `component.bar chart.legendItem2.paddingBottom#unbound`                                                                          | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Legend/LegendItem#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Legend/LegendItem#2 paddingBottom to inset.none, which has the same value.                                                                                                                   |
| `component.bar chart.legendItem2.paddingLeft#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Legend/LegendItem#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Legend/LegendItem#2 paddingLeft to inset.none, which has the same value.                                                                                                                     |
| `component.bar chart.legendItem2.paddingRight#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingRight on /Legend/LegendItem#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Legend/LegendItem#2 paddingRight to inset.none, which has the same value.                                                                                                                    |
| `component.bar chart.legendItem2.paddingTop#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingTop on /Legend/LegendItem#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Legend/LegendItem#2 paddingTop to inset.none, which has the same value.                                                                                                                      |
| `component.bar chart.legendItem2ColorCategory02Strong.height#unbound`                                                            | 10, bound to no variable                                                                                                                                                 | height on /Legend/LegendItem#2/color=category-02-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 10 is intended.                                                                                                                                          |
| `component.bar chart.legendItem2ColorCategory02Strong.width#unbound`                                                             | 10, bound to no variable                                                                                                                                                 | width on /Legend/LegendItem#2/color=category-02-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 10 is intended.                                                                                                                                           |
| `component.bar chart.legendItem2ColorFeedbackWarningMedium.height#unbound`                                                       | 10, bound to no variable                                                                                                                                                 | height on /Legend/LegendItem#2/color=feedback-warning-medium is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 10 is intended.                                                                                                                                          |
| `component.bar chart.legendItem2ColorFeedbackWarningMedium.width#unbound`                                                        | 10, bound to no variable                                                                                                                                                 | width on /Legend/LegendItem#2/color=feedback-warning-medium is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 10 is intended.                                                                                                                                           |
| `component.bar chart.legendItem3.gap#unbound`                                                                                    | 6, bound to no variable                                                                                                                                                  | gap on /Legend/LegendItem#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no gap token; ask SOLAR for one, or confirm 6 is intended.                                                                                                                                                      |
| `component.bar chart.legendItem3.paddingBottom#unbound`                                                                          | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Legend/LegendItem#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Legend/LegendItem#3 paddingBottom to inset.none, which has the same value.                                                                                                                   |
| `component.bar chart.legendItem3.paddingLeft#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Legend/LegendItem#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Legend/LegendItem#3 paddingLeft to inset.none, which has the same value.                                                                                                                     |
| `component.bar chart.legendItem3.paddingRight#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingRight on /Legend/LegendItem#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Legend/LegendItem#3 paddingRight to inset.none, which has the same value.                                                                                                                    |
| `component.bar chart.legendItem3.paddingTop#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingTop on /Legend/LegendItem#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Legend/LegendItem#3 paddingTop to inset.none, which has the same value.                                                                                                                      |
| `component.bar chart.legendItem3ColorFeedbackNeutralSubtle.height#unbound`                                                       | 10, bound to no variable                                                                                                                                                 | height on /Legend/LegendItem#3/color=feedback-neutral-subtle is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 10 is intended.                                                                                                                                          |
| `component.bar chart.legendItem3ColorFeedbackNeutralSubtle.width#unbound`                                                        | 10, bound to no variable                                                                                                                                                 | width on /Legend/LegendItem#3/color=feedback-neutral-subtle is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 10 is intended.                                                                                                                                           |
| `component.bar chart.legendItemColorCategory06Strong.height#unbound`                                                             | 10, bound to no variable                                                                                                                                                 | height on /Legend/LegendItem/color=category-06-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 10 is intended.                                                                                                                                          |
| `component.bar chart.legendItemColorCategory06Strong.width#unbound`                                                              | 10, bound to no variable                                                                                                                                                 | width on /Legend/LegendItem/color=category-06-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 10 is intended.                                                                                                                                           |
| `component.bar chart.legendItemColorFeedbackDangerStrong.height#unbound`                                                         | 10, bound to no variable                                                                                                                                                 | height on /Legend/LegendItem/color=feedback-danger-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 10 is intended.                                                                                                                                          |
| `component.bar chart.legendItemColorFeedbackDangerStrong.width#unbound`                                                          | 10, bound to no variable                                                                                                                                                 | width on /Legend/LegendItem/color=feedback-danger-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 10 is intended.                                                                                                                                           |
| `component.bar chart.plot.gap@type=grouped, orientation=horizontal, breakpoint=desktop`                                          | 1 variant at type=grouped, orientation=horizontal, breakpoint=desktop: inset.sm where inset.xs                                                                           | gap on /Plot should follow no axis only, but at type=grouped, orientation=horizontal, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot gap is meant to change at type=grouped, orientation=horizontal, breakpoint=desktop.                                                                                                     |
| `component.bar chart.plot.gap@type=grouped, orientation=horizontal, breakpoint=mobile`                                           | 1 variant at type=grouped, orientation=horizontal, breakpoint=mobile: inset.sm where inset.xs                                                                            | gap on /Plot should follow no axis only, but at type=grouped, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot gap is meant to change at type=grouped, orientation=horizontal, breakpoint=mobile.                                                                                                      |
| `component.bar chart.plot.paddingBottom#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot paddingBottom to inset.none, which has the same value.                                                                                                                                  |
| `component.bar chart.plot.paddingLeft#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot paddingLeft to inset.none, which has the same value.                                                                                                                                    |
| `component.bar chart.plot.paddingRight#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot paddingRight to inset.none, which has the same value.                                                                                                                                   |
| `component.bar chart.plot.paddingTop#unbound`                                                                                    | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot paddingTop to inset.none, which has the same value.                                                                                                                                     |
| `component.bar chart.root.height#unbound`                                                                                        | 320, 240, bound to no variable                                                                                                                                           | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 320, 240 is intended.                                                                                                                                    |
| `component.bar chart.root.height@type=grouped, orientation=horizontal, breakpoint=mobile`                                        | 1 variant at type=grouped, orientation=horizontal, breakpoint=mobile: 240 where 320                                                                                      | height on / should follow no axis only, but at type=grouped, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart / height is meant to change at type=grouped, orientation=horizontal, breakpoint=mobile.                                                                                                       |
| `component.bar chart.root.height@type=grouped, orientation=vertical, breakpoint=mobile`                                          | 1 variant at type=grouped, orientation=vertical, breakpoint=mobile: 240 where 320                                                                                        | height on / should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart / height is meant to change at type=grouped, orientation=vertical, breakpoint=mobile.                                                                                                         |
| `component.bar chart.root.height@type=simple, orientation=horizontal, breakpoint=mobile`                                         | 1 variant at type=simple, orientation=horizontal, breakpoint=mobile: 240 where 320                                                                                       | height on / should follow no axis only, but at type=simple, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart / height is meant to change at type=simple, orientation=horizontal, breakpoint=mobile.                                                                                                        |
| `component.bar chart.root.height@type=simple, orientation=vertical, breakpoint=mobile`                                           | 1 variant at type=simple, orientation=vertical, breakpoint=mobile: 240 where 320                                                                                         | height on / should follow no axis only, but at type=simple, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart / height is meant to change at type=simple, orientation=vertical, breakpoint=mobile.                                                                                                          |
| `component.bar chart.root.height@type=stacked, orientation=horizontal, breakpoint=mobile`                                        | 1 variant at type=stacked, orientation=horizontal, breakpoint=mobile: 240 where 320                                                                                      | height on / should follow no axis only, but at type=stacked, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart / height is meant to change at type=stacked, orientation=horizontal, breakpoint=mobile.                                                                                                       |
| `component.bar chart.root.height@type=stacked, orientation=vertical, breakpoint=mobile`                                          | 1 variant at type=stacked, orientation=vertical, breakpoint=mobile: 240 where 320                                                                                        | height on / should follow no axis only, but at type=stacked, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart / height is meant to change at type=stacked, orientation=vertical, breakpoint=mobile.                                                                                                         |
| `component.bar chart.root.paddingBottom#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart / paddingBottom to inset.none, which has the same value.                                                                                                                                      |
| `component.bar chart.root.paddingLeft#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart / paddingLeft to inset.none, which has the same value.                                                                                                                                        |
| `component.bar chart.root.paddingRight#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart / paddingRight to inset.none, which has the same value.                                                                                                                                       |
| `component.bar chart.root.paddingTop#unbound`                                                                                    | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart / paddingTop to inset.none, which has the same value.                                                                                                                                         |
| `component.bar chart.root.width#unbound`                                                                                         | 600, 343, bound to no variable                                                                                                                                           | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 600, 343 is intended.                                                                                                                                     |
| `component.bar chart.root.width@type=grouped, orientation=horizontal, breakpoint=mobile`                                         | 1 variant at type=grouped, orientation=horizontal, breakpoint=mobile: 343 where 600                                                                                      | width on / should follow no axis only, but at type=grouped, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart / width is meant to change at type=grouped, orientation=horizontal, breakpoint=mobile.                                                                                                        |
| `component.bar chart.root.width@type=grouped, orientation=vertical, breakpoint=mobile`                                           | 1 variant at type=grouped, orientation=vertical, breakpoint=mobile: 343 where 600                                                                                        | width on / should follow no axis only, but at type=grouped, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart / width is meant to change at type=grouped, orientation=vertical, breakpoint=mobile.                                                                                                          |
| `component.bar chart.root.width@type=simple, orientation=horizontal, breakpoint=mobile`                                          | 1 variant at type=simple, orientation=horizontal, breakpoint=mobile: 343 where 600                                                                                       | width on / should follow no axis only, but at type=simple, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart / width is meant to change at type=simple, orientation=horizontal, breakpoint=mobile.                                                                                                         |
| `component.bar chart.root.width@type=simple, orientation=vertical, breakpoint=mobile`                                            | 1 variant at type=simple, orientation=vertical, breakpoint=mobile: 343 where 600                                                                                         | width on / should follow no axis only, but at type=simple, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart / width is meant to change at type=simple, orientation=vertical, breakpoint=mobile.                                                                                                           |
| `component.bar chart.root.width@type=stacked, orientation=horizontal, breakpoint=mobile`                                         | 1 variant at type=stacked, orientation=horizontal, breakpoint=mobile: 343 where 600                                                                                      | width on / should follow no axis only, but at type=stacked, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart / width is meant to change at type=stacked, orientation=horizontal, breakpoint=mobile.                                                                                                        |
| `component.bar chart.root.width@type=stacked, orientation=vertical, breakpoint=mobile`                                           | 1 variant at type=stacked, orientation=vertical, breakpoint=mobile: 343 where 600                                                                                        | width on / should follow no axis only, but at type=stacked, orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart / width is meant to change at type=stacked, orientation=vertical, breakpoint=mobile.                                                                                                          |
| `component.bar chart.row0.paddingBottom#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Row-0 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-0 paddingBottom to inset.none, which has the same value.                                                                                                                            |
| `component.bar chart.row0.paddingLeft#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Row-0 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-0 paddingLeft to inset.none, which has the same value.                                                                                                                              |
| `component.bar chart.row0.paddingRight#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Row-0 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-0 paddingRight to inset.none, which has the same value.                                                                                                                             |
| `component.bar chart.row0.paddingTop#unbound`                                                                                    | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Row-0 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-0 paddingTop to inset.none, which has the same value.                                                                                                                               |
| `component.bar chart.row0BarTrack.direction@type=grouped, orientation=horizontal, breakpoint=desktop`                            | 1 variant at type=grouped, orientation=horizontal, breakpoint=desktop: VERTICAL where HORIZONTAL                                                                         | direction on /Plot/Row-0/BarTrack should follow no axis only, but at type=grouped, orientation=horizontal, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-0/BarTrack direction is meant to change at type=grouped, orientation=horizontal, breakpoint=desktop.                                                                                |
| `component.bar chart.row0BarTrack.direction@type=grouped, orientation=horizontal, breakpoint=mobile`                             | 1 variant at type=grouped, orientation=horizontal, breakpoint=mobile: VERTICAL where HORIZONTAL                                                                          | direction on /Plot/Row-0/BarTrack should follow no axis only, but at type=grouped, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-0/BarTrack direction is meant to change at type=grouped, orientation=horizontal, breakpoint=mobile.                                                                                 |
| `component.bar chart.row0BarTrack.gap#unbound`                                                                                   | 0, 2, bound to no variable                                                                                                                                               | gap on /Plot/Row-0/BarTrack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-0/BarTrack gap to inset.none or stack.none, which has the same value.                                                                                                               |
| `component.bar chart.row0BarTrack.gap@type=grouped, orientation=horizontal, breakpoint=desktop`                                  | 1 variant at type=grouped, orientation=horizontal, breakpoint=desktop: 2 where 0                                                                                         | gap on /Plot/Row-0/BarTrack should follow no axis only, but at type=grouped, orientation=horizontal, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-0/BarTrack gap is meant to change at type=grouped, orientation=horizontal, breakpoint=desktop.                                                                                      |
| `component.bar chart.row0BarTrack.gap@type=grouped, orientation=horizontal, breakpoint=mobile`                                   | 1 variant at type=grouped, orientation=horizontal, breakpoint=mobile: 2 where 0                                                                                          | gap on /Plot/Row-0/BarTrack should follow no axis only, but at type=grouped, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-0/BarTrack gap is meant to change at type=grouped, orientation=horizontal, breakpoint=mobile.                                                                                       |
| `component.bar chart.row0BarTrack.paddingBottom#unbound`                                                                         | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Row-0/BarTrack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-0/BarTrack paddingBottom to inset.none, which has the same value.                                                                                                                   |
| `component.bar chart.row0BarTrack.paddingLeft#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Row-0/BarTrack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-0/BarTrack paddingLeft to inset.none, which has the same value.                                                                                                                     |
| `component.bar chart.row0BarTrack.paddingRight#unbound`                                                                          | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Row-0/BarTrack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-0/BarTrack paddingRight to inset.none, which has the same value.                                                                                                                    |
| `component.bar chart.row0BarTrack.paddingTop#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Row-0/BarTrack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-0/BarTrack paddingTop to inset.none, which has the same value.                                                                                                                      |
| `component.bar chart.row0BarTrackColorCategory02Strong.height#unbound`                                                           | 12, bound to no variable                                                                                                                                                 | height on /Plot/Row-0/BarTrack/color=category-02-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-0/BarTrack/color=category-02-strong height to icon.xs, which has the same value.                                                                                                    |
| `component.bar chart.row0BarTrackColorCategory02Strong.width#unbound`                                                            | 205, 107, bound to no variable                                                                                                                                           | width on /Plot/Row-0/BarTrack/color=category-02-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 205, 107 is intended.                                                                                                                                     |
| `component.bar chart.row0BarTrackColorCategory02Strong.width@type=grouped, orientation=horizontal, breakpoint=mobile`            | 1 variant at type=grouped, orientation=horizontal, breakpoint=mobile: 107 where 205                                                                                      | width on /Plot/Row-0/BarTrack/color=category-02-strong should follow no axis only, but at type=grouped, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-0/BarTrack/color=category-02-strong width is meant to change at type=grouped, orientation=horizontal, breakpoint=mobile.                                                            |
| `component.bar chart.row0BarTrackColorCategory06Strong.height#unbound`                                                           | 24, 12, bound to no variable                                                                                                                                             | height on /Plot/Row-0/BarTrack/color=category-06-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-0/BarTrack/color=category-06-strong height to icon.lg or icon.xs, which has the same value.                                                                                         |
| `component.bar chart.row0BarTrackColorCategory06Strong.height@type=grouped, orientation=horizontal, breakpoint=desktop`          | 1 variant at type=grouped, orientation=horizontal, breakpoint=desktop: 12 where 24                                                                                       | height on /Plot/Row-0/BarTrack/color=category-06-strong should follow no axis only, but at type=grouped, orientation=horizontal, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-0/BarTrack/color=category-06-strong height is meant to change at type=grouped, orientation=horizontal, breakpoint=desktop.                                                          |
| `component.bar chart.row0BarTrackColorCategory06Strong.height@type=grouped, orientation=horizontal, breakpoint=mobile`           | 1 variant at type=grouped, orientation=horizontal, breakpoint=mobile: 12 where 24                                                                                        | height on /Plot/Row-0/BarTrack/color=category-06-strong should follow no axis only, but at type=grouped, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-0/BarTrack/color=category-06-strong height is meant to change at type=grouped, orientation=horizontal, breakpoint=mobile.                                                           |
| `component.bar chart.row0BarTrackColorCategory06Strong.width#unbound`                                                            | 307, 160, bound to no variable                                                                                                                                           | width on /Plot/Row-0/BarTrack/color=category-06-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 307, 160 is intended.                                                                                                                                     |
| `component.bar chart.row0BarTrackColorCategory06Strong.width@type=grouped, orientation=horizontal, breakpoint=mobile`            | 1 variant at type=grouped, orientation=horizontal, breakpoint=mobile: 160 where 307                                                                                      | width on /Plot/Row-0/BarTrack/color=category-06-strong should follow no axis only, but at type=grouped, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-0/BarTrack/color=category-06-strong width is meant to change at type=grouped, orientation=horizontal, breakpoint=mobile.                                                            |
| `component.bar chart.row0BarTrackColorCategory06Strong.width@type=simple, orientation=horizontal, breakpoint=mobile`             | 1 variant at type=simple, orientation=horizontal, breakpoint=mobile: 160 where 307                                                                                       | width on /Plot/Row-0/BarTrack/color=category-06-strong should follow no axis only, but at type=simple, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-0/BarTrack/color=category-06-strong width is meant to change at type=simple, orientation=horizontal, breakpoint=mobile.                                                             |
| `component.bar chart.row0Frame.gap#unbound`                                                                                      | 0, bound to no variable                                                                                                                                                  | gap on /Plot/Row-0/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-0/Frame gap to inset.none or stack.none, which has the same value.                                                                                                                  |
| `component.bar chart.row0Frame.paddingBottom#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Row-0/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-0/Frame paddingBottom to inset.none, which has the same value.                                                                                                                      |
| `component.bar chart.row0Frame.paddingLeft#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Row-0/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-0/Frame paddingLeft to inset.none, which has the same value.                                                                                                                        |
| `component.bar chart.row0Frame.paddingRight#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Row-0/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-0/Frame paddingRight to inset.none, which has the same value.                                                                                                                       |
| `component.bar chart.row0Frame.paddingTop#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Row-0/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-0/Frame paddingTop to inset.none, which has the same value.                                                                                                                         |
| `component.bar chart.row0Frame.width#unbound`                                                                                    | 48, 36, bound to no variable                                                                                                                                             | width on /Plot/Row-0/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 48, 36 is intended.                                                                                                                                       |
| `component.bar chart.row0Frame.width@type=stacked, orientation=horizontal, breakpoint=mobile`                                    | 1 variant at type=stacked, orientation=horizontal, breakpoint=mobile: 36 where 48                                                                                        | width on /Plot/Row-0/Frame should follow no axis only, but at type=stacked, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-0/Frame width is meant to change at type=stacked, orientation=horizontal, breakpoint=mobile.                                                                                        |
| `component.bar chart.row0Frame2.gap#unbound`                                                                                     | 0, bound to no variable                                                                                                                                                  | gap on /Plot/Row-0/Frame#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-0/Frame#2 gap to inset.none or stack.none, which has the same value.                                                                                                                |
| `component.bar chart.row0Frame2.paddingBottom#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Row-0/Frame#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-0/Frame#2 paddingBottom to inset.none, which has the same value.                                                                                                                    |
| `component.bar chart.row0Frame2.paddingLeft#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Row-0/Frame#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-0/Frame#2 paddingLeft to inset.none, which has the same value.                                                                                                                      |
| `component.bar chart.row0Frame2.paddingRight#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Row-0/Frame#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-0/Frame#2 paddingRight to inset.none, which has the same value.                                                                                                                     |
| `component.bar chart.row0Frame2.paddingTop#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Row-0/Frame#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-0/Frame#2 paddingTop to inset.none, which has the same value.                                                                                                                       |
| `component.bar chart.row0Frame2.radius@type=stacked, orientation=horizontal, breakpoint=mobile`                                  | 1 variant at type=stacked, orientation=horizontal, breakpoint=mobile: none where radius.none                                                                             | radius on /Plot/Row-0/Frame#2 should follow no axis only, but at type=stacked, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-0/Frame#2 radius is meant to change at type=stacked, orientation=horizontal, breakpoint=mobile.                                                                                     |
| `component.bar chart.row0Frame2BarStack.gap#unbound`                                                                             | 2, bound to no variable                                                                                                                                                  | gap on /Plot/Row-0/Frame#2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no gap token; ask SOLAR for one, or confirm 2 is intended.                                                                                                                                                      |
| `component.bar chart.row0Frame2BarStack.height#unbound`                                                                          | 24, bound to no variable                                                                                                                                                 | height on /Plot/Row-0/Frame#2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 24 is intended.                                                                                                                                          |
| `component.bar chart.row0Frame2BarStack.paddingBottom#unbound`                                                                   | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Row-0/Frame#2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-0/Frame#2/Bar Stack paddingBottom to inset.none, which has the same value.                                                                                                          |
| `component.bar chart.row0Frame2BarStack.paddingLeft#unbound`                                                                     | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Row-0/Frame#2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-0/Frame#2/Bar Stack paddingLeft to inset.none, which has the same value.                                                                                                            |
| `component.bar chart.row0Frame2BarStack.paddingRight#unbound`                                                                    | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Row-0/Frame#2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-0/Frame#2/Bar Stack paddingRight to inset.none, which has the same value.                                                                                                           |
| `component.bar chart.row0Frame2BarStack.paddingTop#unbound`                                                                      | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Row-0/Frame#2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-0/Frame#2/Bar Stack paddingTop to inset.none, which has the same value.                                                                                                             |
| `component.bar chart.row0Frame2BarStack.width#unbound`                                                                           | 326, 179, bound to no variable                                                                                                                                           | width on /Plot/Row-0/Frame#2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 326, 179 is intended.                                                                                                                                     |
| `component.bar chart.row0Frame2BarStack.width@type=stacked, orientation=horizontal, breakpoint=mobile`                           | 1 variant at type=stacked, orientation=horizontal, breakpoint=mobile: 179 where 326                                                                                      | width on /Plot/Row-0/Frame#2/Bar Stack should follow no axis only, but at type=stacked, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-0/Frame#2/Bar Stack width is meant to change at type=stacked, orientation=horizontal, breakpoint=mobile.                                                                            |
| `component.bar chart.row0Frame2BarStackColorFeedbackDangerStrong.width#unbound`                                                  | 113, 61, bound to no variable                                                                                                                                            | width on /Plot/Row-0/Frame#2/Bar Stack/color=feedback-danger-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 113, 61 is intended.                                                                                                                                      |
| `component.bar chart.row0Frame2BarStackColorFeedbackDangerStrong.width@type=stacked, orientation=horizontal, breakpoint=mobile`  | 1 variant at type=stacked, orientation=horizontal, breakpoint=mobile: 61 where 113                                                                                       | width on /Plot/Row-0/Frame#2/Bar Stack/color=feedback-danger-strong should follow no axis only, but at type=stacked, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-0/Frame#2/Bar Stack/color=feedback-danger-strong width is meant to change at type=stacked, orientation=horizontal, breakpoint=mobile.                                               |
| `component.bar chart.row0Frame2BarStackColorFeedbackNeutralSubtle.width#unbound`                                                 | 129, 70, bound to no variable                                                                                                                                            | width on /Plot/Row-0/Frame#2/Bar Stack/color=feedback-neutral-subtle is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 129, 70 is intended.                                                                                                                                      |
| `component.bar chart.row0Frame2BarStackColorFeedbackNeutralSubtle.width@type=stacked, orientation=horizontal, breakpoint=mobile` | 1 variant at type=stacked, orientation=horizontal, breakpoint=mobile: 70 where 129                                                                                       | width on /Plot/Row-0/Frame#2/Bar Stack/color=feedback-neutral-subtle should follow no axis only, but at type=stacked, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-0/Frame#2/Bar Stack/color=feedback-neutral-subtle width is meant to change at type=stacked, orientation=horizontal, breakpoint=mobile.                                              |
| `component.bar chart.row0Frame2BarStackColorFeedbackWarningMedium.width#unbound`                                                 | 81, 44, bound to no variable                                                                                                                                             | width on /Plot/Row-0/Frame#2/Bar Stack/color=feedback-warning-medium is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 81, 44 is intended.                                                                                                                                       |
| `component.bar chart.row0Frame2BarStackColorFeedbackWarningMedium.width@type=stacked, orientation=horizontal, breakpoint=mobile` | 1 variant at type=stacked, orientation=horizontal, breakpoint=mobile: 44 where 81                                                                                        | width on /Plot/Row-0/Frame#2/Bar Stack/color=feedback-warning-medium should follow no axis only, but at type=stacked, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-0/Frame#2/Bar Stack/color=feedback-warning-medium width is meant to change at type=stacked, orientation=horizontal, breakpoint=mobile.                                              |
| `component.bar chart.row0Label.gap#unbound`                                                                                      | 0, bound to no variable                                                                                                                                                  | gap on /Plot/Row-0/Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-0/Label gap to inset.none or stack.none, which has the same value.                                                                                                                  |
| `component.bar chart.row0Label.paddingBottom#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Row-0/Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-0/Label paddingBottom to inset.none, which has the same value.                                                                                                                      |
| `component.bar chart.row0Label.paddingLeft#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Row-0/Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-0/Label paddingLeft to inset.none, which has the same value.                                                                                                                        |
| `component.bar chart.row0Label.paddingRight#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Row-0/Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-0/Label paddingRight to inset.none, which has the same value.                                                                                                                       |
| `component.bar chart.row0Label.paddingTop#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Row-0/Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-0/Label paddingTop to inset.none, which has the same value.                                                                                                                         |
| `component.bar chart.row0Label.width#unbound`                                                                                    | 48, 36, bound to no variable                                                                                                                                             | width on /Plot/Row-0/Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 48, 36 is intended.                                                                                                                                       |
| `component.bar chart.row0Label.width@type=grouped, orientation=horizontal, breakpoint=mobile`                                    | 1 variant at type=grouped, orientation=horizontal, breakpoint=mobile: 36 where 48                                                                                        | width on /Plot/Row-0/Label should follow no axis only, but at type=grouped, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-0/Label width is meant to change at type=grouped, orientation=horizontal, breakpoint=mobile.                                                                                        |
| `component.bar chart.row0Label.width@type=simple, orientation=horizontal, breakpoint=mobile`                                     | 1 variant at type=simple, orientation=horizontal, breakpoint=mobile: 36 where 48                                                                                         | width on /Plot/Row-0/Label should follow no axis only, but at type=simple, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-0/Label width is meant to change at type=simple, orientation=horizontal, breakpoint=mobile.                                                                                         |
| `component.bar chart.row1.paddingBottom#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Row-1 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-1 paddingBottom to inset.none, which has the same value.                                                                                                                            |
| `component.bar chart.row1.paddingLeft#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Row-1 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-1 paddingLeft to inset.none, which has the same value.                                                                                                                              |
| `component.bar chart.row1.paddingRight#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Row-1 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-1 paddingRight to inset.none, which has the same value.                                                                                                                             |
| `component.bar chart.row1.paddingTop#unbound`                                                                                    | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Row-1 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-1 paddingTop to inset.none, which has the same value.                                                                                                                               |
| `component.bar chart.row1BarTrack.direction@type=grouped, orientation=horizontal, breakpoint=desktop`                            | 1 variant at type=grouped, orientation=horizontal, breakpoint=desktop: VERTICAL where HORIZONTAL                                                                         | direction on /Plot/Row-1/BarTrack should follow no axis only, but at type=grouped, orientation=horizontal, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-1/BarTrack direction is meant to change at type=grouped, orientation=horizontal, breakpoint=desktop.                                                                                |
| `component.bar chart.row1BarTrack.direction@type=grouped, orientation=horizontal, breakpoint=mobile`                             | 1 variant at type=grouped, orientation=horizontal, breakpoint=mobile: VERTICAL where HORIZONTAL                                                                          | direction on /Plot/Row-1/BarTrack should follow no axis only, but at type=grouped, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-1/BarTrack direction is meant to change at type=grouped, orientation=horizontal, breakpoint=mobile.                                                                                 |
| `component.bar chart.row1BarTrack.gap#unbound`                                                                                   | 0, 2, bound to no variable                                                                                                                                               | gap on /Plot/Row-1/BarTrack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-1/BarTrack gap to inset.none or stack.none, which has the same value.                                                                                                               |
| `component.bar chart.row1BarTrack.gap@type=grouped, orientation=horizontal, breakpoint=desktop`                                  | 1 variant at type=grouped, orientation=horizontal, breakpoint=desktop: 2 where 0                                                                                         | gap on /Plot/Row-1/BarTrack should follow no axis only, but at type=grouped, orientation=horizontal, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-1/BarTrack gap is meant to change at type=grouped, orientation=horizontal, breakpoint=desktop.                                                                                      |
| `component.bar chart.row1BarTrack.gap@type=grouped, orientation=horizontal, breakpoint=mobile`                                   | 1 variant at type=grouped, orientation=horizontal, breakpoint=mobile: 2 where 0                                                                                          | gap on /Plot/Row-1/BarTrack should follow no axis only, but at type=grouped, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-1/BarTrack gap is meant to change at type=grouped, orientation=horizontal, breakpoint=mobile.                                                                                       |
| `component.bar chart.row1BarTrack.paddingBottom#unbound`                                                                         | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Row-1/BarTrack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-1/BarTrack paddingBottom to inset.none, which has the same value.                                                                                                                   |
| `component.bar chart.row1BarTrack.paddingLeft#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Row-1/BarTrack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-1/BarTrack paddingLeft to inset.none, which has the same value.                                                                                                                     |
| `component.bar chart.row1BarTrack.paddingRight#unbound`                                                                          | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Row-1/BarTrack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-1/BarTrack paddingRight to inset.none, which has the same value.                                                                                                                    |
| `component.bar chart.row1BarTrack.paddingTop#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Row-1/BarTrack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-1/BarTrack paddingTop to inset.none, which has the same value.                                                                                                                      |
| `component.bar chart.row1BarTrackColorCategory02Strong.height#unbound`                                                           | 12, bound to no variable                                                                                                                                                 | height on /Plot/Row-1/BarTrack/color=category-02-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-1/BarTrack/color=category-02-strong height to icon.xs, which has the same value.                                                                                                    |
| `component.bar chart.row1BarTrackColorCategory02Strong.width#unbound`                                                            | 282, 147, bound to no variable                                                                                                                                           | width on /Plot/Row-1/BarTrack/color=category-02-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 282, 147 is intended.                                                                                                                                     |
| `component.bar chart.row1BarTrackColorCategory02Strong.width@type=grouped, orientation=horizontal, breakpoint=mobile`            | 1 variant at type=grouped, orientation=horizontal, breakpoint=mobile: 147 where 282                                                                                      | width on /Plot/Row-1/BarTrack/color=category-02-strong should follow no axis only, but at type=grouped, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-1/BarTrack/color=category-02-strong width is meant to change at type=grouped, orientation=horizontal, breakpoint=mobile.                                                            |
| `component.bar chart.row1BarTrackColorCategory06Strong.height#unbound`                                                           | 24, 12, bound to no variable                                                                                                                                             | height on /Plot/Row-1/BarTrack/color=category-06-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-1/BarTrack/color=category-06-strong height to icon.lg or icon.xs, which has the same value.                                                                                         |
| `component.bar chart.row1BarTrackColorCategory06Strong.height@type=grouped, orientation=horizontal, breakpoint=desktop`          | 1 variant at type=grouped, orientation=horizontal, breakpoint=desktop: 12 where 24                                                                                       | height on /Plot/Row-1/BarTrack/color=category-06-strong should follow no axis only, but at type=grouped, orientation=horizontal, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-1/BarTrack/color=category-06-strong height is meant to change at type=grouped, orientation=horizontal, breakpoint=desktop.                                                          |
| `component.bar chart.row1BarTrackColorCategory06Strong.height@type=grouped, orientation=horizontal, breakpoint=mobile`           | 1 variant at type=grouped, orientation=horizontal, breakpoint=mobile: 12 where 24                                                                                        | height on /Plot/Row-1/BarTrack/color=category-06-strong should follow no axis only, but at type=grouped, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-1/BarTrack/color=category-06-strong height is meant to change at type=grouped, orientation=horizontal, breakpoint=mobile.                                                           |
| `component.bar chart.row1BarTrackColorCategory06Strong.width#unbound`                                                            | 399, 208, bound to no variable                                                                                                                                           | width on /Plot/Row-1/BarTrack/color=category-06-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 399, 208 is intended.                                                                                                                                     |
| `component.bar chart.row1BarTrackColorCategory06Strong.width@type=grouped, orientation=horizontal, breakpoint=mobile`            | 1 variant at type=grouped, orientation=horizontal, breakpoint=mobile: 208 where 399                                                                                      | width on /Plot/Row-1/BarTrack/color=category-06-strong should follow no axis only, but at type=grouped, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-1/BarTrack/color=category-06-strong width is meant to change at type=grouped, orientation=horizontal, breakpoint=mobile.                                                            |
| `component.bar chart.row1BarTrackColorCategory06Strong.width@type=simple, orientation=horizontal, breakpoint=mobile`             | 1 variant at type=simple, orientation=horizontal, breakpoint=mobile: 208 where 399                                                                                       | width on /Plot/Row-1/BarTrack/color=category-06-strong should follow no axis only, but at type=simple, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-1/BarTrack/color=category-06-strong width is meant to change at type=simple, orientation=horizontal, breakpoint=mobile.                                                             |
| `component.bar chart.row1Frame.gap#unbound`                                                                                      | 0, bound to no variable                                                                                                                                                  | gap on /Plot/Row-1/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-1/Frame gap to inset.none or stack.none, which has the same value.                                                                                                                  |
| `component.bar chart.row1Frame.paddingBottom#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Row-1/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-1/Frame paddingBottom to inset.none, which has the same value.                                                                                                                      |
| `component.bar chart.row1Frame.paddingLeft#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Row-1/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-1/Frame paddingLeft to inset.none, which has the same value.                                                                                                                        |
| `component.bar chart.row1Frame.paddingRight#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Row-1/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-1/Frame paddingRight to inset.none, which has the same value.                                                                                                                       |
| `component.bar chart.row1Frame.paddingTop#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Row-1/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-1/Frame paddingTop to inset.none, which has the same value.                                                                                                                         |
| `component.bar chart.row1Frame.width#unbound`                                                                                    | 48, 36, bound to no variable                                                                                                                                             | width on /Plot/Row-1/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 48, 36 is intended.                                                                                                                                       |
| `component.bar chart.row1Frame.width@type=stacked, orientation=horizontal, breakpoint=mobile`                                    | 1 variant at type=stacked, orientation=horizontal, breakpoint=mobile: 36 where 48                                                                                        | width on /Plot/Row-1/Frame should follow no axis only, but at type=stacked, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-1/Frame width is meant to change at type=stacked, orientation=horizontal, breakpoint=mobile.                                                                                        |
| `component.bar chart.row1Frame2.gap#unbound`                                                                                     | 0, bound to no variable                                                                                                                                                  | gap on /Plot/Row-1/Frame#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-1/Frame#2 gap to inset.none or stack.none, which has the same value.                                                                                                                |
| `component.bar chart.row1Frame2.paddingBottom#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Row-1/Frame#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-1/Frame#2 paddingBottom to inset.none, which has the same value.                                                                                                                    |
| `component.bar chart.row1Frame2.paddingLeft#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Row-1/Frame#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-1/Frame#2 paddingLeft to inset.none, which has the same value.                                                                                                                      |
| `component.bar chart.row1Frame2.paddingRight#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Row-1/Frame#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-1/Frame#2 paddingRight to inset.none, which has the same value.                                                                                                                     |
| `component.bar chart.row1Frame2.paddingTop#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Row-1/Frame#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-1/Frame#2 paddingTop to inset.none, which has the same value.                                                                                                                       |
| `component.bar chart.row1Frame2.radius@type=stacked, orientation=horizontal, breakpoint=mobile`                                  | 1 variant at type=stacked, orientation=horizontal, breakpoint=mobile: none where radius.none                                                                             | radius on /Plot/Row-1/Frame#2 should follow no axis only, but at type=stacked, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-1/Frame#2 radius is meant to change at type=stacked, orientation=horizontal, breakpoint=mobile.                                                                                     |
| `component.bar chart.row1Frame2BarStack.gap#unbound`                                                                             | 2, bound to no variable                                                                                                                                                  | gap on /Plot/Row-1/Frame#2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no gap token; ask SOLAR for one, or confirm 2 is intended.                                                                                                                                                      |
| `component.bar chart.row1Frame2BarStack.height#unbound`                                                                          | 24, bound to no variable                                                                                                                                                 | height on /Plot/Row-1/Frame#2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 24 is intended.                                                                                                                                          |
| `component.bar chart.row1Frame2BarStack.paddingBottom#unbound`                                                                   | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Row-1/Frame#2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-1/Frame#2/Bar Stack paddingBottom to inset.none, which has the same value.                                                                                                          |
| `component.bar chart.row1Frame2BarStack.paddingLeft#unbound`                                                                     | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Row-1/Frame#2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-1/Frame#2/Bar Stack paddingLeft to inset.none, which has the same value.                                                                                                            |
| `component.bar chart.row1Frame2BarStack.paddingRight#unbound`                                                                    | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Row-1/Frame#2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-1/Frame#2/Bar Stack paddingRight to inset.none, which has the same value.                                                                                                           |
| `component.bar chart.row1Frame2BarStack.paddingTop#unbound`                                                                      | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Row-1/Frame#2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-1/Frame#2/Bar Stack paddingTop to inset.none, which has the same value.                                                                                                             |
| `component.bar chart.row1Frame2BarStack.width#unbound`                                                                           | 424, 233, bound to no variable                                                                                                                                           | width on /Plot/Row-1/Frame#2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 424, 233 is intended.                                                                                                                                     |
| `component.bar chart.row1Frame2BarStack.width@type=stacked, orientation=horizontal, breakpoint=mobile`                           | 1 variant at type=stacked, orientation=horizontal, breakpoint=mobile: 233 where 424                                                                                      | width on /Plot/Row-1/Frame#2/Bar Stack should follow no axis only, but at type=stacked, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-1/Frame#2/Bar Stack width is meant to change at type=stacked, orientation=horizontal, breakpoint=mobile.                                                                            |
| `component.bar chart.row1Frame2BarStackColorFeedbackDangerStrong.width#unbound`                                                  | 42, 23, bound to no variable                                                                                                                                             | width on /Plot/Row-1/Frame#2/Bar Stack/color=feedback-danger-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 42, 23 is intended.                                                                                                                                       |
| `component.bar chart.row1Frame2BarStackColorFeedbackDangerStrong.width@type=stacked, orientation=horizontal, breakpoint=mobile`  | 1 variant at type=stacked, orientation=horizontal, breakpoint=mobile: 23 where 42                                                                                        | width on /Plot/Row-1/Frame#2/Bar Stack/color=feedback-danger-strong should follow no axis only, but at type=stacked, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-1/Frame#2/Bar Stack/color=feedback-danger-strong width is meant to change at type=stacked, orientation=horizontal, breakpoint=mobile.                                               |
| `component.bar chart.row1Frame2BarStackColorFeedbackNeutralSubtle.width#unbound`                                                 | 210, 115, bound to no variable                                                                                                                                           | width on /Plot/Row-1/Frame#2/Bar Stack/color=feedback-neutral-subtle is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 210, 115 is intended.                                                                                                                                     |
| `component.bar chart.row1Frame2BarStackColorFeedbackNeutralSubtle.width@type=stacked, orientation=horizontal, breakpoint=mobile` | 1 variant at type=stacked, orientation=horizontal, breakpoint=mobile: 115 where 210                                                                                      | width on /Plot/Row-1/Frame#2/Bar Stack/color=feedback-neutral-subtle should follow no axis only, but at type=stacked, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-1/Frame#2/Bar Stack/color=feedback-neutral-subtle width is meant to change at type=stacked, orientation=horizontal, breakpoint=mobile.                                              |
| `component.bar chart.row1Frame2BarStackColorFeedbackWarningMedium.width#unbound`                                                 | 168, 92, bound to no variable                                                                                                                                            | width on /Plot/Row-1/Frame#2/Bar Stack/color=feedback-warning-medium is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 168, 92 is intended.                                                                                                                                      |
| `component.bar chart.row1Frame2BarStackColorFeedbackWarningMedium.width@type=stacked, orientation=horizontal, breakpoint=mobile` | 1 variant at type=stacked, orientation=horizontal, breakpoint=mobile: 92 where 168                                                                                       | width on /Plot/Row-1/Frame#2/Bar Stack/color=feedback-warning-medium should follow no axis only, but at type=stacked, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-1/Frame#2/Bar Stack/color=feedback-warning-medium width is meant to change at type=stacked, orientation=horizontal, breakpoint=mobile.                                              |
| `component.bar chart.row1Label.gap#unbound`                                                                                      | 0, bound to no variable                                                                                                                                                  | gap on /Plot/Row-1/Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-1/Label gap to inset.none or stack.none, which has the same value.                                                                                                                  |
| `component.bar chart.row1Label.paddingBottom#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Row-1/Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-1/Label paddingBottom to inset.none, which has the same value.                                                                                                                      |
| `component.bar chart.row1Label.paddingLeft#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Row-1/Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-1/Label paddingLeft to inset.none, which has the same value.                                                                                                                        |
| `component.bar chart.row1Label.paddingRight#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Row-1/Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-1/Label paddingRight to inset.none, which has the same value.                                                                                                                       |
| `component.bar chart.row1Label.paddingTop#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Row-1/Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-1/Label paddingTop to inset.none, which has the same value.                                                                                                                         |
| `component.bar chart.row1Label.width#unbound`                                                                                    | 48, 36, bound to no variable                                                                                                                                             | width on /Plot/Row-1/Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 48, 36 is intended.                                                                                                                                       |
| `component.bar chart.row1Label.width@type=grouped, orientation=horizontal, breakpoint=mobile`                                    | 1 variant at type=grouped, orientation=horizontal, breakpoint=mobile: 36 where 48                                                                                        | width on /Plot/Row-1/Label should follow no axis only, but at type=grouped, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-1/Label width is meant to change at type=grouped, orientation=horizontal, breakpoint=mobile.                                                                                        |
| `component.bar chart.row1Label.width@type=simple, orientation=horizontal, breakpoint=mobile`                                     | 1 variant at type=simple, orientation=horizontal, breakpoint=mobile: 36 where 48                                                                                         | width on /Plot/Row-1/Label should follow no axis only, but at type=simple, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-1/Label width is meant to change at type=simple, orientation=horizontal, breakpoint=mobile.                                                                                         |
| `component.bar chart.row2.paddingBottom#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Row-2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-2 paddingBottom to inset.none, which has the same value.                                                                                                                            |
| `component.bar chart.row2.paddingLeft#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Row-2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-2 paddingLeft to inset.none, which has the same value.                                                                                                                              |
| `component.bar chart.row2.paddingRight#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Row-2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-2 paddingRight to inset.none, which has the same value.                                                                                                                             |
| `component.bar chart.row2.paddingTop#unbound`                                                                                    | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Row-2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-2 paddingTop to inset.none, which has the same value.                                                                                                                               |
| `component.bar chart.row2BarTrack.direction@type=grouped, orientation=horizontal, breakpoint=desktop`                            | 1 variant at type=grouped, orientation=horizontal, breakpoint=desktop: VERTICAL where HORIZONTAL                                                                         | direction on /Plot/Row-2/BarTrack should follow no axis only, but at type=grouped, orientation=horizontal, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-2/BarTrack direction is meant to change at type=grouped, orientation=horizontal, breakpoint=desktop.                                                                                |
| `component.bar chart.row2BarTrack.direction@type=grouped, orientation=horizontal, breakpoint=mobile`                             | 1 variant at type=grouped, orientation=horizontal, breakpoint=mobile: VERTICAL where HORIZONTAL                                                                          | direction on /Plot/Row-2/BarTrack should follow no axis only, but at type=grouped, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-2/BarTrack direction is meant to change at type=grouped, orientation=horizontal, breakpoint=mobile.                                                                                 |
| `component.bar chart.row2BarTrack.gap#unbound`                                                                                   | 0, 2, bound to no variable                                                                                                                                               | gap on /Plot/Row-2/BarTrack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-2/BarTrack gap to inset.none or stack.none, which has the same value.                                                                                                               |
| `component.bar chart.row2BarTrack.gap@type=grouped, orientation=horizontal, breakpoint=desktop`                                  | 1 variant at type=grouped, orientation=horizontal, breakpoint=desktop: 2 where 0                                                                                         | gap on /Plot/Row-2/BarTrack should follow no axis only, but at type=grouped, orientation=horizontal, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-2/BarTrack gap is meant to change at type=grouped, orientation=horizontal, breakpoint=desktop.                                                                                      |
| `component.bar chart.row2BarTrack.gap@type=grouped, orientation=horizontal, breakpoint=mobile`                                   | 1 variant at type=grouped, orientation=horizontal, breakpoint=mobile: 2 where 0                                                                                          | gap on /Plot/Row-2/BarTrack should follow no axis only, but at type=grouped, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-2/BarTrack gap is meant to change at type=grouped, orientation=horizontal, breakpoint=mobile.                                                                                       |
| `component.bar chart.row2BarTrack.paddingBottom#unbound`                                                                         | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Row-2/BarTrack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-2/BarTrack paddingBottom to inset.none, which has the same value.                                                                                                                   |
| `component.bar chart.row2BarTrack.paddingLeft#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Row-2/BarTrack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-2/BarTrack paddingLeft to inset.none, which has the same value.                                                                                                                     |
| `component.bar chart.row2BarTrack.paddingRight#unbound`                                                                          | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Row-2/BarTrack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-2/BarTrack paddingRight to inset.none, which has the same value.                                                                                                                    |
| `component.bar chart.row2BarTrack.paddingTop#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Row-2/BarTrack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-2/BarTrack paddingTop to inset.none, which has the same value.                                                                                                                      |
| `component.bar chart.row2BarTrackColorCategory02Strong.height#unbound`                                                           | 12, bound to no variable                                                                                                                                                 | height on /Plot/Row-2/BarTrack/color=category-02-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-2/BarTrack/color=category-02-strong height to icon.xs, which has the same value.                                                                                                    |
| `component.bar chart.row2BarTrackColorCategory02Strong.width#unbound`                                                            | 358, 187, bound to no variable                                                                                                                                           | width on /Plot/Row-2/BarTrack/color=category-02-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 358, 187 is intended.                                                                                                                                     |
| `component.bar chart.row2BarTrackColorCategory02Strong.width@type=grouped, orientation=horizontal, breakpoint=mobile`            | 1 variant at type=grouped, orientation=horizontal, breakpoint=mobile: 187 where 358                                                                                      | width on /Plot/Row-2/BarTrack/color=category-02-strong should follow no axis only, but at type=grouped, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-2/BarTrack/color=category-02-strong width is meant to change at type=grouped, orientation=horizontal, breakpoint=mobile.                                                            |
| `component.bar chart.row2BarTrackColorCategory06Strong.height#unbound`                                                           | 24, 12, bound to no variable                                                                                                                                             | height on /Plot/Row-2/BarTrack/color=category-06-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-2/BarTrack/color=category-06-strong height to icon.lg or icon.xs, which has the same value.                                                                                         |
| `component.bar chart.row2BarTrackColorCategory06Strong.height@type=grouped, orientation=horizontal, breakpoint=desktop`          | 1 variant at type=grouped, orientation=horizontal, breakpoint=desktop: 12 where 24                                                                                       | height on /Plot/Row-2/BarTrack/color=category-06-strong should follow no axis only, but at type=grouped, orientation=horizontal, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-2/BarTrack/color=category-06-strong height is meant to change at type=grouped, orientation=horizontal, breakpoint=desktop.                                                          |
| `component.bar chart.row2BarTrackColorCategory06Strong.height@type=grouped, orientation=horizontal, breakpoint=mobile`           | 1 variant at type=grouped, orientation=horizontal, breakpoint=mobile: 12 where 24                                                                                        | height on /Plot/Row-2/BarTrack/color=category-06-strong should follow no axis only, but at type=grouped, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-2/BarTrack/color=category-06-strong height is meant to change at type=grouped, orientation=horizontal, breakpoint=mobile.                                                           |
| `component.bar chart.row2BarTrackColorCategory06Strong.width#unbound`                                                            | 230, 120, bound to no variable                                                                                                                                           | width on /Plot/Row-2/BarTrack/color=category-06-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 230, 120 is intended.                                                                                                                                     |
| `component.bar chart.row2BarTrackColorCategory06Strong.width@type=grouped, orientation=horizontal, breakpoint=mobile`            | 1 variant at type=grouped, orientation=horizontal, breakpoint=mobile: 120 where 230                                                                                      | width on /Plot/Row-2/BarTrack/color=category-06-strong should follow no axis only, but at type=grouped, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-2/BarTrack/color=category-06-strong width is meant to change at type=grouped, orientation=horizontal, breakpoint=mobile.                                                            |
| `component.bar chart.row2BarTrackColorCategory06Strong.width@type=simple, orientation=horizontal, breakpoint=mobile`             | 1 variant at type=simple, orientation=horizontal, breakpoint=mobile: 120 where 230                                                                                       | width on /Plot/Row-2/BarTrack/color=category-06-strong should follow no axis only, but at type=simple, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-2/BarTrack/color=category-06-strong width is meant to change at type=simple, orientation=horizontal, breakpoint=mobile.                                                             |
| `component.bar chart.row2Frame.gap#unbound`                                                                                      | 0, bound to no variable                                                                                                                                                  | gap on /Plot/Row-2/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-2/Frame gap to inset.none or stack.none, which has the same value.                                                                                                                  |
| `component.bar chart.row2Frame.paddingBottom#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Row-2/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-2/Frame paddingBottom to inset.none, which has the same value.                                                                                                                      |
| `component.bar chart.row2Frame.paddingLeft#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Row-2/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-2/Frame paddingLeft to inset.none, which has the same value.                                                                                                                        |
| `component.bar chart.row2Frame.paddingRight#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Row-2/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-2/Frame paddingRight to inset.none, which has the same value.                                                                                                                       |
| `component.bar chart.row2Frame.paddingTop#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Row-2/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-2/Frame paddingTop to inset.none, which has the same value.                                                                                                                         |
| `component.bar chart.row2Frame.width#unbound`                                                                                    | 48, 36, bound to no variable                                                                                                                                             | width on /Plot/Row-2/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 48, 36 is intended.                                                                                                                                       |
| `component.bar chart.row2Frame.width@type=stacked, orientation=horizontal, breakpoint=mobile`                                    | 1 variant at type=stacked, orientation=horizontal, breakpoint=mobile: 36 where 48                                                                                        | width on /Plot/Row-2/Frame should follow no axis only, but at type=stacked, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-2/Frame width is meant to change at type=stacked, orientation=horizontal, breakpoint=mobile.                                                                                        |
| `component.bar chart.row2Frame2.gap#unbound`                                                                                     | 0, bound to no variable                                                                                                                                                  | gap on /Plot/Row-2/Frame#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-2/Frame#2 gap to inset.none or stack.none, which has the same value.                                                                                                                |
| `component.bar chart.row2Frame2.paddingBottom#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Row-2/Frame#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-2/Frame#2 paddingBottom to inset.none, which has the same value.                                                                                                                    |
| `component.bar chart.row2Frame2.paddingLeft#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Row-2/Frame#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-2/Frame#2 paddingLeft to inset.none, which has the same value.                                                                                                                      |
| `component.bar chart.row2Frame2.paddingRight#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Row-2/Frame#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-2/Frame#2 paddingRight to inset.none, which has the same value.                                                                                                                     |
| `component.bar chart.row2Frame2.paddingTop#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Row-2/Frame#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-2/Frame#2 paddingTop to inset.none, which has the same value.                                                                                                                       |
| `component.bar chart.row2Frame2.radius@type=stacked, orientation=horizontal, breakpoint=mobile`                                  | 1 variant at type=stacked, orientation=horizontal, breakpoint=mobile: none where radius.none                                                                             | radius on /Plot/Row-2/Frame#2 should follow no axis only, but at type=stacked, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-2/Frame#2 radius is meant to change at type=stacked, orientation=horizontal, breakpoint=mobile.                                                                                     |
| `component.bar chart.row2Frame2BarStack.gap#unbound`                                                                             | 2, bound to no variable                                                                                                                                                  | gap on /Plot/Row-2/Frame#2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no gap token; ask SOLAR for one, or confirm 2 is intended.                                                                                                                                                      |
| `component.bar chart.row2Frame2BarStack.height#unbound`                                                                          | 24, bound to no variable                                                                                                                                                 | height on /Plot/Row-2/Frame#2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 24 is intended.                                                                                                                                          |
| `component.bar chart.row2Frame2BarStack.paddingBottom#unbound`                                                                   | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Row-2/Frame#2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-2/Frame#2/Bar Stack paddingBottom to inset.none, which has the same value.                                                                                                          |
| `component.bar chart.row2Frame2BarStack.paddingLeft#unbound`                                                                     | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Row-2/Frame#2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-2/Frame#2/Bar Stack paddingLeft to inset.none, which has the same value.                                                                                                            |
| `component.bar chart.row2Frame2BarStack.paddingRight#unbound`                                                                    | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Row-2/Frame#2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-2/Frame#2/Bar Stack paddingRight to inset.none, which has the same value.                                                                                                           |
| `component.bar chart.row2Frame2BarStack.paddingTop#unbound`                                                                      | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Row-2/Frame#2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-2/Frame#2/Bar Stack paddingTop to inset.none, which has the same value.                                                                                                             |
| `component.bar chart.row2Frame2BarStack.width#unbound`                                                                           | 245, 135, bound to no variable                                                                                                                                           | width on /Plot/Row-2/Frame#2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 245, 135 is intended.                                                                                                                                     |
| `component.bar chart.row2Frame2BarStack.width@type=stacked, orientation=horizontal, breakpoint=mobile`                           | 1 variant at type=stacked, orientation=horizontal, breakpoint=mobile: 135 where 245                                                                                      | width on /Plot/Row-2/Frame#2/Bar Stack should follow no axis only, but at type=stacked, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-2/Frame#2/Bar Stack width is meant to change at type=stacked, orientation=horizontal, breakpoint=mobile.                                                                            |
| `component.bar chart.row2Frame2BarStackColorFeedbackDangerStrong.width#unbound`                                                  | 121, 66, bound to no variable                                                                                                                                            | width on /Plot/Row-2/Frame#2/Bar Stack/color=feedback-danger-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 121, 66 is intended.                                                                                                                                      |
| `component.bar chart.row2Frame2BarStackColorFeedbackDangerStrong.width@type=stacked, orientation=horizontal, breakpoint=mobile`  | 1 variant at type=stacked, orientation=horizontal, breakpoint=mobile: 66 where 121                                                                                       | width on /Plot/Row-2/Frame#2/Bar Stack/color=feedback-danger-strong should follow no axis only, but at type=stacked, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-2/Frame#2/Bar Stack/color=feedback-danger-strong width is meant to change at type=stacked, orientation=horizontal, breakpoint=mobile.                                               |
| `component.bar chart.row2Frame2BarStackColorFeedbackNeutralSubtle.width#unbound`                                                 | 48, 26, bound to no variable                                                                                                                                             | width on /Plot/Row-2/Frame#2/Bar Stack/color=feedback-neutral-subtle is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 48, 26 is intended.                                                                                                                                       |
| `component.bar chart.row2Frame2BarStackColorFeedbackNeutralSubtle.width@type=stacked, orientation=horizontal, breakpoint=mobile` | 1 variant at type=stacked, orientation=horizontal, breakpoint=mobile: 26 where 48                                                                                        | width on /Plot/Row-2/Frame#2/Bar Stack/color=feedback-neutral-subtle should follow no axis only, but at type=stacked, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-2/Frame#2/Bar Stack/color=feedback-neutral-subtle width is meant to change at type=stacked, orientation=horizontal, breakpoint=mobile.                                              |
| `component.bar chart.row2Frame2BarStackColorFeedbackWarningMedium.width#unbound`                                                 | 72, 39, bound to no variable                                                                                                                                             | width on /Plot/Row-2/Frame#2/Bar Stack/color=feedback-warning-medium is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 72, 39 is intended.                                                                                                                                       |
| `component.bar chart.row2Frame2BarStackColorFeedbackWarningMedium.width@type=stacked, orientation=horizontal, breakpoint=mobile` | 1 variant at type=stacked, orientation=horizontal, breakpoint=mobile: 39 where 72                                                                                        | width on /Plot/Row-2/Frame#2/Bar Stack/color=feedback-warning-medium should follow no axis only, but at type=stacked, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-2/Frame#2/Bar Stack/color=feedback-warning-medium width is meant to change at type=stacked, orientation=horizontal, breakpoint=mobile.                                              |
| `component.bar chart.row2Label.gap#unbound`                                                                                      | 0, bound to no variable                                                                                                                                                  | gap on /Plot/Row-2/Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-2/Label gap to inset.none or stack.none, which has the same value.                                                                                                                  |
| `component.bar chart.row2Label.paddingBottom#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Row-2/Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-2/Label paddingBottom to inset.none, which has the same value.                                                                                                                      |
| `component.bar chart.row2Label.paddingLeft#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Row-2/Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-2/Label paddingLeft to inset.none, which has the same value.                                                                                                                        |
| `component.bar chart.row2Label.paddingRight#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Row-2/Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-2/Label paddingRight to inset.none, which has the same value.                                                                                                                       |
| `component.bar chart.row2Label.paddingTop#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Row-2/Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-2/Label paddingTop to inset.none, which has the same value.                                                                                                                         |
| `component.bar chart.row2Label.width#unbound`                                                                                    | 48, 36, bound to no variable                                                                                                                                             | width on /Plot/Row-2/Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 48, 36 is intended.                                                                                                                                       |
| `component.bar chart.row2Label.width@type=grouped, orientation=horizontal, breakpoint=mobile`                                    | 1 variant at type=grouped, orientation=horizontal, breakpoint=mobile: 36 where 48                                                                                        | width on /Plot/Row-2/Label should follow no axis only, but at type=grouped, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-2/Label width is meant to change at type=grouped, orientation=horizontal, breakpoint=mobile.                                                                                        |
| `component.bar chart.row2Label.width@type=simple, orientation=horizontal, breakpoint=mobile`                                     | 1 variant at type=simple, orientation=horizontal, breakpoint=mobile: 36 where 48                                                                                         | width on /Plot/Row-2/Label should follow no axis only, but at type=simple, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-2/Label width is meant to change at type=simple, orientation=horizontal, breakpoint=mobile.                                                                                         |
| `component.bar chart.row3.paddingBottom#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Row-3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-3 paddingBottom to inset.none, which has the same value.                                                                                                                            |
| `component.bar chart.row3.paddingLeft#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Row-3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-3 paddingLeft to inset.none, which has the same value.                                                                                                                              |
| `component.bar chart.row3.paddingRight#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Row-3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-3 paddingRight to inset.none, which has the same value.                                                                                                                             |
| `component.bar chart.row3.paddingTop#unbound`                                                                                    | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Row-3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-3 paddingTop to inset.none, which has the same value.                                                                                                                               |
| `component.bar chart.row3BarTrack.direction@type=grouped, orientation=horizontal, breakpoint=desktop`                            | 1 variant at type=grouped, orientation=horizontal, breakpoint=desktop: VERTICAL where HORIZONTAL                                                                         | direction on /Plot/Row-3/BarTrack should follow no axis only, but at type=grouped, orientation=horizontal, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-3/BarTrack direction is meant to change at type=grouped, orientation=horizontal, breakpoint=desktop.                                                                                |
| `component.bar chart.row3BarTrack.direction@type=grouped, orientation=horizontal, breakpoint=mobile`                             | 1 variant at type=grouped, orientation=horizontal, breakpoint=mobile: VERTICAL where HORIZONTAL                                                                          | direction on /Plot/Row-3/BarTrack should follow no axis only, but at type=grouped, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-3/BarTrack direction is meant to change at type=grouped, orientation=horizontal, breakpoint=mobile.                                                                                 |
| `component.bar chart.row3BarTrack.gap#unbound`                                                                                   | 0, 2, bound to no variable                                                                                                                                               | gap on /Plot/Row-3/BarTrack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-3/BarTrack gap to inset.none or stack.none, which has the same value.                                                                                                               |
| `component.bar chart.row3BarTrack.gap@type=grouped, orientation=horizontal, breakpoint=desktop`                                  | 1 variant at type=grouped, orientation=horizontal, breakpoint=desktop: 2 where 0                                                                                         | gap on /Plot/Row-3/BarTrack should follow no axis only, but at type=grouped, orientation=horizontal, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-3/BarTrack gap is meant to change at type=grouped, orientation=horizontal, breakpoint=desktop.                                                                                      |
| `component.bar chart.row3BarTrack.gap@type=grouped, orientation=horizontal, breakpoint=mobile`                                   | 1 variant at type=grouped, orientation=horizontal, breakpoint=mobile: 2 where 0                                                                                          | gap on /Plot/Row-3/BarTrack should follow no axis only, but at type=grouped, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-3/BarTrack gap is meant to change at type=grouped, orientation=horizontal, breakpoint=mobile.                                                                                       |
| `component.bar chart.row3BarTrack.paddingBottom#unbound`                                                                         | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Row-3/BarTrack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-3/BarTrack paddingBottom to inset.none, which has the same value.                                                                                                                   |
| `component.bar chart.row3BarTrack.paddingLeft#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Row-3/BarTrack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-3/BarTrack paddingLeft to inset.none, which has the same value.                                                                                                                     |
| `component.bar chart.row3BarTrack.paddingRight#unbound`                                                                          | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Row-3/BarTrack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-3/BarTrack paddingRight to inset.none, which has the same value.                                                                                                                    |
| `component.bar chart.row3BarTrack.paddingTop#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Row-3/BarTrack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-3/BarTrack paddingTop to inset.none, which has the same value.                                                                                                                      |
| `component.bar chart.row3BarTrackColorCategory02Strong.height#unbound`                                                           | 12, bound to no variable                                                                                                                                                 | height on /Plot/Row-3/BarTrack/color=category-02-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-3/BarTrack/color=category-02-strong height to icon.xs, which has the same value.                                                                                                    |
| `component.bar chart.row3BarTrackColorCategory02Strong.width#unbound`                                                            | 256, 134, bound to no variable                                                                                                                                           | width on /Plot/Row-3/BarTrack/color=category-02-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 256, 134 is intended.                                                                                                                                     |
| `component.bar chart.row3BarTrackColorCategory02Strong.width@type=grouped, orientation=horizontal, breakpoint=mobile`            | 1 variant at type=grouped, orientation=horizontal, breakpoint=mobile: 134 where 256                                                                                      | width on /Plot/Row-3/BarTrack/color=category-02-strong should follow no axis only, but at type=grouped, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-3/BarTrack/color=category-02-strong width is meant to change at type=grouped, orientation=horizontal, breakpoint=mobile.                                                            |
| `component.bar chart.row3BarTrackColorCategory06Strong.height#unbound`                                                           | 24, 12, bound to no variable                                                                                                                                             | height on /Plot/Row-3/BarTrack/color=category-06-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-3/BarTrack/color=category-06-strong height to icon.lg or icon.xs, which has the same value.                                                                                         |
| `component.bar chart.row3BarTrackColorCategory06Strong.height@type=grouped, orientation=horizontal, breakpoint=desktop`          | 1 variant at type=grouped, orientation=horizontal, breakpoint=desktop: 12 where 24                                                                                       | height on /Plot/Row-3/BarTrack/color=category-06-strong should follow no axis only, but at type=grouped, orientation=horizontal, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-3/BarTrack/color=category-06-strong height is meant to change at type=grouped, orientation=horizontal, breakpoint=desktop.                                                          |
| `component.bar chart.row3BarTrackColorCategory06Strong.height@type=grouped, orientation=horizontal, breakpoint=mobile`           | 1 variant at type=grouped, orientation=horizontal, breakpoint=mobile: 12 where 24                                                                                        | height on /Plot/Row-3/BarTrack/color=category-06-strong should follow no axis only, but at type=grouped, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-3/BarTrack/color=category-06-strong height is meant to change at type=grouped, orientation=horizontal, breakpoint=mobile.                                                           |
| `component.bar chart.row3BarTrackColorCategory06Strong.width#unbound`                                                            | 461, 240, bound to no variable                                                                                                                                           | width on /Plot/Row-3/BarTrack/color=category-06-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 461, 240 is intended.                                                                                                                                     |
| `component.bar chart.row3BarTrackColorCategory06Strong.width@type=grouped, orientation=horizontal, breakpoint=mobile`            | 1 variant at type=grouped, orientation=horizontal, breakpoint=mobile: 240 where 461                                                                                      | width on /Plot/Row-3/BarTrack/color=category-06-strong should follow no axis only, but at type=grouped, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-3/BarTrack/color=category-06-strong width is meant to change at type=grouped, orientation=horizontal, breakpoint=mobile.                                                            |
| `component.bar chart.row3BarTrackColorCategory06Strong.width@type=simple, orientation=horizontal, breakpoint=mobile`             | 1 variant at type=simple, orientation=horizontal, breakpoint=mobile: 240 where 461                                                                                       | width on /Plot/Row-3/BarTrack/color=category-06-strong should follow no axis only, but at type=simple, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-3/BarTrack/color=category-06-strong width is meant to change at type=simple, orientation=horizontal, breakpoint=mobile.                                                             |
| `component.bar chart.row3Frame.gap#unbound`                                                                                      | 0, bound to no variable                                                                                                                                                  | gap on /Plot/Row-3/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-3/Frame gap to inset.none or stack.none, which has the same value.                                                                                                                  |
| `component.bar chart.row3Frame.paddingBottom#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Row-3/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-3/Frame paddingBottom to inset.none, which has the same value.                                                                                                                      |
| `component.bar chart.row3Frame.paddingLeft#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Row-3/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-3/Frame paddingLeft to inset.none, which has the same value.                                                                                                                        |
| `component.bar chart.row3Frame.paddingRight#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Row-3/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-3/Frame paddingRight to inset.none, which has the same value.                                                                                                                       |
| `component.bar chart.row3Frame.paddingTop#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Row-3/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-3/Frame paddingTop to inset.none, which has the same value.                                                                                                                         |
| `component.bar chart.row3Frame.width#unbound`                                                                                    | 48, 36, bound to no variable                                                                                                                                             | width on /Plot/Row-3/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 48, 36 is intended.                                                                                                                                       |
| `component.bar chart.row3Frame.width@type=stacked, orientation=horizontal, breakpoint=mobile`                                    | 1 variant at type=stacked, orientation=horizontal, breakpoint=mobile: 36 where 48                                                                                        | width on /Plot/Row-3/Frame should follow no axis only, but at type=stacked, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-3/Frame width is meant to change at type=stacked, orientation=horizontal, breakpoint=mobile.                                                                                        |
| `component.bar chart.row3Frame2.gap#unbound`                                                                                     | 0, bound to no variable                                                                                                                                                  | gap on /Plot/Row-3/Frame#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-3/Frame#2 gap to inset.none or stack.none, which has the same value.                                                                                                                |
| `component.bar chart.row3Frame2.paddingBottom#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Row-3/Frame#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-3/Frame#2 paddingBottom to inset.none, which has the same value.                                                                                                                    |
| `component.bar chart.row3Frame2.paddingLeft#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Row-3/Frame#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-3/Frame#2 paddingLeft to inset.none, which has the same value.                                                                                                                      |
| `component.bar chart.row3Frame2.paddingRight#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Row-3/Frame#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-3/Frame#2 paddingRight to inset.none, which has the same value.                                                                                                                     |
| `component.bar chart.row3Frame2.paddingTop#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Row-3/Frame#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-3/Frame#2 paddingTop to inset.none, which has the same value.                                                                                                                       |
| `component.bar chart.row3Frame2.radius@type=stacked, orientation=horizontal, breakpoint=mobile`                                  | 1 variant at type=stacked, orientation=horizontal, breakpoint=mobile: none where radius.none                                                                             | radius on /Plot/Row-3/Frame#2 should follow no axis only, but at type=stacked, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-3/Frame#2 radius is meant to change at type=stacked, orientation=horizontal, breakpoint=mobile.                                                                                     |
| `component.bar chart.row3Frame2BarStack.gap#unbound`                                                                             | 2, bound to no variable                                                                                                                                                  | gap on /Plot/Row-3/Frame#2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no gap token; ask SOLAR for one, or confirm 2 is intended.                                                                                                                                                      |
| `component.bar chart.row3Frame2BarStack.height#unbound`                                                                          | 24, bound to no variable                                                                                                                                                 | height on /Plot/Row-3/Frame#2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 24 is intended.                                                                                                                                          |
| `component.bar chart.row3Frame2BarStack.paddingBottom#unbound`                                                                   | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Row-3/Frame#2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-3/Frame#2/Bar Stack paddingBottom to inset.none, which has the same value.                                                                                                          |
| `component.bar chart.row3Frame2BarStack.paddingLeft#unbound`                                                                     | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Row-3/Frame#2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-3/Frame#2/Bar Stack paddingLeft to inset.none, which has the same value.                                                                                                            |
| `component.bar chart.row3Frame2BarStack.paddingRight#unbound`                                                                    | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Row-3/Frame#2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-3/Frame#2/Bar Stack paddingRight to inset.none, which has the same value.                                                                                                           |
| `component.bar chart.row3Frame2BarStack.paddingTop#unbound`                                                                      | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Row-3/Frame#2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-3/Frame#2/Bar Stack paddingTop to inset.none, which has the same value.                                                                                                             |
| `component.bar chart.row3Frame2BarStack.width#unbound`                                                                           | 490, 269, bound to no variable                                                                                                                                           | width on /Plot/Row-3/Frame#2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 490, 269 is intended.                                                                                                                                     |
| `component.bar chart.row3Frame2BarStack.width@type=stacked, orientation=horizontal, breakpoint=mobile`                           | 1 variant at type=stacked, orientation=horizontal, breakpoint=mobile: 269 where 490                                                                                      | width on /Plot/Row-3/Frame#2/Bar Stack should follow no axis only, but at type=stacked, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-3/Frame#2/Bar Stack width is meant to change at type=stacked, orientation=horizontal, breakpoint=mobile.                                                                            |
| `component.bar chart.row3Frame2BarStackColorFeedbackDangerStrong.width#unbound`                                                  | 73, 40, bound to no variable                                                                                                                                             | width on /Plot/Row-3/Frame#2/Bar Stack/color=feedback-danger-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 73, 40 is intended.                                                                                                                                       |
| `component.bar chart.row3Frame2BarStackColorFeedbackDangerStrong.width@type=stacked, orientation=horizontal, breakpoint=mobile`  | 1 variant at type=stacked, orientation=horizontal, breakpoint=mobile: 40 where 73                                                                                        | width on /Plot/Row-3/Frame#2/Bar Stack/color=feedback-danger-strong should follow no axis only, but at type=stacked, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-3/Frame#2/Bar Stack/color=feedback-danger-strong width is meant to change at type=stacked, orientation=horizontal, breakpoint=mobile.                                               |
| `component.bar chart.row3Frame2BarStackColorFeedbackNeutralSubtle.width#unbound`                                                 | 243, 133, bound to no variable                                                                                                                                           | width on /Plot/Row-3/Frame#2/Bar Stack/color=feedback-neutral-subtle is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 243, 133 is intended.                                                                                                                                     |
| `component.bar chart.row3Frame2BarStackColorFeedbackNeutralSubtle.width@type=stacked, orientation=horizontal, breakpoint=mobile` | 1 variant at type=stacked, orientation=horizontal, breakpoint=mobile: 133 where 243                                                                                      | width on /Plot/Row-3/Frame#2/Bar Stack/color=feedback-neutral-subtle should follow no axis only, but at type=stacked, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-3/Frame#2/Bar Stack/color=feedback-neutral-subtle width is meant to change at type=stacked, orientation=horizontal, breakpoint=mobile.                                              |
| `component.bar chart.row3Frame2BarStackColorFeedbackWarningMedium.width#unbound`                                                 | 170, 93, bound to no variable                                                                                                                                            | width on /Plot/Row-3/Frame#2/Bar Stack/color=feedback-warning-medium is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 170, 93 is intended.                                                                                                                                      |
| `component.bar chart.row3Frame2BarStackColorFeedbackWarningMedium.width@type=stacked, orientation=horizontal, breakpoint=mobile` | 1 variant at type=stacked, orientation=horizontal, breakpoint=mobile: 93 where 170                                                                                       | width on /Plot/Row-3/Frame#2/Bar Stack/color=feedback-warning-medium should follow no axis only, but at type=stacked, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-3/Frame#2/Bar Stack/color=feedback-warning-medium width is meant to change at type=stacked, orientation=horizontal, breakpoint=mobile.                                              |
| `component.bar chart.row3Label.gap#unbound`                                                                                      | 0, bound to no variable                                                                                                                                                  | gap on /Plot/Row-3/Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-3/Label gap to inset.none or stack.none, which has the same value.                                                                                                                  |
| `component.bar chart.row3Label.paddingBottom#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Row-3/Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-3/Label paddingBottom to inset.none, which has the same value.                                                                                                                      |
| `component.bar chart.row3Label.paddingLeft#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Row-3/Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-3/Label paddingLeft to inset.none, which has the same value.                                                                                                                        |
| `component.bar chart.row3Label.paddingRight#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Row-3/Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-3/Label paddingRight to inset.none, which has the same value.                                                                                                                       |
| `component.bar chart.row3Label.paddingTop#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Row-3/Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-3/Label paddingTop to inset.none, which has the same value.                                                                                                                         |
| `component.bar chart.row3Label.width#unbound`                                                                                    | 48, 36, bound to no variable                                                                                                                                             | width on /Plot/Row-3/Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 48, 36 is intended.                                                                                                                                       |
| `component.bar chart.row3Label.width@type=grouped, orientation=horizontal, breakpoint=mobile`                                    | 1 variant at type=grouped, orientation=horizontal, breakpoint=mobile: 36 where 48                                                                                        | width on /Plot/Row-3/Label should follow no axis only, but at type=grouped, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-3/Label width is meant to change at type=grouped, orientation=horizontal, breakpoint=mobile.                                                                                        |
| `component.bar chart.row3Label.width@type=simple, orientation=horizontal, breakpoint=mobile`                                     | 1 variant at type=simple, orientation=horizontal, breakpoint=mobile: 36 where 48                                                                                         | width on /Plot/Row-3/Label should follow no axis only, but at type=simple, orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-3/Label width is meant to change at type=simple, orientation=horizontal, breakpoint=mobile.                                                                                         |
| `component.bar chart.row4.paddingBottom#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Row-4 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-4 paddingBottom to inset.none, which has the same value.                                                                                                                            |
| `component.bar chart.row4.paddingLeft#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Row-4 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-4 paddingLeft to inset.none, which has the same value.                                                                                                                              |
| `component.bar chart.row4.paddingRight#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Row-4 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-4 paddingRight to inset.none, which has the same value.                                                                                                                             |
| `component.bar chart.row4.paddingTop#unbound`                                                                                    | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Row-4 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-4 paddingTop to inset.none, which has the same value.                                                                                                                               |
| `component.bar chart.row4BarTrack.direction@type=grouped, orientation=horizontal, breakpoint=desktop`                            | 1 variant at type=grouped, orientation=horizontal, breakpoint=desktop: VERTICAL where HORIZONTAL                                                                         | direction on /Plot/Row-4/BarTrack should follow no axis only, but at type=grouped, orientation=horizontal, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-4/BarTrack direction is meant to change at type=grouped, orientation=horizontal, breakpoint=desktop.                                                                                |
| `component.bar chart.row4BarTrack.gap#unbound`                                                                                   | 0, 2, bound to no variable                                                                                                                                               | gap on /Plot/Row-4/BarTrack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-4/BarTrack gap to inset.none or stack.none, which has the same value.                                                                                                               |
| `component.bar chart.row4BarTrack.gap@type=grouped, orientation=horizontal, breakpoint=desktop`                                  | 1 variant at type=grouped, orientation=horizontal, breakpoint=desktop: 2 where 0                                                                                         | gap on /Plot/Row-4/BarTrack should follow no axis only, but at type=grouped, orientation=horizontal, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-4/BarTrack gap is meant to change at type=grouped, orientation=horizontal, breakpoint=desktop.                                                                                      |
| `component.bar chart.row4BarTrack.paddingBottom#unbound`                                                                         | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Row-4/BarTrack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-4/BarTrack paddingBottom to inset.none, which has the same value.                                                                                                                   |
| `component.bar chart.row4BarTrack.paddingLeft#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Row-4/BarTrack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-4/BarTrack paddingLeft to inset.none, which has the same value.                                                                                                                     |
| `component.bar chart.row4BarTrack.paddingRight#unbound`                                                                          | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Row-4/BarTrack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-4/BarTrack paddingRight to inset.none, which has the same value.                                                                                                                    |
| `component.bar chart.row4BarTrack.paddingTop#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Row-4/BarTrack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-4/BarTrack paddingTop to inset.none, which has the same value.                                                                                                                      |
| `component.bar chart.row4BarTrackColorCategory02Strong.height#unbound`                                                           | 12, bound to no variable                                                                                                                                                 | height on /Plot/Row-4/BarTrack/color=category-02-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-4/BarTrack/color=category-02-strong height to icon.xs, which has the same value.                                                                                                    |
| `component.bar chart.row4BarTrackColorCategory02Strong.width#unbound`                                                            | 410, bound to no variable                                                                                                                                                | width on /Plot/Row-4/BarTrack/color=category-02-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 410 is intended.                                                                                                                                          |
| `component.bar chart.row4BarTrackColorCategory06Strong.height#unbound`                                                           | 24, 12, bound to no variable                                                                                                                                             | height on /Plot/Row-4/BarTrack/color=category-06-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-4/BarTrack/color=category-06-strong height to icon.lg or icon.xs, which has the same value.                                                                                         |
| `component.bar chart.row4BarTrackColorCategory06Strong.height@type=grouped, orientation=horizontal, breakpoint=desktop`          | 1 variant at type=grouped, orientation=horizontal, breakpoint=desktop: 12 where 24                                                                                       | height on /Plot/Row-4/BarTrack/color=category-06-strong should follow no axis only, but at type=grouped, orientation=horizontal, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-4/BarTrack/color=category-06-strong height is meant to change at type=grouped, orientation=horizontal, breakpoint=desktop.                                                          |
| `component.bar chart.row4BarTrackColorCategory06Strong.width#unbound`                                                            | 343, bound to no variable                                                                                                                                                | width on /Plot/Row-4/BarTrack/color=category-06-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 343 is intended.                                                                                                                                          |
| `component.bar chart.row4Frame.gap#unbound`                                                                                      | 0, bound to no variable                                                                                                                                                  | gap on /Plot/Row-4/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-4/Frame gap to inset.none or stack.none, which has the same value.                                                                                                                  |
| `component.bar chart.row4Frame.paddingBottom#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Row-4/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-4/Frame paddingBottom to inset.none, which has the same value.                                                                                                                      |
| `component.bar chart.row4Frame.paddingLeft#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Row-4/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-4/Frame paddingLeft to inset.none, which has the same value.                                                                                                                        |
| `component.bar chart.row4Frame.paddingRight#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Row-4/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-4/Frame paddingRight to inset.none, which has the same value.                                                                                                                       |
| `component.bar chart.row4Frame.paddingTop#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Row-4/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-4/Frame paddingTop to inset.none, which has the same value.                                                                                                                         |
| `component.bar chart.row4Frame.width#unbound`                                                                                    | 48, bound to no variable                                                                                                                                                 | width on /Plot/Row-4/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 48 is intended.                                                                                                                                           |
| `component.bar chart.row4Frame2.gap#unbound`                                                                                     | 0, bound to no variable                                                                                                                                                  | gap on /Plot/Row-4/Frame#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-4/Frame#2 gap to inset.none or stack.none, which has the same value.                                                                                                                |
| `component.bar chart.row4Frame2.paddingBottom#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Row-4/Frame#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-4/Frame#2 paddingBottom to inset.none, which has the same value.                                                                                                                    |
| `component.bar chart.row4Frame2.paddingLeft#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Row-4/Frame#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-4/Frame#2 paddingLeft to inset.none, which has the same value.                                                                                                                      |
| `component.bar chart.row4Frame2.paddingRight#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Row-4/Frame#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-4/Frame#2 paddingRight to inset.none, which has the same value.                                                                                                                     |
| `component.bar chart.row4Frame2.paddingTop#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Row-4/Frame#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-4/Frame#2 paddingTop to inset.none, which has the same value.                                                                                                                       |
| `component.bar chart.row4Frame2BarStack.gap#unbound`                                                                             | 2, bound to no variable                                                                                                                                                  | gap on /Plot/Row-4/Frame#2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no gap token; ask SOLAR for one, or confirm 2 is intended.                                                                                                                                                      |
| `component.bar chart.row4Frame2BarStack.height#unbound`                                                                          | 24, bound to no variable                                                                                                                                                 | height on /Plot/Row-4/Frame#2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 24 is intended.                                                                                                                                          |
| `component.bar chart.row4Frame2BarStack.paddingBottom#unbound`                                                                   | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Row-4/Frame#2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-4/Frame#2/Bar Stack paddingBottom to inset.none, which has the same value.                                                                                                          |
| `component.bar chart.row4Frame2BarStack.paddingLeft#unbound`                                                                     | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Row-4/Frame#2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-4/Frame#2/Bar Stack paddingLeft to inset.none, which has the same value.                                                                                                            |
| `component.bar chart.row4Frame2BarStack.paddingRight#unbound`                                                                    | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Row-4/Frame#2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-4/Frame#2/Bar Stack paddingRight to inset.none, which has the same value.                                                                                                           |
| `component.bar chart.row4Frame2BarStack.paddingTop#unbound`                                                                      | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Row-4/Frame#2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-4/Frame#2/Bar Stack paddingTop to inset.none, which has the same value.                                                                                                             |
| `component.bar chart.row4Frame2BarStack.width#unbound`                                                                           | 364, bound to no variable                                                                                                                                                | width on /Plot/Row-4/Frame#2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 364 is intended.                                                                                                                                          |
| `component.bar chart.row4Frame2BarStackColorFeedbackDangerStrong.width#unbound`                                                  | 90, bound to no variable                                                                                                                                                 | width on /Plot/Row-4/Frame#2/Bar Stack/color=feedback-danger-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 90 is intended.                                                                                                                                           |
| `component.bar chart.row4Frame2BarStackColorFeedbackNeutralSubtle.width#unbound`                                                 | 180, bound to no variable                                                                                                                                                | width on /Plot/Row-4/Frame#2/Bar Stack/color=feedback-neutral-subtle is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 180 is intended.                                                                                                                                          |
| `component.bar chart.row4Frame2BarStackColorFeedbackWarningMedium.width#unbound`                                                 | 90, bound to no variable                                                                                                                                                 | width on /Plot/Row-4/Frame#2/Bar Stack/color=feedback-warning-medium is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 90 is intended.                                                                                                                                           |
| `component.bar chart.row4Label.gap#unbound`                                                                                      | 0, bound to no variable                                                                                                                                                  | gap on /Plot/Row-4/Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-4/Label gap to inset.none or stack.none, which has the same value.                                                                                                                  |
| `component.bar chart.row4Label.paddingBottom#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Row-4/Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-4/Label paddingBottom to inset.none, which has the same value.                                                                                                                      |
| `component.bar chart.row4Label.paddingLeft#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Row-4/Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-4/Label paddingLeft to inset.none, which has the same value.                                                                                                                        |
| `component.bar chart.row4Label.paddingRight#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Row-4/Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-4/Label paddingRight to inset.none, which has the same value.                                                                                                                       |
| `component.bar chart.row4Label.paddingTop#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Row-4/Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-4/Label paddingTop to inset.none, which has the same value.                                                                                                                         |
| `component.bar chart.row4Label.width#unbound`                                                                                    | 48, bound to no variable                                                                                                                                                 | width on /Plot/Row-4/Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 48 is intended.                                                                                                                                           |
| `component.bar chart.row5.paddingBottom#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Row-5 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-5 paddingBottom to inset.none, which has the same value.                                                                                                                            |
| `component.bar chart.row5.paddingLeft#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Row-5 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-5 paddingLeft to inset.none, which has the same value.                                                                                                                              |
| `component.bar chart.row5.paddingRight#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Row-5 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-5 paddingRight to inset.none, which has the same value.                                                                                                                             |
| `component.bar chart.row5.paddingTop#unbound`                                                                                    | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Row-5 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-5 paddingTop to inset.none, which has the same value.                                                                                                                               |
| `component.bar chart.row5BarTrack.direction@type=grouped, orientation=horizontal, breakpoint=desktop`                            | 1 variant at type=grouped, orientation=horizontal, breakpoint=desktop: VERTICAL where HORIZONTAL                                                                         | direction on /Plot/Row-5/BarTrack should follow no axis only, but at type=grouped, orientation=horizontal, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-5/BarTrack direction is meant to change at type=grouped, orientation=horizontal, breakpoint=desktop.                                                                                |
| `component.bar chart.row5BarTrack.gap#unbound`                                                                                   | 0, 2, bound to no variable                                                                                                                                               | gap on /Plot/Row-5/BarTrack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-5/BarTrack gap to inset.none or stack.none, which has the same value.                                                                                                               |
| `component.bar chart.row5BarTrack.gap@type=grouped, orientation=horizontal, breakpoint=desktop`                                  | 1 variant at type=grouped, orientation=horizontal, breakpoint=desktop: 2 where 0                                                                                         | gap on /Plot/Row-5/BarTrack should follow no axis only, but at type=grouped, orientation=horizontal, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-5/BarTrack gap is meant to change at type=grouped, orientation=horizontal, breakpoint=desktop.                                                                                      |
| `component.bar chart.row5BarTrack.paddingBottom#unbound`                                                                         | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Row-5/BarTrack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-5/BarTrack paddingBottom to inset.none, which has the same value.                                                                                                                   |
| `component.bar chart.row5BarTrack.paddingLeft#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Row-5/BarTrack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-5/BarTrack paddingLeft to inset.none, which has the same value.                                                                                                                     |
| `component.bar chart.row5BarTrack.paddingRight#unbound`                                                                          | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Row-5/BarTrack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-5/BarTrack paddingRight to inset.none, which has the same value.                                                                                                                    |
| `component.bar chart.row5BarTrack.paddingTop#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Row-5/BarTrack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-5/BarTrack paddingTop to inset.none, which has the same value.                                                                                                                      |
| `component.bar chart.row5BarTrackColorCategory02Strong.height#unbound`                                                           | 12, bound to no variable                                                                                                                                                 | height on /Plot/Row-5/BarTrack/color=category-02-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-5/BarTrack/color=category-02-strong height to icon.xs, which has the same value.                                                                                                    |
| `component.bar chart.row5BarTrackColorCategory02Strong.width#unbound`                                                            | 179, bound to no variable                                                                                                                                                | width on /Plot/Row-5/BarTrack/color=category-02-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 179 is intended.                                                                                                                                          |
| `component.bar chart.row5BarTrackColorCategory06Strong.height#unbound`                                                           | 24, 12, bound to no variable                                                                                                                                             | height on /Plot/Row-5/BarTrack/color=category-06-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-5/BarTrack/color=category-06-strong height to icon.lg or icon.xs, which has the same value.                                                                                         |
| `component.bar chart.row5BarTrackColorCategory06Strong.height@type=grouped, orientation=horizontal, breakpoint=desktop`          | 1 variant at type=grouped, orientation=horizontal, breakpoint=desktop: 12 where 24                                                                                       | height on /Plot/Row-5/BarTrack/color=category-06-strong should follow no axis only, but at type=grouped, orientation=horizontal, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR whether Bar Chart /Plot/Row-5/BarTrack/color=category-06-strong height is meant to change at type=grouped, orientation=horizontal, breakpoint=desktop.                                                          |
| `component.bar chart.row5BarTrackColorCategory06Strong.width#unbound`                                                            | 282, bound to no variable                                                                                                                                                | width on /Plot/Row-5/BarTrack/color=category-06-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 282 is intended.                                                                                                                                          |
| `component.bar chart.row5Frame.gap#unbound`                                                                                      | 0, bound to no variable                                                                                                                                                  | gap on /Plot/Row-5/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-5/Frame gap to inset.none or stack.none, which has the same value.                                                                                                                  |
| `component.bar chart.row5Frame.paddingBottom#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Row-5/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-5/Frame paddingBottom to inset.none, which has the same value.                                                                                                                      |
| `component.bar chart.row5Frame.paddingLeft#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Row-5/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-5/Frame paddingLeft to inset.none, which has the same value.                                                                                                                        |
| `component.bar chart.row5Frame.paddingRight#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Row-5/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-5/Frame paddingRight to inset.none, which has the same value.                                                                                                                       |
| `component.bar chart.row5Frame.paddingTop#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Row-5/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-5/Frame paddingTop to inset.none, which has the same value.                                                                                                                         |
| `component.bar chart.row5Frame.width#unbound`                                                                                    | 48, bound to no variable                                                                                                                                                 | width on /Plot/Row-5/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 48 is intended.                                                                                                                                           |
| `component.bar chart.row5Frame2.gap#unbound`                                                                                     | 0, bound to no variable                                                                                                                                                  | gap on /Plot/Row-5/Frame#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-5/Frame#2 gap to inset.none or stack.none, which has the same value.                                                                                                                |
| `component.bar chart.row5Frame2.paddingBottom#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Row-5/Frame#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-5/Frame#2 paddingBottom to inset.none, which has the same value.                                                                                                                    |
| `component.bar chart.row5Frame2.paddingLeft#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Row-5/Frame#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-5/Frame#2 paddingLeft to inset.none, which has the same value.                                                                                                                      |
| `component.bar chart.row5Frame2.paddingRight#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Row-5/Frame#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-5/Frame#2 paddingRight to inset.none, which has the same value.                                                                                                                     |
| `component.bar chart.row5Frame2.paddingTop#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Row-5/Frame#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-5/Frame#2 paddingTop to inset.none, which has the same value.                                                                                                                       |
| `component.bar chart.row5Frame2BarStack.gap#unbound`                                                                             | 2, bound to no variable                                                                                                                                                  | gap on /Plot/Row-5/Frame#2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no gap token; ask SOLAR for one, or confirm 2 is intended.                                                                                                                                                      |
| `component.bar chart.row5Frame2BarStack.height#unbound`                                                                          | 24, bound to no variable                                                                                                                                                 | height on /Plot/Row-5/Frame#2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 24 is intended.                                                                                                                                          |
| `component.bar chart.row5Frame2BarStack.paddingBottom#unbound`                                                                   | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Row-5/Frame#2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-5/Frame#2/Bar Stack paddingBottom to inset.none, which has the same value.                                                                                                          |
| `component.bar chart.row5Frame2BarStack.paddingLeft#unbound`                                                                     | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Row-5/Frame#2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-5/Frame#2/Bar Stack paddingLeft to inset.none, which has the same value.                                                                                                            |
| `component.bar chart.row5Frame2BarStack.paddingRight#unbound`                                                                    | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Row-5/Frame#2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-5/Frame#2/Bar Stack paddingRight to inset.none, which has the same value.                                                                                                           |
| `component.bar chart.row5Frame2BarStack.paddingTop#unbound`                                                                      | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Row-5/Frame#2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-5/Frame#2/Bar Stack paddingTop to inset.none, which has the same value.                                                                                                             |
| `component.bar chart.row5Frame2BarStack.width#unbound`                                                                           | 299, bound to no variable                                                                                                                                                | width on /Plot/Row-5/Frame#2/Bar Stack is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 299 is intended.                                                                                                                                          |
| `component.bar chart.row5Frame2BarStackColorFeedbackDangerStrong.width#unbound`                                                  | 118, bound to no variable                                                                                                                                                | width on /Plot/Row-5/Frame#2/Bar Stack/color=feedback-danger-strong is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 118 is intended.                                                                                                                                          |
| `component.bar chart.row5Frame2BarStackColorFeedbackNeutralSubtle.width#unbound`                                                 | 89, bound to no variable                                                                                                                                                 | width on /Plot/Row-5/Frame#2/Bar Stack/color=feedback-neutral-subtle is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 89 is intended.                                                                                                                                           |
| `component.bar chart.row5Frame2BarStackColorFeedbackWarningMedium.width#unbound`                                                 | 89, bound to no variable                                                                                                                                                 | width on /Plot/Row-5/Frame#2/Bar Stack/color=feedback-warning-medium is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 89 is intended.                                                                                                                                           |
| `component.bar chart.row5Label.gap#unbound`                                                                                      | 0, bound to no variable                                                                                                                                                  | gap on /Plot/Row-5/Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-5/Label gap to inset.none or stack.none, which has the same value.                                                                                                                  |
| `component.bar chart.row5Label.paddingBottom#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Plot/Row-5/Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-5/Label paddingBottom to inset.none, which has the same value.                                                                                                                      |
| `component.bar chart.row5Label.paddingLeft#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Plot/Row-5/Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-5/Label paddingLeft to inset.none, which has the same value.                                                                                                                        |
| `component.bar chart.row5Label.paddingRight#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingRight on /Plot/Row-5/Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-5/Label paddingRight to inset.none, which has the same value.                                                                                                                       |
| `component.bar chart.row5Label.paddingTop#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingTop on /Plot/Row-5/Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. Ask SOLAR to bind Bar Chart /Plot/Row-5/Label paddingTop to inset.none, which has the same value.                                                                                                                         |
| `component.bar chart.row5Label.width#unbound`                                                                                    | 48, bound to no variable                                                                                                                                                 | width on /Plot/Row-5/Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's plot is sample data: its bars' lengths and places, its columns and labels, laid out for six months at two sizes. The library lays out the caller's data; the theme takes the design (colours, strokes, text styles), none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 48 is intended.                                                                                                                                           |
| `component.bar stack.root.gap#unbound`                                                                                           | 2, bound to no variable                                                                                                                                                  | gap on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (set):** Figma's default stack parts its segments by inset.2xs (4), and the other five by an unbound 2, which no spacing token is: the governed one, in every stack. The design review asks which is meant. SOLAR has no gap token; ask SOLAR for one, or confirm 2 is intended.                                                                                                                                                                                                               |
| `component.bar stack.root.height#unbound`                                                                                        | 80, 32, bound to no variable                                                                                                                                             | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (set):** A stack is as wide and as long as its chart makes it: Figma draws a 32 × 80 sample. SOLAR has no control height token; ask SOLAR for one, or confirm 80, 32 is intended.                                                                                                                                                                                                                                                                                                              |
| `component.bar stack.root.height@orientation=horizontal`                                                                         | 1 variant at orientation=horizontal: 32 where 80                                                                                                                         | height on / should follow no axis only, but at orientation=horizontal it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (accept):** A stack is as wide and as long as its chart makes it: Figma draws a 32 × 80 sample. Ask SOLAR whether Bar Stack / height is meant to change at orientation=horizontal.                                                                                                                                                                                                                                                                                                             |
| `component.bar stack.root.paddingBottom#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Bar Stack / paddingBottom to inset.none, which has the same value.                                                   |
| `component.bar stack.root.paddingLeft#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Bar Stack / paddingLeft to inset.none, which has the same value.                                                     |
| `component.bar stack.root.paddingRight#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Bar Stack / paddingRight to inset.none, which has the same value.                                                    |
| `component.bar stack.root.paddingTop#unbound`                                                                                    | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Bar Stack / paddingTop to inset.none, which has the same value.                                                      |
| `component.bar stack.root.width#unbound`                                                                                         | 32, 80, bound to no variable                                                                                                                                             | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** A stack is as wide and as long as its chart makes it: Figma draws a 32 × 80 sample. SOLAR has no control width token; ask SOLAR for one, or confirm 32, 80 is intended.                                                                                                                                                                                                                                                                                                               |
| `component.bar stack.root.width@orientation=horizontal`                                                                          | 1 variant at orientation=horizontal: 80 where 32                                                                                                                         | width on / should follow no axis only, but at orientation=horizontal it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (accept):** A stack is as wide and as long as its chart makes it: Figma draws a 32 × 80 sample. Ask SOLAR whether Bar Stack / width is meant to change at orientation=horizontal.                                                                                                                                                                                                                                                                                                              |
| `component.bar.root.height#unbound`                                                                                              | 80, bound to no variable                                                                                                                                                 | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (set):** A bar is as wide and as long as the chart makes it, by its data: Figma draws a 32 × 80 sample. SOLAR has no control height token; ask SOLAR for one, or confirm 80 is intended.                                                                                                                                                                                                                                                                                                       |
| `component.bar.root.width#unbound`                                                                                               | 32, bound to no variable                                                                                                                                                 | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** A bar is as wide and as long as the chart makes it, by its data: Figma draws a 32 × 80 sample. SOLAR has no control width token; ask SOLAR for one, or confirm 32 is intended.                                                                                                                                                                                                                                                                                                        |
| `component.breadcrumbs.iconChevronRight.width#unbound`                                                                           | 12, bound to no variable                                                                                                                                                 | width on /Icon/ChevronRight is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind):** Same value, governed token; the chevron's height is already bound to it. Ask SOLAR to bind Breadcrumbs /Icon/ChevronRight width to icon.xs, which has the same value.                                                                                                                                                                                                                                                                                                                |
| `component.breadcrumbs.iconChevronRight2.width#unbound`                                                                          | 12, bound to no variable                                                                                                                                                 | width on /Icon/ChevronRight#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (bind):** Same value, governed token; the chevron's height is already bound to it. Ask SOLAR to bind Breadcrumbs /Icon/ChevronRight#2 width to icon.xs, which has the same value.                                                                                                                                                                                                                                                                                                              |
| `component.breadcrumbs.iconChevronRight3.width#unbound`                                                                          | 12, bound to no variable                                                                                                                                                 | width on /Icon/ChevronRight#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (bind):** Same value, governed token; the chevron's height is already bound to it. Ask SOLAR to bind Breadcrumbs /Icon/ChevronRight#3 width to icon.xs, which has the same value.                                                                                                                                                                                                                                                                                                              |
| `component.breadcrumbs.iconChevronRight4.width#unbound`                                                                          | 12, bound to no variable                                                                                                                                                 | width on /Icon/ChevronRight#4 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (bind):** Same value, governed token; the chevron's height is already bound to it. Ask SOLAR to bind Breadcrumbs /Icon/ChevronRight#4 width to icon.xs, which has the same value.                                                                                                                                                                                                                                                                                                              |
| `component.breadcrumbs.root.paddingBottom#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Breadcrumbs / paddingBottom to inset.none, which has the same value.                                                 |
| `component.breadcrumbs.root.paddingLeft#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Breadcrumbs / paddingLeft to inset.none, which has the same value.                                                   |
| `component.breadcrumbs.root.paddingRight#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Breadcrumbs / paddingRight to inset.none, which has the same value.                                                  |
| `component.breadcrumbs.root.paddingTop#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Breadcrumbs / paddingTop to inset.none, which has the same value.                                                    |
| `component.button group.button3.height#unbound`                                                                                  | 40, bound to no variable                                                                                                                                                 | height on /Button#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (set):** As the tertiary one, the Button's own height. SOLAR has no control height token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                                                                                                                                        |
| `component.button group.button3.width#unbound`                                                                                   | 431, bound to no variable                                                                                                                                                | width on /Button#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (set):** As the tertiary one: hidden in the vertical group, where it keeps a fixed 431; every shown button fills. SOLAR has no control width token; ask SOLAR for one, or confirm 431 is intended.                                                                                                                                                                                                                                                                                             |
| `component.button group.root.paddingBottom#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Button Group / paddingBottom to inset.none, which has the same value.                                                |
| `component.button group.root.paddingLeft#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Button Group / paddingLeft to inset.none, which has the same value.                                                  |
| `component.button group.root.paddingRight#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Button Group / paddingRight to inset.none, which has the same value.                                                 |
| `component.button group.root.paddingTop#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Button Group / paddingTop to inset.none, which has the same value.                                                   |
| `component.button group.secondaryCTA.height#unbound`                                                                             | 40, 48, bound to no variable                                                                                                                                             | height on /Button#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (set):** As the tertiary one, the Button's own height. SOLAR has no control height token; ask SOLAR for one, or confirm 40, 48 is intended.                                                                                                                                                                                                                                                                                                                                                    |
| `component.button group.secondaryCTA.height@orientation=horizontal, type=full-width`                                             | 1 variant at orientation=horizontal, type=full-width: 48 where 40                                                                                                        | height on /Button#2 should follow no axis only, but at orientation=horizontal, type=full-width it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** As the tertiary one, the Button's own height. Ask SOLAR whether Button Group /Button#2 height is meant to change at orientation=horizontal, type=full-width.                                                                                                                                                                                                                                                                                                                       |
| `component.button group.secondaryCTA.variant.size@orientation=horizontal, type=full-width`                                       | 1 variant at orientation=horizontal, type=full-width: lg where md                                                                                                        | variant.size on /Button#2 should follow no axis only, but at orientation=horizontal, type=full-width it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (accept):** As the tertiary one, the caller's size. Ask SOLAR whether Button Group /Button#2 variant.size is meant to change at orientation=horizontal, type=full-width.                                                                                                                                                                                                                                                                                                                       |
| `component.button group.tertiaryCTA.height#unbound`                                                                              | 40, 48, bound to no variable                                                                                                                                             | height on /Button is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (set):** A child's height is the Button's own (40 at md, 48 at lg); the group decides its width, not its height, so a caller's sm Buttons stay 32. SOLAR has no control height token; ask SOLAR for one, or confirm 40, 48 is intended.                                                                                                                                                                                                                                                        |
| `component.button group.tertiaryCTA.height@orientation=horizontal, type=full-width`                                              | 1 variant at orientation=horizontal, type=full-width: 48 where 40                                                                                                        | height on /Button should follow no axis only, but at orientation=horizontal, type=full-width it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's full-width example uses lg Buttons, 48 tall; the height is the Button's own, which the group does not set. Ask SOLAR whether Button Group /Button height is meant to change at orientation=horizontal, type=full-width.                                                                                                                                                                                                                                                    |
| `component.button group.tertiaryCTA.variant.size@orientation=horizontal, type=full-width`                                        | 1 variant at orientation=horizontal, type=full-width: lg where md                                                                                                        | variant.size on /Button should follow no axis only, but at orientation=horizontal, type=full-width it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** The buttons are the caller's, whose size the group never changes; Figma's full-width example happens to use lg. Ask SOLAR whether Button Group /Button variant.size is meant to change at orientation=horizontal, type=full-width.                                                                                                                                                                                                                                                 |
| `component.button group.tertiaryCTA.width#unbound`                                                                               | 138, bound to no variable                                                                                                                                                | width on /Button is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (set):** The buttons fill the group in every variant Figma draws. This one is hidden at rest (the TertiaryCTA prop shows it) and keeps a fixed 138 from before it was hidden, which is not how a shown button is drawn. SOLAR has no control width token; ask SOLAR for one, or confirm 138 is intended.                                                                                                                                                                                       |
| `component.button.counter.height#unbound`                                                                                        | 20, bound to no variable                                                                                                                                                 | height on /Counter is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (allowLiteral):** The counter badge is fixed at 20 tall and bound to nothing; no SOLAR token names a badge height. Raised as a governance gap. Ask SOLAR to bind Button /Counter height to icon.md, which has the same value.                                                                                                                                                                                                                                                                  |
| `component.button.iconLeading.width#unbound`                                                                                     | 16, bound to no variable                                                                                                                                                 | width on /Icon/None is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (bind):** The icon is square and its height is already bound to icon.sm. Ask SOLAR to bind Button /Icon/None width to icon.sm, which has the same value.                                                                                                                                                                                                                                                                                                                                       |
| `component.button.iconTrailing.width#unbound`                                                                                    | 16, bound to no variable                                                                                                                                                 | width on /Icon/None#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind):** The icon is square and its height is already bound to icon.sm. Ask SOLAR to bind Button /Icon/None#2 width to icon.sm, which has the same value.                                                                                                                                                                                                                                                                                                                                     |
| `component.button.root.background@size=lg`                                                                                       | 9 variants at size=lg: color.action.tertiary.bg.default where none; color.action.tertiary.bg.danger.default where none; none where color.action.secondary.bg.hover; …    | background on / should follow prio, state, danger only, but at size=lg it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Ask SOLAR whether Button / background is meant to change at size=lg.                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `component.button.root.background@size=sm`                                                                                       | 8 variants at size=sm: none where color.action.secondary.bg.default; none where color.action.secondary.bg.danger.default; none where color.action.secondary.bg.active; … | background on / should follow prio, state, danger only, but at size=sm it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Ask SOLAR whether Button / background is meant to change at size=sm.                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `component.button.root.height#unbound`                                                                                           | 40, 32, 48, bound to no variable                                                                                                                                         | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind):** Same values, governed tokens: size.control, which SOLAR added to Spatial on 2026-09-25 (Button's description: "heights are size/control/{sm,md,lg}"). Figma leaves the heights unbound; the design review asks SOLAR to bind them. SOLAR has no control height token; ask SOLAR for one, or confirm 40, 32, 48 is intended.                                                                                                                                                          |
| `component.button.root.paddingBottom#unbound`                                                                                    | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Button / paddingBottom to inset.none, which has the same value.                                                      |
| `component.button.root.paddingTop#unbound`                                                                                       | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Button / paddingTop to inset.none, which has the same value.                                                         |
| `component.button.root.width#unbound`                                                                                            | 200, bound to no variable                                                                                                                                                | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (allowLiteral):** lg is fixed at 200 wide and SOLAR publishes no control width token. Treated like the heights under the same decision; raised as a governance gap. SOLAR has no control width token; ask SOLAR for one, or confirm 200 is intended.                                                                                                                                                                                                                                           |
| `component.calendar day cell.dayNumPill.borderWidth#unbound`                                                                     | 1, bound to no variable                                                                                                                                                  | borderWidth on /Day Num Row/Day Num Pill is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind):** Same value, governed token, today's pill's edge. Ask SOLAR to bind Calendar Day Cell /Day Num Row/Day Num Pill borderWidth to border.default, which has the same value.                                                                                                                                                                                                                                                                                                              |
| `component.calendar day cell.dayNumPill.gap#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | gap on /Day Num Row/Day Num Pill is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Calendar Day Cell /Day Num Row/Day Num Pill gap to inset.none or stack.none, which has the same value. |
| `component.calendar day cell.dayNumPill.height#unbound`                                                                          | 24, bound to no variable                                                                                                                                                 | height on /Day Num Row/Day Num Pill is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (allowLiteral):** Today's pill is 24 across, bound to nothing: SOLAR has no size for it, as a date picker's day cells. SOLAR has no control height token; ask SOLAR for one, or confirm 24 is intended.                                                                                                                                                                                                                                                                                        |
| `component.calendar day cell.dayNumPill.paddingBottom#unbound`                                                                   | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Day Num Row/Day Num Pill is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Calendar Day Cell /Day Num Row/Day Num Pill paddingBottom to inset.none, which has the same value.                   |
| `component.calendar day cell.dayNumPill.paddingTop#unbound`                                                                      | 0, bound to no variable                                                                                                                                                  | paddingTop on /Day Num Row/Day Num Pill is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Calendar Day Cell /Day Num Row/Day Num Pill paddingTop to inset.none, which has the same value.                      |
| `component.calendar day cell.dayNumPill.width#unbound`                                                                           | 24, bound to no variable                                                                                                                                                 | width on /Day Num Row/Day Num Pill is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (allowLiteral):** Today's pill is 24 across, bound to nothing: SOLAR has no size for it, as a date picker's day cells. SOLAR has no control width token; ask SOLAR for one, or confirm 24 is intended.                                                                                                                                                                                                                                                                                         |
| `component.calendar day cell.dayNumRow.gap#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | gap on /Day Num Row is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Calendar Day Cell /Day Num Row gap to inset.none or stack.none, which has the same value.              |
| `component.calendar day cell.dayNumRow.paddingBottom#unbound`                                                                    | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Day Num Row is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Calendar Day Cell /Day Num Row paddingBottom to inset.none, which has the same value.                                |
| `component.calendar day cell.dayNumRow.paddingTop#unbound`                                                                       | 0, bound to no variable                                                                                                                                                  | paddingTop on /Day Num Row is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Calendar Day Cell /Day Num Row paddingTop to inset.none, which has the same value.                                   |
| `component.calendar day cell.events.opacity#unbound`                                                                             | 0.5, bound to no variable                                                                                                                                                | opacity on /Events is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (allowLiteral):** Another month's events are drawn at 50% opacity. SOLAR has no opacity scale (SOLAR decided, 2026-09-25); carried as Figma's number. SOLAR has no opacity token; ask SOLAR for one, or confirm 0.5 is intended.                                                                                                                                                                                                                                                               |
| `component.calendar day cell.events.paddingBottom#unbound`                                                                       | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Events is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Calendar Day Cell /Events paddingBottom to inset.none, which has the same value.                                     |
| `component.calendar day cell.events.paddingLeft#unbound`                                                                         | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Events is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Calendar Day Cell /Events paddingLeft to inset.none, which has the same value.                                       |
| `component.calendar day cell.events.paddingRight#unbound`                                                                        | 0, bound to no variable                                                                                                                                                  | paddingRight on /Events is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Calendar Day Cell /Events paddingRight to inset.none, which has the same value.                                      |
| `component.calendar day cell.events.paddingTop#unbound`                                                                          | 0, bound to no variable                                                                                                                                                  | paddingTop on /Events is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Calendar Day Cell /Events paddingTop to inset.none, which has the same value.                                        |
| `component.calendar day cell.root.height#unbound`                                                                                | 120, bound to no variable                                                                                                                                                | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (allowLiteral):** A day of the month grid is 120 tall, room for three chips at the comfortable density, bound to nothing: SOLAR has no size for it, one of the sizes with no variable. SOLAR has no control height token; ask SOLAR for one, or confirm 120 is intended.                                                                                                                                                                                                                       |
| `component.calendar day cell.root.width#unbound`                                                                                 | 160, bound to no variable                                                                                                                                                | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** A day fills its column of the month grid: Figma draws a 160 sample, and a week of seven days is as wide as the calendar. SOLAR has no control width token; ask SOLAR for one, or confirm 160 is intended.                                                                                                                                                                                                                                                                             |
| `component.calendar toolbar.action.height#unbound`                                                                               | 32, bound to no variable                                                                                                                                                 | height on /Right/New Event is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** The caller's Button keeps its own height; the toolbar places it, and never sizes it. Ask SOLAR to bind Calendar Toolbar /Right/New Event height to icon.2xl, which has the same value.                                                                                                                                                                                                                                                                                                |
| `component.calendar toolbar.left.height#unbound`                                                                                 | 32, bound to no variable                                                                                                                                                 | height on /Left is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind):** The left group is a control's height, the sm step its Today Button and the right group's controls are, the same value. Its md Icon Buttons (40) reach past it, as Figma draws them; the design review asks. SOLAR has no control height token; ask SOLAR for one, or confirm 32 is intended.                                                                                                                                                                                         |
| `component.calendar toolbar.left.paddingBottom#unbound`                                                                          | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Left is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Calendar Toolbar /Left paddingBottom to inset.none, which has the same value.                                        |
| `component.calendar toolbar.left.paddingLeft#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Left is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Calendar Toolbar /Left paddingLeft to inset.none, which has the same value.                                          |
| `component.calendar toolbar.left.paddingRight#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingRight on /Left is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Calendar Toolbar /Left paddingRight to inset.none, which has the same value.                                         |
| `component.calendar toolbar.left.paddingTop#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingTop on /Left is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Calendar Toolbar /Left paddingTop to inset.none, which has the same value.                                           |
| `component.calendar toolbar.nav.paddingBottom#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Left/Nav is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Calendar Toolbar /Left/Nav paddingBottom to inset.none, which has the same value.                                    |
| `component.calendar toolbar.nav.paddingLeft#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Left/Nav is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Calendar Toolbar /Left/Nav paddingLeft to inset.none, which has the same value.                                      |
| `component.calendar toolbar.nav.paddingRight#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingRight on /Left/Nav is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Calendar Toolbar /Left/Nav paddingRight to inset.none, which has the same value.                                     |
| `component.calendar toolbar.nav.paddingTop#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingTop on /Left/Nav is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Calendar Toolbar /Left/Nav paddingTop to inset.none, which has the same value.                                       |
| `component.calendar toolbar.next.height#unbound`                                                                                 | 40, bound to no variable                                                                                                                                                 | height on /Left/Nav/Next is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (set):** Previous and next are SOLAR Icon Buttons (md, square, tertiary), whose 40px box is their own: the toolbar places them, and never sizes them. SOLAR has no control height token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                                         |
| `component.calendar toolbar.next.width#unbound`                                                                                  | 40, bound to no variable                                                                                                                                                 | width on /Left/Nav/Next is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (set):** Previous and next are SOLAR Icon Buttons (md, square, tertiary), whose 40px box is their own: the toolbar places them, and never sizes them. SOLAR has no control width token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                                          |
| `component.calendar toolbar.prev.height#unbound`                                                                                 | 40, bound to no variable                                                                                                                                                 | height on /Left/Nav/Prev is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (set):** Previous and next are SOLAR Icon Buttons (md, square, tertiary), whose 40px box is their own: the toolbar places them, and never sizes them. SOLAR has no control height token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                                         |
| `component.calendar toolbar.prev.width#unbound`                                                                                  | 40, bound to no variable                                                                                                                                                 | width on /Left/Nav/Prev is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (set):** Previous and next are SOLAR Icon Buttons (md, square, tertiary), whose 40px box is their own: the toolbar places them, and never sizes them. SOLAR has no control width token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                                          |
| `component.calendar toolbar.right.paddingBottom#unbound`                                                                         | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Right is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Calendar Toolbar /Right paddingBottom to inset.none, which has the same value.                                       |
| `component.calendar toolbar.right.paddingLeft#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Right is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Calendar Toolbar /Right paddingLeft to inset.none, which has the same value.                                         |
| `component.calendar toolbar.right.paddingRight#unbound`                                                                          | 0, bound to no variable                                                                                                                                                  | paddingRight on /Right is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Calendar Toolbar /Right paddingRight to inset.none, which has the same value.                                        |
| `component.calendar toolbar.right.paddingTop#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingTop on /Right is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Calendar Toolbar /Right paddingTop to inset.none, which has the same value.                                          |
| `component.calendar toolbar.root.width#unbound`                                                                                  | 1280, bound to no variable                                                                                                                                               | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** The toolbar spans its calendar view: Figma draws a 1280 sample. SOLAR has no control width token; ask SOLAR for one, or confirm 1280 is intended.                                                                                                                                                                                                                                                                                                                                     |
| `component.calendar toolbar.todayButton.height#unbound`                                                                          | 32, bound to no variable                                                                                                                                                 | height on /Left/Today Button is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (set):** Today is a SOLAR Button (sm, secondary), whose 32px height is its own: the toolbar places it, and never sizes it. Ask SOLAR to bind Calendar Toolbar /Left/Today Button height to icon.2xl, which has the same value.                                                                                                                                                                                                                                                                 |
| `component.card.content.paddingBottom#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Card /Content paddingBottom to inset.none, which has the same value.                                                 |
| `component.card.content.paddingLeft#unbound`                                                                                     | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Card /Content paddingLeft to inset.none, which has the same value.                                                   |
| `component.card.content.paddingRight#unbound`                                                                                    | 0, bound to no variable                                                                                                                                                  | paddingRight on /Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Card /Content paddingRight to inset.none, which has the same value.                                                  |
| `component.card.content.paddingTop#unbound`                                                                                      | 0, bound to no variable                                                                                                                                                  | paddingTop on /Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Card /Content paddingTop to inset.none, which has the same value.                                                    |
| `component.card.contentSkeleton.height#unbound`                                                                                  | 12, bound to no variable                                                                                                                                                 | height on /Content/Skeleton is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (allowLiteral):** As the title placeholder's, carried as drawn. SOLAR has no control height token; ask SOLAR for one, or confirm 12 is intended.                                                                                                                                                                                                                                                                                                                                               |
| `component.card.contentSkeleton.width#unbound`                                                                                   | 260, bound to no variable                                                                                                                                                | width on /Content/Skeleton is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (allowLiteral):** As the title placeholder's, carried as drawn. SOLAR has no control width token; ask SOLAR for one, or confirm 260 is intended.                                                                                                                                                                                                                                                                                                                                               |
| `component.card.contentSkeleton2.height#unbound`                                                                                 | 12, bound to no variable                                                                                                                                                 | height on /Content/Skeleton#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (allowLiteral):** As the title placeholder's, carried as drawn. SOLAR has no control height token; ask SOLAR for one, or confirm 12 is intended.                                                                                                                                                                                                                                                                                                                                               |
| `component.card.contentSkeleton2.width#unbound`                                                                                  | 260, bound to no variable                                                                                                                                                | width on /Content/Skeleton#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (allowLiteral):** As the title placeholder's, carried as drawn. SOLAR has no control width token; ask SOLAR for one, or confirm 260 is intended.                                                                                                                                                                                                                                                                                                                                               |
| `component.card.contentSkeleton3.height#unbound`                                                                                 | 12, bound to no variable                                                                                                                                                 | height on /Content/Skeleton#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (allowLiteral):** As the title placeholder's, carried as drawn. SOLAR has no control height token; ask SOLAR for one, or confirm 12 is intended.                                                                                                                                                                                                                                                                                                                                               |
| `component.card.contentSkeleton3.width#unbound`                                                                                  | 200, bound to no variable                                                                                                                                                | width on /Content/Skeleton#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (allowLiteral):** As the title placeholder's, carried as drawn. SOLAR has no control width token; ask SOLAR for one, or confirm 200 is intended.                                                                                                                                                                                                                                                                                                                                               |
| `component.card.icon.width#unbound`                                                                                              | 16, bound to no variable                                                                                                                                                 | width on /Title/Icon/None is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind):** Same value, governed token; the icon's height is already bound to it. Ask SOLAR to bind Card /Title/Icon/None width to icon.sm, which has the same value.                                                                                                                                                                                                                                                                                                                            |
| `component.card.more.width#unbound`                                                                                              | 20, bound to no variable                                                                                                                                                 | width on /Title/Icon/More is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind):** Same value, governed token; the More icon's height is already bound to it. Ask SOLAR to bind Card /Title/Icon/More width to icon.md, which has the same value.                                                                                                                                                                                                                                                                                                                       |
| `component.card.root.opacity#unbound`                                                                                            | 0.4, bound to no variable                                                                                                                                                | opacity on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (allowLiteral):** A disabled card is drawn at 40% opacity. SOLAR has no opacity scale, as for Section Nav Item's disabled 40% and Link's 50% (SOLAR's decision, 2026-09-25); carried as Figma's number. SOLAR has no opacity token; ask SOLAR for one, or confirm 0.4 is intended.                                                                                                                                                                                                             |
| `component.card.root.width#unbound`                                                                                              | 320, bound to no variable                                                                                                                                                | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** A card spans the space it is put in, as a grid's or a column's cell: Figma draws it 320 wide, a sample. The caller sizes it. SOLAR has no control width token; ask SOLAR for one, or confirm 320 is intended.                                                                                                                                                                                                                                                                         |
| `component.card.skeleton.height#unbound`                                                                                         | 16, bound to no variable                                                                                                                                                 | height on /Skeleton is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (allowLiteral):** As the placeholders' widths, carried as drawn. SOLAR has no control height token; ask SOLAR for one, or confirm 16 is intended.                                                                                                                                                                                                                                                                                                                                              |
| `component.card.skeleton.width#unbound`                                                                                          | 140, bound to no variable                                                                                                                                                | width on /Skeleton is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (allowLiteral):** The loading placeholders are Figma's drawing of a title and three lines to come (140, 260, 260 and 200 wide; 16 and 12 tall), no measure of anything: carried as drawn. SOLAR has no skeleton size tokens. SOLAR has no control width token; ask SOLAR for one, or confirm 140 is intended.                                                                                                                                                                                  |
| `component.card.tag.height#unbound`                                                                                              | 24, bound to no variable                                                                                                                                                 | height on /Tag is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (set):** The Tag is a SOLAR Tag, whose 24px height is its own: the card places it, and never sizes it. Ask SOLAR to bind Card /Tag height to icon.lg, which has the same value.                                                                                                                                                                                                                                                                                                                |
| `component.card.title.paddingBottom#unbound`                                                                                     | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Title is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Card /Title paddingBottom to inset.none, which has the same value.                                                   |
| `component.card.title.paddingLeft#unbound`                                                                                       | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Title is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Card /Title paddingLeft to inset.none, which has the same value.                                                     |
| `component.card.title.paddingRight#unbound`                                                                                      | 0, bound to no variable                                                                                                                                                  | paddingRight on /Title is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Card /Title paddingRight to inset.none, which has the same value.                                                    |
| `component.card.title.paddingTop#unbound`                                                                                        | 0, bound to no variable                                                                                                                                                  | paddingTop on /Title is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Card /Title paddingTop to inset.none, which has the same value.                                                      |
| `component.chart axis.axisLine.height#unbound`                                                                                   | 1, bound to no variable                                                                                                                                                  | height on /Axis Line is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind):** Same value, governed token, the axis line's thickness, which the theme takes. SOLAR has no control height token; ask SOLAR for one, or confirm 1 is intended.                                                                                                                                                                                                                                                                                                                        |
| `component.chart axis.axisLine.height@orientation=vertical, breakpoint=desktop`                                                  | 1 variant at orientation=vertical, breakpoint=desktop: FILL where 1                                                                                                      | height on /Axis Line should follow no axis only, but at orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis /Axis Line height is meant to change at orientation=vertical, breakpoint=desktop.                                                                                                                                                                                    |
| `component.chart axis.axisLine.height@orientation=vertical, breakpoint=mobile`                                                   | 1 variant at orientation=vertical, breakpoint=mobile: FILL where 1                                                                                                       | height on /Axis Line should follow no axis only, but at orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis /Axis Line height is meant to change at orientation=vertical, breakpoint=mobile.                                                                                                                                                                                     |
| `component.chart axis.axisLine.width#unbound`                                                                                    | 1, bound to no variable                                                                                                                                                  | width on /Axis Line is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 1 is intended.                                                                                                                                                                                                                    |
| `component.chart axis.axisLine.width@orientation=vertical, breakpoint=desktop`                                                   | 1 variant at orientation=vertical, breakpoint=desktop: 1 where FILL                                                                                                      | width on /Axis Line should follow no axis only, but at orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis /Axis Line width is meant to change at orientation=vertical, breakpoint=desktop.                                                                                                                                                                                     |
| `component.chart axis.axisLine.width@orientation=vertical, breakpoint=mobile`                                                    | 1 variant at orientation=vertical, breakpoint=mobile: 1 where FILL                                                                                                       | width on /Axis Line should follow no axis only, but at orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis /Axis Line width is meant to change at orientation=vertical, breakpoint=mobile.                                                                                                                                                                                      |
| `component.chart axis.root.direction@orientation=vertical, breakpoint=desktop`                                                   | 1 variant at orientation=vertical, breakpoint=desktop: HORIZONTAL where VERTICAL                                                                                         | direction on / should follow no axis only, but at orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis / direction is meant to change at orientation=vertical, breakpoint=desktop.                                                                                                                                                                                          |
| `component.chart axis.root.direction@orientation=vertical, breakpoint=mobile`                                                    | 1 variant at orientation=vertical, breakpoint=mobile: HORIZONTAL where VERTICAL                                                                                          | direction on / should follow no axis only, but at orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis / direction is meant to change at orientation=vertical, breakpoint=mobile.                                                                                                                                                                                           |
| `component.chart axis.root.height#unbound`                                                                                       | 280, 220, bound to no variable                                                                                                                                           | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 280, 220 is intended.                                                                                                                                                                                                            |
| `component.chart axis.root.height@orientation=vertical, breakpoint=desktop`                                                      | 1 variant at orientation=vertical, breakpoint=desktop: 280 where HUG                                                                                                     | height on / should follow no axis only, but at orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis / height is meant to change at orientation=vertical, breakpoint=desktop.                                                                                                                                                                                             |
| `component.chart axis.root.height@orientation=vertical, breakpoint=mobile`                                                       | 1 variant at orientation=vertical, breakpoint=mobile: 220 where HUG                                                                                                      | height on / should follow no axis only, but at orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis / height is meant to change at orientation=vertical, breakpoint=mobile.                                                                                                                                                                                              |
| `component.chart axis.root.paddingBottom#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR to bind Chart Axis / paddingBottom to inset.none, which has the same value.                                                                                                                                                                                                             |
| `component.chart axis.root.paddingLeft#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR to bind Chart Axis / paddingLeft to inset.none, which has the same value.                                                                                                                                                                                                               |
| `component.chart axis.root.paddingRight#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR to bind Chart Axis / paddingRight to inset.none, which has the same value.                                                                                                                                                                                                              |
| `component.chart axis.root.paddingTop#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR to bind Chart Axis / paddingTop to inset.none, which has the same value.                                                                                                                                                                                                                |
| `component.chart axis.root.width#unbound`                                                                                        | 600, 343, bound to no variable                                                                                                                                           | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 600, 343 is intended.                                                                                                                                                                                                             |
| `component.chart axis.root.width@orientation=horizontal, breakpoint=mobile`                                                      | 1 variant at orientation=horizontal, breakpoint=mobile: 343 where 600                                                                                                    | width on / should follow no axis only, but at orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis / width is meant to change at orientation=horizontal, breakpoint=mobile.                                                                                                                                                                                             |
| `component.chart axis.root.width@orientation=vertical, breakpoint=desktop`                                                       | 1 variant at orientation=vertical, breakpoint=desktop: HUG where 600                                                                                                     | width on / should follow no axis only, but at orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis / width is meant to change at orientation=vertical, breakpoint=desktop.                                                                                                                                                                                              |
| `component.chart axis.root.width@orientation=vertical, breakpoint=mobile`                                                        | 1 variant at orientation=vertical, breakpoint=mobile: HUG where 600                                                                                                      | width on / should follow no axis only, but at orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis / width is meant to change at orientation=vertical, breakpoint=mobile.                                                                                                                                                                                               |
| `component.chart axis.tick.align@orientation=vertical, breakpoint=desktop`                                                       | 1 variant at orientation=vertical, breakpoint=desktop: MAX/CENTER where MIN/CENTER                                                                                       | align on /Ticks/Tick should follow no axis only, but at orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis /Ticks/Tick align is meant to change at orientation=vertical, breakpoint=desktop.                                                                                                                                                                                    |
| `component.chart axis.tick.align@orientation=vertical, breakpoint=mobile`                                                        | 1 variant at orientation=vertical, breakpoint=mobile: MAX/CENTER where MIN/CENTER                                                                                        | align on /Ticks/Tick should follow no axis only, but at orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis /Ticks/Tick align is meant to change at orientation=vertical, breakpoint=mobile.                                                                                                                                                                                     |
| `component.chart axis.tick.direction@orientation=vertical, breakpoint=desktop`                                                   | 1 variant at orientation=vertical, breakpoint=desktop: HORIZONTAL where VERTICAL                                                                                         | direction on /Ticks/Tick should follow no axis only, but at orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis /Ticks/Tick direction is meant to change at orientation=vertical, breakpoint=desktop.                                                                                                                                                                                |
| `component.chart axis.tick.direction@orientation=vertical, breakpoint=mobile`                                                    | 1 variant at orientation=vertical, breakpoint=mobile: HORIZONTAL where VERTICAL                                                                                          | direction on /Ticks/Tick should follow no axis only, but at orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis /Ticks/Tick direction is meant to change at orientation=vertical, breakpoint=mobile.                                                                                                                                                                                 |
| `component.chart axis.tick.paddingBottom#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Ticks/Tick is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR to bind Chart Axis /Ticks/Tick paddingBottom to inset.none, which has the same value.                                                                                                                                                                                                   |
| `component.chart axis.tick.paddingLeft#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Ticks/Tick is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR to bind Chart Axis /Ticks/Tick paddingLeft to inset.none, which has the same value.                                                                                                                                                                                                     |
| `component.chart axis.tick.paddingRight#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingRight on /Ticks/Tick is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR to bind Chart Axis /Ticks/Tick paddingRight to inset.none, which has the same value.                                                                                                                                                                                                    |
| `component.chart axis.tick.paddingTop#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingTop on /Ticks/Tick is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR to bind Chart Axis /Ticks/Tick paddingTop to inset.none, which has the same value.                                                                                                                                                                                                      |
| `component.chart axis.tick2.align@orientation=vertical, breakpoint=desktop`                                                      | 1 variant at orientation=vertical, breakpoint=desktop: MAX/CENTER where MIN/CENTER                                                                                       | align on /Ticks/Tick#2 should follow no axis only, but at orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis /Ticks/Tick#2 align is meant to change at orientation=vertical, breakpoint=desktop.                                                                                                                                                                                  |
| `component.chart axis.tick2.align@orientation=vertical, breakpoint=mobile`                                                       | 1 variant at orientation=vertical, breakpoint=mobile: MAX/CENTER where MIN/CENTER                                                                                        | align on /Ticks/Tick#2 should follow no axis only, but at orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis /Ticks/Tick#2 align is meant to change at orientation=vertical, breakpoint=mobile.                                                                                                                                                                                   |
| `component.chart axis.tick2.direction@orientation=vertical, breakpoint=desktop`                                                  | 1 variant at orientation=vertical, breakpoint=desktop: HORIZONTAL where VERTICAL                                                                                         | direction on /Ticks/Tick#2 should follow no axis only, but at orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis /Ticks/Tick#2 direction is meant to change at orientation=vertical, breakpoint=desktop.                                                                                                                                                                              |
| `component.chart axis.tick2.direction@orientation=vertical, breakpoint=mobile`                                                   | 1 variant at orientation=vertical, breakpoint=mobile: HORIZONTAL where VERTICAL                                                                                          | direction on /Ticks/Tick#2 should follow no axis only, but at orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis /Ticks/Tick#2 direction is meant to change at orientation=vertical, breakpoint=mobile.                                                                                                                                                                               |
| `component.chart axis.tick2.paddingBottom#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Ticks/Tick#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR to bind Chart Axis /Ticks/Tick#2 paddingBottom to inset.none, which has the same value.                                                                                                                                                                                                 |
| `component.chart axis.tick2.paddingLeft#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Ticks/Tick#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR to bind Chart Axis /Ticks/Tick#2 paddingLeft to inset.none, which has the same value.                                                                                                                                                                                                   |
| `component.chart axis.tick2.paddingRight#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingRight on /Ticks/Tick#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR to bind Chart Axis /Ticks/Tick#2 paddingRight to inset.none, which has the same value.                                                                                                                                                                                                  |
| `component.chart axis.tick2.paddingTop#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingTop on /Ticks/Tick#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR to bind Chart Axis /Ticks/Tick#2 paddingTop to inset.none, which has the same value.                                                                                                                                                                                                    |
| `component.chart axis.tick2TickMark.height#unbound`                                                                              | 4, 1, bound to no variable                                                                                                                                               | height on /Ticks/Tick#2/Tick Mark is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (allowLiteral):** A tick mark is 4 long, bound to nothing: SOLAR has no size for it, one of the sizes with no variable; the theme takes it as Figma draws it. SOLAR has no control height token; ask SOLAR for one, or confirm 4, 1 is intended.                                                                                                                                                                                                                                               |
| `component.chart axis.tick2TickMark.height@orientation=vertical, breakpoint=desktop`                                             | 1 variant at orientation=vertical, breakpoint=desktop: 1 where 4                                                                                                         | height on /Ticks/Tick#2/Tick Mark should follow no axis only, but at orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis /Ticks/Tick#2/Tick Mark height is meant to change at orientation=vertical, breakpoint=desktop.                                                                                                                                                                       |
| `component.chart axis.tick2TickMark.height@orientation=vertical, breakpoint=mobile`                                              | 1 variant at orientation=vertical, breakpoint=mobile: 1 where 4                                                                                                          | height on /Ticks/Tick#2/Tick Mark should follow no axis only, but at orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis /Ticks/Tick#2/Tick Mark height is meant to change at orientation=vertical, breakpoint=mobile.                                                                                                                                                                        |
| `component.chart axis.tick2TickMark.width#unbound`                                                                               | 1, 4, bound to no variable                                                                                                                                               | width on /Ticks/Tick#2/Tick Mark is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind):** Same value, governed token, a tick mark's thickness. SOLAR has no control width token; ask SOLAR for one, or confirm 1, 4 is intended.                                                                                                                                                                                                                                                                                                                                               |
| `component.chart axis.tick2TickMark.width@orientation=vertical, breakpoint=desktop`                                              | 1 variant at orientation=vertical, breakpoint=desktop: 4 where 1                                                                                                         | width on /Ticks/Tick#2/Tick Mark should follow no axis only, but at orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis /Ticks/Tick#2/Tick Mark width is meant to change at orientation=vertical, breakpoint=desktop.                                                                                                                                                                        |
| `component.chart axis.tick2TickMark.width@orientation=vertical, breakpoint=mobile`                                               | 1 variant at orientation=vertical, breakpoint=mobile: 4 where 1                                                                                                          | width on /Ticks/Tick#2/Tick Mark should follow no axis only, but at orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis /Ticks/Tick#2/Tick Mark width is meant to change at orientation=vertical, breakpoint=mobile.                                                                                                                                                                         |
| `component.chart axis.tick3.align@orientation=vertical, breakpoint=desktop`                                                      | 1 variant at orientation=vertical, breakpoint=desktop: MAX/CENTER where MIN/CENTER                                                                                       | align on /Ticks/Tick#3 should follow no axis only, but at orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis /Ticks/Tick#3 align is meant to change at orientation=vertical, breakpoint=desktop.                                                                                                                                                                                  |
| `component.chart axis.tick3.align@orientation=vertical, breakpoint=mobile`                                                       | 1 variant at orientation=vertical, breakpoint=mobile: MAX/CENTER where MIN/CENTER                                                                                        | align on /Ticks/Tick#3 should follow no axis only, but at orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis /Ticks/Tick#3 align is meant to change at orientation=vertical, breakpoint=mobile.                                                                                                                                                                                   |
| `component.chart axis.tick3.direction@orientation=vertical, breakpoint=desktop`                                                  | 1 variant at orientation=vertical, breakpoint=desktop: HORIZONTAL where VERTICAL                                                                                         | direction on /Ticks/Tick#3 should follow no axis only, but at orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis /Ticks/Tick#3 direction is meant to change at orientation=vertical, breakpoint=desktop.                                                                                                                                                                              |
| `component.chart axis.tick3.direction@orientation=vertical, breakpoint=mobile`                                                   | 1 variant at orientation=vertical, breakpoint=mobile: HORIZONTAL where VERTICAL                                                                                          | direction on /Ticks/Tick#3 should follow no axis only, but at orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis /Ticks/Tick#3 direction is meant to change at orientation=vertical, breakpoint=mobile.                                                                                                                                                                               |
| `component.chart axis.tick3.paddingBottom#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Ticks/Tick#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR to bind Chart Axis /Ticks/Tick#3 paddingBottom to inset.none, which has the same value.                                                                                                                                                                                                 |
| `component.chart axis.tick3.paddingLeft#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Ticks/Tick#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR to bind Chart Axis /Ticks/Tick#3 paddingLeft to inset.none, which has the same value.                                                                                                                                                                                                   |
| `component.chart axis.tick3.paddingRight#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingRight on /Ticks/Tick#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR to bind Chart Axis /Ticks/Tick#3 paddingRight to inset.none, which has the same value.                                                                                                                                                                                                  |
| `component.chart axis.tick3.paddingTop#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingTop on /Ticks/Tick#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR to bind Chart Axis /Ticks/Tick#3 paddingTop to inset.none, which has the same value.                                                                                                                                                                                                    |
| `component.chart axis.tick3TickMark.height#unbound`                                                                              | 4, 1, bound to no variable                                                                                                                                               | height on /Ticks/Tick#3/Tick Mark is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (allowLiteral):** A tick mark is 4 long, bound to nothing: SOLAR has no size for it, one of the sizes with no variable; the theme takes it as Figma draws it. SOLAR has no control height token; ask SOLAR for one, or confirm 4, 1 is intended.                                                                                                                                                                                                                                               |
| `component.chart axis.tick3TickMark.height@orientation=vertical, breakpoint=desktop`                                             | 1 variant at orientation=vertical, breakpoint=desktop: 1 where 4                                                                                                         | height on /Ticks/Tick#3/Tick Mark should follow no axis only, but at orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis /Ticks/Tick#3/Tick Mark height is meant to change at orientation=vertical, breakpoint=desktop.                                                                                                                                                                       |
| `component.chart axis.tick3TickMark.height@orientation=vertical, breakpoint=mobile`                                              | 1 variant at orientation=vertical, breakpoint=mobile: 1 where 4                                                                                                          | height on /Ticks/Tick#3/Tick Mark should follow no axis only, but at orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis /Ticks/Tick#3/Tick Mark height is meant to change at orientation=vertical, breakpoint=mobile.                                                                                                                                                                        |
| `component.chart axis.tick3TickMark.width#unbound`                                                                               | 1, 4, bound to no variable                                                                                                                                               | width on /Ticks/Tick#3/Tick Mark is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind):** Same value, governed token, a tick mark's thickness. SOLAR has no control width token; ask SOLAR for one, or confirm 1, 4 is intended.                                                                                                                                                                                                                                                                                                                                               |
| `component.chart axis.tick3TickMark.width@orientation=vertical, breakpoint=desktop`                                              | 1 variant at orientation=vertical, breakpoint=desktop: 4 where 1                                                                                                         | width on /Ticks/Tick#3/Tick Mark should follow no axis only, but at orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis /Ticks/Tick#3/Tick Mark width is meant to change at orientation=vertical, breakpoint=desktop.                                                                                                                                                                        |
| `component.chart axis.tick3TickMark.width@orientation=vertical, breakpoint=mobile`                                               | 1 variant at orientation=vertical, breakpoint=mobile: 4 where 1                                                                                                          | width on /Ticks/Tick#3/Tick Mark should follow no axis only, but at orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis /Ticks/Tick#3/Tick Mark width is meant to change at orientation=vertical, breakpoint=mobile.                                                                                                                                                                         |
| `component.chart axis.tick4.align@orientation=vertical, breakpoint=desktop`                                                      | 1 variant at orientation=vertical, breakpoint=desktop: MAX/CENTER where MIN/CENTER                                                                                       | align on /Ticks/Tick#4 should follow no axis only, but at orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis /Ticks/Tick#4 align is meant to change at orientation=vertical, breakpoint=desktop.                                                                                                                                                                                  |
| `component.chart axis.tick4.align@orientation=vertical, breakpoint=mobile`                                                       | 1 variant at orientation=vertical, breakpoint=mobile: MAX/CENTER where MIN/CENTER                                                                                        | align on /Ticks/Tick#4 should follow no axis only, but at orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis /Ticks/Tick#4 align is meant to change at orientation=vertical, breakpoint=mobile.                                                                                                                                                                                   |
| `component.chart axis.tick4.direction@orientation=vertical, breakpoint=desktop`                                                  | 1 variant at orientation=vertical, breakpoint=desktop: HORIZONTAL where VERTICAL                                                                                         | direction on /Ticks/Tick#4 should follow no axis only, but at orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis /Ticks/Tick#4 direction is meant to change at orientation=vertical, breakpoint=desktop.                                                                                                                                                                              |
| `component.chart axis.tick4.direction@orientation=vertical, breakpoint=mobile`                                                   | 1 variant at orientation=vertical, breakpoint=mobile: HORIZONTAL where VERTICAL                                                                                          | direction on /Ticks/Tick#4 should follow no axis only, but at orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis /Ticks/Tick#4 direction is meant to change at orientation=vertical, breakpoint=mobile.                                                                                                                                                                               |
| `component.chart axis.tick4.paddingBottom#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Ticks/Tick#4 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR to bind Chart Axis /Ticks/Tick#4 paddingBottom to inset.none, which has the same value.                                                                                                                                                                                                 |
| `component.chart axis.tick4.paddingLeft#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Ticks/Tick#4 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR to bind Chart Axis /Ticks/Tick#4 paddingLeft to inset.none, which has the same value.                                                                                                                                                                                                   |
| `component.chart axis.tick4.paddingRight#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingRight on /Ticks/Tick#4 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR to bind Chart Axis /Ticks/Tick#4 paddingRight to inset.none, which has the same value.                                                                                                                                                                                                  |
| `component.chart axis.tick4.paddingTop#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingTop on /Ticks/Tick#4 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR to bind Chart Axis /Ticks/Tick#4 paddingTop to inset.none, which has the same value.                                                                                                                                                                                                    |
| `component.chart axis.tick4TickMark.height#unbound`                                                                              | 4, 1, bound to no variable                                                                                                                                               | height on /Ticks/Tick#4/Tick Mark is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (allowLiteral):** A tick mark is 4 long, bound to nothing: SOLAR has no size for it, one of the sizes with no variable; the theme takes it as Figma draws it. SOLAR has no control height token; ask SOLAR for one, or confirm 4, 1 is intended.                                                                                                                                                                                                                                               |
| `component.chart axis.tick4TickMark.height@orientation=vertical, breakpoint=desktop`                                             | 1 variant at orientation=vertical, breakpoint=desktop: 1 where 4                                                                                                         | height on /Ticks/Tick#4/Tick Mark should follow no axis only, but at orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis /Ticks/Tick#4/Tick Mark height is meant to change at orientation=vertical, breakpoint=desktop.                                                                                                                                                                       |
| `component.chart axis.tick4TickMark.height@orientation=vertical, breakpoint=mobile`                                              | 1 variant at orientation=vertical, breakpoint=mobile: 1 where 4                                                                                                          | height on /Ticks/Tick#4/Tick Mark should follow no axis only, but at orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis /Ticks/Tick#4/Tick Mark height is meant to change at orientation=vertical, breakpoint=mobile.                                                                                                                                                                        |
| `component.chart axis.tick4TickMark.width#unbound`                                                                               | 1, 4, bound to no variable                                                                                                                                               | width on /Ticks/Tick#4/Tick Mark is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind):** Same value, governed token, a tick mark's thickness. SOLAR has no control width token; ask SOLAR for one, or confirm 1, 4 is intended.                                                                                                                                                                                                                                                                                                                                               |
| `component.chart axis.tick4TickMark.width@orientation=vertical, breakpoint=desktop`                                              | 1 variant at orientation=vertical, breakpoint=desktop: 4 where 1                                                                                                         | width on /Ticks/Tick#4/Tick Mark should follow no axis only, but at orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis /Ticks/Tick#4/Tick Mark width is meant to change at orientation=vertical, breakpoint=desktop.                                                                                                                                                                        |
| `component.chart axis.tick4TickMark.width@orientation=vertical, breakpoint=mobile`                                               | 1 variant at orientation=vertical, breakpoint=mobile: 4 where 1                                                                                                          | width on /Ticks/Tick#4/Tick Mark should follow no axis only, but at orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis /Ticks/Tick#4/Tick Mark width is meant to change at orientation=vertical, breakpoint=mobile.                                                                                                                                                                         |
| `component.chart axis.tick5.align@orientation=vertical, breakpoint=desktop`                                                      | 1 variant at orientation=vertical, breakpoint=desktop: MAX/CENTER where MIN/CENTER                                                                                       | align on /Ticks/Tick#5 should follow no axis only, but at orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis /Ticks/Tick#5 align is meant to change at orientation=vertical, breakpoint=desktop.                                                                                                                                                                                  |
| `component.chart axis.tick5.direction@orientation=vertical, breakpoint=desktop`                                                  | 1 variant at orientation=vertical, breakpoint=desktop: HORIZONTAL where VERTICAL                                                                                         | direction on /Ticks/Tick#5 should follow no axis only, but at orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis /Ticks/Tick#5 direction is meant to change at orientation=vertical, breakpoint=desktop.                                                                                                                                                                              |
| `component.chart axis.tick5.paddingBottom#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Ticks/Tick#5 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR to bind Chart Axis /Ticks/Tick#5 paddingBottom to inset.none, which has the same value.                                                                                                                                                                                                 |
| `component.chart axis.tick5.paddingLeft#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Ticks/Tick#5 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR to bind Chart Axis /Ticks/Tick#5 paddingLeft to inset.none, which has the same value.                                                                                                                                                                                                   |
| `component.chart axis.tick5.paddingRight#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingRight on /Ticks/Tick#5 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR to bind Chart Axis /Ticks/Tick#5 paddingRight to inset.none, which has the same value.                                                                                                                                                                                                  |
| `component.chart axis.tick5.paddingTop#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingTop on /Ticks/Tick#5 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR to bind Chart Axis /Ticks/Tick#5 paddingTop to inset.none, which has the same value.                                                                                                                                                                                                    |
| `component.chart axis.tick5TickMark.height#unbound`                                                                              | 4, 1, bound to no variable                                                                                                                                               | height on /Ticks/Tick#5/Tick Mark is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (allowLiteral):** A tick mark is 4 long, bound to nothing: SOLAR has no size for it, one of the sizes with no variable; the theme takes it as Figma draws it. SOLAR has no control height token; ask SOLAR for one, or confirm 4, 1 is intended.                                                                                                                                                                                                                                               |
| `component.chart axis.tick5TickMark.height@orientation=vertical, breakpoint=desktop`                                             | 1 variant at orientation=vertical, breakpoint=desktop: 1 where 4                                                                                                         | height on /Ticks/Tick#5/Tick Mark should follow no axis only, but at orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis /Ticks/Tick#5/Tick Mark height is meant to change at orientation=vertical, breakpoint=desktop.                                                                                                                                                                       |
| `component.chart axis.tick5TickMark.width#unbound`                                                                               | 1, 4, bound to no variable                                                                                                                                               | width on /Ticks/Tick#5/Tick Mark is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind):** Same value, governed token, a tick mark's thickness. SOLAR has no control width token; ask SOLAR for one, or confirm 1, 4 is intended.                                                                                                                                                                                                                                                                                                                                               |
| `component.chart axis.tick5TickMark.width@orientation=vertical, breakpoint=desktop`                                              | 1 variant at orientation=vertical, breakpoint=desktop: 4 where 1                                                                                                         | width on /Ticks/Tick#5/Tick Mark should follow no axis only, but at orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis /Ticks/Tick#5/Tick Mark width is meant to change at orientation=vertical, breakpoint=desktop.                                                                                                                                                                        |
| `component.chart axis.tickTickMark.height#unbound`                                                                               | 4, 1, bound to no variable                                                                                                                                               | height on /Ticks/Tick/Tick Mark is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (allowLiteral):** A tick mark is 4 long, bound to nothing: SOLAR has no size for it, one of the sizes with no variable; the theme takes it as Figma draws it. SOLAR has no control height token; ask SOLAR for one, or confirm 4, 1 is intended.                                                                                                                                                                                                                                               |
| `component.chart axis.tickTickMark.height@orientation=vertical, breakpoint=desktop`                                              | 1 variant at orientation=vertical, breakpoint=desktop: 1 where 4                                                                                                         | height on /Ticks/Tick/Tick Mark should follow no axis only, but at orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis /Ticks/Tick/Tick Mark height is meant to change at orientation=vertical, breakpoint=desktop.                                                                                                                                                                         |
| `component.chart axis.tickTickMark.height@orientation=vertical, breakpoint=mobile`                                               | 1 variant at orientation=vertical, breakpoint=mobile: 1 where 4                                                                                                          | height on /Ticks/Tick/Tick Mark should follow no axis only, but at orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis /Ticks/Tick/Tick Mark height is meant to change at orientation=vertical, breakpoint=mobile.                                                                                                                                                                          |
| `component.chart axis.tickTickMark.width#unbound`                                                                                | 1, 4, bound to no variable                                                                                                                                               | width on /Ticks/Tick/Tick Mark is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (bind):** Same value, governed token, a tick mark's thickness. SOLAR has no control width token; ask SOLAR for one, or confirm 1, 4 is intended.                                                                                                                                                                                                                                                                                                                                               |
| `component.chart axis.tickTickMark.width@orientation=vertical, breakpoint=desktop`                                               | 1 variant at orientation=vertical, breakpoint=desktop: 4 where 1                                                                                                         | width on /Ticks/Tick/Tick Mark should follow no axis only, but at orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis /Ticks/Tick/Tick Mark width is meant to change at orientation=vertical, breakpoint=desktop.                                                                                                                                                                          |
| `component.chart axis.tickTickMark.width@orientation=vertical, breakpoint=mobile`                                                | 1 variant at orientation=vertical, breakpoint=mobile: 4 where 1                                                                                                          | width on /Ticks/Tick/Tick Mark should follow no axis only, but at orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis /Ticks/Tick/Tick Mark width is meant to change at orientation=vertical, breakpoint=mobile.                                                                                                                                                                           |
| `component.chart axis.ticks.align@orientation=vertical, breakpoint=desktop`                                                      | 1 variant at orientation=vertical, breakpoint=desktop: SPACE_BETWEEN/MAX where SPACE_BETWEEN/MIN                                                                         | align on /Ticks should follow no axis only, but at orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis /Ticks align is meant to change at orientation=vertical, breakpoint=desktop.                                                                                                                                                                                         |
| `component.chart axis.ticks.align@orientation=vertical, breakpoint=mobile`                                                       | 1 variant at orientation=vertical, breakpoint=mobile: SPACE_BETWEEN/MAX where SPACE_BETWEEN/MIN                                                                          | align on /Ticks should follow no axis only, but at orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis /Ticks align is meant to change at orientation=vertical, breakpoint=mobile.                                                                                                                                                                                          |
| `component.chart axis.ticks.direction@orientation=vertical, breakpoint=desktop`                                                  | 1 variant at orientation=vertical, breakpoint=desktop: VERTICAL where HORIZONTAL                                                                                         | direction on /Ticks should follow no axis only, but at orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis /Ticks direction is meant to change at orientation=vertical, breakpoint=desktop.                                                                                                                                                                                     |
| `component.chart axis.ticks.direction@orientation=vertical, breakpoint=mobile`                                                   | 1 variant at orientation=vertical, breakpoint=mobile: VERTICAL where HORIZONTAL                                                                                          | direction on /Ticks should follow no axis only, but at orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis /Ticks direction is meant to change at orientation=vertical, breakpoint=mobile.                                                                                                                                                                                      |
| `component.chart axis.ticks.height@orientation=vertical, breakpoint=desktop`                                                     | 1 variant at orientation=vertical, breakpoint=desktop: FILL where HUG                                                                                                    | height on /Ticks should follow no axis only, but at orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis /Ticks height is meant to change at orientation=vertical, breakpoint=desktop.                                                                                                                                                                                        |
| `component.chart axis.ticks.height@orientation=vertical, breakpoint=mobile`                                                      | 1 variant at orientation=vertical, breakpoint=mobile: FILL where HUG                                                                                                     | height on /Ticks should follow no axis only, but at orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Axis /Ticks height is meant to change at orientation=vertical, breakpoint=mobile.                                                                                                                                                                                         |
| `component.chart axis.ticks.paddingBottom#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Ticks is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR to bind Chart Axis /Ticks paddingBottom to inset.none, which has the same value.                                                                                                                                                                                                        |
| `component.chart axis.ticks.paddingLeft#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Ticks is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR to bind Chart Axis /Ticks paddingLeft to inset.none, which has the same value.                                                                                                                                                                                                          |
| `component.chart axis.ticks.paddingRight#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingRight on /Ticks is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR to bind Chart Axis /Ticks paddingRight to inset.none, which has the same value.                                                                                                                                                                                                         |
| `component.chart axis.ticks.paddingTop#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingTop on /Ticks is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's axis is a sample: its ticks' places and labels (0 to 100) at two lengths. The library places the caller's ticks; the theme takes the design, none of which is left open. Ask SOLAR to bind Chart Axis /Ticks paddingTop to inset.none, which has the same value.                                                                                                                                                                                                           |
| `component.chart gridlines.gridline.height#unbound`                                                                              | 1, bound to no variable                                                                                                                                                  | height on /Gridline is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (bind):** Same value, governed token, a gridline's thickness, which the theme takes. SOLAR has no control height token; ask SOLAR for one, or confirm 1 is intended.                                                                                                                                                                                                                                                                                                                           |
| `component.chart gridlines.gridline.height@orientation=vertical, breakpoint=desktop`                                             | 1 variant at orientation=vertical, breakpoint=desktop: FILL where 1                                                                                                      | height on /Gridline should follow no axis only, but at orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's gridlines are a sample: how many, how far apart, at two sizes. The library places the caller's; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Gridlines /Gridline height is meant to change at orientation=vertical, breakpoint=desktop.                                                                                                                                                                                                 |
| `component.chart gridlines.gridline.height@orientation=vertical, breakpoint=mobile`                                              | 1 variant at orientation=vertical, breakpoint=mobile: FILL where 1                                                                                                       | height on /Gridline should follow no axis only, but at orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** Figma's gridlines are a sample: how many, how far apart, at two sizes. The library places the caller's; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Gridlines /Gridline height is meant to change at orientation=vertical, breakpoint=mobile.                                                                                                                                                                                                  |
| `component.chart gridlines.gridline.width#unbound`                                                                               | 1, bound to no variable                                                                                                                                                  | width on /Gridline is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's gridlines are a sample: how many, how far apart, at two sizes. The library places the caller's; the theme takes the design, none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 1 is intended.                                                                                                                                                                                                                                     |
| `component.chart gridlines.gridline.width@orientation=vertical, breakpoint=desktop`                                              | 1 variant at orientation=vertical, breakpoint=desktop: 1 where FILL                                                                                                      | width on /Gridline should follow no axis only, but at orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** Figma's gridlines are a sample: how many, how far apart, at two sizes. The library places the caller's; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Gridlines /Gridline width is meant to change at orientation=vertical, breakpoint=desktop.                                                                                                                                                                                                  |
| `component.chart gridlines.gridline.width@orientation=vertical, breakpoint=mobile`                                               | 1 variant at orientation=vertical, breakpoint=mobile: 1 where FILL                                                                                                       | width on /Gridline should follow no axis only, but at orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** Figma's gridlines are a sample: how many, how far apart, at two sizes. The library places the caller's; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Gridlines /Gridline width is meant to change at orientation=vertical, breakpoint=mobile.                                                                                                                                                                                                   |
| `component.chart gridlines.gridline2.height#unbound`                                                                             | 1, bound to no variable                                                                                                                                                  | height on /Gridline#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind):** Same value, governed token, a gridline's thickness, which the theme takes. SOLAR has no control height token; ask SOLAR for one, or confirm 1 is intended.                                                                                                                                                                                                                                                                                                                           |
| `component.chart gridlines.gridline2.height@orientation=vertical, breakpoint=desktop`                                            | 1 variant at orientation=vertical, breakpoint=desktop: FILL where 1                                                                                                      | height on /Gridline#2 should follow no axis only, but at orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** Figma's gridlines are a sample: how many, how far apart, at two sizes. The library places the caller's; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Gridlines /Gridline#2 height is meant to change at orientation=vertical, breakpoint=desktop.                                                                                                                                                                                               |
| `component.chart gridlines.gridline2.height@orientation=vertical, breakpoint=mobile`                                             | 1 variant at orientation=vertical, breakpoint=mobile: FILL where 1                                                                                                       | height on /Gridline#2 should follow no axis only, but at orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** Figma's gridlines are a sample: how many, how far apart, at two sizes. The library places the caller's; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Gridlines /Gridline#2 height is meant to change at orientation=vertical, breakpoint=mobile.                                                                                                                                                                                                |
| `component.chart gridlines.gridline2.width#unbound`                                                                              | 1, bound to no variable                                                                                                                                                  | width on /Gridline#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's gridlines are a sample: how many, how far apart, at two sizes. The library places the caller's; the theme takes the design, none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 1 is intended.                                                                                                                                                                                                                                     |
| `component.chart gridlines.gridline2.width@orientation=vertical, breakpoint=desktop`                                             | 1 variant at orientation=vertical, breakpoint=desktop: 1 where FILL                                                                                                      | width on /Gridline#2 should follow no axis only, but at orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** Figma's gridlines are a sample: how many, how far apart, at two sizes. The library places the caller's; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Gridlines /Gridline#2 width is meant to change at orientation=vertical, breakpoint=desktop.                                                                                                                                                                                                |
| `component.chart gridlines.gridline2.width@orientation=vertical, breakpoint=mobile`                                              | 1 variant at orientation=vertical, breakpoint=mobile: 1 where FILL                                                                                                       | width on /Gridline#2 should follow no axis only, but at orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's gridlines are a sample: how many, how far apart, at two sizes. The library places the caller's; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Gridlines /Gridline#2 width is meant to change at orientation=vertical, breakpoint=mobile.                                                                                                                                                                                                 |
| `component.chart gridlines.gridline3.height#unbound`                                                                             | 1, bound to no variable                                                                                                                                                  | height on /Gridline#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind):** Same value, governed token, a gridline's thickness, which the theme takes. SOLAR has no control height token; ask SOLAR for one, or confirm 1 is intended.                                                                                                                                                                                                                                                                                                                           |
| `component.chart gridlines.gridline3.height@orientation=vertical, breakpoint=desktop`                                            | 1 variant at orientation=vertical, breakpoint=desktop: FILL where 1                                                                                                      | height on /Gridline#3 should follow no axis only, but at orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** Figma's gridlines are a sample: how many, how far apart, at two sizes. The library places the caller's; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Gridlines /Gridline#3 height is meant to change at orientation=vertical, breakpoint=desktop.                                                                                                                                                                                               |
| `component.chart gridlines.gridline3.height@orientation=vertical, breakpoint=mobile`                                             | 1 variant at orientation=vertical, breakpoint=mobile: FILL where 1                                                                                                       | height on /Gridline#3 should follow no axis only, but at orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** Figma's gridlines are a sample: how many, how far apart, at two sizes. The library places the caller's; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Gridlines /Gridline#3 height is meant to change at orientation=vertical, breakpoint=mobile.                                                                                                                                                                                                |
| `component.chart gridlines.gridline3.width#unbound`                                                                              | 1, bound to no variable                                                                                                                                                  | width on /Gridline#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's gridlines are a sample: how many, how far apart, at two sizes. The library places the caller's; the theme takes the design, none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 1 is intended.                                                                                                                                                                                                                                     |
| `component.chart gridlines.gridline3.width@orientation=vertical, breakpoint=desktop`                                             | 1 variant at orientation=vertical, breakpoint=desktop: 1 where FILL                                                                                                      | width on /Gridline#3 should follow no axis only, but at orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** Figma's gridlines are a sample: how many, how far apart, at two sizes. The library places the caller's; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Gridlines /Gridline#3 width is meant to change at orientation=vertical, breakpoint=desktop.                                                                                                                                                                                                |
| `component.chart gridlines.gridline3.width@orientation=vertical, breakpoint=mobile`                                              | 1 variant at orientation=vertical, breakpoint=mobile: 1 where FILL                                                                                                       | width on /Gridline#3 should follow no axis only, but at orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's gridlines are a sample: how many, how far apart, at two sizes. The library places the caller's; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Gridlines /Gridline#3 width is meant to change at orientation=vertical, breakpoint=mobile.                                                                                                                                                                                                 |
| `component.chart gridlines.gridline4.height#unbound`                                                                             | 1, bound to no variable                                                                                                                                                  | height on /Gridline#4 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind):** Same value, governed token, a gridline's thickness, which the theme takes. SOLAR has no control height token; ask SOLAR for one, or confirm 1 is intended.                                                                                                                                                                                                                                                                                                                           |
| `component.chart gridlines.gridline4.height@orientation=vertical, breakpoint=desktop`                                            | 1 variant at orientation=vertical, breakpoint=desktop: FILL where 1                                                                                                      | height on /Gridline#4 should follow no axis only, but at orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** Figma's gridlines are a sample: how many, how far apart, at two sizes. The library places the caller's; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Gridlines /Gridline#4 height is meant to change at orientation=vertical, breakpoint=desktop.                                                                                                                                                                                               |
| `component.chart gridlines.gridline4.height@orientation=vertical, breakpoint=mobile`                                             | 1 variant at orientation=vertical, breakpoint=mobile: FILL where 1                                                                                                       | height on /Gridline#4 should follow no axis only, but at orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** Figma's gridlines are a sample: how many, how far apart, at two sizes. The library places the caller's; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Gridlines /Gridline#4 height is meant to change at orientation=vertical, breakpoint=mobile.                                                                                                                                                                                                |
| `component.chart gridlines.gridline4.width#unbound`                                                                              | 1, bound to no variable                                                                                                                                                  | width on /Gridline#4 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's gridlines are a sample: how many, how far apart, at two sizes. The library places the caller's; the theme takes the design, none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 1 is intended.                                                                                                                                                                                                                                     |
| `component.chart gridlines.gridline4.width@orientation=vertical, breakpoint=desktop`                                             | 1 variant at orientation=vertical, breakpoint=desktop: 1 where FILL                                                                                                      | width on /Gridline#4 should follow no axis only, but at orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** Figma's gridlines are a sample: how many, how far apart, at two sizes. The library places the caller's; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Gridlines /Gridline#4 width is meant to change at orientation=vertical, breakpoint=desktop.                                                                                                                                                                                                |
| `component.chart gridlines.gridline4.width@orientation=vertical, breakpoint=mobile`                                              | 1 variant at orientation=vertical, breakpoint=mobile: 1 where FILL                                                                                                       | width on /Gridline#4 should follow no axis only, but at orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's gridlines are a sample: how many, how far apart, at two sizes. The library places the caller's; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Gridlines /Gridline#4 width is meant to change at orientation=vertical, breakpoint=mobile.                                                                                                                                                                                                 |
| `component.chart gridlines.gridline5.height#unbound`                                                                             | 1, bound to no variable                                                                                                                                                  | height on /Gridline#5 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind):** Same value, governed token, a gridline's thickness, which the theme takes. SOLAR has no control height token; ask SOLAR for one, or confirm 1 is intended.                                                                                                                                                                                                                                                                                                                           |
| `component.chart gridlines.gridline5.height@orientation=vertical, breakpoint=desktop`                                            | 1 variant at orientation=vertical, breakpoint=desktop: FILL where 1                                                                                                      | height on /Gridline#5 should follow no axis only, but at orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** Figma's gridlines are a sample: how many, how far apart, at two sizes. The library places the caller's; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Gridlines /Gridline#5 height is meant to change at orientation=vertical, breakpoint=desktop.                                                                                                                                                                                               |
| `component.chart gridlines.gridline5.width#unbound`                                                                              | 1, bound to no variable                                                                                                                                                  | width on /Gridline#5 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's gridlines are a sample: how many, how far apart, at two sizes. The library places the caller's; the theme takes the design, none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 1 is intended.                                                                                                                                                                                                                                     |
| `component.chart gridlines.gridline5.width@orientation=vertical, breakpoint=desktop`                                             | 1 variant at orientation=vertical, breakpoint=desktop: 1 where FILL                                                                                                      | width on /Gridline#5 should follow no axis only, but at orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** Figma's gridlines are a sample: how many, how far apart, at two sizes. The library places the caller's; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Gridlines /Gridline#5 width is meant to change at orientation=vertical, breakpoint=desktop.                                                                                                                                                                                                |
| `component.chart gridlines.root.direction@orientation=vertical, breakpoint=desktop`                                              | 1 variant at orientation=vertical, breakpoint=desktop: HORIZONTAL where VERTICAL                                                                                         | direction on / should follow no axis only, but at orientation=vertical, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's gridlines are a sample: how many, how far apart, at two sizes. The library places the caller's; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Gridlines / direction is meant to change at orientation=vertical, breakpoint=desktop.                                                                                                                                                                                                      |
| `component.chart gridlines.root.direction@orientation=vertical, breakpoint=mobile`                                               | 1 variant at orientation=vertical, breakpoint=mobile: HORIZONTAL where VERTICAL                                                                                          | direction on / should follow no axis only, but at orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Figma's gridlines are a sample: how many, how far apart, at two sizes. The library places the caller's; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Gridlines / direction is meant to change at orientation=vertical, breakpoint=mobile.                                                                                                                                                                                                       |
| `component.chart gridlines.root.height#unbound`                                                                                  | 280, 220, bound to no variable                                                                                                                                           | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's gridlines are a sample: how many, how far apart, at two sizes. The library places the caller's; the theme takes the design, none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 280, 220 is intended.                                                                                                                                                                                                                             |
| `component.chart gridlines.root.height@orientation=horizontal, breakpoint=mobile`                                                | 1 variant at orientation=horizontal, breakpoint=mobile: 220 where 280                                                                                                    | height on / should follow no axis only, but at orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (accept):** Figma's gridlines are a sample: how many, how far apart, at two sizes. The library places the caller's; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Gridlines / height is meant to change at orientation=horizontal, breakpoint=mobile.                                                                                                                                                                                                        |
| `component.chart gridlines.root.height@orientation=vertical, breakpoint=mobile`                                                  | 1 variant at orientation=vertical, breakpoint=mobile: 220 where 280                                                                                                      | height on / should follow no axis only, but at orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (accept):** Figma's gridlines are a sample: how many, how far apart, at two sizes. The library places the caller's; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Gridlines / height is meant to change at orientation=vertical, breakpoint=mobile.                                                                                                                                                                                                          |
| `component.chart gridlines.root.paddingBottom#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's gridlines are a sample: how many, how far apart, at two sizes. The library places the caller's; the theme takes the design, none of which is left open. Ask SOLAR to bind Chart Gridlines / paddingBottom to inset.none, which has the same value.                                                                                                                                                                                                                         |
| `component.chart gridlines.root.paddingLeft#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (accept):** Figma's gridlines are a sample: how many, how far apart, at two sizes. The library places the caller's; the theme takes the design, none of which is left open. Ask SOLAR to bind Chart Gridlines / paddingLeft to inset.none, which has the same value.                                                                                                                                                                                                                           |
| `component.chart gridlines.root.paddingRight#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Figma's gridlines are a sample: how many, how far apart, at two sizes. The library places the caller's; the theme takes the design, none of which is left open. Ask SOLAR to bind Chart Gridlines / paddingRight to inset.none, which has the same value.                                                                                                                                                                                                                          |
| `component.chart gridlines.root.paddingTop#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** Figma's gridlines are a sample: how many, how far apart, at two sizes. The library places the caller's; the theme takes the design, none of which is left open. Ask SOLAR to bind Chart Gridlines / paddingTop to inset.none, which has the same value.                                                                                                                                                                                                                            |
| `component.chart gridlines.root.width#unbound`                                                                                   | 600, 343, bound to no variable                                                                                                                                           | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's gridlines are a sample: how many, how far apart, at two sizes. The library places the caller's; the theme takes the design, none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 600, 343 is intended.                                                                                                                                                                                                                              |
| `component.chart gridlines.root.width@orientation=horizontal, breakpoint=mobile`                                                 | 1 variant at orientation=horizontal, breakpoint=mobile: 343 where 600                                                                                                    | width on / should follow no axis only, but at orientation=horizontal, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** Figma's gridlines are a sample: how many, how far apart, at two sizes. The library places the caller's; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Gridlines / width is meant to change at orientation=horizontal, breakpoint=mobile.                                                                                                                                                                                                         |
| `component.chart gridlines.root.width@orientation=vertical, breakpoint=mobile`                                                   | 1 variant at orientation=vertical, breakpoint=mobile: 343 where 600                                                                                                      | width on / should follow no axis only, but at orientation=vertical, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (accept):** Figma's gridlines are a sample: how many, how far apart, at two sizes. The library places the caller's; the theme takes the design, none of which is left open. Ask SOLAR whether Chart Gridlines / width is meant to change at orientation=vertical, breakpoint=mobile.                                                                                                                                                                                                           |
| `component.chart tooltip.frame.paddingBottom#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Chart Tooltip /Frame paddingBottom to inset.none, which has the same value.                                          |
| `component.chart tooltip.frame.paddingLeft#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Chart Tooltip /Frame paddingLeft to inset.none, which has the same value.                                            |
| `component.chart tooltip.frame.paddingRight#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingRight on /Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Chart Tooltip /Frame paddingRight to inset.none, which has the same value.                                           |
| `component.chart tooltip.frame.paddingTop#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingTop on /Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Chart Tooltip /Frame paddingTop to inset.none, which has the same value.                                             |
| `component.chart tooltip.frame.width#unbound`                                                                                    | 132, bound to no variable                                                                                                                                                | width on /Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (allowLiteral):** A multi row is 132 wide, bound to nothing: SOLAR has no size for it, one of the sizes with no variable; its values align at its end. SOLAR has no control width token; ask SOLAR for one, or confirm 132 is intended.                                                                                                                                                                                                                                                        |
| `component.chart tooltip.root.borderWidth#unbound`                                                                               | 1, bound to no variable                                                                                                                                                  | borderWidth on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind):** Same value, governed token, the tooltip's edge. Ask SOLAR to bind Chart Tooltip / borderWidth to border.default, which has the same value.                                                                                                                                                                                                                                                                                                                                           |
| `component.chart tooltip.rowSwatch.height#unbound`                                                                               | 8, bound to no variable                                                                                                                                                  | height on /Frame/Frame/StatusIndicator is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (set):** The dot is a SOLAR StatusIndicator (xs), whose 8px box is its own: the tooltip places it, and never sizes it. SOLAR has no control height token; ask SOLAR for one, or confirm 8 is intended.                                                                                                                                                                                                                                                                                         |
| `component.chart tooltip.rowSwatch.width#unbound`                                                                                | 8, bound to no variable                                                                                                                                                  | width on /Frame/Frame/StatusIndicator is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (set):** The dot is a SOLAR StatusIndicator (xs), whose 8px box is its own: the tooltip places it, and never sizes it. SOLAR has no control width token; ask SOLAR for one, or confirm 8 is intended.                                                                                                                                                                                                                                                                                          |
| `component.chart tooltip.series.paddingBottom#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Frame/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Chart Tooltip /Frame/Frame paddingBottom to inset.none, which has the same value.                                    |
| `component.chart tooltip.series.paddingLeft#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Frame/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Chart Tooltip /Frame/Frame paddingLeft to inset.none, which has the same value.                                      |
| `component.chart tooltip.series.paddingRight#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingRight on /Frame/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Chart Tooltip /Frame/Frame paddingRight to inset.none, which has the same value.                                     |
| `component.chart tooltip.series.paddingTop#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingTop on /Frame/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Chart Tooltip /Frame/Frame paddingTop to inset.none, which has the same value.                                       |
| `component.chart tooltip.swatch.height#unbound`                                                                                  | 8, bound to no variable                                                                                                                                                  | height on /Frame/StatusIndicator is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (set):** The dot is a SOLAR StatusIndicator (xs), whose 8px box is its own: the tooltip places it, and never sizes it. SOLAR has no control height token; ask SOLAR for one, or confirm 8 is intended.                                                                                                                                                                                                                                                                                         |
| `component.chart tooltip.swatch.width#unbound`                                                                                   | 8, bound to no variable                                                                                                                                                  | width on /Frame/StatusIndicator is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (set):** The dot is a SOLAR StatusIndicator (xs), whose 8px box is its own: the tooltip places it, and never sizes it. SOLAR has no control width token; ask SOLAR for one, or confirm 8 is intended.                                                                                                                                                                                                                                                                                          |
| `component.checkbox.container.height#unbound`                                                                                    | 2, bound to no variable                                                                                                                                                  | height on /Container is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (allowLiteral):** The dash's own size, 10 by 2, is its drawing's, as the tick's is. SOLAR has no control height token; ask SOLAR for one, or confirm 2 is intended.                                                                                                                                                                                                                                                                                                                            |
| `component.checkbox.container.width#unbound`                                                                                     | 10, bound to no variable                                                                                                                                                 | width on /Container is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (allowLiteral):** The dash's own size, 10 by 2, is its drawing's, as the tick's is. SOLAR has no control width token; ask SOLAR for one, or confirm 10 is intended.                                                                                                                                                                                                                                                                                                                            |
| `component.checkbox.icon.height#unbound`                                                                                         | 7, bound to no variable                                                                                                                                                  | height on /Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (allowLiteral):** The tick's own size, 10 by 7, is its drawing's, as Figma's outline is: a glyph, not an icon from the ladder. SOLAR has no control height token; ask SOLAR for one, or confirm 7 is intended.                                                                                                                                                                                                                                                                                 |
| `component.checkbox.icon.width#unbound`                                                                                          | 10, bound to no variable                                                                                                                                                 | width on /Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (allowLiteral):** The tick's own size, 10 by 7, is its drawing's, as Figma's outline is: a glyph, not an icon from the ladder. SOLAR has no control width token; ask SOLAR for one, or confirm 10 is intended.                                                                                                                                                                                                                                                                                 |
| `component.checkbox.root.height#unbound`                                                                                         | 16, bound to no variable                                                                                                                                                 | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (allowLiteral):** The box is 16px, and SOLAR has no control size token (the design review's question on control sizes and the target size); carried as Figma's number. SOLAR has no control height token; ask SOLAR for one, or confirm 16 is intended.                                                                                                                                                                                                                                        |
| `component.checkbox.root.paddingBottom#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Checkbox / paddingBottom to inset.none, which has the same value.                                                    |
| `component.checkbox.root.paddingLeft#unbound`                                                                                    | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Checkbox / paddingLeft to inset.none, which has the same value.                                                      |
| `component.checkbox.root.paddingRight#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Checkbox / paddingRight to inset.none, which has the same value.                                                     |
| `component.checkbox.root.paddingTop#unbound`                                                                                     | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Checkbox / paddingTop to inset.none, which has the same value.                                                       |
| `component.checkbox.root.width#unbound`                                                                                          | 16, bound to no variable                                                                                                                                                 | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (allowLiteral):** The box is 16px, and SOLAR has no control size token (the design review's question on control sizes and the target size); carried as Figma's number. SOLAR has no control width token; ask SOLAR for one, or confirm 16 is intended.                                                                                                                                                                                                                                         |
| `component.coachmark.card.width#unbound`                                                                                         | 320, bound to no variable                                                                                                                                                | width on /Card is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (allowLiteral):** A coachmark's card is 320 wide, bound to nothing: SOLAR has no overlay width, one of the sizes with no variable. Its words wrap within it. SOLAR has no control width token; ask SOLAR for one, or confirm 320 is intended.                                                                                                                                                                                                                                                  |
| `component.coachmark.close.height#unbound`                                                                                       | 20, bound to no variable                                                                                                                                                 | height on /Card/Header/Icon/Close is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind):** Same value, governed token; the icon's width is already bound to it. Ask SOLAR to bind Coachmark /Card/Header/Icon/Close height to icon.md, which has the same value.                                                                                                                                                                                                                                                                                                                |
| `component.coachmark.connector.height#unbound`                                                                                   | 6, 7, bound to no variable                                                                                                                                               | height on /Connector is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (allowLiteral):** The connector's box is as tall as its dot, in the drawing's own coordinates: part of the drawing, not a size of the system. SOLAR has no control height token; ask SOLAR for one, or confirm 6, 7 is intended.                                                                                                                                                                                                                                                               |
| `component.coachmark.connector.paddingBottom#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Connector is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Coachmark /Connector paddingBottom to inset.none, which has the same value.                                          |
| `component.coachmark.connector.paddingLeft#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Connector is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Coachmark /Connector paddingLeft to inset.none, which has the same value.                                            |
| `component.coachmark.connector.paddingRight#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingRight on /Connector is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Coachmark /Connector paddingRight to inset.none, which has the same value.                                           |
| `component.coachmark.connector.paddingTop#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingTop on /Connector is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Coachmark /Connector paddingTop to inset.none, which has the same value.                                             |
| `component.coachmark.connector.width#unbound`                                                                                    | 100, bound to no variable                                                                                                                                                | width on /Connector is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (allowLiteral):** The connector is 100 long, bound to nothing: SOLAR has no length for it. The card sits that far from the element the step is about. SOLAR has no control width token; ask SOLAR for one, or confirm 100 is intended.                                                                                                                                                                                                                                                         |
| `component.coachmark.header.paddingBottom#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Card/Header is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Coachmark /Card/Header paddingBottom to inset.none, which has the same value.                                        |
| `component.coachmark.header.paddingLeft#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Card/Header is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Coachmark /Card/Header paddingLeft to inset.none, which has the same value.                                          |
| `component.coachmark.header.paddingRight#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingRight on /Card/Header is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Coachmark /Card/Header paddingRight to inset.none, which has the same value.                                         |
| `component.coachmark.header.paddingTop#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingTop on /Card/Header is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Coachmark /Card/Header paddingTop to inset.none, which has the same value.                                           |
| `component.coachmark.line.height#unbound`                                                                                        | 1, bound to no variable                                                                                                                                                  | height on /Connector/Line is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind):** The connector's line is a hairline, 1px, border.default's width, the same value, as a Divider's rule. SOLAR has no control height token; ask SOLAR for one, or confirm 1 is intended.                                                                                                                                                                                                                                                                                                |
| `component.coachmark.nodeEnd.height#unbound`                                                                                     | 12, bound to no variable                                                                                                                                                 | height on /Connector/Node End is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (set):** The connector's end is a SOLAR Node End (halo), whose 12px box is its own: the coachmark places it, and never sizes it. Ask SOLAR to bind Coachmark /Connector/Node End height to icon.xs, which has the same value.                                                                                                                                                                                                                                                                  |
| `component.coachmark.nodeEnd.width#unbound`                                                                                      | 12, bound to no variable                                                                                                                                                 | width on /Connector/Node End is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (set):** The connector's end is a SOLAR Node End (halo), whose 12px box is its own: the coachmark places it, and never sizes it. Ask SOLAR to bind Coachmark /Connector/Node End width to icon.xs, which has the same value.                                                                                                                                                                                                                                                                   |
| `component.coachmark.originDot.height#unbound`                                                                                   | 6, bound to no variable                                                                                                                                                  | height on /Connector/Origin Dot is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (allowLiteral):** The dot the connector leaves the card from is 6px, part of the drawing, as Node End's dot: SOLAR has no marker size. SOLAR has no control height token; ask SOLAR for one, or confirm 6 is intended.                                                                                                                                                                                                                                                                         |
| `component.coachmark.originDot.width#unbound`                                                                                    | 6, bound to no variable                                                                                                                                                  | width on /Connector/Origin Dot is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (allowLiteral):** The dot the connector leaves the card from is 6px, part of the drawing, as Node End's dot: SOLAR has no marker size. SOLAR has no control width token; ask SOLAR for one, or confirm 6 is intended.                                                                                                                                                                                                                                                                          |
| `component.coachmark.root.gap#unbound`                                                                                           | 0, bound to no variable                                                                                                                                                  | gap on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Coachmark / gap to inset.none or stack.none, which has the same value.                                 |
| `component.coachmark.root.paddingBottom#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Coachmark / paddingBottom to inset.none, which has the same value.                                                   |
| `component.coachmark.root.paddingLeft#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Coachmark / paddingLeft to inset.none, which has the same value.                                                     |
| `component.coachmark.root.paddingRight#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Coachmark / paddingRight to inset.none, which has the same value.                                                    |
| `component.coachmark.root.paddingTop#unbound`                                                                                    | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Coachmark / paddingTop to inset.none, which has the same value.                                                      |
| `component.column item.avatar.height#unbound`                                                                                    | 24, bound to no variable                                                                                                                                                 | height on /Avatar is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (set):** The user is the caller's SOLAR Avatar, sm, whose 24px box is its own. Ask SOLAR to bind Column Item /Avatar height to icon.lg, which has the same value.                                                                                                                                                                                                                                                                                                                              |
| `component.column item.avatar.width#unbound`                                                                                     | 24, bound to no variable                                                                                                                                                 | width on /Avatar is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (set):** The user is the caller's SOLAR Avatar, sm, whose 24px box is its own. Ask SOLAR to bind Column Item /Avatar width to icon.lg, which has the same value.                                                                                                                                                                                                                                                                                                                               |
| `component.column item.button.height#unbound`                                                                                    | 32, bound to no variable                                                                                                                                                 | height on /Button is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (set):** The action is the caller's SOLAR Button, sm, whose 32px height is its own. Ask SOLAR to bind Column Item /Button height to icon.2xl, which has the same value.                                                                                                                                                                                                                                                                                                                        |
| `component.column item.icon.width#unbound`                                                                                       | 20, bound to no variable                                                                                                                                                 | width on /Icon/More is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (bind):** Same value, governed token; the icon's height is already bound to it. Ask SOLAR to bind Column Item /Icon/More width to icon.md, which has the same value.                                                                                                                                                                                                                                                                                                                           |
| `component.column item.root.height#unbound`                                                                                      | 40, bound to no variable                                                                                                                                                 | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (allowLiteral):** A table cell is 40 tall, bound to nothing: SOLAR publishes no row height (RowExpand's and a tree row's are the same gap). In a Row it fills the row. SOLAR has no control height token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                        |
| `component.column item.separator.height#unbound`                                                                                 | 26, bound to no variable                                                                                                                                                 | height on /Separator is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (allowLiteral):** The separator is 26 tall, centred in the 40px cell, bound to nothing: SOLAR has no size for a column rule. SOLAR has no control height token; ask SOLAR for one, or confirm 26 is intended.                                                                                                                                                                                                                                                                                  |
| `component.column item.separator.width#unbound`                                                                                  | 1, bound to no variable                                                                                                                                                  | width on /Separator is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (bind):** The separator is the column's edge, a 1px rule: border.default, the same value. SOLAR has no control width token; ask SOLAR for one, or confirm 1 is intended.                                                                                                                                                                                                                                                                                                                       |
| `component.column item.tag.height#unbound`                                                                                       | 24, bound to no variable                                                                                                                                                 | height on /Tag is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (set):** The status is the caller's SOLAR Tag, whose 24px height is its own: the cell places it, and never sizes it. Ask SOLAR to bind Column Item /Tag height to icon.lg, which has the same value.                                                                                                                                                                                                                                                                                           |
| `component.column item.toggle.height#unbound`                                                                                    | 18, bound to no variable                                                                                                                                                 | height on /Toggle is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (set):** The setting is the caller's SOLAR Toggle, whose 32 × 18 track is its own. SOLAR has no control height token; ask SOLAR for one, or confirm 18 is intended.                                                                                                                                                                                                                                                                                                                            |
| `component.column item.toggle.width#unbound`                                                                                     | 32, bound to no variable                                                                                                                                                 | width on /Toggle is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (set):** The setting is the caller's SOLAR Toggle, whose 32 × 18 track is its own. Ask SOLAR to bind Column Item /Toggle width to icon.2xl, which has the same value.                                                                                                                                                                                                                                                                                                                          |
| `component.confirmationdialog.root.gap#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | gap on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind ConfirmationDialog / gap to inset.none or stack.none, which has the same value.                        |
| `component.confirmationdialog.root.paddingBottom#unbound`                                                                        | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind ConfirmationDialog / paddingBottom to inset.none, which has the same value.                                          |
| `component.confirmationdialog.root.paddingLeft#unbound`                                                                          | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind ConfirmationDialog / paddingLeft to inset.none, which has the same value.                                            |
| `component.confirmationdialog.root.paddingRight#unbound`                                                                         | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind ConfirmationDialog / paddingRight to inset.none, which has the same value.                                           |
| `component.confirmationdialog.root.paddingTop#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind ConfirmationDialog / paddingTop to inset.none, which has the same value.                                             |
| `component.container.content.gap#unbound`                                                                                        | 0, bound to no variable                                                                                                                                                  | gap on /content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Container /content gap to inset.none or stack.none, which has the same value.                          |
| `component.container.content.paddingBottom#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingBottom on /content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Container /content paddingBottom to inset.none, which has the same value.                                            |
| `component.container.content.paddingLeft#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingLeft on /content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Container /content paddingLeft to inset.none, which has the same value.                                              |
| `component.container.content.paddingRight#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingRight on /content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Container /content paddingRight to inset.none, which has the same value.                                             |
| `component.container.content.paddingTop#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingTop on /content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Container /content paddingTop to inset.none, which has the same value.                                               |
| `component.container.root.width#unbound`                                                                                         | 440, bound to no variable                                                                                                                                                | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** A container spans the surface it groups content in: Figma draws it 440 wide, a sample. The caller sizes it. SOLAR has no control width token; ask SOLAR for one, or confirm 440 is intended.                                                                                                                                                                                                                                                                                          |
| `component.context menu item.leadingIcon.width#unbound`                                                                          | 16, bound to no variable                                                                                                                                                 | width on /LeadingIcon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind):** Same value, governed token; the icon's height is already bound to it. Ask SOLAR to bind Context Menu Item /LeadingIcon width to icon.sm, which has the same value.                                                                                                                                                                                                                                                                                                                   |
| `component.context menu item.root.height#unbound`                                                                                | 36, bound to no variable                                                                                                                                                 | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (allowLiteral):** A row is 36 tall, bound to nothing, between two of SOLAR's control heights (size.control: 32, 40); carried as drawn, the design review asks. SOLAR has no control height token; ask SOLAR for one, or confirm 36 is intended.                                                                                                                                                                                                                                                |
| `component.context menu item.root.paddingBottom#unbound`                                                                         | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Context Menu Item / paddingBottom to inset.none, which has the same value.                                           |
| `component.context menu item.root.paddingTop#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Context Menu Item / paddingTop to inset.none, which has the same value.                                              |
| `component.context menu item.root.width#unbound`                                                                                 | 200, bound to no variable                                                                                                                                                | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** A row spans its menu, whose width is its widest row's: Figma draws it 200 wide, a sample. SOLAR has no control width token; ask SOLAR for one, or confirm 200 is intended.                                                                                                                                                                                                                                                                                                            |
| `component.context menu item.trailingIcon.width#unbound`                                                                         | 16, bound to no variable                                                                                                                                                 | width on /LeadingIcon#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind):** As the leading icon's. Ask SOLAR to bind Context Menu Item /LeadingIcon#2 width to icon.sm, which has the same value.                                                                                                                                                                                                                                                                                                                                                                |
| `component.context menu.content.gap#unbound`                                                                                     | 0, bound to no variable                                                                                                                                                  | gap on /Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Context Menu /Content gap to inset.none or stack.none, which has the same value.                       |
| `component.context menu.content.paddingBottom#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Context Menu /Content paddingBottom to inset.none, which has the same value.                                         |
| `component.context menu.content.paddingLeft#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Context Menu /Content paddingLeft to inset.none, which has the same value.                                           |
| `component.context menu.content.paddingRight#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingRight on /Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Context Menu /Content paddingRight to inset.none, which has the same value.                                          |
| `component.context menu.content.paddingTop#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingTop on /Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Context Menu /Content paddingTop to inset.none, which has the same value.                                            |
| `component.context menu.contextMenuItem.height#unbound`                                                                          | 36, bound to no variable                                                                                                                                                 | height on /Content/Context Menu Item is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (set):** A row's height is its own (36), the Context Menu Item's; the menu never sizes it. SOLAR has no control height token; ask SOLAR for one, or confirm 36 is intended.                                                                                                                                                                                                                                                                                                                    |
| `component.context menu.contextMenuItem.width#unbound`                                                                           | 200, bound to no variable                                                                                                                                                | width on /Content/Context Menu Item is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (set):** The rows are the caller's, and span the menu: Figma draws them 200 wide, a sample of a menu's width, which is its widest row's. SOLAR has no control width token; ask SOLAR for one, or confirm 200 is intended.                                                                                                                                                                                                                                                                      |
| `component.context menu.contextMenuItem2.height#unbound`                                                                         | 36, bound to no variable                                                                                                                                                 | height on /Content/Context Menu Item#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (set):** A row's height is its own (36), the Context Menu Item's; the menu never sizes it. SOLAR has no control height token; ask SOLAR for one, or confirm 36 is intended.                                                                                                                                                                                                                                                                                                                    |
| `component.context menu.contextMenuItem2.width#unbound`                                                                          | 200, bound to no variable                                                                                                                                                | width on /Content/Context Menu Item#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (set):** The rows are the caller's, and span the menu: Figma draws them 200 wide, a sample of a menu's width, which is its widest row's. SOLAR has no control width token; ask SOLAR for one, or confirm 200 is intended.                                                                                                                                                                                                                                                                      |
| `component.context menu.contextMenuItem3.height#unbound`                                                                         | 36, bound to no variable                                                                                                                                                 | height on /Content/Context Menu Item#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (set):** A row's height is its own (36), the Context Menu Item's; the menu never sizes it. SOLAR has no control height token; ask SOLAR for one, or confirm 36 is intended.                                                                                                                                                                                                                                                                                                                    |
| `component.context menu.contextMenuItem3.width#unbound`                                                                          | 200, bound to no variable                                                                                                                                                | width on /Content/Context Menu Item#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (set):** The rows are the caller's, and span the menu: Figma draws them 200 wide, a sample of a menu's width, which is its widest row's. SOLAR has no control width token; ask SOLAR for one, or confirm 200 is intended.                                                                                                                                                                                                                                                                      |
| `component.context menu.contextMenuItem4.height#unbound`                                                                         | 36, bound to no variable                                                                                                                                                 | height on /Content/Context Menu Item#4 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (set):** A row's height is its own (36), the Context Menu Item's; the menu never sizes it. SOLAR has no control height token; ask SOLAR for one, or confirm 36 is intended.                                                                                                                                                                                                                                                                                                                    |
| `component.context menu.contextMenuItem4.width#unbound`                                                                          | 200, bound to no variable                                                                                                                                                | width on /Content/Context Menu Item#4 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (set):** The rows are the caller's, and span the menu: Figma draws them 200 wide, a sample of a menu's width, which is its widest row's. SOLAR has no control width token; ask SOLAR for one, or confirm 200 is intended.                                                                                                                                                                                                                                                                      |
| `component.context menu.contextMenuItem5.height#unbound`                                                                         | 36, bound to no variable                                                                                                                                                 | height on /Content/Context Menu Item#5 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (set):** A row's height is its own (36), the Context Menu Item's; the menu never sizes it. SOLAR has no control height token; ask SOLAR for one, or confirm 36 is intended.                                                                                                                                                                                                                                                                                                                    |
| `component.context menu.contextMenuItem5.width#unbound`                                                                          | 200, bound to no variable                                                                                                                                                | width on /Content/Context Menu Item#5 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (set):** The rows are the caller's, and span the menu: Figma draws them 200 wide, a sample of a menu's width, which is its widest row's. SOLAR has no control width token; ask SOLAR for one, or confirm 200 is intended.                                                                                                                                                                                                                                                                      |
| `component.context menu.contextMenuItem6.height#unbound`                                                                         | 36, bound to no variable                                                                                                                                                 | height on /Content/Context Menu Item#6 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (set):** A row's height is its own (36), the Context Menu Item's; the menu never sizes it. SOLAR has no control height token; ask SOLAR for one, or confirm 36 is intended.                                                                                                                                                                                                                                                                                                                    |
| `component.context menu.contextMenuItem6.width#unbound`                                                                          | 200, bound to no variable                                                                                                                                                | width on /Content/Context Menu Item#6 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (set):** The rows are the caller's, and span the menu: Figma draws them 200 wide, a sample of a menu's width, which is its widest row's. SOLAR has no control width token; ask SOLAR for one, or confirm 200 is intended.                                                                                                                                                                                                                                                                      |
| `component.context menu.contextMenuItem7.height#unbound`                                                                         | 36, bound to no variable                                                                                                                                                 | height on /Content/Context Menu Item#7 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (set):** A row's height is its own (36), the Context Menu Item's; the menu never sizes it. SOLAR has no control height token; ask SOLAR for one, or confirm 36 is intended.                                                                                                                                                                                                                                                                                                                    |
| `component.context menu.contextMenuItem7.width#unbound`                                                                          | 200, bound to no variable                                                                                                                                                | width on /Content/Context Menu Item#7 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (set):** The rows are the caller's, and span the menu: Figma draws them 200 wide, a sample of a menu's width, which is its widest row's. SOLAR has no control width token; ask SOLAR for one, or confirm 200 is intended.                                                                                                                                                                                                                                                                      |
| `component.context menu.divider.height#unbound`                                                                                  | 1, bound to no variable                                                                                                                                                  | height on /Content/Divider is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** The divider is a SOLAR Divider, whose 1px rule is its own: the menu places it, and never sizes it. SOLAR has no control height token; ask SOLAR for one, or confirm 1 is intended.                                                                                                                                                                                                                                                                                                    |
| `component.context menu.root.gap#unbound`                                                                                        | 0, bound to no variable                                                                                                                                                  | gap on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Context Menu / gap to inset.none or stack.none, which has the same value.                              |
| `component.counter.root.gap#unbound`                                                                                             | 0, bound to no variable                                                                                                                                                  | gap on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Counter / gap to inset.none or stack.none, which has the same value.                                   |
| `component.counter.root.height#unbound`                                                                                          | 20, bound to no variable                                                                                                                                                 | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (allowLiteral):** The badge is 20 tall and bound to nothing; no SOLAR token names a badge height, as Button's counter slot is not (button.yaml). Raised as a governance gap. SOLAR has no control height token; ask SOLAR for one, or confirm 20 is intended.                                                                                                                                                                                                                                  |
| `component.counter.root.paddingBottom#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Counter / paddingBottom to inset.none, which has the same value.                                                     |
| `component.counter.root.paddingTop#unbound`                                                                                      | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Counter / paddingTop to inset.none, which has the same value.                                                        |
| `component.data legend.item.paddingBottom#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Item is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Data Legend /Item paddingBottom to inset.none, which has the same value.                                             |
| `component.data legend.item.paddingLeft#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Item is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Data Legend /Item paddingLeft to inset.none, which has the same value.                                               |
| `component.data legend.item.paddingRight#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingRight on /Item is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Data Legend /Item paddingRight to inset.none, which has the same value.                                              |
| `component.data legend.item.paddingTop#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingTop on /Item is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Data Legend /Item paddingTop to inset.none, which has the same value.                                                |
| `component.data legend.root.paddingBottom#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Data Legend / paddingBottom to inset.none, which has the same value.                                                 |
| `component.data legend.root.paddingLeft#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Data Legend / paddingLeft to inset.none, which has the same value.                                                   |
| `component.data legend.root.paddingRight#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Data Legend / paddingRight to inset.none, which has the same value.                                                  |
| `component.data legend.root.paddingTop#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Data Legend / paddingTop to inset.none, which has the same value.                                                    |
| `component.data legend.swatch.height#unbound`                                                                                    | 8, bound to no variable                                                                                                                                                  | height on /Item/StatusIndicator is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (set):** The dot is a SOLAR StatusIndicator (xs), whose 8px box is its own: the legend places it, and never sizes it. SOLAR has no control height token; ask SOLAR for one, or confirm 8 is intended.                                                                                                                                                                                                                                                                                          |
| `component.data legend.swatch.width#unbound`                                                                                     | 8, bound to no variable                                                                                                                                                  | width on /Item/StatusIndicator is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (set):** The dot is a SOLAR StatusIndicator (xs), whose 8px box is its own: the legend places it, and never sizes it. SOLAR has no control width token; ask SOLAR for one, or confirm 8 is intended.                                                                                                                                                                                                                                                                                           |
| `component.date picker day cell.root.borderWidth#unbound`                                                                        | 1, bound to no variable                                                                                                                                                  | borderWidth on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind):** Same value, governed token; today's and the focused cell's edges are bound to it. Ask SOLAR to bind Date Picker Day Cell / borderWidth to border.default, which has the same value.                                                                                                                                                                                                                                                                                                  |
| `component.date picker day cell.root.gap#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | gap on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Date Picker Day Cell / gap to inset.none or stack.none, which has the same value.                      |
| `component.date picker day cell.root.height#unbound`                                                                             | 36, bound to no variable                                                                                                                                                 | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (allowLiteral):** As its width. SOLAR has no control height token; ask SOLAR for one, or confirm 36 is intended.                                                                                                                                                                                                                                                                                                                                                                               |
| `component.date picker day cell.root.paddingBottom#unbound`                                                                      | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Date Picker Day Cell / paddingBottom to inset.none, which has the same value.                                        |
| `component.date picker day cell.root.paddingLeft#unbound`                                                                        | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Date Picker Day Cell / paddingLeft to inset.none, which has the same value.                                          |
| `component.date picker day cell.root.paddingRight#unbound`                                                                       | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Date Picker Day Cell / paddingRight to inset.none, which has the same value.                                         |
| `component.date picker day cell.root.paddingTop#unbound`                                                                         | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Date Picker Day Cell / paddingTop to inset.none, which has the same value.                                           |
| `component.date picker day cell.root.radiusBottomLeft#unbound`                                                                   | 0, bound to no variable                                                                                                                                                  | radiusBottomLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind):** As the top-left corner's. Ask SOLAR to bind Date Picker Day Cell / radiusBottomLeft to radius.none, which has the same value.                                                                                                                                                                                                                                                                                                                                                        |
| `component.date picker day cell.root.radiusBottomRight#unbound`                                                                  | 0, 18, bound to no variable                                                                                                                                              | radiusBottomRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (set):** As its top-right corner. Ask SOLAR to bind Date Picker Day Cell / radiusBottomRight to radius.none, which has the same value.                                                                                                                                                                                                                                                                                                                                                         |
| `component.date picker day cell.root.radiusTopLeft#unbound`                                                                      | 0, bound to no variable                                                                                                                                                  | radiusTopLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind):** Same value, governed token, a range's square side. Ask SOLAR to bind Date Picker Day Cell / radiusTopLeft to radius.none, which has the same value.                                                                                                                                                                                                                                                                                                                                  |
| `component.date picker day cell.root.radiusTopRight#unbound`                                                                     | 0, 18, bound to no variable                                                                                                                                              | radiusTopRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (set):** Same value, governed token, a range start's square side, as the end's left. Ask SOLAR to bind Date Picker Day Cell / radiusTopRight to radius.none, which has the same value.                                                                                                                                                                                                                                                                                                         |
| `component.date picker day cell.root.width#unbound`                                                                              | 36, bound to no variable                                                                                                                                                 | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (allowLiteral):** A day is 36 square, bound to nothing: SOLAR has no size token for a grid's cell (the design review's question on sizes with no variable). SOLAR has no control width token; ask SOLAR for one, or confirm 36 is intended.                                                                                                                                                                                                                                                    |
| `component.date picker open.container.paddingBottom#unbound`                                                                     | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Container is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Date Picker Open /Container paddingBottom to inset.none, which has the same value.                                   |
| `component.date picker open.container.paddingLeft#unbound`                                                                       | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Container is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Date Picker Open /Container paddingLeft to inset.none, which has the same value.                                     |
| `component.date picker open.container.paddingRight#unbound`                                                                      | 0, bound to no variable                                                                                                                                                  | paddingRight on /Container is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Date Picker Open /Container paddingRight to inset.none, which has the same value.                                    |
| `component.date picker open.container.paddingTop#unbound`                                                                        | 0, bound to no variable                                                                                                                                                  | paddingTop on /Container is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Date Picker Open /Container paddingTop to inset.none, which has the same value.                                      |
| `component.date picker open.container2.paddingBottom#unbound`                                                                    | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Container#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Date Picker Open /Container#2 paddingBottom to inset.none, which has the same value.                                 |
| `component.date picker open.container2.paddingLeft#unbound`                                                                      | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Container#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Date Picker Open /Container#2 paddingLeft to inset.none, which has the same value.                                   |
| `component.date picker open.container2.paddingRight#unbound`                                                                     | 0, bound to no variable                                                                                                                                                  | paddingRight on /Container#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Date Picker Open /Container#2 paddingRight to inset.none, which has the same value.                                  |
| `component.date picker open.container2.paddingTop#unbound`                                                                       | 0, bound to no variable                                                                                                                                                  | paddingTop on /Container#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Date Picker Open /Container#2 paddingTop to inset.none, which has the same value.                                    |
| `component.date picker open.container2DayGrid.paddingBottom#unbound`                                                             | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Container#2/DayGrid is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Date Picker Open /Container#2/DayGrid paddingBottom to inset.none, which has the same value.                         |
| `component.date picker open.container2DayGrid.paddingLeft#unbound`                                                               | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Container#2/DayGrid is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Date Picker Open /Container#2/DayGrid paddingLeft to inset.none, which has the same value.                           |
| `component.date picker open.container2DayGrid.paddingRight#unbound`                                                              | 0, bound to no variable                                                                                                                                                  | paddingRight on /Container#2/DayGrid is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Date Picker Open /Container#2/DayGrid paddingRight to inset.none, which has the same value.                          |
| `component.date picker open.container2DayGrid.paddingTop#unbound`                                                                | 0, bound to no variable                                                                                                                                                  | paddingTop on /Container#2/DayGrid is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Date Picker Open /Container#2/DayGrid paddingTop to inset.none, which has the same value.                            |
| `component.date picker open.container2DayGridDayCell.height#unbound`                                                             | 36, bound to no variable                                                                                                                                                 | height on /Container#2/DayGrid/Day Cell is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (set):** As the first day's, its own. SOLAR has no control height token; ask SOLAR for one, or confirm 36 is intended.                                                                                                                                                                                                                                                                                                                                                                         |
| `component.date picker open.container2DayGridDayCell.width#unbound`                                                              | 36, bound to no variable                                                                                                                                                 | width on /Container#2/DayGrid/Day Cell is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (set):** As the first day's, its own. SOLAR has no control width token; ask SOLAR for one, or confirm 36 is intended.                                                                                                                                                                                                                                                                                                                                                                          |
| `component.date picker open.container2MonthHeader.gap#unbound`                                                                   | 0, bound to no variable                                                                                                                                                  | gap on /Container#2/MonthHeader is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Date Picker Open /Container#2/MonthHeader gap to inset.none or stack.none, which has the same value.   |
| `component.date picker open.container2MonthHeader.height#unbound`                                                                | 32, bound to no variable                                                                                                                                                 | height on /Container#2/MonthHeader is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (allowLiteral):** As the single calendar's header. SOLAR has no control height token; ask SOLAR for one, or confirm 32 is intended.                                                                                                                                                                                                                                                                                                                                                            |
| `component.date picker open.container2MonthHeader.paddingBottom#unbound`                                                         | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Container#2/MonthHeader is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Date Picker Open /Container#2/MonthHeader paddingBottom to inset.none, which has the same value.                     |
| `component.date picker open.container2MonthHeader.paddingLeft#unbound`                                                           | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Container#2/MonthHeader is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Date Picker Open /Container#2/MonthHeader paddingLeft to inset.none, which has the same value.                       |
| `component.date picker open.container2MonthHeader.paddingRight#unbound`                                                          | 0, bound to no variable                                                                                                                                                  | paddingRight on /Container#2/MonthHeader is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Date Picker Open /Container#2/MonthHeader paddingRight to inset.none, which has the same value.                      |
| `component.date picker open.container2MonthHeader.paddingTop#unbound`                                                            | 0, bound to no variable                                                                                                                                                  | paddingTop on /Container#2/MonthHeader is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Date Picker Open /Container#2/MonthHeader paddingTop to inset.none, which has the same value.                        |
| `component.date picker open.container2MonthHeaderIconArrowRight.width#unbound`                                                   | 20, bound to no variable                                                                                                                                                 | width on /Container#2/MonthHeader/Icon/ArrowRight is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind):** As the single calendar's arrow. Ask SOLAR to bind Date Picker Open /Container#2/MonthHeader/Icon/ArrowRight width to icon.md, which has the same value.                                                                                                                                                                                                                                                                                                                              |
| `component.date picker open.container2WeekdayRow.paddingBottom#unbound`                                                          | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Container#2/WeekdayRow is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Date Picker Open /Container#2/WeekdayRow paddingBottom to inset.none, which has the same value.                      |
| `component.date picker open.container2WeekdayRow.paddingLeft#unbound`                                                            | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Container#2/WeekdayRow is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Date Picker Open /Container#2/WeekdayRow paddingLeft to inset.none, which has the same value.                        |
| `component.date picker open.container2WeekdayRow.paddingRight#unbound`                                                           | 0, bound to no variable                                                                                                                                                  | paddingRight on /Container#2/WeekdayRow is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Date Picker Open /Container#2/WeekdayRow paddingRight to inset.none, which has the same value.                       |
| `component.date picker open.container2WeekdayRow.paddingTop#unbound`                                                             | 0, bound to no variable                                                                                                                                                  | paddingTop on /Container#2/WeekdayRow is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Date Picker Open /Container#2/WeekdayRow paddingTop to inset.none, which has the same value.                         |
| `component.date picker open.containerDayGrid.paddingBottom#unbound`                                                              | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Container/DayGrid is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Date Picker Open /Container/DayGrid paddingBottom to inset.none, which has the same value.                           |
| `component.date picker open.containerDayGrid.paddingLeft#unbound`                                                                | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Container/DayGrid is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Date Picker Open /Container/DayGrid paddingLeft to inset.none, which has the same value.                             |
| `component.date picker open.containerDayGrid.paddingRight#unbound`                                                               | 0, bound to no variable                                                                                                                                                  | paddingRight on /Container/DayGrid is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Date Picker Open /Container/DayGrid paddingRight to inset.none, which has the same value.                            |
| `component.date picker open.containerDayGrid.paddingTop#unbound`                                                                 | 0, bound to no variable                                                                                                                                                  | paddingTop on /Container/DayGrid is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Date Picker Open /Container/DayGrid paddingTop to inset.none, which has the same value.                              |
| `component.date picker open.containerDayGridDayCell.height#unbound`                                                              | 36, bound to no variable                                                                                                                                                 | height on /Container/DayGrid/Day Cell is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (set):** As the first day's, its own. SOLAR has no control height token; ask SOLAR for one, or confirm 36 is intended.                                                                                                                                                                                                                                                                                                                                                                         |
| `component.date picker open.containerDayGridDayCell.width#unbound`                                                               | 36, bound to no variable                                                                                                                                                 | width on /Container/DayGrid/Day Cell is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (set):** As the first day's, its own. SOLAR has no control width token; ask SOLAR for one, or confirm 36 is intended.                                                                                                                                                                                                                                                                                                                                                                          |
| `component.date picker open.containerMonthHeader.gap#unbound`                                                                    | 0, bound to no variable                                                                                                                                                  | gap on /Container/MonthHeader is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Date Picker Open /Container/MonthHeader gap to inset.none or stack.none, which has the same value.     |
| `component.date picker open.containerMonthHeader.height#unbound`                                                                 | 32, bound to no variable                                                                                                                                                 | height on /Container/MonthHeader is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (allowLiteral):** As the single calendar's header. SOLAR has no control height token; ask SOLAR for one, or confirm 32 is intended.                                                                                                                                                                                                                                                                                                                                                            |
| `component.date picker open.containerMonthHeader.paddingBottom#unbound`                                                          | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Container/MonthHeader is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Date Picker Open /Container/MonthHeader paddingBottom to inset.none, which has the same value.                       |
| `component.date picker open.containerMonthHeader.paddingLeft#unbound`                                                            | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Container/MonthHeader is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Date Picker Open /Container/MonthHeader paddingLeft to inset.none, which has the same value.                         |
| `component.date picker open.containerMonthHeader.paddingRight#unbound`                                                           | 0, bound to no variable                                                                                                                                                  | paddingRight on /Container/MonthHeader is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Date Picker Open /Container/MonthHeader paddingRight to inset.none, which has the same value.                        |
| `component.date picker open.containerMonthHeader.paddingTop#unbound`                                                             | 0, bound to no variable                                                                                                                                                  | paddingTop on /Container/MonthHeader is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Date Picker Open /Container/MonthHeader paddingTop to inset.none, which has the same value.                          |
| `component.date picker open.containerMonthHeaderIconArrowLeft.width#unbound`                                                     | 20, bound to no variable                                                                                                                                                 | width on /Container/MonthHeader/Icon/ArrowLeft is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (bind):** As the single calendar's arrow. Ask SOLAR to bind Date Picker Open /Container/MonthHeader/Icon/ArrowLeft width to icon.md, which has the same value.                                                                                                                                                                                                                                                                                                                                 |
| `component.date picker open.containerWeekdayRow.paddingBottom#unbound`                                                           | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Container/WeekdayRow is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Date Picker Open /Container/WeekdayRow paddingBottom to inset.none, which has the same value.                        |
| `component.date picker open.containerWeekdayRow.paddingLeft#unbound`                                                             | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Container/WeekdayRow is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Date Picker Open /Container/WeekdayRow paddingLeft to inset.none, which has the same value.                          |
| `component.date picker open.containerWeekdayRow.paddingRight#unbound`                                                            | 0, bound to no variable                                                                                                                                                  | paddingRight on /Container/WeekdayRow is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Date Picker Open /Container/WeekdayRow paddingRight to inset.none, which has the same value.                         |
| `component.date picker open.containerWeekdayRow.paddingTop#unbound`                                                              | 0, bound to no variable                                                                                                                                                  | paddingTop on /Container/WeekdayRow is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Date Picker Open /Container/WeekdayRow paddingTop to inset.none, which has the same value.                           |
| `component.date picker open.dayGrid.paddingBottom#unbound`                                                                       | 0, bound to no variable                                                                                                                                                  | paddingBottom on /DayGrid is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Date Picker Open /DayGrid paddingBottom to inset.none, which has the same value.                                     |
| `component.date picker open.dayGrid.paddingLeft#unbound`                                                                         | 0, bound to no variable                                                                                                                                                  | paddingLeft on /DayGrid is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Date Picker Open /DayGrid paddingLeft to inset.none, which has the same value.                                       |
| `component.date picker open.dayGrid.paddingRight#unbound`                                                                        | 0, bound to no variable                                                                                                                                                  | paddingRight on /DayGrid is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Date Picker Open /DayGrid paddingRight to inset.none, which has the same value.                                      |
| `component.date picker open.dayGrid.paddingTop#unbound`                                                                          | 0, bound to no variable                                                                                                                                                  | paddingTop on /DayGrid is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Date Picker Open /DayGrid paddingTop to inset.none, which has the same value.                                        |
| `component.date picker open.dayGridDayCell.height#unbound`                                                                       | 36, bound to no variable                                                                                                                                                 | height on /DayGrid/Day Cell is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (set):** A day is a Date Picker Day Cell, whose 36px is its own: the grid lays it out and never sizes it. SOLAR has no control height token; ask SOLAR for one, or confirm 36 is intended.                                                                                                                                                                                                                                                                                                     |
| `component.date picker open.dayGridDayCell.width#unbound`                                                                        | 36, bound to no variable                                                                                                                                                 | width on /DayGrid/Day Cell is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** A day is a Date Picker Day Cell, whose 36px is its own: the grid lays it out and never sizes it. SOLAR has no control width token; ask SOLAR for one, or confirm 36 is intended.                                                                                                                                                                                                                                                                                                      |
| `component.date picker open.monthHeader.gap#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | gap on /MonthHeader is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Date Picker Open /MonthHeader gap to inset.none or stack.none, which has the same value.               |
| `component.date picker open.monthHeader.height#unbound`                                                                          | 32, bound to no variable                                                                                                                                                 | height on /MonthHeader is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (allowLiteral):** The month's header row is 32 tall, bound to nothing: SOLAR has no size token for it (the design review's question on sizes with no variable). SOLAR has no control height token; ask SOLAR for one, or confirm 32 is intended.                                                                                                                                                                                                                                               |
| `component.date picker open.monthHeader.paddingBottom#unbound`                                                                   | 0, bound to no variable                                                                                                                                                  | paddingBottom on /MonthHeader is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Date Picker Open /MonthHeader paddingBottom to inset.none, which has the same value.                                 |
| `component.date picker open.monthHeader.paddingLeft#unbound`                                                                     | 0, bound to no variable                                                                                                                                                  | paddingLeft on /MonthHeader is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Date Picker Open /MonthHeader paddingLeft to inset.none, which has the same value.                                   |
| `component.date picker open.monthHeader.paddingRight#unbound`                                                                    | 0, bound to no variable                                                                                                                                                  | paddingRight on /MonthHeader is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Date Picker Open /MonthHeader paddingRight to inset.none, which has the same value.                                  |
| `component.date picker open.monthHeader.paddingTop#unbound`                                                                      | 0, bound to no variable                                                                                                                                                  | paddingTop on /MonthHeader is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Date Picker Open /MonthHeader paddingTop to inset.none, which has the same value.                                    |
| `component.date picker open.monthHeaderIconArrowLeft.width#unbound`                                                              | 20, bound to no variable                                                                                                                                                 | width on /MonthHeader/Icon/ArrowLeft is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind):** Same value, governed token; the arrow's height is already bound to it. Ask SOLAR to bind Date Picker Open /MonthHeader/Icon/ArrowLeft width to icon.md, which has the same value.                                                                                                                                                                                                                                                                                                    |
| `component.date picker open.monthHeaderIconArrowRight.width#unbound`                                                             | 20, bound to no variable                                                                                                                                                 | width on /MonthHeader/Icon/ArrowRight is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind):** As the previous month's arrow. Ask SOLAR to bind Date Picker Open /MonthHeader/Icon/ArrowRight width to icon.md, which has the same value.                                                                                                                                                                                                                                                                                                                                           |
| `component.date picker open.root.width#unbound`                                                                                  | 588, bound to no variable                                                                                                                                                | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | SOLAR has no control width token; ask SOLAR for one, or confirm 588 is intended.                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `component.date picker open.root.width@inline=true, type=double`                                                                 | 1 variant at inline=true, type=double: 588 where HUG                                                                                                                     | width on / should follow no axis only, but at inline=true, type=double it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (accept):** The two months' width, 588, is what the calendar hugs: two grids of seven 36px days and their gaps, and its padding and gap. It hugs, as the single calendar does. Ask SOLAR whether Date Picker Open / width is meant to change at inline=true, type=double.                                                                                                                                                                                                                      |
| `component.date picker open.sparse@type=double`                                                                                  | no variant with type=double and inline=false                                                                                                                             | Date Picker Open has no variant that holds inline at the default for type=double, so that combination is read from inline=true, type=double instead.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma draws no floating double calendar: it is the inline one's two months on the floating calendar's surface, as each axis draws them. Ask SOLAR whether Date Picker Open should have the variant with type=double at the default inline.                                                                                                                                                                                                                                         |
| `component.date picker open.weekdayRow.paddingBottom#unbound`                                                                    | 0, bound to no variable                                                                                                                                                  | paddingBottom on /WeekdayRow is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Date Picker Open /WeekdayRow paddingBottom to inset.none, which has the same value.                                  |
| `component.date picker open.weekdayRow.paddingLeft#unbound`                                                                      | 0, bound to no variable                                                                                                                                                  | paddingLeft on /WeekdayRow is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Date Picker Open /WeekdayRow paddingLeft to inset.none, which has the same value.                                    |
| `component.date picker open.weekdayRow.paddingRight#unbound`                                                                     | 0, bound to no variable                                                                                                                                                  | paddingRight on /WeekdayRow is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Date Picker Open /WeekdayRow paddingRight to inset.none, which has the same value.                                   |
| `component.date picker open.weekdayRow.paddingTop#unbound`                                                                       | 0, bound to no variable                                                                                                                                                  | paddingTop on /WeekdayRow is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Date Picker Open /WeekdayRow paddingTop to inset.none, which has the same value.                                     |
| `component.datepicker.field.height#unbound`                                                                                      | 40, 32, bound to no variable                                                                                                                                             | height on /Field is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind):** As Button's height: size.control, one step per size, which SOLAR added on 2026-09-25; Figma leaves the height unbound, which the design review asks SOLAR to bind. SOLAR has no control height token; ask SOLAR for one, or confirm 40, 32 is intended.                                                                                                                                                                                                                              |
| `component.datepicker.iconCalendar.width#unbound`                                                                                | 16, 12, bound to no variable                                                                                                                                             | width on /Field/Icon/Calendar is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (bind):** Same values, governed tokens, a step of the icon ladder per size; the icon's height is already bound to them. Ask SOLAR to bind DatePicker /Field/Icon/Calendar width to icon.sm or icon.xs, which has the same value.                                                                                                                                                                                                                                                               |
| `component.datepicker.label.paddingBottom#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind DatePicker /Label paddingBottom to inset.none, which has the same value.                                             |
| `component.datepicker.label.paddingLeft#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind DatePicker /Label paddingLeft to inset.none, which has the same value.                                               |
| `component.datepicker.label.paddingRight#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingRight on /Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind DatePicker /Label paddingRight to inset.none, which has the same value.                                              |
| `component.datepicker.label.paddingTop#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingTop on /Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind DatePicker /Label paddingTop to inset.none, which has the same value.                                                |
| `component.datepicker.root.paddingBottom#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind DatePicker / paddingBottom to inset.none, which has the same value.                                                  |
| `component.datepicker.root.paddingLeft#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind DatePicker / paddingLeft to inset.none, which has the same value.                                                    |
| `component.datepicker.root.paddingRight#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind DatePicker / paddingRight to inset.none, which has the same value.                                                   |
| `component.datepicker.root.paddingTop#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind DatePicker / paddingTop to inset.none, which has the same value.                                                     |
| `component.device card.button.height#unbound`                                                                                    | 32, bound to no variable                                                                                                                                                 | height on /Button is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (set):** The action is the caller's Button ("Try again"), sm as Figma draws it, whose height is its own: the card places it and never sizes it. Ask SOLAR to bind Device Card /Button height to icon.2xl, which has the same value.                                                                                                                                                                                                                                                            |
| `component.device card.content.paddingBottom#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Device Card /Content paddingBottom to inset.none, which has the same value.                                          |
| `component.device card.content.paddingLeft#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Device Card /Content paddingLeft to inset.none, which has the same value.                                            |
| `component.device card.content.paddingRight#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingRight on /Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Device Card /Content paddingRight to inset.none, which has the same value.                                           |
| `component.device card.content.paddingTop#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingTop on /Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Device Card /Content paddingTop to inset.none, which has the same value.                                             |
| `component.device card.contentSkeleton.height#unbound`                                                                           | 10, bound to no variable                                                                                                                                                 | height on /Content/Skeleton is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (allowLiteral):** As its width, carried as drawn. SOLAR has no control height token; ask SOLAR for one, or confirm 10 is intended.                                                                                                                                                                                                                                                                                                                                                             |
| `component.device card.contentSkeleton.width#unbound`                                                                            | 334, bound to no variable                                                                                                                                                | width on /Content/Skeleton is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (allowLiteral):** The loading placeholder is Figma's drawing of a line to come (334 × 10), no measure of anything: carried as drawn. SOLAR has no skeleton size tokens. SOLAR has no control width token; ask SOLAR for one, or confirm 334 is intended.                                                                                                                                                                                                                                       |
| `component.device card.headlineContent.paddingBottom#unbound`                                                                    | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Headline/Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Device Card /Headline/Content paddingBottom to inset.none, which has the same value.                                 |
| `component.device card.headlineContent.paddingLeft#unbound`                                                                      | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Headline/Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Device Card /Headline/Content paddingLeft to inset.none, which has the same value.                                   |
| `component.device card.headlineContent.paddingRight#unbound`                                                                     | 0, bound to no variable                                                                                                                                                  | paddingRight on /Headline/Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Device Card /Headline/Content paddingRight to inset.none, which has the same value.                                  |
| `component.device card.headlineContent.paddingTop#unbound`                                                                       | 0, bound to no variable                                                                                                                                                  | paddingTop on /Headline/Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Device Card /Headline/Content paddingTop to inset.none, which has the same value.                                    |
| `component.device card.headlineContentSkeleton.height#unbound`                                                                   | 10, bound to no variable                                                                                                                                                 | height on /Headline/Content/Skeleton is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (allowLiteral):** As the single card's placeholder, carried as drawn. SOLAR has no control height token; ask SOLAR for one, or confirm 10 is intended.                                                                                                                                                                                                                                                                                                                                         |
| `component.device card.headlineContentSkeleton.width#unbound`                                                                    | 334, bound to no variable                                                                                                                                                | width on /Headline/Content/Skeleton is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (allowLiteral):** As the single card's placeholder, carried as drawn. SOLAR has no control width token; ask SOLAR for one, or confirm 334 is intended.                                                                                                                                                                                                                                                                                                                                         |
| `component.device card.headlineIcon.gap#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | gap on /Headline/Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Device Card /Headline/Icon gap to inset.none or stack.none, which has the same value.                  |
| `component.device card.headlineIcon.height#unbound`                                                                              | 40, bound to no variable                                                                                                                                                 | height on /Headline/Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (allowLiteral):** As its width, a square. SOLAR has no control height token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                                                                                                                                                     |
| `component.device card.headlineIcon.paddingBottom#unbound`                                                                       | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Headline/Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Device Card /Headline/Icon paddingBottom to inset.none, which has the same value.                                    |
| `component.device card.headlineIcon.paddingLeft#unbound`                                                                         | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Headline/Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Device Card /Headline/Icon paddingLeft to inset.none, which has the same value.                                      |
| `component.device card.headlineIcon.paddingRight#unbound`                                                                        | 0, bound to no variable                                                                                                                                                  | paddingRight on /Headline/Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Device Card /Headline/Icon paddingRight to inset.none, which has the same value.                                     |
| `component.device card.headlineIcon.paddingTop#unbound`                                                                          | 0, bound to no variable                                                                                                                                                  | paddingTop on /Headline/Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Device Card /Headline/Icon paddingTop to inset.none, which has the same value.                                       |
| `component.device card.headlineIcon.width#unbound`                                                                               | 40, bound to no variable                                                                                                                                                 | width on /Headline/Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (allowLiteral):** As the single card's tile, a batch's 40px square, carried as Figma's number. SOLAR has no control width token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                                                                                                 |
| `component.device card.headlineIconIconDevice.height#unbound`                                                                    | 20, bound to no variable                                                                                                                                                 | height on /Headline/Icon/Icon/Device is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind):** As the single card's device icon. Ask SOLAR to bind Device Card /Headline/Icon/Icon/Device height to icon.md, which has the same value.                                                                                                                                                                                                                                                                                                                                              |
| `component.device card.headlineIconIconDevice.width#unbound`                                                                     | 20, bound to no variable                                                                                                                                                 | width on /Headline/Icon/Icon/Device is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (bind):** As the single card's device icon. Ask SOLAR to bind Device Card /Headline/Icon/Icon/Device width to icon.md, which has the same value.                                                                                                                                                                                                                                                                                                                                               |
| `component.device card.headlineIconIconProgress.height#unbound`                                                                  | 20, bound to no variable                                                                                                                                                 | height on /Headline/Icon/Icon/Progress is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind):** As the single card's device icon. Ask SOLAR to bind Device Card /Headline/Icon/Icon/Progress height to icon.md, which has the same value.                                                                                                                                                                                                                                                                                                                                            |
| `component.device card.headlineIconIconProgress.width#unbound`                                                                   | 20, bound to no variable                                                                                                                                                 | width on /Headline/Icon/Icon/Progress is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind):** As the single card's device icon. Ask SOLAR to bind Device Card /Headline/Icon/Icon/Progress width to icon.md, which has the same value.                                                                                                                                                                                                                                                                                                                                             |
| `component.device card.headlineTag.height#unbound`                                                                               | 24, bound to no variable                                                                                                                                                 | height on /Headline/Tag is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (set):** As the single card's Tag, its own height. Ask SOLAR to bind Device Card /Headline/Tag height to icon.lg, which has the same value.                                                                                                                                                                                                                                                                                                                                                    |
| `component.device card.icon.gap#unbound`                                                                                         | 0, bound to no variable                                                                                                                                                  | gap on /Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Device Card /Icon gap to inset.none or stack.none, which has the same value.                           |
| `component.device card.icon.height#unbound`                                                                                      | 52, bound to no variable                                                                                                                                                 | height on /Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (allowLiteral):** As the tile's width, a square. SOLAR has no control height token; ask SOLAR for one, or confirm 52 is intended.                                                                                                                                                                                                                                                                                                                                                              |
| `component.device card.icon.paddingBottom#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Device Card /Icon paddingBottom to inset.none, which has the same value.                                             |
| `component.device card.icon.paddingLeft#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Device Card /Icon paddingLeft to inset.none, which has the same value.                                               |
| `component.device card.icon.paddingRight#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingRight on /Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Device Card /Icon paddingRight to inset.none, which has the same value.                                              |
| `component.device card.icon.paddingTop#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingTop on /Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Device Card /Icon paddingTop to inset.none, which has the same value.                                                |
| `component.device card.icon.width#unbound`                                                                                       | 52, bound to no variable                                                                                                                                                 | width on /Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (allowLiteral):** The device's tile is a 52px square, bound to nothing: SOLAR has no size token for it (as for Insight Card's); carried as Figma's number, and raised as a governance gap. SOLAR has no control width token; ask SOLAR for one, or confirm 52 is intended.                                                                                                                                                                                                                     |
| `component.device card.iconIconDevice.height#unbound`                                                                            | 20, bound to no variable                                                                                                                                                 | height on /Icon/Icon/Device is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind):** Same value, governed token. Ask SOLAR to bind Device Card /Icon/Icon/Device height to icon.md, which has the same value.                                                                                                                                                                                                                                                                                                                                                             |
| `component.device card.iconIconDevice.width#unbound`                                                                             | 20, bound to no variable                                                                                                                                                 | width on /Icon/Icon/Device is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (bind):** Same value, governed token. Ask SOLAR to bind Device Card /Icon/Icon/Device width to icon.md, which has the same value.                                                                                                                                                                                                                                                                                                                                                              |
| `component.device card.iconIconProgress.height#unbound`                                                                          | 20, bound to no variable                                                                                                                                                 | height on /Icon/Icon/Progress is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (bind):** As the device icon's. Ask SOLAR to bind Device Card /Icon/Icon/Progress height to icon.md, which has the same value.                                                                                                                                                                                                                                                                                                                                                                 |
| `component.device card.iconIconProgress.width#unbound`                                                                           | 20, bound to no variable                                                                                                                                                 | width on /Icon/Icon/Progress is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (bind):** As the device icon's. Ask SOLAR to bind Device Card /Icon/Icon/Progress width to icon.md, which has the same value.                                                                                                                                                                                                                                                                                                                                                                  |
| `component.device card.root.paddingBottom#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Device Card / paddingBottom to inset.none, which has the same value.                                                 |
| `component.device card.root.paddingLeft#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Device Card / paddingLeft to inset.none, which has the same value.                                                   |
| `component.device card.root.paddingRight#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Device Card / paddingRight to inset.none, which has the same value.                                                  |
| `component.device card.root.paddingTop#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Device Card / paddingTop to inset.none, which has the same value.                                                    |
| `component.device card.root.width#unbound`                                                                                       | 560, bound to no variable                                                                                                                                                | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** A card spans the list it is in: Figma draws it 560 wide, a sample. The caller sizes it. SOLAR has no control width token; ask SOLAR for one, or confirm 560 is intended.                                                                                                                                                                                                                                                                                                              |
| `component.device card.row.paddingBottom#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Headline/Content/Row is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Device Card /Headline/Content/Row paddingBottom to inset.none, which has the same value.                             |
| `component.device card.row.paddingLeft#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Headline/Content/Row is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Device Card /Headline/Content/Row paddingLeft to inset.none, which has the same value.                               |
| `component.device card.row.paddingRight#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingRight on /Headline/Content/Row is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Device Card /Headline/Content/Row paddingRight to inset.none, which has the same value.                              |
| `component.device card.row.paddingTop#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingTop on /Headline/Content/Row is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Device Card /Headline/Content/Row paddingTop to inset.none, which has the same value.                                |
| `component.device card.serialLocation.gap#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | gap on /Serial & Location is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Device Card /Serial & Location gap to inset.none or stack.none, which has the same value.              |
| `component.device card.tag.height#unbound`                                                                                       | 24, bound to no variable                                                                                                                                                 | height on /Tag is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (set):** The health is a SOLAR Tag, whose 24px height is its own: the card places it, and never sizes it. Ask SOLAR to bind Device Card /Tag height to icon.lg, which has the same value.                                                                                                                                                                                                                                                                                                      |
| `component.dialog.close.height#unbound`                                                                                          | 40, 36, bound to no variable                                                                                                                                             | height on /Header/Text/Icon Button is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (set):** The close button is a SOLAR Icon Button (md, round, tertiary), whose 40px box is its own: the dialog places it, and never sizes it. SOLAR has no control height token; ask SOLAR for one, or confirm 40, 36 is intended.                                                                                                                                                                                                                                                              |
| `component.dialog.close.height@type=wizard`                                                                                      | 1 variant at type=wizard: 36 where 40                                                                                                                                    | height on /Header/Text/Icon Button should follow no axis only, but at type=wizard it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** The wizard's close button is resized to 36 where the default's is the Icon Button's own 40: a slip, the same button. The code draws the Icon Button's own; the design review lists it. Ask SOLAR whether Dialog /Header/Text/Icon Button height is meant to change at type=wizard.                                                                                                                                                                                                 |
| `component.dialog.close.width#unbound`                                                                                           | 40, 36, bound to no variable                                                                                                                                             | width on /Header/Text/Icon Button is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (set):** The close button is a SOLAR Icon Button (md, round, tertiary), whose 40px box is its own: the dialog places it, and never sizes it. SOLAR has no control width token; ask SOLAR for one, or confirm 40, 36 is intended.                                                                                                                                                                                                                                                               |
| `component.dialog.close.width@type=wizard`                                                                                       | 1 variant at type=wizard: 36 where 40                                                                                                                                    | width on /Header/Text/Icon Button should follow no axis only, but at type=wizard it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** The wizard's close button is resized to 36 where the default's is the Icon Button's own 40: a slip, the same button. The code draws the Icon Button's own; the design review lists it. Ask SOLAR whether Dialog /Header/Text/Icon Button width is meant to change at type=wizard.                                                                                                                                                                                                  |
| `component.dialog.content.height@type=image`                                                                                     | 1 variant at type=image: HUG where FILL                                                                                                                                  | height on /content should follow no axis only, but at type=image it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** A dialog is as tall as what it holds: Figma fixes the default and the wizard at 464, room for their sample content, and hugs the image dialog's. Ask SOLAR whether Dialog /content height is meant to change at type=image.                                                                                                                                                                                                                                                        |
| `component.dialog.icon.width#unbound`                                                                                            | 12, bound to no variable                                                                                                                                                 | width on /Header/Text/Icon/Icon/Empty is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind):** Same value, governed token; the icon's height is already bound to it. Ask SOLAR to bind Dialog /Header/Text/Icon/Icon/Empty width to icon.xs, which has the same value.                                                                                                                                                                                                                                                                                                              |
| `component.dialog.imageClose.height#unbound`                                                                                     | 40, bound to no variable                                                                                                                                                 | height on /image/Icon Button is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (set):** The close button is a SOLAR Icon Button (md, round, tertiary), whose 40px box is its own: the dialog places it, and never sizes it. SOLAR has no control height token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                                                  |
| `component.dialog.imageClose.width#unbound`                                                                                      | 40, bound to no variable                                                                                                                                                 | width on /image/Icon Button is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (set):** The close button is a SOLAR Icon Button (md, round, tertiary), whose 40px box is its own: the dialog places it, and never sizes it. SOLAR has no control width token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                                                   |
| `component.dialog.leading.height#unbound`                                                                                        | 36, bound to no variable                                                                                                                                                 | height on /Header/Text/Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (allowLiteral):** The title's icon sits in a 36px box, bound to nothing: SOLAR has no size for it. SOLAR has no control height token; ask SOLAR for one, or confirm 36 is intended.                                                                                                                                                                                                                                                                                                            |
| `component.dialog.leading.paddingBottom#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Header/Text/Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Dialog /Header/Text/Icon paddingBottom to inset.none, which has the same value.                                      |
| `component.dialog.leading.paddingLeft#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Header/Text/Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Dialog /Header/Text/Icon paddingLeft to inset.none, which has the same value.                                        |
| `component.dialog.leading.paddingRight#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingRight on /Header/Text/Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Dialog /Header/Text/Icon paddingRight to inset.none, which has the same value.                                       |
| `component.dialog.leading.paddingTop#unbound`                                                                                    | 0, bound to no variable                                                                                                                                                  | paddingTop on /Header/Text/Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Dialog /Header/Text/Icon paddingTop to inset.none, which has the same value.                                         |
| `component.dialog.leading.width#unbound`                                                                                         | 36, bound to no variable                                                                                                                                                 | width on /Header/Text/Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (allowLiteral):** The title's icon sits in a 36px box, bound to nothing: SOLAR has no size for it. SOLAR has no control width token; ask SOLAR for one, or confirm 36 is intended.                                                                                                                                                                                                                                                                                                             |
| `component.dialog.modalImage.height#unbound`                                                                                     | 279, bound to no variable                                                                                                                                                | height on /image is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (allowLiteral):** The image dialog's picture is 279 tall, bound to nothing: SOLAR has no media size. SOLAR has no control height token; ask SOLAR for one, or confirm 279 is intended.                                                                                                                                                                                                                                                                                                         |
| `component.dialog.modalImage.paddingBottom#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingBottom on /image is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Dialog /image paddingBottom to inset.none, which has the same value.                                                 |
| `component.dialog.modalImage.paddingLeft#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingLeft on /image is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Dialog /image paddingLeft to inset.none, which has the same value.                                                   |
| `component.dialog.modalImage.paddingRight#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingRight on /image is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Dialog /image paddingRight to inset.none, which has the same value.                                                  |
| `component.dialog.modalImage.paddingTop#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingTop on /image is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Dialog /image paddingTop to inset.none, which has the same value.                                                    |
| `component.dialog.root.gap#unbound`                                                                                              | 0, bound to no variable                                                                                                                                                  | gap on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Dialog / gap to inset.none or stack.none, which has the same value.                                    |
| `component.dialog.root.height#unbound`                                                                                           | 464, bound to no variable                                                                                                                                                | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (set):** A dialog is as tall as what it holds: Figma fixes the default and the wizard at 464, room for their sample content, and hugs the image dialog's. SOLAR has no control height token; ask SOLAR for one, or confirm 464 is intended.                                                                                                                                                                                                                                                    |
| `component.dialog.root.height@type=image`                                                                                        | 1 variant at type=image: HUG where 464                                                                                                                                   | height on / should follow no axis only, but at type=image it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** A dialog is as tall as what it holds: Figma fixes the default and the wizard at 464, room for their sample content, and hugs the image dialog's. Ask SOLAR whether Dialog / height is meant to change at type=image.                                                                                                                                                                                                                                                               |
| `component.dialog.root.paddingBottom#unbound`                                                                                    | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Dialog / paddingBottom to inset.none, which has the same value.                                                      |
| `component.dialog.root.paddingLeft#unbound`                                                                                      | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Dialog / paddingLeft to inset.none, which has the same value.                                                        |
| `component.dialog.root.paddingRight#unbound`                                                                                     | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Dialog / paddingRight to inset.none, which has the same value.                                                       |
| `component.dialog.root.paddingTop#unbound`                                                                                       | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Dialog / paddingTop to inset.none, which has the same value.                                                         |
| `component.dialog.root.width#unbound`                                                                                            | 480, bound to no variable                                                                                                                                                | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (allowLiteral):** A dialog is 480 wide, bound to nothing: SOLAR has no dialog width, one of the sizes with no variable. The page may be narrower, and the dialog then fits it. SOLAR has no control width token; ask SOLAR for one, or confirm 480 is intended.                                                                                                                                                                                                                                |
| `component.dialog.text.paddingBottom#unbound`                                                                                    | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Header/Text is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Dialog /Header/Text paddingBottom to inset.none, which has the same value.                                           |
| `component.dialog.text.paddingLeft#unbound`                                                                                      | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Header/Text is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Dialog /Header/Text paddingLeft to inset.none, which has the same value.                                             |
| `component.dialog.text.paddingRight#unbound`                                                                                     | 0, bound to no variable                                                                                                                                                  | paddingRight on /Header/Text is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Dialog /Header/Text paddingRight to inset.none, which has the same value.                                            |
| `component.dialog.text.paddingTop#unbound`                                                                                       | 0, bound to no variable                                                                                                                                                  | paddingTop on /Header/Text is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Dialog /Header/Text paddingTop to inset.none, which has the same value.                                              |
| `component.divider.root.gap#unbound`                                                                                             | 0, bound to no variable                                                                                                                                                  | gap on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Divider / gap to inset.none or stack.none, which has the same value.                                   |
| `component.divider.root.height#unbound`                                                                                          | 1, 20, 32, bound to no variable                                                                                                                                          | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (allowLiteral):** A labelled divider is 20 tall, its label's row, and bound to nothing: SOLAR has no size for it. Raised as a governance gap. SOLAR has no control height token; ask SOLAR for one, or confirm 1, 20, 32 is intended.                                                                                                                                                                                                                                                          |
| `component.divider.root.paddingBottom#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Divider / paddingBottom to inset.none, which has the same value.                                                     |
| `component.divider.root.paddingLeft#unbound`                                                                                     | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Divider / paddingLeft to inset.none, which has the same value.                                                       |
| `component.divider.root.paddingRight#unbound`                                                                                    | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Divider / paddingRight to inset.none, which has the same value.                                                      |
| `component.divider.root.paddingTop#unbound`                                                                                      | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Divider / paddingTop to inset.none, which has the same value.                                                        |
| `component.divider.root.width#unbound`                                                                                           | 320, 1, bound to no variable                                                                                                                                             | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** A divider spans its container: Figma draws the full one 320 wide, a sample, and the inset one filling already. SOLAR has no control width token; ask SOLAR for one, or confirm 320, 1 is intended.                                                                                                                                                                                                                                                                                    |
| `component.divider.rule.height#unbound`                                                                                          | 1, bound to no variable                                                                                                                                                  | height on /Rule is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind):** The horizontal rule's thickness, as the vertical one's. SOLAR has no control height token; ask SOLAR for one, or confirm 1 is intended.                                                                                                                                                                                                                                                                                                                                              |
| `component.divider.rule.width#unbound`                                                                                           | 1, bound to no variable                                                                                                                                                  | width on /Rule is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (bind):** The vertical rule's thickness, 1px: a hairline, border.default's width, the same value. SOLAR has no control width token; ask SOLAR for one, or confirm 1 is intended.                                                                                                                                                                                                                                                                                                               |
| `component.divider.rule2.height#unbound`                                                                                         | 1, bound to no variable                                                                                                                                                  | height on /Rule#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind):** The second rule of a labelled divider, as the first. SOLAR has no control height token; ask SOLAR for one, or confirm 1 is intended.                                                                                                                                                                                                                                                                                                                                                 |
| `component.donut chart.centerLabel.paddingBottom#unbound`                                                                        | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Center Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (accept):** Figma's ring is sample data: its segments' angles, at one size. The library draws the caller's shares; the theme takes the design, none of which is left open. Ask SOLAR to bind Donut Chart /Center Label paddingBottom to inset.none, which has the same value.                                                                                                                                                                                                                  |
| `component.donut chart.centerLabel.paddingLeft#unbound`                                                                          | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Center Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (accept):** Figma's ring is sample data: its segments' angles, at one size. The library draws the caller's shares; the theme takes the design, none of which is left open. Ask SOLAR to bind Donut Chart /Center Label paddingLeft to inset.none, which has the same value.                                                                                                                                                                                                                    |
| `component.donut chart.centerLabel.paddingRight#unbound`                                                                         | 0, bound to no variable                                                                                                                                                  | paddingRight on /Center Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (accept):** Figma's ring is sample data: its segments' angles, at one size. The library draws the caller's shares; the theme takes the design, none of which is left open. Ask SOLAR to bind Donut Chart /Center Label paddingRight to inset.none, which has the same value.                                                                                                                                                                                                                   |
| `component.donut chart.centerLabel.paddingTop#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingTop on /Center Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's ring is sample data: its segments' angles, at one size. The library draws the caller's shares; the theme takes the design, none of which is left open. Ask SOLAR to bind Donut Chart /Center Label paddingTop to inset.none, which has the same value.                                                                                                                                                                                                                     |
| `component.donut chart.segment.height#unbound`                                                                                   | 200, bound to no variable                                                                                                                                                | height on /Segment is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's ring is sample data: its segments' angles, at one size. The library draws the caller's shares; the theme takes the design, none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 200 is intended.                                                                                                                                                                                                                                   |
| `component.donut chart.segment.width#unbound`                                                                                    | 200, bound to no variable                                                                                                                                                | width on /Segment is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Figma's ring is sample data: its segments' angles, at one size. The library draws the caller's shares; the theme takes the design, none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 200 is intended.                                                                                                                                                                                                                                    |
| `component.donut chart.segment2.height#unbound`                                                                                  | 200, bound to no variable                                                                                                                                                | height on /Segment#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's ring is sample data: its segments' angles, at one size. The library draws the caller's shares; the theme takes the design, none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 200 is intended.                                                                                                                                                                                                                                   |
| `component.donut chart.segment2.width#unbound`                                                                                   | 200, bound to no variable                                                                                                                                                | width on /Segment#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's ring is sample data: its segments' angles, at one size. The library draws the caller's shares; the theme takes the design, none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 200 is intended.                                                                                                                                                                                                                                    |
| `component.donut chart.segment3.height#unbound`                                                                                  | 200, bound to no variable                                                                                                                                                | height on /Segment#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's ring is sample data: its segments' angles, at one size. The library draws the caller's shares; the theme takes the design, none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 200 is intended.                                                                                                                                                                                                                                   |
| `component.donut chart.segment3.width#unbound`                                                                                   | 200, bound to no variable                                                                                                                                                | width on /Segment#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's ring is sample data: its segments' angles, at one size. The library draws the caller's shares; the theme takes the design, none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 200 is intended.                                                                                                                                                                                                                                    |
| `component.donut chart.segment4.height#unbound`                                                                                  | 200, bound to no variable                                                                                                                                                | height on /Segment#4 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's ring is sample data: its segments' angles, at one size. The library draws the caller's shares; the theme takes the design, none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 200 is intended.                                                                                                                                                                                                                                   |
| `component.donut chart.segment4.width#unbound`                                                                                   | 200, bound to no variable                                                                                                                                                | width on /Segment#4 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's ring is sample data: its segments' angles, at one size. The library draws the caller's shares; the theme takes the design, none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 200 is intended.                                                                                                                                                                                                                                    |
| `component.donut chart.segment5.height#unbound`                                                                                  | 200, bound to no variable                                                                                                                                                | height on /Segment#5 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's ring is sample data: its segments' angles, at one size. The library draws the caller's shares; the theme takes the design, none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 200 is intended.                                                                                                                                                                                                                                   |
| `component.donut chart.segment5.width#unbound`                                                                                   | 200, bound to no variable                                                                                                                                                | width on /Segment#5 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's ring is sample data: its segments' angles, at one size. The library draws the caller's shares; the theme takes the design, none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 200 is intended.                                                                                                                                                                                                                                    |
| `component.donut chart.segment6.height#unbound`                                                                                  | 200, bound to no variable                                                                                                                                                | height on /Segment#6 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's ring is sample data: its segments' angles, at one size. The library draws the caller's shares; the theme takes the design, none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 200 is intended.                                                                                                                                                                                                                                   |
| `component.donut chart.segment6.width#unbound`                                                                                   | 200, bound to no variable                                                                                                                                                | width on /Segment#6 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's ring is sample data: its segments' angles, at one size. The library draws the caller's shares; the theme takes the design, none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 200 is intended.                                                                                                                                                                                                                                    |
| `component.draghandle.col1.paddingBottom#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Col1 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind DragHandle /Col1 paddingBottom to inset.none, which has the same value.                                              |
| `component.draghandle.col1.paddingLeft#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Col1 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind DragHandle /Col1 paddingLeft to inset.none, which has the same value.                                                |
| `component.draghandle.col1.paddingRight#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingRight on /Col1 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind DragHandle /Col1 paddingRight to inset.none, which has the same value.                                               |
| `component.draghandle.col1.paddingTop#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingTop on /Col1 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind DragHandle /Col1 paddingTop to inset.none, which has the same value.                                                 |
| `component.draghandle.col1Dot.height#unbound`                                                                                    | 3, 4, bound to no variable                                                                                                                                               | height on /Col1/Dot is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (allowLiteral):** A dot is 3px at sm and 4px at md, and SOLAR has no size that small: the icon ladder starts at 12. Carried as Figma's numbers, and raised as a governance gap. SOLAR has no control height token; ask SOLAR for one, or confirm 3, 4 is intended.                                                                                                                                                                                                                             |
| `component.draghandle.col1Dot.width#unbound`                                                                                     | 3, 4, bound to no variable                                                                                                                                               | width on /Col1/Dot is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (allowLiteral):** A dot is 3px at sm and 4px at md, and SOLAR has no size that small: the icon ladder starts at 12. Carried as Figma's numbers, and raised as a governance gap. SOLAR has no control width token; ask SOLAR for one, or confirm 3, 4 is intended.                                                                                                                                                                                                                              |
| `component.draghandle.col1Dot2.height#unbound`                                                                                   | 3, 4, bound to no variable                                                                                                                                               | height on /Col1/Dot#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (allowLiteral):** A dot is 3px at sm and 4px at md, and SOLAR has no size that small: the icon ladder starts at 12. Carried as Figma's numbers, and raised as a governance gap. SOLAR has no control height token; ask SOLAR for one, or confirm 3, 4 is intended.                                                                                                                                                                                                                             |
| `component.draghandle.col1Dot2.width#unbound`                                                                                    | 3, 4, bound to no variable                                                                                                                                               | width on /Col1/Dot#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (allowLiteral):** A dot is 3px at sm and 4px at md, and SOLAR has no size that small: the icon ladder starts at 12. Carried as Figma's numbers, and raised as a governance gap. SOLAR has no control width token; ask SOLAR for one, or confirm 3, 4 is intended.                                                                                                                                                                                                                              |
| `component.draghandle.col1Dot3.height#unbound`                                                                                   | 3, 4, bound to no variable                                                                                                                                               | height on /Col1/Dot#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (allowLiteral):** A dot is 3px at sm and 4px at md, and SOLAR has no size that small: the icon ladder starts at 12. Carried as Figma's numbers, and raised as a governance gap. SOLAR has no control height token; ask SOLAR for one, or confirm 3, 4 is intended.                                                                                                                                                                                                                             |
| `component.draghandle.col1Dot3.width#unbound`                                                                                    | 3, 4, bound to no variable                                                                                                                                               | width on /Col1/Dot#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (allowLiteral):** A dot is 3px at sm and 4px at md, and SOLAR has no size that small: the icon ladder starts at 12. Carried as Figma's numbers, and raised as a governance gap. SOLAR has no control width token; ask SOLAR for one, or confirm 3, 4 is intended.                                                                                                                                                                                                                              |
| `component.draghandle.col2.paddingBottom#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Col2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind DragHandle /Col2 paddingBottom to inset.none, which has the same value.                                              |
| `component.draghandle.col2.paddingLeft#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Col2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind DragHandle /Col2 paddingLeft to inset.none, which has the same value.                                                |
| `component.draghandle.col2.paddingRight#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingRight on /Col2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind DragHandle /Col2 paddingRight to inset.none, which has the same value.                                               |
| `component.draghandle.col2.paddingTop#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingTop on /Col2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind DragHandle /Col2 paddingTop to inset.none, which has the same value.                                                 |
| `component.draghandle.col2Dot.height#unbound`                                                                                    | 3, 4, bound to no variable                                                                                                                                               | height on /Col2/Dot is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (allowLiteral):** As the first dot's, 3px at sm and 4px at md. SOLAR has no control height token; ask SOLAR for one, or confirm 3, 4 is intended.                                                                                                                                                                                                                                                                                                                                              |
| `component.draghandle.col2Dot.width#unbound`                                                                                     | 3, 4, bound to no variable                                                                                                                                               | width on /Col2/Dot is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (allowLiteral):** As the first dot's, 3px at sm and 4px at md. SOLAR has no control width token; ask SOLAR for one, or confirm 3, 4 is intended.                                                                                                                                                                                                                                                                                                                                               |
| `component.draghandle.col2Dot2.height#unbound`                                                                                   | 3, 4, bound to no variable                                                                                                                                               | height on /Col2/Dot#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (allowLiteral):** As the first dot's, 3px at sm and 4px at md. SOLAR has no control height token; ask SOLAR for one, or confirm 3, 4 is intended.                                                                                                                                                                                                                                                                                                                                              |
| `component.draghandle.col2Dot2.width#unbound`                                                                                    | 3, 4, bound to no variable                                                                                                                                               | width on /Col2/Dot#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (allowLiteral):** As the first dot's, 3px at sm and 4px at md. SOLAR has no control width token; ask SOLAR for one, or confirm 3, 4 is intended.                                                                                                                                                                                                                                                                                                                                               |
| `component.draghandle.col2Dot3.height#unbound`                                                                                   | 3, 4, bound to no variable                                                                                                                                               | height on /Col2/Dot#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (allowLiteral):** As the first dot's, 3px at sm and 4px at md. SOLAR has no control height token; ask SOLAR for one, or confirm 3, 4 is intended.                                                                                                                                                                                                                                                                                                                                              |
| `component.draghandle.col2Dot3.width#unbound`                                                                                    | 3, 4, bound to no variable                                                                                                                                               | width on /Col2/Dot#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (allowLiteral):** As the first dot's, 3px at sm and 4px at md. SOLAR has no control width token; ask SOLAR for one, or confirm 3, 4 is intended.                                                                                                                                                                                                                                                                                                                                               |
| `component.drawer.close.height#unbound`                                                                                          | 40, bound to no variable                                                                                                                                                 | height on /Header/Icon Button is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (set):** The close button is a SOLAR Icon Button (md, square, tertiary), whose 40px box is its own: the drawer places it, and never sizes it. SOLAR has no control height token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                                                 |
| `component.drawer.close.width#unbound`                                                                                           | 40, bound to no variable                                                                                                                                                 | width on /Header/Icon Button is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (set):** The close button is a SOLAR Icon Button (md, square, tertiary), whose 40px box is its own: the drawer places it, and never sizes it. SOLAR has no control width token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                                                  |
| `component.drawer.header.gap#unbound`                                                                                            | 0, bound to no variable                                                                                                                                                  | gap on /Header is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Drawer /Header gap to inset.none or stack.none, which has the same value.                              |
| `component.drawer.header.height#unbound`                                                                                         | 56, bound to no variable                                                                                                                                                 | height on /Header is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (set):** The header is as tall as its close button and padding (8 + 40 + 8): Figma fixes it at 56, the same. SOLAR has no control height token; ask SOLAR for one, or confirm 56 is intended.                                                                                                                                                                                                                                                                                                  |
| `component.drawer.root.gap#unbound`                                                                                              | 0, bound to no variable                                                                                                                                                  | gap on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Drawer / gap to inset.none or stack.none, which has the same value.                                    |
| `component.drawer.root.height#unbound`                                                                                           | 800, bound to no variable                                                                                                                                                | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (set):** A drawer runs the viewport's full height, from its edge: Figma draws it 800 tall, a sample of a screen. SOLAR has no control height token; ask SOLAR for one, or confirm 800 is intended.                                                                                                                                                                                                                                                                                             |
| `component.drawer.root.paddingBottom#unbound`                                                                                    | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Drawer / paddingBottom to inset.none, which has the same value.                                                      |
| `component.drawer.root.paddingLeft#unbound`                                                                                      | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Drawer / paddingLeft to inset.none, which has the same value.                                                        |
| `component.drawer.root.paddingRight#unbound`                                                                                     | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Drawer / paddingRight to inset.none, which has the same value.                                                       |
| `component.drawer.root.paddingTop#unbound`                                                                                       | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Drawer / paddingTop to inset.none, which has the same value.                                                         |
| `component.drawer.root.radiusBottomRight#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | radiusBottomRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind):** Same value, governed token: the drawer is square along the viewport's edge, rounded (radius.dialog) on the side it opens into. Ask SOLAR to bind Drawer / radiusBottomRight to radius.none, which has the same value.                                                                                                                                                                                                                                                                |
| `component.drawer.root.radiusTopRight#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | radiusTopRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (bind):** Same value, governed token: the drawer is square along the viewport's edge, rounded (radius.dialog) on the side it opens into. Ask SOLAR to bind Drawer / radiusTopRight to radius.none, which has the same value.                                                                                                                                                                                                                                                                   |
| `component.drawer.root.width#unbound`                                                                                            | 348, bound to no variable                                                                                                                                                | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (allowLiteral):** A drawer is 348 wide, bound to nothing: SOLAR has no panel width, one of the sizes with no variable. SOLAR has no control width token; ask SOLAR for one, or confirm 348 is intended.                                                                                                                                                                                                                                                                                        |
| `component.dropdown group label.root.gap#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | gap on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Dropdown Group Label / gap to inset.none or stack.none, which has the same value.                      |
| `component.dropdown group label.root.width#unbound`                                                                              | 240, 160, bound to no variable                                                                                                                                           | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** As md's, the sm heading's 160 a sample of its menu's width. SOLAR has no control width token; ask SOLAR for one, or confirm 240, 160 is intended.                                                                                                                                                                                                                                                                                                                                     |
| `component.dropdown item.checkbox.height#unbound`                                                                                | 16, bound to no variable                                                                                                                                                 | height on /Checkbox is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (set):** As its width, the Checkbox's own. Ask SOLAR to bind Dropdown Item /Checkbox height to icon.sm, which has the same value.                                                                                                                                                                                                                                                                                                                                                              |
| `component.dropdown item.checkbox.width#unbound`                                                                                 | 16, bound to no variable                                                                                                                                                 | width on /Checkbox is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (set):** The checkbox is a SOLAR Checkbox, whose 16px box is its own: the row places it, and never sizes it. Ask SOLAR to bind Dropdown Item /Checkbox width to icon.sm, which has the same value.                                                                                                                                                                                                                                                                                             |
| `component.dropdown item.content.paddingBottom#unbound`                                                                          | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Dropdown Item /Content paddingBottom to inset.none, which has the same value.                                        |
| `component.dropdown item.content.paddingLeft#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Dropdown Item /Content paddingLeft to inset.none, which has the same value.                                          |
| `component.dropdown item.content.paddingRight#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingRight on /Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Dropdown Item /Content paddingRight to inset.none, which has the same value.                                         |
| `component.dropdown item.content.paddingTop#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingTop on /Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Dropdown Item /Content paddingTop to inset.none, which has the same value.                                           |
| `component.dropdown item.icon.width#unbound`                                                                                     | 20, 16, bound to no variable                                                                                                                                             | width on /Icon/None is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (bind):** Same values, governed tokens, a step of the icon ladder per size; the icon's height is already bound to them. Ask SOLAR to bind Dropdown Item /Icon/None width to icon.md or icon.sm, which has the same value.                                                                                                                                                                                                                                                                      |
| `component.dropdown item.root.width#unbound`                                                                                     | 240, 160, bound to no variable                                                                                                                                           | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** As md's, the sm row's 160 a sample of its menu's width. SOLAR has no control width token; ask SOLAR for one, or confirm 240, 160 is intended.                                                                                                                                                                                                                                                                                                                                         |
| `component.dropdown menu.content.paddingBottom#unbound`                                                                          | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Dropdown Menu /Content paddingBottom to inset.none, which has the same value.                                        |
| `component.dropdown menu.content.paddingLeft#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Dropdown Menu /Content paddingLeft to inset.none, which has the same value.                                          |
| `component.dropdown menu.content.paddingRight#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingRight on /Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Dropdown Menu /Content paddingRight to inset.none, which has the same value.                                         |
| `component.dropdown menu.content.paddingTop#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingTop on /Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Dropdown Menu /Content paddingTop to inset.none, which has the same value.                                           |
| `component.dropdown menu.dropdownGroupLabel.width#unbound`                                                                       | 240, 160, bound to no variable                                                                                                                                           | width on /Content/Dropdown Group Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (set):** As md's, a sample. SOLAR has no control width token; ask SOLAR for one, or confirm 240, 160 is intended.                                                                                                                                                                                                                                                                                                                                                                              |
| `component.dropdown menu.dropdownItem.width#unbound`                                                                             | 240, 160, bound to no variable                                                                                                                                           | width on /Content/Dropdown Item is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (set):** As md's, a sample. SOLAR has no control width token; ask SOLAR for one, or confirm 240, 160 is intended.                                                                                                                                                                                                                                                                                                                                                                              |
| `component.dropdown menu.dropdownItem2.width#unbound`                                                                            | 240, 160, bound to no variable                                                                                                                                           | width on /Content/Dropdown Item#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (set):** As md's, a sample. SOLAR has no control width token; ask SOLAR for one, or confirm 240, 160 is intended.                                                                                                                                                                                                                                                                                                                                                                              |
| `component.dropdown menu.dropdownItem3.width#unbound`                                                                            | 240, 160, bound to no variable                                                                                                                                           | width on /Content/Dropdown Item#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (set):** As md's, a sample. SOLAR has no control width token; ask SOLAR for one, or confirm 240, 160 is intended.                                                                                                                                                                                                                                                                                                                                                                              |
| `component.dropdown menu.dropdownItem4.width#unbound`                                                                            | 240, 160, bound to no variable                                                                                                                                           | width on /Content/Dropdown Item#4 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (set):** As md's, a sample. SOLAR has no control width token; ask SOLAR for one, or confirm 240, 160 is intended.                                                                                                                                                                                                                                                                                                                                                                              |
| `component.dropdown menu.dropdownItem5.width#unbound`                                                                            | 240, 160, bound to no variable                                                                                                                                           | width on /Content/Dropdown Item#5 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (set):** As md's, a sample. SOLAR has no control width token; ask SOLAR for one, or confirm 240, 160 is intended.                                                                                                                                                                                                                                                                                                                                                                              |
| `component.dropdown menu.dropdownItem6.width#unbound`                                                                            | 240, 160, bound to no variable                                                                                                                                           | width on /Content/Dropdown Item#6 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (set):** As md's, a sample. SOLAR has no control width token; ask SOLAR for one, or confirm 240, 160 is intended.                                                                                                                                                                                                                                                                                                                                                                              |
| `component.dropdown menu.root.gap#unbound`                                                                                       | 0, bound to no variable                                                                                                                                                  | gap on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Dropdown Menu / gap to inset.none or stack.none, which has the same value.                             |
| `component.dropdown.field.height#unbound`                                                                                        | 40, 32, bound to no variable                                                                                                                                             | height on /Field is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind):** As Button's height: size.control, one step per size, which SOLAR added on 2026-09-25; Figma leaves the height unbound, which the design review asks SOLAR to bind. SOLAR has no control height token; ask SOLAR for one, or confirm 40, 32 is intended.                                                                                                                                                                                                                              |
| `component.dropdown.iconChevronDown.width#unbound`                                                                               | 16, 12, bound to no variable                                                                                                                                             | width on /Field/Icon/ChevronDown is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind):** As the icons', a step smaller for the chevron. Ask SOLAR to bind Dropdown /Field/Icon/ChevronDown width to icon.sm or icon.xs, which has the same value.                                                                                                                                                                                                                                                                                                                             |
| `component.dropdown.iconChevronUp.width#unbound`                                                                                 | 16, 12, bound to no variable                                                                                                                                             | width on /Field/Icon/ChevronUp is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (bind):** As the down chevron's. Ask SOLAR to bind Dropdown /Field/Icon/ChevronUp width to icon.sm or icon.xs, which has the same value.                                                                                                                                                                                                                                                                                                                                                       |
| `component.dropdown.label.paddingBottom#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Dropdown /Label paddingBottom to inset.none, which has the same value.                                               |
| `component.dropdown.label.paddingLeft#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Dropdown /Label paddingLeft to inset.none, which has the same value.                                                 |
| `component.dropdown.label.paddingRight#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingRight on /Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Dropdown /Label paddingRight to inset.none, which has the same value.                                                |
| `component.dropdown.label.paddingTop#unbound`                                                                                    | 0, bound to no variable                                                                                                                                                  | paddingTop on /Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Dropdown /Label paddingTop to inset.none, which has the same value.                                                  |
| `component.dropdown.leadingIcon.width#unbound`                                                                                   | 20, 16, bound to no variable                                                                                                                                             | width on /Field/Icon/None is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind):** Same values, governed tokens, a step of the icon ladder per size; the icon's height is already bound to them. Ask SOLAR to bind Dropdown /Field/Icon/None width to icon.md or icon.sm, which has the same value.                                                                                                                                                                                                                                                                     |
| `component.dropdown.root.paddingBottom#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Dropdown / paddingBottom to inset.none, which has the same value.                                                    |
| `component.dropdown.root.paddingLeft#unbound`                                                                                    | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Dropdown / paddingLeft to inset.none, which has the same value.                                                      |
| `component.dropdown.root.paddingRight#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Dropdown / paddingRight to inset.none, which has the same value.                                                     |
| `component.dropdown.root.paddingTop#unbound`                                                                                     | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Dropdown / paddingTop to inset.none, which has the same value.                                                       |
| `component.dropdown.root.width#unbound`                                                                                          | 240, 160, bound to no variable                                                                                                                                           | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** As md's, a sample. SOLAR has no control width token; ask SOLAR for one, or confirm 240, 160 is intended.                                                                                                                                                                                                                                                                                                                                                                              |
| `component.dropdown.trailingIcon.width#unbound`                                                                                  | 20, 16, bound to no variable                                                                                                                                             | width on /Field/Icon/None#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind):** As the leading icon's. Ask SOLAR to bind Dropdown /Field/Icon/None#2 width to icon.md or icon.sm, which has the same value.                                                                                                                                                                                                                                                                                                                                                          |
| `component.emptystate.action.height#unbound`                                                                                     | 32, bound to no variable                                                                                                                                                 | height on /Button is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (set):** The action is the caller's SOLAR Button, sm as Figma draws it, whose height is its own: the empty state places it and never sizes it. Ask SOLAR to bind EmptyState /Button height to icon.2xl, which has the same value.                                                                                                                                                                                                                                                              |
| `component.emptystate.icon.height#unbound`                                                                                       | 24, bound to no variable                                                                                                                                                 | height on /Icon/None is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind):** Same value, governed token; the icon's width is already bound to it. Ask SOLAR to bind EmptyState /Icon/None height to icon.lg, which has the same value.                                                                                                                                                                                                                                                                                                                            |
| `component.emptystate.textContent.paddingBottom#unbound`                                                                         | 0, bound to no variable                                                                                                                                                  | paddingBottom on /TextContent is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind EmptyState /TextContent paddingBottom to inset.none, which has the same value.                                       |
| `component.emptystate.textContent.paddingLeft#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingLeft on /TextContent is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind EmptyState /TextContent paddingLeft to inset.none, which has the same value.                                         |
| `component.emptystate.textContent.paddingRight#unbound`                                                                          | 0, bound to no variable                                                                                                                                                  | paddingRight on /TextContent is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind EmptyState /TextContent paddingRight to inset.none, which has the same value.                                        |
| `component.emptystate.textContent.paddingTop#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingTop on /TextContent is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind EmptyState /TextContent paddingTop to inset.none, which has the same value.                                          |
| `component.event chip.repeating.width#unbound`                                                                                   | 12, bound to no variable                                                                                                                                                 | width on /Content/Icon/Repeat is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (bind):** Same value, governed token; the icon's height is already bound to it. Ask SOLAR to bind Event Chip /Content/Icon/Repeat width to icon.xs, which has the same value.                                                                                                                                                                                                                                                                                                                  |
| `component.event chip.root.gap#unbound`                                                                                          | 0, bound to no variable                                                                                                                                                  | gap on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Event Chip / gap to inset.none or stack.none, which has the same value.                                |
| `component.event chip.root.height#unbound`                                                                                       | 22, bound to no variable                                                                                                                                                 | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (allowLiteral):** A chip is 22 tall, bound to nothing: SOLAR has no size for it, one of the sizes with no variable (a calendar's row). SOLAR has no control height token; ask SOLAR for one, or confirm 22 is intended.                                                                                                                                                                                                                                                                        |
| `component.event chip.root.paddingBottom#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Event Chip / paddingBottom to inset.none, which has the same value.                                                  |
| `component.event chip.root.paddingLeft#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Event Chip / paddingLeft to inset.none, which has the same value.                                                    |
| `component.event chip.root.paddingRight#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Event Chip / paddingRight to inset.none, which has the same value.                                                   |
| `component.event chip.root.paddingTop#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Event Chip / paddingTop to inset.none, which has the same value.                                                     |
| `component.event chip.root.width#unbound`                                                                                        | 160, bound to no variable                                                                                                                                                | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** A chip fills the width its cell, grid column or row gives it: Figma draws a 160 sample, and a Day Cell stretches its chips across. SOLAR has no control width token; ask SOLAR for one, or confirm 160 is intended.                                                                                                                                                                                                                                                                   |
| `component.event chip.stripe.height#unbound`                                                                                     | 22, bound to no variable                                                                                                                                                 | height on /Stripe is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (allowLiteral):** The 22 Figma keeps on the stripe where a tinted or solid chip hides it, never drawn (follows stripe.height); where it is drawn it fills the chip. SOLAR has no control height token; ask SOLAR for one, or confirm 22 is intended.                                                                                                                                                                                                                                           |
| `component.event chip.stripe.width#unbound`                                                                                      | 3, bound to no variable                                                                                                                                                  | width on /Stripe is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (allowLiteral):** The stripe is 3 wide, bound to nothing: no border width is 3 (border.strong is 2), and a stripe is a fill, not an edge. SOLAR has no control width token; ask SOLAR for one, or confirm 3 is intended.                                                                                                                                                                                                                                                                       |
| `component.event row.content.paddingBottom#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Event Row /Content paddingBottom to inset.none, which has the same value.                                            |
| `component.event row.content.paddingLeft#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Event Row /Content paddingLeft to inset.none, which has the same value.                                              |
| `component.event row.content.paddingRight#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingRight on /Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Event Row /Content paddingRight to inset.none, which has the same value.                                             |
| `component.event row.content.paddingTop#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingTop on /Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Event Row /Content paddingTop to inset.none, which has the same value.                                               |
| `component.event row.leading.height#unbound`                                                                                     | 32, bound to no variable                                                                                                                                                 | height on /Leading is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (set):** As its width, the leading Avatar's own. Ask SOLAR to bind Event Row /Leading height to icon.2xl, which has the same value.                                                                                                                                                                                                                                                                                                                                                            |
| `component.event row.leading.width#unbound`                                                                                      | 32, bound to no variable                                                                                                                                                 | width on /Leading is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (set):** The leading slot holds the caller's Avatar (md, 32px) or icon, whose size is its own: the row places it and never sizes it. Ask SOLAR to bind Event Row /Leading width to icon.2xl, which has the same value.                                                                                                                                                                                                                                                                         |
| `component.event row.meta.background#unbound`                                                                                    | #ffffff, bound to no variable                                                                                                                                            | background on /Content/Meta is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (set):** Figma's focused row fills its meta line white (#ffffff, bound to nothing) and draws no focus ring: a stray fill, unseen on the row's own white, where its description asks for a 2px ring (shadow/focus/default). The code draws no fill there, and the ring below; the design review lists it. SOLAR has no background token; ask SOLAR for one, or confirm #ffffff is intended.                                                                                                     |
| `component.event row.meta.paddingBottom#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Content/Meta is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Event Row /Content/Meta paddingBottom to inset.none, which has the same value.                                       |
| `component.event row.meta.paddingLeft#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Content/Meta is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Event Row /Content/Meta paddingLeft to inset.none, which has the same value.                                         |
| `component.event row.meta.paddingRight#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingRight on /Content/Meta is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Event Row /Content/Meta paddingRight to inset.none, which has the same value.                                        |
| `component.event row.meta.paddingTop#unbound`                                                                                    | 0, bound to no variable                                                                                                                                                  | paddingTop on /Content/Meta is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Event Row /Content/Meta paddingTop to inset.none, which has the same value.                                          |
| `component.event row.more.width#unbound`                                                                                         | 20, bound to no variable                                                                                                                                                 | width on /Icon/More is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (bind):** Same value, governed token; the More icon's height is already bound to it. Ask SOLAR to bind Event Row /Icon/More width to icon.md, which has the same value.                                                                                                                                                                                                                                                                                                                        |
| `component.expandable card.content.gap#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | gap on /Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Expandable Card /Content gap to inset.none or stack.none, which has the same value.                    |
| `component.expandable card.content.paddingBottom#unbound`                                                                        | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Expandable Card /Content paddingBottom to inset.none, which has the same value.                                      |
| `component.expandable card.content.paddingLeft#unbound`                                                                          | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Expandable Card /Content paddingLeft to inset.none, which has the same value.                                        |
| `component.expandable card.content.paddingRight#unbound`                                                                         | 0, bound to no variable                                                                                                                                                  | paddingRight on /Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Expandable Card /Content paddingRight to inset.none, which has the same value.                                       |
| `component.expandable card.content.paddingTop#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingTop on /Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Expandable Card /Content paddingTop to inset.none, which has the same value.                                         |
| `component.expandable card.header.align@expanded=false, state=hover`                                                             | 1 variant at expanded=false, state=hover: SPACE_BETWEEN/CENTER where MIN/CENTER                                                                                          | align on /Header should follow no axis only, but at expanded=false, state=hover it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** The hovered collapsed card spreads its header (SPACE_BETWEEN), every other packs it (MIN): the title fills the header, so the chevron sits at its end in both, and they draw alike. The code packs it in all; the design review lists it. Ask SOLAR whether Expandable Card /Header align is meant to change at expanded=false, state=hover.                                                                                                                                       |
| `component.expandable card.header.paddingBottom#unbound`                                                                         | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Header is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Expandable Card /Header paddingBottom to inset.none, which has the same value.                                       |
| `component.expandable card.header.paddingLeft#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Header is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Expandable Card /Header paddingLeft to inset.none, which has the same value.                                         |
| `component.expandable card.header.paddingRight#unbound`                                                                          | 0, bound to no variable                                                                                                                                                  | paddingRight on /Header is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Expandable Card /Header paddingRight to inset.none, which has the same value.                                        |
| `component.expandable card.header.paddingTop#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingTop on /Header is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Expandable Card /Header paddingTop to inset.none, which has the same value.                                          |
| `component.expandable card.iconChevronDown.width#unbound`                                                                        | 16, bound to no variable                                                                                                                                                 | width on /Header/Icon/ChevronDown is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind):** Same value, governed token; the chevron's height is already bound to it. Ask SOLAR to bind Expandable Card /Header/Icon/ChevronDown width to icon.sm, which has the same value.                                                                                                                                                                                                                                                                                                      |
| `component.expandable card.iconChevronUp.width#unbound`                                                                          | 16, bound to no variable                                                                                                                                                 | width on /Header/Icon/ChevronUp is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind):** As the down chevron's. Ask SOLAR to bind Expandable Card /Header/Icon/ChevronUp width to icon.sm, which has the same value.                                                                                                                                                                                                                                                                                                                                                          |
| `component.expandable card.root.width#unbound`                                                                                   | 320, bound to no variable                                                                                                                                                | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** A card spans the space it is put in: Figma draws it 320 wide, a sample. The caller sizes it. SOLAR has no control width token; ask SOLAR for one, or confirm 320 is intended.                                                                                                                                                                                                                                                                                                         |
| `component.fab.icon.height#unbound`                                                                                              | 24, bound to no variable                                                                                                                                                 | height on /Icon/None is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind):** Same value, governed token: an extended FAB's icon is 24 tall and bound to nothing, and 24 wide bound to icon.lg. Ask SOLAR to bind FAB /Icon/None height to icon.lg, which has the same value.                                                                                                                                                                                                                                                                                      |
| `component.fab.icon.width#unbound`                                                                                               | 20, bound to no variable                                                                                                                                                 | width on /Icon/None is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (bind):** Same value, governed token: an icon FAB's icon is 20 wide and bound to nothing, and 20 tall bound to icon.md. Ask SOLAR to bind FAB /Icon/None width to icon.md, which has the same value.                                                                                                                                                                                                                                                                                           |
| `component.fab.label.width@type=extended, state=loading`                                                                         | 2 variants at type=extended, state=loading: HUG where FILL                                                                                                               | width on /Label should follow size only, but at type=extended, state=loading it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Loading hides the label, keeping its room: Figma sizes the hidden words to hug, where the shown ones fill the FAB, which draws nothing either way. The label fills in every state. Ask SOLAR whether FAB /Label width is meant to change at type=extended, state=loading.                                                                                                                                                                                                          |
| `component.fab.root.gap#unbound`                                                                                                 | 0, bound to no variable                                                                                                                                                  | gap on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind FAB / gap to inset.none or stack.none, which has the same value.                                       |
| `component.fab.root.height#unbound`                                                                                              | 44, 56, bound to no variable                                                                                                                                             | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (allowLiteral):** Drawn 44 and 56, as the description says, bound to nothing: neither is one of SOLAR's control heights (size.control: 32, 40, 48), so carried as drawn; the design review asks. SOLAR has no control height token; ask SOLAR for one, or confirm 44, 56 is intended.                                                                                                                                                                                                          |
| `component.fab.root.paddingRight@state=loading`                                                                                  | 2 variants at state=loading: inset.sm where inset.lg; inset.md where inset.xl                                                                                            | paddingRight on / should follow type, size only, but at state=loading it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (accept):** As its width, the loading FAB keeps the padding it has at rest. Ask SOLAR whether FAB / paddingRight is meant to change at state=loading.                                                                                                                                                                                                                                                                                                                                          |
| `component.fab.root.width#unbound`                                                                                               | 44, 56, 91, 112, bound to no variable                                                                                                                                    | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (allowLiteral):** An icon FAB is a circle, so its width is its height, under the same decision. SOLAR has no control width token; ask SOLAR for one, or confirm 44, 56, 91, 112 is intended.                                                                                                                                                                                                                                                                                                   |
| `component.fab.root.width@state=loading`                                                                                         | 2 variants at state=loading: 91 where HUG; 112 where HUG                                                                                                                 | width on / should follow type, size only, but at state=loading it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** A loading FAB keeps its size, the label hidden but keeping its room, as Button's does, so it does not shrink under the pointer. Figma's loading extended FAB is 7 and 8px narrower (84 and 104, beside 91 and 112) and padded evenly, which looks like an accident; the design review lists it. Ask SOLAR whether FAB / width is meant to change at state=loading.                                                                                                                 |
| `component.file card.fileIcon.height#unbound`                                                                                    | 24, bound to no variable                                                                                                                                                 | height on /Thumbnail/Icon/Image is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind):** Same value, governed token; the file icon's width is already bound to it. Ask SOLAR to bind File Card /Thumbnail/Icon/Image height to icon.lg, which has the same value.                                                                                                                                                                                                                                                                                                             |
| `component.file card.iconMore.width#unbound`                                                                                     | 20, bound to no variable                                                                                                                                                 | width on /Footer/Icon/More is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (bind):** Same value, governed token; the More icon's height is already bound to it. Ask SOLAR to bind File Card /Footer/Icon/More width to icon.md, which has the same value.                                                                                                                                                                                                                                                                                                                 |
| `component.file card.iconPlus.height#unbound`                                                                                    | 24, bound to no variable                                                                                                                                                 | height on /Frame 1/Icon/Plus is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (bind):** Same value, governed token; the Plus's width is already bound to it. Ask SOLAR to bind File Card /Frame 1/Icon/Plus height to icon.lg, which has the same value.                                                                                                                                                                                                                                                                                                                     |
| `component.file card.root.borderWidth#unbound`                                                                                   | 1, bound to no variable                                                                                                                                                  | borderWidth on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (accept):** As the create tile's edge's, border.default's value, which the code draws. Ask SOLAR to bind File Card / borderWidth to border.default, which has the same value.                                                                                                                                                                                                                                                                                                                  |
| `component.file card.root.borderWidth@type=New Asset Tile`                                                                       | 1 variant at type=New Asset Tile: 1 where border.default                                                                                                                 | borderWidth on / should follow no axis only, but at type=New Asset Tile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** The create tile's edge is 1px, bound to nothing, where the file's tile's is border.default, also 1px: the same edge. The code draws border.default on both. Ask SOLAR whether File Card / borderWidth is meant to change at type=New Asset Tile.                                                                                                                                                                                                                                   |
| `component.file card.root.gap#unbound`                                                                                           | 0, bound to no variable                                                                                                                                                  | gap on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind File Card / gap to inset.none or stack.none, which has the same value.                                 |
| `component.file card.root.height#unbound`                                                                                        | 222, bound to no variable                                                                                                                                                | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (allowLiteral):** A tile is 222 tall, bound to nothing, its thumbnail taking what the footer leaves: SOLAR has no tile size token. Carried as Figma's number, and raised as a governance gap. SOLAR has no control height token; ask SOLAR for one, or confirm 222 is intended.                                                                                                                                                                                                                |
| `component.file card.root.paddingBottom#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind File Card / paddingBottom to inset.none, which has the same value.                                                   |
| `component.file card.root.paddingLeft#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind File Card / paddingLeft to inset.none, which has the same value.                                                     |
| `component.file card.root.paddingRight#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind File Card / paddingRight to inset.none, which has the same value.                                                    |
| `component.file card.root.paddingTop#unbound`                                                                                    | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind File Card / paddingTop to inset.none, which has the same value.                                                      |
| `component.file card.root.width#unbound`                                                                                         | 308, bound to no variable                                                                                                                                                | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** A tile spans its cell of the grid: Figma draws it 308 wide, a sample. The caller sizes it. SOLAR has no control width token; ask SOLAR for one, or confirm 308 is intended.                                                                                                                                                                                                                                                                                                           |
| `component.file card.text.paddingBottom#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Footer/Text is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind File Card /Footer/Text paddingBottom to inset.none, which has the same value.                                        |
| `component.file card.text.paddingLeft#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Footer/Text is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind File Card /Footer/Text paddingLeft to inset.none, which has the same value.                                          |
| `component.file card.text.paddingRight#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingRight on /Footer/Text is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind File Card /Footer/Text paddingRight to inset.none, which has the same value.                                         |
| `component.file card.text.paddingTop#unbound`                                                                                    | 0, bound to no variable                                                                                                                                                  | paddingTop on /Footer/Text is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind File Card /Footer/Text paddingTop to inset.none, which has the same value.                                           |
| `component.file card.thumbnail.gap#unbound`                                                                                      | 0, bound to no variable                                                                                                                                                  | gap on /Thumbnail is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind File Card /Thumbnail gap to inset.none or stack.none, which has the same value.                        |
| `component.file card.thumbnail.paddingBottom#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Thumbnail is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind File Card /Thumbnail paddingBottom to inset.none, which has the same value.                                          |
| `component.file card.thumbnail.paddingLeft#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Thumbnail is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind File Card /Thumbnail paddingLeft to inset.none, which has the same value.                                            |
| `component.file card.thumbnail.paddingRight#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingRight on /Thumbnail is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind File Card /Thumbnail paddingRight to inset.none, which has the same value.                                           |
| `component.file card.thumbnail.paddingTop#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingTop on /Thumbnail is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind File Card /Thumbnail paddingTop to inset.none, which has the same value.                                             |
| `component.file card.thumbnail.radiusBottomLeft#unbound`                                                                         | 0, bound to no variable                                                                                                                                                  | radiusBottomLeft on /Thumbnail is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (bind):** The thumbnail's bottom corners are square, where it meets the footer; its top ones are the card's (radius.container). Same value, governed token. Ask SOLAR to bind File Card /Thumbnail radiusBottomLeft to radius.none, which has the same value.                                                                                                                                                                                                                                  |
| `component.file card.thumbnail.radiusBottomRight#unbound`                                                                        | 0, bound to no variable                                                                                                                                                  | radiusBottomRight on /Thumbnail is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind):** As the bottom left corner's. Ask SOLAR to bind File Card /Thumbnail radiusBottomRight to radius.none, which has the same value.                                                                                                                                                                                                                                                                                                                                                      |
| `component.fileupload.button.height#unbound`                                                                                     | 40, bound to no variable                                                                                                                                                 | height on /Field/Button is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (set):** Browse is a SOLAR Button, md as Figma draws it, whose height is its own: the field lays it out and never sizes it. SOLAR has no control height token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                                                                   |
| `component.fileupload.iconButton.height#unbound`                                                                                 | 40, bound to no variable                                                                                                                                                 | height on /Field/Icon Button is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (set):** As Browse's, the replace Icon Button's own size. SOLAR has no control height token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                                                                                                                                     |
| `component.fileupload.iconButton.width#unbound`                                                                                  | 40, bound to no variable                                                                                                                                                 | width on /Field/Icon Button is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (set):** As Browse's, the replace Icon Button's own size. SOLAR has no control width token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                                                                                                                                      |
| `component.fileupload.iconButton2.height#unbound`                                                                                | 40, bound to no variable                                                                                                                                                 | height on /Field/Icon Button#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (set):** As the replace button's, the remove Icon Button's own size. SOLAR has no control height token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                                                                                                                          |
| `component.fileupload.iconButton2.width#unbound`                                                                                 | 40, bound to no variable                                                                                                                                                 | width on /Field/Icon Button#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (set):** As the replace button's, the remove Icon Button's own size. SOLAR has no control width token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                                                                                                                           |
| `component.fileupload.iconFile.width#unbound`                                                                                    | 20, bound to no variable                                                                                                                                                 | width on /Field/Icon/File is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind):** Same value, governed token; the icon's height is already bound to it. Ask SOLAR to bind FileUpload /Field/Icon/File width to icon.md, which has the same value.                                                                                                                                                                                                                                                                                                                      |
| `component.fileupload.label.paddingBottom#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind FileUpload /Label paddingBottom to inset.none, which has the same value.                                             |
| `component.fileupload.label.paddingLeft#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind FileUpload /Label paddingLeft to inset.none, which has the same value.                                               |
| `component.fileupload.label.paddingRight#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingRight on /Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind FileUpload /Label paddingRight to inset.none, which has the same value.                                              |
| `component.fileupload.label.paddingTop#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingTop on /Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind FileUpload /Label paddingTop to inset.none, which has the same value.                                                |
| `component.fileupload.root.paddingBottom#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind FileUpload / paddingBottom to inset.none, which has the same value.                                                  |
| `component.fileupload.root.paddingLeft#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind FileUpload / paddingLeft to inset.none, which has the same value.                                                    |
| `component.fileupload.root.paddingRight#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind FileUpload / paddingRight to inset.none, which has the same value.                                                   |
| `component.fileupload.root.paddingTop#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind FileUpload / paddingTop to inset.none, which has the same value.                                                     |
| `component.globalsearch.iconSearch.width#unbound`                                                                                | 16, 12, bound to no variable                                                                                                                                             | width on /Icon/Search is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind):** Same values, governed tokens, a step of the icon ladder per size; the icon's height is already bound to them. Ask SOLAR to bind GlobalSearch /Icon/Search width to icon.sm or icon.xs, which has the same value.                                                                                                                                                                                                                                                                     |
| `component.globalsearch.root.height#unbound`                                                                                     | 40, 32, bound to no variable                                                                                                                                             | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind):** As Button's height: size.control, one step per size, which SOLAR added on 2026-09-25; Figma leaves the height unbound, which the design review asks SOLAR to bind. SOLAR has no control height token; ask SOLAR for one, or confirm 40, 32 is intended.                                                                                                                                                                                                                              |
| `component.globalsearch.root.width#unbound`                                                                                      | 240, 200, bound to no variable                                                                                                                                           | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** As md's, the sm trigger's 200 a sample. SOLAR has no control width token; ask SOLAR for one, or confirm 240, 200 is intended.                                                                                                                                                                                                                                                                                                                                                         |
| `component.icon button.icon.height#unbound`                                                                                      | 12, 16, 20, bound to no variable                                                                                                                                         | height on /Icon/None is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind):** Same values, governed tokens, one step of the icon ladder per size; the icon's width is already bound to them. Ask SOLAR to bind Icon Button /Icon/None height to icon.xs or icon.sm or icon.md, which has the same value.                                                                                                                                                                                                                                                           |
| `component.icon button.root.background@size=lg`                                                                                  | 8 variants at size=lg: color.action.secondary.bg.default where none; color.action.secondary.bg.active where none                                                         | background on / should follow shape, prio, state only, but at size=lg it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (accept):** Secondary has no background (its description, and the variables since 2026-09-25): Figma leaves the sm fill empty and binds md's and lg's to action.secondary.bg, transparent in both modes. The two draw the same. Ask SOLAR whether Icon Button / background is meant to change at size=lg.                                                                                                                                                                                      |
| `component.icon button.root.background@size=md`                                                                                  | 8 variants at size=md: color.action.secondary.bg.default where none; color.action.secondary.bg.active where none                                                         | background on / should follow shape, prio, state only, but at size=md it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (accept):** Secondary has no background (its description, and the variables since 2026-09-25): Figma leaves the sm fill empty and binds md's and lg's to action.secondary.bg, transparent in both modes. The two draw the same. Ask SOLAR whether Icon Button / background is meant to change at size=md.                                                                                                                                                                                      |
| `component.icon button.root.gap#unbound`                                                                                         | 0, bound to no variable                                                                                                                                                  | gap on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Icon Button / gap to inset.none or stack.none, which has the same value.                               |
| `component.icon button.root.height#unbound`                                                                                      | 32, 40, 48, bound to no variable                                                                                                                                         | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind):** As Button's height: size.control, one step per size, which SOLAR added on 2026-09-25; Figma leaves the height unbound, which the design review asks SOLAR to bind. SOLAR has no control height token; ask SOLAR for one, or confirm 32, 40, 48 is intended.                                                                                                                                                                                                                          |
| `component.icon button.root.paddingBottom#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Icon Button / paddingBottom to inset.none, which has the same value.                                                 |
| `component.icon button.root.paddingLeft#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Icon Button / paddingLeft to inset.none, which has the same value.                                                   |
| `component.icon button.root.paddingRight#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Icon Button / paddingRight to inset.none, which has the same value.                                                  |
| `component.icon button.root.paddingTop#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Icon Button / paddingTop to inset.none, which has the same value.                                                    |
| `component.icon button.root.width#unbound`                                                                                       | 32, 40, 48, bound to no variable                                                                                                                                         | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (allowLiteral):** An icon button is square, so its width is its height, under the same decision. SOLAR has no control width token; ask SOLAR for one, or confirm 32, 40, 48 is intended.                                                                                                                                                                                                                                                                                                       |
| `component.image card.checkbox.height#unbound`                                                                                   | 16, bound to no variable                                                                                                                                                 | height on /Image/Checkbox is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (set):** As its width, the Checkbox's own. Ask SOLAR to bind Image Card /Image/Checkbox height to icon.sm, which has the same value.                                                                                                                                                                                                                                                                                                                                                           |
| `component.image card.checkbox.width#unbound`                                                                                    | 16, bound to no variable                                                                                                                                                 | width on /Image/Checkbox is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (set):** The selection is a SOLAR Checkbox, whose 16px box is its own: the tile places it, and never sizes it. Ask SOLAR to bind Image Card /Image/Checkbox width to icon.sm, which has the same value.                                                                                                                                                                                                                                                                                        |
| `component.image card.icon.height#unbound`                                                                                       | 14, bound to no variable                                                                                                                                                 | height on /Text/Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (allowLiteral):** As the mark's width, carried as drawn. SOLAR has no control height token; ask SOLAR for one, or confirm 14 is intended.                                                                                                                                                                                                                                                                                                                                                      |
| `component.image card.icon.width#unbound`                                                                                        | 3, bound to no variable                                                                                                                                                  | width on /Text/Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (allowLiteral):** The More mark is Figma's drawing (a vector, 3 × 14), not a SOLAR icon: carried as drawn, the button around it given a 44 × 44 target. The design review asks SOLAR to use Icon/More. SOLAR has no control width token; ask SOLAR for one, or confirm 3 is intended.                                                                                                                                                                                                          |
| `component.image card.iconCircle.gap#unbound`                                                                                    | 0, bound to no variable                                                                                                                                                  | gap on /IconCircle is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Image Card /IconCircle gap to inset.none or stack.none, which has the same value.                      |
| `component.image card.iconCircle.height#unbound`                                                                                 | 44, bound to no variable                                                                                                                                                 | height on /IconCircle is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (allowLiteral):** As the circle's width, a circle. SOLAR has no control height token; ask SOLAR for one, or confirm 44 is intended.                                                                                                                                                                                                                                                                                                                                                            |
| `component.image card.iconCircle.paddingBottom#unbound`                                                                          | 0, bound to no variable                                                                                                                                                  | paddingBottom on /IconCircle is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Image Card /IconCircle paddingBottom to inset.none, which has the same value.                                        |
| `component.image card.iconCircle.paddingLeft#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingLeft on /IconCircle is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Image Card /IconCircle paddingLeft to inset.none, which has the same value.                                          |
| `component.image card.iconCircle.paddingRight#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingRight on /IconCircle is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Image Card /IconCircle paddingRight to inset.none, which has the same value.                                         |
| `component.image card.iconCircle.paddingTop#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingTop on /IconCircle is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Image Card /IconCircle paddingTop to inset.none, which has the same value.                                           |
| `component.image card.iconCircle.width#unbound`                                                                                  | 44, bound to no variable                                                                                                                                                 | width on /IconCircle is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (allowLiteral):** The unfilled tile's Plus sits in a 44px circle, bound to nothing: SOLAR has no size token for it (as for Option Card's 64); carried as Figma's number, and raised as a governance gap. SOLAR has no control width token; ask SOLAR for one, or confirm 44 is intended.                                                                                                                                                                                                       |
| `component.image card.iconPlus.height#unbound`                                                                                   | 22, bound to no variable                                                                                                                                                 | height on /IconCircle/Icon/Plus is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (allowLiteral):** As the Plus's width. SOLAR has no control height token; ask SOLAR for one, or confirm 22 is intended.                                                                                                                                                                                                                                                                                                                                                                        |
| `component.image card.iconPlus.width#unbound`                                                                                    | 22, bound to no variable                                                                                                                                                 | width on /IconCircle/Icon/Plus is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (allowLiteral):** The Plus is 22px, between the icon ladder's lg (24) and md (20), bound to nothing: carried as Figma's number, and raised as a governance gap (a size off the ladder). SOLAR has no control width token; ask SOLAR for one, or confirm 22 is intended.                                                                                                                                                                                                                        |
| `component.image card.image.gap#unbound`                                                                                         | 0, bound to no variable                                                                                                                                                  | gap on /Image is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Image Card /Image gap to inset.none or stack.none, which has the same value.                           |
| `component.image card.root.gap#unbound`                                                                                          | 0, bound to no variable                                                                                                                                                  | gap on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Image Card / gap to inset.none or stack.none, which has the same value.                                |
| `component.image card.root.height#unbound`                                                                                       | 190, bound to no variable                                                                                                                                                | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (allowLiteral):** A tile is 190 tall, bound to nothing, its image taking what its words leave: SOLAR has no tile size token. Carried as Figma's number, and raised as a governance gap. SOLAR has no control height token; ask SOLAR for one, or confirm 190 is intended.                                                                                                                                                                                                                      |
| `component.image card.root.paddingBottom#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Image Card / paddingBottom to inset.none, which has the same value.                                                  |
| `component.image card.root.paddingLeft#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Image Card / paddingLeft to inset.none, which has the same value.                                                    |
| `component.image card.root.paddingRight#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Image Card / paddingRight to inset.none, which has the same value.                                                   |
| `component.image card.root.paddingTop#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Image Card / paddingTop to inset.none, which has the same value.                                                     |
| `component.image card.root.width#unbound`                                                                                        | 200, bound to no variable                                                                                                                                                | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** A tile spans its cell of a grid: Figma draws it 200 wide, a sample. The caller sizes it. SOLAR has no control width token; ask SOLAR for one, or confirm 200 is intended.                                                                                                                                                                                                                                                                                                             |
| `component.image card.sparse@filled=false`                                                                                       | no variant with filled=false and selected=true, state=default                                                                                                            | Image Card has no variant that holds selected, state at the default for filled=false, so that combination is read from filled=false, selected=false, hover=false instead.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (accept):** The unfilled tile adds an image and has none to select: Figma draws it unselected alone, which is the look it has. Figma's default is selected (true), which the code's is not: a tile starts unselected. Ask SOLAR whether Image Card should have the variant with filled=false at the default selected, state.                                                                                                                                                                   |
| `component.inline input.cancel.height#unbound`                                                                                   | 32, bound to no variable                                                                                                                                                 | height on /Frame 1/Cancel is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (set):** As the edit button's. Ask SOLAR to bind Inline Input /Frame 1/Cancel height to icon.2xl, which has the same value.                                                                                                                                                                                                                                                                                                                                                                    |
| `component.inline input.cancel.width#unbound`                                                                                    | 32, bound to no variable                                                                                                                                                 | width on /Frame 1/Cancel is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (set):** As the edit button's. Ask SOLAR to bind Inline Input /Frame 1/Cancel width to icon.2xl, which has the same value.                                                                                                                                                                                                                                                                                                                                                                     |
| `component.inline input.confirm.height#unbound`                                                                                  | 32, bound to no variable                                                                                                                                                 | height on /Frame 1/Confirm is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** As the edit button's. Ask SOLAR to bind Inline Input /Frame 1/Confirm height to icon.2xl, which has the same value.                                                                                                                                                                                                                                                                                                                                                                   |
| `component.inline input.confirm.width#unbound`                                                                                   | 32, bound to no variable                                                                                                                                                 | width on /Frame 1/Confirm is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (set):** As the edit button's. Ask SOLAR to bind Inline Input /Frame 1/Confirm width to icon.2xl, which has the same value.                                                                                                                                                                                                                                                                                                                                                                    |
| `component.inline input.frame1.gap#unbound`                                                                                      | 0, bound to no variable                                                                                                                                                  | gap on /Frame 1 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Inline Input /Frame 1 gap to inset.none or stack.none, which has the same value.                       |
| `component.inline input.frame1.paddingBottom#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Frame 1 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Inline Input /Frame 1 paddingBottom to inset.none, which has the same value.                                         |
| `component.inline input.frame1.paddingLeft#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Frame 1 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Inline Input /Frame 1 paddingLeft to inset.none, which has the same value.                                           |
| `component.inline input.frame1.paddingRight#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingRight on /Frame 1 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Inline Input /Frame 1 paddingRight to inset.none, which has the same value.                                          |
| `component.inline input.frame1.paddingTop#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingTop on /Frame 1 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Inline Input /Frame 1 paddingTop to inset.none, which has the same value.                                            |
| `component.inline input.iconButton.height#unbound`                                                                               | 32, bound to no variable                                                                                                                                                 | height on /Icon Button is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (set):** The edit, Confirm and Cancel buttons are SOLAR Icon Buttons, sm as Figma draws them, whose size is their own: the row places them and never sizes them. Ask SOLAR to bind Inline Input /Icon Button height to icon.2xl, which has the same value.                                                                                                                                                                                                                                     |
| `component.inline input.iconButton.width#unbound`                                                                                | 32, bound to no variable                                                                                                                                                 | width on /Icon Button is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (set):** The edit, Confirm and Cancel buttons are SOLAR Icon Buttons, sm as Figma draws them, whose size is their own: the row places them and never sizes them. Ask SOLAR to bind Inline Input /Icon Button width to icon.2xl, which has the same value.                                                                                                                                                                                                                                      |
| `component.inline input.root.height#unbound`                                                                                     | 40, bound to no variable                                                                                                                                                 | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind):** As Button's height: size.control, one step per size, which SOLAR added on 2026-09-25; Figma leaves the height unbound, which the design review asks SOLAR to bind. SOLAR has no control height token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                  |
| `component.inline input.root.paddingBottom#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Inline Input / paddingBottom to inset.none, which has the same value.                                                |
| `component.inline input.root.paddingTop#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Inline Input / paddingTop to inset.none, which has the same value.                                                   |
| `component.inline input.root.width#unbound`                                                                                      | 240, bound to no variable                                                                                                                                                | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** An inline value spans the room its row gives it: Figma draws it 240 wide, a sample. The caller places it. SOLAR has no control width token; ask SOLAR for one, or confirm 240 is intended.                                                                                                                                                                                                                                                                                            |
| `component.insight card small.content.paddingBottom#unbound`                                                                     | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Insight Card Small /Content paddingBottom to inset.none, which has the same value.                                   |
| `component.insight card small.content.paddingLeft#unbound`                                                                       | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Insight Card Small /Content paddingLeft to inset.none, which has the same value.                                     |
| `component.insight card small.content.paddingRight#unbound`                                                                      | 0, bound to no variable                                                                                                                                                  | paddingRight on /Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Insight Card Small /Content paddingRight to inset.none, which has the same value.                                    |
| `component.insight card small.content.paddingTop#unbound`                                                                        | 0, bound to no variable                                                                                                                                                  | paddingTop on /Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Insight Card Small /Content paddingTop to inset.none, which has the same value.                                      |
| `component.insight card small.icon.height#unbound`                                                                               | 40, bound to no variable                                                                                                                                                 | height on /Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (allowLiteral):** As the tile's width, a square. SOLAR has no control height token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                                                                                                                                              |
| `component.insight card small.icon.paddingBottom#unbound`                                                                        | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Insight Card Small /Icon paddingBottom to inset.none, which has the same value.                                      |
| `component.insight card small.icon.paddingLeft#unbound`                                                                          | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Insight Card Small /Icon paddingLeft to inset.none, which has the same value.                                        |
| `component.insight card small.icon.paddingRight#unbound`                                                                         | 0, bound to no variable                                                                                                                                                  | paddingRight on /Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Insight Card Small /Icon paddingRight to inset.none, which has the same value.                                       |
| `component.insight card small.icon.paddingTop#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingTop on /Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Insight Card Small /Icon paddingTop to inset.none, which has the same value.                                         |
| `component.insight card small.icon.width#unbound`                                                                                | 40, bound to no variable                                                                                                                                                 | width on /Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (allowLiteral):** The severity's tile is a 40px square, bound to nothing: SOLAR has no size token for it (as for Insight Card's 52); carried as Figma's number, and raised as a governance gap. SOLAR has no control width token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                |
| `component.insight card small.root.width#unbound`                                                                                | 320, bound to no variable                                                                                                                                                | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** A card spans the space it is put in: Figma draws it 320 wide, a sample. The caller sizes it. SOLAR has no control width token; ask SOLAR for one, or confirm 320 is intended.                                                                                                                                                                                                                                                                                                         |
| `component.insight card small.skeleton.height#unbound`                                                                           | 12, bound to no variable                                                                                                                                                 | height on /Content/Skeleton is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (allowLiteral):** The loading placeholders are Figma's drawing of two lines to come (the first filling the content, the second 170 wide; 12 tall), no measure of anything: carried as drawn. SOLAR has no skeleton size tokens. SOLAR has no control height token; ask SOLAR for one, or confirm 12 is intended.                                                                                                                                                                               |
| `component.insight card small.skeleton2.height#unbound`                                                                          | 12, bound to no variable                                                                                                                                                 | height on /Content/Skeleton#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (allowLiteral):** As the first placeholder's, carried as drawn. SOLAR has no control height token; ask SOLAR for one, or confirm 12 is intended.                                                                                                                                                                                                                                                                                                                                               |
| `component.insight card small.skeleton2.width#unbound`                                                                           | 170, bound to no variable                                                                                                                                                | width on /Content/Skeleton#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (allowLiteral):** As the first placeholder's height, carried as drawn. SOLAR has no control width token; ask SOLAR for one, or confirm 170 is intended.                                                                                                                                                                                                                                                                                                                                        |
| `component.insight card small.statusIndicator.height#unbound`                                                                    | 20, bound to no variable                                                                                                                                                 | height on /Icon/StatusIndicator is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (set):** As its width, the StatusIndicator's own. Ask SOLAR to bind Insight Card Small /Icon/StatusIndicator height to icon.md, which has the same value.                                                                                                                                                                                                                                                                                                                                      |
| `component.insight card small.statusIndicator.width#unbound`                                                                     | 20, bound to no variable                                                                                                                                                 | width on /Icon/StatusIndicator is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (set):** The mark is a StatusIndicator, whose md, 20px is its own: the tile centres it, and never sizes it. Ask SOLAR to bind Insight Card Small /Icon/StatusIndicator width to icon.md, which has the same value.                                                                                                                                                                                                                                                                             |
| `component.insight card.content.paddingBottom#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Insight Card /Content paddingBottom to inset.none, which has the same value.                                         |
| `component.insight card.content.paddingLeft#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Insight Card /Content paddingLeft to inset.none, which has the same value.                                           |
| `component.insight card.content.paddingRight#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingRight on /Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Insight Card /Content paddingRight to inset.none, which has the same value.                                          |
| `component.insight card.content.paddingTop#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingTop on /Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Insight Card /Content paddingTop to inset.none, which has the same value.                                            |
| `component.insight card.icon.height#unbound`                                                                                     | 52, bound to no variable                                                                                                                                                 | height on /Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (allowLiteral):** As the tile's width, a square. SOLAR has no control height token; ask SOLAR for one, or confirm 52 is intended.                                                                                                                                                                                                                                                                                                                                                              |
| `component.insight card.icon.paddingBottom#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Insight Card /Icon paddingBottom to inset.none, which has the same value.                                            |
| `component.insight card.icon.paddingLeft#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Insight Card /Icon paddingLeft to inset.none, which has the same value.                                              |
| `component.insight card.icon.paddingRight#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingRight on /Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Insight Card /Icon paddingRight to inset.none, which has the same value.                                             |
| `component.insight card.icon.paddingTop#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingTop on /Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Insight Card /Icon paddingTop to inset.none, which has the same value.                                               |
| `component.insight card.icon.width#unbound`                                                                                      | 52, bound to no variable                                                                                                                                                 | width on /Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (allowLiteral):** The severity's tile is a 52px square, bound to nothing: SOLAR has no size token for it (as for Avatar's sizes); carried as Figma's number, and raised as a governance gap. SOLAR has no control width token; ask SOLAR for one, or confirm 52 is intended.                                                                                                                                                                                                                   |
| `component.insight card.iconMore.width#unbound`                                                                                  | 20, bound to no variable                                                                                                                                                 | width on /Icon/More is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (bind):** Same value, governed token; the More icon's height is already bound to it. Ask SOLAR to bind Insight Card /Icon/More width to icon.md, which has the same value.                                                                                                                                                                                                                                                                                                                     |
| `component.insight card.root.background#covered`                                                                                 | surface/background over surface/base in 5 variants                                                                                                                       | background on / is painted twice, and the top paint is opaque, so only it is ever seen; the recipe draws it. The paint under it is left over.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (accept):** The selected card is painted twice, surface/background over surface/base; the top paint is what shows, and what the code draws. The design review asks SOLAR to leave the one fill. Ask SOLAR to remove the covered surface/base from Insight Card / background.                                                                                                                                                                                                                   |
| `component.insight card.root.width#unbound`                                                                                      | 320, bound to no variable                                                                                                                                                | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** A card spans the space it is put in, as a list's row: Figma draws it 320 wide, a sample. The caller sizes it. SOLAR has no control width token; ask SOLAR for one, or confirm 320 is intended.                                                                                                                                                                                                                                                                                        |
| `component.insight card.skeleton.height#unbound`                                                                                 | 12, bound to no variable                                                                                                                                                 | height on /Content/Skeleton is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (allowLiteral):** The loading placeholders are Figma's drawing of two lines to come (the first filling the content, the second 170 wide; 12 tall), no measure of anything: carried as drawn. SOLAR has no skeleton size tokens. SOLAR has no control height token; ask SOLAR for one, or confirm 12 is intended.                                                                                                                                                                               |
| `component.insight card.skeleton2.height#unbound`                                                                                | 12, bound to no variable                                                                                                                                                 | height on /Content/Skeleton#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (allowLiteral):** As the first placeholder's, carried as drawn. SOLAR has no control height token; ask SOLAR for one, or confirm 12 is intended.                                                                                                                                                                                                                                                                                                                                               |
| `component.insight card.skeleton2.width#unbound`                                                                                 | 170, bound to no variable                                                                                                                                                | width on /Content/Skeleton#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (allowLiteral):** As the first placeholder's height, carried as drawn. SOLAR has no control width token; ask SOLAR for one, or confirm 170 is intended.                                                                                                                                                                                                                                                                                                                                        |
| `component.insight card.statusIndicator.height#unbound`                                                                          | 20, bound to no variable                                                                                                                                                 | height on /Icon/StatusIndicator is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (set):** As its width, the StatusIndicator's own. Ask SOLAR to bind Insight Card /Icon/StatusIndicator height to icon.md, which has the same value.                                                                                                                                                                                                                                                                                                                                            |
| `component.insight card.statusIndicator.width#unbound`                                                                           | 20, bound to no variable                                                                                                                                                 | width on /Icon/StatusIndicator is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (set):** The mark is a StatusIndicator, whose md, 20px is its own: the tile centres it, and never sizes it. Ask SOLAR to bind Insight Card /Icon/StatusIndicator width to icon.md, which has the same value.                                                                                                                                                                                                                                                                                   |
| `component.insight row.action.height#unbound`                                                                                    | 32, bound to no variable                                                                                                                                                 | height on /Content/Action is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (set):** The action is the caller's Button, sm as Figma draws it, whose height is its own: the row places it and never sizes it. Ask SOLAR to bind Insight Row /Content/Action height to icon.2xl, which has the same value.                                                                                                                                                                                                                                                                   |
| `component.insight row.body.paddingBottom#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Content/Body is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Insight Row /Content/Body paddingBottom to inset.none, which has the same value.                                     |
| `component.insight row.body.paddingLeft#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Content/Body is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Insight Row /Content/Body paddingLeft to inset.none, which has the same value.                                       |
| `component.insight row.body.paddingRight#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingRight on /Content/Body is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Insight Row /Content/Body paddingRight to inset.none, which has the same value.                                      |
| `component.insight row.body.paddingTop#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingTop on /Content/Body is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Insight Row /Content/Body paddingTop to inset.none, which has the same value.                                        |
| `component.insight row.meta.width@severity=info, state=default, ghost=true`                                                      | 1 variant at severity=info, state=default, ghost=true: HUG where FILL                                                                                                    | width on /Content/Body/Meta should follow no axis only, but at severity=info, state=default, ghost=true it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** As the title's, the meta line hugs in the same two variants, where every other row's fills. Ask SOLAR whether Insight Row /Content/Body/Meta width is meant to change at severity=info, state=default, ghost=true.                                                                                                                                                                                                                                                                 |
| `component.insight row.meta.width@severity=info, state=hover, ghost=true`                                                        | 1 variant at severity=info, state=hover, ghost=true: HUG where FILL                                                                                                      | width on /Content/Body/Meta should follow no axis only, but at severity=info, state=hover, ghost=true it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (accept):** As the title's, the meta line hugs in the same two variants, where every other row's fills. Ask SOLAR whether Insight Row /Content/Body/Meta width is meant to change at severity=info, state=hover, ghost=true.                                                                                                                                                                                                                                                                   |
| `component.insight row.root.gap#unbound`                                                                                         | 0, bound to no variable                                                                                                                                                  | gap on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Insight Row / gap to inset.none or stack.none, which has the same value.                               |
| `component.insight row.root.height#unbound`                                                                                      | 64, bound to no variable                                                                                                                                                 | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (set):** Figma fixes the row at 64, bound to nothing, its words' boxes drawn shorter than their lines (the body 27 tall, for a 20 and a 16 line 8 apart): at their own line heights the words need 76, and a fixed 64 would cut them. The row hugs its words, each cut short on one line; the design review asks SOLAR for the row's height. SOLAR has no control height token; ask SOLAR for one, or confirm 64 is intended.                                                                  |
| `component.insight row.root.width#unbound`                                                                                       | 480, bound to no variable                                                                                                                                                | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** A row spans the feed or panel it is in: Figma draws it 480 wide, a sample. The caller sizes it. SOLAR has no control width token; ask SOLAR for one, or confirm 480 is intended.                                                                                                                                                                                                                                                                                                      |
| `component.insight row.severityBar.width#unbound`                                                                                | 4, bound to no variable                                                                                                                                                  | width on /Severity Bar is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (allowLiteral):** The severity's bar is 4px wide, bound to nothing: SOLAR has no size token for an accent bar; carried as Figma's number, and raised as a governance gap. SOLAR has no control width token; ask SOLAR for one, or confirm 4 is intended.                                                                                                                                                                                                                                       |
| `component.insight row.skeleton.height#unbound`                                                                                  | 12, bound to no variable                                                                                                                                                 | height on /Content/Body/Skeleton is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (allowLiteral):** The loading placeholders are Figma's drawing of two lines to come (the first filling the body, the second 140 wide; 12 tall), no measure of anything: carried as drawn. SOLAR has no skeleton size tokens. SOLAR has no control height token; ask SOLAR for one, or confirm 12 is intended.                                                                                                                                                                                  |
| `component.insight row.skeleton2.height#unbound`                                                                                 | 12, bound to no variable                                                                                                                                                 | height on /Content/Body/Skeleton#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (allowLiteral):** As the first placeholder's, carried as drawn. SOLAR has no control height token; ask SOLAR for one, or confirm 12 is intended.                                                                                                                                                                                                                                                                                                                                               |
| `component.insight row.skeleton2.width#unbound`                                                                                  | 140, bound to no variable                                                                                                                                                | width on /Content/Body/Skeleton#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (allowLiteral):** As the first placeholder's height, carried as drawn. SOLAR has no control width token; ask SOLAR for one, or confirm 140 is intended.                                                                                                                                                                                                                                                                                                                                        |
| `component.insight row.title.width@severity=info, state=default, ghost=true`                                                     | 1 variant at severity=info, state=default, ghost=true: HUG where FILL                                                                                                    | width on /Content/Body/Title should follow no axis only, but at severity=info, state=default, ghost=true it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (accept):** Figma sizes the loading info row's title to hug at rest and on hover, where every other row's fills it: a slip in two of ten, which draws the same, since the title's placeholder is its own width. The title fills in every row; the design review lists it. Ask SOLAR whether Insight Row /Content/Body/Title width is meant to change at severity=info, state=default, ghost=true.                                                                                              |
| `component.insight row.title.width@severity=info, state=hover, ghost=true`                                                       | 1 variant at severity=info, state=hover, ghost=true: HUG where FILL                                                                                                      | width on /Content/Body/Title should follow no axis only, but at severity=info, state=hover, ghost=true it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (accept):** Figma sizes the loading info row's title to hug at rest and on hover, where every other row's fills it: a slip in two of ten, which draws the same, since the title's placeholder is its own width. The title fills in every row; the design review lists it. Ask SOLAR whether Insight Row /Content/Body/Title width is meant to change at severity=info, state=hover, ghost=true.                                                                                                |
| `component.interactive card.actions.paddingBottom#unbound`                                                                       | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Actions is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Interactive Card /Actions paddingBottom to inset.none, which has the same value.                                     |
| `component.interactive card.actions.paddingLeft#unbound`                                                                         | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Actions is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Interactive Card /Actions paddingLeft to inset.none, which has the same value.                                       |
| `component.interactive card.actions.paddingRight#unbound`                                                                        | 0, bound to no variable                                                                                                                                                  | paddingRight on /Actions is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Interactive Card /Actions paddingRight to inset.none, which has the same value.                                      |
| `component.interactive card.actions.paddingTop#unbound`                                                                          | 0, bound to no variable                                                                                                                                                  | paddingTop on /Actions is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Interactive Card /Actions paddingTop to inset.none, which has the same value.                                        |
| `component.interactive card.checkbox.height#unbound`                                                                             | 16, bound to no variable                                                                                                                                                 | height on /Checkbox is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (set):** As the Toggle's, the Checkbox's own size. Ask SOLAR to bind Interactive Card /Checkbox height to icon.sm, which has the same value.                                                                                                                                                                                                                                                                                                                                                   |
| `component.interactive card.checkbox.width#unbound`                                                                              | 16, bound to no variable                                                                                                                                                 | width on /Checkbox is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (set):** As the Toggle's, the Checkbox's own size. Ask SOLAR to bind Interactive Card /Checkbox width to icon.sm, which has the same value.                                                                                                                                                                                                                                                                                                                                                    |
| `component.interactive card.icon.height#unbound`                                                                                 | 16, bound to no variable                                                                                                                                                 | height on /Text/Text+icon/Icon/None is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (bind):** As the icon's width. Ask SOLAR to bind Interactive Card /Text/Text+icon/Icon/None height to icon.sm, which has the same value.                                                                                                                                                                                                                                                                                                                                                       |
| `component.interactive card.icon.width#unbound`                                                                                  | 16, bound to no variable                                                                                                                                                 | width on /Text/Text+icon/Icon/None is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind):** Same value, governed token. Ask SOLAR to bind Interactive Card /Text/Text+icon/Icon/None width to icon.sm, which has the same value.                                                                                                                                                                                                                                                                                                                                                 |
| `component.interactive card.iconButton.height#unbound`                                                                           | 32, bound to no variable                                                                                                                                                 | height on /Actions/Icon Button is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (set):** As its width, the Icon Button's own. Ask SOLAR to bind Interactive Card /Actions/Icon Button height to icon.2xl, which has the same value.                                                                                                                                                                                                                                                                                                                                            |
| `component.interactive card.iconButton.width#unbound`                                                                            | 32, bound to no variable                                                                                                                                                 | width on /Actions/Icon Button is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (set):** The actions are the caller's Icon Buttons, sm, whose 32px box is their own: the card places them and never sizes them. Ask SOLAR to bind Interactive Card /Actions/Icon Button width to icon.2xl, which has the same value.                                                                                                                                                                                                                                                           |
| `component.interactive card.iconButton2.height#unbound`                                                                          | 32, bound to no variable                                                                                                                                                 | height on /Actions/Icon Button#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (set):** As its width, the Icon Button's own. Ask SOLAR to bind Interactive Card /Actions/Icon Button#2 height to icon.2xl, which has the same value.                                                                                                                                                                                                                                                                                                                                          |
| `component.interactive card.iconButton2.width#unbound`                                                                           | 32, bound to no variable                                                                                                                                                 | width on /Actions/Icon Button#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (set):** The actions are the caller's Icon Buttons, sm, whose 32px box is their own: the card places them and never sizes them. Ask SOLAR to bind Interactive Card /Actions/Icon Button#2 width to icon.2xl, which has the same value.                                                                                                                                                                                                                                                         |
| `component.interactive card.iconButton3.height#unbound`                                                                          | 32, bound to no variable                                                                                                                                                 | height on /Actions/Icon Button#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (set):** As its width, the Icon Button's own. Ask SOLAR to bind Interactive Card /Actions/Icon Button#3 height to icon.2xl, which has the same value.                                                                                                                                                                                                                                                                                                                                          |
| `component.interactive card.iconButton3.width#unbound`                                                                           | 32, bound to no variable                                                                                                                                                 | width on /Actions/Icon Button#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (set):** The actions are the caller's Icon Buttons, sm, whose 32px box is their own: the card places them and never sizes them. Ask SOLAR to bind Interactive Card /Actions/Icon Button#3 width to icon.2xl, which has the same value.                                                                                                                                                                                                                                                         |
| `component.interactive card.radioButton.height#unbound`                                                                          | 18, bound to no variable                                                                                                                                                 | height on /Radio is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (set):** As the Toggle's, the Radio's own size. SOLAR has no control height token; ask SOLAR for one, or confirm 18 is intended.                                                                                                                                                                                                                                                                                                                                                               |
| `component.interactive card.radioButton.width#unbound`                                                                           | 18, bound to no variable                                                                                                                                                 | width on /Radio is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (set):** As the Toggle's, the Radio's own size. SOLAR has no control width token; ask SOLAR for one, or confirm 18 is intended.                                                                                                                                                                                                                                                                                                                                                                |
| `component.interactive card.root.width#unbound`                                                                                  | 423, bound to no variable                                                                                                                                                | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** A card spans the list it is in: Figma draws it 423 wide, a sample. The caller sizes it. SOLAR has no control width token; ask SOLAR for one, or confirm 423 is intended.                                                                                                                                                                                                                                                                                                              |
| `component.interactive card.text.paddingBottom#unbound`                                                                          | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Text is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Interactive Card /Text paddingBottom to inset.none, which has the same value.                                        |
| `component.interactive card.text.paddingLeft#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Text is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Interactive Card /Text paddingLeft to inset.none, which has the same value.                                          |
| `component.interactive card.text.paddingRight#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingRight on /Text is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Interactive Card /Text paddingRight to inset.none, which has the same value.                                         |
| `component.interactive card.text.paddingTop#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingTop on /Text is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Interactive Card /Text paddingTop to inset.none, which has the same value.                                           |
| `component.interactive card.textIcon.paddingBottom#unbound`                                                                      | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Text/Text+icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Interactive Card /Text/Text+icon paddingBottom to inset.none, which has the same value.                              |
| `component.interactive card.textIcon.paddingLeft#unbound`                                                                        | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Text/Text+icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Interactive Card /Text/Text+icon paddingLeft to inset.none, which has the same value.                                |
| `component.interactive card.textIcon.paddingRight#unbound`                                                                       | 0, bound to no variable                                                                                                                                                  | paddingRight on /Text/Text+icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Interactive Card /Text/Text+icon paddingRight to inset.none, which has the same value.                               |
| `component.interactive card.textIcon.paddingTop#unbound`                                                                         | 0, bound to no variable                                                                                                                                                  | paddingTop on /Text/Text+icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Interactive Card /Text/Text+icon paddingTop to inset.none, which has the same value.                                 |
| `component.interactive card.toggle.height#unbound`                                                                               | 18, bound to no variable                                                                                                                                                 | height on /Toggle is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (set):** As the Toggle's width, its own. SOLAR has no control height token; ask SOLAR for one, or confirm 18 is intended.                                                                                                                                                                                                                                                                                                                                                                      |
| `component.interactive card.toggle.width#unbound`                                                                                | 32, bound to no variable                                                                                                                                                 | width on /Toggle is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (set):** The controls are SOLAR's own, sized by their own recipes (the Toggle 32 × 18, the Radio 18, the Checkbox 16): the card places them, and never sizes them. Ask SOLAR to bind Interactive Card /Toggle width to icon.2xl, which has the same value.                                                                                                                                                                                                                                     |
| `component.launch card full screen.action.height#unbound`                                                                        | 40, bound to no variable                                                                                                                                                 | height on /Text/Button is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (set):** The action is the caller's Button, md, whose height is its own: the page places it, and never sizes it. SOLAR has no control height token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                                                                              |
| `component.launch card full screen.appIcon.height#unbound`                                                                       | 40, bound to no variable                                                                                                                                                 | height on /Text/Content/Headline/App Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (allowLiteral):** As its width, a square. SOLAR has no control height token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                                                                                                                                                     |
| `component.launch card full screen.appIcon.width#unbound`                                                                        | 40, bound to no variable                                                                                                                                                 | width on /Text/Content/Headline/App Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (allowLiteral):** The App Icon is 40px, bound to nothing: an app icon is an image of the assets, not a SOLAR icon of the ladder's. Carried as Figma's number. SOLAR has no control width token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                                  |
| `component.launch card full screen.bodyCopy.paddingBottom#unbound`                                                               | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Text/Content/Body copy is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Launch Card Full Screen /Text/Content/Body copy paddingBottom to inset.none, which has the same value.               |
| `component.launch card full screen.bodyCopy.paddingLeft#unbound`                                                                 | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Text/Content/Body copy is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Launch Card Full Screen /Text/Content/Body copy paddingLeft to inset.none, which has the same value.                 |
| `component.launch card full screen.bodyCopy.paddingRight#unbound`                                                                | 0, bound to no variable                                                                                                                                                  | paddingRight on /Text/Content/Body copy is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Launch Card Full Screen /Text/Content/Body copy paddingRight to inset.none, which has the same value.                |
| `component.launch card full screen.bodyCopy.paddingTop#unbound`                                                                  | 0, bound to no variable                                                                                                                                                  | paddingTop on /Text/Content/Body copy is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Launch Card Full Screen /Text/Content/Body copy paddingTop to inset.none, which has the same value.                  |
| `component.launch card full screen.content.paddingBottom#unbound`                                                                | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Text/Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Launch Card Full Screen /Text/Content paddingBottom to inset.none, which has the same value.                         |
| `component.launch card full screen.content.paddingLeft#unbound`                                                                  | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Text/Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Launch Card Full Screen /Text/Content paddingLeft to inset.none, which has the same value.                           |
| `component.launch card full screen.content.paddingRight#unbound`                                                                 | 0, bound to no variable                                                                                                                                                  | paddingRight on /Text/Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Launch Card Full Screen /Text/Content paddingRight to inset.none, which has the same value.                          |
| `component.launch card full screen.content.paddingTop#unbound`                                                                   | 0, bound to no variable                                                                                                                                                  | paddingTop on /Text/Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Launch Card Full Screen /Text/Content paddingTop to inset.none, which has the same value.                            |
| `component.launch card full screen.favourite.height#unbound`                                                                     | 40, bound to no variable                                                                                                                                                 | height on /Text/Content/Headline/Favourite is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** As its width, the Icon Button's own. SOLAR has no control height token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                                                                                                                                                 |
| `component.launch card full screen.favourite.width#unbound`                                                                      | 40, bound to no variable                                                                                                                                                 | width on /Text/Content/Headline/Favourite is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (set):** The favourite is the caller's Icon Button, md, whose 40px box is its own: the page places it, and never sizes it. SOLAR has no control width token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                                                                     |
| `component.launch card full screen.headline.paddingBottom#unbound`                                                               | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Text/Content/Headline is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Launch Card Full Screen /Text/Content/Headline paddingBottom to inset.none, which has the same value.                |
| `component.launch card full screen.headline.paddingLeft#unbound`                                                                 | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Text/Content/Headline is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Launch Card Full Screen /Text/Content/Headline paddingLeft to inset.none, which has the same value.                  |
| `component.launch card full screen.headline.paddingRight#unbound`                                                                | 0, bound to no variable                                                                                                                                                  | paddingRight on /Text/Content/Headline is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Launch Card Full Screen /Text/Content/Headline paddingRight to inset.none, which has the same value.                 |
| `component.launch card full screen.headline.paddingTop#unbound`                                                                  | 0, bound to no variable                                                                                                                                                  | paddingTop on /Text/Content/Headline is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Launch Card Full Screen /Text/Content/Headline paddingTop to inset.none, which has the same value.                   |
| `component.launch card full screen.image.height#unbound`                                                                         | 420, bound to no variable                                                                                                                                                | height on /Image is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (allowLiteral):** As the image's width. SOLAR has no control height token; ask SOLAR for one, or confirm 420 is intended.                                                                                                                                                                                                                                                                                                                                                                      |
| `component.launch card full screen.image.width#unbound`                                                                          | 520, bound to no variable                                                                                                                                                | width on /Image is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (allowLiteral):** The image is 520 × 420, bound to nothing: SOLAR has no media size token. Carried as Figma's numbers (the owner kept its columns as drawn), and raised as a governance gap. SOLAR has no control width token; ask SOLAR for one, or confirm 520 is intended.                                                                                                                                                                                                                  |
| `component.launch card full screen.root.width#unbound`                                                                           | 979, bound to no variable                                                                                                                                                | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** The page spans the space it is shown in: Figma draws it 979 wide, a sample. The caller sizes it; its words' column takes what the image leaves. SOLAR has no control width token; ask SOLAR for one, or confirm 979 is intended.                                                                                                                                                                                                                                                      |
| `component.launch card full screen.text.height#unbound`                                                                          | 420, bound to no variable                                                                                                                                                | height on /Text is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (allowLiteral):** The words' column is as tall as the image (420), its action at the column's foot, as Figma draws it: carried as Figma's number, with the image's. SOLAR has no control height token; ask SOLAR for one, or confirm 420 is intended.                                                                                                                                                                                                                                          |
| `component.launch card full screen.text.paddingBottom#unbound`                                                                   | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Text is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Launch Card Full Screen /Text paddingBottom to inset.none, which has the same value.                                 |
| `component.launch card full screen.text.paddingLeft#unbound`                                                                     | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Text is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Launch Card Full Screen /Text paddingLeft to inset.none, which has the same value.                                   |
| `component.launch card full screen.text.paddingRight#unbound`                                                                    | 0, bound to no variable                                                                                                                                                  | paddingRight on /Text is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Launch Card Full Screen /Text paddingRight to inset.none, which has the same value.                                  |
| `component.launch card full screen.text.paddingTop#unbound`                                                                      | 0, bound to no variable                                                                                                                                                  | paddingTop on /Text is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Launch Card Full Screen /Text paddingTop to inset.none, which has the same value.                                    |
| `component.launch card.actions.width#unbound`                                                                                    | 310, bound to no variable                                                                                                                                                | width on /Content/Button Group is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (set):** The actions span the card's content: Figma fixes the group at 310 in a content 308 wide, a sample. The Button Group fills it. SOLAR has no control width token; ask SOLAR for one, or confirm 310 is intended.                                                                                                                                                                                                                                                                        |
| `component.launch card.appIcon.height#unbound`                                                                                   | 24, bound to no variable                                                                                                                                                 | height on /Content/Text/Headline/App Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (allowLiteral):** As its width, a square. Ask SOLAR to bind Launch Card /Content/Text/Headline/App Icon height to icon.lg, which has the same value.                                                                                                                                                                                                                                                                                                                                           |
| `component.launch card.appIcon.width#unbound`                                                                                    | 24, bound to no variable                                                                                                                                                 | width on /Content/Text/Headline/App Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (allowLiteral):** The App Icon is 24px, bound to nothing: an app icon is an image of the assets, not a SOLAR icon of the ladder's. Carried as Figma's number. Ask SOLAR to bind Launch Card /Content/Text/Headline/App Icon width to icon.lg, which has the same value.                                                                                                                                                                                                                        |
| `component.launch card.favourite.height#unbound`                                                                                 | 32, bound to no variable                                                                                                                                                 | height on /Image/Favourite is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** As its width, the Icon Button's own. Ask SOLAR to bind Launch Card /Image/Favourite height to icon.2xl, which has the same value.                                                                                                                                                                                                                                                                                                                                                     |
| `component.launch card.favourite.width#unbound`                                                                                  | 32, bound to no variable                                                                                                                                                 | width on /Image/Favourite is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (set):** The favourite is the caller's Icon Button (sm, round, tertiary), whose 32px box is its own: the card places it, and never sizes it. Ask SOLAR to bind Launch Card /Image/Favourite width to icon.2xl, which has the same value.                                                                                                                                                                                                                                                       |
| `component.launch card.favouriteNoImage.height#unbound`                                                                          | 32, bound to no variable                                                                                                                                                 | height on /Content/Text/Headline/FavouriteNoImage is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (set):** As the favourite on the image, the Icon Button's own. Ask SOLAR to bind Launch Card /Content/Text/Headline/FavouriteNoImage height to icon.2xl, which has the same value.                                                                                                                                                                                                                                                                                                             |
| `component.launch card.favouriteNoImage.width#unbound`                                                                           | 32, bound to no variable                                                                                                                                                 | width on /Content/Text/Headline/FavouriteNoImage is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (set):** As the favourite on the image, the Icon Button's own. Ask SOLAR to bind Launch Card /Content/Text/Headline/FavouriteNoImage width to icon.2xl, which has the same value.                                                                                                                                                                                                                                                                                                              |
| `component.launch card.headline.height#unbound`                                                                                  | 24, bound to no variable                                                                                                                                                 | height on /Content/Text/Headline is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (set):** The headline is as tall as what it holds: Figma fixes it at 24, its App Icon's and Tag's height, where the favourite it holds without an image is 32. It hugs them, drawing Figma's 24 for an icon and a Tag. SOLAR has no control height token; ask SOLAR for one, or confirm 24 is intended.                                                                                                                                                                                        |
| `component.launch card.headline.paddingBottom#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Content/Text/Headline is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Launch Card /Content/Text/Headline paddingBottom to inset.none, which has the same value.                            |
| `component.launch card.headline.paddingLeft#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Content/Text/Headline is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Launch Card /Content/Text/Headline paddingLeft to inset.none, which has the same value.                              |
| `component.launch card.headline.paddingRight#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingRight on /Content/Text/Headline is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Launch Card /Content/Text/Headline paddingRight to inset.none, which has the same value.                             |
| `component.launch card.headline.paddingTop#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingTop on /Content/Text/Headline is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Launch Card /Content/Text/Headline paddingTop to inset.none, which has the same value.                               |
| `component.launch card.image.height#unbound`                                                                                     | 160, bound to no variable                                                                                                                                                | height on /Image is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (allowLiteral):** The image is 160 tall, bound to nothing: SOLAR has no media size token. Carried as Figma's number, and raised as a governance gap. SOLAR has no control height token; ask SOLAR for one, or confirm 160 is intended.                                                                                                                                                                                                                                                         |
| `component.launch card.image.paddingBottom#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Image is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Launch Card /Image paddingBottom to inset.none, which has the same value.                                            |
| `component.launch card.image.paddingLeft#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Image is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Launch Card /Image paddingLeft to inset.none, which has the same value.                                              |
| `component.launch card.image.paddingRight#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingRight on /Image is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Launch Card /Image paddingRight to inset.none, which has the same value.                                             |
| `component.launch card.image.paddingTop#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingTop on /Image is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Launch Card /Image paddingTop to inset.none, which has the same value.                                               |
| `component.launch card.root.gap#unbound`                                                                                         | 0, bound to no variable                                                                                                                                                  | gap on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Launch Card / gap to inset.none or stack.none, which has the same value.                               |
| `component.launch card.root.paddingBottom#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Launch Card / paddingBottom to inset.none, which has the same value.                                                 |
| `component.launch card.root.paddingLeft#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Launch Card / paddingLeft to inset.none, which has the same value.                                                   |
| `component.launch card.root.paddingRight#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Launch Card / paddingRight to inset.none, which has the same value.                                                  |
| `component.launch card.root.paddingTop#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Launch Card / paddingTop to inset.none, which has the same value.                                                    |
| `component.launch card.root.width#unbound`                                                                                       | 340, bound to no variable                                                                                                                                                | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** A card spans the space it is put in: Figma draws it 340 wide, a sample. The caller sizes it. SOLAR has no control width token; ask SOLAR for one, or confirm 340 is intended.                                                                                                                                                                                                                                                                                                         |
| `component.launch card.tag.height#unbound`                                                                                       | 24, bound to no variable                                                                                                                                                 | height on /Content/Text/Headline/Tag is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (set):** The Tag is a SOLAR Tag, whose 24px height is its own: the card places it, and never sizes it. Ask SOLAR to bind Launch Card /Content/Text/Headline/Tag height to icon.lg, which has the same value.                                                                                                                                                                                                                                                                                   |
| `component.launch card.text.paddingBottom#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Content/Text is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Launch Card /Content/Text paddingBottom to inset.none, which has the same value.                                     |
| `component.launch card.text.paddingLeft#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Content/Text is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Launch Card /Content/Text paddingLeft to inset.none, which has the same value.                                       |
| `component.launch card.text.paddingRight#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingRight on /Content/Text is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Launch Card /Content/Text paddingRight to inset.none, which has the same value.                                      |
| `component.launch card.text.paddingTop#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingTop on /Content/Text is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Launch Card /Content/Text paddingTop to inset.none, which has the same value.                                        |
| `component.line chart.frame.align@series=multi, breakpoint=desktop`                                                              | 1 variant at series=multi, breakpoint=desktop: MIN/CENTER where SPACE_BETWEEN/CENTER                                                                                     | align on /Frame should follow no axis only, but at series=multi, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its lines' paths, its plot's size and its month labels, and a legend of its own the code draws as SOLAR's Data Legend. The library lays out the caller's data; the theme takes the design, none of which is left open. Ask SOLAR whether Line Chart /Frame align is meant to change at series=multi, breakpoint=desktop.                                                                                                                              |
| `component.line chart.frame.align@series=multi, breakpoint=mobile`                                                               | 1 variant at series=multi, breakpoint=mobile: MIN/CENTER where SPACE_BETWEEN/CENTER                                                                                      | align on /Frame should follow no axis only, but at series=multi, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's plot is sample data: its lines' paths, its plot's size and its month labels, and a legend of its own the code draws as SOLAR's Data Legend. The library lays out the caller's data; the theme takes the design, none of which is left open. Ask SOLAR whether Line Chart /Frame align is meant to change at series=multi, breakpoint=mobile.                                                                                                                               |
| `component.line chart.frame.paddingBottom#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's plot is sample data: its lines' paths, its plot's size and its month labels, and a legend of its own the code draws as SOLAR's Data Legend. The library lays out the caller's data; the theme takes the design, none of which is left open. Ask SOLAR to bind Line Chart /Frame paddingBottom to inset.none, which has the same value.                                                                                                                                     |
| `component.line chart.frame.paddingLeft#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** Figma's plot is sample data: its lines' paths, its plot's size and its month labels, and a legend of its own the code draws as SOLAR's Data Legend. The library lays out the caller's data; the theme takes the design, none of which is left open. Ask SOLAR to bind Line Chart /Frame paddingLeft to inset.none, which has the same value.                                                                                                                                       |
| `component.line chart.frame.paddingRight#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingRight on /Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** Figma's plot is sample data: its lines' paths, its plot's size and its month labels, and a legend of its own the code draws as SOLAR's Data Legend. The library lays out the caller's data; the theme takes the design, none of which is left open. Ask SOLAR to bind Line Chart /Frame paddingRight to inset.none, which has the same value.                                                                                                                                      |
| `component.line chart.frame.paddingTop#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingTop on /Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma's plot is sample data: its lines' paths, its plot's size and its month labels, and a legend of its own the code draws as SOLAR's Data Legend. The library lays out the caller's data; the theme takes the design, none of which is left open. Ask SOLAR to bind Line Chart /Frame paddingTop to inset.none, which has the same value.                                                                                                                                        |
| `component.line chart.frame2.paddingBottom#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Frame#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** Figma's plot is sample data: its lines' paths, its plot's size and its month labels, and a legend of its own the code draws as SOLAR's Data Legend. The library lays out the caller's data; the theme takes the design, none of which is left open. Ask SOLAR to bind Line Chart /Frame#2 paddingBottom to inset.none, which has the same value.                                                                                                                                   |
| `component.line chart.frame2.paddingLeft#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Frame#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's plot is sample data: its lines' paths, its plot's size and its month labels, and a legend of its own the code draws as SOLAR's Data Legend. The library lays out the caller's data; the theme takes the design, none of which is left open. Ask SOLAR to bind Line Chart /Frame#2 paddingLeft to inset.none, which has the same value.                                                                                                                                     |
| `component.line chart.frame2.paddingRight#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingRight on /Frame#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** Figma's plot is sample data: its lines' paths, its plot's size and its month labels, and a legend of its own the code draws as SOLAR's Data Legend. The library lays out the caller's data; the theme takes the design, none of which is left open. Ask SOLAR to bind Line Chart /Frame#2 paddingRight to inset.none, which has the same value.                                                                                                                                    |
| `component.line chart.frame2.paddingTop#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingTop on /Frame#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** Figma's plot is sample data: its lines' paths, its plot's size and its month labels, and a legend of its own the code draws as SOLAR's Data Legend. The library lays out the caller's data; the theme takes the design, none of which is left open. Ask SOLAR to bind Line Chart /Frame#2 paddingTop to inset.none, which has the same value.                                                                                                                                      |
| `component.line chart.frame2Frame.height#unbound`                                                                                | 10, bound to no variable                                                                                                                                                 | height on /Frame/Frame#2/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (accept):** Figma's plot is sample data: its lines' paths, its plot's size and its month labels, and a legend of its own the code draws as SOLAR's Data Legend. The library lays out the caller's data; the theme takes the design, none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 10 is intended.                                                                                                                                               |
| `component.line chart.frame2Frame.radius#unbound`                                                                                | 2, bound to no variable                                                                                                                                                  | radius on /Frame/Frame#2/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (accept):** Figma's plot is sample data: its lines' paths, its plot's size and its month labels, and a legend of its own the code draws as SOLAR's Data Legend. The library lays out the caller's data; the theme takes the design, none of which is left open. SOLAR has no radius token; ask SOLAR for one, or confirm 2 is intended.                                                                                                                                                        |
| `component.line chart.frame2Frame.width#unbound`                                                                                 | 10, bound to no variable                                                                                                                                                 | width on /Frame/Frame#2/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (accept):** Figma's plot is sample data: its lines' paths, its plot's size and its month labels, and a legend of its own the code draws as SOLAR's Data Legend. The library lays out the caller's data; the theme takes the design, none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 10 is intended.                                                                                                                                                |
| `component.line chart.frameFrame.gap#unbound`                                                                                    | 6, bound to no variable                                                                                                                                                  | gap on /Frame/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma's plot is sample data: its lines' paths, its plot's size and its month labels, and a legend of its own the code draws as SOLAR's Data Legend. The library lays out the caller's data; the theme takes the design, none of which is left open. SOLAR has no gap token; ask SOLAR for one, or confirm 6 is intended.                                                                                                                                                           |
| `component.line chart.frameFrame.paddingBottom#unbound`                                                                          | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Frame/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (accept):** Figma's plot is sample data: its lines' paths, its plot's size and its month labels, and a legend of its own the code draws as SOLAR's Data Legend. The library lays out the caller's data; the theme takes the design, none of which is left open. Ask SOLAR to bind Line Chart /Frame/Frame paddingBottom to inset.none, which has the same value.                                                                                                                               |
| `component.line chart.frameFrame.paddingLeft#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Frame/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its lines' paths, its plot's size and its month labels, and a legend of its own the code draws as SOLAR's Data Legend. The library lays out the caller's data; the theme takes the design, none of which is left open. Ask SOLAR to bind Line Chart /Frame/Frame paddingLeft to inset.none, which has the same value.                                                                                                                                 |
| `component.line chart.frameFrame.paddingRight#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingRight on /Frame/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (accept):** Figma's plot is sample data: its lines' paths, its plot's size and its month labels, and a legend of its own the code draws as SOLAR's Data Legend. The library lays out the caller's data; the theme takes the design, none of which is left open. Ask SOLAR to bind Line Chart /Frame/Frame paddingRight to inset.none, which has the same value.                                                                                                                                |
| `component.line chart.frameFrame.paddingTop#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingTop on /Frame/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's plot is sample data: its lines' paths, its plot's size and its month labels, and a legend of its own the code draws as SOLAR's Data Legend. The library lays out the caller's data; the theme takes the design, none of which is left open. Ask SOLAR to bind Line Chart /Frame/Frame paddingTop to inset.none, which has the same value.                                                                                                                                  |
| `component.line chart.frameFrame2.gap#unbound`                                                                                   | 6, bound to no variable                                                                                                                                                  | gap on /Frame/Frame#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** Figma's plot is sample data: its lines' paths, its plot's size and its month labels, and a legend of its own the code draws as SOLAR's Data Legend. The library lays out the caller's data; the theme takes the design, none of which is left open. SOLAR has no gap token; ask SOLAR for one, or confirm 6 is intended.                                                                                                                                                           |
| `component.line chart.frameFrame2.paddingBottom#unbound`                                                                         | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Frame/Frame#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** Figma's plot is sample data: its lines' paths, its plot's size and its month labels, and a legend of its own the code draws as SOLAR's Data Legend. The library lays out the caller's data; the theme takes the design, none of which is left open. Ask SOLAR to bind Line Chart /Frame/Frame#2 paddingBottom to inset.none, which has the same value.                                                                                                                             |
| `component.line chart.frameFrame2.paddingLeft#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Frame/Frame#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (accept):** Figma's plot is sample data: its lines' paths, its plot's size and its month labels, and a legend of its own the code draws as SOLAR's Data Legend. The library lays out the caller's data; the theme takes the design, none of which is left open. Ask SOLAR to bind Line Chart /Frame/Frame#2 paddingLeft to inset.none, which has the same value.                                                                                                                               |
| `component.line chart.frameFrame2.paddingRight#unbound`                                                                          | 0, bound to no variable                                                                                                                                                  | paddingRight on /Frame/Frame#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (accept):** Figma's plot is sample data: its lines' paths, its plot's size and its month labels, and a legend of its own the code draws as SOLAR's Data Legend. The library lays out the caller's data; the theme takes the design, none of which is left open. Ask SOLAR to bind Line Chart /Frame/Frame#2 paddingRight to inset.none, which has the same value.                                                                                                                              |
| `component.line chart.frameFrame2.paddingTop#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingTop on /Frame/Frame#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (accept):** Figma's plot is sample data: its lines' paths, its plot's size and its month labels, and a legend of its own the code draws as SOLAR's Data Legend. The library lays out the caller's data; the theme takes the design, none of which is left open. Ask SOLAR to bind Line Chart /Frame/Frame#2 paddingTop to inset.none, which has the same value.                                                                                                                                |
| `component.line chart.frameFrameFrame.height#unbound`                                                                            | 10, bound to no variable                                                                                                                                                 | height on /Frame/Frame/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (accept):** Figma's plot is sample data: its lines' paths, its plot's size and its month labels, and a legend of its own the code draws as SOLAR's Data Legend. The library lays out the caller's data; the theme takes the design, none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 10 is intended.                                                                                                                                               |
| `component.line chart.frameFrameFrame.radius#unbound`                                                                            | 2, bound to no variable                                                                                                                                                  | radius on /Frame/Frame/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (accept):** Figma's plot is sample data: its lines' paths, its plot's size and its month labels, and a legend of its own the code draws as SOLAR's Data Legend. The library lays out the caller's data; the theme takes the design, none of which is left open. SOLAR has no radius token; ask SOLAR for one, or confirm 2 is intended.                                                                                                                                                        |
| `component.line chart.frameFrameFrame.width#unbound`                                                                             | 10, bound to no variable                                                                                                                                                 | width on /Frame/Frame/Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its lines' paths, its plot's size and its month labels, and a legend of its own the code draws as SOLAR's Data Legend. The library lays out the caller's data; the theme takes the design, none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 10 is intended.                                                                                                                                                |
| `component.line chart.lineA.borderWidth#unbound`                                                                                 | 2, bound to no variable                                                                                                                                                  | borderWidth on /Plot/Line A is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind):** Same value, governed token, the line's stroke, which the theme takes. Ask SOLAR to bind Line Chart /Plot/Line A borderWidth to border.strong, which has the same value.                                                                                                                                                                                                                                                                                                              |
| `component.line chart.lineB.borderWidth#unbound`                                                                                 | 2, bound to no variable                                                                                                                                                  | borderWidth on /Plot/Line B is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind):** Same value, governed token, the line's stroke, which the theme takes. Ask SOLAR to bind Line Chart /Plot/Line B borderWidth to border.strong, which has the same value.                                                                                                                                                                                                                                                                                                              |
| `component.line chart.plot.height#unbound`                                                                                       | 204, 164, 176, 136, bound to no variable                                                                                                                                 | height on /Plot is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** Figma's plot is sample data: its lines' paths, its plot's size and its month labels, and a legend of its own the code draws as SOLAR's Data Legend. The library lays out the caller's data; the theme takes the design, none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 204, 164, 176, 136 is intended.                                                                                                                               |
| `component.line chart.plot.height@series=multi, breakpoint=desktop`                                                              | 1 variant at series=multi, breakpoint=desktop: 176 where 204                                                                                                             | height on /Plot should follow no axis only, but at series=multi, breakpoint=desktop it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its lines' paths, its plot's size and its month labels, and a legend of its own the code draws as SOLAR's Data Legend. The library lays out the caller's data; the theme takes the design, none of which is left open. Ask SOLAR whether Line Chart /Plot height is meant to change at series=multi, breakpoint=desktop.                                                                                                                              |
| `component.line chart.plot.height@series=multi, breakpoint=mobile`                                                               | 1 variant at series=multi, breakpoint=mobile: 136 where 204                                                                                                              | height on /Plot should follow no axis only, but at series=multi, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's plot is sample data: its lines' paths, its plot's size and its month labels, and a legend of its own the code draws as SOLAR's Data Legend. The library lays out the caller's data; the theme takes the design, none of which is left open. Ask SOLAR whether Line Chart /Plot height is meant to change at series=multi, breakpoint=mobile.                                                                                                                               |
| `component.line chart.plot.height@series=single, breakpoint=mobile`                                                              | 1 variant at series=single, breakpoint=mobile: 164 where 204                                                                                                             | height on /Plot should follow no axis only, but at series=single, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its lines' paths, its plot's size and its month labels, and a legend of its own the code draws as SOLAR's Data Legend. The library lays out the caller's data; the theme takes the design, none of which is left open. Ask SOLAR whether Line Chart /Plot height is meant to change at series=single, breakpoint=mobile.                                                                                                                              |
| `component.line chart.root.height#unbound`                                                                                       | 240, 200, bound to no variable                                                                                                                                           | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** Figma's plot is sample data: its lines' paths, its plot's size and its month labels, and a legend of its own the code draws as SOLAR's Data Legend. The library lays out the caller's data; the theme takes the design, none of which is left open. SOLAR has no control height token; ask SOLAR for one, or confirm 240, 200 is intended.                                                                                                                                         |
| `component.line chart.root.height@series=multi, breakpoint=mobile`                                                               | 1 variant at series=multi, breakpoint=mobile: 200 where 240                                                                                                              | height on / should follow no axis only, but at series=multi, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** Figma's plot is sample data: its lines' paths, its plot's size and its month labels, and a legend of its own the code draws as SOLAR's Data Legend. The library lays out the caller's data; the theme takes the design, none of which is left open. Ask SOLAR whether Line Chart / height is meant to change at series=multi, breakpoint=mobile.                                                                                                                                   |
| `component.line chart.root.height@series=single, breakpoint=mobile`                                                              | 1 variant at series=single, breakpoint=mobile: 200 where 240                                                                                                             | height on / should follow no axis only, but at series=single, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma's plot is sample data: its lines' paths, its plot's size and its month labels, and a legend of its own the code draws as SOLAR's Data Legend. The library lays out the caller's data; the theme takes the design, none of which is left open. Ask SOLAR whether Line Chart / height is meant to change at series=single, breakpoint=mobile.                                                                                                                                  |
| `component.line chart.root.paddingBottom#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma's plot is sample data: its lines' paths, its plot's size and its month labels, and a legend of its own the code draws as SOLAR's Data Legend. The library lays out the caller's data; the theme takes the design, none of which is left open. Ask SOLAR to bind Line Chart / paddingBottom to inset.none, which has the same value.                                                                                                                                          |
| `component.line chart.root.paddingLeft#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (accept):** Figma's plot is sample data: its lines' paths, its plot's size and its month labels, and a legend of its own the code draws as SOLAR's Data Legend. The library lays out the caller's data; the theme takes the design, none of which is left open. Ask SOLAR to bind Line Chart / paddingLeft to inset.none, which has the same value.                                                                                                                                            |
| `component.line chart.root.paddingRight#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Figma's plot is sample data: its lines' paths, its plot's size and its month labels, and a legend of its own the code draws as SOLAR's Data Legend. The library lays out the caller's data; the theme takes the design, none of which is left open. Ask SOLAR to bind Line Chart / paddingRight to inset.none, which has the same value.                                                                                                                                           |
| `component.line chart.root.paddingTop#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** Figma's plot is sample data: its lines' paths, its plot's size and its month labels, and a legend of its own the code draws as SOLAR's Data Legend. The library lays out the caller's data; the theme takes the design, none of which is left open. Ask SOLAR to bind Line Chart / paddingTop to inset.none, which has the same value.                                                                                                                                             |
| `component.line chart.root.width#unbound`                                                                                        | 600, 343, bound to no variable                                                                                                                                           | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** Figma's plot is sample data: its lines' paths, its plot's size and its month labels, and a legend of its own the code draws as SOLAR's Data Legend. The library lays out the caller's data; the theme takes the design, none of which is left open. SOLAR has no control width token; ask SOLAR for one, or confirm 600, 343 is intended.                                                                                                                                          |
| `component.line chart.root.width@series=multi, breakpoint=mobile`                                                                | 1 variant at series=multi, breakpoint=mobile: 343 where 600                                                                                                              | width on / should follow no axis only, but at series=multi, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** Figma's plot is sample data: its lines' paths, its plot's size and its month labels, and a legend of its own the code draws as SOLAR's Data Legend. The library lays out the caller's data; the theme takes the design, none of which is left open. Ask SOLAR whether Line Chart / width is meant to change at series=multi, breakpoint=mobile.                                                                                                                                    |
| `component.line chart.root.width@series=single, breakpoint=mobile`                                                               | 1 variant at series=single, breakpoint=mobile: 343 where 600                                                                                                             | width on / should follow no axis only, but at series=single, breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** Figma's plot is sample data: its lines' paths, its plot's size and its month labels, and a legend of its own the code draws as SOLAR's Data Legend. The library lays out the caller's data; the theme takes the design, none of which is left open. Ask SOLAR whether Line Chart / width is meant to change at series=single, breakpoint=mobile.                                                                                                                                   |
| `component.link.leadingIcon.height#unbound`                                                                                      | 24, 16, 12, bound to no variable                                                                                                                                         | height on /Icon/None is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind):** Same values, governed tokens, a step of the icon ladder per size; the icon's width is already bound to them ("icon size tracks text size", says the description). Ask SOLAR to bind Link /Icon/None height to icon.lg or icon.sm or icon.xs, which has the same value.                                                                                                                                                                                                               |
| `component.link.root.opacity#unbound`                                                                                            | 0.5, bound to no variable                                                                                                                                                | opacity on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (allowLiteral):** A disabled link is drawn at 50% opacity, over its disabled colours. SOLAR has no opacity scale, as for Node End's halo (SOLAR's decision, 2026-09-25); carried as Figma's number. SOLAR has no opacity token; ask SOLAR for one, or confirm 0.5 is intended.                                                                                                                                                                                                                 |
| `component.link.root.paddingBottom#unbound`                                                                                      | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Link / paddingBottom to inset.none, which has the same value.                                                        |
| `component.link.root.paddingLeft#unbound`                                                                                        | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Link / paddingLeft to inset.none, which has the same value.                                                          |
| `component.link.root.paddingRight#unbound`                                                                                       | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Link / paddingRight to inset.none, which has the same value.                                                         |
| `component.link.root.paddingTop#unbound`                                                                                         | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Link / paddingTop to inset.none, which has the same value.                                                           |
| `component.link.trailingIcon.height#unbound`                                                                                     | 24, 16, 12, bound to no variable                                                                                                                                         | height on /Icon/None#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind):** As the leading icon's. Ask SOLAR to bind Link /Icon/None#2 height to icon.lg or icon.sm or icon.xs, which has the same value.                                                                                                                                                                                                                                                                                                                                                        |
| `component.list.divider.height#unbound`                                                                                          | 1, bound to no variable                                                                                                                                                  | height on /Items/Divider is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (set):** The divider is a SOLAR Divider, whose 1px rule is its own: the list places it, and never sizes it. SOLAR has no control height token; ask SOLAR for one, or confirm 1 is intended.                                                                                                                                                                                                                                                                                                    |
| `component.list.divider.height@in-card=false`                                                                                    | 1 variant at in-card=false: HUG where 1                                                                                                                                  | height on /Items/Divider should follow no axis only, but at in-card=false it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** Both are the Divider's own 1px rule: Figma fixes it at 1 in the in-card list and hugs it in the other, and the code leaves it the Divider's in both (set above). Ask SOLAR whether List /Items/Divider height is meant to change at in-card=false.                                                                                                                                                                                                                                 |
| `component.list.divider2.height#unbound`                                                                                         | 1, bound to no variable                                                                                                                                                  | height on /Items/Divider#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** The divider is a SOLAR Divider, whose 1px rule is its own: the list places it, and never sizes it. SOLAR has no control height token; ask SOLAR for one, or confirm 1 is intended.                                                                                                                                                                                                                                                                                                    |
| `component.list.divider2.height@in-card=false`                                                                                   | 1 variant at in-card=false: HUG where 1                                                                                                                                  | height on /Items/Divider#2 should follow no axis only, but at in-card=false it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** As the first divider's. Ask SOLAR whether List /Items/Divider#2 height is meant to change at in-card=false.                                                                                                                                                                                                                                                                                                                                                                        |
| `component.list.divider3.height#unbound`                                                                                         | 1, bound to no variable                                                                                                                                                  | height on /Items/Divider#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** The divider is a SOLAR Divider, whose 1px rule is its own: the list places it, and never sizes it. SOLAR has no control height token; ask SOLAR for one, or confirm 1 is intended.                                                                                                                                                                                                                                                                                                    |
| `component.list.divider3.height@in-card=false`                                                                                   | 1 variant at in-card=false: HUG where 1                                                                                                                                  | height on /Items/Divider#3 should follow no axis only, but at in-card=false it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** As the first divider's. Ask SOLAR whether List /Items/Divider#3 height is meant to change at in-card=false.                                                                                                                                                                                                                                                                                                                                                                        |
| `component.list.divider4.height#unbound`                                                                                         | 1, bound to no variable                                                                                                                                                  | height on /Items/Divider#4 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** The divider is a SOLAR Divider, whose 1px rule is its own: the list places it, and never sizes it. SOLAR has no control height token; ask SOLAR for one, or confirm 1 is intended.                                                                                                                                                                                                                                                                                                    |
| `component.list.divider4.height@in-card=false`                                                                                   | 1 variant at in-card=false: HUG where 1                                                                                                                                  | height on /Items/Divider#4 should follow no axis only, but at in-card=false it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** As the first divider's. Ask SOLAR whether List /Items/Divider#4 height is meant to change at in-card=false.                                                                                                                                                                                                                                                                                                                                                                        |
| `component.list.items.gap#unbound`                                                                                               | 0, bound to no variable                                                                                                                                                  | gap on /Items is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind List /Items gap to inset.none or stack.none, which has the same value.                                 |
| `component.list.items.paddingBottom#unbound`                                                                                     | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Items is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind List /Items paddingBottom to inset.none, which has the same value.                                                   |
| `component.list.items.paddingLeft#unbound`                                                                                       | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Items is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind List /Items paddingLeft to inset.none, which has the same value.                                                     |
| `component.list.items.paddingRight#unbound`                                                                                      | 0, bound to no variable                                                                                                                                                  | paddingRight on /Items is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind List /Items paddingRight to inset.none, which has the same value.                                                    |
| `component.list.items.paddingTop#unbound`                                                                                        | 0, bound to no variable                                                                                                                                                  | paddingTop on /Items is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind List /Items paddingTop to inset.none, which has the same value.                                                      |
| `component.list.items.width@in-card=false`                                                                                       | 1 variant at in-card=false: FILL where HUG                                                                                                                               | width on /Items should follow no axis only, but at in-card=false it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (set):** As the in-card list's. Ask SOLAR whether List /Items width is meant to change at in-card=false.                                                                                                                                                                                                                                                                                                                                                                                       |
| `component.list.listItem.width#unbound`                                                                                          | 387, bound to no variable                                                                                                                                                | width on /Items/ListItem is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (set):** A row spans the list: Figma draws it 387 wide, a sample. SOLAR has no control width token; ask SOLAR for one, or confirm 387 is intended.                                                                                                                                                                                                                                                                                                                                             |
| `component.list.listItem.width@in-card=false`                                                                                    | 1 variant at in-card=false: FILL where 387                                                                                                                               | width on /Items/ListItem should follow no axis only, but at in-card=false it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (set):** As the in-card list's rows. Ask SOLAR whether List /Items/ListItem width is meant to change at in-card=false.                                                                                                                                                                                                                                                                                                                                                                         |
| `component.list.listItem2.width#unbound`                                                                                         | 387, bound to no variable                                                                                                                                                | width on /Items/ListItem#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** A row spans the list: Figma draws it 387 wide, a sample. SOLAR has no control width token; ask SOLAR for one, or confirm 387 is intended.                                                                                                                                                                                                                                                                                                                                             |
| `component.list.listItem2.width@in-card=false`                                                                                   | 1 variant at in-card=false: FILL where 387                                                                                                                               | width on /Items/ListItem#2 should follow no axis only, but at in-card=false it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (set):** As the in-card list's rows. Ask SOLAR whether List /Items/ListItem#2 width is meant to change at in-card=false.                                                                                                                                                                                                                                                                                                                                                                       |
| `component.list.listItem3.width#unbound`                                                                                         | 387, bound to no variable                                                                                                                                                | width on /Items/ListItem#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** A row spans the list: Figma draws it 387 wide, a sample. SOLAR has no control width token; ask SOLAR for one, or confirm 387 is intended.                                                                                                                                                                                                                                                                                                                                             |
| `component.list.listItem3.width@in-card=false`                                                                                   | 1 variant at in-card=false: FILL where 387                                                                                                                               | width on /Items/ListItem#3 should follow no axis only, but at in-card=false it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (set):** As the in-card list's rows. Ask SOLAR whether List /Items/ListItem#3 width is meant to change at in-card=false.                                                                                                                                                                                                                                                                                                                                                                       |
| `component.list.listItem4.width#unbound`                                                                                         | 387, bound to no variable                                                                                                                                                | width on /Items/ListItem#4 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** A row spans the list: Figma draws it 387 wide, a sample. SOLAR has no control width token; ask SOLAR for one, or confirm 387 is intended.                                                                                                                                                                                                                                                                                                                                             |
| `component.list.listItem4.width@in-card=false`                                                                                   | 1 variant at in-card=false: FILL where 387                                                                                                                               | width on /Items/ListItem#4 should follow no axis only, but at in-card=false it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (set):** As the in-card list's rows. Ask SOLAR whether List /Items/ListItem#4 width is meant to change at in-card=false.                                                                                                                                                                                                                                                                                                                                                                       |
| `component.list.listItem5.width#unbound`                                                                                         | 387, bound to no variable                                                                                                                                                | width on /Items/ListItem#5 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** A row spans the list: Figma draws it 387 wide, a sample. SOLAR has no control width token; ask SOLAR for one, or confirm 387 is intended.                                                                                                                                                                                                                                                                                                                                             |
| `component.list.listItem5.width@in-card=false`                                                                                   | 1 variant at in-card=false: FILL where 387                                                                                                                               | width on /Items/ListItem#5 should follow no axis only, but at in-card=false it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (set):** As the in-card list's rows. Ask SOLAR whether List /Items/ListItem#5 width is meant to change at in-card=false.                                                                                                                                                                                                                                                                                                                                                                       |
| `component.list.root.gap#unbound`                                                                                                | 0, bound to no variable                                                                                                                                                  | gap on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind List / gap to inset.none or stack.none, which has the same value.                                      |
| `component.list.root.paddingBottom#unbound`                                                                                      | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind List / paddingBottom to inset.none, which has the same value.                                                        |
| `component.list.root.paddingLeft#unbound`                                                                                        | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind List / paddingLeft to inset.none, which has the same value.                                                          |
| `component.list.root.paddingRight#unbound`                                                                                       | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind List / paddingRight to inset.none, which has the same value.                                                         |
| `component.list.root.paddingTop#unbound`                                                                                         | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind List / paddingTop to inset.none, which has the same value.                                                           |
| `component.list.root.width#unbound`                                                                                              | 368, bound to no variable                                                                                                                                                | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** A list spans what holds it, as its rows do: Figma's in-card list hugs its 387-wide rows and its other one is 368 wide, samples. SOLAR has no control width token; ask SOLAR for one, or confirm 368 is intended.                                                                                                                                                                                                                                                                      |
| `component.list.root.width@in-card=false`                                                                                        | 1 variant at in-card=false: 368 where HUG                                                                                                                                | width on / should follow no axis only, but at in-card=false it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Both lists' widths are samples, one hugging its rows and the other 368 wide: the code's list fills what holds it in both (set above). Ask SOLAR whether List / width is meant to change at in-card=false.                                                                                                                                                                                                                                                                          |
| `component.listitem.avatar.height#unbound`                                                                                       | 32, bound to no variable                                                                                                                                                 | height on /Leading is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (set):** As its width, the Avatar's own. Ask SOLAR to bind ListItem /Leading height to icon.2xl, which has the same value.                                                                                                                                                                                                                                                                                                                                                                     |
| `component.listitem.avatar.width#unbound`                                                                                        | 32, bound to no variable                                                                                                                                                 | width on /Leading is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (set):** The avatar is the caller's SOLAR Avatar, md as Figma draws it, whose 32px is its own: the row places it, and never sizes it. Ask SOLAR to bind ListItem /Leading width to icon.2xl, which has the same value.                                                                                                                                                                                                                                                                         |
| `component.listitem.body.paddingBottom#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Body is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind ListItem /Body paddingBottom to inset.none, which has the same value.                                                |
| `component.listitem.body.paddingLeft#unbound`                                                                                    | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Body is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind ListItem /Body paddingLeft to inset.none, which has the same value.                                                  |
| `component.listitem.body.paddingRight#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingRight on /Body is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind ListItem /Body paddingRight to inset.none, which has the same value.                                                 |
| `component.listitem.body.paddingTop#unbound`                                                                                     | 0, bound to no variable                                                                                                                                                  | paddingTop on /Body is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind ListItem /Body paddingTop to inset.none, which has the same value.                                                   |
| `component.listitem.icon.width#unbound`                                                                                          | 20, bound to no variable                                                                                                                                                 | width on /Icon/None is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (bind):** Same value, governed token; the icon's height is already bound to it. Ask SOLAR to bind ListItem /Icon/None width to icon.md, which has the same value.                                                                                                                                                                                                                                                                                                                              |
| `component.listitem.trailing.width#unbound`                                                                                      | 16, bound to no variable                                                                                                                                                 | width on /Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (bind):** As the icon's, a step smaller; its height is already bound to it. Ask SOLAR to bind ListItem /Icon width to icon.sm, which has the same value.                                                                                                                                                                                                                                                                                                                                       |
| `component.nav item.iconOutline.height#unbound`                                                                                  | 20, bound to no variable                                                                                                                                                 | height on /Icon/Home is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind):** Same value, governed token; the icon's width is already bound to it. Ask SOLAR to bind Nav Item /Icon/Home height to icon.md, which has the same value.                                                                                                                                                                                                                                                                                                                              |
| `component.nav item.root.height#unbound`                                                                                         | 40, bound to no variable                                                                                                                                                 | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind):** As Button's height: size.control, one step per size, which SOLAR added on 2026-09-25; Figma leaves the height unbound, which the design review asks SOLAR to bind. SOLAR has no control height token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                  |
| `component.nav item.root.width#unbound`                                                                                          | 40, 182, bound to no variable                                                                                                                                            | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (allowLiteral):** Collapsed, the item is as wide as it is tall, a 40px square around its icon, as its description says ("40×40 collapsed (icon-only)"); expanded, it fills its sidebar. SOLAR has no control width token; ask SOLAR for one, or confirm 40, 182 is intended.                                                                                                                                                                                                                   |
| `component.node end.dot.height#unbound`                                                                                          | 6, bound to no variable                                                                                                                                                  | height on /Dot is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (allowLiteral):** As the width, part of the drawing. SOLAR has no control height token; ask SOLAR for one, or confirm 6 is intended.                                                                                                                                                                                                                                                                                                                                                           |
| `component.node end.dot.width#unbound`                                                                                           | 6, bound to no variable                                                                                                                                                  | width on /Dot is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (allowLiteral):** The dot's 6px ("a plain 6px Dot", says the description), part of the drawing. SOLAR has no control width token; ask SOLAR for one, or confirm 6 is intended.                                                                                                                                                                                                                                                                                                                 |
| `component.node end.halo.height#unbound`                                                                                         | 6, 14, bound to no variable                                                                                                                                              | height on /Halo is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (allowLiteral):** As the width, part of the drawing. SOLAR has no control height token; ask SOLAR for one, or confirm 6, 14 is intended.                                                                                                                                                                                                                                                                                                                                                       |
| `component.node end.halo.opacity#unbound`                                                                                        | 0.2, bound to no variable                                                                                                                                                | opacity on /Halo is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (allowLiteral):** The halo is the dot's colour at 20% opacity. SOLAR has alpha colours but no opacity scale, and no alpha form of surface/feedback/info/strong; carried as Figma's number. SOLAR decided on 2026-09-25 against a scale for its four one-off opacities (20%, 30%, 40%, 50%). SOLAR has no opacity token; ask SOLAR for one, or confirm 0.2 is intended.                                                                                                                         |
| `component.node end.halo.width#unbound`                                                                                          | 6, 14, bound to no variable                                                                                                                                              | width on /Halo is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (allowLiteral):** The halo's size, 6 and 14, part of the drawing. SOLAR has no control width token; ask SOLAR for one, or confirm 6, 14 is intended.                                                                                                                                                                                                                                                                                                                                           |
| `component.node end.root.height#unbound`                                                                                         | 12, bound to no variable                                                                                                                                                 | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (allowLiteral):** As the width, the marker's box. SOLAR has no control height token; ask SOLAR for one, or confirm 12 is intended.                                                                                                                                                                                                                                                                                                                                                             |
| `component.node end.root.width#unbound`                                                                                          | 12, bound to no variable                                                                                                                                                 | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (allowLiteral):** The marker's box, 12px, in the drawing's own coordinates: part of the drawing, as a glyph's box is, not a size of the system. SOLAR has no marker size. SOLAR has no control width token; ask SOLAR for one, or confirm 12 is intended.                                                                                                                                                                                                                                      |
| `component.number input.chevronDown.width#unbound`                                                                               | 16, 12, bound to no variable                                                                                                                                             | width on /Field/Stepper/Decrement/ChevronDown is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (bind):** As the up chevron's. Ask SOLAR to bind Number Input /Field/Stepper/Decrement/ChevronDown width to icon.sm or icon.xs, which has the same value.                                                                                                                                                                                                                                                                                                                                      |
| `component.number input.chevronUp.width#unbound`                                                                                 | 16, 12, bound to no variable                                                                                                                                             | width on /Field/Stepper/Increment/ChevronUp is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind):** As the minus's, a step smaller. Ask SOLAR to bind Number Input /Field/Stepper/Increment/ChevronUp width to icon.sm or icon.xs, which has the same value.                                                                                                                                                                                                                                                                                                                             |
| `component.number input.divider.height#unbound`                                                                                  | 1, bound to no variable                                                                                                                                                  | height on /Field/Stepper/Divider is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind):** The stepper's divider is a hairline, 1px, border.default's width, as Divider's rule is: the same value. SOLAR has no control height token; ask SOLAR for one, or confirm 1 is intended.                                                                                                                                                                                                                                                                                              |
| `component.number input.field.gap#unbound`                                                                                       | 0, bound to no variable                                                                                                                                                  | gap on /Field is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Number Input /Field gap to inset.none or stack.none, which has the same value.                         |
| `component.number input.field.height#unbound`                                                                                    | 40, 32, bound to no variable                                                                                                                                             | height on /Field is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind):** As Button's height: size.control, one step per size, which SOLAR added on 2026-09-25; Figma leaves the height unbound, which the design review asks SOLAR to bind. SOLAR has no control height token; ask SOLAR for one, or confirm 40, 32 is intended.                                                                                                                                                                                                                              |
| `component.number input.field.paddingRight#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingRight on /Field is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Number Input /Field paddingRight to inset.none, which has the same value.                                            |
| `component.number input.field.width#unbound`                                                                                     | 88, 68, bound to no variable                                                                                                                                             | width on /Field is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (allowLiteral):** Beside a side stepper, the field is 88 (md) and 68 (sm), bound to nothing: SOLAR has no control width token (the design review's question on control sizes). SOLAR has no control width token; ask SOLAR for one, or confirm 88, 68 is intended.                                                                                                                                                                                                                             |
| `component.number input.fieldDecrement.width#unbound`                                                                            | 20, 16, bound to no variable                                                                                                                                             | width on /Field/Decrement is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind):** Same values, governed tokens, a step of the icon ladder per size; the icon's height is already bound to them. Ask SOLAR to bind Number Input /Field/Decrement width to icon.md or icon.sm, which has the same value.                                                                                                                                                                                                                                                                 |
| `component.number input.fieldIncrement.width#unbound`                                                                            | 20, 16, bound to no variable                                                                                                                                             | width on /Field/Increment is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind):** As the minus's. Ask SOLAR to bind Number Input /Field/Increment width to icon.md or icon.sm, which has the same value.                                                                                                                                                                                                                                                                                                                                                               |
| `component.number input.label.paddingBottom#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Number Input /Label paddingBottom to inset.none, which has the same value.                                           |
| `component.number input.label.paddingLeft#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Number Input /Label paddingLeft to inset.none, which has the same value.                                             |
| `component.number input.label.paddingRight#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingRight on /Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Number Input /Label paddingRight to inset.none, which has the same value.                                            |
| `component.number input.label.paddingTop#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingTop on /Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Number Input /Label paddingTop to inset.none, which has the same value.                                              |
| `component.number input.leadingIcon.width#unbound`                                                                               | 16, 20, bound to no variable                                                                                                                                             | width on /Field/LeadingIcon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind):** As the minus's. Ask SOLAR to bind Number Input /Field/LeadingIcon width to icon.sm or icon.md, which has the same value.                                                                                                                                                                                                                                                                                                                                                             |
| `component.number input.root.paddingBottom#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Number Input / paddingBottom to inset.none, which has the same value.                                                |
| `component.number input.root.paddingLeft#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Number Input / paddingLeft to inset.none, which has the same value.                                                  |
| `component.number input.root.paddingRight#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Number Input / paddingRight to inset.none, which has the same value.                                                 |
| `component.number input.root.paddingTop#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Number Input / paddingTop to inset.none, which has the same value.                                                   |
| `component.number input.stepper.gap#unbound`                                                                                     | 0, bound to no variable                                                                                                                                                  | gap on /Field/Stepper is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Number Input /Field/Stepper gap to inset.none or stack.none, which has the same value.                 |
| `component.number input.stepper.paddingBottom#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Field/Stepper is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Number Input /Field/Stepper paddingBottom to inset.none, which has the same value.                                   |
| `component.number input.stepper.paddingLeft#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Field/Stepper is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Number Input /Field/Stepper paddingLeft to inset.none, which has the same value.                                     |
| `component.number input.stepper.paddingRight#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingRight on /Field/Stepper is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Number Input /Field/Stepper paddingRight to inset.none, which has the same value.                                    |
| `component.number input.stepper.paddingTop#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingTop on /Field/Stepper is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Number Input /Field/Stepper paddingTop to inset.none, which has the same value.                                      |
| `component.number input.stepper.width#unbound`                                                                                   | 28, 24, bound to no variable                                                                                                                                             | width on /Field/Stepper is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (allowLiteral):** The side stepper's column is 28 (md) and 24 (sm) wide, bound to nothing, as the field's width is. SOLAR has no control width token; ask SOLAR for one, or confirm 28, 24 is intended.                                                                                                                                                                                                                                                                                        |
| `component.number input.stepperDecrement.gap#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | gap on /Field/Stepper/Decrement is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Number Input /Field/Stepper/Decrement gap to inset.none or stack.none, which has the same value.       |
| `component.number input.stepperDecrement.paddingBottom#unbound`                                                                  | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Field/Stepper/Decrement is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Number Input /Field/Stepper/Decrement paddingBottom to inset.none, which has the same value.                         |
| `component.number input.stepperDecrement.paddingLeft#unbound`                                                                    | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Field/Stepper/Decrement is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Number Input /Field/Stepper/Decrement paddingLeft to inset.none, which has the same value.                           |
| `component.number input.stepperDecrement.paddingRight#unbound`                                                                   | 0, bound to no variable                                                                                                                                                  | paddingRight on /Field/Stepper/Decrement is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Number Input /Field/Stepper/Decrement paddingRight to inset.none, which has the same value.                          |
| `component.number input.stepperDecrement.paddingTop#unbound`                                                                     | 0, bound to no variable                                                                                                                                                  | paddingTop on /Field/Stepper/Decrement is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Number Input /Field/Stepper/Decrement paddingTop to inset.none, which has the same value.                            |
| `component.number input.stepperIncrement.gap#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | gap on /Field/Stepper/Increment is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Number Input /Field/Stepper/Increment gap to inset.none or stack.none, which has the same value.       |
| `component.number input.stepperIncrement.paddingBottom#unbound`                                                                  | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Field/Stepper/Increment is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Number Input /Field/Stepper/Increment paddingBottom to inset.none, which has the same value.                         |
| `component.number input.stepperIncrement.paddingLeft#unbound`                                                                    | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Field/Stepper/Increment is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Number Input /Field/Stepper/Increment paddingLeft to inset.none, which has the same value.                           |
| `component.number input.stepperIncrement.paddingRight#unbound`                                                                   | 0, bound to no variable                                                                                                                                                  | paddingRight on /Field/Stepper/Increment is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Number Input /Field/Stepper/Increment paddingRight to inset.none, which has the same value.                          |
| `component.number input.stepperIncrement.paddingTop#unbound`                                                                     | 0, bound to no variable                                                                                                                                                  | paddingTop on /Field/Stepper/Increment is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Number Input /Field/Stepper/Increment paddingTop to inset.none, which has the same value.                            |
| `component.option card.container.gap#unbound`                                                                                    | 0, bound to no variable                                                                                                                                                  | gap on /Container is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Option Card /Container gap to inset.none or stack.none, which has the same value.                      |
| `component.option card.container.height#unbound`                                                                                 | 64, bound to no variable                                                                                                                                                 | height on /Container is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (allowLiteral):** As the circle's width, a circle. SOLAR has no control height token; ask SOLAR for one, or confirm 64 is intended.                                                                                                                                                                                                                                                                                                                                                            |
| `component.option card.container.paddingBottom#unbound`                                                                          | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Container is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Option Card /Container paddingBottom to inset.none, which has the same value.                                        |
| `component.option card.container.paddingLeft#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Container is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Option Card /Container paddingLeft to inset.none, which has the same value.                                          |
| `component.option card.container.paddingRight#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingRight on /Container is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Option Card /Container paddingRight to inset.none, which has the same value.                                         |
| `component.option card.container.paddingTop#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingTop on /Container is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Option Card /Container paddingTop to inset.none, which has the same value.                                           |
| `component.option card.container.width#unbound`                                                                                  | 64, bound to no variable                                                                                                                                                 | width on /Container is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (allowLiteral):** The Plus sits in a 64px circle, bound to nothing: SOLAR has no size token for it (as for Insight Card's tile); carried as Figma's number, and raised as a governance gap. SOLAR has no control width token; ask SOLAR for one, or confirm 64 is intended.                                                                                                                                                                                                                    |
| `component.option card.iconPlus.height#unbound`                                                                                  | 24, bound to no variable                                                                                                                                                 | height on /Container/Icon/Plus is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (bind):** Same value, governed token; the Plus's width is already bound to it. Ask SOLAR to bind Option Card /Container/Icon/Plus height to icon.lg, which has the same value.                                                                                                                                                                                                                                                                                                                 |
| `component.option card.root.height#unbound`                                                                                      | 228, bound to no variable                                                                                                                                                | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (allowLiteral):** A tile is 228 tall, bound to nothing: SOLAR has no tile size token. Carried as Figma's number, and raised as a governance gap. SOLAR has no control height token; ask SOLAR for one, or confirm 228 is intended.                                                                                                                                                                                                                                                             |
| `component.option card.root.paddingBottom#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Option Card / paddingBottom to inset.none, which has the same value.                                                 |
| `component.option card.root.paddingLeft#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Option Card / paddingLeft to inset.none, which has the same value.                                                   |
| `component.option card.root.paddingRight#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Option Card / paddingRight to inset.none, which has the same value.                                                  |
| `component.option card.root.paddingTop#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Option Card / paddingTop to inset.none, which has the same value.                                                    |
| `component.option card.root.width#unbound`                                                                                       | 240, bound to no variable                                                                                                                                                | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** A tile spans its cell of a grid: Figma draws it 240 wide, a sample. The caller sizes it. SOLAR has no control width token; ask SOLAR for one, or confirm 240 is intended.                                                                                                                                                                                                                                                                                                             |
| `component.option row.control.height#unbound`                                                                                    | 16, bound to no variable                                                                                                                                                 | height on /Control is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (set):** As its width, the control's own. Ask SOLAR to bind Option Row /Control height to icon.sm, which has the same value.                                                                                                                                                                                                                                                                                                                                                                   |
| `component.option row.control.width#unbound`                                                                                     | 16, bound to no variable                                                                                                                                                 | width on /Control is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (set):** The control is a SOLAR Checkbox or Radio, whose 16px box is its own: the row places it, and never sizes it. Ask SOLAR to bind Option Row /Control width to icon.sm, which has the same value.                                                                                                                                                                                                                                                                                         |
| `component.option row.root.paddingLeft#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Option Row / paddingLeft to inset.none, which has the same value.                                                    |
| `component.option row.root.paddingRight#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Option Row / paddingRight to inset.none, which has the same value.                                                   |
| `component.option row.root.width#unbound`                                                                                        | 320, bound to no variable                                                                                                                                                | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** A row is the width it is given, so long words wrap rather than stretch it, as the description says ("row width is fixed"): Figma draws it 320 wide, a sample of a form's column. SOLAR has no control width token; ask SOLAR for one, or confirm 320 is intended.                                                                                                                                                                                                                     |
| `component.option row.text.paddingBottom#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Text is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Option Row /Text paddingBottom to inset.none, which has the same value.                                              |
| `component.option row.text.paddingLeft#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Text is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Option Row /Text paddingLeft to inset.none, which has the same value.                                                |
| `component.option row.text.paddingRight#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingRight on /Text is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Option Row /Text paddingRight to inset.none, which has the same value.                                               |
| `component.option row.text.paddingTop#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingTop on /Text is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Option Row /Text paddingTop to inset.none, which has the same value.                                                 |
| `component.option row.toggle.height#unbound`                                                                                     | 18, bound to no variable                                                                                                                                                 | height on /Toggle is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (set):** As its width, the Toggle's own. SOLAR has no control height token; ask SOLAR for one, or confirm 18 is intended.                                                                                                                                                                                                                                                                                                                                                                      |
| `component.option row.toggle.width#unbound`                                                                                      | 32, bound to no variable                                                                                                                                                 | width on /Toggle is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (set):** The Toggle's 32 × 18 track is its own, as the Checkbox's box is. Ask SOLAR to bind Option Row /Toggle width to icon.2xl, which has the same value.                                                                                                                                                                                                                                                                                                                                    |
| `component.options list.content.gap#unbound`                                                                                     | 0, bound to no variable                                                                                                                                                  | gap on /Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Options List /Content gap to inset.none or stack.none, which has the same value.                       |
| `component.options list.content.paddingBottom#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Options List /Content paddingBottom to inset.none, which has the same value.                                         |
| `component.options list.content.paddingLeft#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Options List /Content paddingLeft to inset.none, which has the same value.                                           |
| `component.options list.content.paddingRight#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingRight on /Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Options List /Content paddingRight to inset.none, which has the same value.                                          |
| `component.options list.content.paddingTop#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingTop on /Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Options List /Content paddingTop to inset.none, which has the same value.                                            |
| `component.options list.root.gap#unbound`                                                                                        | 0, bound to no variable                                                                                                                                                  | gap on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Options List / gap to inset.none or stack.none, which has the same value.                              |
| `component.options list.root.paddingBottom#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Options List / paddingBottom to inset.none, which has the same value.                                                |
| `component.options list.root.paddingLeft#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Options List / paddingLeft to inset.none, which has the same value.                                                  |
| `component.options list.root.paddingRight#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Options List / paddingRight to inset.none, which has the same value.                                                 |
| `component.options list.root.paddingTop#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Options List / paddingTop to inset.none, which has the same value.                                                   |
| `component.options list.root.width#unbound`                                                                                      | 320, bound to no variable                                                                                                                                                | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** A list spans its form's column, as its rows do: Figma draws it 320 wide, a sample. SOLAR has no control width token; ask SOLAR for one, or confirm 320 is intended.                                                                                                                                                                                                                                                                                                                   |
| `component.pagenavbutton.iconArrowLeft.height#unbound`                                                                           | 20, bound to no variable                                                                                                                                                 | height on /Icon/ArrowLeft is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind):** Same value, governed token; the icon's width is already bound to it. Ask SOLAR to bind PageNavButton /Icon/ArrowLeft height to icon.md, which has the same value.                                                                                                                                                                                                                                                                                                                    |
| `component.pagenavbutton.iconArrowRight.height#unbound`                                                                          | 20, bound to no variable                                                                                                                                                 | height on /Icon/ArrowRight is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (bind):** As the left arrow's. Ask SOLAR to bind PageNavButton /Icon/ArrowRight height to icon.md, which has the same value.                                                                                                                                                                                                                                                                                                                                                                   |
| `component.pagenavbutton.root.gap@direction=next, state=default`                                                                 | 1 variant at direction=next, state=default: inset.xs where stack.xs                                                                                                      | gap on / should follow no axis only, but at direction=next, state=default it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** As the gap's unbound finding, the same 8 as stack.xs. Ask SOLAR whether PageNavButton / gap is meant to change at direction=next, state=default.                                                                                                                                                                                                                                                                                                                                   |
| `component.pagenavbutton.root.gap@direction=next, state=disabled`                                                                | 1 variant at direction=next, state=disabled: inset.xs where stack.xs                                                                                                     | gap on / should follow no axis only, but at direction=next, state=disabled it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** As the gap's unbound finding, the same 8 as stack.xs. Ask SOLAR whether PageNavButton / gap is meant to change at direction=next, state=disabled.                                                                                                                                                                                                                                                                                                                                  |
| `component.pagenavbutton.root.gap@direction=next, state=focus`                                                                   | 1 variant at direction=next, state=focus: inset.xs where stack.xs                                                                                                        | gap on / should follow no axis only, but at direction=next, state=focus it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** As the gap's unbound finding, the same 8 as stack.xs. Ask SOLAR whether PageNavButton / gap is meant to change at direction=next, state=focus.                                                                                                                                                                                                                                                                                                                                     |
| `component.pagenavbutton.root.gap@direction=next, state=hover`                                                                   | 1 variant at direction=next, state=hover: inset.xs where stack.xs                                                                                                        | gap on / should follow no axis only, but at direction=next, state=hover it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** As the gap's unbound finding, the same 8 as stack.xs. Ask SOLAR whether PageNavButton / gap is meant to change at direction=next, state=hover.                                                                                                                                                                                                                                                                                                                                     |
| `component.pagenavbutton.root.gap@direction=next, state=pressed`                                                                 | 1 variant at direction=next, state=pressed: inset.xs where stack.xs                                                                                                      | gap on / should follow no axis only, but at direction=next, state=pressed it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** As the gap's unbound finding, the same 8 as stack.xs. Ask SOLAR whether PageNavButton / gap is meant to change at direction=next, state=pressed.                                                                                                                                                                                                                                                                                                                                   |
| `component.pagenavbutton.root.height#unbound`                                                                                    | 36, bound to no variable                                                                                                                                                 | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (allowLiteral):** The button is drawn 36 tall, bound to nothing, between two of SOLAR's control heights (size.control: 32, 40); carried as drawn, the design review asks. SOLAR has no control height token; ask SOLAR for one, or confirm 36 is intended.                                                                                                                                                                                                                                     |
| `component.pagenavbutton.root.paddingBottom#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind PageNavButton / paddingBottom to inset.none, which has the same value.                                               |
| `component.pagenavbutton.root.paddingTop#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind PageNavButton / paddingTop to inset.none, which has the same value.                                                  |
| `component.pagenavbutton.root.width#unbound`                                                                                     | 112, bound to no variable                                                                                                                                                | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** A button is as wide as its words and arrow, as SOLAR's Button is, so a longer word (a translation) fits: Figma draws it 112 wide, bound to nothing (the design review asks). SOLAR has no control width token; ask SOLAR for one, or confirm 112 is intended.                                                                                                                                                                                                                         |
| `component.pagenavigator.nextButton.height#unbound`                                                                              | 36, bound to no variable                                                                                                                                                 | height on /NextButton is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (set):** As the previous button's, its own. SOLAR has no control height token; ask SOLAR for one, or confirm 36 is intended.                                                                                                                                                                                                                                                                                                                                                                   |
| `component.pagenavigator.nextButton.width#unbound`                                                                               | 112, bound to no variable                                                                                                                                                | width on /NextButton is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (set):** As the previous button's, its own. SOLAR has no control width token; ask SOLAR for one, or confirm 112 is intended.                                                                                                                                                                                                                                                                                                                                                                   |
| `component.pagenavigator.prevButton.height#unbound`                                                                              | 36, bound to no variable                                                                                                                                                 | height on /PrevButton is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (set):** Each button is a SOLAR PageNavButton, whose size is its own: the navigator places it, and never sizes it. SOLAR has no control height token; ask SOLAR for one, or confirm 36 is intended.                                                                                                                                                                                                                                                                                            |
| `component.pagenavigator.prevButton.width#unbound`                                                                               | 112, bound to no variable                                                                                                                                                | width on /PrevButton is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (set):** Each button is a SOLAR PageNavButton, whose size is its own: the navigator places it, and never sizes it. SOLAR has no control width token; ask SOLAR for one, or confirm 112 is intended.                                                                                                                                                                                                                                                                                            |
| `component.pagenavigator.root.paddingBottom#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind PageNavigator / paddingBottom to inset.none, which has the same value.                                               |
| `component.pagenavigator.root.paddingLeft#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind PageNavigator / paddingLeft to inset.none, which has the same value.                                                 |
| `component.pagenavigator.root.paddingRight#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind PageNavigator / paddingRight to inset.none, which has the same value.                                                |
| `component.pagenavigator.root.paddingTop#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind PageNavigator / paddingTop to inset.none, which has the same value.                                                  |
| `component.pagination.next.height#unbound`                                                                                       | 24, bound to no variable                                                                                                                                                 | height on /Next is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (set):** As the previous arrow's, its own. Ask SOLAR to bind Pagination /Next height to icon.lg, which has the same value.                                                                                                                                                                                                                                                                                                                                                                     |
| `component.pagination.next.width#unbound`                                                                                        | 24, bound to no variable                                                                                                                                                 | width on /Next is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (set):** As the previous arrow's, its own. Ask SOLAR to bind Pagination /Next width to icon.lg, which has the same value.                                                                                                                                                                                                                                                                                                                                                                      |
| `component.pagination.page1.height#unbound`                                                                                      | 24, bound to no variable                                                                                                                                                 | height on /Page1 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (set):** As the previous arrow's, its own. Ask SOLAR to bind Pagination /Page1 height to icon.lg, which has the same value.                                                                                                                                                                                                                                                                                                                                                                    |
| `component.pagination.page1.width#unbound`                                                                                       | 24, bound to no variable                                                                                                                                                 | width on /Page1 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (set):** As the previous arrow's, its own. Ask SOLAR to bind Pagination /Page1 width to icon.lg, which has the same value.                                                                                                                                                                                                                                                                                                                                                                     |
| `component.pagination.page12.height#unbound`                                                                                     | 24, bound to no variable                                                                                                                                                 | height on /Page12 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (set):** As the previous arrow's, its own. Ask SOLAR to bind Pagination /Page12 height to icon.lg, which has the same value.                                                                                                                                                                                                                                                                                                                                                                   |
| `component.pagination.page12.width#unbound`                                                                                      | 24, bound to no variable                                                                                                                                                 | width on /Page12 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (set):** As the previous arrow's, its own. Ask SOLAR to bind Pagination /Page12 width to icon.lg, which has the same value.                                                                                                                                                                                                                                                                                                                                                                    |
| `component.pagination.page2.height#unbound`                                                                                      | 24, bound to no variable                                                                                                                                                 | height on /Page2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (set):** As the previous arrow's, its own. Ask SOLAR to bind Pagination /Page2 height to icon.lg, which has the same value.                                                                                                                                                                                                                                                                                                                                                                    |
| `component.pagination.page2.width#unbound`                                                                                       | 24, bound to no variable                                                                                                                                                 | width on /Page2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (set):** As the previous arrow's, its own. Ask SOLAR to bind Pagination /Page2 width to icon.lg, which has the same value.                                                                                                                                                                                                                                                                                                                                                                     |
| `component.pagination.page3.height#unbound`                                                                                      | 24, bound to no variable                                                                                                                                                 | height on /Page3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (set):** As the previous arrow's, its own. Ask SOLAR to bind Pagination /Page3 height to icon.lg, which has the same value.                                                                                                                                                                                                                                                                                                                                                                    |
| `component.pagination.page3.width#unbound`                                                                                       | 24, bound to no variable                                                                                                                                                 | width on /Page3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (set):** As the previous arrow's, its own. Ask SOLAR to bind Pagination /Page3 width to icon.lg, which has the same value.                                                                                                                                                                                                                                                                                                                                                                     |
| `component.pagination.paginationEllipsis.height#unbound`                                                                         | 24, bound to no variable                                                                                                                                                 | height on /PaginationEllipsis is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (set):** As the previous arrow's, its own. Ask SOLAR to bind Pagination /PaginationEllipsis height to icon.lg, which has the same value.                                                                                                                                                                                                                                                                                                                                                       |
| `component.pagination.paginationEllipsis.width#unbound`                                                                          | 24, bound to no variable                                                                                                                                                 | width on /PaginationEllipsis is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (set):** As the previous arrow's, its own. Ask SOLAR to bind Pagination /PaginationEllipsis width to icon.lg, which has the same value.                                                                                                                                                                                                                                                                                                                                                        |
| `component.pagination.previous.height#unbound`                                                                                   | 24, bound to no variable                                                                                                                                                 | height on /Previous is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (set):** As the previous arrow's, its own. Ask SOLAR to bind Pagination /Previous height to icon.lg, which has the same value.                                                                                                                                                                                                                                                                                                                                                                 |
| `component.pagination.previous.width#unbound`                                                                                    | 24, bound to no variable                                                                                                                                                 | width on /Previous is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (set):** Each item is a SOLAR PaginationNav, PaginationItem or PaginationEllipsis, whose 24px square is its own: the pagination lays it out and never sizes it. Ask SOLAR to bind Pagination /Previous width to icon.lg, which has the same value.                                                                                                                                                                                                                                             |
| `component.pagination.root.paddingBottom#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Pagination / paddingBottom to inset.none, which has the same value.                                                  |
| `component.pagination.root.paddingLeft#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Pagination / paddingLeft to inset.none, which has the same value.                                                    |
| `component.pagination.root.paddingRight#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Pagination / paddingRight to inset.none, which has the same value.                                                   |
| `component.pagination.root.paddingTop#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Pagination / paddingTop to inset.none, which has the same value.                                                     |
| `component.paginationellipsis.root.gap#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | gap on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind PaginationEllipsis / gap to inset.none or stack.none, which has the same value.                        |
| `component.paginationellipsis.root.height#unbound`                                                                               | 24, bound to no variable                                                                                                                                                 | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (allowLiteral):** As a PaginationItem's, a 24px square, so it keeps the items' rhythm (a governance gap). SOLAR has no control height token; ask SOLAR for one, or confirm 24 is intended.                                                                                                                                                                                                                                                                                                     |
| `component.paginationellipsis.root.paddingBottom#unbound`                                                                        | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind PaginationEllipsis / paddingBottom to inset.none, which has the same value.                                          |
| `component.paginationellipsis.root.paddingLeft#unbound`                                                                          | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind PaginationEllipsis / paddingLeft to inset.none, which has the same value.                                            |
| `component.paginationellipsis.root.paddingRight#unbound`                                                                         | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind PaginationEllipsis / paddingRight to inset.none, which has the same value.                                           |
| `component.paginationellipsis.root.paddingTop#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind PaginationEllipsis / paddingTop to inset.none, which has the same value.                                             |
| `component.paginationellipsis.root.width#unbound`                                                                                | 24, bound to no variable                                                                                                                                                 | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (allowLiteral):** As a PaginationItem's, a 24px square, so it keeps the items' rhythm (a governance gap). SOLAR has no control width token; ask SOLAR for one, or confirm 24 is intended.                                                                                                                                                                                                                                                                                                      |
| `component.paginationitem.root.gap#unbound`                                                                                      | 0, bound to no variable                                                                                                                                                  | gap on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind PaginationItem / gap to inset.none or stack.none, which has the same value.                            |
| `component.paginationitem.root.height#unbound`                                                                                   | 24, bound to no variable                                                                                                                                                 | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (allowLiteral):** A page is a 24px square, bound to nothing: SOLAR publishes no control size token (owner decision 2026-09-23, raised as a governance gap). Four figures or more would outgrow it; the design review asks. SOLAR has no control height token; ask SOLAR for one, or confirm 24 is intended.                                                                                                                                                                                    |
| `component.paginationitem.root.paddingBottom#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind PaginationItem / paddingBottom to inset.none, which has the same value.                                              |
| `component.paginationitem.root.paddingLeft#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind PaginationItem / paddingLeft to inset.none, which has the same value.                                                |
| `component.paginationitem.root.paddingRight#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind PaginationItem / paddingRight to inset.none, which has the same value.                                               |
| `component.paginationitem.root.paddingTop#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind PaginationItem / paddingTop to inset.none, which has the same value.                                                 |
| `component.paginationitem.root.width#unbound`                                                                                    | 24, bound to no variable                                                                                                                                                 | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (allowLiteral):** A page is a 24px square, bound to nothing: SOLAR publishes no control size token (owner decision 2026-09-23, raised as a governance gap). Four figures or more would outgrow it; the design review asks. SOLAR has no control width token; ask SOLAR for one, or confirm 24 is intended.                                                                                                                                                                                     |
| `component.paginationnav.icon.width#unbound`                                                                                     | 16, bound to no variable                                                                                                                                                 | width on /Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (bind):** Same value, governed token; the icon's height is already bound to it. Ask SOLAR to bind PaginationNav /Icon width to icon.sm, which has the same value.                                                                                                                                                                                                                                                                                                                              |
| `component.paginationnav.root.gap#unbound`                                                                                       | 0, bound to no variable                                                                                                                                                  | gap on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind PaginationNav / gap to inset.none or stack.none, which has the same value.                             |
| `component.paginationnav.root.height#unbound`                                                                                    | 24, bound to no variable                                                                                                                                                 | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (allowLiteral):** As a PaginationItem's, a 24px square, bound to nothing (a governance gap). SOLAR has no control height token; ask SOLAR for one, or confirm 24 is intended.                                                                                                                                                                                                                                                                                                                  |
| `component.paginationnav.root.paddingBottom#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind PaginationNav / paddingBottom to inset.none, which has the same value.                                               |
| `component.paginationnav.root.paddingLeft#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind PaginationNav / paddingLeft to inset.none, which has the same value.                                                 |
| `component.paginationnav.root.paddingRight#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind PaginationNav / paddingRight to inset.none, which has the same value.                                                |
| `component.paginationnav.root.paddingTop#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind PaginationNav / paddingTop to inset.none, which has the same value.                                                  |
| `component.paginationnav.root.width#unbound`                                                                                     | 24, bound to no variable                                                                                                                                                 | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (allowLiteral):** As a PaginationItem's, a 24px square, bound to nothing (a governance gap). SOLAR has no control width token; ask SOLAR for one, or confirm 24 is intended.                                                                                                                                                                                                                                                                                                                   |
| `component.password input.field.height#unbound`                                                                                  | 40, 32, bound to no variable                                                                                                                                             | height on /Field is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind):** As Button's height: size.control, one step per size, which SOLAR added on 2026-09-25; Figma leaves the height unbound, which the design review asks SOLAR to bind. SOLAR has no control height token; ask SOLAR for one, or confirm 40, 32 is intended.                                                                                                                                                                                                                              |
| `component.password input.forgotPassword.present@size=sm`                                                                        | 1 variant at size=sm: false where true                                                                                                                                   | present on /Forgot password? should follow state only, but at size=sm it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (accept):** As the helper's: the link shows where the caller gives it, at both sizes; Figma draws it in the focused md field alone. Ask SOLAR whether Password Input /Forgot password? present is meant to change at size=sm.                                                                                                                                                                                                                                                                  |
| `component.password input.helper.present@state=focus`                                                                            | 1 variant at state=focus: false where true                                                                                                                               | present on /Helper text should follow size only, but at state=focus it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** The helper shows where given, in every state, as Text Input's does: Figma's focused md field swaps it for the forgot-password link, a sample of what may sit below the field, which the code draws beside it where the caller gives one (the slot above). Ask SOLAR whether Password Input /Helper text present is meant to change at state=focus.                                                                                                                                 |
| `component.password input.icon.width#unbound`                                                                                    | 16, 12, bound to no variable                                                                                                                                             | width on /Field/Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind):** Same values, governed tokens, a step of the icon ladder per size; the icon's height is already bound to them. Ask SOLAR to bind Password Input /Field/Icon width to icon.sm or icon.xs, which has the same value.                                                                                                                                                                                                                                                                    |
| `component.password input.label.paddingBottom#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Password Input /Label paddingBottom to inset.none, which has the same value.                                         |
| `component.password input.label.paddingLeft#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Password Input /Label paddingLeft to inset.none, which has the same value.                                           |
| `component.password input.label.paddingRight#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingRight on /Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Password Input /Label paddingRight to inset.none, which has the same value.                                          |
| `component.password input.label.paddingTop#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingTop on /Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Password Input /Label paddingTop to inset.none, which has the same value.                                            |
| `component.password input.root.paddingBottom#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Password Input / paddingBottom to inset.none, which has the same value.                                              |
| `component.password input.root.paddingLeft#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Password Input / paddingLeft to inset.none, which has the same value.                                                |
| `component.password input.root.paddingRight#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Password Input / paddingRight to inset.none, which has the same value.                                               |
| `component.password input.root.paddingTop#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Password Input / paddingTop to inset.none, which has the same value.                                                 |
| `component.password input.root.width#unbound`                                                                                    | 240, 160, bound to no variable                                                                                                                                           | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** As md's, the sm field's 160 a sample. SOLAR has no control width token; ask SOLAR for one, or confirm 240, 160 is intended.                                                                                                                                                                                                                                                                                                                                                           |
| `component.pin input.cells.paddingBottom#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Cells is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind PIN Input /Cells paddingBottom to inset.none, which has the same value.                                              |
| `component.pin input.cells.paddingLeft#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Cells is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind PIN Input /Cells paddingLeft to inset.none, which has the same value.                                                |
| `component.pin input.cells.paddingRight#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingRight on /Cells is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind PIN Input /Cells paddingRight to inset.none, which has the same value.                                               |
| `component.pin input.cells.paddingTop#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingTop on /Cells is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind PIN Input /Cells paddingTop to inset.none, which has the same value.                                                 |
| `component.pin input.field.height#unbound`                                                                                       | 40, 32, bound to no variable                                                                                                                                             | height on /Cells/Field is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (allowLiteral):** A cell is 36 by 40 (md) and 28 by 32 (sm), bound to nothing: SOLAR has no control size token (the design review's question on control sizes). SOLAR has no control height token; ask SOLAR for one, or confirm 40, 32 is intended.                                                                                                                                                                                                                                           |
| `component.pin input.field.width#unbound`                                                                                        | 36, 28, bound to no variable                                                                                                                                             | width on /Cells/Field is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (allowLiteral):** A cell is 36 by 40 (md) and 28 by 32 (sm), bound to nothing: SOLAR has no control size token (the design review's question on control sizes). SOLAR has no control width token; ask SOLAR for one, or confirm 36, 28 is intended.                                                                                                                                                                                                                                            |
| `component.pin input.field2.height#unbound`                                                                                      | 40, 32, bound to no variable                                                                                                                                             | height on /Cells/Field#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (allowLiteral):** A cell is 36 by 40 (md) and 28 by 32 (sm), bound to nothing: SOLAR has no control size token (the design review's question on control sizes). SOLAR has no control height token; ask SOLAR for one, or confirm 40, 32 is intended.                                                                                                                                                                                                                                           |
| `component.pin input.field2.width#unbound`                                                                                       | 36, 28, bound to no variable                                                                                                                                             | width on /Cells/Field#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (allowLiteral):** A cell is 36 by 40 (md) and 28 by 32 (sm), bound to nothing: SOLAR has no control size token (the design review's question on control sizes). SOLAR has no control width token; ask SOLAR for one, or confirm 36, 28 is intended.                                                                                                                                                                                                                                            |
| `component.pin input.field3.height#unbound`                                                                                      | 40, 32, bound to no variable                                                                                                                                             | height on /Cells/Field#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (allowLiteral):** A cell is 36 by 40 (md) and 28 by 32 (sm), bound to nothing: SOLAR has no control size token (the design review's question on control sizes). SOLAR has no control height token; ask SOLAR for one, or confirm 40, 32 is intended.                                                                                                                                                                                                                                           |
| `component.pin input.field3.width#unbound`                                                                                       | 36, 28, bound to no variable                                                                                                                                             | width on /Cells/Field#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (allowLiteral):** A cell is 36 by 40 (md) and 28 by 32 (sm), bound to nothing: SOLAR has no control size token (the design review's question on control sizes). SOLAR has no control width token; ask SOLAR for one, or confirm 36, 28 is intended.                                                                                                                                                                                                                                            |
| `component.pin input.field4.height#unbound`                                                                                      | 40, 32, bound to no variable                                                                                                                                             | height on /Cells/Field#4 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (allowLiteral):** A cell is 36 by 40 (md) and 28 by 32 (sm), bound to nothing: SOLAR has no control size token (the design review's question on control sizes). SOLAR has no control height token; ask SOLAR for one, or confirm 40, 32 is intended.                                                                                                                                                                                                                                           |
| `component.pin input.field4.width#unbound`                                                                                       | 36, 28, bound to no variable                                                                                                                                             | width on /Cells/Field#4 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (allowLiteral):** A cell is 36 by 40 (md) and 28 by 32 (sm), bound to nothing: SOLAR has no control size token (the design review's question on control sizes). SOLAR has no control width token; ask SOLAR for one, or confirm 36, 28 is intended.                                                                                                                                                                                                                                            |
| `component.pin input.field5.height#unbound`                                                                                      | 40, 32, bound to no variable                                                                                                                                             | height on /Cells/Field#5 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (allowLiteral):** A cell is 36 by 40 (md) and 28 by 32 (sm), bound to nothing: SOLAR has no control size token (the design review's question on control sizes). SOLAR has no control height token; ask SOLAR for one, or confirm 40, 32 is intended.                                                                                                                                                                                                                                           |
| `component.pin input.field5.width#unbound`                                                                                       | 36, 28, bound to no variable                                                                                                                                             | width on /Cells/Field#5 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (allowLiteral):** A cell is 36 by 40 (md) and 28 by 32 (sm), bound to nothing: SOLAR has no control size token (the design review's question on control sizes). SOLAR has no control width token; ask SOLAR for one, or confirm 36, 28 is intended.                                                                                                                                                                                                                                            |
| `component.pin input.field6.height#unbound`                                                                                      | 40, 32, bound to no variable                                                                                                                                             | height on /Cells/Field#6 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (allowLiteral):** A cell is 36 by 40 (md) and 28 by 32 (sm), bound to nothing: SOLAR has no control size token (the design review's question on control sizes). SOLAR has no control height token; ask SOLAR for one, or confirm 40, 32 is intended.                                                                                                                                                                                                                                           |
| `component.pin input.field6.width#unbound`                                                                                       | 36, 28, bound to no variable                                                                                                                                             | width on /Cells/Field#6 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (allowLiteral):** A cell is 36 by 40 (md) and 28 by 32 (sm), bound to nothing: SOLAR has no control size token (the design review's question on control sizes). SOLAR has no control width token; ask SOLAR for one, or confirm 36, 28 is intended.                                                                                                                                                                                                                                            |
| `component.pin input.label.paddingBottom#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind PIN Input /Label paddingBottom to inset.none, which has the same value.                                              |
| `component.pin input.label.paddingLeft#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind PIN Input /Label paddingLeft to inset.none, which has the same value.                                                |
| `component.pin input.label.paddingRight#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingRight on /Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind PIN Input /Label paddingRight to inset.none, which has the same value.                                               |
| `component.pin input.label.paddingTop#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingTop on /Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind PIN Input /Label paddingTop to inset.none, which has the same value.                                                 |
| `component.pin input.root.paddingBottom#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind PIN Input / paddingBottom to inset.none, which has the same value.                                                   |
| `component.pin input.root.paddingLeft#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind PIN Input / paddingLeft to inset.none, which has the same value.                                                     |
| `component.pin input.root.paddingRight#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind PIN Input / paddingRight to inset.none, which has the same value.                                                    |
| `component.pin input.root.paddingTop#unbound`                                                                                    | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind PIN Input / paddingTop to inset.none, which has the same value.                                                      |
| `component.popover.content.width#unbound`                                                                                        | 320, 240, bound to no variable                                                                                                                                           | width on /Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (allowLiteral):** A popover is 320 wide (md) or 240 (sm), bound to nothing: SOLAR has no overlay width, one of the sizes with no variable. Its words wrap within it. SOLAR has no control width token; ask SOLAR for one, or confirm 320, 240 is intended.                                                                                                                                                                                                                                     |
| `component.popover.root.gap#unbound`                                                                                             | 0, bound to no variable                                                                                                                                                  | gap on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Popover / gap to inset.none or stack.none, which has the same value.                                   |
| `component.popover.root.paddingBottom#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Popover / paddingBottom to inset.none, which has the same value.                                                     |
| `component.popover.root.paddingLeft#unbound`                                                                                     | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Popover / paddingLeft to inset.none, which has the same value.                                                       |
| `component.popover.root.paddingRight#unbound`                                                                                    | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Popover / paddingRight to inset.none, which has the same value.                                                      |
| `component.popover.root.paddingTop#unbound`                                                                                      | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Popover / paddingTop to inset.none, which has the same value.                                                        |
| `component.popover.tip.height#unbound`                                                                                           | 10, bound to no variable                                                                                                                                                 | height on /Tip is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (allowLiteral):** The tip is 10 × 10, bound to nothing: SOLAR has no size for an overlay's arrow, as Tooltip's. SOLAR has no control height token; ask SOLAR for one, or confirm 10 is intended.                                                                                                                                                                                                                                                                                               |
| `component.popover.tip.width#unbound`                                                                                            | 10, bound to no variable                                                                                                                                                 | width on /Tip is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (allowLiteral):** The tip is 10 × 10, bound to nothing: SOLAR has no size for an overlay's arrow, as Tooltip's. SOLAR has no control width token; ask SOLAR for one, or confirm 10 is intended.                                                                                                                                                                                                                                                                                                |
| `component.progressbar.indicator.height#unbound`                                                                                 | 6, bound to no variable                                                                                                                                                  | height on /Indicator is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (controlDraws):** LinearProgress draws the bar itself, as wide as its track, moved by a transform to show the value, so its place and box are the control's; Figma draws one still frame of it, at the variant's sample value. SOLAR has no control height token; ask SOLAR for one, or confirm 6 is intended.                                                                                                                                                                                 |
| `component.progressbar.indicator.width#unbound`                                                                                  | 80, 120, 200, 180, 190, bound to no variable                                                                                                                             | width on /Indicator is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (controlDraws):** LinearProgress draws the bar itself, as wide as its track, moved by a transform to show the value, so its place and box are the control's; Figma draws one still frame of it, at the variant's sample value. SOLAR has no control width token; ask SOLAR for one, or confirm 80, 120, 200, 180, 190 is intended.                                                                                                                                                             |
| `component.progressbar.root.height#unbound`                                                                                      | 6, bound to no variable                                                                                                                                                  | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (allowLiteral):** The track is 6 tall and bound to nothing: SOLAR has no progress bar height. Raised as a governance gap. SOLAR has no control height token; ask SOLAR for one, or confirm 6 is intended.                                                                                                                                                                                                                                                                                      |
| `component.progressbar.root.width#unbound`                                                                                       | 200, bound to no variable                                                                                                                                                | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** A progress bar spans its container: Figma draws it 200 wide, a sample. The caller sizes it. SOLAR has no control width token; ask SOLAR for one, or confirm 200 is intended.                                                                                                                                                                                                                                                                                                          |
| `component.propertylist.divider.height#unbound`                                                                                  | 1, bound to no variable                                                                                                                                                  | height on /items/Divider is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (set):** The divider is a SOLAR Divider, whose 1px rule is its own: the list places it, and never sizes it. SOLAR has no control height token; ask SOLAR for one, or confirm 1 is intended.                                                                                                                                                                                                                                                                                                    |
| `component.propertylist.divider2.height#unbound`                                                                                 | 1, bound to no variable                                                                                                                                                  | height on /items/Divider#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** The divider is a SOLAR Divider, whose 1px rule is its own: the list places it, and never sizes it. SOLAR has no control height token; ask SOLAR for one, or confirm 1 is intended.                                                                                                                                                                                                                                                                                                    |
| `component.propertylist.divider3.height#unbound`                                                                                 | 1, bound to no variable                                                                                                                                                  | height on /items/Divider#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** The divider is a SOLAR Divider, whose 1px rule is its own: the list places it, and never sizes it. SOLAR has no control height token; ask SOLAR for one, or confirm 1 is intended.                                                                                                                                                                                                                                                                                                    |
| `component.propertylist.items.gap#unbound`                                                                                       | 0, bound to no variable                                                                                                                                                  | gap on /items is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind PropertyList /items gap to inset.none or stack.none, which has the same value.                         |
| `component.propertylist.items.paddingBottom#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingBottom on /items is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind PropertyList /items paddingBottom to inset.none, which has the same value.                                           |
| `component.propertylist.items.paddingLeft#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingLeft on /items is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind PropertyList /items paddingLeft to inset.none, which has the same value.                                             |
| `component.propertylist.items.paddingRight#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingRight on /items is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind PropertyList /items paddingRight to inset.none, which has the same value.                                            |
| `component.propertylist.items.paddingTop#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingTop on /items is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind PropertyList /items paddingTop to inset.none, which has the same value.                                              |
| `component.propertylist.root.borderWidth#unbound`                                                                                | 1, bound to no variable                                                                                                                                                  | borderWidth on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind):** Same value, governed token: the list's edge is SOLAR's default border, as List's is. Ask SOLAR to bind PropertyList / borderWidth to border.default, which has the same value.                                                                                                                                                                                                                                                                                                       |
| `component.propertylist.root.gap#unbound`                                                                                        | 0, bound to no variable                                                                                                                                                  | gap on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind PropertyList / gap to inset.none or stack.none, which has the same value.                              |
| `component.propertyrow.button.height#unbound`                                                                                    | 40, bound to no variable                                                                                                                                                 | height on /Trailing/Button is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** The action is a SOLAR Button (md), whose 40px height is its own: the row places it, and never sizes it. SOLAR has no control height token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                                                                              |
| `component.propertyrow.iconButton.height#unbound`                                                                                | 40, bound to no variable                                                                                                                                                 | height on /Trailing/Icon Button is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (set):** The action is a SOLAR Icon Button (md), whose 40px box is its own. SOLAR has no control height token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                                                                                                                   |
| `component.propertyrow.iconButton.width#unbound`                                                                                 | 40, bound to no variable                                                                                                                                                 | width on /Trailing/Icon Button is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (set):** The action is a SOLAR Icon Button (md), whose 40px box is its own. SOLAR has no control width token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                                                                                                                    |
| `component.propertyrow.leading.height#unbound`                                                                                   | 24, bound to no variable                                                                                                                                                 | height on /Leading is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind):** Same value, governed token; the icon's width is already bound to it. Ask SOLAR to bind PropertyRow /Leading height to icon.lg, which has the same value.                                                                                                                                                                                                                                                                                                                             |
| `component.propertyrow.root.paddingLeft#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind PropertyRow / paddingLeft to inset.none, which has the same value.                                                   |
| `component.propertyrow.root.paddingRight#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind PropertyRow / paddingRight to inset.none, which has the same value.                                                  |
| `component.propertyrow.root.width#unbound`                                                                                       | 505, bound to no variable                                                                                                                                                | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** A row spans its list, as a list's rows do: Figma draws it 505 wide, a sample. SOLAR has no control width token; ask SOLAR for one, or confirm 505 is intended.                                                                                                                                                                                                                                                                                                                        |
| `component.propertyrow.select.width#unbound`                                                                                     | 160, bound to no variable                                                                                                                                                | width on /Trailing/Select is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (allowLiteral):** The choice's Select is 160 wide, bound to nothing: a field fills what holds it, and SOLAR has no width for a row's field; carried as Figma's number, the sizes with no variable. SOLAR has no control width token; ask SOLAR for one, or confirm 160 is intended.                                                                                                                                                                                                            |
| `component.propertyrow.tag.height#unbound`                                                                                       | 24, bound to no variable                                                                                                                                                 | height on /Trailing/Tag is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (set):** The status is a SOLAR Tag, whose 24px height is its own. Ask SOLAR to bind PropertyRow /Trailing/Tag height to icon.lg, which has the same value.                                                                                                                                                                                                                                                                                                                                     |
| `component.propertyrow.toggle.height#unbound`                                                                                    | 18, bound to no variable                                                                                                                                                 | height on /Trailing/Toggle is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** The setting is a SOLAR Toggle, whose 32 × 18 track is its own. SOLAR has no control height token; ask SOLAR for one, or confirm 18 is intended.                                                                                                                                                                                                                                                                                                                                       |
| `component.propertyrow.toggle.width#unbound`                                                                                     | 32, bound to no variable                                                                                                                                                 | width on /Trailing/Toggle is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (set):** The setting is a SOLAR Toggle, whose 32 × 18 track is its own. Ask SOLAR to bind PropertyRow /Trailing/Toggle width to icon.2xl, which has the same value.                                                                                                                                                                                                                                                                                                                            |
| `component.propertyrow.trailing.gap#unbound`                                                                                     | 0, bound to no variable                                                                                                                                                  | gap on /Trailing is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind PropertyRow /Trailing gap to inset.none or stack.none, which has the same value.                       |
| `component.propertyrow.trailing.paddingBottom#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Trailing is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind PropertyRow /Trailing paddingBottom to inset.none, which has the same value.                                         |
| `component.propertyrow.trailing.paddingLeft#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Trailing is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind PropertyRow /Trailing paddingLeft to inset.none, which has the same value.                                           |
| `component.propertyrow.trailing.paddingRight#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingRight on /Trailing is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind PropertyRow /Trailing paddingRight to inset.none, which has the same value.                                          |
| `component.propertyrow.trailing.paddingTop#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingTop on /Trailing is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind PropertyRow /Trailing paddingTop to inset.none, which has the same value.                                            |
| `component.radio.root.height#unbound`                                                                                            | 18, bound to no variable                                                                                                                                                 | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (allowLiteral):** The ring is 18px, and SOLAR has no control size token (the design review's question on control sizes and the target size); carried as Figma's number, as Checkbox's box is. SOLAR has no control height token; ask SOLAR for one, or confirm 18 is intended.                                                                                                                                                                                                                 |
| `component.radio.root.width#unbound`                                                                                             | 18, bound to no variable                                                                                                                                                 | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (allowLiteral):** The ring is 18px, and SOLAR has no control size token (the design review's question on control sizes and the target size); carried as Figma's number, as Checkbox's box is. SOLAR has no control width token; ask SOLAR for one, or confirm 18 is intended.                                                                                                                                                                                                                  |
| `component.row.checkBox.height#unbound`                                                                                          | 40, bound to no variable                                                                                                                                                 | height on /RowSelect is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (set):** The select cell is a SOLAR RowSelect, whose 40px square is its own: the row places it, and never sizes it. SOLAR has no control height token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                                                                           |
| `component.row.checkBox.width#unbound`                                                                                           | 40, bound to no variable                                                                                                                                                 | width on /RowSelect is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (set):** The select cell is a SOLAR RowSelect, whose 40px square is its own: the row places it, and never sizes it. SOLAR has no control width token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                                                                            |
| `component.row.columnItem.height#unbound`                                                                                        | 40, bound to no variable                                                                                                                                                 | height on /TitleRowContent/Column Item is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (set):** A cell is as tall as its row, as Figma's data cells fill theirs (a header cell is fixed at the header row's 40, the same). SOLAR has no control height token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                                                           |
| `component.row.columnItem.height@type=bottom, state=default`                                                                     | 1 variant at type=bottom, state=default: FILL where 40                                                                                                                   | height on /TitleRowContent/Column Item should follow no axis only, but at type=bottom, state=default it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (set):** A cell is as tall as its row, as Figma's data cells fill theirs (a header cell is fixed at the header row's 40, the same). Ask SOLAR whether Row /TitleRowContent/Column Item height is meant to change at type=bottom, state=default.                                                                                                                                                                                                                                                |
| `component.row.columnItem.height@type=bottom, state=selected`                                                                    | 1 variant at type=bottom, state=selected: FILL where 40                                                                                                                  | height on /TitleRowContent/Column Item should follow no axis only, but at type=bottom, state=selected it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (set):** A cell is as tall as its row, as Figma's data cells fill theirs (a header cell is fixed at the header row's 40, the same). Ask SOLAR whether Row /TitleRowContent/Column Item height is meant to change at type=bottom, state=selected.                                                                                                                                                                                                                                               |
| `component.row.columnItem.height@type=middle, state=default`                                                                     | 1 variant at type=middle, state=default: FILL where 40                                                                                                                   | height on /TitleRowContent/Column Item should follow no axis only, but at type=middle, state=default it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (set):** A cell is as tall as its row, as Figma's data cells fill theirs (a header cell is fixed at the header row's 40, the same). Ask SOLAR whether Row /TitleRowContent/Column Item height is meant to change at type=middle, state=default.                                                                                                                                                                                                                                                |
| `component.row.columnItem.height@type=middle, state=selected`                                                                    | 1 variant at type=middle, state=selected: FILL where 40                                                                                                                  | height on /TitleRowContent/Column Item should follow no axis only, but at type=middle, state=selected it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (set):** A cell is as tall as its row, as Figma's data cells fill theirs (a header cell is fixed at the header row's 40, the same). Ask SOLAR whether Row /TitleRowContent/Column Item height is meant to change at type=middle, state=selected.                                                                                                                                                                                                                                               |
| `component.row.columnItem.height@type=non-expandable, state=default`                                                             | 1 variant at type=non-expandable, state=default: FILL where 40                                                                                                           | height on /TitleRowContent/Column Item should follow no axis only, but at type=non-expandable, state=default it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (set):** A cell is as tall as its row, as Figma's data cells fill theirs (a header cell is fixed at the header row's 40, the same). Ask SOLAR whether Row /TitleRowContent/Column Item height is meant to change at type=non-expandable, state=default.                                                                                                                                                                                                                                        |
| `component.row.columnItem.height@type=non-expandable, state=selected`                                                            | 1 variant at type=non-expandable, state=selected: FILL where 40                                                                                                          | height on /TitleRowContent/Column Item should follow no axis only, but at type=non-expandable, state=selected it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (set):** A cell is as tall as its row, as Figma's data cells fill theirs (a header cell is fixed at the header row's 40, the same). Ask SOLAR whether Row /TitleRowContent/Column Item height is meant to change at type=non-expandable, state=selected.                                                                                                                                                                                                                                       |
| `component.row.columnItem.height@type=top, state=default`                                                                        | 1 variant at type=top, state=default: FILL where 40                                                                                                                      | height on /TitleRowContent/Column Item should follow no axis only, but at type=top, state=default it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (set):** A cell is as tall as its row, as Figma's data cells fill theirs (a header cell is fixed at the header row's 40, the same). Ask SOLAR whether Row /TitleRowContent/Column Item height is meant to change at type=top, state=default.                                                                                                                                                                                                                                                   |
| `component.row.columnItem.height@type=top, state=selected`                                                                       | 1 variant at type=top, state=selected: FILL where 40                                                                                                                     | height on /TitleRowContent/Column Item should follow no axis only, but at type=top, state=selected it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** A cell is as tall as its row, as Figma's data cells fill theirs (a header cell is fixed at the header row's 40, the same). Ask SOLAR whether Row /TitleRowContent/Column Item height is meant to change at type=top, state=selected.                                                                                                                                                                                                                                                  |
| `component.row.expand.height#unbound`                                                                                            | 40, bound to no variable                                                                                                                                                 | height on /RowExpand is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (set):** As its width, the RowExpand's own; it is as tall as a table row, as the row's header cells are. SOLAR has no control height token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                                                                                      |
| `component.row.expand.width#unbound`                                                                                             | 16, bound to no variable                                                                                                                                                 | width on /RowExpand is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (set):** The expand cell is a SOLAR RowExpand, whose 16px column is its own: the row places it, and never sizes it. Ask SOLAR to bind Row /RowExpand width to icon.sm, which has the same value.                                                                                                                                                                                                                                                                                               |
| `component.row.root.gap#unbound`                                                                                                 | 0, bound to no variable                                                                                                                                                  | gap on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Row / gap to inset.none or stack.none, which has the same value.                                       |
| `component.row.root.height#unbound`                                                                                              | 44, bound to no variable                                                                                                                                                 | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (allowLiteral):** A table row is 40 tall (the header) or 44 (a data row), bound to nothing: SOLAR publishes no row height, as for RowExpand's and Column Item's. SOLAR has no control height token; ask SOLAR for one, or confirm 44 is intended.                                                                                                                                                                                                                                              |
| `component.row.root.paddingBottom#unbound`                                                                                       | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Row / paddingBottom to inset.none, which has the same value.                                                         |
| `component.row.root.paddingLeft#unbound`                                                                                         | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Row / paddingLeft to inset.none, which has the same value.                                                           |
| `component.row.root.paddingRight#unbound`                                                                                        | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Row / paddingRight to inset.none, which has the same value.                                                          |
| `component.row.root.paddingTop#unbound`                                                                                          | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Row / paddingTop to inset.none, which has the same value.                                                            |
| `component.row.titleRowContent.gap#unbound`                                                                                      | 0, bound to no variable                                                                                                                                                  | gap on /TitleRowContent is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Row /TitleRowContent gap to inset.none or stack.none, which has the same value.                        |
| `component.row.titleRowContent.height@type=bottom, state=default`                                                                | 1 variant at type=bottom, state=default: FILL where HUG                                                                                                                  | height on /TitleRowContent should follow no axis only, but at type=bottom, state=default it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (set):** The cells' row is as tall as the row: Figma's data rows fill it, and the header row's hugs its 40px cells in its 40px row, the same. Ask SOLAR whether Row /TitleRowContent height is meant to change at type=bottom, state=default.                                                                                                                                                                                                                                                  |
| `component.row.titleRowContent.height@type=bottom, state=selected`                                                               | 1 variant at type=bottom, state=selected: FILL where HUG                                                                                                                 | height on /TitleRowContent should follow no axis only, but at type=bottom, state=selected it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (set):** The cells' row is as tall as the row: Figma's data rows fill it, and the header row's hugs its 40px cells in its 40px row, the same. Ask SOLAR whether Row /TitleRowContent height is meant to change at type=bottom, state=selected.                                                                                                                                                                                                                                                 |
| `component.row.titleRowContent.height@type=middle, state=default`                                                                | 1 variant at type=middle, state=default: FILL where HUG                                                                                                                  | height on /TitleRowContent should follow no axis only, but at type=middle, state=default it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (set):** The cells' row is as tall as the row: Figma's data rows fill it, and the header row's hugs its 40px cells in its 40px row, the same. Ask SOLAR whether Row /TitleRowContent height is meant to change at type=middle, state=default.                                                                                                                                                                                                                                                  |
| `component.row.titleRowContent.height@type=middle, state=selected`                                                               | 1 variant at type=middle, state=selected: FILL where HUG                                                                                                                 | height on /TitleRowContent should follow no axis only, but at type=middle, state=selected it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (set):** The cells' row is as tall as the row: Figma's data rows fill it, and the header row's hugs its 40px cells in its 40px row, the same. Ask SOLAR whether Row /TitleRowContent height is meant to change at type=middle, state=selected.                                                                                                                                                                                                                                                 |
| `component.row.titleRowContent.height@type=non-expandable, state=default`                                                        | 1 variant at type=non-expandable, state=default: FILL where HUG                                                                                                          | height on /TitleRowContent should follow no axis only, but at type=non-expandable, state=default it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (set):** The cells' row is as tall as the row: Figma's data rows fill it, and the header row's hugs its 40px cells in its 40px row, the same. Ask SOLAR whether Row /TitleRowContent height is meant to change at type=non-expandable, state=default.                                                                                                                                                                                                                                          |
| `component.row.titleRowContent.height@type=non-expandable, state=selected`                                                       | 1 variant at type=non-expandable, state=selected: FILL where HUG                                                                                                         | height on /TitleRowContent should follow no axis only, but at type=non-expandable, state=selected it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (set):** The cells' row is as tall as the row: Figma's data rows fill it, and the header row's hugs its 40px cells in its 40px row, the same. Ask SOLAR whether Row /TitleRowContent height is meant to change at type=non-expandable, state=selected.                                                                                                                                                                                                                                         |
| `component.row.titleRowContent.height@type=top, state=default`                                                                   | 1 variant at type=top, state=default: FILL where HUG                                                                                                                     | height on /TitleRowContent should follow no axis only, but at type=top, state=default it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (set):** The cells' row is as tall as the row: Figma's data rows fill it, and the header row's hugs its 40px cells in its 40px row, the same. Ask SOLAR whether Row /TitleRowContent height is meant to change at type=top, state=default.                                                                                                                                                                                                                                                     |
| `component.row.titleRowContent.height@type=top, state=selected`                                                                  | 1 variant at type=top, state=selected: FILL where HUG                                                                                                                    | height on /TitleRowContent should follow no axis only, but at type=top, state=selected it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (set):** The cells' row is as tall as the row: Figma's data rows fill it, and the header row's hugs its 40px cells in its 40px row, the same. Ask SOLAR whether Row /TitleRowContent height is meant to change at type=top, state=selected.                                                                                                                                                                                                                                                    |
| `component.row.titleRowContent.paddingBottom#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingBottom on /TitleRowContent is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Row /TitleRowContent paddingBottom to inset.none, which has the same value.                                          |
| `component.row.titleRowContent.paddingLeft#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingLeft on /TitleRowContent is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Row /TitleRowContent paddingLeft to inset.none, which has the same value.                                            |
| `component.row.titleRowContent.paddingRight#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingRight on /TitleRowContent is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Row /TitleRowContent paddingRight to inset.none, which has the same value.                                           |
| `component.row.titleRowContent.paddingTop#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingTop on /TitleRowContent is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Row /TitleRowContent paddingTop to inset.none, which has the same value.                                             |
| `component.rowexpand.iconChevronDown.width#unbound`                                                                              | 16, bound to no variable                                                                                                                                                 | width on /Icon/ChevronDown is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (bind):** As the right chevron's. Ask SOLAR to bind RowExpand /Icon/ChevronDown width to icon.sm, which has the same value.                                                                                                                                                                                                                                                                                                                                                                    |
| `component.rowexpand.iconChevronRight.width#unbound`                                                                             | 16, bound to no variable                                                                                                                                                 | width on /Icon/ChevronRight is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind):** Same value, governed token; Figma binds the chevron's height to icon.sm already. Ask SOLAR to bind RowExpand /Icon/ChevronRight width to icon.sm, which has the same value.                                                                                                                                                                                                                                                                                                          |
| `component.rowexpand.root.height#unbound`                                                                                        | 40, bound to no variable                                                                                                                                                 | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (allowLiteral):** A table row's height, 40, bound to nothing: SOLAR publishes no row height. Raised as a governance gap. SOLAR has no control height token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                                                                      |
| `component.rowexpand.root.width#unbound`                                                                                         | 16, bound to no variable                                                                                                                                                 | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (bind):** The cell is the width of its chevron, an icon.sm (16px), the same value, so the connector lines up with the chevron above it. SOLAR has no control width token; ask SOLAR for one, or confirm 16 is intended.                                                                                                                                                                                                                                                                        |
| `component.rowselect.checkbox.height#unbound`                                                                                    | 16, bound to no variable                                                                                                                                                 | height on /Checkbox is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (set):** The Checkbox is a SOLAR Checkbox, whose 16px box is its own: the cell places it, and never sizes it. Ask SOLAR to bind RowSelect /Checkbox height to icon.sm, which has the same value.                                                                                                                                                                                                                                                                                               |
| `component.rowselect.checkbox.width#unbound`                                                                                     | 16, bound to no variable                                                                                                                                                 | width on /Checkbox is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (set):** The Checkbox is a SOLAR Checkbox, whose 16px box is its own: the cell places it, and never sizes it. Ask SOLAR to bind RowSelect /Checkbox width to icon.sm, which has the same value.                                                                                                                                                                                                                                                                                                |
| `component.rowselect.root.height#unbound`                                                                                        | 40, bound to no variable                                                                                                                                                 | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (allowLiteral):** The cell is a 40px square, bound to nothing: SOLAR has no size for a table's select column, as it has none for a row's height (Column Item's, RowExpand's). SOLAR has no control height token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                 |
| `component.rowselect.root.width#unbound`                                                                                         | 40, bound to no variable                                                                                                                                                 | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (allowLiteral):** The cell is a 40px square, bound to nothing: SOLAR has no size for a table's select column, as it has none for a row's height (Column Item's, RowExpand's). SOLAR has no control width token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                  |
| `component.scrim.root.height#unbound`                                                                                            | 800, bound to no variable                                                                                                                                                | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (set):** As its width, the viewport's height: Figma draws it 800 tall, a sample of a screen. SOLAR has no control height token; ask SOLAR for one, or confirm 800 is intended.                                                                                                                                                                                                                                                                                                                 |
| `component.searchfield.filter.width#unbound`                                                                                     | 16, 12, bound to no variable                                                                                                                                             | width on /Icon/Filter is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind):** Same values, governed tokens: the icon's height is already bound to them, icon.sm in the md field and icon.xs in the sm one. Ask SOLAR to bind SearchField /Icon/Filter width to icon.sm or icon.xs, which has the same value.                                                                                                                                                                                                                                                       |
| `component.searchfield.iconSearch.width#unbound`                                                                                 | 16, 12, bound to no variable                                                                                                                                             | width on /Icon/Search is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind):** Same values, governed tokens: the icon's height is already bound to them, icon.sm in the md field and icon.xs in the sm one. Ask SOLAR to bind SearchField /Icon/Search width to icon.sm or icon.xs, which has the same value.                                                                                                                                                                                                                                                       |
| `component.searchfield.root.height#unbound`                                                                                      | 40, 32, bound to no variable                                                                                                                                             | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind):** As Button's height: size.control, one step per size, which SOLAR added on 2026-09-25; Figma leaves the height unbound, which the design review asks SOLAR to bind. SOLAR has no control height token; ask SOLAR for one, or confirm 40, 32 is intended.                                                                                                                                                                                                                              |
| `component.searchfield.root.width#unbound`                                                                                       | 240, 200, bound to no variable                                                                                                                                           | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** As md's, the sm field's 200 a sample. SOLAR has no control width token; ask SOLAR for one, or confirm 240, 200 is intended.                                                                                                                                                                                                                                                                                                                                                           |
| `component.section nav group header.root.gap#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | gap on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Section Nav Group Header / gap to inset.none or stack.none, which has the same value.                  |
| `component.section nav group header.root.height#unbound`                                                                         | 32, bound to no variable                                                                                                                                                 | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (allowLiteral):** As a Section Nav Item's: SOLAR publishes no height token for a row (owner decision 2026-09-23, raised as a governance gap). A heading is drawn 32 tall, its items' height. SOLAR has no control height token; ask SOLAR for one, or confirm 32 is intended.                                                                                                                                                                                                                  |
| `component.section nav group header.root.paddingBottom#unbound`                                                                  | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Section Nav Group Header / paddingBottom to inset.none, which has the same value.                                    |
| `component.section nav group header.root.paddingTop#unbound`                                                                     | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Section Nav Group Header / paddingTop to inset.none, which has the same value.                                       |
| `component.section nav group header.root.width#unbound`                                                                          | 220, bound to no variable                                                                                                                                                | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** A heading spans its rail, as its items do: Figma draws it 220 wide, bound to nothing, a sample of the rail's width. SOLAR has no control width token; ask SOLAR for one, or confirm 220 is intended.                                                                                                                                                                                                                                                                                  |
| `component.section nav item.icon.width#unbound`                                                                                  | 16, bound to no variable                                                                                                                                                 | width on /Icon/None is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (bind):** Same value, governed token; the icon's height is already bound to it. Ask SOLAR to bind Section Nav Item /Icon/None width to icon.sm, which has the same value.                                                                                                                                                                                                                                                                                                                      |
| `component.section nav item.label.width@state=disabled`                                                                          | 1 variant at state=disabled: FILL where HUG                                                                                                                              | width on /Label should follow no axis only, but at state=disabled it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (set):** The words take the room the item leaves beside its icon, as Figma draws them in every state but rest, where they are still fixed at 180 (the design review asks): as the item's width, the rest is the odd one. Ask SOLAR whether Section Nav Item /Label width is meant to change at state=disabled.                                                                                                                                                                                 |
| `component.section nav item.label.width@state=focus`                                                                             | 1 variant at state=focus: FILL where HUG                                                                                                                                 | width on /Label should follow no axis only, but at state=focus it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (set):** The words take the room the item leaves beside its icon, as Figma draws them in every state but rest, where they are still fixed at 180 (the design review asks): as the item's width, the rest is the odd one. Ask SOLAR whether Section Nav Item /Label width is meant to change at state=focus.                                                                                                                                                                                    |
| `component.section nav item.label.width@state=hover`                                                                             | 1 variant at state=hover: FILL where HUG                                                                                                                                 | width on /Label should follow no axis only, but at state=hover it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (set):** The words take the room the item leaves beside its icon, as Figma draws them in every state but rest, where they are still fixed at 180 (the design review asks): as the item's width, the rest is the odd one. Ask SOLAR whether Section Nav Item /Label width is meant to change at state=hover.                                                                                                                                                                                    |
| `component.section nav item.label.width@state=selected`                                                                          | 1 variant at state=selected: FILL where HUG                                                                                                                              | width on /Label should follow no axis only, but at state=selected it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (set):** The words take the room the item leaves beside its icon, as Figma draws them in every state but rest, where they are still fixed at 180 (the design review asks): as the item's width, the rest is the odd one. Ask SOLAR whether Section Nav Item /Label width is meant to change at state=selected.                                                                                                                                                                                 |
| `component.section nav item.root.height#unbound`                                                                                 | 32, bound to no variable                                                                                                                                                 | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind):** As Button's height: size.control, one step per size, which SOLAR added on 2026-09-25; Figma leaves the height unbound, which the design review asks SOLAR to bind. SOLAR has no control height token; ask SOLAR for one, or confirm 32 is intended.                                                                                                                                                                                                                                  |
| `component.section nav item.root.opacity#unbound`                                                                                | 0.4, bound to no variable                                                                                                                                                | opacity on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (allowLiteral):** A disabled item is drawn at 40% opacity. SOLAR has no opacity scale, as for Link's disabled 50% and Node End's halo (SOLAR's decision, 2026-09-25); carried as Figma's number. SOLAR has no opacity token; ask SOLAR for one, or confirm 0.4 is intended.                                                                                                                                                                                                                    |
| `component.section nav item.root.width#unbound`                                                                                  | 220, bound to no variable                                                                                                                                                | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** An item spans its rail, as a section nav lays its items out: Figma draws it 220 wide in every state, a sample (at rest too since 2026-09-25). The rail sizes it. SOLAR has no control width token; ask SOLAR for one, or confirm 220 is intended.                                                                                                                                                                                                                                     |
| `component.segmented control item.iconLeading.width#unbound`                                                                     | 16, 12, bound to no variable                                                                                                                                             | width on /Icon/None is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (bind):** Same values, governed tokens, a step of the icon ladder per size; the icon's height is already bound to them. Ask SOLAR to bind Segmented Control Item /Icon/None width to icon.sm or icon.xs, which has the same value.                                                                                                                                                                                                                                                             |
| `component.segmented control item.iconTrailing.width#unbound`                                                                    | 16, 12, bound to no variable                                                                                                                                             | width on /Icon/None#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind):** As the leading icon's. Ask SOLAR to bind Segmented Control Item /Icon/None#2 width to icon.sm or icon.xs, which has the same value.                                                                                                                                                                                                                                                                                                                                                  |
| `component.segmented control item.root.borderWidth@selected=false, state=default`                                                | 2 variants at selected=false, state=default: none where border.default                                                                                                   | borderWidth on / should follow size only, but at selected=false, state=default it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** An unselected segment has no edge in Figma, where a border takes no room. In CSS it takes a pixel a side of a segment that hugs its words, so the code keeps the edge, transparent, and a segment does not grow when it is chosen. Ask SOLAR whether Segmented Control Item / borderWidth is meant to change at selected=false, state=default.                                                                                                                                     |
| `component.segmented control item.root.borderWidth@selected=false, state=focus`                                                  | 2 variants at selected=false, state=focus: none where border.default                                                                                                     | borderWidth on / should follow size only, but at selected=false, state=focus it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** As at rest, a transparent edge; the focus ring is the shadow. Ask SOLAR whether Segmented Control Item / borderWidth is meant to change at selected=false, state=focus.                                                                                                                                                                                                                                                                                                            |
| `component.segmented control item.root.borderWidth@selected=false, state=hover`                                                  | 2 variants at selected=false, state=hover: none where border.default                                                                                                     | borderWidth on / should follow size only, but at selected=false, state=hover it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** As at rest, a transparent edge. Ask SOLAR whether Segmented Control Item / borderWidth is meant to change at selected=false, state=hover.                                                                                                                                                                                                                                                                                                                                          |
| `component.segmented control item.root.height#unbound`                                                                           | 32, 24, bound to no variable                                                                                                                                             | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (allowLiteral):** A segment is 32 tall at md and 24 at sm, bound to nothing: 24 is none of SOLAR's control heights (size.control: 32, 40, 48), and the two stay one decision; the design review asks. SOLAR has no control height token; ask SOLAR for one, or confirm 32, 24 is intended.                                                                                                                                                                                                     |
| `component.segmented control item.root.paddingBottom#unbound`                                                                    | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Segmented Control Item / paddingBottom to inset.none, which has the same value.                                      |
| `component.segmented control item.root.paddingTop#unbound`                                                                       | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Segmented Control Item / paddingTop to inset.none, which has the same value.                                         |
| `component.segmented control.label.paddingBottom#unbound`                                                                        | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Segmented Control /Label paddingBottom to inset.none, which has the same value.                                      |
| `component.segmented control.label.paddingLeft#unbound`                                                                          | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Segmented Control /Label paddingLeft to inset.none, which has the same value.                                        |
| `component.segmented control.label.paddingRight#unbound`                                                                         | 0, bound to no variable                                                                                                                                                  | paddingRight on /Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Segmented Control /Label paddingRight to inset.none, which has the same value.                                       |
| `component.segmented control.label.paddingTop#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingTop on /Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Segmented Control /Label paddingTop to inset.none, which has the same value.                                         |
| `component.segmented control.root.paddingBottom#unbound`                                                                         | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Segmented Control / paddingBottom to inset.none, which has the same value.                                           |
| `component.segmented control.root.paddingLeft#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Segmented Control / paddingLeft to inset.none, which has the same value.                                             |
| `component.segmented control.root.paddingRight#unbound`                                                                          | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Segmented Control / paddingRight to inset.none, which has the same value.                                            |
| `component.segmented control.root.paddingTop#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Segmented Control / paddingTop to inset.none, which has the same value.                                              |
| `component.segmented control.segmentedControlItem.height#unbound`                                                                | 32, 24, bound to no variable                                                                                                                                             | height on /Track/Segmented Control Item is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (set):** As at md, the segment's own height. Ask SOLAR to bind Segmented Control /Track/Segmented Control Item height to icon.2xl or icon.lg, which has the same value.                                                                                                                                                                                                                                                                                                                        |
| `component.segmented control.segmentedControlItem2.height#unbound`                                                               | 32, 24, bound to no variable                                                                                                                                             | height on /Track/Segmented Control Item#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (set):** As at md, the segment's own height. Ask SOLAR to bind Segmented Control /Track/Segmented Control Item#2 height to icon.2xl or icon.lg, which has the same value.                                                                                                                                                                                                                                                                                                                      |
| `component.segmented control.segmentedControlItem3.height#unbound`                                                               | 32, 24, bound to no variable                                                                                                                                             | height on /Track/Segmented Control Item#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (set):** As at md, the segment's own height. Ask SOLAR to bind Segmented Control /Track/Segmented Control Item#3 height to icon.2xl or icon.lg, which has the same value.                                                                                                                                                                                                                                                                                                                      |
| `component.segmented control.segmentedControlItem4.height#unbound`                                                               | 32, 24, bound to no variable                                                                                                                                             | height on /Track/Segmented Control Item#4 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (set):** As at md, the segment's own height. Ask SOLAR to bind Segmented Control /Track/Segmented Control Item#4 height to icon.2xl or icon.lg, which has the same value.                                                                                                                                                                                                                                                                                                                      |
| `component.segmented control.segmentedControlItem5.height#unbound`                                                               | 32, 24, bound to no variable                                                                                                                                             | height on /Track/Segmented Control Item#5 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (set):** As at md, the segment's own height. Ask SOLAR to bind Segmented Control /Track/Segmented Control Item#5 height to icon.2xl or icon.lg, which has the same value.                                                                                                                                                                                                                                                                                                                      |
| `component.segmented control.segmentedControlItem6.height#unbound`                                                               | 32, 24, bound to no variable                                                                                                                                             | height on /Track/Segmented Control Item#6 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (set):** As at md, the segment's own height. Ask SOLAR to bind Segmented Control /Track/Segmented Control Item#6 height to icon.2xl or icon.lg, which has the same value.                                                                                                                                                                                                                                                                                                                      |
| `component.select.dropdownItem4.variant.state@size=sm`                                                                           | 1 variant at size=sm: default where disabled                                                                                                                             | variant.state on /Dropdown Menu/Dropdown Item#4 should follow state only, but at size=sm it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (accept):** The rows are samples: md's fourth is a disabled option, sm's a resting one. Each is a Dropdown Item, disabled where the caller says. Ask SOLAR whether Select /Dropdown Menu/Dropdown Item#4 variant.state is meant to change at size=sm.                                                                                                                                                                                                                                          |
| `component.select.dropdownMenu.gap#unbound`                                                                                      | 0, bound to no variable                                                                                                                                                  | gap on /Dropdown Menu is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Select /Dropdown Menu gap to inset.none or stack.none, which has the same value.                       |
| `component.select.dropdownMenu.paddingLeft#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Dropdown Menu is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Select /Dropdown Menu paddingLeft to inset.none, which has the same value.                                           |
| `component.select.dropdownMenu.paddingRight#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingRight on /Dropdown Menu is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Select /Dropdown Menu paddingRight to inset.none, which has the same value.                                          |
| `component.select.dropdownMenu.width#unbound`                                                                                    | 240, 200, bound to no variable                                                                                                                                           | width on /Dropdown Menu is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (set):** As md's, the field's. SOLAR has no control width token; ask SOLAR for one, or confirm 240, 200 is intended.                                                                                                                                                                                                                                                                                                                                                                           |
| `component.select.field.height#unbound`                                                                                          | 40, 32, bound to no variable                                                                                                                                             | height on /Field is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind):** As Button's height: size.control, one step per size, which SOLAR added on 2026-09-25; Figma leaves the height unbound, which the design review asks SOLAR to bind. SOLAR has no control height token; ask SOLAR for one, or confirm 40, 32 is intended.                                                                                                                                                                                                                              |
| `component.select.root.paddingBottom#unbound`                                                                                    | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Select / paddingBottom to inset.none, which has the same value.                                                      |
| `component.select.root.paddingLeft#unbound`                                                                                      | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Select / paddingLeft to inset.none, which has the same value.                                                        |
| `component.select.root.paddingRight#unbound`                                                                                     | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Select / paddingRight to inset.none, which has the same value.                                                       |
| `component.select.root.paddingTop#unbound`                                                                                       | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Select / paddingTop to inset.none, which has the same value.                                                         |
| `component.select.root.width#unbound`                                                                                            | 240, 200, bound to no variable                                                                                                                                           | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** As md's, a sample. SOLAR has no control width token; ask SOLAR for one, or confirm 240, 200 is intended.                                                                                                                                                                                                                                                                                                                                                                              |
| `component.select.trailingIcon.width#unbound`                                                                                    | 16, 12, bound to no variable                                                                                                                                             | width on /Field/Icon/ChevronDown is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind):** Same values, governed tokens, a step of the icon ladder per size; the chevron's height is already bound to them. Ask SOLAR to bind Select /Field/Icon/ChevronDown width to icon.sm or icon.xs, which has the same value.                                                                                                                                                                                                                                                             |
| `component.skeleton.root.gap#unbound`                                                                                            | 0, bound to no variable                                                                                                                                                  | gap on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Skeleton / gap to inset.none or stack.none, which has the same value.                                  |
| `component.skeleton.root.height#unbound`                                                                                         | 12, 16, 20, 24, 32, 40, 80, 120, 180, bound to no variable                                                                                                               | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (allowLiteral):** As the width, the content's height (12 to 180). SOLAR has no control height token; ask SOLAR for one, or confirm 12, 16, 20, 24, 32, 40, 80, 120, 180 is intended.                                                                                                                                                                                                                                                                                                           |
| `component.skeleton.root.paddingBottom#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Skeleton / paddingBottom to inset.none, which has the same value.                                                    |
| `component.skeleton.root.paddingLeft#unbound`                                                                                    | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Skeleton / paddingLeft to inset.none, which has the same value.                                                      |
| `component.skeleton.root.paddingRight#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Skeleton / paddingRight to inset.none, which has the same value.                                                     |
| `component.skeleton.root.paddingTop#unbound`                                                                                     | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Skeleton / paddingTop to inset.none, which has the same value.                                                       |
| `component.skeleton.root.width#unbound`                                                                                          | 120, 200, 280, 24, 32, 40, 320, bound to no variable                                                                                                                     | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (allowLiteral):** The placeholder is the size of what it stands in for (120 to 320 wide) and bound to nothing: SOLAR has no size for it, and the caller gives the real content's (width, height). Raised as a governance gap. SOLAR has no control width token; ask SOLAR for one, or confirm 120, 200, 280, 24, 32, 40, 320 is intended.                                                                                                                                                      |
| `component.slider range.fill.height#unbound`                                                                                     | 4, bound to no variable                                                                                                                                                  | height on /Fill is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (allowLiteral):** As the slider's height. SOLAR has no control height token; ask SOLAR for one, or confirm 4 is intended.                                                                                                                                                                                                                                                                                                                                                                      |
| `component.slider range.fill.width#unbound`                                                                                      | 160, bound to no variable                                                                                                                                                | width on /Fill is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (controlDraws):** The fill runs from the start (or the lower end) to the value, which the control draws: Figma draws one still frame at a sample value. Its height and look are the recipe's. SOLAR has no control width token; ask SOLAR for one, or confirm 160 is intended.                                                                                                                                                                                                                 |
| `component.slider range.handle.height#unbound`                                                                                   | 16, 14, bound to no variable                                                                                                                                             | height on /Handle is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (allowLiteral):** As the slider's height. SOLAR has no control height token; ask SOLAR for one, or confirm 16, 14 is intended.                                                                                                                                                                                                                                                                                                                                                                 |
| `component.slider range.handle.width#unbound`                                                                                    | 16, 14, bound to no variable                                                                                                                                             | width on /Handle is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (allowLiteral):** As the slider's height. SOLAR has no control width token; ask SOLAR for one, or confirm 16, 14 is intended.                                                                                                                                                                                                                                                                                                                                                                  |
| `component.slider range.handle2.height#unbound`                                                                                  | 16, 14, bound to no variable                                                                                                                                             | height on /Handle#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (allowLiteral):** As the slider's height. SOLAR has no control height token; ask SOLAR for one, or confirm 16, 14 is intended.                                                                                                                                                                                                                                                                                                                                                                 |
| `component.slider range.handle2.width#unbound`                                                                                   | 16, 14, bound to no variable                                                                                                                                             | width on /Handle#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (allowLiteral):** As the slider's height. SOLAR has no control width token; ask SOLAR for one, or confirm 16, 14 is intended.                                                                                                                                                                                                                                                                                                                                                                  |
| `component.slider range.root.height#unbound`                                                                                     | 20, bound to no variable                                                                                                                                                 | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (allowLiteral):** The slider is 20 tall, its track 4 and its handle 16 (14 disabled), bound to nothing: SOLAR has no size tokens for them. Carried as Figma's numbers, and raised as a governance gap. SOLAR has no control height token; ask SOLAR for one, or confirm 20 is intended.                                                                                                                                                                                                        |
| `component.slider range.root.width#unbound`                                                                                      | 320, bound to no variable                                                                                                                                                | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** A slider spans its container: Figma draws it 320 wide, a sample. The caller sizes it, as a ProgressBar. SOLAR has no control width token; ask SOLAR for one, or confirm 320 is intended.                                                                                                                                                                                                                                                                                              |
| `component.slider range.track.height#unbound`                                                                                    | 4, bound to no variable                                                                                                                                                  | height on /Track is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (allowLiteral):** As the slider's height. SOLAR has no control height token; ask SOLAR for one, or confirm 4 is intended.                                                                                                                                                                                                                                                                                                                                                                      |
| `component.slider range.track.width#unbound`                                                                                     | 320, bound to no variable                                                                                                                                                | width on /Track is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (set):** The rail spans the slider, whatever its width. SOLAR has no control width token; ask SOLAR for one, or confirm 320 is intended.                                                                                                                                                                                                                                                                                                                                                       |
| `component.slider.fill.height#unbound`                                                                                           | 4, bound to no variable                                                                                                                                                  | height on /Fill is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (allowLiteral):** As the slider's height. SOLAR has no control height token; ask SOLAR for one, or confirm 4 is intended.                                                                                                                                                                                                                                                                                                                                                                      |
| `component.slider.fill.width#unbound`                                                                                            | 160, bound to no variable                                                                                                                                                | width on /Fill is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (controlDraws):** The fill runs from the start (or the lower end) to the value, which the control draws: Figma draws one still frame at a sample value. Its height and look are the recipe's. SOLAR has no control width token; ask SOLAR for one, or confirm 160 is intended.                                                                                                                                                                                                                 |
| `component.slider.handle.height#unbound`                                                                                         | 16, 14, bound to no variable                                                                                                                                             | height on /Handle is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (allowLiteral):** As the slider's height. SOLAR has no control height token; ask SOLAR for one, or confirm 16, 14 is intended.                                                                                                                                                                                                                                                                                                                                                                 |
| `component.slider.handle.width#unbound`                                                                                          | 16, 14, bound to no variable                                                                                                                                             | width on /Handle is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (allowLiteral):** As the slider's height. SOLAR has no control width token; ask SOLAR for one, or confirm 16, 14 is intended.                                                                                                                                                                                                                                                                                                                                                                  |
| `component.slider.root.height#unbound`                                                                                           | 20, bound to no variable                                                                                                                                                 | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (allowLiteral):** The slider is 20 tall, its track 4 and its handle 16 (14 disabled), bound to nothing: SOLAR has no size tokens for them. Carried as Figma's numbers, and raised as a governance gap. SOLAR has no control height token; ask SOLAR for one, or confirm 20 is intended.                                                                                                                                                                                                        |
| `component.slider.root.width#unbound`                                                                                            | 320, bound to no variable                                                                                                                                                | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** A slider spans its container: Figma draws it 320 wide, a sample. The caller sizes it, as a ProgressBar. SOLAR has no control width token; ask SOLAR for one, or confirm 320 is intended.                                                                                                                                                                                                                                                                                              |
| `component.slider.track.height#unbound`                                                                                          | 4, bound to no variable                                                                                                                                                  | height on /Track is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (allowLiteral):** As the slider's height. SOLAR has no control height token; ask SOLAR for one, or confirm 4 is intended.                                                                                                                                                                                                                                                                                                                                                                      |
| `component.slider.track.width#unbound`                                                                                           | 320, bound to no variable                                                                                                                                                | width on /Track is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (set):** The rail spans the slider, whatever its width. SOLAR has no control width token; ask SOLAR for one, or confirm 320 is intended.                                                                                                                                                                                                                                                                                                                                                       |
| `component.sparkline.line.borderWidth#unbound`                                                                                   | 1.5, 2, bound to no variable                                                                                                                                             | borderWidth on /Line is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (allowLiteral):** The sm line's stroke is 1.5, between border.default's 1 and border.strong's 2: no border width is 1.5, one of the sizes with no variable. Ask SOLAR to bind Sparkline /Line borderWidth to border.strong, which has the same value.                                                                                                                                                                                                                                          |
| `component.sparkline.root.height#unbound`                                                                                        | 32, bound to no variable                                                                                                                                                 | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (allowLiteral):** Figma's frame is 115 × 32 at both sizes, its line inside it at its own size (owner decision 2026-09-26: the component is Figma's frame), bound to nothing: SOLAR has no size for it. SOLAR has no control height token; ask SOLAR for one, or confirm 32 is intended.                                                                                                                                                                                                        |
| `component.sparkline.root.width#unbound`                                                                                         | 115, bound to no variable                                                                                                                                                | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (allowLiteral):** Figma's frame is 115 × 32 at both sizes, its line inside it at its own size (owner decision 2026-09-26: the component is Figma's frame), bound to nothing: SOLAR has no size for it. SOLAR has no control width token; ask SOLAR for one, or confirm 115 is intended.                                                                                                                                                                                                        |
| `component.spinner.root.gap#unbound`                                                                                             | 0, bound to no variable                                                                                                                                                  | gap on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Spinner / gap to inset.none or stack.none, which has the same value.                                   |
| `component.spinner.root.paddingBottom#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Spinner / paddingBottom to inset.none, which has the same value.                                                     |
| `component.spinner.root.paddingLeft#unbound`                                                                                     | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Spinner / paddingLeft to inset.none, which has the same value.                                                       |
| `component.spinner.root.paddingRight#unbound`                                                                                    | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Spinner / paddingRight to inset.none, which has the same value.                                                      |
| `component.spinner.root.paddingTop#unbound`                                                                                      | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Spinner / paddingTop to inset.none, which has the same value.                                                        |
| `component.spinner.spinnerRing.height#unbound`                                                                                   | 16, 24, 32, bound to no variable                                                                                                                                         | height on /SpinnerRing is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (allowLiteral):** The ring is square; see spinnerRing.width. SOLAR has no control height token; ask SOLAR for one, or confirm 16, 24, 32 is intended.                                                                                                                                                                                                                                                                                                                                          |
| `component.spinner.spinnerRing.width#unbound`                                                                                    | 16, 24, 32, bound to no variable                                                                                                                                         | width on /SpinnerRing is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (allowLiteral):** The ring is 16, 24 and 32 across and bound to nothing. SOLAR publishes no spinner size token; carried as literals under the owner's decision for control heights (2026-09-23), and raised as a governance gap. SOLAR has no control width token; ask SOLAR for one, or confirm 16, 24, 32 is intended.                                                                                                                                                                       |
| `component.spinner.track.height#unbound`                                                                                         | 16, 24, 32, bound to no variable                                                                                                                                         | height on /SpinnerRing/Track is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (controlDraws):** CircularProgress draws the ring as an SVG circle in its own view box, scaled to the box the recipe sizes, so its place and size inside the ring are the control's; Figma's track fills the ring at [0, 0], as MUI's does, with its stroke inside. SOLAR has no control height token; ask SOLAR for one, or confirm 16, 24, 32 is intended.                                                                                                                                   |
| `component.spinner.track.width#unbound`                                                                                          | 16, 24, 32, bound to no variable                                                                                                                                         | width on /SpinnerRing/Track is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (controlDraws):** CircularProgress draws the ring as an SVG circle in its own view box, scaled to the box the recipe sizes, so its place and size inside the ring are the control's; Figma's track fills the ring at [0, 0], as MUI's does, with its stroke inside. SOLAR has no control width token; ask SOLAR for one, or confirm 16, 24, 32 is intended.                                                                                                                                    |
| `component.split dialog.body.gap#unbound`                                                                                        | 0, bound to no variable                                                                                                                                                  | gap on /Body is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Split Dialog /Body gap to inset.none or stack.none, which has the same value.                          |
| `component.split dialog.body.paddingBottom#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Body is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Split Dialog /Body paddingBottom to inset.none, which has the same value.                                            |
| `component.split dialog.body.paddingLeft#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Body is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Split Dialog /Body paddingLeft to inset.none, which has the same value.                                              |
| `component.split dialog.body.paddingRight#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingRight on /Body is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Split Dialog /Body paddingRight to inset.none, which has the same value.                                             |
| `component.split dialog.body.paddingTop#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingTop on /Body is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Split Dialog /Body paddingTop to inset.none, which has the same value.                                               |
| `component.split dialog.close.height#unbound`                                                                                    | 40, bound to no variable                                                                                                                                                 | height on /Header/Text/Icon Button is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (set):** The close button is a SOLAR Icon Button (md, round, tertiary), whose 40px box is its own: the dialog places it, and never sizes it. SOLAR has no control height token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                                                  |
| `component.split dialog.close.width#unbound`                                                                                     | 40, bound to no variable                                                                                                                                                 | width on /Header/Text/Icon Button is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (set):** The close button is a SOLAR Icon Button (md, round, tertiary), whose 40px box is its own: the dialog places it, and never sizes it. SOLAR has no control width token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                                                   |
| `component.split dialog.container.gap#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | gap on /Body/Container is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Split Dialog /Body/Container gap to inset.none or stack.none, which has the same value.                |
| `component.split dialog.container.paddingBottom#unbound`                                                                         | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Body/Container is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Split Dialog /Body/Container paddingBottom to inset.none, which has the same value.                                  |
| `component.split dialog.container.paddingLeft#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Body/Container is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Split Dialog /Body/Container paddingLeft to inset.none, which has the same value.                                    |
| `component.split dialog.container.paddingRight#unbound`                                                                          | 0, bound to no variable                                                                                                                                                  | paddingRight on /Body/Container is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Split Dialog /Body/Container paddingRight to inset.none, which has the same value.                                   |
| `component.split dialog.container.paddingTop#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingTop on /Body/Container is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Split Dialog /Body/Container paddingTop to inset.none, which has the same value.                                     |
| `component.split dialog.icon.width#unbound`                                                                                      | 12, bound to no variable                                                                                                                                                 | width on /Header/Text/Icon/Icon/Empty is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind):** Same value, governed token; the icon's height is already bound to it. Ask SOLAR to bind Split Dialog /Header/Text/Icon/Icon/Empty width to icon.xs, which has the same value.                                                                                                                                                                                                                                                                                                        |
| `component.split dialog.leading.height#unbound`                                                                                  | 36, bound to no variable                                                                                                                                                 | height on /Header/Text/Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (allowLiteral):** The title's icon sits in a 36px box, bound to nothing: SOLAR has no size for it, as Dialog's. SOLAR has no control height token; ask SOLAR for one, or confirm 36 is intended.                                                                                                                                                                                                                                                                                               |
| `component.split dialog.leading.paddingBottom#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Header/Text/Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Split Dialog /Header/Text/Icon paddingBottom to inset.none, which has the same value.                                |
| `component.split dialog.leading.paddingLeft#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Header/Text/Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Split Dialog /Header/Text/Icon paddingLeft to inset.none, which has the same value.                                  |
| `component.split dialog.leading.paddingRight#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingRight on /Header/Text/Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Split Dialog /Header/Text/Icon paddingRight to inset.none, which has the same value.                                 |
| `component.split dialog.leading.paddingTop#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingTop on /Header/Text/Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Split Dialog /Header/Text/Icon paddingTop to inset.none, which has the same value.                                   |
| `component.split dialog.leading.width#unbound`                                                                                   | 36, bound to no variable                                                                                                                                                 | width on /Header/Text/Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (allowLiteral):** The title's icon sits in a 36px box, bound to nothing: SOLAR has no size for it, as Dialog's. SOLAR has no control width token; ask SOLAR for one, or confirm 36 is intended.                                                                                                                                                                                                                                                                                                |
| `component.split dialog.root.gap#unbound`                                                                                        | 0, bound to no variable                                                                                                                                                  | gap on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Split Dialog / gap to inset.none or stack.none, which has the same value.                              |
| `component.split dialog.root.height#unbound`                                                                                     | 480, bound to no variable                                                                                                                                                | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (set):** A split dialog is as tall as its panes: Figma fixes it at 480, room for its sample content. SOLAR has no control height token; ask SOLAR for one, or confirm 480 is intended.                                                                                                                                                                                                                                                                                                         |
| `component.split dialog.root.paddingBottom#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Split Dialog / paddingBottom to inset.none, which has the same value.                                                |
| `component.split dialog.root.paddingLeft#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Split Dialog / paddingLeft to inset.none, which has the same value.                                                  |
| `component.split dialog.root.paddingRight#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Split Dialog / paddingRight to inset.none, which has the same value.                                                 |
| `component.split dialog.root.paddingTop#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Split Dialog / paddingTop to inset.none, which has the same value.                                                   |
| `component.split dialog.root.width#unbound`                                                                                      | 640, bound to no variable                                                                                                                                                | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (allowLiteral):** A split dialog is 640 wide, bound to nothing: SOLAR has no dialog width, one of the sizes with no variable. The page may be narrower, and the dialog then fits it. SOLAR has no control width token; ask SOLAR for one, or confirm 640 is intended.                                                                                                                                                                                                                          |
| `component.split dialog.text.paddingBottom#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Header/Text is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Split Dialog /Header/Text paddingBottom to inset.none, which has the same value.                                     |
| `component.split dialog.text.paddingLeft#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Header/Text is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Split Dialog /Header/Text paddingLeft to inset.none, which has the same value.                                       |
| `component.split dialog.text.paddingRight#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingRight on /Header/Text is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Split Dialog /Header/Text paddingRight to inset.none, which has the same value.                                      |
| `component.split dialog.text.paddingTop#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingTop on /Header/Text is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Split Dialog /Header/Text paddingTop to inset.none, which has the same value.                                        |
| `component.split dropdown.lowerContent.gap#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | gap on /Lower/Lower Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Split Dropdown /Lower/Lower Content gap to inset.none or stack.none, which has the same value.         |
| `component.split dropdown.lowerContent.paddingBottom#unbound`                                                                    | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Lower/Lower Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Split Dropdown /Lower/Lower Content paddingBottom to inset.none, which has the same value.                           |
| `component.split dropdown.lowerContent.paddingLeft#unbound`                                                                      | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Lower/Lower Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Split Dropdown /Lower/Lower Content paddingLeft to inset.none, which has the same value.                             |
| `component.split dropdown.lowerContent.paddingRight#unbound`                                                                     | 0, bound to no variable                                                                                                                                                  | paddingRight on /Lower/Lower Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Split Dropdown /Lower/Lower Content paddingRight to inset.none, which has the same value.                            |
| `component.split dropdown.lowerContent.paddingTop#unbound`                                                                       | 0, bound to no variable                                                                                                                                                  | paddingTop on /Lower/Lower Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Split Dropdown /Lower/Lower Content paddingTop to inset.none, which has the same value.                              |
| `component.split dropdown.root.width#unbound`                                                                                    | 680, bound to no variable                                                                                                                                                | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** The box spans the space it is put in: Figma draws it 680 wide, a sample. The caller sizes it. SOLAR has no control width token; ask SOLAR for one, or confirm 680 is intended.                                                                                                                                                                                                                                                                                                        |
| `component.split dropdown.top.height#unbound`                                                                                    | 80, bound to no variable                                                                                                                                                 | height on /Top is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (set):** The top zone holds the primary control, padded 16 above and below: Figma fixes it at 80, its 48 control's height with that padding, bound to nothing. It hugs what it holds, which draws Figma's 80 for a 48 control and grows with a taller one. SOLAR has no control height token; ask SOLAR for one, or confirm 80 is intended.                                                                                                                                                    |
| `component.split dropdown.topContent.gap#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | gap on /Top/Top Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Split Dropdown /Top/Top Content gap to inset.none or stack.none, which has the same value.             |
| `component.split dropdown.topContent.paddingBottom#unbound`                                                                      | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Top/Top Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Split Dropdown /Top/Top Content paddingBottom to inset.none, which has the same value.                               |
| `component.split dropdown.topContent.paddingLeft#unbound`                                                                        | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Top/Top Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Split Dropdown /Top/Top Content paddingLeft to inset.none, which has the same value.                                 |
| `component.split dropdown.topContent.paddingRight#unbound`                                                                       | 0, bound to no variable                                                                                                                                                  | paddingRight on /Top/Top Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Split Dropdown /Top/Top Content paddingRight to inset.none, which has the same value.                                |
| `component.split dropdown.topContent.paddingTop#unbound`                                                                         | 0, bound to no variable                                                                                                                                                  | paddingTop on /Top/Top Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Split Dropdown /Top/Top Content paddingTop to inset.none, which has the same value.                                  |
| `component.splitbutton.action.height#unbound`                                                                                    | 44, 36, bound to no variable                                                                                                                                             | height on /Action is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** The action half is hidden while loading, where Figma sizes it 44 and 36, never drawn; it keeps its resting room (its FILL), as a loading Button keeps its label's. SOLAR has no control height token; ask SOLAR for one, or confirm 44, 36 is intended.                                                                                                                                                                                                                            |
| `component.splitbutton.action.height@prio=primary, state=loading`                                                                | 2 variants at prio=primary, state=loading: 44 where FILL; 36 where FILL                                                                                                  | height on /Action should follow size only, but at prio=primary, state=loading it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** As the unbound height, a size of a half the loading state hides. Ask SOLAR whether SplitButton /Action height is meant to change at prio=primary, state=loading.                                                                                                                                                                                                                                                                                                                   |
| `component.splitbutton.action.height@prio=secondary, state=loading`                                                              | 2 variants at prio=secondary, state=loading: 44 where FILL; 36 where FILL                                                                                                | height on /Action should follow size only, but at prio=secondary, state=loading it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** As the unbound height, a size of a half the loading state hides. Ask SOLAR whether SplitButton /Action height is meant to change at prio=secondary, state=loading.                                                                                                                                                                                                                                                                                                                 |
| `component.splitbutton.action.paddingBottom#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Action is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind SplitButton /Action paddingBottom to inset.none, which has the same value.                                           |
| `component.splitbutton.action.paddingTop#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingTop on /Action is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind SplitButton /Action paddingTop to inset.none, which has the same value.                                              |
| `component.splitbutton.divider.height#unbound`                                                                                   | 44, 36, bound to no variable                                                                                                                                             | height on /Divider is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** As the action half's, the rule is hidden while loading, keeping its resting room. SOLAR has no control height token; ask SOLAR for one, or confirm 44, 36 is intended.                                                                                                                                                                                                                                                                                                             |
| `component.splitbutton.divider.height@prio=primary, state=loading`                                                               | 2 variants at prio=primary, state=loading: 44 where FILL; 36 where FILL                                                                                                  | height on /Divider should follow size only, but at prio=primary, state=loading it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** As the unbound height, hidden while loading. Ask SOLAR whether SplitButton /Divider height is meant to change at prio=primary, state=loading.                                                                                                                                                                                                                                                                                                                                      |
| `component.splitbutton.divider.height@prio=secondary, state=loading`                                                             | 2 variants at prio=secondary, state=loading: 44 where FILL; 36 where FILL                                                                                                | height on /Divider should follow size only, but at prio=secondary, state=loading it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** As the unbound height, hidden while loading. Ask SOLAR whether SplitButton /Divider height is meant to change at prio=secondary, state=loading.                                                                                                                                                                                                                                                                                                                                    |
| `component.splitbutton.divider.opacity#unbound`                                                                                  | 0.3, bound to no variable                                                                                                                                                | opacity on /Divider is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (allowLiteral):** The rule is the label's colour at 30% opacity. SOLAR has no opacity scale, as for Node End's halo (SOLAR's decision, 2026-09-25); carried as Figma's number. SOLAR has no opacity token; ask SOLAR for one, or confirm 0.3 is intended.                                                                                                                                                                                                                                      |
| `component.splitbutton.divider.width#unbound`                                                                                    | 1, bound to no variable                                                                                                                                                  | width on /Divider is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind):** The rule between the halves is 1px, a hairline, border.default's width, the same value, as Divider's rule is. SOLAR has no control width token; ask SOLAR for one, or confirm 1 is intended.                                                                                                                                                                                                                                                                                         |
| `component.splitbutton.iconChevronDown.width#unbound`                                                                            | 20, 16, bound to no variable                                                                                                                                             | width on /Trigger/Icon/ChevronDown is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind):** Same values, governed tokens, a step of the icon ladder per size; the chevron's height is already bound to them. Ask SOLAR to bind SplitButton /Trigger/Icon/ChevronDown width to icon.md or icon.sm, which has the same value.                                                                                                                                                                                                                                                      |
| `component.splitbutton.root.gap#unbound`                                                                                         | 0, bound to no variable                                                                                                                                                  | gap on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind SplitButton / gap to inset.none or stack.none, which has the same value.                               |
| `component.splitbutton.root.height#unbound`                                                                                      | 40, 32, bound to no variable                                                                                                                                             | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind):** As Button's height: size.control, one step per size, which SOLAR added on 2026-09-25; Figma leaves the height unbound, which the design review asks SOLAR to bind. SOLAR has no control height token; ask SOLAR for one, or confirm 40, 32 is intended.                                                                                                                                                                                                                              |
| `component.splitbutton.root.paddingBottom#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind SplitButton / paddingBottom to inset.none, which has the same value.                                                 |
| `component.splitbutton.root.paddingLeft#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind SplitButton / paddingLeft to inset.none, which has the same value.                                                   |
| `component.splitbutton.root.paddingRight#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind SplitButton / paddingRight to inset.none, which has the same value.                                                  |
| `component.splitbutton.root.paddingTop#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind SplitButton / paddingTop to inset.none, which has the same value.                                                    |
| `component.splitbutton.trigger.gap#unbound`                                                                                      | 0, bound to no variable                                                                                                                                                  | gap on /Trigger is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind SplitButton /Trigger gap to inset.none or stack.none, which has the same value.                        |
| `component.splitbutton.trigger.height#unbound`                                                                                   | 32, 44, 36, bound to no variable                                                                                                                                         | height on /Trigger is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind):** As Button's height: size.control, one step per size, which SOLAR added on 2026-09-25; Figma leaves the height unbound, which the design review asks SOLAR to bind. SOLAR has no control height token; ask SOLAR for one, or confirm 32, 44, 36 is intended.                                                                                                                                                                                                                          |
| `component.splitbutton.trigger.height@prio=primary, state=loading`                                                               | 2 variants at prio=primary, state=loading: 44 where FILL; 36 where 32                                                                                                    | height on /Trigger should follow size only, but at prio=primary, state=loading it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** As the action half's, the chevron's half is hidden while loading, keeping its resting room. Ask SOLAR whether SplitButton /Trigger height is meant to change at prio=primary, state=loading.                                                                                                                                                                                                                                                                                       |
| `component.splitbutton.trigger.height@prio=primary, state=pressed`                                                               | 1 variant at prio=primary, state=pressed: FILL where 32                                                                                                                  | height on /Trigger should follow size only, but at prio=primary, state=pressed it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** As its width, the chevron's half keeps its size in every state. Ask SOLAR whether SplitButton /Trigger height is meant to change at prio=primary, state=pressed.                                                                                                                                                                                                                                                                                                                   |
| `component.splitbutton.trigger.height@prio=secondary, state=loading`                                                             | 2 variants at prio=secondary, state=loading: 44 where FILL; 36 where 32                                                                                                  | height on /Trigger should follow size only, but at prio=secondary, state=loading it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** As primary's, hidden while loading. Ask SOLAR whether SplitButton /Trigger height is meant to change at prio=secondary, state=loading.                                                                                                                                                                                                                                                                                                                                             |
| `component.splitbutton.trigger.paddingBottom#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Trigger is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind SplitButton /Trigger paddingBottom to inset.none, which has the same value.                                          |
| `component.splitbutton.trigger.paddingLeft#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Trigger is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind SplitButton /Trigger paddingLeft to inset.none, which has the same value.                                            |
| `component.splitbutton.trigger.paddingRight#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingRight on /Trigger is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind SplitButton /Trigger paddingRight to inset.none, which has the same value.                                           |
| `component.splitbutton.trigger.paddingTop#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingTop on /Trigger is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind SplitButton /Trigger paddingTop to inset.none, which has the same value.                                             |
| `component.splitbutton.trigger.width#unbound`                                                                                    | 40, 32, 36, bound to no variable                                                                                                                                         | width on /Trigger is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (allowLiteral):** The chevron's half is square, 40 and 32, the control's height, under the same decision. SOLAR has no control width token; ask SOLAR for one, or confirm 40, 32, 36 is intended.                                                                                                                                                                                                                                                                                              |
| `component.splitbutton.trigger.width@prio=primary, state=loading`                                                                | 1 variant at prio=primary, state=loading: 36 where 32                                                                                                                    | width on /Trigger should follow size only, but at prio=primary, state=loading it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** As its height, hidden while loading. Ask SOLAR whether SplitButton /Trigger width is meant to change at prio=primary, state=loading.                                                                                                                                                                                                                                                                                                                                               |
| `component.splitbutton.trigger.width@prio=secondary, state=loading`                                                              | 1 variant at prio=secondary, state=loading: 36 where 32                                                                                                                  | width on /Trigger should follow size only, but at prio=secondary, state=loading it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** As its height, hidden while loading. Ask SOLAR whether SplitButton /Trigger width is meant to change at prio=secondary, state=loading.                                                                                                                                                                                                                                                                                                                                             |
| `component.status card.iconMore.width#unbound`                                                                                   | 20, bound to no variable                                                                                                                                                 | width on /Title/Icon/More is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind):** Same value, governed token; the More icon's height is already bound to it. Ask SOLAR to bind Status Card /Title/Icon/More width to icon.md, which has the same value.                                                                                                                                                                                                                                                                                                                |
| `component.status card.root.width#unbound`                                                                                       | 240, bound to no variable                                                                                                                                                | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** A card spans the space it is put in, as a grid's cell: Figma draws it 240 wide, a sample. The caller sizes it. SOLAR has no control width token; ask SOLAR for one, or confirm 240 is intended.                                                                                                                                                                                                                                                                                       |
| `component.status card.skeleton.height#unbound`                                                                                  | 12, bound to no variable                                                                                                                                                 | height on /Skeleton is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (allowLiteral):** As the placeholders' widths, carried as drawn. SOLAR has no control height token; ask SOLAR for one, or confirm 12 is intended.                                                                                                                                                                                                                                                                                                                                              |
| `component.status card.skeleton.width#unbound`                                                                                   | 170, bound to no variable                                                                                                                                                | width on /Skeleton is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (allowLiteral):** The loading placeholders are Figma's drawing of a title and a figure to come (170 × 12 and 62 × 24), no measure of anything: carried as drawn. SOLAR has no skeleton size tokens. SOLAR has no control width token; ask SOLAR for one, or confirm 170 is intended.                                                                                                                                                                                                           |
| `component.status card.skeleton2.height#unbound`                                                                                 | 24, bound to no variable                                                                                                                                                 | height on /Skeleton#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (allowLiteral):** As the title placeholder's, carried as drawn. SOLAR has no control height token; ask SOLAR for one, or confirm 24 is intended.                                                                                                                                                                                                                                                                                                                                               |
| `component.status card.skeleton2.width#unbound`                                                                                  | 62, bound to no variable                                                                                                                                                 | width on /Skeleton#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (allowLiteral):** As the title placeholder's, carried as drawn. SOLAR has no control width token; ask SOLAR for one, or confirm 62 is intended.                                                                                                                                                                                                                                                                                                                                                |
| `component.status card.statusIndicator.height#unbound`                                                                           | 20, bound to no variable                                                                                                                                                 | height on /Trend/StatusIndicator is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (set):** As its width, the StatusIndicator's own. Ask SOLAR to bind Status Card /Trend/StatusIndicator height to icon.md, which has the same value.                                                                                                                                                                                                                                                                                                                                            |
| `component.status card.statusIndicator.width#unbound`                                                                            | 20, bound to no variable                                                                                                                                                 | width on /Trend/StatusIndicator is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (set):** The mark is a StatusIndicator, whose md, 20px is its own: the card places it, and never sizes it. Ask SOLAR to bind Status Card /Trend/StatusIndicator width to icon.md, which has the same value.                                                                                                                                                                                                                                                                                    |
| `component.status card.title.align@status=warning, state=disabled, ghost=false`                                                  | 1 variant at status=warning, state=disabled, ghost=false: MIN/CENTER where SPACE_BETWEEN/CENTER                                                                          | align on /Title should follow no axis only, but at status=warning, state=disabled, ghost=false it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** The disabled warning card packs its title row to the start (MIN), where every other variant spreads it (SPACE_BETWEEN), its More at the end: a slip in one of sixteen. The code spreads it in all; the design review lists it. Ask SOLAR whether Status Card /Title align is meant to change at status=warning, state=disabled, ghost=false.                                                                                                                                       |
| `component.status card.title.opacity#unbound`                                                                                    | 0.2, bound to no variable                                                                                                                                                | opacity on /Title is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (allowLiteral):** A disabled card's title and figure are drawn at 20% opacity. SOLAR has no opacity scale, as for Card's disabled 40% (SOLAR's decision, 2026-09-25); carried as Figma's number. SOLAR has no opacity token; ask SOLAR for one, or confirm 0.2 is intended.                                                                                                                                                                                                                    |
| `component.status card.title.paddingBottom#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Title is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Status Card /Title paddingBottom to inset.none, which has the same value.                                            |
| `component.status card.title.paddingLeft#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Title is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Status Card /Title paddingLeft to inset.none, which has the same value.                                              |
| `component.status card.title.paddingRight#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingRight on /Title is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Status Card /Title paddingRight to inset.none, which has the same value.                                             |
| `component.status card.title.paddingTop#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingTop on /Title is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Status Card /Title paddingTop to inset.none, which has the same value.                                               |
| `component.status card.trend.opacity#unbound`                                                                                    | 0.2, bound to no variable                                                                                                                                                | opacity on /Trend is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (allowLiteral):** As the title's, carried as Figma's number. SOLAR has no opacity token; ask SOLAR for one, or confirm 0.2 is intended.                                                                                                                                                                                                                                                                                                                                                        |
| `component.status card.trend.paddingBottom#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Trend is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Status Card /Trend paddingBottom to inset.none, which has the same value.                                            |
| `component.status card.trend.paddingLeft#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Trend is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Status Card /Trend paddingLeft to inset.none, which has the same value.                                              |
| `component.status card.trend.paddingRight#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingRight on /Trend is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Status Card /Trend paddingRight to inset.none, which has the same value.                                             |
| `component.status card.trend.paddingTop#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingTop on /Trend is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Status Card /Trend paddingTop to inset.none, which has the same value.                                               |
| `component.statusindicator.container.gap#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | gap on /Container is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind StatusIndicator /Container gap to inset.none or stack.none, which has the same value.                  |
| `component.statusindicator.container.height#unbound`                                                                             | 20, 16, bound to no variable                                                                                                                                             | height on /Container is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind):** As the width, the mark's size. SOLAR has no control height token; ask SOLAR for one, or confirm 20, 16 is intended.                                                                                                                                                                                                                                                                                                                                                                  |
| `component.statusindicator.container.paddingBottom#unbound`                                                                      | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Container is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind StatusIndicator /Container paddingBottom to inset.none, which has the same value.                                    |
| `component.statusindicator.container.paddingLeft#unbound`                                                                        | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Container is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind StatusIndicator /Container paddingLeft to inset.none, which has the same value.                                      |
| `component.statusindicator.container.paddingRight#unbound`                                                                       | 0, bound to no variable                                                                                                                                                  | paddingRight on /Container is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind StatusIndicator /Container paddingRight to inset.none, which has the same value.                                     |
| `component.statusindicator.container.paddingTop#unbound`                                                                         | 0, bound to no variable                                                                                                                                                  | paddingTop on /Container is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind StatusIndicator /Container paddingTop to inset.none, which has the same value.                                       |
| `component.statusindicator.container.width#unbound`                                                                              | 20, 16, bound to no variable                                                                                                                                             | width on /Container is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (bind):** The disc of danger, neutral, help and private fills the mark, so it is the mark's size. SOLAR has no control width token; ask SOLAR for one, or confirm 20, 16 is intended.                                                                                                                                                                                                                                                                                                          |
| `component.statusindicator.containerInnerPath.height#unbound`                                                                    | 1, bound to no variable                                                                                                                                                  | height on /Container/inner-path is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (allowLiteral):** Neutral's dash, part of the drawing. SOLAR has no control height token; ask SOLAR for one, or confirm 1 is intended.                                                                                                                                                                                                                                                                                                                                                         |
| `component.statusindicator.containerInnerPath.width#unbound`                                                                     | 9, bound to no variable                                                                                                                                                  | width on /Container/inner-path is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (allowLiteral):** Neutral's dash, part of the drawing. SOLAR has no control width token; ask SOLAR for one, or confirm 9 is intended.                                                                                                                                                                                                                                                                                                                                                          |
| `component.statusindicator.containerUnion.height#unbound`                                                                        | 8, 6, bound to no variable                                                                                                                                               | height on /Container/Union is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (allowLiteral):** Private's lock, part of the drawing. SOLAR has no control height token; ask SOLAR for one, or confirm 8, 6 is intended.                                                                                                                                                                                                                                                                                                                                                      |
| `component.statusindicator.containerUnion.width#unbound`                                                                         | 6, 5, bound to no variable                                                                                                                                               | width on /Container/Union is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (allowLiteral):** Private's lock, part of the drawing. SOLAR has no control width token; ask SOLAR for one, or confirm 6, 5 is intended.                                                                                                                                                                                                                                                                                                                                                       |
| `component.statusindicator.frame3.gap#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | gap on /Frame 3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind StatusIndicator /Frame 3 gap to inset.none or stack.none, which has the same value.                    |
| `component.statusindicator.frame3.height#unbound`                                                                                | 20, 16, bound to no variable                                                                                                                                             | height on /Frame 3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind):** As the width, the mark's size. SOLAR has no control height token; ask SOLAR for one, or confirm 20, 16 is intended.                                                                                                                                                                                                                                                                                                                                                                  |
| `component.statusindicator.frame3.paddingBottom#unbound`                                                                         | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Frame 3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind StatusIndicator /Frame 3 paddingBottom to inset.none, which has the same value.                                      |
| `component.statusindicator.frame3.paddingLeft#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Frame 3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind StatusIndicator /Frame 3 paddingLeft to inset.none, which has the same value.                                        |
| `component.statusindicator.frame3.paddingRight#unbound`                                                                          | 0, bound to no variable                                                                                                                                                  | paddingRight on /Frame 3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind StatusIndicator /Frame 3 paddingRight to inset.none, which has the same value.                                       |
| `component.statusindicator.frame3.paddingTop#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingTop on /Frame 3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind StatusIndicator /Frame 3 paddingTop to inset.none, which has the same value.                                         |
| `component.statusindicator.frame3.width#unbound`                                                                                 | 20, 16, bound to no variable                                                                                                                                             | width on /Frame 3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind):** Info's disc fills the mark, so it is the mark's size. SOLAR has no control width token; ask SOLAR for one, or confirm 20, 16 is intended.                                                                                                                                                                                                                                                                                                                                            |
| `component.statusindicator.frame3InnerPath.height#unbound`                                                                       | 10, 8, bound to no variable                                                                                                                                              | height on /Frame 3/inner-path is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (allowLiteral):** Info's mark, part of the drawing. SOLAR has no control height token; ask SOLAR for one, or confirm 10, 8 is intended.                                                                                                                                                                                                                                                                                                                                                        |
| `component.statusindicator.frame3InnerPath.width#unbound`                                                                        | 5, 4, bound to no variable                                                                                                                                               | width on /Frame 3/inner-path is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (allowLiteral):** Info's mark, part of the drawing. SOLAR has no control width token; ask SOLAR for one, or confirm 5, 4 is intended.                                                                                                                                                                                                                                                                                                                                                          |
| `component.statusindicator.icon.height#unbound`                                                                                  | 9, 6, bound to no variable                                                                                                                                               | height on /Container/Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (allowLiteral):** Help's question mark, part of the drawing. SOLAR has no control height token; ask SOLAR for one, or confirm 9, 6 is intended.                                                                                                                                                                                                                                                                                                                                                |
| `component.statusindicator.icon.width#unbound`                                                                                   | 6, 4, bound to no variable                                                                                                                                               | width on /Container/Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (allowLiteral):** Help's question mark, part of the drawing. SOLAR has no control width token; ask SOLAR for one, or confirm 6, 4 is intended.                                                                                                                                                                                                                                                                                                                                                 |
| `component.statusindicator.innerPath.height#unbound`                                                                             | 7, 5, 9, 10, 8, bound to no variable                                                                                                                                     | height on /inner-path is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (allowLiteral):** As the width, part of the drawing. SOLAR has no control height token; ask SOLAR for one, or confirm 7, 5, 9, 10, 8 is intended.                                                                                                                                                                                                                                                                                                                                              |
| `component.statusindicator.innerPath.width#unbound`                                                                              | 10, 7, 2, 8, bound to no variable                                                                                                                                        | width on /inner-path is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (allowLiteral):** The box of a drawn mark (the tick, the exclamation, the cross), in the drawing's own coordinates, as its outline is: part of the drawing, not a size of the system. SOLAR has no control width token; ask SOLAR for one, or confirm 10, 7, 2, 8 is intended.                                                                                                                                                                                                                 |
| `component.statusindicator.root.height#unbound`                                                                                  | 20, 16, 8, bound to no variable                                                                                                                                          | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (allowLiteral):** As the width, the 8px dot. SOLAR has no control height token; ask SOLAR for one, or confirm 20, 16, 8 is intended.                                                                                                                                                                                                                                                                                                                                                           |
| `component.statusindicator.root.paddingBottom#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind StatusIndicator / paddingBottom to inset.none, which has the same value.                                             |
| `component.statusindicator.root.paddingLeft#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind StatusIndicator / paddingLeft to inset.none, which has the same value.                                               |
| `component.statusindicator.root.paddingRight#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind StatusIndicator / paddingRight to inset.none, which has the same value.                                              |
| `component.statusindicator.root.paddingTop#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind StatusIndicator / paddingTop to inset.none, which has the same value.                                                |
| `component.statusindicator.root.width#unbound`                                                                                   | 20, 16, 8, bound to no variable                                                                                                                                          | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (allowLiteral):** The xs dot is 8px, and the icon ladder starts at 12: SOLAR has no size for it, and decided on 2026-09-25 to add none, since two components use it. Carried as a raw value. SOLAR has no control width token; ask SOLAR for one, or confirm 20, 16, 8 is intended.                                                                                                                                                                                                            |
| `component.step.line.height#unbound`                                                                                             | 3, bound to no variable                                                                                                                                                  | height on /Line is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (allowLiteral):** A horizontal step's line is 3px thick, bound to nothing: SOLAR has no line-thickness token beside its border widths (the design review's question on sizes with no variable). SOLAR has no control height token; ask SOLAR for one, or confirm 3 is intended.                                                                                                                                                                                                                |
| `component.step.root.paddingBottom#unbound`                                                                                      | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Step / paddingBottom to inset.none, which has the same value.                                                        |
| `component.step.root.paddingLeft#unbound`                                                                                        | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Step / paddingLeft to inset.none, which has the same value.                                                          |
| `component.step.root.paddingRight#unbound`                                                                                       | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Step / paddingRight to inset.none, which has the same value.                                                         |
| `component.step.root.paddingTop#unbound`                                                                                         | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Step / paddingTop to inset.none, which has the same value.                                                           |
| `component.step.stepperIndicator.height#unbound`                                                                                 | 24, bound to no variable                                                                                                                                                 | height on /Stepper Indicator is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (set):** The circle is a SOLAR Stepper Indicator, whose 24px is its own: the step places it, and never sizes it. Ask SOLAR to bind Step /Stepper Indicator height to icon.lg, which has the same value.                                                                                                                                                                                                                                                                                        |
| `component.step.stepperIndicator.width#unbound`                                                                                  | 24, bound to no variable                                                                                                                                                 | width on /Stepper Indicator is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (set):** The circle is a SOLAR Stepper Indicator, whose 24px is its own: the step places it, and never sizes it. Ask SOLAR to bind Step /Stepper Indicator width to icon.lg, which has the same value.                                                                                                                                                                                                                                                                                         |
| `component.stepper indicator.iconCheck.width#unbound`                                                                            | 16, bound to no variable                                                                                                                                                 | width on /Icon/Check is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind):** Same value, governed token; the tick's height is already bound to it. Ask SOLAR to bind Stepper Indicator /Icon/Check width to icon.sm, which has the same value.                                                                                                                                                                                                                                                                                                                    |
| `component.stepper indicator.root.gap#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | gap on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Stepper Indicator / gap to inset.none or stack.none, which has the same value.                         |
| `component.stepper indicator.root.height#unbound`                                                                                | 24, bound to no variable                                                                                                                                                 | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (allowLiteral):** The circle is 24 across, bound to nothing: SOLAR has no size token for it (the design review's question on sizes with no variable). SOLAR has no control height token; ask SOLAR for one, or confirm 24 is intended.                                                                                                                                                                                                                                                         |
| `component.stepper indicator.root.paddingBottom#unbound`                                                                         | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Stepper Indicator / paddingBottom to inset.none, which has the same value.                                           |
| `component.stepper indicator.root.paddingLeft#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Stepper Indicator / paddingLeft to inset.none, which has the same value.                                             |
| `component.stepper indicator.root.paddingRight#unbound`                                                                          | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Stepper Indicator / paddingRight to inset.none, which has the same value.                                            |
| `component.stepper indicator.root.paddingTop#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Stepper Indicator / paddingTop to inset.none, which has the same value.                                              |
| `component.stepper indicator.root.width#unbound`                                                                                 | 24, bound to no variable                                                                                                                                                 | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (allowLiteral):** The circle is 24 across, bound to nothing: SOLAR has no size token for it (the design review's question on sizes with no variable). SOLAR has no control width token; ask SOLAR for one, or confirm 24 is intended.                                                                                                                                                                                                                                                          |
| `component.stepper.frame.height#unbound`                                                                                         | 2, bound to no variable                                                                                                                                                  | height on /Frame is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (allowLiteral):** A line between two circles is 2px thick, bound to nothing (as the bar's, no token). SOLAR has no control height token; ask SOLAR for one, or confirm 2 is intended.                                                                                                                                                                                                                                                                                                          |
| `component.stepper.frame2.height#unbound`                                                                                        | 2, bound to no variable                                                                                                                                                  | height on /Frame#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (allowLiteral):** As the first line's, 2px. SOLAR has no control height token; ask SOLAR for one, or confirm 2 is intended.                                                                                                                                                                                                                                                                                                                                                                    |
| `component.stepper.progress.height#unbound`                                                                                      | 4, bound to no variable                                                                                                                                                  | height on /Progress is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (allowLiteral):** The bar is 4px thick, bound to nothing: SOLAR has no line-thickness token beside its border widths (the design review's question on sizes with no variable). SOLAR has no control height token; ask SOLAR for one, or confirm 4 is intended.                                                                                                                                                                                                                                 |
| `component.stepper.progress.paddingBottom#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Progress is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Stepper /Progress paddingBottom to inset.none, which has the same value.                                             |
| `component.stepper.progress.paddingLeft#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Progress is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Stepper /Progress paddingLeft to inset.none, which has the same value.                                               |
| `component.stepper.progress.paddingRight#unbound`                                                                                | 225, bound to no variable                                                                                                                                                | paddingRight on /Progress is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (set):** The progress bar's 225px right padding is Figma presentation only (design team, 2026-09-24), the room its sample fill leaves: not reproduced. The shell draws the fill's width from the active step. SOLAR has no paddingRight token; ask SOLAR for one, or confirm 225 is intended.                                                                                                                                                                                                  |
| `component.stepper.progress.paddingTop#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingTop on /Progress is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Stepper /Progress paddingTop to inset.none, which has the same value.                                                |
| `component.stepper.progress.width#unbound`                                                                                       | 313, bound to no variable                                                                                                                                                | width on /Progress is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (controlDraws):** The bar spans the stepper under its steps, drawn by the shell from edge to edge (the first and last circles cover its ends): Figma places it 313 wide, 18 from the right, a sample of the stepper's width. Its height, corners, colour and its place from the top are the recipe's. SOLAR has no control width token; ask SOLAR for one, or confirm 313 is intended.                                                                                                         |
| `component.stepper.progressRectangle2.height#unbound`                                                                            | 4, bound to no variable                                                                                                                                                  | height on /Progress/Rectangle 2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (allowLiteral):** As the bar's, 4px. SOLAR has no control height token; ask SOLAR for one, or confirm 4 is intended.                                                                                                                                                                                                                                                                                                                                                                           |
| `component.stepper.progressRectangle2.width#unbound`                                                                             | 153, bound to no variable                                                                                                                                                | width on /Progress/Rectangle 2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (controlDraws):** The bar's fill reaches the active step, a share of the bar the shell draws: Figma's 153 is its sample, the second of three. SOLAR has no control width token; ask SOLAR for one, or confirm 153 is intended.                                                                                                                                                                                                                                                                 |
| `component.stepper.rectangle1.height#unbound`                                                                                    | 4, bound to no variable                                                                                                                                                  | height on /Rectangle 1 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (allowLiteral):** As the progress bar's, a segment 4px thick. SOLAR has no control height token; ask SOLAR for one, or confirm 4 is intended.                                                                                                                                                                                                                                                                                                                                                  |
| `component.stepper.rectangle2.height#unbound`                                                                                    | 4, bound to no variable                                                                                                                                                  | height on /Rectangle 2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (allowLiteral):** As the progress bar's, a segment 4px thick. SOLAR has no control height token; ask SOLAR for one, or confirm 4 is intended.                                                                                                                                                                                                                                                                                                                                                  |
| `component.stepper.rectangle3.height#unbound`                                                                                    | 4, bound to no variable                                                                                                                                                  | height on /Rectangle 3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (allowLiteral):** As the progress bar's, a segment 4px thick. SOLAR has no control height token; ask SOLAR for one, or confirm 4 is intended.                                                                                                                                                                                                                                                                                                                                                  |
| `component.stepper.rectangle4.height#unbound`                                                                                    | 4, bound to no variable                                                                                                                                                  | height on /Rectangle 4 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (allowLiteral):** As the progress bar's, a segment 4px thick. SOLAR has no control height token; ask SOLAR for one, or confirm 4 is intended.                                                                                                                                                                                                                                                                                                                                                  |
| `component.stepper.rectangle5.height#unbound`                                                                                    | 4, bound to no variable                                                                                                                                                  | height on /Rectangle 5 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (allowLiteral):** As the progress bar's, a segment 4px thick. SOLAR has no control height token; ask SOLAR for one, or confirm 4 is intended.                                                                                                                                                                                                                                                                                                                                                  |
| `component.stepper.root.gap#unbound`                                                                                             | 0, bound to no variable                                                                                                                                                  | gap on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Stepper / gap to inset.none or stack.none, which has the same value.                                   |
| `component.stepper.root.height#unbound`                                                                                          | 4, bound to no variable                                                                                                                                                  | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (set):** The line stepper is as tall as its segments: Figma fixes it at their 4px, bound to nothing. SOLAR has no control height token; ask SOLAR for one, or confirm 4 is intended.                                                                                                                                                                                                                                                                                                           |
| `component.stepper.root.paddingBottom#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Stepper / paddingBottom to inset.none, which has the same value.                                                     |
| `component.stepper.root.paddingBottom@type=line`                                                                                 | 1 variant at type=line: inset.none where 0                                                                                                                               | paddingBottom on / should follow no axis only, but at type=line it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** As the first, the same 0. Ask SOLAR whether Stepper / paddingBottom is meant to change at type=line.                                                                                                                                                                                                                                                                                                                                                                               |
| `component.stepper.root.paddingBottom@type=line+text`                                                                            | 1 variant at type=line+text: inset.none where 0                                                                                                                          | paddingBottom on / should follow no axis only, but at type=line+text it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (accept):** As the first, the same 0. Ask SOLAR whether Stepper / paddingBottom is meant to change at type=line+text.                                                                                                                                                                                                                                                                                                                                                                          |
| `component.stepper.root.paddingBottom@type=no label`                                                                             | 1 variant at type=no label: inset.none where 0                                                                                                                           | paddingBottom on / should follow no axis only, but at type=no label it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** As the first, the same 0. Ask SOLAR whether Stepper / paddingBottom is meant to change at type=no label.                                                                                                                                                                                                                                                                                                                                                                           |
| `component.stepper.root.paddingLeft#unbound`                                                                                     | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Stepper / paddingLeft to inset.none, which has the same value.                                                       |
| `component.stepper.root.paddingLeft@type=line`                                                                                   | 1 variant at type=line: inset.none where 0                                                                                                                               | paddingLeft on / should follow no axis only, but at type=line it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** As the first, the same 0. Ask SOLAR whether Stepper / paddingLeft is meant to change at type=line.                                                                                                                                                                                                                                                                                                                                                                                 |
| `component.stepper.root.paddingLeft@type=line+text`                                                                              | 1 variant at type=line+text: inset.none where 0                                                                                                                          | paddingLeft on / should follow no axis only, but at type=line+text it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** As the first, the same 0. Ask SOLAR whether Stepper / paddingLeft is meant to change at type=line+text.                                                                                                                                                                                                                                                                                                                                                                            |
| `component.stepper.root.paddingLeft@type=no label`                                                                               | 1 variant at type=no label: inset.none where 0                                                                                                                           | paddingLeft on / should follow no axis only, but at type=no label it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** As the first, the same 0. Ask SOLAR whether Stepper / paddingLeft is meant to change at type=no label.                                                                                                                                                                                                                                                                                                                                                                             |
| `component.stepper.root.paddingRight#unbound`                                                                                    | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Stepper / paddingRight to inset.none, which has the same value.                                                      |
| `component.stepper.root.paddingRight@type=line`                                                                                  | 1 variant at type=line: inset.none where 0                                                                                                                               | paddingRight on / should follow no axis only, but at type=line it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** As the first, the same 0. Ask SOLAR whether Stepper / paddingRight is meant to change at type=line.                                                                                                                                                                                                                                                                                                                                                                                |
| `component.stepper.root.paddingRight@type=line+text`                                                                             | 1 variant at type=line+text: inset.none where 0                                                                                                                          | paddingRight on / should follow no axis only, but at type=line+text it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** As the first, the same 0. Ask SOLAR whether Stepper / paddingRight is meant to change at type=line+text.                                                                                                                                                                                                                                                                                                                                                                           |
| `component.stepper.root.paddingRight@type=no label`                                                                              | 1 variant at type=no label: inset.none where 0                                                                                                                           | paddingRight on / should follow no axis only, but at type=no label it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** As the first, the same 0. Ask SOLAR whether Stepper / paddingRight is meant to change at type=no label.                                                                                                                                                                                                                                                                                                                                                                            |
| `component.stepper.root.paddingTop#unbound`                                                                                      | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Stepper / paddingTop to inset.none, which has the same value.                                                        |
| `component.stepper.root.paddingTop@type=line`                                                                                    | 1 variant at type=line: inset.none where 0                                                                                                                               | paddingTop on / should follow no axis only, but at type=line it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma binds this type's padding to inset.none and leaves the with label type's an unbound 0, which the shared default binds to inset.none: the same 0. Ask SOLAR whether Stepper / paddingTop is meant to change at type=line.                                                                                                                                                                                                                                                     |
| `component.stepper.root.paddingTop@type=line+text`                                                                               | 1 variant at type=line+text: inset.none where 0                                                                                                                          | paddingTop on / should follow no axis only, but at type=line+text it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** As the first, the same 0. Ask SOLAR whether Stepper / paddingTop is meant to change at type=line+text.                                                                                                                                                                                                                                                                                                                                                                             |
| `component.stepper.root.paddingTop@type=no label`                                                                                | 1 variant at type=no label: inset.none where 0                                                                                                                           | paddingTop on / should follow no axis only, but at type=no label it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** As the first, the same 0. Ask SOLAR whether Stepper / paddingTop is meant to change at type=no label.                                                                                                                                                                                                                                                                                                                                                                              |
| `component.stepper.stepperIndicator.height#unbound`                                                                              | 24, bound to no variable                                                                                                                                                 | height on /Stepper Indicator is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (set):** A step's circle is a SOLAR Stepper Indicator, whose 24px is its own. Ask SOLAR to bind Stepper /Stepper Indicator height to icon.lg, which has the same value.                                                                                                                                                                                                                                                                                                                        |
| `component.stepper.stepperIndicator.width#unbound`                                                                               | 24, bound to no variable                                                                                                                                                 | width on /Stepper Indicator is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (set):** A step's circle is a SOLAR Stepper Indicator, whose 24px is its own. Ask SOLAR to bind Stepper /Stepper Indicator width to icon.lg, which has the same value.                                                                                                                                                                                                                                                                                                                         |
| `component.stepper.stepperIndicator2.height#unbound`                                                                             | 24, bound to no variable                                                                                                                                                 | height on /Stepper Indicator#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (set):** A step's circle is a SOLAR Stepper Indicator, whose 24px is its own. Ask SOLAR to bind Stepper /Stepper Indicator#2 height to icon.lg, which has the same value.                                                                                                                                                                                                                                                                                                                      |
| `component.stepper.stepperIndicator2.width#unbound`                                                                              | 24, bound to no variable                                                                                                                                                 | width on /Stepper Indicator#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (set):** A step's circle is a SOLAR Stepper Indicator, whose 24px is its own. Ask SOLAR to bind Stepper /Stepper Indicator#2 width to icon.lg, which has the same value.                                                                                                                                                                                                                                                                                                                       |
| `component.stepper.stepperIndicator3.height#unbound`                                                                             | 24, bound to no variable                                                                                                                                                 | height on /Stepper Indicator#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (set):** A step's circle is a SOLAR Stepper Indicator, whose 24px is its own. Ask SOLAR to bind Stepper /Stepper Indicator#3 height to icon.lg, which has the same value.                                                                                                                                                                                                                                                                                                                      |
| `component.stepper.stepperIndicator3.width#unbound`                                                                              | 24, bound to no variable                                                                                                                                                 | width on /Stepper Indicator#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (set):** A step's circle is a SOLAR Stepper Indicator, whose 24px is its own. Ask SOLAR to bind Stepper /Stepper Indicator#3 width to icon.lg, which has the same value.                                                                                                                                                                                                                                                                                                                       |
| `component.stepper.steps.paddingBottom#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Steps is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Stepper /Steps paddingBottom to inset.none, which has the same value.                                                |
| `component.stepper.steps.paddingLeft#unbound`                                                                                    | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Steps is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Stepper /Steps paddingLeft to inset.none, which has the same value.                                                  |
| `component.stepper.steps.paddingRight#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingRight on /Steps is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Stepper /Steps paddingRight to inset.none, which has the same value.                                                 |
| `component.stepper.steps.paddingTop#unbound`                                                                                     | 0, bound to no variable                                                                                                                                                  | paddingTop on /Steps is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Stepper /Steps paddingTop to inset.none, which has the same value.                                                   |
| `component.tab item.counter.height#unbound`                                                                                      | 20, bound to no variable                                                                                                                                                 | height on /Counter is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (set):** The counter is a SOLAR Counter, whose 20px pill is its own: the tab places it, and never sizes it. Ask SOLAR to bind Tab Item /Counter height to icon.md, which has the same value.                                                                                                                                                                                                                                                                                                   |
| `component.tab item.leadingIcon.width#unbound`                                                                                   | 16, 12, bound to no variable                                                                                                                                             | width on /LeadingIcon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind):** Same values, governed tokens, a step of the icon ladder per size; the icon's height is already bound to them. Ask SOLAR to bind Tab Item /LeadingIcon width to icon.sm or icon.xs, which has the same value.                                                                                                                                                                                                                                                                         |
| `component.tab item.root.height#unbound`                                                                                         | 40, 32, bound to no variable                                                                                                                                             | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind):** As Button's height: size.control, one step per size, which SOLAR added on 2026-09-25; Figma leaves the height unbound, which the design review asks SOLAR to bind. SOLAR has no control height token; ask SOLAR for one, or confirm 40, 32 is intended.                                                                                                                                                                                                                              |
| `component.tab item.trailingIcon.width#unbound`                                                                                  | 16, 12, bound to no variable                                                                                                                                             | width on /TrailingIcon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind):** As the leading icon's. Ask SOLAR to bind Tab Item /TrailingIcon width to icon.sm or icon.xs, which has the same value.                                                                                                                                                                                                                                                                                                                                                               |
| `component.table.dimming.height#unbound`                                                                                         | 640, bound to no variable                                                                                                                                                | height on /Dimming is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (set):** The mobile fade runs the table's full height, as Figma draws it (640, the table's): pinned to the table's right edge (Figma's constraint), it spans a table of any number of rows. SOLAR has no control height token; ask SOLAR for one, or confirm 640 is intended.                                                                                                                                                                                                                  |
| `component.table.dimming.width#unbound`                                                                                          | 32, 38, bound to no variable                                                                                                                                             | width on /Dimming is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (allowLiteral):** The mobile fade is 32px wide, bound to nothing: SOLAR has no size for a scroll fade. SOLAR has no control width token; ask SOLAR for one, or confirm 32, 38 is intended.                                                                                                                                                                                                                                                                                                     |
| `component.table.dimming.width@breakpoint=mobile, expandable=false, selectable=false`                                            | 1 variant at breakpoint=mobile, expandable=false, selectable=false: 38 where 32                                                                                          | width on /Dimming should follow no axis only, but at breakpoint=mobile, expandable=false, selectable=false it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** The fade is 38 wide in the mobile table with neither select nor expand cells, and 32 in the other two: a slip, the same fade at the same edge. The code draws 32 in all; the design review lists it. Ask SOLAR whether Table /Dimming width is meant to change at breakpoint=mobile, expandable=false, selectable=false.                                                                                                                                                           |
| `component.table.root.gap#unbound`                                                                                               | 0, bound to no variable                                                                                                                                                  | gap on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Table / gap to inset.none or stack.none, which has the same value.                                     |
| `component.table.root.height#unbound`                                                                                            | 640, bound to no variable                                                                                                                                                | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (set):** A table is as tall as its rows: Figma draws it 640 tall, room for its fifteen sample rows. SOLAR has no control height token; ask SOLAR for one, or confirm 640 is intended.                                                                                                                                                                                                                                                                                                          |
| `component.table.root.height@breakpoint=desktop, expandable=false, selectable=false`                                             | 1 variant at breakpoint=desktop, expandable=false, selectable=false: 640 where HUG                                                                                       | height on / should follow no axis only, but at breakpoint=desktop, expandable=false, selectable=false it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (accept):** A table is as tall as its rows: Figma draws it 640 tall, room for its fifteen sample rows. Ask SOLAR whether Table / height is meant to change at breakpoint=desktop, expandable=false, selectable=false.                                                                                                                                                                                                                                                                          |
| `component.table.root.height@breakpoint=desktop, expandable=false, selectable=true`                                              | 1 variant at breakpoint=desktop, expandable=false, selectable=true: 640 where HUG                                                                                        | height on / should follow no axis only, but at breakpoint=desktop, expandable=false, selectable=true it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (accept):** A table is as tall as its rows: Figma draws it 640 tall, room for its fifteen sample rows. Ask SOLAR whether Table / height is meant to change at breakpoint=desktop, expandable=false, selectable=true.                                                                                                                                                                                                                                                                           |
| `component.table.root.height@breakpoint=mobile, expandable=false, selectable=false`                                              | 1 variant at breakpoint=mobile, expandable=false, selectable=false: 640 where HUG                                                                                        | height on / should follow no axis only, but at breakpoint=mobile, expandable=false, selectable=false it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (accept):** A table is as tall as its rows: Figma draws it 640 tall, room for its fifteen sample rows. Ask SOLAR whether Table / height is meant to change at breakpoint=mobile, expandable=false, selectable=false.                                                                                                                                                                                                                                                                           |
| `component.table.root.height@breakpoint=mobile, expandable=false, selectable=true`                                               | 1 variant at breakpoint=mobile, expandable=false, selectable=true: 640 where HUG                                                                                         | height on / should follow no axis only, but at breakpoint=mobile, expandable=false, selectable=true it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** A table is as tall as its rows: Figma draws it 640 tall, room for its fifteen sample rows. Ask SOLAR whether Table / height is meant to change at breakpoint=mobile, expandable=false, selectable=true.                                                                                                                                                                                                                                                                            |
| `component.table.root.height@breakpoint=mobile, expandable=true, selectable=true`                                                | 1 variant at breakpoint=mobile, expandable=true, selectable=true: 640 where HUG                                                                                          | height on / should follow no axis only, but at breakpoint=mobile, expandable=true, selectable=true it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** A table is as tall as its rows: Figma draws it 640 tall, room for its fifteen sample rows. Ask SOLAR whether Table / height is meant to change at breakpoint=mobile, expandable=true, selectable=true.                                                                                                                                                                                                                                                                             |
| `component.table.root.paddingBottom#unbound`                                                                                     | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Table / paddingBottom to inset.none, which has the same value.                                                       |
| `component.table.root.paddingTop#unbound`                                                                                        | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Table / paddingTop to inset.none, which has the same value.                                                          |
| `component.table.root.width#unbound`                                                                                             | 361, bound to no variable                                                                                                                                                | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** A table spans what holds it: Figma draws it 1020 wide on desktop and 361 on mobile, each a sample of a page's width. SOLAR has no control width token; ask SOLAR for one, or confirm 361 is intended.                                                                                                                                                                                                                                                                                 |
| `component.table.root.width@breakpoint=mobile, expandable=false, selectable=false`                                               | 1 variant at breakpoint=mobile, expandable=false, selectable=false: 361 where FILL                                                                                       | width on / should follow no axis only, but at breakpoint=mobile, expandable=false, selectable=false it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (accept):** A table spans what holds it: Figma draws it 1020 wide on desktop and 361 on mobile, each a sample of a page's width. Ask SOLAR whether Table / width is meant to change at breakpoint=mobile, expandable=false, selectable=false.                                                                                                                                                                                                                                                  |
| `component.table.root.width@breakpoint=mobile, expandable=false, selectable=true`                                                | 1 variant at breakpoint=mobile, expandable=false, selectable=true: 361 where FILL                                                                                        | width on / should follow no axis only, but at breakpoint=mobile, expandable=false, selectable=true it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (accept):** A table spans what holds it: Figma draws it 1020 wide on desktop and 361 on mobile, each a sample of a page's width. Ask SOLAR whether Table / width is meant to change at breakpoint=mobile, expandable=false, selectable=true.                                                                                                                                                                                                                                                   |
| `component.table.root.width@breakpoint=mobile, expandable=true, selectable=true`                                                 | 1 variant at breakpoint=mobile, expandable=true, selectable=true: 361 where FILL                                                                                         | width on / should follow no axis only, but at breakpoint=mobile, expandable=true, selectable=true it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (accept):** A table spans what holds it: Figma draws it 1020 wide on desktop and 361 on mobile, each a sample of a page's width. Ask SOLAR whether Table / width is meant to change at breakpoint=mobile, expandable=true, selectable=true.                                                                                                                                                                                                                                                    |
| `component.table.row.height#unbound`                                                                                             | 40, bound to no variable                                                                                                                                                 | height on /Rows/Row is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (set):** A row is a SOLAR Row, whose height is its own, 44 for a data row: Figma resizes the table's rows to 40, where Row draws them 44, and the code draws Row's. The design review asks which is meant. SOLAR has no control height token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                    |
| `component.table.rows.height#unbound`                                                                                            | 600, bound to no variable                                                                                                                                                | height on /Rows is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (set):** A table is as tall as its rows: Figma draws it 640 tall, room for its fifteen sample rows. SOLAR has no control height token; ask SOLAR for one, or confirm 600 is intended.                                                                                                                                                                                                                                                                                                          |
| `component.table.rows.height@breakpoint=desktop, expandable=false, selectable=false`                                             | 1 variant at breakpoint=desktop, expandable=false, selectable=false: FILL where 600                                                                                      | height on /Rows should follow no axis only, but at breakpoint=desktop, expandable=false, selectable=false it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (accept):** A table is as tall as its rows: Figma draws it 640 tall, room for its fifteen sample rows. Ask SOLAR whether Table /Rows height is meant to change at breakpoint=desktop, expandable=false, selectable=false.                                                                                                                                                                                                                                                                      |
| `component.table.rows.height@breakpoint=desktop, expandable=false, selectable=true`                                              | 1 variant at breakpoint=desktop, expandable=false, selectable=true: FILL where 600                                                                                       | height on /Rows should follow no axis only, but at breakpoint=desktop, expandable=false, selectable=true it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (accept):** A table is as tall as its rows: Figma draws it 640 tall, room for its fifteen sample rows. Ask SOLAR whether Table /Rows height is meant to change at breakpoint=desktop, expandable=false, selectable=true.                                                                                                                                                                                                                                                                       |
| `component.table.rows.height@breakpoint=mobile, expandable=false, selectable=false`                                              | 1 variant at breakpoint=mobile, expandable=false, selectable=false: FILL where 600                                                                                       | height on /Rows should follow no axis only, but at breakpoint=mobile, expandable=false, selectable=false it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (accept):** A table is as tall as its rows: Figma draws it 640 tall, room for its fifteen sample rows. Ask SOLAR whether Table /Rows height is meant to change at breakpoint=mobile, expandable=false, selectable=false.                                                                                                                                                                                                                                                                       |
| `component.table.rows.height@breakpoint=mobile, expandable=false, selectable=true`                                               | 1 variant at breakpoint=mobile, expandable=false, selectable=true: FILL where 600                                                                                        | height on /Rows should follow no axis only, but at breakpoint=mobile, expandable=false, selectable=true it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (accept):** A table is as tall as its rows: Figma draws it 640 tall, room for its fifteen sample rows. Ask SOLAR whether Table /Rows height is meant to change at breakpoint=mobile, expandable=false, selectable=true.                                                                                                                                                                                                                                                                        |
| `component.table.rows.height@breakpoint=mobile, expandable=true, selectable=true`                                                | 1 variant at breakpoint=mobile, expandable=true, selectable=true: FILL where 600                                                                                         | height on /Rows should follow no axis only, but at breakpoint=mobile, expandable=true, selectable=true it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (accept):** A table is as tall as its rows: Figma draws it 640 tall, room for its fifteen sample rows. Ask SOLAR whether Table /Rows height is meant to change at breakpoint=mobile, expandable=true, selectable=true.                                                                                                                                                                                                                                                                         |
| `component.table.rows.paddingBottom#unbound`                                                                                     | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Rows is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Table /Rows paddingBottom to inset.none, which has the same value.                                                   |
| `component.table.rows.paddingLeft#unbound`                                                                                       | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Rows is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Table /Rows paddingLeft to inset.none, which has the same value.                                                     |
| `component.table.rows.paddingRight#unbound`                                                                                      | 0, bound to no variable                                                                                                                                                  | paddingRight on /Rows is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Table /Rows paddingRight to inset.none, which has the same value.                                                    |
| `component.table.rows.paddingTop#unbound`                                                                                        | 0, bound to no variable                                                                                                                                                  | paddingTop on /Rows is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Table /Rows paddingTop to inset.none, which has the same value.                                                      |
| `component.tablefooter.button.height#unbound`                                                                                    | 40, bound to no variable                                                                                                                                                 | height on /Container/Button is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (set):** The action is a SOLAR Button (md), whose 40px height is its own: the strip places it, and never sizes it. SOLAR has no control height token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                                                                            |
| `component.tablefooter.container.paddingBottom#unbound`                                                                          | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Container is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind TableFooter /Container paddingBottom to inset.none, which has the same value.                                        |
| `component.tablefooter.container.paddingLeft#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Container is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind TableFooter /Container paddingLeft to inset.none, which has the same value.                                          |
| `component.tablefooter.container.paddingRight#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingRight on /Container is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind TableFooter /Container paddingRight to inset.none, which has the same value.                                         |
| `component.tablefooter.container.paddingTop#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingTop on /Container is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind TableFooter /Container paddingTop to inset.none, which has the same value.                                           |
| `component.tablefooter.containerContainer.paddingBottom#unbound`                                                                 | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Container/Container is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind TableFooter /Container/Container paddingBottom to inset.none, which has the same value.                              |
| `component.tablefooter.containerContainer.paddingLeft#unbound`                                                                   | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Container/Container is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind TableFooter /Container/Container paddingLeft to inset.none, which has the same value.                                |
| `component.tablefooter.containerContainer.paddingRight#unbound`                                                                  | 0, bound to no variable                                                                                                                                                  | paddingRight on /Container/Container is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind TableFooter /Container/Container paddingRight to inset.none, which has the same value.                               |
| `component.tablefooter.containerContainer.paddingTop#unbound`                                                                    | 0, bound to no variable                                                                                                                                                  | paddingTop on /Container/Container is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind TableFooter /Container/Container paddingTop to inset.none, which has the same value.                                 |
| `component.tablefooter.iconButton.height#unbound`                                                                                | 40, bound to no variable                                                                                                                                                 | height on /Icon Button is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (set):** On mobile the action is a SOLAR Icon Button (md), whose 40px box is its own: the strip places it, and never sizes it. SOLAR has no control height token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                                                                |
| `component.tablefooter.iconButton.width#unbound`                                                                                 | 40, bound to no variable                                                                                                                                                 | width on /Icon Button is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (set):** On mobile the action is a SOLAR Icon Button (md), whose 40px box is its own: the strip places it, and never sizes it. SOLAR has no control width token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                                                                 |
| `component.tablefooter.root.gap#unbound`                                                                                         | 0, bound to no variable                                                                                                                                                  | gap on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind TableFooter / gap to inset.none or stack.none, which has the same value.                               |
| `component.tablefooter.root.width#unbound`                                                                                       | 377, bound to no variable                                                                                                                                                | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** The strip spans its table, as Figma's does (FILL on desktop): the mobile one is fixed at 377, a sample of a phone's width. SOLAR has no control width token; ask SOLAR for one, or confirm 377 is intended.                                                                                                                                                                                                                                                                           |
| `component.tablefooter.root.width@breakpoint=mobile`                                                                             | 1 variant at breakpoint=mobile: 377 where FILL                                                                                                                           | width on / should follow no axis only, but at breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** The strip spans its table, as Figma's does (FILL on desktop): the mobile one is fixed at 377, a sample of a phone's width. Ask SOLAR whether TableFooter / width is meant to change at breakpoint=mobile.                                                                                                                                                                                                                                                                          |
| `component.tableheader.actions.paddingBottom#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Container/Actions is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind TableHeader /Container/Actions paddingBottom to inset.none, which has the same value.                                |
| `component.tableheader.actions.paddingLeft#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Container/Actions is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind TableHeader /Container/Actions paddingLeft to inset.none, which has the same value.                                  |
| `component.tableheader.actions.paddingRight#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingRight on /Container/Actions is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind TableHeader /Container/Actions paddingRight to inset.none, which has the same value.                                 |
| `component.tableheader.actions.paddingTop#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingTop on /Container/Actions is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind TableHeader /Container/Actions paddingTop to inset.none, which has the same value.                                   |
| `component.tableheader.actions.width#unbound`                                                                                    | 240, bound to no variable                                                                                                                                                | width on /Container/Actions is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (set):** The actions are as wide as the caller's buttons, packed to the strip's end: Figma fixes the frame at 240 for its three samples. SOLAR has no control width token; ask SOLAR for one, or confirm 240 is intended.                                                                                                                                                                                                                                                                      |
| `component.tableheader.actionsIconButton.height#unbound`                                                                         | 40, bound to no variable                                                                                                                                                 | height on /Actions/Icon Button is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (set):** Each action is the caller's SOLAR Icon Button (md, square, secondary), whose 40px box is its own: the strip places it, and never sizes it. SOLAR has no control height token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                                           |
| `component.tableheader.actionsIconButton.width#unbound`                                                                          | 40, bound to no variable                                                                                                                                                 | width on /Actions/Icon Button is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (set):** Each action is the caller's SOLAR Icon Button (md, square, secondary), whose 40px box is its own: the strip places it, and never sizes it. SOLAR has no control width token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                                            |
| `component.tableheader.actionsIconButton2.height#unbound`                                                                        | 40, bound to no variable                                                                                                                                                 | height on /Actions/Icon Button#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (set):** Each action is the caller's SOLAR Icon Button (md, square, secondary), whose 40px box is its own: the strip places it, and never sizes it. SOLAR has no control height token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                                           |
| `component.tableheader.actionsIconButton2.width#unbound`                                                                         | 40, bound to no variable                                                                                                                                                 | width on /Actions/Icon Button#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (set):** Each action is the caller's SOLAR Icon Button (md, square, secondary), whose 40px box is its own: the strip places it, and never sizes it. SOLAR has no control width token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                                            |
| `component.tableheader.actionsIconButton3.height#unbound`                                                                        | 40, bound to no variable                                                                                                                                                 | height on /Actions/Icon Button#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (set):** Each action is the caller's SOLAR Icon Button (md, square, secondary), whose 40px box is its own: the strip places it, and never sizes it. SOLAR has no control height token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                                           |
| `component.tableheader.actionsIconButton3.width#unbound`                                                                         | 40, bound to no variable                                                                                                                                                 | width on /Actions/Icon Button#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (set):** Each action is the caller's SOLAR Icon Button (md, square, secondary), whose 40px box is its own: the strip places it, and never sizes it. SOLAR has no control width token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                                            |
| `component.tableheader.actionsMobile.paddingBottom#unbound`                                                                      | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Actions is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind TableHeader /Actions paddingBottom to inset.none, which has the same value.                                          |
| `component.tableheader.actionsMobile.paddingLeft#unbound`                                                                        | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Actions is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind TableHeader /Actions paddingLeft to inset.none, which has the same value.                                            |
| `component.tableheader.actionsMobile.paddingRight#unbound`                                                                       | 0, bound to no variable                                                                                                                                                  | paddingRight on /Actions is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind TableHeader /Actions paddingRight to inset.none, which has the same value.                                           |
| `component.tableheader.actionsMobile.paddingTop#unbound`                                                                         | 0, bound to no variable                                                                                                                                                  | paddingTop on /Actions is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind TableHeader /Actions paddingTop to inset.none, which has the same value.                                             |
| `component.tableheader.container.paddingBottom#unbound`                                                                          | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Container is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind TableHeader /Container paddingBottom to inset.none, which has the same value.                                        |
| `component.tableheader.container.paddingLeft#unbound`                                                                            | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Container is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind TableHeader /Container paddingLeft to inset.none, which has the same value.                                          |
| `component.tableheader.container.paddingRight#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingRight on /Container is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind TableHeader /Container paddingRight to inset.none, which has the same value.                                         |
| `component.tableheader.container.paddingTop#unbound`                                                                             | 0, bound to no variable                                                                                                                                                  | paddingTop on /Container is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind TableHeader /Container paddingTop to inset.none, which has the same value.                                           |
| `component.tableheader.containerActionsIconButton.height#unbound`                                                                | 40, bound to no variable                                                                                                                                                 | height on /Container/Actions/Icon Button is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (set):** Each action is the caller's SOLAR Icon Button (md, square, secondary), whose 40px box is its own: the strip places it, and never sizes it. SOLAR has no control height token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                                           |
| `component.tableheader.containerActionsIconButton.width#unbound`                                                                 | 40, bound to no variable                                                                                                                                                 | width on /Container/Actions/Icon Button is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (set):** Each action is the caller's SOLAR Icon Button (md, square, secondary), whose 40px box is its own: the strip places it, and never sizes it. SOLAR has no control width token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                                            |
| `component.tableheader.containerActionsIconButton2.height#unbound`                                                               | 40, bound to no variable                                                                                                                                                 | height on /Container/Actions/Icon Button#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** Each action is the caller's SOLAR Icon Button (md, square, secondary), whose 40px box is its own: the strip places it, and never sizes it. SOLAR has no control height token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                                           |
| `component.tableheader.containerActionsIconButton2.width#unbound`                                                                | 40, bound to no variable                                                                                                                                                 | width on /Container/Actions/Icon Button#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (set):** Each action is the caller's SOLAR Icon Button (md, square, secondary), whose 40px box is its own: the strip places it, and never sizes it. SOLAR has no control width token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                                            |
| `component.tableheader.containerActionsIconButton3.height#unbound`                                                               | 40, bound to no variable                                                                                                                                                 | height on /Container/Actions/Icon Button#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** Each action is the caller's SOLAR Icon Button (md, square, secondary), whose 40px box is its own: the strip places it, and never sizes it. SOLAR has no control height token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                                           |
| `component.tableheader.containerActionsIconButton3.width#unbound`                                                                | 40, bound to no variable                                                                                                                                                 | width on /Container/Actions/Icon Button#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (set):** Each action is the caller's SOLAR Icon Button (md, square, secondary), whose 40px box is its own: the strip places it, and never sizes it. SOLAR has no control width token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                                            |
| `component.tableheader.root.width#unbound`                                                                                       | 377, bound to no variable                                                                                                                                                | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** The strip spans its table, as Figma's does (FILL on desktop): the mobile one is fixed at 377, a sample of a phone's width. SOLAR has no control width token; ask SOLAR for one, or confirm 377 is intended.                                                                                                                                                                                                                                                                           |
| `component.tableheader.root.width@breakpoint=mobile`                                                                             | 1 variant at breakpoint=mobile: 377 where FILL                                                                                                                           | width on / should follow no axis only, but at breakpoint=mobile it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** The strip spans its table, as Figma's does (FILL on desktop): the mobile one is fixed at 377, a sample of a phone's width. Ask SOLAR whether TableHeader / width is meant to change at breakpoint=mobile.                                                                                                                                                                                                                                                                          |
| `component.tableheader.search.height#unbound`                                                                                    | 40, bound to no variable                                                                                                                                                 | height on /Container/SearchField is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (set):** The search is a SOLAR SearchField, whose 40px height at md is its own: the strip places it, and never sizes it. SOLAR has no control height token; ask SOLAR for one, or confirm 40 is intended.                                                                                                                                                                                                                                                                                      |
| `component.tableheader.search.width#unbound`                                                                                     | 240, bound to no variable                                                                                                                                                | width on /Container/SearchField is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (allowLiteral):** The toolbar's search is 240 wide, bound to nothing: a field fills what holds it, and SOLAR has no width for a toolbar's field; carried as Figma's number, the sizes with no variable. SOLAR has no control width token; ask SOLAR for one, or confirm 240 is intended.                                                                                                                                                                                                       |
| `component.tabs.root.gap#unbound`                                                                                                | 0, bound to no variable                                                                                                                                                  | gap on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Tabs / gap to inset.none or stack.none, which has the same value.                                      |
| `component.tabs.root.paddingBottom#unbound`                                                                                      | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Tabs / paddingBottom to inset.none, which has the same value.                                                        |
| `component.tabs.root.paddingTop#unbound`                                                                                         | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Tabs / paddingTop to inset.none, which has the same value.                                                           |
| `component.tabs.tabItem.height#unbound`                                                                                          | 32, 40, bound to no variable                                                                                                                                             | height on /Tabs/Tab Item is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (set):** As sm's, its own. Ask SOLAR to bind Tabs /Tabs/Tab Item height to icon.2xl, which has the same value.                                                                                                                                                                                                                                                                                                                                                                                 |
| `component.tabs.tabItem2.height#unbound`                                                                                         | 32, 40, bound to no variable                                                                                                                                             | height on /Tabs/Tab Item#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** As sm's, its own. Ask SOLAR to bind Tabs /Tabs/Tab Item#2 height to icon.2xl, which has the same value.                                                                                                                                                                                                                                                                                                                                                                               |
| `component.tabs.tabItem3.height#unbound`                                                                                         | 32, 40, bound to no variable                                                                                                                                             | height on /Tabs/Tab Item#3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** As sm's, its own. Ask SOLAR to bind Tabs /Tabs/Tab Item#3 height to icon.2xl, which has the same value.                                                                                                                                                                                                                                                                                                                                                                               |
| `component.tabs.tabItem4.height#unbound`                                                                                         | 32, 40, bound to no variable                                                                                                                                             | height on /Tabs/Tab Item#4 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** As sm's, its own. Ask SOLAR to bind Tabs /Tabs/Tab Item#4 height to icon.2xl, which has the same value.                                                                                                                                                                                                                                                                                                                                                                               |
| `component.tabs.tabItem5.height#unbound`                                                                                         | 32, 40, bound to no variable                                                                                                                                             | height on /Tabs/Tab Item#5 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** As sm's, its own. Ask SOLAR to bind Tabs /Tabs/Tab Item#5 height to icon.2xl, which has the same value.                                                                                                                                                                                                                                                                                                                                                                               |
| `component.tabs.tabItem6.height#unbound`                                                                                         | 32, 40, bound to no variable                                                                                                                                             | height on /Tabs/Tab Item#6 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** As sm's, its own. Ask SOLAR to bind Tabs /Tabs/Tab Item#6 height to icon.2xl, which has the same value.                                                                                                                                                                                                                                                                                                                                                                               |
| `component.tabs.tabItem7.height#unbound`                                                                                         | 32, 40, bound to no variable                                                                                                                                             | height on /Tabs/Tab Item#7 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** As sm's, its own. Ask SOLAR to bind Tabs /Tabs/Tab Item#7 height to icon.2xl, which has the same value.                                                                                                                                                                                                                                                                                                                                                                               |
| `component.tabs.tabItem8.height#unbound`                                                                                         | 32, 40, bound to no variable                                                                                                                                             | height on /Tabs/Tab Item#8 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** As sm's, its own. Ask SOLAR to bind Tabs /Tabs/Tab Item#8 height to icon.2xl, which has the same value.                                                                                                                                                                                                                                                                                                                                                                               |
| `component.tabs.tabs.gap#unbound`                                                                                                | 0, bound to no variable                                                                                                                                                  | gap on /Tabs is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Tabs /Tabs gap to inset.none or stack.none, which has the same value.                                  |
| `component.tabs.tabs.paddingBottom#unbound`                                                                                      | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Tabs is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Tabs /Tabs paddingBottom to inset.none, which has the same value.                                                    |
| `component.tabs.tabs.paddingLeft#unbound`                                                                                        | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Tabs is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Tabs /Tabs paddingLeft to inset.none, which has the same value.                                                      |
| `component.tabs.tabs.paddingRight#unbound`                                                                                       | 0, bound to no variable                                                                                                                                                  | paddingRight on /Tabs is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Tabs /Tabs paddingRight to inset.none, which has the same value.                                                     |
| `component.tabs.tabs.paddingTop#unbound`                                                                                         | 0, bound to no variable                                                                                                                                                  | paddingTop on /Tabs is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Tabs /Tabs paddingTop to inset.none, which has the same value.                                                       |
| `component.tag.icon.width#unbound`                                                                                               | 12, bound to no variable                                                                                                                                                 | width on /Icon/Plus is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (bind):** Same value, governed token; the icon's height is already bound to it ("icon size tracks text size"). Ask SOLAR to bind Tag /Icon/Plus width to icon.xs, which has the same value.                                                                                                                                                                                                                                                                                                    |
| `component.tag.iconClose.width#unbound`                                                                                          | 12, 16, bound to no variable                                                                                                                                             | width on /Icon/Close is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind):** As the icon's, a step of the ladder for each close button; its height is bound already. Ask SOLAR to bind Tag /Icon/Close width to icon.xs or icon.sm, which has the same value.                                                                                                                                                                                                                                                                                                     |
| `component.tag.iconNone.width#unbound`                                                                                           | 12, bound to no variable                                                                                                                                                 | width on /Icon/None is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (bind):** As the icon-only tag's icon. Ask SOLAR to bind Tag /Icon/None width to icon.xs, which has the same value.                                                                                                                                                                                                                                                                                                                                                                            |
| `component.tag.root.height#unbound`                                                                                              | 24, bound to no variable                                                                                                                                                 | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (allowLiteral):** An icon-only tag is a 24px square, and every tag 24 tall, bound to nothing: SOLAR has no control height token (the design review's question on control heights, as Button's is). SOLAR has no control height token; ask SOLAR for one, or confirm 24 is intended.                                                                                                                                                                                                            |
| `component.tag.root.width#unbound`                                                                                               | 24, bound to no variable                                                                                                                                                 | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (allowLiteral):** An icon-only tag is a 24px square, and every tag 24 tall, bound to nothing: SOLAR has no control height token (the design review's question on control heights, as Button's is). SOLAR has no control width token; ask SOLAR for one, or confirm 24 is intended.                                                                                                                                                                                                             |
| `component.tag.sparse@invert=true`                                                                                               | no variant with invert=true and status=success, type=status                                                                                                              | Tag has no variant that holds status, type at the default for invert=true, so that combination is read from status=success, type=closable, invert=true instead.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (accept):** Figma draws no inverted status tag: its dot is the status's strong colour, which is the inverted tag's fill, so it would not show. The shells refuse an inverted tag with a dot (the React types, and an assert in Flutter), as Button Group's refuse the vertical full-width group Figma does not draw. Ask SOLAR whether Tag should have the variant with invert=true at the default status, type.                                                                               |
| `component.tag.statusIndicator.height#unbound`                                                                                   | 8, bound to no variable                                                                                                                                                  | height on /StatusIndicator is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** As its width, the StatusIndicator's own. SOLAR has no control height token; ask SOLAR for one, or confirm 8 is intended.                                                                                                                                                                                                                                                                                                                                                              |
| `component.tag.statusIndicator.width#unbound`                                                                                    | 8, bound to no variable                                                                                                                                                  | width on /StatusIndicator is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (set):** The dot is a StatusIndicator, whose xs size is its own (8px): the tag places it, and never sizes it. SOLAR has no control width token; ask SOLAR for one, or confirm 8 is intended.                                                                                                                                                                                                                                                                                                   |
| `component.text area.attachment.height#unbound`                                                                                  | 32, bound to no variable                                                                                                                                                 | height on /Field/Icon Button#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (allowLiteral):** As the send button's. Ask SOLAR to bind Text Area /Field/Icon Button#2 height to icon.2xl, which has the same value.                                                                                                                                                                                                                                                                                                                                                         |
| `component.text area.attachment.width#unbound`                                                                                   | 32, bound to no variable                                                                                                                                                 | width on /Field/Icon Button#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (allowLiteral):** As the send button's. Ask SOLAR to bind Text Area /Field/Icon Button#2 width to icon.2xl, which has the same value.                                                                                                                                                                                                                                                                                                                                                          |
| `component.text area.cta.height#unbound`                                                                                         | 32, bound to no variable                                                                                                                                                 | height on /Field/Icon Button is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (allowLiteral):** The buttons are the caller's Icon Buttons, sm as Figma draws them: 32 square, which is where the field places them, bound to nothing (SOLAR has no control size token, the design review's question on control sizes). Each is drawn at its own size, centred in that box, so a touch target around it reaches past the box rather than moving it. Ask SOLAR to bind Text Area /Field/Icon Button height to icon.2xl, which has the same value.                              |
| `component.text area.cta.width#unbound`                                                                                          | 32, bound to no variable                                                                                                                                                 | width on /Field/Icon Button is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (allowLiteral):** The buttons are the caller's Icon Buttons, sm as Figma draws them: 32 square, which is where the field places them, bound to nothing (SOLAR has no control size token, the design review's question on control sizes). Each is drawn at its own size, centred in that box, so a touch target around it reaches past the box rather than moving it. Ask SOLAR to bind Text Area /Field/Icon Button width to icon.2xl, which has the same value.                               |
| `component.text area.field.gap#unbound`                                                                                          | 0, bound to no variable                                                                                                                                                  | gap on /Field is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Text Area /Field gap to inset.none or stack.none, which has the same value.                            |
| `component.text area.field.height#unbound`                                                                                       | 120, 100, bound to no variable                                                                                                                                           | height on /Field is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (allowLiteral):** Drawn 120 (md) and 100 (sm), bound to nothing: taller than SOLAR's control heights (size.control, 32 to 48), so carried as drawn; the words scroll within it. SOLAR has no control height token; ask SOLAR for one, or confirm 120, 100 is intended.                                                                                                                                                                                                                         |
| `component.text area.footer.paddingBottom#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Footer is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Text Area /Footer paddingBottom to inset.none, which has the same value.                                             |
| `component.text area.footer.paddingLeft#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Footer is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Text Area /Footer paddingLeft to inset.none, which has the same value.                                               |
| `component.text area.footer.paddingRight#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingRight on /Footer is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Text Area /Footer paddingRight to inset.none, which has the same value.                                              |
| `component.text area.footer.paddingTop#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingTop on /Footer is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Text Area /Footer paddingTop to inset.none, which has the same value.                                                |
| `component.text area.label.paddingBottom#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Text Area /Label paddingBottom to inset.none, which has the same value.                                              |
| `component.text area.label.paddingLeft#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Text Area /Label paddingLeft to inset.none, which has the same value.                                                |
| `component.text area.label.paddingRight#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingRight on /Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Text Area /Label paddingRight to inset.none, which has the same value.                                               |
| `component.text area.label.paddingTop#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingTop on /Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Text Area /Label paddingTop to inset.none, which has the same value.                                                 |
| `component.text area.root.paddingBottom#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Text Area / paddingBottom to inset.none, which has the same value.                                                   |
| `component.text area.root.paddingLeft#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Text Area / paddingLeft to inset.none, which has the same value.                                                     |
| `component.text area.root.paddingRight#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Text Area / paddingRight to inset.none, which has the same value.                                                    |
| `component.text area.root.paddingTop#unbound`                                                                                    | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Text Area / paddingTop to inset.none, which has the same value.                                                      |
| `component.text area.root.width#unbound`                                                                                         | 280, 200, bound to no variable                                                                                                                                           | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** As md's, the sm field's 200 a sample. SOLAR has no control width token; ask SOLAR for one, or confirm 280, 200 is intended.                                                                                                                                                                                                                                                                                                                                                           |
| `component.text input.field.height#unbound`                                                                                      | 40, 32, bound to no variable                                                                                                                                             | height on /Field is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind):** As Button's height: size.control, one step per size, which SOLAR added on 2026-09-25; Figma leaves the height unbound, which the design review asks SOLAR to bind. SOLAR has no control height token; ask SOLAR for one, or confirm 40, 32 is intended.                                                                                                                                                                                                                              |
| `component.text input.label.paddingBottom#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Text Input /Label paddingBottom to inset.none, which has the same value.                                             |
| `component.text input.label.paddingLeft#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Text Input /Label paddingLeft to inset.none, which has the same value.                                               |
| `component.text input.label.paddingRight#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingRight on /Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Text Input /Label paddingRight to inset.none, which has the same value.                                              |
| `component.text input.label.paddingTop#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingTop on /Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Text Input /Label paddingTop to inset.none, which has the same value.                                                |
| `component.text input.leadingIcon.width#unbound`                                                                                 | 20, 16, bound to no variable                                                                                                                                             | width on /Field/Icon/None is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind):** Same values, governed tokens, a step of the icon ladder per size; the icon's height is already bound to them. Ask SOLAR to bind Text Input /Field/Icon/None width to icon.md or icon.sm, which has the same value.                                                                                                                                                                                                                                                                   |
| `component.text input.root.paddingBottom#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Text Input / paddingBottom to inset.none, which has the same value.                                                  |
| `component.text input.root.paddingLeft#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Text Input / paddingLeft to inset.none, which has the same value.                                                    |
| `component.text input.root.paddingRight#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Text Input / paddingRight to inset.none, which has the same value.                                                   |
| `component.text input.root.paddingTop#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Text Input / paddingTop to inset.none, which has the same value.                                                     |
| `component.text input.root.width#unbound`                                                                                        | 240, 160, bound to no variable                                                                                                                                           | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** As md's, the sm field's 160 a sample. SOLAR has no control width token; ask SOLAR for one, or confirm 240, 160 is intended.                                                                                                                                                                                                                                                                                                                                                           |
| `component.text input.trailingIcon.width#unbound`                                                                                | 20, 16, bound to no variable                                                                                                                                             | width on /Field/Icon/None#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind):** As the leading icon's. Ask SOLAR to bind Text Input /Field/Icon/None#2 width to icon.md or icon.sm, which has the same value.                                                                                                                                                                                                                                                                                                                                                        |
| `component.time axis label.root.gap#unbound`                                                                                     | 0, bound to no variable                                                                                                                                                  | gap on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Time Axis Label / gap to inset.none or stack.none, which has the same value.                           |
| `component.time axis label.root.height#unbound`                                                                                  | 48, 32, bound to no variable                                                                                                                                             | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (allowLiteral):** An hour's row is 48 tall at the comfortable density and 32 at compact, bound to nothing: SOLAR has no size for it, as the Time Slot's. SOLAR has no control height token; ask SOLAR for one, or confirm 48, 32 is intended.                                                                                                                                                                                                                                                  |
| `component.time axis label.root.paddingBottom#unbound`                                                                           | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Time Axis Label / paddingBottom to inset.none, which has the same value.                                             |
| `component.time axis label.root.paddingTop#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Time Axis Label / paddingTop to inset.none, which has the same value.                                                |
| `component.time axis label.root.width#unbound`                                                                                   | 56, bound to no variable                                                                                                                                                 | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (allowLiteral):** The time rail is 56 wide, bound to nothing: SOLAR has no size for it, one of the sizes with no variable (a calendar's). SOLAR has no control width token; ask SOLAR for one, or confirm 56 is intended.                                                                                                                                                                                                                                                                      |
| `component.time slot.halfHourRule.borderTopWidth#unbound`                                                                        | 1, bound to no variable                                                                                                                                                  | borderTopWidth on /Half-hour rule is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind):** Same value, governed token, the half-hour rule's hairline, as the slot's own edges. Ask SOLAR to bind Time Slot /Half-hour rule borderTopWidth to border.default, which has the same value.                                                                                                                                                                                                                                                                                          |
| `component.time slot.halfHourRule.height#unbound`                                                                                | 1, bound to no variable                                                                                                                                                  | height on /Half-hour rule is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind):** The rule is as tall as its hairline, border.default, the same value. SOLAR has no control height token; ask SOLAR for one, or confirm 1 is intended.                                                                                                                                                                                                                                                                                                                                 |
| `component.time slot.halfHourRule.width#unbound`                                                                                 | 160, bound to no variable                                                                                                                                                | width on /Half-hour rule is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (set):** The half-hour rule runs the slot's width, its dashes along it, as the slot fills its column: Figma draws it 160, the sample slot's width. SOLAR has no control width token; ask SOLAR for one, or confirm 160 is intended.                                                                                                                                                                                                                                                            |
| `component.time slot.root.height#unbound`                                                                                        | 48, 32, bound to no variable                                                                                                                                             | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (allowLiteral):** An hour's row is 48 tall at the comfortable density and 32 at compact, bound to nothing: SOLAR has no size for it, one of the sizes with no variable (a calendar's row). SOLAR has no control height token; ask SOLAR for one, or confirm 48, 32 is intended.                                                                                                                                                                                                                |
| `component.time slot.root.width#unbound`                                                                                         | 160, bound to no variable                                                                                                                                                | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** A slot fills its day's column of the grid, as a day fills the month grid's: Figma draws a 160 sample. SOLAR has no control width token; ask SOLAR for one, or confirm 160 is intended.                                                                                                                                                                                                                                                                                                |
| `component.timepicker dropdown.content.paddingBottom#unbound`                                                                    | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind TimePicker Dropdown /Content paddingBottom to inset.none, which has the same value.                                  |
| `component.timepicker dropdown.content.paddingLeft#unbound`                                                                      | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind TimePicker Dropdown /Content paddingLeft to inset.none, which has the same value.                                    |
| `component.timepicker dropdown.content.paddingRight#unbound`                                                                     | 0, bound to no variable                                                                                                                                                  | paddingRight on /Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind TimePicker Dropdown /Content paddingRight to inset.none, which has the same value.                                   |
| `component.timepicker dropdown.content.paddingTop#unbound`                                                                       | 0, bound to no variable                                                                                                                                                  | paddingTop on /Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind TimePicker Dropdown /Content paddingTop to inset.none, which has the same value.                                     |
| `component.timepicker dropdown.root.gap#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | gap on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind TimePicker Dropdown / gap to inset.none or stack.none, which has the same value.                       |
| `component.timepicker dropdown.root.width#unbound`                                                                               | 136, bound to no variable                                                                                                                                                | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** As a Dropdown Menu, as wide as its widest row, a time as the locale writes it: Figma draws it 136 wide, bound to no variable, a sample of the panel under a field. SOLAR has no control width token; ask SOLAR for one, or confirm 136 is intended.                                                                                                                                                                                                                                   |
| `component.timepicker.field.height#unbound`                                                                                      | 40, 32, bound to no variable                                                                                                                                             | height on /Field is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind):** As Button's height: size.control, one step per size, which SOLAR added on 2026-09-25; Figma leaves the height unbound, which the design review asks SOLAR to bind. SOLAR has no control height token; ask SOLAR for one, or confirm 40, 32 is intended.                                                                                                                                                                                                                              |
| `component.timepicker.iconClock.width#unbound`                                                                                   | 16, 12, bound to no variable                                                                                                                                             | width on /Field/Icon/Clock is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (bind):** Same values, governed tokens, a step of the icon ladder per size; the icon's height is already bound to them. Ask SOLAR to bind TimePicker /Field/Icon/Clock width to icon.sm or icon.xs, which has the same value.                                                                                                                                                                                                                                                                  |
| `component.timepicker.label.paddingBottom#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind TimePicker /Label paddingBottom to inset.none, which has the same value.                                             |
| `component.timepicker.label.paddingLeft#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind TimePicker /Label paddingLeft to inset.none, which has the same value.                                               |
| `component.timepicker.label.paddingRight#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingRight on /Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind TimePicker /Label paddingRight to inset.none, which has the same value.                                              |
| `component.timepicker.label.paddingTop#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingTop on /Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind TimePicker /Label paddingTop to inset.none, which has the same value.                                                |
| `component.timepicker.root.paddingBottom#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind TimePicker / paddingBottom to inset.none, which has the same value.                                                  |
| `component.timepicker.root.paddingLeft#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind TimePicker / paddingLeft to inset.none, which has the same value.                                                    |
| `component.timepicker.root.paddingRight#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind TimePicker / paddingRight to inset.none, which has the same value.                                                   |
| `component.timepicker.root.paddingTop#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind TimePicker / paddingTop to inset.none, which has the same value.                                                     |
| `component.timestamp.root.paddingBottom#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Timestamp / paddingBottom to inset.none, which has the same value.                                                   |
| `component.timestamp.root.paddingLeft#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Timestamp / paddingLeft to inset.none, which has the same value.                                                     |
| `component.timestamp.root.paddingRight#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Timestamp / paddingRight to inset.none, which has the same value.                                                    |
| `component.timestamp.root.paddingTop#unbound`                                                                                    | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Timestamp / paddingTop to inset.none, which has the same value.                                                      |
| `component.toast.chevron.width#unbound`                                                                                          | 12, bound to no variable                                                                                                                                                 | width on /Icon/ChevronRight is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind):** Same value, governed token; the chevron's height is already bound to it. Ask SOLAR to bind Toast /Icon/ChevronRight width to icon.xs, which has the same value.                                                                                                                                                                                                                                                                                                                      |
| `component.toast.tag.height#unbound`                                                                                             | 24, bound to no variable                                                                                                                                                 | height on /Tag is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (set):** The Tag is 24 tall, its own height; the toast places it and never sizes it. Ask SOLAR to bind Toast /Tag height to icon.lg, which has the same value.                                                                                                                                                                                                                                                                                                                                 |
| `component.toggle.root.height#unbound`                                                                                           | 18, bound to no variable                                                                                                                                                 | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (allowLiteral):** The track is 32 by 18, and SOLAR has no control size token (the design review's question on control sizes and the target size); carried as Figma's numbers, as Checkbox's box is. SOLAR has no control height token; ask SOLAR for one, or confirm 18 is intended.                                                                                                                                                                                                           |
| `component.toggle.root.width#unbound`                                                                                            | 32, bound to no variable                                                                                                                                                 | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (allowLiteral):** The track is 32 by 18, and SOLAR has no control size token (the design review's question on control sizes and the target size); carried as Figma's numbers, as Checkbox's box is. SOLAR has no control width token; ask SOLAR for one, or confirm 32 is intended.                                                                                                                                                                                                            |
| `component.toggle.thumb.height#unbound`                                                                                          | 12, bound to no variable                                                                                                                                                 | height on /Thumb is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (allowLiteral):** The thumb is 12px, as the track's size, a number with no token. SOLAR has no control height token; ask SOLAR for one, or confirm 12 is intended.                                                                                                                                                                                                                                                                                                                             |
| `component.toggle.thumb.width#unbound`                                                                                           | 12, bound to no variable                                                                                                                                                 | width on /Thumb is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (allowLiteral):** The thumb is 12px, as the track's size, a number with no token. SOLAR has no control width token; ask SOLAR for one, or confirm 12 is intended.                                                                                                                                                                                                                                                                                                                              |
| `component.token input.addItems.present@state=error`                                                                             | 2 variants at state=error: false where true                                                                                                                              | present on /Field/Tags/Add items… should follow size only, but at state=error it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** As focused, the input empty beside the entries. Ask SOLAR whether Token Input /Field/Tags/Add items… present is meant to change at state=error.                                                                                                                                                                                                                                                                                                                                    |
| `component.token input.addItems.present@state=filled`                                                                            | 2 variants at state=filled: false where true                                                                                                                             | present on /Field/Tags/Add items… should follow size only, but at state=filled it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** As focused, the input empty beside the entries. Ask SOLAR whether Token Input /Field/Tags/Add items… present is meant to change at state=filled.                                                                                                                                                                                                                                                                                                                                   |
| `component.token input.addItems.present@state=focus`                                                                             | 2 variants at state=focus: false where true                                                                                                                              | present on /Field/Tags/Add items… should follow size only, but at state=focus it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** Focused with an entry, the input is there, empty, for the next one: its placeholder is gone, which is what Figma's hidden words draw. Ask SOLAR whether Token Input /Field/Tags/Add items… present is meant to change at state=focus.                                                                                                                                                                                                                                              |
| `component.token input.addItems.present@state=readonly`                                                                          | 2 variants at state=readonly: false where true                                                                                                                           | present on /Field/Tags/Add items… should follow size only, but at state=readonly it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** Read-only, there is no input at all: the entries are shown, and cannot be changed. The code draws none, as Figma draws none. Ask SOLAR whether Token Input /Field/Tags/Add items… present is meant to change at state=readonly.                                                                                                                                                                                                                                                    |
| `component.token input.addItems.width@state=disabled`                                                                            | 2 variants at state=disabled: HUG where FILL                                                                                                                             | width on /Field/Tags/Add items… should follow size only, but at state=disabled it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** The disabled field draws its placeholder beside its Tag, hugging its words, where the resting field's fills the row; the words sit at the same place either way. The input takes the room the Tags leave, in every state. Ask SOLAR whether Token Input /Field/Tags/Add items… width is meant to change at state=disabled.                                                                                                                                                         |
| `component.token input.counter.height#unbound`                                                                                   | 20, bound to no variable                                                                                                                                                 | height on /Field/Counter is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (set):** The Counter's height is its own, as the Tags' are. Ask SOLAR to bind Token Input /Field/Counter height to icon.md, which has the same value.                                                                                                                                                                                                                                                                                                                                          |
| `component.token input.label.paddingBottom#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Token Input /Label paddingBottom to inset.none, which has the same value.                                            |
| `component.token input.label.paddingLeft#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Token Input /Label paddingLeft to inset.none, which has the same value.                                              |
| `component.token input.label.paddingRight#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingRight on /Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Token Input /Label paddingRight to inset.none, which has the same value.                                             |
| `component.token input.label.paddingTop#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingTop on /Label is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Token Input /Label paddingTop to inset.none, which has the same value.                                               |
| `component.token input.root.paddingBottom#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Token Input / paddingBottom to inset.none, which has the same value.                                                 |
| `component.token input.root.paddingLeft#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Token Input / paddingLeft to inset.none, which has the same value.                                                   |
| `component.token input.root.paddingRight#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Token Input / paddingRight to inset.none, which has the same value.                                                  |
| `component.token input.root.paddingTop#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Token Input / paddingTop to inset.none, which has the same value.                                                    |
| `component.token input.root.width#unbound`                                                                                       | 240, 160, bound to no variable                                                                                                                                           | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** As md's, the sm field's 160 a sample. SOLAR has no control width token; ask SOLAR for one, or confirm 240, 160 is intended.                                                                                                                                                                                                                                                                                                                                                           |
| `component.token input.tag.height#unbound`                                                                                       | 24, bound to no variable                                                                                                                                                 | height on /Field/Tags/Tag is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (set):** The tokens are SOLAR Tags, whose height is their own: the field lays them out and never sizes them. Ask SOLAR to bind Token Input /Field/Tags/Tag height to icon.lg, which has the same value.                                                                                                                                                                                                                                                                                        |
| `component.token input.tag2.height#unbound`                                                                                      | 24, bound to no variable                                                                                                                                                 | height on /Field/Tags/Tag#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (set):** As the first token's. Ask SOLAR to bind Token Input /Field/Tags/Tag#2 height to icon.lg, which has the same value.                                                                                                                                                                                                                                                                                                                                                                    |
| `component.token input.tags.paddingBottom#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Field/Tags is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Token Input /Field/Tags paddingBottom to inset.none, which has the same value.                                       |
| `component.token input.tags.paddingLeft#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Field/Tags is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Token Input /Field/Tags paddingLeft to inset.none, which has the same value.                                         |
| `component.token input.tags.paddingRight#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingRight on /Field/Tags is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Token Input /Field/Tags paddingRight to inset.none, which has the same value.                                        |
| `component.token input.tags.paddingTop#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingTop on /Field/Tags is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Token Input /Field/Tags paddingTop to inset.none, which has the same value.                                          |
| `component.tooltip.arrow.height#unbound`                                                                                         | 3, 7, bound to no variable                                                                                                                                               | height on /arrow is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (allowLiteral):** The arrow is 7 × 3, bound to nothing: SOLAR has no size for an overlay's arrow, one of the sizes with no variable. SOLAR has no control height token; ask SOLAR for one, or confirm 3, 7 is intended.                                                                                                                                                                                                                                                                        |
| `component.tooltip.arrow.width#unbound`                                                                                          | 7, 3, bound to no variable                                                                                                                                               | width on /arrow is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (allowLiteral):** The arrow is 7 × 3, bound to nothing: SOLAR has no size for an overlay's arrow, one of the sizes with no variable. SOLAR has no control width token; ask SOLAR for one, or confirm 7, 3 is intended.                                                                                                                                                                                                                                                                         |
| `component.tooltip.root.gap#unbound`                                                                                             | 0, bound to no variable                                                                                                                                                  | gap on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Tooltip / gap to inset.none or stack.none, which has the same value.                                   |
| `component.tooltip.root.paddingBottom#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Tooltip / paddingBottom to inset.none, which has the same value.                                                     |
| `component.tooltip.root.paddingLeft#unbound`                                                                                     | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Tooltip / paddingLeft to inset.none, which has the same value.                                                       |
| `component.tooltip.root.paddingRight#unbound`                                                                                    | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Tooltip / paddingRight to inset.none, which has the same value.                                                      |
| `component.tooltip.root.paddingTop#unbound`                                                                                      | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Tooltip / paddingTop to inset.none, which has the same value.                                                        |
| `component.tooltip.tooltipContent.gap#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | gap on /Tooltip Content is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Tooltip /Tooltip Content gap to inset.none or stack.none, which has the same value.                    |
| `component.tree indent.root.gap#unbound`                                                                                         | 0, bound to no variable                                                                                                                                                  | gap on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Tree Indent / gap to inset.none or stack.none, which has the same value.                               |
| `component.tree indent.root.height#unbound`                                                                                      | 32, bound to no variable                                                                                                                                                 | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (allowLiteral):** A tree row's height, 32, bound to nothing: SOLAR publishes no row height. Raised as a governance gap. SOLAR has no control height token; ask SOLAR for one, or confirm 32 is intended.                                                                                                                                                                                                                                                                                       |
| `component.tree indent.root.paddingBottom#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Tree Indent / paddingBottom to inset.none, which has the same value.                                                 |
| `component.tree indent.root.paddingLeft#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Tree Indent / paddingLeft to inset.none, which has the same value.                                                   |
| `component.tree indent.root.paddingRight#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Tree Indent / paddingRight to inset.none, which has the same value.                                                  |
| `component.tree indent.root.paddingTop#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Tree Indent / paddingTop to inset.none, which has the same value.                                                    |
| `component.tree indent.root.width#unbound`                                                                                       | 0, bound to no variable                                                                                                                                                  | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (allowLiteral):** Depth 00 is an empty frame 0px wide, where no unit is drawn: no indent. SOLAR's none tokens are spacings, and this is a width. SOLAR has no control width token; ask SOLAR for one, or confirm 0 is intended.                                                                                                                                                                                                                                                                |
| `component.tree indent.unit1.height#unbound`                                                                                     | 32, bound to no variable                                                                                                                                                 | height on /1 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (allowLiteral):** Each unit is 39 tall in a 32-tall indent, overflowing it, and bound to nothing. Drawn as Figma draws it; the design review asks whether 32 is meant. SOLAR has no control height token; ask SOLAR for one, or confirm 32 is intended.                                                                                                                                                                                                                                        |
| `component.tree indent.unit1.width#unbound`                                                                                      | 16, bound to no variable                                                                                                                                                 | width on /1 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind):** One level of indent is 16px, "matching the 16px chevron slot", says the description: the width of Tree Item's chevron, an icon.sm, the same value. SOLAR has no control width token; ask SOLAR for one, or confirm 16 is intended.                                                                                                                                                                                                                                                   |
| `component.tree indent.unit10.height#unbound`                                                                                    | 32, bound to no variable                                                                                                                                                 | height on /10 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **Decided (allowLiteral):** Each unit is 39 tall in a 32-tall indent, overflowing it, and bound to nothing. Drawn as Figma draws it; the design review asks whether 32 is meant. SOLAR has no control height token; ask SOLAR for one, or confirm 32 is intended.                                                                                                                                                                                                                                        |
| `component.tree indent.unit10.width#unbound`                                                                                     | 16, bound to no variable                                                                                                                                                 | width on /10 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (bind):** One level of indent is 16px, "matching the 16px chevron slot", says the description: the width of Tree Item's chevron, an icon.sm, the same value. SOLAR has no control width token; ask SOLAR for one, or confirm 16 is intended.                                                                                                                                                                                                                                                   |
| `component.tree indent.unit2.height#unbound`                                                                                     | 32, bound to no variable                                                                                                                                                 | height on /2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (allowLiteral):** Each unit is 39 tall in a 32-tall indent, overflowing it, and bound to nothing. Drawn as Figma draws it; the design review asks whether 32 is meant. SOLAR has no control height token; ask SOLAR for one, or confirm 32 is intended.                                                                                                                                                                                                                                        |
| `component.tree indent.unit2.width#unbound`                                                                                      | 16, bound to no variable                                                                                                                                                 | width on /2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind):** One level of indent is 16px, "matching the 16px chevron slot", says the description: the width of Tree Item's chevron, an icon.sm, the same value. SOLAR has no control width token; ask SOLAR for one, or confirm 16 is intended.                                                                                                                                                                                                                                                   |
| `component.tree indent.unit3.height#unbound`                                                                                     | 32, bound to no variable                                                                                                                                                 | height on /3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (allowLiteral):** Each unit is 39 tall in a 32-tall indent, overflowing it, and bound to nothing. Drawn as Figma draws it; the design review asks whether 32 is meant. SOLAR has no control height token; ask SOLAR for one, or confirm 32 is intended.                                                                                                                                                                                                                                        |
| `component.tree indent.unit3.width#unbound`                                                                                      | 16, bound to no variable                                                                                                                                                 | width on /3 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind):** One level of indent is 16px, "matching the 16px chevron slot", says the description: the width of Tree Item's chevron, an icon.sm, the same value. SOLAR has no control width token; ask SOLAR for one, or confirm 16 is intended.                                                                                                                                                                                                                                                   |
| `component.tree indent.unit4.height#unbound`                                                                                     | 32, bound to no variable                                                                                                                                                 | height on /4 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (allowLiteral):** Each unit is 39 tall in a 32-tall indent, overflowing it, and bound to nothing. Drawn as Figma draws it; the design review asks whether 32 is meant. SOLAR has no control height token; ask SOLAR for one, or confirm 32 is intended.                                                                                                                                                                                                                                        |
| `component.tree indent.unit4.width#unbound`                                                                                      | 16, bound to no variable                                                                                                                                                 | width on /4 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind):** One level of indent is 16px, "matching the 16px chevron slot", says the description: the width of Tree Item's chevron, an icon.sm, the same value. SOLAR has no control width token; ask SOLAR for one, or confirm 16 is intended.                                                                                                                                                                                                                                                   |
| `component.tree indent.unit5.height#unbound`                                                                                     | 32, bound to no variable                                                                                                                                                 | height on /5 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (allowLiteral):** Each unit is 39 tall in a 32-tall indent, overflowing it, and bound to nothing. Drawn as Figma draws it; the design review asks whether 32 is meant. SOLAR has no control height token; ask SOLAR for one, or confirm 32 is intended.                                                                                                                                                                                                                                        |
| `component.tree indent.unit5.width#unbound`                                                                                      | 16, bound to no variable                                                                                                                                                 | width on /5 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind):** One level of indent is 16px, "matching the 16px chevron slot", says the description: the width of Tree Item's chevron, an icon.sm, the same value. SOLAR has no control width token; ask SOLAR for one, or confirm 16 is intended.                                                                                                                                                                                                                                                   |
| `component.tree indent.unit6.height#unbound`                                                                                     | 32, bound to no variable                                                                                                                                                 | height on /6 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (allowLiteral):** Each unit is 39 tall in a 32-tall indent, overflowing it, and bound to nothing. Drawn as Figma draws it; the design review asks whether 32 is meant. SOLAR has no control height token; ask SOLAR for one, or confirm 32 is intended.                                                                                                                                                                                                                                        |
| `component.tree indent.unit6.width#unbound`                                                                                      | 16, bound to no variable                                                                                                                                                 | width on /6 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind):** One level of indent is 16px, "matching the 16px chevron slot", says the description: the width of Tree Item's chevron, an icon.sm, the same value. SOLAR has no control width token; ask SOLAR for one, or confirm 16 is intended.                                                                                                                                                                                                                                                   |
| `component.tree indent.unit7.height#unbound`                                                                                     | 32, bound to no variable                                                                                                                                                 | height on /7 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (allowLiteral):** Each unit is 39 tall in a 32-tall indent, overflowing it, and bound to nothing. Drawn as Figma draws it; the design review asks whether 32 is meant. SOLAR has no control height token; ask SOLAR for one, or confirm 32 is intended.                                                                                                                                                                                                                                        |
| `component.tree indent.unit7.width#unbound`                                                                                      | 16, bound to no variable                                                                                                                                                 | width on /7 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind):** One level of indent is 16px, "matching the 16px chevron slot", says the description: the width of Tree Item's chevron, an icon.sm, the same value. SOLAR has no control width token; ask SOLAR for one, or confirm 16 is intended.                                                                                                                                                                                                                                                   |
| `component.tree indent.unit8.height#unbound`                                                                                     | 32, bound to no variable                                                                                                                                                 | height on /8 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (allowLiteral):** Each unit is 39 tall in a 32-tall indent, overflowing it, and bound to nothing. Drawn as Figma draws it; the design review asks whether 32 is meant. SOLAR has no control height token; ask SOLAR for one, or confirm 32 is intended.                                                                                                                                                                                                                                        |
| `component.tree indent.unit8.width#unbound`                                                                                      | 16, bound to no variable                                                                                                                                                 | width on /8 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind):** One level of indent is 16px, "matching the 16px chevron slot", says the description: the width of Tree Item's chevron, an icon.sm, the same value. SOLAR has no control width token; ask SOLAR for one, or confirm 16 is intended.                                                                                                                                                                                                                                                   |
| `component.tree indent.unit9.height#unbound`                                                                                     | 32, bound to no variable                                                                                                                                                 | height on /9 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **Decided (allowLiteral):** Each unit is 39 tall in a 32-tall indent, overflowing it, and bound to nothing. Drawn as Figma draws it; the design review asks whether 32 is meant. SOLAR has no control height token; ask SOLAR for one, or confirm 32 is intended.                                                                                                                                                                                                                                        |
| `component.tree indent.unit9.width#unbound`                                                                                      | 16, bound to no variable                                                                                                                                                 | width on /9 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (bind):** One level of indent is 16px, "matching the 16px chevron slot", says the description: the width of Tree Item's chevron, an icon.sm, the same value. SOLAR has no control width token; ask SOLAR for one, or confirm 16 is intended.                                                                                                                                                                                                                                                   |
| `component.tree item.buttons.paddingBottom#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingBottom on /CTAs is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Tree Item /CTAs paddingBottom to inset.none, which has the same value.                                               |
| `component.tree item.buttons.paddingLeft#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingLeft on /CTAs is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Tree Item /CTAs paddingLeft to inset.none, which has the same value.                                                 |
| `component.tree item.buttons.paddingRight#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingRight on /CTAs is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Tree Item /CTAs paddingRight to inset.none, which has the same value.                                                |
| `component.tree item.buttons.paddingTop#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingTop on /CTAs is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Tree Item /CTAs paddingTop to inset.none, which has the same value.                                                  |
| `component.tree item.checkbox.height#unbound`                                                                                    | 16, bound to no variable                                                                                                                                                 | height on /Checkbox is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (set):** As its width, the Checkbox's own. Ask SOLAR to bind Tree Item /Checkbox height to icon.sm, which has the same value.                                                                                                                                                                                                                                                                                                                                                                  |
| `component.tree item.checkbox.width#unbound`                                                                                     | 16, bound to no variable                                                                                                                                                 | width on /Checkbox is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (set):** The checkbox is a SOLAR Checkbox, whose 16px box is its own: the row places it, and never sizes it. Ask SOLAR to bind Tree Item /Checkbox width to icon.sm, which has the same value.                                                                                                                                                                                                                                                                                                 |
| `component.tree item.chevron.paddingBottom#unbound`                                                                              | 0, bound to no variable                                                                                                                                                  | paddingBottom on /Chevron is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Tree Item /Chevron paddingBottom to inset.none, which has the same value.                                            |
| `component.tree item.chevron.paddingLeft#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingLeft on /Chevron is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Tree Item /Chevron paddingLeft to inset.none, which has the same value.                                              |
| `component.tree item.chevron.paddingRight#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingRight on /Chevron is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Tree Item /Chevron paddingRight to inset.none, which has the same value.                                             |
| `component.tree item.chevron.paddingTop#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingTop on /Chevron is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Tree Item /Chevron paddingTop to inset.none, which has the same value.                                               |
| `component.tree item.chevronChevron.width#unbound`                                                                               | 16, bound to no variable                                                                                                                                                 | width on /Chevron/Icon/ChevronRight is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (bind):** Same value, governed token; the icon's height is already bound to it. Ask SOLAR to bind Tree Item /Chevron/Icon/ChevronRight width to icon.sm, which has the same value.                                                                                                                                                                                                                                                                                                             |
| `component.tree item.counter.height#unbound`                                                                                     | 20, bound to no variable                                                                                                                                                 | height on /Counter is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (set):** The counter is a SOLAR Counter, whose 20px pill is its own. Ask SOLAR to bind Tree Item /Counter height to icon.md, which has the same value.                                                                                                                                                                                                                                                                                                                                         |
| `component.tree item.iconChevronDown.width#unbound`                                                                              | 16, bound to no variable                                                                                                                                                 | width on /Chevron/Icon/ChevronDown is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind):** Same value, governed token; the icon's height is already bound to it. Ask SOLAR to bind Tree Item /Chevron/Icon/ChevronDown width to icon.sm, which has the same value.                                                                                                                                                                                                                                                                                                              |
| `component.tree item.iconMore.width#unbound`                                                                                     | 16, bound to no variable                                                                                                                                                 | width on /CTAs/Icon/More is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind):** Same value, governed token; the icon's height is already bound to it. Ask SOLAR to bind Tree Item /CTAs/Icon/More width to icon.sm, which has the same value.                                                                                                                                                                                                                                                                                                                        |
| `component.tree item.iconPlus.width#unbound`                                                                                     | 16, bound to no variable                                                                                                                                                 | width on /CTAs/Icon/Plus is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind):** Same value, governed token; the icon's height is already bound to it. Ask SOLAR to bind Tree Item /CTAs/Icon/Plus width to icon.sm, which has the same value.                                                                                                                                                                                                                                                                                                                        |
| `component.tree item.leadingIcon.width#unbound`                                                                                  | 16, bound to no variable                                                                                                                                                 | width on /Icon/None is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (bind):** Same value, governed token; the icon's height is already bound to it. Ask SOLAR to bind Tree Item /Icon/None width to icon.sm, which has the same value.                                                                                                                                                                                                                                                                                                                             |
| `component.tree item.root.height#unbound`                                                                                        | 32, bound to no variable                                                                                                                                                 | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (allowLiteral):** As a list's row: SOLAR publishes no row height token (owner decision 2026-09-23, raised as a governance gap). A row is drawn 32 tall, its description's. SOLAR has no control height token; ask SOLAR for one, or confirm 32 is intended.                                                                                                                                                                                                                                    |
| `component.tree item.root.paddingBottom#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Tree Item / paddingBottom to inset.none, which has the same value.                                                   |
| `component.tree item.root.paddingBottom@selected=false, expanded=false, state=edit`                                              | 1 variant at selected=false, expanded=false, state=edit: inset.none where 0                                                                                              | paddingBottom on / should follow no axis only, but at selected=false, expanded=false, state=edit it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (accept):** Figma binds the editing row's bottom padding to stack.none and leaves the others an unbound 0, which the shared default binds to inset.none: the same 0 either way. Ask SOLAR whether Tree Item / paddingBottom is meant to change at selected=false, expanded=false, state=edit.                                                                                                                                                                                                  |
| `component.tree item.root.paddingBottom@selected=false, expanded=true, state=edit`                                               | 1 variant at selected=false, expanded=true, state=edit: inset.none where 0                                                                                               | paddingBottom on / should follow no axis only, but at selected=false, expanded=true, state=edit it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma binds the editing row's bottom padding to stack.none and leaves the others an unbound 0, which the shared default binds to inset.none: the same 0 either way. Ask SOLAR whether Tree Item / paddingBottom is meant to change at selected=false, expanded=true, state=edit.                                                                                                                                                                                                   |
| `component.tree item.root.paddingBottom@selected=true, expanded=false, state=edit`                                               | 1 variant at selected=true, expanded=false, state=edit: inset.none where 0                                                                                               | paddingBottom on / should follow no axis only, but at selected=true, expanded=false, state=edit it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **Decided (accept):** Figma binds the editing row's bottom padding to stack.none and leaves the others an unbound 0, which the shared default binds to inset.none: the same 0 either way. Ask SOLAR whether Tree Item / paddingBottom is meant to change at selected=true, expanded=false, state=edit.                                                                                                                                                                                                   |
| `component.tree item.root.paddingBottom@selected=true, expanded=true, state=edit`                                                | 1 variant at selected=true, expanded=true, state=edit: inset.none where 0                                                                                                | paddingBottom on / should follow no axis only, but at selected=true, expanded=true, state=edit it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (accept):** Figma binds the editing row's bottom padding to stack.none and leaves the others an unbound 0, which the shared default binds to inset.none: the same 0 either way. Ask SOLAR whether Tree Item / paddingBottom is meant to change at selected=true, expanded=true, state=edit.                                                                                                                                                                                                    |
| `component.tree item.root.paddingTop#unbound`                                                                                    | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Tree Item / paddingTop to inset.none, which has the same value.                                                      |
| `component.tree item.root.paddingTop@selected=false, expanded=false, state=edit`                                                 | 1 variant at selected=false, expanded=false, state=edit: inset.none where 0                                                                                              | paddingTop on / should follow no axis only, but at selected=false, expanded=false, state=edit it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (accept):** Figma binds the editing row's top padding to stack.none and leaves the others an unbound 0, which the shared default binds to inset.none: the same 0 either way. Ask SOLAR whether Tree Item / paddingTop is meant to change at selected=false, expanded=false, state=edit.                                                                                                                                                                                                        |
| `component.tree item.root.paddingTop@selected=false, expanded=true, state=edit`                                                  | 1 variant at selected=false, expanded=true, state=edit: inset.none where 0                                                                                               | paddingTop on / should follow no axis only, but at selected=false, expanded=true, state=edit it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma binds the editing row's top padding to stack.none and leaves the others an unbound 0, which the shared default binds to inset.none: the same 0 either way. Ask SOLAR whether Tree Item / paddingTop is meant to change at selected=false, expanded=true, state=edit.                                                                                                                                                                                                         |
| `component.tree item.root.paddingTop@selected=true, expanded=false, state=edit`                                                  | 1 variant at selected=true, expanded=false, state=edit: inset.none where 0                                                                                               | paddingTop on / should follow no axis only, but at selected=true, expanded=false, state=edit it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **Decided (accept):** Figma binds the editing row's top padding to stack.none and leaves the others an unbound 0, which the shared default binds to inset.none: the same 0 either way. Ask SOLAR whether Tree Item / paddingTop is meant to change at selected=true, expanded=false, state=edit.                                                                                                                                                                                                         |
| `component.tree item.root.paddingTop@selected=true, expanded=true, state=edit`                                                   | 1 variant at selected=true, expanded=true, state=edit: inset.none where 0                                                                                                | paddingTop on / should follow no axis only, but at selected=true, expanded=true, state=edit it differs from the variant that defines it. The recipe keeps the defining value; this is either a Figma mistake or a real interaction between axes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **Decided (accept):** Figma binds the editing row's top padding to stack.none and leaves the others an unbound 0, which the shared default binds to inset.none: the same 0 either way. Ask SOLAR whether Tree Item / paddingTop is meant to change at selected=true, expanded=true, state=edit.                                                                                                                                                                                                          |
| `component.tree item.root.width#unbound`                                                                                         | 200, bound to no variable                                                                                                                                                | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** A row spans its tree, as a list's does: Figma draws it 200 wide, bound to nothing, a sample of a tree's width. SOLAR has no control width token; ask SOLAR for one, or confirm 200 is intended.                                                                                                                                                                                                                                                                                       |
| `component.tree item.status.height#unbound`                                                                                      | 8, bound to no variable                                                                                                                                                  | height on /StatusIndicator is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** As its width, the StatusIndicator's own. SOLAR has no control height token; ask SOLAR for one, or confirm 8 is intended.                                                                                                                                                                                                                                                                                                                                                              |
| `component.tree item.status.width#unbound`                                                                                       | 8, bound to no variable                                                                                                                                                  | width on /StatusIndicator is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **Decided (set):** The status is a SOLAR StatusIndicator, whose 8px dot (xs) is its own. SOLAR has no control width token; ask SOLAR for one, or confirm 8 is intended.                                                                                                                                                                                                                                                                                                                                  |
| `component.tree item.tag.height#unbound`                                                                                         | 24, bound to no variable                                                                                                                                                 | height on /Tag is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (set):** The tag is a SOLAR Tag, whose 24px height is its own. Ask SOLAR to bind Tree Item /Tag height to icon.lg, which has the same value.                                                                                                                                                                                                                                                                                                                                                   |
| `component.tree item.trailingIcon.width#unbound`                                                                                 | 16, bound to no variable                                                                                                                                                 | width on /Icon/None#2 is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (bind):** Same value, governed token; the icon's height is already bound to it. Ask SOLAR to bind Tree Item /Icon/None#2 width to icon.sm, which has the same value.                                                                                                                                                                                                                                                                                                                           |
| `component.tree item.treeIndent.height#unbound`                                                                                  | 32, bound to no variable                                                                                                                                                 | height on /Tree Indent is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **Decided (set):** As its width, the Tree Indent's own, its row's height. Ask SOLAR to bind Tree Item /Tree Indent height to icon.2xl, which has the same value.                                                                                                                                                                                                                                                                                                                                         |
| `component.tree item.treeIndent.width#unbound`                                                                                   | 0, bound to no variable                                                                                                                                                  | width on /Tree Indent is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **Decided (set):** The indent is a SOLAR .Tree Indent, as wide as its depth (16px a level, its description says): the row lays it out and never sizes it; Figma's 0 is depth 00's. SOLAR has no control width token; ask SOLAR for one, or confirm 0 is intended.                                                                                                                                                                                                                                        |
| `component.trend badge.icon.height#unbound`                                                                                      | 8, 6, 4, bound to no variable                                                                                                                                            | height on /Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (allowLiteral):** As the width, part of the drawing. SOLAR has no control height token; ask SOLAR for one, or confirm 8, 6, 4 is intended.                                                                                                                                                                                                                                                                                                                                                     |
| `component.trend badge.icon.width#unbound`                                                                                       | 8, 6, bound to no variable                                                                                                                                               | width on /Icon is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **Decided (allowLiteral):** The box of the drawn arrow or dash, in the drawing's own coordinates, as its outline is: part of the drawing, not a size of the system. SOLAR has no control width token; ask SOLAR for one, or confirm 8, 6 is intended.                                                                                                                                                                                                                                                    |
| `component.trend badge.root.height#unbound`                                                                                      | 20, 16, 8, bound to no variable                                                                                                                                          | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (allowLiteral):** As the width, the 8px dot. SOLAR has no control height token; ask SOLAR for one, or confirm 20, 16, 8 is intended.                                                                                                                                                                                                                                                                                                                                                           |
| `component.trend badge.root.paddingBottom#unbound`                                                                               | 0, bound to no variable                                                                                                                                                  | paddingBottom on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Trend Badge / paddingBottom to inset.none, which has the same value.                                                 |
| `component.trend badge.root.paddingLeft#unbound`                                                                                 | 0, bound to no variable                                                                                                                                                  | paddingLeft on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Trend Badge / paddingLeft to inset.none, which has the same value.                                                   |
| `component.trend badge.root.paddingRight#unbound`                                                                                | 0, bound to no variable                                                                                                                                                  | paddingRight on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Trend Badge / paddingRight to inset.none, which has the same value.                                                  |
| `component.trend badge.root.paddingTop#unbound`                                                                                  | 0, bound to no variable                                                                                                                                                  | paddingTop on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **Decided (bind, shared default zero-insets):** Same value, governed token: a padding Figma leaves unbound at 0 is inset.none, SOLAR's token for no inset. It is the one answer in every component, so it is decided once here. SOLAR decided on 2026-09-25 to leave the zeros unbound, since binding them changes nothing drawn, and to have them read this way. Ask SOLAR to bind Trend Badge / paddingTop to inset.none, which has the same value.                                                    |
| `component.trend badge.root.width#unbound`                                                                                       | 20, 16, 8, bound to no variable                                                                                                                                          | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (allowLiteral):** The xs dot is 8px, and the icon ladder starts at 12: SOLAR has no size for it, as StatusIndicator's xs dot has none (SOLAR's decision, 2026-09-25). Carried as a raw value. SOLAR has no control width token; ask SOLAR for one, or confirm 20, 16, 8 is intended.                                                                                                                                                                                                           |
| `component.weekday header.root.gap#unbound`                                                                                      | 0, bound to no variable                                                                                                                                                  | gap on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **Decided (bind, shared default zero-gaps):** Same value, governed token: a gap Figma leaves unbound at 0 is the none of its family, and SOLAR picks the family by direction, as its own bound gaps do: a horizontal auto layout's gap is an inset, a vertical one's a stack (the Foundations agent reference and the Spatial chapter). Decided once here, as the paddings are. Ask SOLAR to bind Weekday Header / gap to inset.none or stack.none, which has the same value.                            |
| `component.weekday header.root.height#unbound`                                                                                   | 36, bound to no variable                                                                                                                                                 | height on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **Decided (allowLiteral):** A weekday's header is 36 tall, bound to nothing: SOLAR has no size for it, one of the sizes with no variable (a calendar's row). SOLAR has no control height token; ask SOLAR for one, or confirm 36 is intended.                                                                                                                                                                                                                                                            |
| `component.weekday header.root.width#unbound`                                                                                    | 160, bound to no variable                                                                                                                                                | width on / is a literal in Figma, bound to no variable, so the recipe carries a raw value where it should name a token.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **Decided (set):** A weekday's header fills its column of the grid, as the days under it do: Figma draws a 160 sample. SOLAR has no control width token; ask SOLAR for one, or confirm 160 is intended.                                                                                                                                                                                                                                                                                                  |
| `logo.os-logo.teams`                                                                                                             | 12 gradient fills and 7 fill-opacity attributes                                                                                                                          | The Teams mark is drawn with radial and linear gradients and per-path opacity, neither of which the vector IR represents. It ships as its raw SVG source instead of as paths. React and the raw SVG output render it faithfully -- React from JSX generated out of that source, with per-instance gradient ids -- but Flutter omits the Teams variant entirely: redrawing 11 radial gradients with focal points, gradientTransform matrices and 27 stops in a hand-written painter is disproportionate for one third-party mark, and approximating it with flat colours would invent a brand colour, which is worse than a missing asset. It is therefore the only asset the two targets do not share, and the parity suite asserts that it is the only one. | None: SOLAR confirmed on 2026-09-25 that a flat mark is not its decision but depends on Microsoft publishing one. Flutter leaves it out until then.                                                                                                                                                                                                                                                                                                                                                      |
| `logo.size`                                                                                                                      | no logo size scale                                                                                                                                                       | SOLAR publishes icon.xs through icon.2xl but no equivalent ladder for logos, so a named size step on a logo component resolves to the icon variable. No name is invented -- an existing token is reused -- but a wordmark is not an icon and the two scales have no reason to stay equal.                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | None: SOLAR decided on 2026-09-25 on no logo.* scale for now; logos keep borrowing the icon ladder.                                                                                                                                                                                                                                                                                                                                                                                                      |
| `motion.ease.both`                                                                                                               | ease-both                                                                                                                                                                | ease-both is not a valid CSS timing function. The intended curve is ease-in-out, emitted as its cubic bezier so CSS and Flutter share one definition.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Ask SOLAR to rename the value to ease-in-out.                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `mui.theme`                                                                                                                      | no mapping to MUI palette slots or typography variants                                                                                                                   | MUI components read palette.primary, body1, button and the like, which SOLAR does not name. The generated theme maps them to the SOLAR roles used the same way (MUI_PALETTE and MUI_TYPOGRAPHY in the MUI emitter): action._.bg for primary, secondary and error, surface.feedback._.strong for warning, info and success, display and title for h1 to h6, label.md for button. overline has no counterpart. Its breakpoints are the viewport tokens (xs 0, as MUI requires), its spacing unit inset.2xs, and its seven durations and four easings the nearest of SOLAR's three each (MUI_DURATIONS, MUI_EASINGS).                                                                                                                                           | Ask SOLAR to confirm the mapping, or to publish one for MUI-based products.                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `type.font-weight.100`                                                                                                           | Thin                                                                                                                                                                     | The Figma value is the type style name, which is not a valid CSS font-weight. The numeric weight is carried by the token name, so that is what is emitted; the style name is kept in $extensions for font loading.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | no action                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `type.font-weight.200`                                                                                                           | Extra Light                                                                                                                                                              | The Figma value is the type style name, which is not a valid CSS font-weight. The numeric weight is carried by the token name, so that is what is emitted; the style name is kept in $extensions for font loading.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | no action                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `type.font-weight.300`                                                                                                           | Light                                                                                                                                                                    | The Figma value is the type style name, which is not a valid CSS font-weight. The numeric weight is carried by the token name, so that is what is emitted; the style name is kept in $extensions for font loading.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | no action                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `type.font-weight.400`                                                                                                           | Regular                                                                                                                                                                  | The Figma value is the type style name, which is not a valid CSS font-weight. The numeric weight is carried by the token name, so that is what is emitted; the style name is kept in $extensions for font loading.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | no action                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `type.font-weight.500`                                                                                                           | Medium                                                                                                                                                                   | The Figma value is the type style name, which is not a valid CSS font-weight. The numeric weight is carried by the token name, so that is what is emitted; the style name is kept in $extensions for font loading.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | no action                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `type.font-weight.600`                                                                                                           | Semi Bold                                                                                                                                                                | The Figma value is the type style name, which is not a valid CSS font-weight. The numeric weight is carried by the token name, so that is what is emitted; the style name is kept in $extensions for font loading.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | no action                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `type.font-weight.700`                                                                                                           | Bold                                                                                                                                                                     | The Figma value is the type style name, which is not a valid CSS font-weight. The numeric weight is carried by the token name, so that is what is emitted; the style name is kept in $extensions for font loading.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | no action                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `type.font-weight.800`                                                                                                           | Extra Bold                                                                                                                                                               | The Figma value is the type style name, which is not a valid CSS font-weight. The numeric weight is carried by the token name, so that is what is emitted; the style name is kept in $extensions for font loading.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | no action                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `type.font-weight.900`                                                                                                           | Black                                                                                                                                                                    | The Figma value is the type style name, which is not a valid CSS font-weight. The numeric weight is carried by the token name, so that is what is emitted; the style name is kept in $extensions for font loading.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | no action                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `typography.display.xs.medium`                                                                                                   | no Mobile value                                                                                                                                                          | The Type collection has no matching variable for this text style, so Figma exposes no Mobile value. The Desktop value is used for both modes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Ask SOLAR to add the missing Type variable for display/xs/medium.                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `typography.display.xs.semibold`                                                                                                 | no Mobile value                                                                                                                                                          | The Type collection has no matching variable for this text style, so Figma exposes no Mobile value. The Desktop value is used for both modes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Ask SOLAR to add the missing Type variable for display/xs/semibold.                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `typography.title.2xs`                                                                                                           | text case UPPER                                                                                                                                                          | Flutter TextStyle has no text case, so the Flutter target cannot apply it; a Flutter widget using this style has to transform its string. The web targets apply it as text-transform or font-variant-caps.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | no action                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
