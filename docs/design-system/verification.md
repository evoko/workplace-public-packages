# Verification

What checks exist today, what each one proves, and how to read its output.
The full verification design is section 10 of the spec; steps arrive with the
plans that add the pieces they check.

## Today

| Command                                                  | Proves                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Needs                                       |
| -------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------- |
| `bwp-ds lint`                                            | Every authoring rule holds: config, token structure and values, aliases, manifests, selector grammar, property table, no TODO leftovers, entry file current.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Node 22                                     |
| `bwp-ds build`                                           | Lint plus a complete IR was produced and written, and `src/index.css` is regenerated.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Node 22                                     |
| `bwp-ds generate [--target <id>]`                        | Writes each registered target's files from the IR (nothing when lint fails) and deletes files the generator did not produce.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Node 22                                     |
| `bwp-ds capture-defaults --target mui`                   | Renders every component mapped onto an MUI component, in every axis permutation, with the target package's own installed React/Emotion/MUI, and writes `packages/styles-css/catalogs/mui.json`. Deterministic: run twice with nothing else changed, the file is byte-identical. Commit the result.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Node 22, `packages/styles-mui/node_modules` |
| `bwp-ds verify`                                          | Lint; drift (a fresh build and generation equal the committed `design.ir.json` and every generated file); round-trip (each target's output re-parses to the source IR, and for a mapped component the leading reset entries are first recomputed from the IR and the catalog and compared for equality); coverage (every component is supported, partial, or excluded for every target). Writes `docs/design-system/coverage.md`. Drift fails (`DS-E086`) when a mapped component's catalog entry is missing, stale (the manifest's `axisMap`/`slotMap`/`defaultProps` changed since capture), or was captured from a different `@mui/material` version than is installed; pass `--allow-catalog-mismatch` to accept a deliberate version difference (downgrades that one check to the warning `DS-W004`). | Node 22                                     |
| `npm run test`                                           | The compiler behaves as specified, including one deliberately invalid input per error code.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Node 22                                     |
| CI "Capture MUI defaults catalog"                        | Runs `npm run ds -- capture-defaults --target mui` before verify, so the committed `packages/styles-css/catalogs/mui.json` is replaced with a fresh capture against exactly the MUI version CI just installed. `bwp-ds verify` on its own does **not** do this: it only checks that the committed catalog's recorded MUI version matches what is installed and that it has an entry for every mapped component and axis permutation with the manifest's current `axisMap`/`slotMap`/`defaultProps` — a catalog that satisfies all of that but no longer matches what a fresh capture would produce (MUI shipped a patch that changed a default without a version bump it fails to represent, say) would pass `verify` alone. The next step's diff is what actually catches that.                           | CI                                          |
| CI "Verify design system"                                | `npm run verify` passes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | CI                                          |
| CI "Check generated files are up to date"                | `design.ir.json` and `src/index.css` are rewritten by `npm run build`, `docs/design-system/coverage.md` by `npm run verify`, `packages/styles-css/catalogs/mui.json` by the capture step above, and the diff catches a stale committed copy of any of them; `packages/styles-tailwind/src/generated` and `packages/styles-mui/src/generated` are guarded by `verify`'s drift check (`DS-E080`), not by the diff.                                                                                                                                                                                                                                                                                                                                                                                           | CI                                          |
| CI `npm run lint`, `npm run typecheck`, `npm run format` | Every package's ESLint, TypeScript, and Prettier checks pass.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | CI                                          |

## Reading a diagnostic

```
src/components/button/button.css:14:3 error DS-E041 Token required: "color: #fff" must reference a color token (or one of: transparent, currentColor, inherit)
  hint: This property must reference a token: var(--<prefix>-<category>-…). If no token fits, add one to the category file first.
```

File, line, column, severity, code, title, message, then the fix hint. The CLI
prints errors to stderr and warnings plus the summary to stdout, sorted by
file, line, and column. With `--json`, `lint` and `build` print one object to
stdout: `diagnostics` (same order), `summary`, and for `build` the `wrote` and
`entry` paths. `scaffold --json` prints `{ "wrote": [...] }` or
`{ "error": "..." }`; if the config cannot be loaded it prints the
`diagnostics` object instead.

## From a symptom to the code

- "This value is wrong in target X" and the CSS is right: the target plugin's
  handler for that property (`packages/ds-compiler/src/targets/<id>/`).
- "This value is wrong everywhere": the source CSS, or the IR normalization in
  `packages/ds-compiler/src/tokens/values.ts` and
  `src/components/parse-component.ts`.
- "Lint rejects something valid": the rule in `src/tokens/parse-tokens.ts`,
  `src/tokens/resolve-tokens.ts`, `src/components/selector.ts`, or
  `src/components/properties.ts`, in that order of likelihood.

## Planned steps

| Step                       | Plan | Proves                                                                                    |
| -------------------------- | ---- | ----------------------------------------------------------------------------------------- |
| `bwp-ds verify --rendered` | 4    | Computed styles match across web targets in a browser.                                    |
| Flutter checks             | 5    | `dart analyze`, `dart format --set-exit-if-changed`, `dart test` when the SDK is present. |
