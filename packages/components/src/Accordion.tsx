// Generated by @bwp-web/codegen from packages/codegen/src/components/accordion.mjs. Do not edit.
/**
 * SOLAR Accordion.
 *
 * Generated from its template in `packages/codegen/src/components/` on every `npm run
 * solar:codegen`: change the template there, never this file. What it looks like is not here. That
 * is the recipe, `solarAccordionStyle` and `solarAccordionCompose` in `@bwp-web/styles/mui`: the
 * header's rule, fill and focus ring by state, and the content's, collapsed and expanded.
 *
 * One item of an accordion, stacked with others: its header (the `title` and a chevron) is a button
 * that shows and hides the content under it (the `description`, in Figma's words' look, then the
 * caller's children), announced expanded or collapsed. Expanded, the header is drawn as the
 * collapsed item is, its chevron turned up. For a card that expands, use an Expandable Card.
 * `expanded` and `onExpandedChange` hold whether it is expanded, where the caller keeps it;
 * `defaultExpanded` where it does not. Drawn from Figma's layer tree (`internal/layers.tsx`). The
 * app must load `@bwp-web/styles/tokens.css`.
 */

import Box, { type BoxProps } from '@mui/material/Box';
import ButtonBase, { type ButtonBaseProps } from '@mui/material/ButtonBase';
import { useControlled } from '@mui/material/utils';
import { IconChevronDown } from '@bwp-web/assets';
import { forwardRef, useId, type ReactNode, type Ref } from 'react';
import {
  solarAccordionCompose,
  solarAccordionStyle,
  type SolarAccordionProps,
} from '@bwp-web/styles/mui';
import { drawChildren, type DrawnLayer } from './internal/layers.js';

/** Each layer's children, as Figma nests them. */
const TREE: Record<string, string[]> = {
  root: ['title', 'iconChevronDown', 'accordion', 'content'],
  content: ['description'],
};

export interface AccordionProps
  extends
    SolarAccordionProps,
    // MUI types BoxProps' ref for any element; the component's own comes from forwardRef.
    Omit<BoxProps, keyof SolarAccordionProps | 'title' | 'ref'> {
  /** What the item is about: its header's words, which name its button. */
  title: ReactNode;
  /** The content's words, in the look Figma draws them. */
  description?: ReactNode;
  /** The caller's content, after the description. */
  children?: ReactNode;
  /** Whether it starts expanded, where `expanded` does not say. */
  defaultExpanded?: boolean;
  /** Called with whether it is to be expanded, as its header is pressed. */
  onExpandedChange?: (expanded: boolean) => void;
}

export const Accordion = forwardRef<HTMLElement, AccordionProps>(
  function Accordion(
    {
      disabled = false,
      expanded: expandedProp,
      defaultExpanded = false,
      onExpandedChange,
      title,
      description,
      children,
      className,
      sx,
      ...rest
    },
    ref,
  ) {
    const [expanded, setExpanded] = useControlled({
      controlled: expandedProp,
      default: defaultExpanded,
      name: 'Accordion',
      state: 'expanded',
    });
    const id = useId();
    const toggle = () => {
      setExpanded(!expanded);
      onExpandedChange?.(!expanded);
    };
    const classes = (open: boolean, own?: string) =>
      [
        disabled ? 'SolarAccordion-disabled' : null,
        open ? 'SolarAccordion-expanded' : null,
        own,
      ]
        .filter(Boolean)
        .join(' ') || undefined;
    // The header's title and chevron, as the collapsed item draws them.
    const heading = drawChildren('root', {
      prefix: 'SolarAccordion',
      tree: TREE,
      parts: solarAccordionCompose({ disabled, expanded: false }),
      text: { title },
      icons: { iconChevronDown: <IconChevronDown /> },
    });
    const button = {
      disabled,
      disableRipple: true,
      'aria-expanded': expanded,
      onClick: toggle,
    } satisfies ButtonBaseProps;
    const sxOf = (open: boolean) => [
      solarAccordionStyle({ disabled, expanded: open }),
      ...(Array.isArray(sx) ? sx : [sx]),
    ];
    // Collapsed, the item is its header.
    if (!expanded)
      return (
        <ButtonBase
          ref={ref as Ref<HTMLButtonElement>}
          {...(rest as ButtonBaseProps)}
          {...button}
          className={classes(false, className)}
          sx={sxOf(false)}
        >
          {heading}
        </ButtonBase>
      );
    const composed = solarAccordionCompose({ disabled, expanded: true });
    // A slot left empty is not drawn.
    const parts = {
      ...composed,
      description: {
        ...composed.description,
        present: composed.description?.present !== false && description != null,
      },
    };
    return (
      <Box
        ref={ref}
        {...rest}
        className={classes(true, className)}
        sx={sxOf(true)}
      >
        {drawChildren('root', {
          prefix: 'SolarAccordion',
          tree: TREE,
          parts,
          text: { description },
          render: {
            // The header, nested: the collapsed item's look, a button naming the content it shows.
            accordion: ({ className: c, style }: DrawnLayer) => (
              <ButtonBase
                data-layer="accordion"
                {...button}
                aria-controls={`${id}-content`}
                className={[c, classes(false)].filter(Boolean).join(' ')}
                style={style}
                sx={solarAccordionStyle({ disabled, expanded: false })}
              >
                {heading}
              </ButtonBase>
            ),
            // The content: Figma's description, then the caller's children.
            content: ({ className: c, style, children: drawn }: DrawnLayer) => (
              <div id={`${id}-content`} className={c} style={style}>
                {drawn}
                {children}
              </div>
            ),
          },
        })}
      </Box>
    );
  },
);
