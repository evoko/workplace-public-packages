// Generated by @bwp-web/codegen from packages/codegen/src/components/dropdown.mjs. Do not edit.
/**
 * SOLAR Dropdown.
 *
 * Generated from its template in `packages/codegen/src/components/` on every `npm run
 * solar:codegen`: change the template there, never this file. What it looks like is not here. That
 * is the recipe, `solarDropdownStyle` and `solarDropdownCompose` in `@bwp-web/styles/mui`: the
 * field's fill, edge and focus ring by state, its icons and chevron, and the label and helper.
 *
 * One choice from about ten or fewer, SOLAR says (beyond that, an Autocomplete), the same control
 * as a Select in Dropdown's look: its `label` above (a `mandatory` one is starred), its `helper`
 * below, which says what is wrong where it is in `error`, and the field showing the choice or the
 * `placeholder`, with a `leadingIcon` before it, a `trailingIcon` after it and a chevron that turns
 * up while open. It is MUI's Select on InputBase: a click, Enter or the arrow keys open its panel,
 * a DropdownMenu of DropdownItems, each with a `value`. `value` and `onChange` hold the choice, as
 * a string; `open`, `onOpen` and `onClose` its panel, where the caller keeps it. The app must load
 * `@bwp-web/styles/tokens.css`.
 */

import Box from '@mui/material/Box';
import InputBase from '@mui/material/InputBase';
import MuiSelect, {
  type SelectChangeEvent,
  type SelectProps as MuiSelectProps,
} from '@mui/material/Select';
import { useControlled } from '@mui/material/utils';
import { IconChevronDown, IconChevronUp } from '@bwp-web/assets';
import {
  Children,
  forwardRef,
  isValidElement,
  useId,
  type ReactNode,
} from 'react';
import {
  solarDropdownCompose,
  solarDropdownStyle,
  solarDropdownMenuStyle,
  type SolarDropdownProps,
} from '@bwp-web/styles/mui';
import { DropdownMenuSizeContext } from './DropdownMenu.js';
import { drawChildren, type LayerDrawing } from './internal/layers.js';

/** Each layer's children, as Figma nests them. */
const TREE: Record<string, string[]> = {
  root: ['label', 'field', 'helper'],
  label: ['labelLabel', 'mandatory'],
  field: [
    'leadingIcon',
    'fieldLabel',
    'trailingIcon',
    'iconChevronDown',
    'iconChevronUp',
  ],
};

/** MUI's own icon, which the field draws as its chevron layer instead. */
const NoIcon = () => null;

export interface DropdownProps
  extends
    SolarDropdownProps,
    Omit<
      MuiSelectProps<string>,
      | keyof SolarDropdownProps
      | 'size'
      | 'variant'
      | 'color'
      | 'input'
      | 'label'
      | 'multiple'
      | 'native'
      | 'autoWidth'
      | 'IconComponent'
      | 'renderValue'
      | 'displayEmpty'
      | 'value'
      | 'defaultValue'
      | 'onChange'
      | 'children'
      | 'ref'
    > {
  /** What it asks for, above it. */
  label?: ReactNode;
  /** Whether a choice must be made, which stars the label. */
  mandatory?: boolean;
  /** More about it, below; where it is in `error`, what is wrong. */
  helper?: ReactNode;
  /** What the field says before a choice is made. */
  placeholder?: ReactNode;
  /** The chosen row's `value`, where the caller keeps it; '' for none. */
  value?: string;
  /** The choice it starts with, where `value` does not say. */
  defaultValue?: string;
  /** Called with the row chosen's `value`. */
  onChange?: (event: SelectChangeEvent<string>, value: string) => void;
  /** The rows: DropdownItems, each with a `value`. */
  children: ReactNode;
  /** An icon before the choice. */
  leadingIcon?: ReactNode;
  /** An icon after the choice. */
  trailingIcon?: ReactNode;
}

export const Dropdown = forwardRef<HTMLDivElement, DropdownProps>(
  function Dropdown(
    {
      size,
      disabled = false,
      error = false,
      open: openProp,
      label,
      mandatory = false,
      helper,
      placeholder,
      leadingIcon,
      trailingIcon,
      value: valueProp,
      defaultValue,
      onChange,
      onOpen,
      onClose,
      children,
      id: idProp,
      MenuProps,
      className,
      style,
      sx,
      ...rest
    },
    ref,
  ) {
    const own = useId();
    const id = idProp ?? own;
    const [value, setValue] = useControlled<string>({
      controlled: valueProp,
      default: defaultValue ?? '',
      name: 'Dropdown',
      state: 'value',
    });
    const [open, setOpen] = useControlled<boolean>({
      controlled: openProp,
      default: false,
      name: 'Dropdown',
      state: 'open',
    });
    const look = { size, disabled, error, open };
    // What shows depends on whether it is open (Dropdown's chevron turns up).
    const parts = solarDropdownCompose(look, open ? 'open' : 'default');
    // The chosen row's words, or the placeholder.
    const chosen = Children.toArray(children).find(
      (row) =>
        isValidElement<{ value?: unknown }>(row) && row.props.value === value,
    );
    const words = isValidElement<{ children?: ReactNode }>(chosen)
      ? chosen.props.children
      : placeholder;
    const drawing: LayerDrawing = {
      prefix: 'SolarDropdown',
      tree: TREE,
      // A part left empty is not drawn; the panel is MUI's menu, drawn below.
      parts: {
        ...parts,
        label: { ...parts.label, present: label != null },
        mandatory: { ...parts.mandatory, present: mandatory },
        helper: { ...parts.helper, present: helper != null },
        leadingIcon: { ...parts.leadingIcon, present: leadingIcon != null },
        trailingIcon: { ...parts.trailingIcon, present: trailingIcon != null },
      },
      text: {
        labelLabel: label,
        mandatory: <span aria-hidden>*</span>,
        fieldLabel: words,
      },
      icons: {
        iconChevronDown: <IconChevronDown />,
        iconChevronUp: <IconChevronUp />,
        leadingIcon: <span>{leadingIcon}</span>,
        trailingIcon: <span>{trailingIcon}</span>,
      },
      render: {
        label: (layer) => <label id={`${id}-label`} htmlFor={id} {...layer} />,
        // The field is MUI's InputBase around its Select, the combobox the choice's words are in.
        // Its rows take its size, as a Dropdown Menu's do.
        field: (layer) => (
          <DropdownMenuSizeContext.Provider value={size ?? 'md'}>
            <MuiSelect
              {...rest}
              input={
                <InputBase className={layer.className} style={layer.style} />
              }
              id={id}
              labelId={label != null ? `${id}-label` : undefined}
              value={value}
              displayEmpty
              // The combobox draws the field's parts, as Figma nests them: the choice's words and the
              // chevron, which is no icon of MUI's.
              renderValue={() => drawChildren('field', drawing)}
              IconComponent={NoIcon}
              open={open}
              onOpen={(event) => {
                setOpen(true);
                onOpen?.(event);
              }}
              onClose={(event) => {
                setOpen(false);
                onClose?.(event);
              }}
              onChange={(event) => {
                setValue(event.target.value);
                onChange?.(event, event.target.value);
              }}
              disabled={disabled}
              error={error}
              SelectDisplayProps={{
                'aria-describedby': helper != null ? `${id}-helper` : undefined,
              }}
              MenuProps={{
                ...MenuProps,
                // In the component, so the recipe reaches the panel.
                disablePortal: true,
                slotProps: {
                  ...MenuProps?.slotProps,
                  paper: {
                    className: 'SolarDropdownMenu',
                    sx: solarDropdownMenuStyle({ size }),
                  },
                  list: { disablePadding: true },
                },
              }}
            >
              {children}
            </MuiSelect>
          </DropdownMenuSizeContext.Provider>
        ),
        helper: (layer) => (
          <span
            id={`${id}-helper`}
            className={layer.className}
            style={layer.style}
          >
            {helper}
          </span>
        ),
      },
    };
    return (
      <Box
        ref={ref}
        className={
          [
            open ? 'SolarDropdown-open' : null,
            error ? 'SolarDropdown-error' : null,
            disabled ? 'SolarDropdown-disabled' : null,
            className,
          ]
            .filter(Boolean)
            .join(' ') || undefined
        }
        style={style}
        sx={[solarDropdownStyle(look), ...(Array.isArray(sx) ? sx : [sx])]}
      >
        {drawChildren('root', drawing)}
      </Box>
    );
  },
);
