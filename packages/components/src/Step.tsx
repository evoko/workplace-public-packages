// Generated by @bwp-web/codegen from packages/codegen/src/components/step.mjs. Do not edit.
/**
 * SOLAR Step.
 *
 * Generated from its template in `packages/codegen/src/components/` on every `npm run
 * solar:codegen`: change the template there, never this file. What it looks like is not here. That
 * is the recipe, `solarStepStyle` and `solarStepCompose` in `@bwp-web/styles/mui`: its label’s text
 * style and ink, and its line, by status and type.
 *
 * Bespoke: one step of a Stepper, drawn from Figma's layer tree (`internal/layers.tsx`): its
 * StepperIndicator in the variant Figma draws for its `status` and its `label` under it (`round`),
 * or its numbered label over a line (`horizontal`). A button where it has `onClick` (a completed
 * step the flow lets one go back to), words otherwise. A Stepper gives it its status. The app must
 * load `@bwp-web/styles/tokens.css`.
 */

import Box, { type BoxProps } from '@mui/material/Box';
import { StepperIndicator } from './StepperIndicator.js';
import { forwardRef, type MouseEventHandler, type ReactNode } from 'react';
import {
  solarStepCompose,
  solarStepStyle,
  type SolarStepProps,
} from '@bwp-web/styles/mui';
import { drawChildren } from './internal/layers.js';

/** Each layer's children, as Figma nests them. */
const TREE: Record<string, string[]> = {
  root: ['stepperIndicator', 'step', 'label', 'line'],
};
const SLOTS: Record<string, string> = {};

export interface StepProps
  extends
    SolarStepProps,
    // MUI types BoxProps' ref for any element; the component's own comes from forwardRef.
    Omit<BoxProps, keyof SolarStepProps | 'children' | 'ref' | 'onClick'> {
  /** The step's name. */
  label: ReactNode;
  /** The step's number, from 1. */
  number: number;
  /** Called when it is chosen: it is a button. */
  onClick?: MouseEventHandler<HTMLElement>;
}

export const Step = forwardRef<HTMLElement, StepProps>(function Step(
  { status, type, label, number, onClick, sx, ...rest },
  ref,
) {
  const composed = solarStepCompose({ status, type });
  const parts = {
    ...composed,
    step: { ...composed.step, present: composed.step?.present !== false },
  };
  return (
    <Box
      component={onClick ? 'button' : 'span'}
      ref={ref}
      type={onClick ? 'button' : undefined}
      onClick={onClick}
      {...rest}
      sx={[
        solarStepStyle({ status, type }),
        ...(Array.isArray(sx) ? sx : [sx]),
      ]}
    >
      {drawChildren('root', {
        prefix: 'SolarStep',
        tree: TREE,
        slots: SLOTS,
        parts,
        text: {
          step: label,
          label: (
            <>
              {number}. {label}
            </>
          ),
        },
        content: {
          stepperIndicator: (
            <StepperIndicator
              number={number}
              status={composed.stepperIndicator['variant.status'] as never}
            />
          ),
        },
      })}
    </Box>
  );
});
