// Generated by @bwp-web/codegen from packages/codegen/src/components/options-list.mjs. Do not edit.
/**
 * SOLAR Options List.
 *
 * Generated from its template in `packages/codegen/src/components/` on every `npm run
 * solar:codegen`: change the template there, never this file. What it looks like is not here. That
 * is the recipe, `solarOptionsListStyle` and `solarOptionsListCompose` in `@bwp-web/styles/mui`:
 * the list’s gap, and its rows’ place.
 *
 * Bespoke: the fieldset around OptionRows that answer one question, drawn from Figma's layer tree
 * (`internal/layers.tsx`). Its `label` is the question, the fieldset's legend, which a screen
 * reader reads and the page does not show (Figma draws none); where the question is on the page
 * already, pass it. One kind of control a list: radios go in MUI's RadioGroup inside it, one group.
 * Above five choices of one, SOLAR says, use a Select. The app must load
 * `@bwp-web/styles/tokens.css`.
 */

import Box, { type BoxProps } from '@mui/material/Box';
import { forwardRef, type ReactNode } from 'react';
import {
  solarOptionsListCompose,
  solarOptionsListStyle,
  type SolarOptionsListProps,
} from '@bwp-web/styles/mui';
import { drawChildren } from './internal/layers.js';

/** Each layer's children, as Figma nests them. */
const TREE: Record<string, string[]> = {
  root: ['content'],
  content: ['optionRow', 'optionRow2'],
};
const SLOTS: Record<string, string> = { content: 'content' };

export interface OptionsListProps
  extends
    SolarOptionsListProps,
    // MUI types BoxProps' ref for any element; the component's own comes from forwardRef.
    Omit<BoxProps, keyof SolarOptionsListProps | 'children' | 'ref'> {
  /** The question the rows answer: the fieldset's legend, read by a screen reader. */
  label: string;
  /** The rows: OptionRows of one control. */
  children: ReactNode;
}

export const OptionsList = forwardRef<HTMLFieldSetElement, OptionsListProps>(
  function OptionsList({ label, children, sx, ...rest }, ref) {
    const parts = solarOptionsListCompose({});
    return (
      <Box
        component="fieldset"
        ref={ref}
        {...rest}
        sx={[solarOptionsListStyle({}), ...(Array.isArray(sx) ? sx : [sx])]}
      >
        <legend className="SolarOptionsList-legend">{label}</legend>
        {drawChildren('root', {
          prefix: 'SolarOptionsList',
          tree: TREE,
          slots: SLOTS,
          parts,
          content: { content: children },
        })}
      </Box>
    );
  },
);
