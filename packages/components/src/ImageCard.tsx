// Generated by @bwp-web/codegen from packages/codegen/src/components/image-card.mjs. Do not edit.
/**
 * SOLAR Image Card.
 *
 * Generated from its template in `packages/codegen/src/components/` on every `npm run
 * solar:codegen`: change the template there, never this file. What it looks like is not here. That
 * is the recipe, `solarImageCardStyle` and `solarImageCardCompose` in `@bwp-web/styles/mui`: the
 * tile's fill, edge, shadow and focus ring, the image's frame, its words' ink, the Checkbox it
 * shows, and the unfilled tile's Plus.
 *
 * An image in a grid of them: its `image`, its `title` and `subtitle`, and a More menu of
 * `moreItems`; or, not `filled`, the tile that adds one, a Plus over its `label`. Given
 * `onSelectedChange`, it is selectable by a SOLAR Checkbox (named by `selectLabel`), shown where it
 * is `selected` and while the pointer or the keyboard is on the tile. Given `onClick` or `href`, it
 * is pressable: its title (or label) is the button or link, and its hit area the whole tile, the
 * Checkbox and the More menu reachable above it; it is focused only then. Drawn from Figma's layer
 * tree (`internal/layers.tsx`). The app must load `@bwp-web/styles/tokens.css`.
 */

import Box, { type BoxProps } from '@mui/material/Box';
import ButtonBase from '@mui/material/ButtonBase';
import { IconPlus } from '@bwp-web/assets';
import {
  forwardRef,
  useRef,
  useState,
  type FocusEvent,
  type MouseEvent,
  type ReactNode,
} from 'react';
import {
  solarImageCardCompose,
  solarImageCardStyle,
  type SolarImageCardProps,
} from '@bwp-web/styles/mui';
import type { CardMoreItem } from './Card.js';
import { Checkbox } from './Checkbox.js';
import { DropdownItem } from './DropdownItem.js';
import { DropdownMenu } from './DropdownMenu.js';
import { drawChildren, type DrawnLayer } from './internal/layers.js';

/** Each layer's children, as Figma nests them. */
const TREE: Record<string, string[]> = {
  root: ['image', 'text', 'iconCircle', 'label'],
  image: ['checkbox'],
  text: ['title', 'icon', 'subtitle'],
  iconCircle: ['iconPlus'],
};

export interface ImageCardProps
  extends
    SolarImageCardProps,
    // MUI types BoxProps' ref for any element; the component's own, a <div>, comes from forwardRef.
    Omit<BoxProps, keyof SolarImageCardProps | 'title' | 'onClick' | 'ref'> {
  /** The image's name; its action's name where it is pressable. */
  title?: ReactNode;
  /** Its details (“Last modified 2h ago”). */
  subtitle?: ReactNode;
  /** What the unfilled tile adds (“Create new”); its action's name where it is pressable. */
  label?: ReactNode;
  /** The picture's address, filling the image's frame. */
  image?: string;
  /** Called with whether it is to be selected, by its Checkbox, which it then shows. */
  onSelectedChange?: (selected: boolean) => void;
  /** The Checkbox's accessible name. */
  selectLabel?: string;
  /** The More menu's actions: a button opens them. */
  moreItems?: CardMoreItem[];
  /** The More button's accessible name. */
  moreLabel?: string;
  /** Makes it pressable: its title is a button that calls it. */
  onClick?: (event: MouseEvent<HTMLElement>) => void;
  /** Makes it pressable: its title is a link here. */
  href?: string;
}

export const ImageCard = forwardRef<HTMLDivElement, ImageCardProps>(
  function ImageCard(
    {
      filled = true,
      selected = false,
      title,
      subtitle,
      label,
      image,
      onSelectedChange,
      selectLabel = 'Select',
      moreItems,
      moreLabel = 'More actions',
      onClick,
      href,
      className,
      sx,
      onMouseEnter,
      onMouseLeave,
      onFocus,
      onBlur,
      ...rest
    },
    ref,
  ) {
    const look = { filled, selected };
    const composed = solarImageCardCompose(look);
    const pressable = onClick != null || href != null;
    const menu = moreItems != null && moreItems.length > 0;
    // The Checkbox shows where the tile is selected, and while the pointer or the keyboard is on
    // it, as Figma draws it hovered.
    const [pointer, setPointer] = useState(false);
    const [within, setWithin] = useState(false);
    const selectable = filled && onSelectedChange != null;
    const parts = {
      ...composed,
      checkbox: {
        ...composed.checkbox,
        present: selectable && (selected || pointer || within),
      },
      icon: {
        ...composed.icon,
        present: composed.icon?.present !== false && menu,
      },
      title: {
        ...composed.title,
        present: composed.title?.present !== false && title != null,
      },
      subtitle: {
        ...composed.subtitle,
        present: composed.subtitle?.present !== false && subtitle != null,
      },
      label: {
        ...composed.label,
        present: composed.label?.present !== false && label != null,
      },
    };
    const [open, setOpen] = useState(false);
    const moreRef = useRef<HTMLButtonElement>(null);
    // Pressable, its title (or the unfilled tile's label) is its action, stretched over the tile.
    const press = (words: ReactNode) =>
      pressable ? (
        <ButtonBase
          className="SolarImageCard-press"
          disableRipple
          {...(href != null ? { href } : {})}
          onClick={onClick}
        >
          {words}
        </ButtonBase>
      ) : (
        words
      );
    return (
      <>
        <Box
          ref={ref}
          className={
            [pressable ? 'SolarImageCard-pressable' : null, className]
              .filter(Boolean)
              .join(' ') || undefined
          }
          {...rest}
          onMouseEnter={(e: MouseEvent<HTMLDivElement>) => {
            setPointer(true);
            onMouseEnter?.(e);
          }}
          onMouseLeave={(e: MouseEvent<HTMLDivElement>) => {
            setPointer(false);
            onMouseLeave?.(e);
          }}
          onFocus={(e: FocusEvent<HTMLDivElement>) => {
            setWithin(true);
            onFocus?.(e);
          }}
          onBlur={(e: FocusEvent<HTMLDivElement>) => {
            if (!e.currentTarget.contains(e.relatedTarget as Node | null))
              setWithin(false);
            onBlur?.(e);
          }}
          sx={[solarImageCardStyle(look), ...(Array.isArray(sx) ? sx : [sx])]}
        >
          {drawChildren('root', {
            prefix: 'SolarImageCard',
            tree: TREE,
            parts,
            text: { title: press(title), subtitle, label: press(label) },
            icons: { iconPlus: <IconPlus /> },
            render: {
              // The picture fills the image's frame, under the Checkbox.
              image: ({ className: c, style, children }: DrawnLayer) => (
                <span className={c} style={style}>
                  {image != null ? <img src={image} alt="" /> : null}
                  {children}
                </span>
              ),
              // The selection, a SOLAR Checkbox in its layer's element, named by selectLabel.
              checkbox: ({ className: c, style }: DrawnLayer) => (
                <span className={c} style={style}>
                  <Checkbox
                    checked={selected}
                    onChange={(e) => onSelectedChange?.(e.target.checked)}
                    slotProps={{ input: { 'aria-label': selectLabel } }}
                  />
                </span>
              ),
              // The More mark, Figma's drawing, is its menu's button.
              icon: ({ className: c, style, children }: DrawnLayer) => (
                <ButtonBase
                  ref={moreRef}
                  className={c}
                  style={style}
                  disableRipple
                  aria-label={moreLabel}
                  aria-haspopup="menu"
                  aria-expanded={open}
                  onClick={() => setOpen(true)}
                >
                  {children}
                </ButtonBase>
              ),
            },
          })}
        </Box>
        {menu ? (
          <DropdownMenu
            anchorEl={moreRef.current}
            open={open}
            onClose={() => setOpen(false)}
          >
            {moreItems.map((item, i) => (
              <DropdownItem
                key={i}
                disabled={item.disabled}
                icon={item.icon}
                onClick={() => {
                  setOpen(false);
                  item.onSelect();
                }}
              >
                {item.label}
              </DropdownItem>
            ))}
          </DropdownMenu>
        ) : null}
      </>
    );
  },
);
