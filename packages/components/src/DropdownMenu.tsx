// Generated by @bwp-web/codegen from packages/codegen/src/components/dropdown-menu.mjs. Do not edit.
/**
 * SOLAR Dropdown Menu.
 *
 * Generated from its template in `packages/codegen/src/components/` on every `npm run
 * solar:codegen`: change the template there, never this file. What it looks like is not here. That
 * is the recipe, `solarDropdownMenuStyle` and `solarDropdownMenuCompose` in `@bwp-web/styles/mui`:
 * the surface’s fill, edge, corners and shadow.
 *
 * The surface of a trigger's DropdownItems and DropdownGroupLabels, drawn from Figma's layer tree
 * (`internal/layers.tsx`) around MUI's MenuList: the arrow keys move the focus from row to row,
 * Home and End to the ends, and a typed letter to a row it starts; its rows take its size. Given
 * `anchorEl` (its trigger), it floats under it while `open`, in MUI's Popover: Escape, a click
 * outside or Tab call `onClose`, the focus is held in it and goes back to the trigger. Without one,
 * it draws in place. Past 300px its rows scroll, as SOLAR's description says. Name it for a screen
 * reader with `aria-labelledby`, its trigger's id. The app must load `@bwp-web/styles/tokens.css`.
 */

import Box, { type BoxProps } from '@mui/material/Box';
import MenuList, { type MenuListProps } from '@mui/material/MenuList';
import {
  createContext,
  forwardRef,
  useContext,
  type KeyboardEvent,
  type ReactNode,
} from 'react';
import {
  solarDropdownMenuCompose,
  solarDropdownMenuStyle,
  type SolarDropdownMenuProps,
} from '@bwp-web/styles/mui';
import { Float, floats, type Floating } from './internal/float.js';
import { drawChildren } from './internal/layers.js';

/** Each layer's children, as Figma nests them. */
const TREE: Record<string, string[]> = {
  root: ['content'],
  content: [
    'dropdownGroupLabel',
    'dropdownItem',
    'dropdownItem2',
    'dropdownItem3',
    'dropdownItem4',
    'dropdownItem5',
    'dropdownItem6',
  ],
};

/** The menu's size, which its rows take; a picker that holds its own panel of rows gives it too. */
export const DropdownMenuSizeContext = createContext<
  SolarDropdownMenuProps['size'] | undefined
>(undefined);

/** The size of the DropdownMenu around a row, which the row takes; undefined outside one. */
export const useDropdownMenuSize = () => useContext(DropdownMenuSizeContext);

export interface DropdownMenuProps
  extends
    SolarDropdownMenuProps,
    Floating,
    // MUI types BoxProps' ref for any element; the component's own comes from forwardRef.
    Omit<
      BoxProps,
      keyof SolarDropdownMenuProps | keyof Floating | 'children' | 'ref'
    > {
  /** The rows and headings: DropdownItems, and DropdownGroupLabels between them. */
  children: ReactNode;
  /** More of the list's props: a combobox's listbox (`role`, `id`, its handlers). */
  listProps?: MenuListProps;
}

export const DropdownMenu = forwardRef<HTMLDivElement, DropdownMenuProps>(
  function DropdownMenu(
    {
      size,
      children,
      anchorEl,
      anchorPosition,
      open,
      onClose,
      keepFocus,
      popperProps,
      listProps,
      sx,
      ...rest
    },
    ref,
  ) {
    const floating = floats({ anchorEl, anchorPosition });
    const parts = solarDropdownMenuCompose({ size });
    // Tab leaves a floating menu, as MUI's Menu does: it closes, and the focus moves on.
    const onKeyDown = (event: KeyboardEvent) => {
      if (floating && !keepFocus && event.key === 'Tab') {
        event.preventDefault();
        onClose?.();
      }
    };
    return (
      <DropdownMenuSizeContext.Provider value={size ?? 'md'}>
        <Float
          anchorEl={anchorEl}
          anchorPosition={anchorPosition}
          open={open}
          onClose={onClose}
          keepFocus={keepFocus}
          popperProps={popperProps}
        >
          <Box
            ref={ref}
            {...rest}
            sx={[
              solarDropdownMenuStyle({ size }),
              ...(Array.isArray(sx) ? sx : [sx]),
            ]}
          >
            {drawChildren('root', {
              prefix: 'SolarDropdownMenu',
              tree: TREE,
              parts,
              content: { content: children },
              // The content is the list the keyboard moves along; a floating one focuses its
              // first row as it opens.
              render: {
                content: ({ className, style, children: rows }) => (
                  <MenuList
                    className={className}
                    style={style}
                    disablePadding
                    autoFocusItem={floating && open && !keepFocus}
                    onKeyDown={onKeyDown}
                    {...listProps}
                  >
                    {rows}
                  </MenuList>
                ),
              },
            })}
          </Box>
        </Float>
      </DropdownMenuSizeContext.Provider>
    );
  },
);
