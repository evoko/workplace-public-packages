// Generated by @bwp-web/codegen from packages/codegen/src/components/status-card.mjs. Do not edit.
/**
 * SOLAR Status Card.
 *
 * Generated from its template in `packages/codegen/src/components/` on every `npm run
 * solar:codegen`: change the template there, never this file. What it looks like is not here. That
 * is the recipe, `solarStatusCardStyle` and `solarStatusCardCompose` in `@bwp-web/styles/mui`: the
 * surface’s edge, shadow and focus ring by state, its words’ ink, the StatusIndicator it shows, and
 * the placeholders.
 *
 * A figure and its status at a glance, on a dashboard: its `title`, a More menu of `moreItems`, and
 * the `value` beside the StatusIndicator of its `status`, named by `statusLabel` (its status's word
 * by default), so the status is read, not only seen. Given `onClick` or `href`, it is pressable:
 * its title is the button or link, and its hit area the whole card, the More menu reachable above
 * it; it is hovered and focused only then. `loading` draws Figma's placeholders, announced busy.
 * Drawn from Figma's layer tree (`internal/layers.tsx`). The app must load
 * `@bwp-web/styles/tokens.css`.
 */

import Box, { type BoxProps } from '@mui/material/Box';
import ButtonBase from '@mui/material/ButtonBase';
import { IconMore } from '@bwp-web/assets';
import {
  forwardRef,
  useRef,
  useState,
  type MouseEvent,
  type ReactNode,
} from 'react';
import {
  solarStatusCardCompose,
  solarStatusCardStyle,
  type SolarStatusCardProps,
} from '@bwp-web/styles/mui';
import type { CardMoreItem } from './Card.js';
import { DropdownItem } from './DropdownItem.js';
import { DropdownMenu } from './DropdownMenu.js';
import { drawChildren, type DrawnLayer } from './internal/layers.js';
import {
  StatusIndicator,
  type StatusIndicatorProps,
} from './StatusIndicator.js';

/** Each layer's children, as Figma nests them. */
const TREE: Record<string, string[]> = {
  root: ['title', 'trend', 'skeleton', 'skeleton2'],
  title: ['titleTitle', 'iconMore'],
  trend: ['statusIndicator', 'value'],
};

export interface StatusCardProps
  extends
    SolarStatusCardProps,
    // MUI types BoxProps' ref for any element; the component's own, a <div>, comes from forwardRef.
    Omit<
      BoxProps,
      | keyof SolarStatusCardProps
      | 'title'
      | 'value'
      | 'statusLabel'
      | 'onClick'
      | 'ref'
    > {
  /** What the card counts, in a few words; its action’s name where it is pressable. */
  title: ReactNode;
  /** The figure: a count, a share. */
  value: ReactNode;
  /** The status’s name, which the StatusIndicator announces; its word by default. */
  statusLabel?: string;
  /** The More menu's actions: a button opens them. */
  moreItems?: CardMoreItem[];
  /** The More button's accessible name. */
  moreLabel?: string;
  /** Makes it pressable: its title is a button that calls it. */
  onClick?: (event: MouseEvent<HTMLElement>) => void;
  /** Makes it pressable: its title is a link here. */
  href?: string;
}

export const StatusCard = forwardRef<HTMLDivElement, StatusCardProps>(
  function StatusCard(
    {
      status = 'success',
      disabled = false,
      loading = false,
      title,
      value,
      statusLabel,
      moreItems,
      moreLabel = 'More actions',
      onClick,
      href,
      className,
      sx,
      ...rest
    },
    ref,
  ) {
    const look = { status, disabled, loading };
    const composed = solarStatusCardCompose(look);
    // Pressable while it loads too, as Figma draws a loading card hovered: its title, the action,
    // is then its name alone.
    const pressable = (onClick != null || href != null) && !disabled;
    const menu = moreItems != null && moreItems.length > 0;
    const dot = composed.statusIndicator;
    // The status is read as well as seen: its word, where the caller names it no other way.
    const named =
      statusLabel ?? status.charAt(0).toUpperCase() + status.slice(1);
    // A slot left empty is not drawn.
    const parts = {
      ...composed,
      iconMore: {
        ...composed.iconMore,
        present: composed.iconMore?.present !== false && menu,
      },
    };
    const [open, setOpen] = useState(false);
    const moreRef = useRef<HTMLButtonElement>(null);
    // Pressable, its title is its action, stretched over the card.
    const titled = pressable ? (
      <ButtonBase
        className="SolarStatusCard-press"
        disableRipple
        {...(href != null ? { href } : {})}
        onClick={onClick}
      >
        {title}
      </ButtonBase>
    ) : (
      title
    );
    // Loading, where its title is not drawn (Card's; a Device Card's is), the action is drawn in
    // its place, its name alone.
    const waiting =
      pressable &&
      loading &&
      !['titleTitle'].some((l) => composed[l]?.present !== false) ? (
        <ButtonBase
          className="SolarStatusCard-press"
          disableRipple
          {...(href != null ? { href } : {})}
          onClick={onClick}
        >
          <span className="SolarStatusCard-name">{title}</span>
        </ButtonBase>
      ) : null;
    return (
      <>
        <Box
          ref={ref}
          aria-busy={loading || undefined}
          aria-disabled={disabled || undefined}
          className={
            [
              pressable ? 'SolarStatusCard-pressable' : null,
              disabled ? 'SolarStatusCard-disabled' : null,
              className,
            ]
              .filter(Boolean)
              .join(' ') || undefined
          }
          {...rest}
          sx={[solarStatusCardStyle(look), ...(Array.isArray(sx) ? sx : [sx])]}
        >
          {waiting}
          {drawChildren('root', {
            prefix: 'SolarStatusCard',
            tree: TREE,
            parts,
            text: { titleTitle: titled, value },
            icons: {
              iconMore: (
                <ButtonBase
                  ref={moreRef}
                  disableRipple
                  disabled={disabled}
                  aria-label={moreLabel}
                  aria-haspopup="menu"
                  aria-expanded={open}
                  onClick={() => setOpen(true)}
                >
                  <IconMore />
                </ButtonBase>
              ),
            },
            render: {
              // The mark is a StatusIndicator, in the type the recipe names, in its layer's element, named by
              // the status's word.
              statusIndicator: ({ className: c, style }: DrawnLayer) => (
                <span className={c} style={style}>
                  <StatusIndicator
                    type={dot['variant.type'] as StatusIndicatorProps['type']}
                    size={dot['variant.size'] as StatusIndicatorProps['size']}
                    label={named}
                  />
                </span>
              ),
            },
          })}
        </Box>
        {menu ? (
          <DropdownMenu
            anchorEl={moreRef.current}
            open={open}
            onClose={() => setOpen(false)}
          >
            {moreItems.map((item, i) => (
              <DropdownItem
                key={i}
                disabled={item.disabled}
                icon={item.icon}
                onClick={() => {
                  setOpen(false);
                  item.onSelect();
                }}
              >
                {item.label}
              </DropdownItem>
            ))}
          </DropdownMenu>
        ) : null}
      </>
    );
  },
);
