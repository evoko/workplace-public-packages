// Generated by @bwp-web/codegen from packages/codegen/src/components/timepicker-dropdown.mjs. Do not edit.
/**
 * SOLAR TimePicker Dropdown.
 *
 * Generated from its template in `packages/codegen/src/components/` on every `npm run
 * solar:codegen`: change the template there, never this file. What it looks like is not here. That
 * is the recipe, `solarTimePickerDropdownStyle` and `solarTimePickerDropdownCompose` in
 * `@bwp-web/styles/mui`: the surface’s fill, edge, corners and shadow.
 *
 * The times a TimePicker picks from, one list, drawn from Figma's layer tree
 * (`internal/layers.tsx`) around MUI's MenuList as a listbox: every `step` minutes (30 by default)
 * from `min` to `max` (the whole day), each a DropdownItem, as the locale writes it, on its 12- or
 * 24-hour clock, the `value` selected and in sight as it opens. The arrow keys move from time to
 * time, Home and End to the ends; a click or Enter calls `onChange` with the time, `HH:mm`
 * (`09:30`), with no date. Given `anchorEl`, it floats under it while `open`, in MUI's Popover:
 * Escape, a click outside or Tab call `onClose`. Without one, it draws in place. Past 300px its
 * rows scroll, as a Dropdown Menu's. The app must load `@bwp-web/styles/tokens.css`.
 */

import Box, { type BoxProps } from '@mui/material/Box';
import MenuList, { type MenuListProps } from '@mui/material/MenuList';
import { forwardRef, useLayoutEffect, useRef, type KeyboardEvent } from 'react';
import {
  solarTimePickerDropdownCompose,
  solarTimePickerDropdownStyle,
  type SolarTimePickerDropdownProps,
} from '@bwp-web/styles/mui';
import { DropdownItem, type DropdownItemProps } from './DropdownItem.js';
import { DropdownMenuSizeContext } from './DropdownMenu.js';
import {
  hhmmOf,
  minutesOf,
  parseHhmm,
  timesOf,
  timeWords,
} from './internal/clock.js';
import { Float, floats, type Floating } from './internal/float.js';
import { drawChildren } from './internal/layers.js';

/** Each layer's children, as Figma nests them. */
const TREE: Record<string, string[]> = {
  root: ['content'],
  content: [
    'dropdownItem',
    'dropdownItem2',
    'dropdownItem3',
    'dropdownItem4',
    'dropdownItem5',
    'dropdownItem6',
  ],
};
const SLOTS: Record<string, string> = { content: 'content' };

export interface TimePickerDropdownProps
  extends
    SolarTimePickerDropdownProps,
    Floating,
    // MUI types BoxProps' ref for any element; the component's own comes from forwardRef.
    Omit<
      BoxProps,
      | keyof SolarTimePickerDropdownProps
      | keyof Floating
      | 'children'
      | 'onChange'
      | 'ref'
    > {
  /** The time chosen, `HH:mm` (`09:30`); null for none. */
  value?: string | null;
  /** Called with the time chosen, `HH:mm`. */
  onChange?: (value: string) => void;
  /** The minutes between one time and the next. */
  step?: number;
  /** The earliest and latest times, `HH:mm`. */
  min?: string;
  max?: string;
  /** The locale its times are written in, on its clock; the page's by default. */
  locale?: string;
  /** More of a row's props, over those the list gives it, by its time (`HH:mm`): a test id. */
  optionProps?: (time: string) => Partial<DropdownItemProps> | undefined;
  /** More of the list's props: a combobox's listbox (`role`, `id`, its handlers). */
  listProps?: MenuListProps;
}

export const TimePickerDropdown = forwardRef<
  HTMLDivElement,
  TimePickerDropdownProps
>(function TimePickerDropdown(
  {
    size,
    value = null,
    onChange,
    step = 30,
    min,
    max,
    locale,
    optionProps,
    anchorEl,
    anchorPosition,
    open,
    onClose,
    keepFocus,
    popperProps,
    listProps,
    sx,
    ...rest
  },
  ref,
) {
  const floating = floats({ anchorEl, anchorPosition });
  const parts = solarTimePickerDropdownCompose({ size });
  const chosen = parseHhmm(value);
  const list = useRef<HTMLUListElement>(null);
  // The chosen time in sight as the list first draws: scrolled within the list, not the page.
  useLayoutEffect(() => {
    const at = list.current;
    const row = at?.querySelector<HTMLElement>('[aria-selected="true"]');
    if (at && row)
      at.scrollTop +=
        row.getBoundingClientRect().top - at.getBoundingClientRect().top;
  }, []);
  const rows = timesOf(step, parseHhmm(min), parseHhmm(max)).map((t) => (
    <DropdownItem
      key={hhmmOf(t)}
      role="option"
      selected={chosen !== null && minutesOf(chosen) === minutesOf(t)}
      onClick={() => onChange?.(hhmmOf(t))}
      {...optionProps?.(hhmmOf(t))}
    >
      {timeWords(t, locale)}
    </DropdownItem>
  ));
  // Tab leaves a floating menu, as MUI's Menu does: it closes, and the focus moves on.
  const onKeyDown = (event: KeyboardEvent) => {
    if (floating && !keepFocus && event.key === 'Tab') {
      event.preventDefault();
      onClose?.();
    }
  };
  return (
    <DropdownMenuSizeContext.Provider value={size ?? 'md'}>
      <Float
        anchorEl={anchorEl}
        anchorPosition={anchorPosition}
        open={open}
        onClose={onClose}
        keepFocus={keepFocus}
        popperProps={popperProps}
      >
        <Box
          ref={ref}
          {...rest}
          sx={[
            solarTimePickerDropdownStyle({ size }),
            ...(Array.isArray(sx) ? sx : [sx]),
          ]}
        >
          {drawChildren('root', {
            prefix: 'SolarTimePickerDropdown',
            tree: TREE,
            slots: SLOTS,
            parts,
            content: { content: rows },
            // The content is the list the keyboard moves along; a floating one focuses its
            // first row as it opens.
            render: {
              content: ({ className, style, children: rows }) => (
                <MenuList
                  ref={list}
                  className={className}
                  style={style}
                  role="listbox"
                  disablePadding
                  autoFocusItem={floating && open && !keepFocus}
                  onKeyDown={onKeyDown}
                  {...listProps}
                >
                  {rows}
                </MenuList>
              ),
            },
          })}
        </Box>
      </Float>
    </DropdownMenuSizeContext.Provider>
  );
});
