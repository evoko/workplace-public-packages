// Generated by @bwp-web/codegen from packages/codegen/src/components/context-menu.mjs. Do not edit.
/**
 * SOLAR Context Menu.
 *
 * Generated from its template in `packages/codegen/src/components/` on every `npm run
 * solar:codegen`: change the template there, never this file. What it looks like is not here. That
 * is the recipe, `solarContextMenuStyle` and `solarContextMenuCompose` in `@bwp-web/styles/mui`:
 * the surface’s fill, edge, corners and shadow.
 *
 * The surface of an object's actions, ContextMenuItems with Dividers between them, drawn from
 * Figma's layer tree (`internal/layers.tsx`) around MUI's MenuList: the arrow keys move the focus
 * from row to row. Given `anchorPosition` (where the pointer was, on a right-click or a long
 * press), it floats there while `open`, in MUI's Popover, kept on the page: Escape, a click outside
 * or Tab call `onClose`, the focus is held in it and goes back. Without one, it draws in place. For
 * a toolbar's actions, use a DropdownMenu. A Divider in it is a list item: `<Divider component="li"
 * />`. The app must load `@bwp-web/styles/tokens.css`.
 */

import { useSolarProps } from './internal/theme.js';
import Box, { type BoxProps } from '@mui/material/Box';
import MenuList, { type MenuListProps } from '@mui/material/MenuList';
import { forwardRef, type KeyboardEvent, type ReactNode } from 'react';
import {
  solarContextMenuCompose,
  solarContextMenuStyle,
  type SolarContextMenuProps,
} from '@bwp-web/styles/mui';
import { Float, floats, type Floating } from './internal/float.js';
import { drawChildren } from './internal/layers.js';

/** Each layer's children, as Figma nests them. */
const TREE: Record<string, string[]> = {
  root: ['content'],
  content: [
    'contextMenuItem',
    'contextMenuItem2',
    'contextMenuItem3',
    'contextMenuItem4',
    'contextMenuItem5',
    'contextMenuItem6',
    'divider',
    'contextMenuItem7',
  ],
};
const SLOTS: Record<string, string> = { content: 'content' };

export interface ContextMenuProps
  extends
    SolarContextMenuProps,
    Floating,
    // MUI types BoxProps' ref for any element; the component's own comes from forwardRef.
    Omit<
      BoxProps,
      keyof SolarContextMenuProps | keyof Floating | 'children' | 'ref'
    > {
  /** The actions: ContextMenuItems, and Dividers between their groups. */
  children: ReactNode;
  /** More of the list's props: a combobox's listbox (`role`, `id`, its handlers). */
  listProps?: MenuListProps;
}

export const ContextMenu = forwardRef<HTMLDivElement, ContextMenuProps>(
  function ContextMenu(inProps, ref) {
    // As the app's MUI theme sets them (components.SolarContextMenu), under the caller's own.
    const {
      children,
      anchorEl,
      anchorPosition,
      open,
      onClose,
      keepFocus,
      popperProps,
      listProps,
      sx,
      ...rest
    } = useSolarProps(inProps, 'SolarContextMenu');
    const floating = floats({ anchorEl, anchorPosition });
    const parts = solarContextMenuCompose({});
    // Tab leaves a floating menu, as MUI's Menu does: it closes, and the focus moves on.
    const onKeyDown = (event: KeyboardEvent) => {
      if (floating && !keepFocus && event.key === 'Tab') {
        event.preventDefault();
        onClose?.();
      }
    };
    return (
      <Float
        anchorEl={anchorEl}
        anchorPosition={anchorPosition}
        open={open}
        onClose={onClose}
        keepFocus={keepFocus}
        popperProps={popperProps}
      >
        <Box
          ref={ref}
          {...rest}
          sx={[solarContextMenuStyle({}), ...(Array.isArray(sx) ? sx : [sx])]}
        >
          {drawChildren('root', {
            prefix: 'SolarContextMenu',
            tree: TREE,
            slots: SLOTS,
            parts,
            content: { content: children },
            // The content is the list the keyboard moves along; a floating one focuses its
            // first row as it opens.
            render: {
              content: ({ className, style, children: rows }) => (
                <MenuList
                  className={className}
                  style={style}
                  disablePadding
                  autoFocusItem={floating && open && !keepFocus}
                  onKeyDown={onKeyDown}
                  {...listProps}
                >
                  {rows}
                </MenuList>
              ),
            },
          })}
        </Box>
      </Float>
    );
  },
);
