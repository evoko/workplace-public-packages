// Generated by @bwp-web/codegen from packages/codegen/src/components/insight-card.mjs. Do not edit.
/**
 * SOLAR Insight Card.
 *
 * Generated from its template in `packages/codegen/src/components/` on every `npm run
 * solar:codegen`: change the template there, never this file. What it looks like is not here. That
 * is the recipe, `solarInsightCardStyle` and `solarInsightCardCompose` in `@bwp-web/styles/mui`:
 * the surface’s fill, edge, shadow and focus ring by state, the severity’s tile and More ink, its
 * words, the StatusIndicator it shows, and the placeholders.
 *
 * One insight in a list of them: the StatusIndicator of its `severity` in a tile, named by
 * `severityLabel` (its severity's word by default), its `title` and `description`, and a More menu
 * of `moreItems`. The `selected` one is the current one of its set (the insight shown beside the
 * list). Given `onClick` or `href`, it is pressable: its title is the button or link, and its hit
 * area the whole card, the More menu reachable above it; it is hovered and focused only then.
 * `loading` draws Figma's placeholders, announced busy. For a smaller one with no menu use an
 * Insight Card Small. Drawn from Figma's layer tree (`internal/layers.tsx`). The app must load
 * `@bwp-web/styles/tokens.css`.
 */

import Box, { type BoxProps } from '@mui/material/Box';
import ButtonBase from '@mui/material/ButtonBase';
import { IconMore } from '@bwp-web/assets';
import {
  forwardRef,
  useRef,
  useState,
  type MouseEvent,
  type ReactNode,
} from 'react';
import {
  solarInsightCardCompose,
  solarInsightCardStyle,
  type SolarInsightCardProps,
} from '@bwp-web/styles/mui';
import type { CardMoreItem } from './Card.js';
import { DropdownItem } from './DropdownItem.js';
import { DropdownMenu } from './DropdownMenu.js';
import { drawChildren, type DrawnLayer } from './internal/layers.js';
import {
  StatusIndicator,
  type StatusIndicatorProps,
} from './StatusIndicator.js';

/** Each layer's children, as Figma nests them. */
const TREE: Record<string, string[]> = {
  root: ['icon', 'content', 'iconMore'],
  icon: ['statusIndicator'],
  content: ['title', 'description', 'skeleton', 'skeleton2'],
};
const SLOTS: Record<string, string> = {
  title: 'title',
  description: 'description',
};

export interface InsightCardProps
  extends
    SolarInsightCardProps,
    // MUI types BoxProps' ref for any element; the component's own, a <div>, comes from forwardRef.
    Omit<
      BoxProps,
      | keyof SolarInsightCardProps
      | 'title'
      | 'description'
      | 'severityLabel'
      | 'onClick'
      | 'ref'
    > {
  /** What the insight is, in a few words; its action’s name where it is pressable. */
  title: ReactNode;
  /** What it means. */
  description?: ReactNode;
  /** The severity’s name, which the StatusIndicator announces; its word by default. */
  severityLabel?: string;
  /** The More menu's actions: a button opens them. */
  moreItems?: CardMoreItem[];
  /** The More button's accessible name. */
  moreLabel?: string;
  /** Makes it pressable: its title is a button that calls it. */
  onClick?: (event: MouseEvent<HTMLElement>) => void;
  /** Makes it pressable: its title is a link here. */
  href?: string;
}

export const InsightCard = forwardRef<HTMLDivElement, InsightCardProps>(
  function InsightCard(
    {
      severity = 'success',
      selected = false,
      loading = false,
      title,
      description,
      severityLabel,
      moreItems,
      moreLabel = 'More actions',
      onClick,
      href,
      className,
      sx,
      ...rest
    },
    ref,
  ) {
    // Figma draws a loading one, with no status: its look is the same for all.
    const look = {
      severity: loading ? ('info' as const) : severity,
      selected,
      loading,
    };
    const composed = solarInsightCardCompose(look);
    // Pressable while it loads too, as Figma draws a loading card hovered: its title, the action,
    // is then its name alone.
    const pressable = onClick != null || href != null;
    const menu = moreItems != null && moreItems.length > 0;
    const dot = composed.statusIndicator;
    // The severity is read as well as seen: its word, where the caller names it no other way.
    const named =
      severityLabel ?? severity.charAt(0).toUpperCase() + severity.slice(1);
    // A slot left empty is not drawn.
    const parts = {
      ...composed,
      description: {
        ...composed.description,
        present: composed.description?.present !== false && description != null,
      },
      iconMore: {
        ...composed.iconMore,
        present: composed.iconMore?.present !== false && menu,
      },
    };
    const [open, setOpen] = useState(false);
    const moreRef = useRef<HTMLButtonElement>(null);
    // Pressable, its title is its action, stretched over the card.
    const titled = pressable ? (
      <ButtonBase
        className="SolarInsightCard-press"
        disableRipple
        // The selected card is the current one of its set (the insight shown beside the list).
        aria-current={selected || undefined}
        {...(href != null ? { href } : {})}
        onClick={onClick}
      >
        {title}
      </ButtonBase>
    ) : (
      title
    );
    // Loading, where its title is not drawn (Card's; a Device Card's is), the action is drawn in
    // its place, its name alone.
    const waiting =
      pressable &&
      loading &&
      !['title'].some((l) => composed[l]?.present !== false) ? (
        <ButtonBase
          className="SolarInsightCard-press"
          disableRipple
          {...(href != null ? { href } : {})}
          onClick={onClick}
        >
          <span className="SolarInsightCard-name">{title}</span>
        </ButtonBase>
      ) : null;
    return (
      <>
        <Box
          ref={ref}
          aria-busy={loading || undefined}
          className={
            [
              pressable ? 'SolarInsightCard-pressable' : null,
              selected ? 'SolarInsightCard-selected' : null,
              className,
            ]
              .filter(Boolean)
              .join(' ') || undefined
          }
          {...rest}
          sx={[solarInsightCardStyle(look), ...(Array.isArray(sx) ? sx : [sx])]}
        >
          {waiting}
          {drawChildren('root', {
            prefix: 'SolarInsightCard',
            tree: TREE,
            slots: SLOTS,
            parts,
            text: { title: titled, description },
            icons: {
              iconMore: (
                <ButtonBase
                  ref={moreRef}
                  disableRipple
                  disabled={false}
                  aria-label={moreLabel}
                  aria-haspopup="menu"
                  aria-expanded={open}
                  onClick={() => setOpen(true)}
                >
                  <IconMore />
                </ButtonBase>
              ),
            },
            render: {
              // The mark is a StatusIndicator, in the type the recipe names, in its layer's element, named by
              // the severity's word.
              statusIndicator: ({ className: c, style }: DrawnLayer) => (
                <span className={c} style={style}>
                  <StatusIndicator
                    type={dot['variant.type'] as StatusIndicatorProps['type']}
                    size={dot['variant.size'] as StatusIndicatorProps['size']}
                    label={named}
                  />
                </span>
              ),
            },
          })}
        </Box>
        {menu ? (
          <DropdownMenu
            anchorEl={moreRef.current}
            open={open}
            onClose={() => setOpen(false)}
          >
            {moreItems.map((item, i) => (
              <DropdownItem
                key={i}
                disabled={item.disabled}
                icon={item.icon}
                onClick={() => {
                  setOpen(false);
                  item.onSelect();
                }}
              >
                {item.label}
              </DropdownItem>
            ))}
          </DropdownMenu>
        ) : null}
      </>
    );
  },
);
