// Generated by @bwp-web/codegen from packages/codegen/src/components/divider.mjs. Do not edit.
/**
 * SOLAR Divider.
 *
 * Generated from its template in `packages/codegen/src/components/` on every `npm run
 * solar:codegen`: change the template there, never this file. What it looks like is not here. That
 * is the recipe, `solarDividerStyle` and `solarDividerCompose` in `@bwp-web/styles/mui`: the rule’s
 * colour and thickness, the label’s text style, and each type’s gap and inset.
 *
 * Bespoke: a rule, an inset rule, or a label between two rules, drawn from Figma’s layer tree
 * (`internal/layers.tsx`). It fills what it separates: a horizontal divider the width it is given,
 * a vertical one the height. A screen reader hears a separator, and a labelled one’s label. The app
 * must load `@bwp-web/styles/tokens.css`.
 */

import Box, { type BoxProps } from '@mui/material/Box';
import { forwardRef, type ReactNode } from 'react';
import {
  solarDividerCompose,
  solarDividerStyle,
  type SolarDividerProps,
} from '@bwp-web/styles/mui';
import { drawChildren } from './internal/layers.js';

/** Each layer's children, as Figma nests them. */
const TREE: Record<string, string[]> = { root: ['rule', 'label', 'rule2'] };
const SLOTS: Record<string, string> = {};

export interface DividerProps
  extends
    SolarDividerProps,
    // MUI types BoxProps' ref for any element; the component's own comes from forwardRef.
    Omit<BoxProps, keyof SolarDividerProps | 'children' | 'ref'> {
  /** For `with-label`: the words between the rules ("Or"). */
  children?: ReactNode;
}

export const Divider = forwardRef<HTMLDivElement, DividerProps>(
  function Divider({ orientation, type, children, sx, ...rest }, ref) {
    const parts = solarDividerCompose({ orientation, type });
    return (
      <Box
        component="div"
        ref={ref}
        role="separator"
        aria-orientation={
          orientation === 'vertical' ? 'vertical' : 'horizontal'
        }
        aria-label={typeof children === 'string' ? children : undefined}
        {...rest}
        sx={[
          solarDividerStyle({ orientation, type }),
          ...(Array.isArray(sx) ? sx : [sx]),
        ]}
      >
        {drawChildren('root', {
          prefix: 'SolarDivider',
          tree: TREE,
          slots: SLOTS,
          parts,
          text: { label: children },
        })}
      </Box>
    );
  },
);
