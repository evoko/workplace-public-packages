// Generated by @bwp-web/codegen from packages/codegen/src/components/interactive-card.mjs. Do not edit.
/**
 * SOLAR Interactive Card.
 *
 * Generated from its template in `packages/codegen/src/components/` on every `npm run
 * solar:codegen`: change the template there, never this file. What it looks like is not here. That
 * is the recipe, `solarInteractiveCardStyle` and `solarInteractiveCardCompose` in
 * `@bwp-web/styles/mui`: the card’s fill, edge and focus ring by selection and drag, its words’ and
 * icon’s ink, and the controls it shows.
 *
 * One choice in a list of them (a room to book, a setting): a drag handle where it can be reordered
 * (`dragHandle`), the `control` that selects it (a Checkbox, a Radio or a Toggle, following
 * `selected` and calling `onSelectedChange`, named by `selectLabel` or its title), an `icon` and
 * its `title`, its `description`, and the caller's `actions` (SOLAR Icon Buttons, sm). `dragging`
 * draws it lifted, as it moves. Given `onClick` or `href`, it is pressable: its title is the button
 * or link, and its hit area the whole card, its control and actions reachable above it; it is
 * focused only then. Drawn from Figma's layer tree (`internal/layers.tsx`). The app must load
 * `@bwp-web/styles/tokens.css`.
 */

import Box, { type BoxProps } from '@mui/material/Box';
import ButtonBase from '@mui/material/ButtonBase';
import { forwardRef, type MouseEvent, type ReactNode } from 'react';
import {
  solarInteractiveCardCompose,
  solarInteractiveCardStyle,
  type SolarInteractiveCardProps,
} from '@bwp-web/styles/mui';
import { drawChildren, type DrawnLayer } from './internal/layers.js';
import { Checkbox } from './Checkbox.js';
import { DragHandle, type DragHandleProps } from './DragHandle.js';
import { Radio } from './Radio.js';
import { Toggle } from './Toggle.js';

/** Each layer's children, as Figma nests them. */
const TREE: Record<string, string[]> = {
  root: ['dragHandle', 'toggle', 'radioButton', 'checkbox', 'text', 'actions'],
  text: ['textIcon', 'description'],
  textIcon: ['icon', 'title'],
  actions: ['iconButton', 'iconButton2', 'iconButton3'],
};

export interface InteractiveCardProps
  extends
    SolarInteractiveCardProps,
    // MUI types BoxProps' ref for any element; the component's own, a <div>, comes from forwardRef.
    Omit<
      BoxProps,
      | keyof SolarInteractiveCardProps
      | 'title'
      | 'icon'
      | 'description'
      | 'actions'
      | 'dragHandle'
      | 'onSelectedChange'
      | 'selectLabel'
      | 'onClick'
      | 'ref'
    > {
  /** What it is, its headline; its action’s name where it is pressable. */
  title: ReactNode;
  /** An icon before the title. */
  icon?: ReactNode;
  /** What it means. */
  description?: ReactNode;
  /** The caller’s actions: SOLAR Icon Buttons, sm, square and secondary. */
  actions?: ReactNode;
  /** Shows a drag handle, where the list can be reordered. */
  dragHandle?: boolean;
  /** Called with whether it is to be selected, by its control. */
  onSelectedChange?: (selected: boolean) => void;
  /** The control’s accessible name; its title, where that is words. */
  selectLabel?: string;
  /** Makes it pressable: its title is a button that calls it. */
  onClick?: (event: MouseEvent<HTMLElement>) => void;
  /** Makes it pressable: its title is a link here. */
  href?: string;
}

export const InteractiveCard = forwardRef<HTMLDivElement, InteractiveCardProps>(
  function InteractiveCard(
    {
      selected = false,
      dragging = false,
      control = 'none',
      title,
      icon,
      description,
      actions,
      dragHandle,
      onSelectedChange,
      selectLabel,
      onClick,
      href,
      className,
      sx,
      ...rest
    },
    ref,
  ) {
    const look = { selected, dragging, control };
    const composed = solarInteractiveCardCompose(look);
    const pressable = onClick != null || href != null;
    // The control is named by the caller, or by the title where it is words.
    const named = selectLabel ?? (typeof title === 'string' ? title : 'Select');
    const handle = composed.dragHandle;
    // A slot left empty is not drawn, and one Figma hides at rest is drawn where it is given.
    const parts = {
      ...composed,
      icon: { ...composed.icon, present: icon != null },
      description: {
        ...composed.description,
        present: composed.description?.present !== false && description != null,
      },
      actions: { ...composed.actions, present: actions != null },
      dragHandle: { ...composed.dragHandle, present: dragHandle === true },
      checkbox: { ...composed.checkbox, present: control === 'checkbox' },
      radioButton: { ...composed.radioButton, present: control === 'radio' },
      toggle: { ...composed.toggle, present: control === 'toggle' },
    };
    // Pressable, its title is its action, stretched over the card.
    const titled = pressable ? (
      <ButtonBase
        className="SolarInteractiveCard-press"
        disableRipple
        // The selected card is the current one of its set (the insight shown beside the list).
        aria-current={selected || undefined}
        {...(href != null ? { href } : {})}
        onClick={onClick}
      >
        {title}
      </ButtonBase>
    ) : (
      title
    );
    return (
      <>
        <Box
          ref={ref}
          className={
            [
              pressable ? 'SolarInteractiveCard-pressable' : null,
              selected ? 'SolarInteractiveCard-selected' : null,
              className,
            ]
              .filter(Boolean)
              .join(' ') || undefined
          }
          {...rest}
          sx={[
            solarInteractiveCardStyle(look),
            ...(Array.isArray(sx) ? sx : [sx]),
          ]}
        >
          {drawChildren('root', {
            prefix: 'SolarInteractiveCard',
            tree: TREE,
            parts,
            text: { title: titled, description },
            icons: {
              icon: <span>{icon}</span>,
              actions: <span>{actions}</span>,
            },
            render: {
              // The drag handle, a SOLAR DragHandle of the size the recipe names.
              dragHandle: ({ className: c, style }: DrawnLayer) => (
                <span className={c} style={style}>
                  <DragHandle
                    size={handle['variant.size'] as DragHandleProps['size']}
                  />
                </span>
              ),
              // The control that selects it, SOLAR's own, following selected.
              checkbox: ({ className: c, style }: DrawnLayer) => (
                <span className={c} style={style}>
                  <Checkbox
                    checked={selected}
                    onChange={(e) => onSelectedChange?.(e.target.checked)}
                    slotProps={{ input: { 'aria-label': named } }}
                  />
                </span>
              ),
              radioButton: ({ className: c, style }: DrawnLayer) => (
                <span className={c} style={style}>
                  <Radio
                    checked={selected}
                    onChange={() => onSelectedChange?.(true)}
                    slotProps={{ input: { 'aria-label': named } }}
                  />
                </span>
              ),
              toggle: ({ className: c, style }: DrawnLayer) => (
                <span className={c} style={style}>
                  <Toggle
                    selected={selected}
                    onChange={(_, on) => onSelectedChange?.(on)}
                    slotProps={{ input: { 'aria-label': named } }}
                  />
                </span>
              ),
            },
          })}
        </Box>
      </>
    );
  },
);
