// Generated by @bwp-web/codegen from packages/codegen/src/components/option-card.mjs. Do not edit.
/**
 * SOLAR Option Card.
 *
 * Generated from its template in `packages/codegen/src/components/` on every `npm run
 * solar:codegen`: change the template there, never this file. What it looks like is not here. That
 * is the recipe, `solarOptionCardStyle` and `solarOptionCardCompose` in `@bwp-web/styles/mui`: the
 * tile’s edge, shadow and focus ring by state, the Plus’s circle and ink, and its label.
 *
 * A tile to create something new, in a grid of them (“New design”): a Plus in a circle over its
 * `label`. Given `onClick` or `href`, it is pressable: its label is the button or link, and its hit
 * area the whole tile; it is hovered and focused only then. The `selected` one is the current one
 * of its set. Drawn from Figma's layer tree (`internal/layers.tsx`). The app must load
 * `@bwp-web/styles/tokens.css`.
 */

import Box, { type BoxProps } from '@mui/material/Box';
import ButtonBase from '@mui/material/ButtonBase';
import { IconPlus } from '@bwp-web/assets';
import { forwardRef, type MouseEvent, type ReactNode } from 'react';
import {
  solarOptionCardCompose,
  solarOptionCardStyle,
  type SolarOptionCardProps,
} from '@bwp-web/styles/mui';
import { drawChildren } from './internal/layers.js';

/** Each layer's children, as Figma nests them. */
const TREE: Record<string, string[]> = {
  root: ['container', 'label'],
  container: ['iconPlus'],
};

export interface OptionCardProps
  extends
    SolarOptionCardProps,
    // MUI types BoxProps' ref for any element; the component's own, a <div>, comes from forwardRef.
    Omit<BoxProps, keyof SolarOptionCardProps | 'label' | 'onClick' | 'ref'> {
  /** What it creates, in a few words; its action’s name where it is pressable. */
  label: ReactNode;
  /** Makes it pressable: its label is a button that calls it. */
  onClick?: (event: MouseEvent<HTMLElement>) => void;
  /** Makes it pressable: its label is a link here. */
  href?: string;
}

export const OptionCard = forwardRef<HTMLDivElement, OptionCardProps>(
  function OptionCard(
    { selected = false, label, onClick, href, className, sx, ...rest },
    ref,
  ) {
    const look = { selected };
    const composed = solarOptionCardCompose(look);
    const pressable = onClick != null || href != null;
    // A slot left empty is not drawn.
    const parts = {
      ...composed,
    };
    // Pressable, its label is its action, stretched over the card.
    const titled = pressable ? (
      <ButtonBase
        className="SolarOptionCard-press"
        disableRipple
        // The selected card is the current one of its set (the insight shown beside the list).
        aria-current={selected || undefined}
        {...(href != null ? { href } : {})}
        onClick={onClick}
      >
        {label}
      </ButtonBase>
    ) : (
      label
    );
    return (
      <>
        <Box
          ref={ref}
          className={
            [
              pressable ? 'SolarOptionCard-pressable' : null,
              selected ? 'SolarOptionCard-selected' : null,
              className,
            ]
              .filter(Boolean)
              .join(' ') || undefined
          }
          {...rest}
          sx={[solarOptionCardStyle(look), ...(Array.isArray(sx) ? sx : [sx])]}
        >
          {drawChildren('root', {
            prefix: 'SolarOptionCard',
            tree: TREE,
            parts,
            text: { label: titled },
            icons: {
              iconPlus: <IconPlus />,
            },
          })}
        </Box>
      </>
    );
  },
);
