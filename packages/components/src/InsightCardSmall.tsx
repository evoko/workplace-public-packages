// Generated by @bwp-web/codegen from packages/codegen/src/components/insight-card-small.mjs. Do not edit.
/**
 * SOLAR Insight Card Small.
 *
 * Generated from its template in `packages/codegen/src/components/` on every `npm run
 * solar:codegen`: change the template there, never this file. What it looks like is not here. That
 * is the recipe, `solarInsightCardSmallStyle` and `solarInsightCardSmallCompose` in
 * `@bwp-web/styles/mui`: the surface’s edge, shadow and focus ring by state, the severity’s tile,
 * its words, the StatusIndicator it shows, and the placeholders.
 *
 * One insight, in a narrow panel or a summary: the StatusIndicator of its `severity` in a tile,
 * named by `severityLabel` (its severity's word by default), and its `title` and `description`.
 * Given `onClick` or `href`, it is pressable: its title is the button or link, and its hit area the
 * whole card; it is hovered and focused only then. `loading` draws Figma's placeholders, announced
 * busy. For one with a menu, selectable in a list, use an Insight Card. Drawn from Figma's layer
 * tree (`internal/layers.tsx`). The app must load `@bwp-web/styles/tokens.css`.
 */

import Box, { type BoxProps } from '@mui/material/Box';
import ButtonBase from '@mui/material/ButtonBase';
import { forwardRef, type MouseEvent, type ReactNode } from 'react';
import {
  solarInsightCardSmallCompose,
  solarInsightCardSmallStyle,
  type SolarInsightCardSmallProps,
} from '@bwp-web/styles/mui';
import { drawChildren, type DrawnLayer } from './internal/layers.js';
import {
  StatusIndicator,
  type StatusIndicatorProps,
} from './StatusIndicator.js';

/** Each layer's children, as Figma nests them. */
const TREE: Record<string, string[]> = {
  root: ['icon', 'content'],
  icon: ['statusIndicator'],
  content: ['title', 'description', 'skeleton', 'skeleton2'],
};

export interface InsightCardSmallProps
  extends
    SolarInsightCardSmallProps,
    // MUI types BoxProps' ref for any element; the component's own, a <div>, comes from forwardRef.
    Omit<
      BoxProps,
      | keyof SolarInsightCardSmallProps
      | 'title'
      | 'description'
      | 'severityLabel'
      | 'onClick'
      | 'ref'
    > {
  /** What the insight is, in a few words; its action’s name where it is pressable. */
  title: ReactNode;
  /** What it means. */
  description?: ReactNode;
  /** The severity’s name, which the StatusIndicator announces; its word by default. */
  severityLabel?: string;
  /** Makes it pressable: its title is a button that calls it. */
  onClick?: (event: MouseEvent<HTMLElement>) => void;
  /** Makes it pressable: its title is a link here. */
  href?: string;
}

export const InsightCardSmall = forwardRef<
  HTMLDivElement,
  InsightCardSmallProps
>(function InsightCardSmall(
  {
    severity = 'success',
    loading = false,
    title,
    description,
    severityLabel,
    onClick,
    href,
    className,
    sx,
    ...rest
  },
  ref,
) {
  // Figma draws a loading one, with no status: its look is the same for all.
  const look = { severity: loading ? ('info' as const) : severity, loading };
  const composed = solarInsightCardSmallCompose(look);
  // Pressable while it loads too, as Figma draws a loading card hovered: its title, the action,
  // is then its name alone.
  const pressable = onClick != null || href != null;
  const dot = composed.statusIndicator;
  // The severity is read as well as seen: its word, where the caller names it no other way.
  const named =
    severityLabel ?? severity.charAt(0).toUpperCase() + severity.slice(1);
  // A slot left empty is not drawn.
  const parts = {
    ...composed,
    description: {
      ...composed.description,
      present: composed.description?.present !== false && description != null,
    },
  };
  // Pressable, its title is its action, stretched over the card.
  const titled = pressable ? (
    <ButtonBase
      className="SolarInsightCardSmall-press"
      disableRipple
      {...(href != null ? { href } : {})}
      onClick={onClick}
    >
      {title}
    </ButtonBase>
  ) : (
    title
  );
  // Loading, where its title is not drawn (Card's; a Device Card's is), the action is drawn in
  // its place, its name alone.
  const waiting =
    pressable &&
    loading &&
    !['title'].some((l) => composed[l]?.present !== false) ? (
      <ButtonBase
        className="SolarInsightCardSmall-press"
        disableRipple
        {...(href != null ? { href } : {})}
        onClick={onClick}
      >
        <span className="SolarInsightCardSmall-name">{title}</span>
      </ButtonBase>
    ) : null;
  return (
    <>
      <Box
        ref={ref}
        aria-busy={loading || undefined}
        className={
          [pressable ? 'SolarInsightCardSmall-pressable' : null, className]
            .filter(Boolean)
            .join(' ') || undefined
        }
        {...rest}
        sx={[
          solarInsightCardSmallStyle(look),
          ...(Array.isArray(sx) ? sx : [sx]),
        ]}
      >
        {waiting}
        {drawChildren('root', {
          prefix: 'SolarInsightCardSmall',
          tree: TREE,
          parts,
          text: { title: titled, description },
          render: {
            // The mark is a StatusIndicator, in the type the recipe names, in its layer's element, named by
            // the severity's word.
            statusIndicator: ({ className: c, style }: DrawnLayer) => (
              <span className={c} style={style}>
                <StatusIndicator
                  type={dot['variant.type'] as StatusIndicatorProps['type']}
                  size={dot['variant.size'] as StatusIndicatorProps['size']}
                  label={named}
                />
              </span>
            ),
          },
        })}
      </Box>
    </>
  );
});
