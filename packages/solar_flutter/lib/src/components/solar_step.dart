// Generated by @bwp-web/codegen from packages/codegen/src/components/step.mjs. Do not edit.
/// SOLAR Step.
///
/// Generated from its template in `packages/codegen/src/components/` on every `npm run
/// solar:codegen`: change the template there, never this file. What it looks like is not here. That
/// is the recipe, [SolarStepRecipe]: its label’s text style and ink, and its line, by status and
/// type, read cell by cell.
///
/// Bespoke: one step of a SolarStepper, drawn from Figma's layer tree with [SolarLayers]: its
/// SolarStepperIndicator in the variant Figma draws for its [status] and its [label] under it
/// (round), or its numbered label over a line (horizontal). Pressable where it has [onPressed] (a
/// completed step the flow lets one go back to). A SolarStepper gives it its status.
library;

import 'package:flutter/material.dart';

import '../generated/components/step.dart';
import '../solar_layers.dart';
import '../solar_states.dart';
import '../generated/components/stepper_indicator.dart';
import 'solar_stepper_indicator.dart';
import 'solar_theme_of.dart';

class SolarStep extends StatelessWidget {
  const SolarStep({
    super.key,
    this.status = SolarStepStatus.complete,
    this.type = SolarStepType.round,
    required this.label,
    required this.number,
    this.onPressed,
    this.statesController,
  });

  final SolarStepStatus status;
  final SolarStepType type;

  /// The step's name.
  final String label;

  /// The step's number, from 1.
  final int number;

  /// Called when it is tapped, which makes it a control of its own; without it, it takes the
  /// states of the control around it (a Button's).
  final VoidCallback? onPressed;

  /// Its states, where the caller keeps them.
  final WidgetStatesController? statesController;

  /// Each layer's children, as Figma nests them.
  static const _tree = <String, List<String>>{
    'root': ['stepperIndicator', 'step', 'label', 'line'],
  };

  @override
  Widget build(BuildContext context) {
    final t = solarThemeOf(context);
    final p = SolarStepProps(status: status, type: type);
    Widget draw(Set<WidgetState> states) => SolarLayers(
      recipe: SolarLayerRecipe(
        lookup: (c) => SolarStepRecipe.lookup(c, p, states),
        dimension: (c) => SolarStepRecipe.dimension(c, p, states),
        color: (c) => SolarStepRecipe.color(t, c, p, states),
        shadow: (c) => SolarStepRecipe.shadow(t, c, p, states),
        textStyle: (c) => SolarStepRecipe.textStyle(t, c, p, states),
        present: (l) => SolarStepRecipe.present(l, p, states),
        glyph: (_) => null,
      ),
      tree: _tree,
      keyPrefix: 'step',
      text: {'step': label, 'label': '$number. $label'},
      composed: {
        'stepperIndicator': SolarStepperIndicator(
          number: number,
          status: SolarStepperIndicatorStatus.values.byName(
            SolarStepRecipe.lookup(
              'stepperIndicator.variant.status',
              p,
              states,
            )!.substring(2),
          ),
        ),
      },
    ).layer('root');
    // A control of its own only when it has something to do; otherwise it takes the states of
    // the control around it (a Counter in a Button).
    final mark = onPressed == null && statesController == null
        ? SolarStatesBuilder(builder: (_, states) => draw(states))
        : SolarPressable(
            onPressed: onPressed,
            statesController: statesController,
            target: true,
            builder: (_, states) => draw(states),
          );
    return mark;
  }
}
