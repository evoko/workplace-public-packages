// Generated by @bwp-web/codegen from packages/codegen/src/components/insight-card.mjs. Do not edit.
/// SOLAR Insight Card.
///
/// Generated from its template in `packages/codegen/src/components/` on every `npm run
/// solar:codegen`: change the template there, never this file. What it looks like is not here. That
/// is the recipe, [SolarInsightCardRecipe]: the surface’s fill, edge, shadow and focus ring by
/// state, the severity’s tile and More ink, its words, the StatusIndicator it shows, and the
/// placeholders, read cell by cell.
///
/// One insight in a list of them: the StatusIndicator of its [severity] in a tile, named by
/// [severityLabel] (its severity's word by default), its [title] and [description], and a More menu
/// of [moreItems]. The [selected] one is the current one of its set (the insight shown beside the
/// list). Given [onPressed], it is pressable: its title is the button or link, and its hit area the
/// whole card, the More menu reachable above it; it is hovered and focused only then. [loading]
/// draws Figma's placeholders, announced busy. For a smaller one with no menu use an Insight Card
/// Small. Drawn from Figma's layer tree with [SolarLayers]; pressable, the whole card is its
/// button, named by its title, and its own controls are controls of their own inside it.
library;

import 'package:flutter/material.dart';

import '../generated/components/insight_card.dart';
import '../generated/icons.dart';
import '../solar_layers.dart';
import '../solar_menu.dart';
import '../solar_states.dart';
import '../solar_target.dart';
import 'solar_card.dart';
import 'solar_dropdown_item.dart';
import 'solar_dropdown_menu.dart';
import '../generated/components/statusindicator.dart';
import 'solar_statusindicator.dart';
import 'solar_theme_of.dart';

class SolarInsightCard extends StatelessWidget {
  const SolarInsightCard({
    super.key,
    required this.title,
    this.severity = SolarInsightCardSeverity.success,
    this.selected = false,
    this.loading = false,
    this.description,
    this.severityLabel,
    this.moreItems,
    this.moreLabel = 'More actions',
    this.onPressed,
    this.statesController,
  });

  final SolarInsightCardSeverity severity;

  final bool selected;

  final bool loading;

  /// What the insight is, in a few words; its action’s name where it is pressable.
  final String title;

  /// What it means.
  final String? description;

  /// The severity’s name, which the StatusIndicator announces; its word by default.
  final String? severityLabel;

  /// The More menu's actions: a button opens them.
  final List<SolarCardMoreItem>? moreItems;

  /// The More button's accessible name.
  final String moreLabel;

  /// Makes it pressable: the whole card is a button that calls it.
  final VoidCallback? onPressed;

  /// Its states, where the caller keeps them.
  final WidgetStatesController? statesController;

  /// Each layer's children, as Figma nests them.
  static const _tree = <String, List<String>>{
    'root': ['icon', 'content', 'iconMore'],
    'icon': ['statusIndicator'],
    'content': ['title', 'description', 'skeleton', 'skeleton2'],
  };

  @override
  Widget build(BuildContext context) {
    final rows = moreItems;
    if (rows == null || rows.isEmpty) return _card(context, null);
    // Its own menu, under the More button.
    return SolarMenuAnchor(
      menu: SolarDropdownMenu(
        children: [
          for (final item in rows)
            Builder(
              builder: (context) => SolarDropdownItem(
                label: item.label,
                icon: item.icon,
                disabled: item.disabled,
                onPressed: () {
                  MenuController.maybeOf(context)?.close();
                  item.onSelected();
                },
              ),
            ),
        ],
      ),
      builder: (context, controller) => _card(
        context,
        () => controller.isOpen ? controller.close() : controller.open(),
      ),
    );
  }

  Widget _card(BuildContext context, VoidCallback? onMore) {
    final t = solarThemeOf(context);
    // Figma draws a loading one, with no status: its look is the same for all.
    final p = SolarInsightCardProps(
      severity: loading ? SolarInsightCardSeverity.info : severity,
      selected: selected,
      loading: loading,
    );
    // Pressable while it loads too, as Figma draws a loading card hovered.
    final pressable = onPressed != null;
    Widget draw(Set<WidgetState> states) {
      final recipe = SolarLayerRecipe(
        lookup: (c) => SolarInsightCardRecipe.lookup(c, p, states),
        dimension: (c) => SolarInsightCardRecipe.dimension(c, p, states),
        color: (c) => SolarInsightCardRecipe.color(t, c, p, states),
        shadow: (c) => SolarInsightCardRecipe.shadow(t, c, p, states),
        textStyle: (c) => SolarInsightCardRecipe.textStyle(t, c, p, states),
        // A slot left empty is not drawn.
        present: (l) => switch (l) {
          'description' =>
            description != null && SolarInsightCardRecipe.present(l, p, states),
          'iconMore' =>
            onMore != null && SolarInsightCardRecipe.present(l, p, states),
          _ => SolarInsightCardRecipe.present(l, p, states),
        },
        glyph: (_) => null,
      );
      SolarLayers layers(Map<String, List<Widget>> content) => SolarLayers(
        recipe: recipe,
        tree: _tree,
        keyPrefix: 'insightCard',
        text: {'title': title, 'description': ?description},
        icons: const {'iconMore': SolarIcons.moreOutline},
        content: content,
        builders: {
          'iconMore': (glyph) => SolarTarget.inside(
            child: Semantics(
              // A node of its own, so the control keeps its name inside the card's.
              container: true,
              child: SolarPressable(
                onPressed: onMore,
                builder: (_, _) => Semantics(
                  label: moreLabel,
                  excludeSemantics: true,
                  child: glyph,
                ),
              ),
            ),
          ),
        },
        composed: {
          // The mark is a StatusIndicator, in the type the recipe names, named by the severity's word.
          'statusIndicator': SolarStatusIndicator(
            type: SolarStatusIndicatorType.values.byName(
              SolarInsightCardRecipe.lookup(
                'statusIndicator.variant.type',
                p,
                states,
              )!.substring(2),
            ),
            size: SolarStatusIndicatorSize.values.byName(
              SolarInsightCardRecipe.lookup(
                'statusIndicator.variant.size',
                p,
                states,
              )!.substring(2),
            ),
            label:
                severityLabel ??
                '${severity.name[0].toUpperCase()}${severity.name.substring(1)}',
          ),
        },
      );
      return layers(const {}).layer('root');
    }

    final Widget mark = pressable
        ? SolarPressable(
            onPressed: onPressed,
            statesController: statesController,
            selected: selected,
            builder: (_, states) => Semantics(
              // Loading, where its title is not drawn (Card's; a Device Card's is), the button is
              // named by it all the same.
              label:
                  loading &&
                      ![
                        'title',
                      ].any((l) => SolarInsightCardRecipe.present(l, p, states))
                  ? title
                  : null,
              child: draw({...states, if (selected) WidgetState.selected}),
            ),
          )
        : draw({if (selected) WidgetState.selected});
    return Semantics(
      container: true,
      // Loading, it says so, as the web's aria-busy does.
      label: loading ? 'Loading' : null,
      child: mark,
    );
  }
}
