// Generated by @bwp-web/codegen from packages/codegen/src/components/status-card.mjs. Do not edit.
/// SOLAR Status Card.
///
/// Generated from its template in `packages/codegen/src/components/` on every `npm run
/// solar:codegen`: change the template there, never this file. What it looks like is not here. That
/// is the recipe, [SolarStatusCardRecipe]: the surface’s edge, shadow and focus ring by state, its
/// words’ ink, the StatusIndicator it shows, and the placeholders, read cell by cell.
///
/// A figure and its status at a glance, on a dashboard: its [title], a More menu of [moreItems],
/// and the [value] beside the StatusIndicator of its [status], named by [statusLabel] (its status's
/// word by default), so the status is read, not only seen. Given [onPressed], it is pressable: its
/// title is the button or link, and its hit area the whole card, the More menu reachable above it;
/// it is hovered and focused only then. [loading] draws Figma's placeholders, announced busy. Drawn
/// from Figma's layer tree with [SolarLayers]; pressable, the whole card is its button, named by
/// its title, and its own controls are controls of their own inside it.
library;

import 'package:flutter/material.dart';

import '../generated/components/status_card.dart';
import '../generated/icons.dart';
import '../solar_layers.dart';
import '../solar_menu.dart';
import '../solar_states.dart';
import '../solar_target.dart';
import 'solar_card.dart';
import 'solar_dropdown_item.dart';
import 'solar_dropdown_menu.dart';
import '../generated/components/statusindicator.dart';
import 'solar_statusindicator.dart';
import 'solar_theme_of.dart';

class SolarStatusCard extends StatelessWidget {
  const SolarStatusCard({
    super.key,
    required this.title,
    required this.value,
    this.status = SolarStatusCardStatus.success,
    this.disabled = false,
    this.loading = false,
    this.statusLabel,
    this.moreItems,
    this.moreLabel = 'More actions',
    this.onPressed,
    this.statesController,
  });

  final SolarStatusCardStatus status;

  final bool disabled;

  final bool loading;

  /// What the card counts, in a few words; its action’s name where it is pressable.
  final String title;

  /// The figure: a count, a share.
  final String value;

  /// The status’s name, which the StatusIndicator announces; its word by default.
  final String? statusLabel;

  /// The More menu's actions: a button opens them.
  final List<SolarCardMoreItem>? moreItems;

  /// The More button's accessible name.
  final String moreLabel;

  /// Makes it pressable: the whole card is a button that calls it.
  final VoidCallback? onPressed;

  /// Its states, where the caller keeps them.
  final WidgetStatesController? statesController;

  /// Each layer's children, as Figma nests them.
  static const _tree = <String, List<String>>{
    'root': ['title', 'trend', 'skeleton', 'skeleton2'],
    'title': ['titleTitle', 'iconMore'],
    'trend': ['statusIndicator', 'value'],
  };

  @override
  Widget build(BuildContext context) {
    final rows = moreItems;
    if (rows == null || rows.isEmpty) return _card(context, null);
    // Its own menu, under the More button.
    return SolarMenuAnchor(
      menu: SolarDropdownMenu(
        children: [
          for (final item in rows)
            Builder(
              builder: (context) => SolarDropdownItem(
                label: item.label,
                icon: item.icon,
                disabled: item.disabled,
                onPressed: () {
                  MenuController.maybeOf(context)?.close();
                  item.onSelected();
                },
              ),
            ),
        ],
      ),
      builder: (context, controller) => _card(
        context,
        () => controller.isOpen ? controller.close() : controller.open(),
      ),
    );
  }

  Widget _card(BuildContext context, VoidCallback? onMore) {
    final t = solarThemeOf(context);
    final p = SolarStatusCardProps(
      status: status,
      disabled: disabled,
      loading: loading,
    );
    // Pressable while it loads too, as Figma draws a loading card hovered.
    final pressable = onPressed != null && !disabled;
    Widget draw(Set<WidgetState> states) {
      final recipe = SolarLayerRecipe(
        lookup: (c) => SolarStatusCardRecipe.lookup(c, p, states),
        dimension: (c) => SolarStatusCardRecipe.dimension(c, p, states),
        color: (c) => SolarStatusCardRecipe.color(t, c, p, states),
        shadow: (c) => SolarStatusCardRecipe.shadow(t, c, p, states),
        textStyle: (c) => SolarStatusCardRecipe.textStyle(t, c, p, states),
        // A slot left empty is not drawn.
        present: (l) => switch (l) {
          'iconMore' =>
            onMore != null && SolarStatusCardRecipe.present(l, p, states),
          _ => SolarStatusCardRecipe.present(l, p, states),
        },
        glyph: (_) => null,
      );
      SolarLayers layers(Map<String, List<Widget>> content) => SolarLayers(
        recipe: recipe,
        tree: _tree,
        keyPrefix: 'statusCard',
        text: {'titleTitle': title, 'value': value},
        icons: const {'iconMore': SolarIcons.moreOutline},
        content: content,
        builders: {
          'iconMore': (glyph) => SolarTarget.inside(
            child: Semantics(
              // A node of its own, so the control keeps its name inside the card's.
              container: true,
              child: SolarPressable(
                onPressed: disabled ? null : onMore,
                builder: (_, _) => Semantics(
                  label: moreLabel,
                  excludeSemantics: true,
                  child: glyph,
                ),
              ),
            ),
          ),
        },
        composed: {
          // The mark is a StatusIndicator, in the type the recipe names, named by the status's word.
          'statusIndicator': SolarStatusIndicator(
            type: SolarStatusIndicatorType.values.byName(
              SolarStatusCardRecipe.lookup(
                'statusIndicator.variant.type',
                p,
                states,
              )!.substring(2),
            ),
            size: SolarStatusIndicatorSize.values.byName(
              SolarStatusCardRecipe.lookup(
                'statusIndicator.variant.size',
                p,
                states,
              )!.substring(2),
            ),
            label:
                statusLabel ??
                '${status.name[0].toUpperCase()}${status.name.substring(1)}',
          ),
        },
      );
      return layers(const {}).layer('root');
    }

    final Widget mark = pressable
        ? SolarPressable(
            onPressed: onPressed,
            statesController: statesController,
            builder: (_, states) => Semantics(
              // Loading, where its title is not drawn (Card's; a Device Card's is), the button is
              // named by it all the same.
              label:
                  loading &&
                      ![
                        'titleTitle',
                      ].any((l) => SolarStatusCardRecipe.present(l, p, states))
                  ? title
                  : null,
              child: draw(states),
            ),
          )
        : draw({if (disabled) WidgetState.disabled});
    return Semantics(
      container: true,
      // Loading, it says so, as the web's aria-busy does.
      label: loading ? 'Loading' : null,
      // Disabled, it says so, as the web's aria-disabled does.
      enabled: disabled ? false : null,
      child: mark,
    );
  }
}
