// Generated by @bwp-web/codegen from packages/codegen/src/components/paginationitem.mjs. Do not edit.
/// SOLAR PaginationItem.
///
/// Generated from its template in `packages/codegen/src/components/` on every `npm run
/// solar:codegen`: change the template there, never this file. What it looks like is not here. That
/// is the recipe, [SolarPaginationItemRecipe]: the page’s fill and ring by state, and its number’s
/// ink, read cell by cell.
///
/// Bespoke: one page of a SolarPagination, drawn from Figma's layer tree with [SolarLayers],
/// pressable and focusable, named "Page 3" for a screen reader; the [selected] one is the current
/// page, announced selected. Its own 24 × 24 box is its target: the pages sit 4px apart. Use it
/// inside a SolarPagination.
library;

import 'package:flutter/material.dart';

import '../generated/components/paginationitem.dart';
import '../solar_layers.dart';
import '../solar_states.dart';
import 'solar_theme_of.dart';

class SolarPaginationItem extends StatelessWidget {
  const SolarPaginationItem({
    super.key,
    this.selected = false,
    required this.page,
    required this.onPressed,
    this.statesController,
  });

  final bool selected;

  /// The page's number.
  final int page;

  /// Called when it is chosen; null disables it.
  final VoidCallback? onPressed;

  /// Whether it is disabled: by a null [onPressed], as Flutter's own controls are, not a
  /// parameter of its own.
  bool get disabled => onPressed == null;

  /// Its states, where the caller keeps them.
  final WidgetStatesController? statesController;

  /// Each layer's children, as Figma nests them.
  static const _tree = <String, List<String>>{
    'root': ['page'],
  };

  @override
  Widget build(BuildContext context) {
    final t = solarThemeOf(context);
    final p = SolarPaginationItemProps(selected: selected, disabled: disabled);
    Widget draw(Set<WidgetState> states) => SolarLayers(
      recipe: SolarLayerRecipe(
        lookup: (c) => SolarPaginationItemRecipe.lookup(c, p, states),
        dimension: (c) => SolarPaginationItemRecipe.dimension(c, p, states),
        color: (c) => SolarPaginationItemRecipe.color(t, c, p, states),
        shadow: (c) => SolarPaginationItemRecipe.shadow(t, c, p, states),
        textStyle: (c) => SolarPaginationItemRecipe.textStyle(t, c, p, states),
        present: (l) => SolarPaginationItemRecipe.present(l, p, states),
        glyph: (_) => null,
      ),
      tree: _tree,
      keyPrefix: 'paginationItem',
      text: {'page': '$page'},
      builders: {'page': (layer) => ExcludeSemantics(child: layer)},
    ).layer('root');
    final mark = SolarPressable(
      onPressed: disabled ? null : onPressed,
      statesController: statesController,
      selected: selected,
      target: false,
      builder: (_, states) => draw(states),
    );
    return Semantics(label: 'Page $page', child: mark);
  }
}
