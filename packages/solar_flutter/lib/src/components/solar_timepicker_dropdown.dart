// Generated by @bwp-web/codegen from packages/codegen/src/components/timepicker-dropdown.mjs. Do not edit.
/// SOLAR TimePicker Dropdown.
///
/// Generated from its template in `packages/codegen/src/components/` on every `npm run
/// solar:codegen`: change the template there, never this file. What it looks like is not here. That
/// is the recipe, [SolarTimePickerDropdownRecipe]: the surface’s fill, edge, corners and shadow,
/// read cell by cell.
///
/// Bespoke: the times a SolarTimePicker picks from, one list, drawn from Figma's layer tree with
/// [SolarLayers] around a [SolarMenuList]: every [step] minutes (30 by default) from [first] to
/// [last] (the whole day), each a SolarDropdownItem, as MaterialLocalizations writes it on the
/// platform's clock, the [value] selected and in sight as it is first built. The arrow keys move
/// from time to time, and a tap or Enter calls [onChanged] with the time. It draws where it is put;
/// to float it under a field, give it to a [SolarMenuAnchor]. Past 300 its rows scroll, as a
/// Dropdown Menu's.
library;

import 'package:flutter/material.dart';

import '../generated/components/timepicker_dropdown.dart';
import '../solar_layers.dart';
import '../solar_menu.dart';
import '../solar_time.dart';
import 'solar_dropdown_item.dart';
import 'solar_theme_of.dart';

class SolarTimePickerDropdown extends StatelessWidget {
  const SolarTimePickerDropdown({
    super.key,
    this.size = SolarTimePickerDropdownSize.md,
    this.value,
    this.onChanged,
    this.step = 30,
    this.first,
    this.last,
    this.optionBuilder,
    this.maxHeight = solarMenuMaxHeight,
  });

  final SolarTimePickerDropdownSize size;

  /// The time chosen; null for none.
  final TimeOfDay? value;

  /// Called with the time chosen.
  final ValueChanged<TimeOfDay>? onChanged;

  /// The minutes between one time and the next.
  final int step;

  /// The earliest and latest times.
  final TimeOfDay? first, last;

  /// What each time's row is drawn as, given its time and the row the list builds for it (a test's
  /// key); the row by default.
  final Widget Function(TimeOfDay time, SolarDropdownItem row)? optionBuilder;

  /// The tallest it grows before its rows scroll.
  final double maxHeight;

  /// Each layer's children, as Figma nests them.
  static const _tree = <String, List<String>>{
    'root': ['content'],
    'content': [
      'dropdownItem',
      'dropdownItem2',
      'dropdownItem3',
      'dropdownItem4',
      'dropdownItem5',
      'dropdownItem6',
    ],
  };

  @override
  Widget build(BuildContext context) {
    final l = MaterialLocalizations.of(context);
    final clock = MediaQuery.alwaysUse24HourFormatOf(context);
    final t = solarThemeOf(context);
    final p = SolarTimePickerDropdownProps(size: size);
    const states = <WidgetState>{};
    final mark = SolarLayers(
      recipe: SolarLayerRecipe(
        lookup: (c) => SolarTimePickerDropdownRecipe.lookup(c, p, states),
        dimension: (c) => SolarTimePickerDropdownRecipe.dimension(c, p, states),
        color: (c) => SolarTimePickerDropdownRecipe.color(t, c, p, states),
        shadow: (c) => SolarTimePickerDropdownRecipe.shadow(t, c, p, states),
        textStyle: (c) =>
            SolarTimePickerDropdownRecipe.textStyle(t, c, p, states),
        present: (l) => SolarTimePickerDropdownRecipe.present(l, p, states),
        glyph: (_) => null,
      ),
      tree: _tree,
      keyPrefix: 'timePickerDropdown',
      content: {
        'content': [
          SolarMenuList(
            size: size.name,
            maxHeight: maxHeight,
            children: [
              for (final time in solarTimesOf(step, first: first, last: last))
                _InSight(
                  shown: time == value,
                  child: Builder(
                    builder: (context) {
                      final row = SolarDropdownItem(
                        label: l.formatTimeOfDay(
                          time,
                          alwaysUse24HourFormat: clock,
                        ),
                        selected: time == value,
                        onPressed: () => onChanged?.call(time),
                      );
                      return optionBuilder?.call(time, row) ?? row;
                    },
                  ),
                ),
            ],
          ),
        ],
      },
    ).layer('root');
    return mark;
  }
}

/// A row brought into sight in the list as it is first built, where [shown] (the chosen time).
class _InSight extends StatefulWidget {
  const _InSight({required this.shown, required this.child});

  final bool shown;
  final Widget child;

  @override
  State<_InSight> createState() => _InSightState();
}

class _InSightState extends State<_InSight> {
  @override
  void initState() {
    super.initState();
    if (widget.shown) {
      WidgetsBinding.instance.addPostFrameCallback((_) {
        if (mounted) Scrollable.ensureVisible(context);
      });
    }
  }

  @override
  Widget build(BuildContext context) => widget.child;
}
