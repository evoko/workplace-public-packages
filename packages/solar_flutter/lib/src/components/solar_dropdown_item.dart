// Generated by @bwp-web/codegen from packages/codegen/src/components/dropdown-item.mjs. Do not edit.
/// SOLAR Dropdown Item.
///
/// Generated from its template in `packages/codegen/src/components/` on every `npm run
/// solar:codegen`: change the template there, never this file. What it looks like is not here. That
/// is the recipe, [SolarDropdownItemRecipe]: the row’s fill by state, and its words’ and icon’s
/// ink, read cell by cell.
///
/// Bespoke: one row of a SolarDropdownMenu, drawn from Figma's layer tree with [SolarLayers],
/// pressable, and focusable in the menu's order; a focused row draws Figma's hover, as SOLAR's
/// description says a highlighted one does. Its words, a second line ([helper]), an [icon] before
/// them and a [checkbox] that follows [selected]. Inside a menu it takes the menu's size, and is
/// announced as a menu item (a checkable one with a checkbox); outside one, as a button.
library;

import 'package:flutter/material.dart';

import '../generated/components/dropdown_item.dart';
import '../solar_layers.dart';
import '../solar_states.dart';

import 'package:flutter/semantics.dart';

import '../solar_menu.dart';
import 'solar_checkbox.dart';
import 'solar_theme_of.dart';

class SolarDropdownItem extends StatelessWidget {
  const SolarDropdownItem({
    super.key,
    this.size = SolarDropdownItemSize.md,
    this.selected = false,
    required this.label,
    required this.onPressed,
    this.helper,
    this.icon,
    this.checkbox = false,
    this.statesController,
  });

  final SolarDropdownItemSize size;
  final bool selected;

  /// The row's words.
  final String label;

  /// Called when it is chosen; null disables it.
  final VoidCallback? onPressed;

  /// A second line under the words, quieter.
  final String? helper;

  /// An icon before the words.
  final Widget? icon;

  /// Shows a checkbox before the words, checked where the row is selected: a choice of several.
  final bool checkbox;

  /// Whether it is disabled: by a null [onPressed], as Flutter's own controls are, not a
  /// parameter of its own.
  bool get disabled => onPressed == null;

  /// Its states, where the caller keeps them.
  final WidgetStatesController? statesController;

  /// Each layer's children, as Figma nests them.
  static const _tree = <String, List<String>>{
    'root': ['checkbox', 'icon', 'content'],
    'content': ['label', 'helper'],
  };

  @override
  Widget build(BuildContext context) {
    final t = solarThemeOf(context);
    final p = SolarDropdownItemProps(
      size:
          SolarMenuScope.sizeOf(context, SolarDropdownItemSize.values) ?? size,
      selected: selected,
      disabled: disabled || onPressed == null,
    );
    Widget draw(Set<WidgetState> states) => SolarLayers(
      recipe: SolarLayerRecipe(
        lookup: (c) => SolarDropdownItemRecipe.lookup(c, p, states),
        dimension: (c) => SolarDropdownItemRecipe.dimension(c, p, states),
        color: (c) => SolarDropdownItemRecipe.color(t, c, p, states),
        shadow: (c) => SolarDropdownItemRecipe.shadow(t, c, p, states),
        textStyle: (c) => SolarDropdownItemRecipe.textStyle(t, c, p, states),
        present: (l) => switch (l) {
          'checkbox' => checkbox,
          'icon' => icon != null,
          'helper' => helper != null,
          _ => SolarDropdownItemRecipe.present(l, p, states),
        },
        glyph: (_) => null,
      ),
      tree: _tree,
      keyPrefix: 'dropdownItem',
      text: {'label': label, 'helper': ?helper},
      slots: {'icon': ?icon},
      composed: {
        'checkbox': SolarCheckbox(
          checked:
              SolarDropdownItemRecipe.lookup(
                'checkbox.variant.checked',
                p,
                states,
              ) ==
              'k:true',
          onChanged: null,
          inStates: {
            if (SolarDropdownItemRecipe.lookup(
                  'checkbox.variant.hover',
                  p,
                  states,
                ) ==
                'k:true')
              WidgetState.hovered,
            if (SolarDropdownItemRecipe.lookup(
                  'checkbox.variant.disabled',
                  p,
                  states,
                ) ==
                'k:true')
              WidgetState.disabled,
          },
        ),
      },
    ).layer('root');
    final mark = SolarPressable(
      onPressed: disabled ? null : onPressed,
      statesController: statesController,
      role: SolarMenuScope.of(context)
          ? (checkbox ? SemanticsRole.menuItemCheckbox : SemanticsRole.menuItem)
          : null,
      checked: checkbox ? selected : null,
      selected: checkbox ? null : selected,
      target: true,
      builder: (_, states) => draw(states),
    );
    return mark;
  }
}
