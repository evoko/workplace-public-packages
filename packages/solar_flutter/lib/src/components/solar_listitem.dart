// Generated by @bwp-web/codegen from packages/codegen/src/components/listitem.mjs. Do not edit.
/// SOLAR ListItem.
///
/// Generated from its template in `packages/codegen/src/components/` on every `npm run
/// solar:codegen`: change the template there, never this file. What it looks like is not here. That
/// is the recipe, [SolarListItemRecipe]: the row’s fill and focus ring by state, its padding,
/// compact or not, and its words’ and icons’ ink, read cell by cell.
///
/// Bespoke: one row of a SolarList, which a user chooses, drawn from Figma's layer tree with
/// [SolarLayers], pressable, and announced as a button, selected where it is. Its words, a second
/// line ([helper]), an [icon] or an [avatar] (a SolarAvatar, which makes it an avatar row) before
/// them and a [trailing] icon (a chevron) after. One trailing element, never two, SOLAR says. In a
/// SolarList it takes the list's compactness.
library;

import 'package:flutter/material.dart';

import '../generated/components/listitem.dart';
import '../solar_layers.dart';
import '../solar_states.dart';
import '../solar_list.dart';
import 'solar_theme_of.dart';

class SolarListItem extends StatelessWidget {
  const SolarListItem({
    super.key,
    this.selected = false,
    this.disabled = false,
    this.compact = false,
    required this.label,
    required this.onPressed,
    this.helper,
    this.icon,
    this.avatar,
    this.trailing,
    this.statesController,
  });

  final bool selected;
  final bool disabled;
  final bool compact;

  /// The row's words.
  final String label;

  /// Called when it is chosen; null disables it.
  final VoidCallback? onPressed;

  /// A second line under the words, quieter.
  final String? helper;

  /// An icon before the words.
  final Widget? icon;

  /// A SolarAvatar before the words, in place of an icon: an avatar row.
  final Widget? avatar;

  /// An icon after the words: a chevron, where the row opens something.
  final Widget? trailing;

  /// Its states, where the caller keeps them.
  final WidgetStatesController? statesController;

  /// Each layer's children, as Figma nests them.
  static const _tree = <String, List<String>>{
    'root': ['icon', 'body', 'trailing', 'avatar'],
    'body': ['label', 'helper'],
  };

  @override
  Widget build(BuildContext context) {
    final t = solarThemeOf(context);
    final p = SolarListItemProps(
      selected: selected,
      disabled: disabled || onPressed == null,
      compact: SolarListScope.compactOf(context) ?? compact,
      type: avatar != null ? SolarListItemType.avatar : SolarListItemType.icon,
    );
    Widget draw(Set<WidgetState> states) => SolarLayers(
      recipe: SolarLayerRecipe(
        lookup: (c) => SolarListItemRecipe.lookup(c, p, states),
        dimension: (c) => SolarListItemRecipe.dimension(c, p, states),
        color: (c) => SolarListItemRecipe.color(t, c, p, states),
        shadow: (c) => SolarListItemRecipe.shadow(t, c, p, states),
        textStyle: (c) => SolarListItemRecipe.textStyle(t, c, p, states),
        present: (l) => switch (l) {
          'icon' => avatar == null && icon != null,
          'avatar' => avatar != null,
          'helper' => helper != null,
          'trailing' => trailing != null,
          _ => SolarListItemRecipe.present(l, p, states),
        },
        glyph: (_) => null,
      ),
      tree: _tree,
      keyPrefix: 'listItem',
      text: {'label': label, 'helper': ?helper},
      slots: {'icon': ?icon, 'trailing': ?trailing},
      composed: {'avatar': ?avatar},
    ).layer('root');
    final mark = SolarPressable(
      onPressed: disabled ? null : onPressed,
      statesController: statesController,
      selected: selected,
      target: true,
      builder: (_, states) => draw(states),
    );
    return mark;
  }
}
