// Generated by @bwp-web/codegen from packages/codegen/src/components/date-picker.mjs. Do not edit.
/// SOLAR DatePicker.
///
/// Generated from its template in `packages/codegen/src/components/` on every `npm run
/// solar:codegen`: change the template there, never this file. What it looks like is not here. That
/// is the recipe, [SolarDatePickerRecipe]: the field's fill, edge and focus ring by state, its
/// words' and icon's ink, and the label and helper, read cell by cell.
///
/// A date, typed or picked: its [label] above (a [mandatory] one is starred), its [helper] below,
/// which says what is wrong where it is in [error]. Its words are a [TextField], undecorated,
/// taking the date in the locale's figures (MaterialLocalizations' compact date, `04/24/2026` in
/// the US), read on submit and as the focus leaves; its calendar icon, or the down arrow, opens a
/// SolarDatePickerOpen under the field, the focus on the chosen day, where a tap or Enter picks one
/// and Escape closes it. [value] and [onDateChanged] hold the date, its time ignored, or null for
/// none; words that are no date leave it as it was, for the caller to flag with [error].
/// [firstDate], [lastDate] and [selectableDayPredicate] refuse dates in the calendar. It reads as
/// one text field, named by its label and described by its helper.
library;

import 'package:flutter/material.dart';

import '../generated/components/datepicker.dart';
import '../generated/icons.dart';
import '../solar_field.dart';
import '../solar_layers.dart';

import 'package:flutter/services.dart';

import '../solar_menu.dart';
import '../solar_states.dart';
import '../solar_target.dart';
import 'solar_date_picker_open.dart';
import 'solar_theme_of.dart';

class SolarDatePicker extends StatelessWidget {
  const SolarDatePicker({
    super.key,
    this.size = SolarDatePickerSize.md,
    this.disabled = false,
    this.error = false,
    this.label,
    this.mandatory = false,
    this.helper,
    this.value,
    this.onDateChanged,
    this.firstDate,
    this.lastDate,
    this.selectableDayPredicate,
    this.weekStartsOn,
    this.controller,
    this.placeholder,
    this.onChanged,
    this.autofocus = false,
    this.focusNode,
    this.statesController,
  });

  final SolarDatePickerSize size;
  final bool disabled;
  final bool error;

  /// What it asks for, above it.
  final String? label;

  /// Whether it must be filled, which stars the label.
  final bool mandatory;

  /// More about it, below; where it is in [error], what is wrong.
  final String? helper;

  /// The date; null for none. Its time is ignored.
  final DateTime? value;

  /// Called with the date typed or picked, at midnight, or null where the words are cleared.
  final ValueChanged<DateTime?>? onDateChanged;

  /// The earliest and latest dates the calendar takes.
  final DateTime? firstDate, lastDate;

  /// Whether the calendar lets a date be chosen (a weekend, a booked day).
  final bool Function(DateTime date)? selectableDayPredicate;

  /// The week's first day, 0 for Sunday to 6 for Saturday; the locale's by default.
  final int? weekStartsOn;

  /// Its words, where the caller keeps them; one of its own, empty, otherwise.
  final TextEditingController? controller;

  /// What it shows while empty, in its words' place.
  final String? placeholder;

  /// Called with the words as they change.
  final ValueChanged<String>? onChanged;

  /// Whether it takes the focus when first built.
  final bool autofocus;

  /// Its focus, where the caller keeps it.
  final FocusNode? focusNode;

  /// States to draw it in beside its own, where the caller keeps them (the visual checks force a
  /// state through it).
  final WidgetStatesController? statesController;

  /// Each layer's children, as Figma nests them.
  static const _tree = <String, List<String>>{
    'root': ['label', 'field', 'helper'],
    'label': ['labelLabel', 'required'],
    'field': ['iconCalendar', 'value'],
  };

  @override
  Widget build(BuildContext context) {
    final t = solarThemeOf(context);
    final enabled = !disabled;
    Widget field([TextEditingController? text, FocusNode? focus]) => SolarField(
      controller: text ?? controller,
      focusNode: focus ?? focusNode,
      statesController: statesController,
      builder: (context, field) {
        final states = field.states;
        // Filled where it holds text: its words are then the value's, not the placeholder's.
        final p = SolarDatePickerProps(
          size: size,
          disabled: disabled,
          error: error,
          filled: field.text.text.isNotEmpty,
        );
        return SolarLayers(
          recipe: SolarLayerRecipe(
            lookup: (c) => SolarDatePickerRecipe.lookup(c, p, states),
            dimension: (c) => SolarDatePickerRecipe.dimension(c, p, states),
            color: (c) => SolarDatePickerRecipe.color(t, c, p, states),
            shadow: (c) => SolarDatePickerRecipe.shadow(t, c, p, states),
            textStyle: (c) => SolarDatePickerRecipe.textStyle(t, c, p, states),
            // A part left empty is not drawn.
            present: (l) => switch (l) {
              'label' => label != null,
              'required' => mandatory,
              'helper' => helper != null,
              _ => SolarDatePickerRecipe.present(l, p, states),
            },
            glyph: (_) => null,
          ),
          tree: _tree,
          keyPrefix: 'datePicker',
          text: {'labelLabel': ?label, 'required': '*', 'helper': ?helper},
          wraps: const {'helper': TextAlign.start},
          icons: const {'iconCalendar': SolarIcons.calendarOutline},
          // The field's words, in the recipe's style.
          fields: {
            'value': (style) => field.read(
              TextField(
                controller: field.text,
                focusNode: field.focus,
                enabled: enabled,
                autofocus: autofocus,
                onChanged: onChanged,
                style: style,
                maxLines: 1,
                decoration: InputDecoration.collapsed(
                  hintText: placeholder,
                  hintStyle: style,
                ),
              ),
              label: label,
              hint: helper,
            ),
          },
          builders: {
            'label': (layer) => ExcludeSemantics(child: layer),
            'helper': (layer) => ExcludeSemantics(child: layer),
            'iconCalendar': (layer) => Builder(
              builder: (context) {
                final anchor = context
                    .findAncestorStateOfType<_SolarDatePickerAnchorState>()!;
                final l = MaterialLocalizations.of(context);
                return SolarTarget.inside(
                  child: SolarPressable(
                    onPressed: enabled ? anchor.show : null,
                    builder: (_, _) =>
                        Semantics(label: l.datePickerHelpText, child: layer),
                  ),
                );
              },
            ),
            'field': (layer) => field.area(layer, enabled: enabled),
          },
        ).layer('root');
      },
    );
    return _SolarDatePickerAnchor(picker: this, field: field);
  }
}

/// The words and the panel: the words follow the value, as the locale writes it, and are read back
/// as one; the panel floats under the field while open.
class _SolarDatePickerAnchor extends StatefulWidget {
  const _SolarDatePickerAnchor({required this.picker, required this.field});

  final SolarDatePicker picker;
  final Widget Function([TextEditingController? text, FocusNode? focus]) field;

  @override
  State<_SolarDatePickerAnchor> createState() => _SolarDatePickerAnchorState();
}

class _SolarDatePickerAnchorState extends State<_SolarDatePickerAnchor> {
  final _menu = MenuController();
  TextEditingController? _ownText;
  FocusNode? _ownFocus;
  TextEditingController get _text =>
      widget.picker.controller ?? (_ownText ??= TextEditingController());
  FocusNode get _focus =>
      widget.picker.focusNode ?? (_ownFocus ??= FocusNode());
  bool _started = false;

  @override
  void initState() {
    super.initState();
    _focus.addListener(_left);
  }

  @override
  void didChangeDependencies() {
    super.didChangeDependencies();
    if (!_started) {
      _started = true;
      _show(widget.picker.value);
    }
  }

  @override
  void didUpdateWidget(_SolarDatePickerAnchor old) {
    super.didUpdateWidget(old);
    if (old.picker.focusNode != widget.picker.focusNode) {
      (old.picker.focusNode ?? _ownFocus)?.removeListener(_left);
      _focus.addListener(_left);
    }
    if (old.picker.value != widget.picker.value) _show(widget.picker.value);
  }

  @override
  void dispose() {
    _focus.removeListener(_left);
    _ownText?.dispose();
    _ownFocus?.dispose();
    super.dispose();
  }

  /// The words of [v], as the locale writes it.
  void _show(DateTime? v) {
    final l = MaterialLocalizations.of(context);
    final words = v == null ? '' : l.formatCompactDate(v);
    if (_text.text != words) _text.text = words;
  }

  /// The words read back: cleared, no value; a value, it; anything else leaves it as it was.
  void _commit() {
    final l = MaterialLocalizations.of(context);
    final typed = _text.text.trim();
    final value = widget.picker.value;
    if (typed.isEmpty) {
      if (value != null) widget.picker.onDateChanged?.call(null);
      return;
    }
    final next = l.parseCompactDate(typed);
    if (next == null) return;
    final a = next, b = value;
    if (b == null || !(DateUtils.isSameDay(a, b))) {
      widget.picker.onDateChanged?.call(next);
    } else {
      _show(value);
    }
  }

  void _left() {
    if (!_focus.hasFocus && !_menu.isOpen) _commit();
  }

  /// Opens the panel.
  void show() {
    if (!widget.picker.disabled) _menu.open();
  }

  /// Takes [value] from the panel, closes it, and gives the field its focus back.
  void choose(DateTime value) {
    widget.picker.onDateChanged?.call(value);
    _menu.close();
    _focus.requestFocus();
  }

  @override
  Widget build(BuildContext context) {
    final p = widget.picker;
    // Under the field, as far from it as the helper is.
    final gap =
        SolarDatePickerRecipe.dimension(
          'root.gap',
          SolarDatePickerProps(
            size: p.size,
            disabled: p.disabled,
            error: p.error,
          ),
          const {},
        ) ??
        0;
    return SolarMenuAnchor(
      controller: _menu,
      menu: Padding(
        padding: EdgeInsets.only(top: gap),
        child: SolarDatePickerOpen(
          value: p.value,
          firstDate: p.firstDate,
          lastDate: p.lastDate,
          selectableDayPredicate: p.selectableDayPredicate,
          weekStartsOn: p.weekStartsOn,
          autofocus: true,
          onChanged: choose,
        ),
      ),
      builder: (context, _) => CallbackShortcuts(
        bindings: {const SingleActivator(LogicalKeyboardKey.arrowDown): show},
        child: widget.field(_text, _focus),
      ),
    );
  }
}
