// Generated by @bwp-web/codegen from packages/codegen/src/components/file-card.mjs. Do not edit.
/// SOLAR File Card.
///
/// Generated from its template in `packages/codegen/src/components/` on every `npm run
/// solar:codegen`: change the template there, never this file. What it looks like is not here. That
/// is the recipe, [SolarFileCardRecipe]: the tile’s edge, shadow and focus ring, the thumbnail’s
/// fill, its words’ ink, and the create tile’s Plus, read cell by cell.
///
/// One file in a file browser's grid: its [thumbnail] (a preview, filling it) or its type's
/// [fileIcon], its [title] (its name) and [meta] (when it changed), and a More menu of [moreItems];
/// or, as [create], the tile that adds one, a Plus over its [title] (“New design”). Given
/// [onPressed], it is pressable: its name is the button or link, and its hit area the whole tile,
/// the More menu reachable above it; the tile is named by it, and focused only then. Drawn from
/// Figma's layer tree with [SolarLayers]; pressable, the whole card is its button, named by its
/// title, and its own controls are controls of their own inside it.
library;

import 'package:flutter/material.dart';

import '../generated/components/file_card.dart';
import '../generated/icons.dart';
import '../solar_layers.dart';
import '../solar_menu.dart';
import '../solar_states.dart';
import '../solar_target.dart';
import 'solar_card.dart';
import 'solar_dropdown_item.dart';
import 'solar_dropdown_menu.dart';
import 'solar_theme_of.dart';

class SolarFileCard extends StatelessWidget {
  const SolarFileCard({
    super.key,
    required this.title,
    this.type = SolarFileCardType.file,
    this.meta,
    this.fileIcon,
    this.thumbnail,
    this.moreItems,
    this.moreLabel = 'More actions',
    this.onPressed,
    this.statesController,
  });

  final SolarFileCardType type;

  /// The file’s name, or what the create tile adds; its action’s name where it is pressable.
  final String title;

  /// When it changed (“Edited just now”).
  final String? meta;

  /// Its type’s icon, where it has no thumbnail.
  final Widget? fileIcon;

  /// A preview of it (an image), filling the thumbnail in place of its icon.
  final Widget? thumbnail;

  /// The More menu's actions: a button opens them.
  final List<SolarCardMoreItem>? moreItems;

  /// The More button's accessible name.
  final String moreLabel;

  /// Makes it pressable: the whole card is a button that calls it.
  final VoidCallback? onPressed;

  /// Its states, where the caller keeps them.
  final WidgetStatesController? statesController;

  /// Each layer's children, as Figma nests them.
  static const _tree = <String, List<String>>{
    'root': ['thumbnail', 'footer', 'frame1', 'label'],
    'thumbnail': ['fileIcon'],
    'footer': ['text', 'iconMore'],
    'text': ['title', 'meta'],
    'frame1': ['iconPlus'],
  };

  @override
  Widget build(BuildContext context) {
    final rows = moreItems;
    if (rows == null || rows.isEmpty) return _card(context, null);
    // Its own menu, under the More button.
    return SolarMenuAnchor(
      menu: SolarDropdownMenu(
        children: [
          for (final item in rows)
            Builder(
              builder: (context) => SolarDropdownItem(
                label: item.label,
                icon: item.icon,
                onPressed: item.disabled
                    ? null
                    : () {
                        MenuController.maybeOf(context)?.close();
                        item.onSelected();
                      },
              ),
            ),
        ],
      ),
      builder: (context, controller) => _card(
        context,
        () => controller.isOpen ? controller.close() : controller.open(),
      ),
    );
  }

  Widget _card(BuildContext context, VoidCallback? onMore) {
    final t = solarThemeOf(context);
    final p = SolarFileCardProps(type: type);
    final pressable = onPressed != null;
    Widget draw(Set<WidgetState> states) {
      final recipe = SolarLayerRecipe(
        lookup: (c) => SolarFileCardRecipe.lookup(c, p, states),
        dimension: (c) => SolarFileCardRecipe.dimension(c, p, states),
        color: (c) => SolarFileCardRecipe.color(t, c, p, states),
        shadow: (c) => SolarFileCardRecipe.shadow(t, c, p, states),
        textStyle: (c) => SolarFileCardRecipe.textStyle(t, c, p, states),
        // A slot left empty is not drawn.
        present: (l) => switch (l) {
          'meta' => meta != null && SolarFileCardRecipe.present(l, p, states),
          'fileIcon' =>
            fileIcon != null && SolarFileCardRecipe.present(l, p, states),
          'iconMore' =>
            onMore != null && SolarFileCardRecipe.present(l, p, states),
          _ => SolarFileCardRecipe.present(l, p, states),
        },
        glyph: (_) => null,
      );
      SolarLayers layers(Map<String, List<Widget>> content) => SolarLayers(
        recipe: recipe,
        tree: _tree,
        keyPrefix: 'fileCard',
        text: {'label': title, 'title': title, 'meta': ?meta},
        slots: {'fileIcon': ?fileIcon},
        icons: const {
          'iconPlus': SolarIcons.plusOutline,
          'iconMore': SolarIcons.moreOutline,
        },
        content: content,
        clips: const {'thumbnail'},
        builders: {
          'iconMore': (glyph) => SolarTarget.inside(
            child: Semantics(
              // A node of its own, so the control keeps its name inside the card's.
              container: true,
              child: SolarPressable(
                onPressed: onMore,
                builder: (_, _) => Semantics(
                  label: moreLabel,
                  excludeSemantics: true,
                  child: glyph,
                ),
              ),
            ),
          ),
        },
      );
      return layers({
        'thumbnail': ?(thumbnail == null ? null : [thumbnail!]),
      }).layer('root');
    }

    final Widget mark = pressable
        ? SolarPressable(
            onPressed: onPressed,
            statesController: statesController,
            builder: (_, states) => draw(states),
          )
        : draw({});
    return Semantics(container: true, child: mark);
  }
}
