// SOLAR Radio recipe for Flutter. Generated by @bwp-web/codegen from spec/components/radio.json. Do not edit.
//
// The recipe is data: token *names*, keyed by layer and cell and by the base, size, appearance
// and combined sections of the IR. Values resolve against the ambient SolarTheme, so Light and
// Dark switch with the theme and no colour here is a literal.

import 'package:flutter/foundation.dart' show immutable;
import 'package:flutter/material.dart'
    show BoxShadow, Color, Colors, TextStyle, WidgetState;

import '../../solar_glyph.dart';
import '../../solar_icon.dart' show SolarVectorPath;
import '../tokens.dart';

/// The props a SOLAR Radio takes. Hover, pressed and focus are not here: they are
/// platform states, tracked by Flutter as [WidgetState]s.
@immutable
class SolarRadioProps {
  const SolarRadioProps({this.disabled = false, this.checked = true});

  final bool disabled;
  final bool checked;
}

abstract final class SolarRadioRecipe {
  /// Every IR entry, as `<layer>.<cell>|<section>…` to a token name (`t:`), a keyword (`k:`),
  /// `none`, a presence (`b:`) or a literal the overlay allowed (`px:`).
  static const Map<String, String> cells = {
    'root.present|base': 'b:true',
    'root.background|base': 'none',
    'root.borderColor|base': 't:color.border.subtle',
    'root.shadow|base': 'none',
    'root.radius|base': 't:radius.pill',
    'root.borderWidth|base': 't:border.default',
    'root.width|base': 'px:18',
    'root.height|base': 'px:18',
    'root.background|appearance|checked=false|hover':
        't:color.surface.background',
    'root.borderColor|appearance|checked=false|hover': 't:color.border.medium',
    'root.background|appearance|checked=false|focus': 't:color.surface.base',
    'root.borderColor|appearance|checked=false|focus':
        't:color.border.feedback.focus.strong',
    'root.shadow|appearance|checked=false|focus': 't:shadow.focus.default',
    'root.borderColor|appearance|checked=false|default':
        't:color.border.medium',
    'root.borderColor|appearance|checked=false|disabled':
        't:color.border.medium',
    'root.background|appearance|checked=true|focus': 't:color.surface.base',
    'root.borderColor|appearance|checked=true|focus':
        't:color.border.feedback.focus.strong',
    'root.shadow|appearance|checked=true|focus': 't:shadow.focus.default',
    'root.borderColor|appearance|checked=true|hover': 't:color.border.medium',
    'root.borderColor|appearance|checked=true|disabled':
        't:color.border.medium',
    'icon.present|base': 'b:true',
    'icon.glyph|base': 'g:0',
    'icon.background|base': 't:color.icon.primary',
    'icon.borderColor|base': 'none',
    'icon.shadow|base': 'none',
    'icon.radius|base': 'none',
    'icon.borderWidth|base': 'none',
    'icon.x|base': 'px:4',
    'icon.y|base': 'px:4',
    'icon.present|appearance|checked=false|default': 'b:false',
    'icon.present|appearance|checked=false|hover': 'b:false',
    'icon.present|appearance|checked=false|disabled': 'b:false',
    'icon.present|appearance|checked=false|focus': 'b:false',
    'icon.background|appearance|checked=true|disabled': 't:color.icon.disabled',
  };

  /// Which state wins when several hold, highest first: the MUI recipe's cascade, read backwards.
  static const List<String> statePrecedence = ['disabled', 'focus', 'hover'];

  static bool _holds(String state, SolarRadioProps p, Set<WidgetState> s) =>
      switch (state) {
        'disabled' => p.disabled || s.contains(WidgetState.disabled),
        'focus' => s.contains(WidgetState.focused),
        'hover' => s.contains(WidgetState.hovered),
        _ => false,
      };

  /// The entry for one cell under these props and states, by the IR's precedence: a state that
  /// holds beats the resting value, the per-size-and-appearance entry beats the per-appearance
  /// one, and the resting value falls back through appearance, size and base.
  static String? lookup(String cell, SolarRadioProps p, Set<WidgetState> s) {
    final combo = 'checked=${p.checked}';
    const size = '';
    for (final state in statePrecedence) {
      if (!_holds(state, p, s)) continue;
      final hit =
          cells['$cell|combined|$size|$combo|$state'] ??
          cells['$cell|appearance|$combo|$state'];
      if (hit != null) return hit;
    }
    return cells['$cell|combined|$size|$combo|default'] ??
        cells['$cell|appearance|$combo|default'] ??
        cells['$cell|size|$size'] ??
        cells['$cell|base'];
  }

  static Color color(
    SolarTheme t,
    String cell,
    SolarRadioProps p,
    Set<WidgetState> s,
  ) {
    final c = t.colors;
    return switch (lookup(cell, p, s)) {
      'none' => Colors.transparent,
      't:color.border.feedback.focus.strong' => c.borderFeedbackFocusStrong,
      't:color.border.medium' => c.borderMedium,
      't:color.border.subtle' => c.borderSubtle,
      't:color.icon.disabled' => c.iconDisabled,
      't:color.icon.primary' => c.iconPrimary,
      't:color.surface.background' => c.surfaceBackground,
      't:color.surface.base' => c.surfaceBase,
      final v => throw StateError('$cell: no colour for $v'),
    };
  }

  static List<BoxShadow> shadow(
    SolarTheme t,
    String cell,
    SolarRadioProps p,
    Set<WidgetState> s,
  ) => switch (lookup(cell, p, s)) {
    'none' || null => const <BoxShadow>[],
    't:shadow.focus.default' => t.shadows.focusDefault,
    final v => throw StateError('$cell: no shadow for $v'),
  };

  /// A length in logical pixels, or null where the IR says the layer hugs its content.
  static double? dimension(String cell, SolarRadioProps p, Set<WidgetState> s) {
    final v = lookup(cell, p, s);
    if (v == null || v == 'k:HUG') return null;
    if (v == 'k:FILL') return double.infinity;
    if (v.startsWith('px:')) return double.parse(v.substring(3));
    return switch (v) {
      't:border.default' => SolarBorder.$default,
      't:radius.pill' => SolarRadius.pill,
      _ => throw StateError('$cell: no length for $v'),
    };
  }

  static TextStyle? textStyle(
    SolarTheme t,
    String cell,
    SolarRadioProps p,
    Set<WidgetState> s,
  ) => switch (lookup(cell, p, s)) {
    'none' || null => null,

    final v => throw StateError('$cell: no text style for $v'),
  };

  /// Whether a layer is drawn: the shell reads this, the style does not.
  static bool present(String layer, SolarRadioProps p, Set<WidgetState> s) =>
      lookup('$layer.present', p, s) != 'b:false';

  /// The shapes the layers draw themselves (a cell's `g:<n>` is the nth), Figma's path data.
  static const List<SolarGlyph> glyphs = <SolarGlyph>[
    SolarGlyph(
      width: 10.0,
      height: 10.0,
      fill: <SolarVectorPath>[
        SolarVectorPath(
          'M5 0C2.24 0 0 2.24 0 5C0 7.76 2.24 10 5 10C7.76 10 10 7.76 10 5C10 2.24 7.76 0 5 0Z',
        ),
      ],
      stroke: <SolarVectorPath>[],
    ),
  ];

  /// The shape a layer draws under these props and states, or null where it draws none.
  static SolarGlyph? glyph(
    String layer,
    SolarRadioProps p,
    Set<WidgetState> s,
  ) {
    final v = lookup('$layer.glyph', p, s);
    return v != null && v.startsWith('g:')
        ? glyphs[int.parse(v.substring(2))]
        : null;
  }
}
