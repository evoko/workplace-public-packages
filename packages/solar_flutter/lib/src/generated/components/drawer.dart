// SOLAR Drawer recipe for Flutter. Generated by @bwp-web/codegen from spec/components/drawer.json. Do not edit.
//
// The recipe is data: token *names*, keyed by layer and cell and by the base, size, appearance
// and combined sections of the IR. Values resolve against the ambient SolarTheme, so Light and
// Dark switch with the theme and no colour here is a literal.

import 'package:flutter/foundation.dart' show immutable;
import 'package:flutter/material.dart'
    show BoxShadow, Color, Colors, TextStyle, WidgetState;

import '../tokens.dart';

/// The props a SOLAR Drawer takes. Hover, pressed and focus are not here: they are
/// platform states, tracked by Flutter as [WidgetState]s.
@immutable
class SolarDrawerProps {
  const SolarDrawerProps();
}

abstract final class SolarDrawerRecipe {
  /// Every IR entry, as `<layer>.<cell>|<section>…` to a token name (`t:`), a keyword (`k:`),
  /// `none`, a presence (`b:`) or a literal the overlay allowed (`px:`).
  static const Map<String, String> cells = {
    'root.present|base': 'b:true',
    'root.background|base': 't:color.surface.raised',
    'root.borderColor|base': 't:color.border.surface',
    'root.shadow|base': 't:shadow.overlay',
    'root.radiusTopLeft|base': 't:radius.dialog',
    'root.radiusTopRight|base': 't:radius.none',
    'root.radiusBottomRight|base': 't:radius.none',
    'root.radiusBottomLeft|base': 't:radius.dialog',
    'root.borderTopWidth|base': 'none',
    'root.borderRightWidth|base': 'none',
    'root.borderBottomWidth|base': 'none',
    'root.borderLeftWidth|base': 't:border.default',
    'root.direction|base': 'k:VERTICAL',
    'root.align|base': 'k:MIN/MIN',
    'root.gap|base': 't:stack.none',
    'root.paddingTop|base': 't:inset.none',
    'root.paddingRight|base': 't:inset.none',
    'root.paddingBottom|base': 't:inset.none',
    'root.paddingLeft|base': 't:inset.none',
    'root.width|base': 'px:348',
    'root.height|base': 'k:FILL',
    'header.present|base': 'b:true',
    'header.background|base': 'none',
    'header.borderColor|base': 't:color.border.surface',
    'header.shadow|base': 'none',
    'header.radius|base': 'none',
    'header.borderTopWidth|base': 'none',
    'header.borderRightWidth|base': 'none',
    'header.borderBottomWidth|base': 't:border.default',
    'header.borderLeftWidth|base': 'none',
    'header.direction|base': 'k:HORIZONTAL',
    'header.align|base': 'k:CENTER/CENTER',
    'header.gap|base': 't:inset.none',
    'header.paddingTop|base': 't:inset.xs',
    'header.paddingRight|base': 't:inset.lg',
    'header.paddingBottom|base': 't:inset.xs',
    'header.paddingLeft|base': 't:inset.lg',
    'header.width|base': 'k:FILL',
    'header.height|base': 'k:HUG',
    'title.present|base': 'b:true',
    'title.color|base': 't:color.text.primary',
    'title.typography|base': 't:typography.body.lg.medium',
    'title.width|base': 'k:FILL',
    'close.present|base': 'b:true',
    'close.component|base': 'k:Icon Button',
    'close.variant.size|base': 'k:md',
    'close.variant.shape|base': 'k:square',
    'close.variant.prio|base': 'k:tertiary',
    'close.variant.state|base': 'k:default',
    'close.width|base': 'none',
    'close.height|base': 'none',
    'content.present|base': 'b:true',
    'content.background|base': 'none',
    'content.borderColor|base': 'none',
    'content.shadow|base': 'none',
    'content.radius|base': 'none',
    'content.borderWidth|base': 'none',
    'content.direction|base': 'k:VERTICAL',
    'content.align|base': 'k:MIN/MIN',
    'content.gap|base': 't:stack.sm',
    'content.paddingTop|base': 't:inset.md',
    'content.paddingRight|base': 't:inset.lg',
    'content.paddingBottom|base': 't:inset.md',
    'content.paddingLeft|base': 't:inset.lg',
    'content.width|base': 'k:FILL',
    'content.height|base': 'k:FILL',
    'cta.present|base': 'b:true',
    'cta.component|base': 'k:Button Group',
    'cta.variant.orientation|base': 'k:horizontal',
    'cta.variant.type|base': 'k:full-width',
    'cta.width|base': 'k:FILL',
    'cta.height|base': 'k:HUG',
  };

  /// Each layer's children, in Figma's order: the tree the widget draws (SolarLayers).
  static const Map<String, List<String>> tree = {
    'root': ['header', 'content', 'cta'],
    'header': ['title', 'close'],
  };

  /// Which state wins when several hold, highest first: the MUI recipe's cascade, read backwards.
  static const List<String> statePrecedence = <String>[];

  static bool _holds(String state, SolarDrawerProps p, Set<WidgetState> s) =>
      switch (state) {
        _ => false,
      };

  /// The entry for one cell under these props and states, by the IR's precedence: a state that
  /// holds beats the resting value, the per-size-and-appearance entry beats the per-appearance
  /// one, and the resting value falls back through appearance, size and base.
  static String? lookup(String cell, SolarDrawerProps p, Set<WidgetState> s) {
    const combo = 'default';
    const size = '';
    for (final state in statePrecedence) {
      if (!_holds(state, p, s)) continue;
      final hit =
          cells['$cell|combined|$size|$combo|$state'] ??
          cells['$cell|appearance|$combo|$state'];
      if (hit != null) return hit;
    }
    return cells['$cell|combined|$size|$combo|default'] ??
        cells['$cell|appearance|$combo|default'] ??
        cells['$cell|size|$size'] ??
        cells['$cell|base'];
  }

  static Color color(
    SolarTheme t,
    String cell,
    SolarDrawerProps p,
    Set<WidgetState> s,
  ) {
    final c = t.colors;
    return switch (lookup(cell, p, s)) {
      'none' => Colors.transparent,
      't:color.border.surface' => c.borderSurface,
      't:color.surface.raised' => c.surfaceRaised,
      't:color.text.primary' => c.textPrimary,
      final v => throw StateError('$cell: no colour for $v'),
    };
  }

  static List<BoxShadow> shadow(
    SolarTheme t,
    String cell,
    SolarDrawerProps p,
    Set<WidgetState> s,
  ) => switch (lookup(cell, p, s)) {
    'none' || null => const <BoxShadow>[],
    't:shadow.overlay' => t.shadows.overlay,
    final v => throw StateError('$cell: no shadow for $v'),
  };

  /// A length in logical pixels, or null where the IR says the layer hugs its content.
  static double? dimension(
    String cell,
    SolarDrawerProps p,
    Set<WidgetState> s,
  ) {
    final v = lookup(cell, p, s);
    if (v == null || v == 'k:HUG') return null;
    if (v == 'k:FILL') return double.infinity;
    if (v.startsWith('px:')) return double.parse(v.substring(3));
    return switch (v) {
      't:border.default' => SolarBorder.$default,
      't:inset.lg' => SolarInset.lg,
      't:inset.md' => SolarInset.md,
      't:inset.none' => SolarInset.none,
      't:inset.xs' => SolarInset.xs,
      't:radius.dialog' => SolarRadius.dialog,
      't:radius.none' => SolarRadius.none,
      't:stack.none' => SolarStack.none,
      't:stack.sm' => SolarStack.sm,
      _ => throw StateError('$cell: no length for $v'),
    };
  }

  static TextStyle? textStyle(
    SolarTheme t,
    String cell,
    SolarDrawerProps p,
    Set<WidgetState> s,
  ) => switch (lookup(cell, p, s)) {
    'none' || null => null,
    't:typography.body.lg.medium' => t.typography.bodyLgMedium,
    final v => throw StateError('$cell: no text style for $v'),
  };

  /// Whether a layer is drawn: the shell reads this, the style does not.
  static bool present(String layer, SolarDrawerProps p, Set<WidgetState> s) =>
      lookup('$layer.present', p, s) != 'b:false';
}
