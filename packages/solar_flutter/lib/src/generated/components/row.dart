// SOLAR Row recipe for Flutter. Generated by @bwp-web/codegen from spec/components/row.json. Do not edit.
//
// The recipe is data: token *names*, keyed by layer and cell and by the base, size, appearance
// and combined sections of the IR. Values resolve against the ambient SolarTheme, so Light and
// Dark switch with the theme and no colour here is a literal.

import 'package:flutter/foundation.dart' show immutable;
import 'package:flutter/material.dart'
    show BoxShadow, Color, Colors, TextStyle, WidgetState;

import '../tokens.dart';

enum SolarRowType {
  top('top'),
  middle('middle'),
  title('title'),
  bottom('bottom'),
  nonExpandable('non-expandable');

  const SolarRowType(this.figma);

  /// The value as Figma spells it, which the recipe is keyed by.
  final String figma;
}

/// The props a SOLAR Row takes. Hover, pressed and focus are not here: they are
/// platform states, tracked by Flutter as [WidgetState]s.
@immutable
class SolarRowProps {
  const SolarRowProps({
    this.type = SolarRowType.nonExpandable,
    this.selected = false,
  });

  final SolarRowType type;
  final bool selected;
}

abstract final class SolarRowRecipe {
  /// Every IR entry, as `<layer>.<cell>|<section>…` to a token name (`t:`), a keyword (`k:`),
  /// `none`, a presence (`b:`) or a literal the overlay allowed (`px:`).
  static const Map<String, String> cells = {
    'root.present|base': 'b:true',
    'root.background|base': 'none',
    'root.borderColor|base': 'none',
    'root.shadow|base': 'none',
    'root.radius|base': 't:radius.container',
    'root.borderWidth|base': 'none',
    'root.direction|base': 'k:HORIZONTAL',
    'root.align|base': 'k:MIN/CENTER',
    'root.gap|base': 't:inset.none',
    'root.paddingTop|base': 't:inset.none',
    'root.paddingRight|base': 't:inset.none',
    'root.paddingBottom|base': 't:inset.none',
    'root.paddingLeft|base': 't:inset.none',
    'root.width|base': 'k:FILL',
    'root.height|base': 'k:HUG',
    'root.background|appearance|type=non-expandable|selected':
        't:color.surface.active',
    'root.radius|appearance|type=non-expandable|default': 'none',
    'root.height|appearance|type=non-expandable|default': 'px:44',
    'root.background|appearance|type=non-expandable|hover':
        't:color.surface.hover',
    'root.background|appearance|type=top|selected': 't:color.surface.active',
    'root.radius|appearance|type=top|default': 'none',
    'root.height|appearance|type=top|default': 'px:44',
    'root.background|appearance|type=top|hover': 't:color.surface.hover',
    'root.background|appearance|type=middle|selected': 't:color.surface.active',
    'root.radius|appearance|type=middle|default': 'none',
    'root.height|appearance|type=middle|default': 'px:44',
    'root.background|appearance|type=middle|hover': 't:color.surface.hover',
    'root.background|appearance|type=bottom|selected': 't:color.surface.active',
    'root.radius|appearance|type=bottom|default': 'none',
    'root.height|appearance|type=bottom|default': 'px:44',
    'root.background|appearance|type=bottom|hover': 't:color.surface.hover',
    'checkBox.present|base': 'b:true',
    'checkBox.component|base': 'k:RowSelect',
    'checkBox.variant.title-row|base': 'k:true',
    'checkBox.width|base': 'none',
    'checkBox.height|base': 'none',
    'checkBox.variant.title-row|appearance|type=non-expandable|default':
        'k:false',
    'checkBox.variant.title-row|appearance|type=non-expandable|selected':
        'k:false',
    'checkBox.variant.title-row|appearance|type=top|default': 'k:false',
    'checkBox.variant.title-row|appearance|type=top|selected': 'k:false',
    'checkBox.variant.title-row|appearance|type=middle|default': 'k:false',
    'checkBox.variant.title-row|appearance|type=middle|selected': 'k:false',
    'checkBox.variant.title-row|appearance|type=bottom|default': 'k:false',
    'checkBox.variant.title-row|appearance|type=bottom|selected': 'k:false',
    'expand.present|base': 'b:true',
    'expand.component|base': 'k:RowExpand',
    'expand.variant.type|base': 'k:title-row',
    'expand.width|base': 'none',
    'expand.height|base': 'none',
    'expand.variant.type|appearance|type=non-expandable|default': 'k:collapsed',
    'expand.variant.type|appearance|type=non-expandable|selected':
        'k:collapsed',
    'expand.variant.type|appearance|type=top|default': 'k:collapsed',
    'expand.variant.type|appearance|type=top|selected': 'k:collapsed',
    'expand.variant.type|appearance|type=middle|default': 'k:middle-row',
    'expand.variant.type|appearance|type=middle|selected': 'k:middle-row',
    'expand.variant.type|appearance|type=bottom|default': 'k:bottom-row',
    'expand.variant.type|appearance|type=bottom|selected': 'k:bottom-row',
    'titleRowContent.present|base': 'b:true',
    'titleRowContent.background|base': 'none',
    'titleRowContent.borderColor|base': 'none',
    'titleRowContent.shadow|base': 'none',
    'titleRowContent.radius|base': 'none',
    'titleRowContent.borderWidth|base': 'none',
    'titleRowContent.direction|base': 'k:HORIZONTAL',
    'titleRowContent.align|base': 'k:MIN/CENTER',
    'titleRowContent.gap|base': 't:inset.none',
    'titleRowContent.paddingTop|base': 't:inset.none',
    'titleRowContent.paddingRight|base': 't:inset.none',
    'titleRowContent.paddingBottom|base': 't:inset.none',
    'titleRowContent.paddingLeft|base': 't:inset.none',
    'titleRowContent.width|base': 'k:FILL',
    'titleRowContent.height|base': 'k:FILL',
    'columnItem.present|base': 'b:true',
    'columnItem.component|base': 'k:Column Item',
    'columnItem.variant.title|base': 'k:true',
    'columnItem.variant.type|base': 'k:text',
    'columnItem.width|base': 'k:FILL',
    'columnItem.height|base': 'k:FILL',
    'columnItem.variant.title|appearance|type=non-expandable|default':
        'k:false',
    'columnItem.variant.title|appearance|type=non-expandable|selected':
        'k:false',
    'columnItem.variant.title|appearance|type=top|default': 'k:false',
    'columnItem.variant.title|appearance|type=top|selected': 'k:false',
    'columnItem.variant.title|appearance|type=middle|default': 'k:false',
    'columnItem.variant.title|appearance|type=middle|selected': 'k:false',
    'columnItem.variant.title|appearance|type=bottom|default': 'k:false',
    'columnItem.variant.title|appearance|type=bottom|selected': 'k:false',
  };

  /// Each layer's children, in Figma's order: the tree the widget draws (SolarLayers).
  static const Map<String, List<String>> tree = {
    'root': ['checkBox', 'expand', 'titleRowContent'],
    'titleRowContent': ['columnItem'],
  };

  /// Which state wins when several hold, highest first: the MUI recipe's cascade, read backwards.
  static const List<String> statePrecedence = ['selected', 'hover'];

  static bool _holds(String state, SolarRowProps p, Set<WidgetState> s) =>
      switch (state) {
        'selected' => p.selected,
        'hover' => s.contains(WidgetState.hovered),
        _ => false,
      };

  /// The entry for one cell under these props and states, by the IR's precedence: a state that
  /// holds beats the resting value, the per-size-and-appearance entry beats the per-appearance
  /// one, and the resting value falls back through appearance, size and base.
  static String? lookup(String cell, SolarRowProps p, Set<WidgetState> s) {
    final combo = 'type=${p.type.figma}';
    const size = '';
    for (final state in statePrecedence) {
      if (!_holds(state, p, s)) continue;
      final hit =
          cells['$cell|combined|$size|$combo|$state'] ??
          cells['$cell|appearance|$combo|$state'];
      if (hit != null) return hit;
    }
    return cells['$cell|combined|$size|$combo|default'] ??
        cells['$cell|appearance|$combo|default'] ??
        cells['$cell|size|$size'] ??
        cells['$cell|base'];
  }

  static Color color(
    SolarTheme t,
    String cell,
    SolarRowProps p,
    Set<WidgetState> s,
  ) {
    final c = t.colors;
    return switch (lookup(cell, p, s)) {
      'none' => Colors.transparent,
      't:color.surface.active' => c.surfaceActive,
      't:color.surface.hover' => c.surfaceHover,
      final v => throw StateError('$cell: no colour for $v'),
    };
  }

  static List<BoxShadow> shadow(
    SolarTheme t,
    String cell,
    SolarRowProps p,
    Set<WidgetState> s,
  ) => switch (lookup(cell, p, s)) {
    'none' || null => const <BoxShadow>[],

    final v => throw StateError('$cell: no shadow for $v'),
  };

  /// A length in logical pixels, or null where the IR says the layer hugs its content.
  static double? dimension(String cell, SolarRowProps p, Set<WidgetState> s) {
    final v = lookup(cell, p, s);
    if (v == null || v == 'k:HUG') return null;
    if (v == 'k:FILL') return double.infinity;
    if (v.startsWith('px:')) return double.parse(v.substring(3));
    return switch (v) {
      't:inset.none' => SolarInset.none,
      't:radius.container' => SolarRadius.container,
      _ => throw StateError('$cell: no length for $v'),
    };
  }

  static TextStyle? textStyle(
    SolarTheme t,
    String cell,
    SolarRowProps p,
    Set<WidgetState> s,
  ) => switch (lookup(cell, p, s)) {
    'none' || null => null,

    final v => throw StateError('$cell: no text style for $v'),
  };

  /// Whether a layer is drawn: the shell reads this, the style does not.
  static bool present(String layer, SolarRowProps p, Set<WidgetState> s) =>
      lookup('$layer.present', p, s) != 'b:false';
}
