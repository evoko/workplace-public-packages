// SOLAR Nav Item recipe for Flutter. Generated by @bwp-web/codegen from spec/components/nav item.json. Do not edit.
//
// The recipe is data: token *names*, keyed by layer and cell and by the base, size, appearance
// and combined sections of the IR. Values resolve against the ambient SolarTheme, so Light and
// Dark switch with the theme and no colour here is a literal.

import 'package:flutter/foundation.dart' show immutable;
import 'package:flutter/material.dart'
    show BoxShadow, Color, Colors, TextStyle, WidgetState;

import '../tokens.dart';

/// The props a SOLAR Nav Item takes. Hover, pressed and focus are not here: they are
/// platform states, tracked by Flutter as [WidgetState]s.
@immutable
class SolarNavItemProps {
  const SolarNavItemProps({this.selected = false, this.expanded = false});

  final bool selected;
  final bool expanded;
}

abstract final class SolarNavItemRecipe {
  /// Every IR entry, as `<layer>.<cell>|<section>…` to a token name (`t:`), a keyword (`k:`),
  /// `none`, a presence (`b:`) or a literal the overlay allowed (`px:`).
  static const Map<String, String> cells = {
    'root.present|base': 'b:true',
    'root.background|base': 'none',
    'root.borderColor|base': 'none',
    'root.shadow|base': 'none',
    'root.radius|base': 't:radius.control',
    'root.borderWidth|base': 'none',
    'root.direction|base': 'k:HORIZONTAL',
    'root.align|base': 'k:CENTER/CENTER',
    'root.gap|base': 't:inset.none',
    'root.paddingTop|base': 't:inset.none',
    'root.paddingRight|base': 't:inset.none',
    'root.paddingBottom|base': 't:inset.none',
    'root.paddingLeft|base': 't:inset.none',
    'root.width|base': 'px:40',
    'root.height|base': 'px:40',
    'root.background|appearance|selected=false, expanded=false|hover':
        't:color.surface.hover',
    'root.shadow|appearance|selected=false, expanded=false|focus':
        't:shadow.focus.default',
    'root.background|appearance|selected=false, expanded=false|focus': 'none',
    'root.background|appearance|selected=false, expanded=true|hover':
        't:color.surface.hover',
    'root.gap|appearance|selected=false, expanded=true|default': 't:inset.sm',
    'root.paddingRight|appearance|selected=false, expanded=true|default':
        't:inset.sm',
    'root.paddingLeft|appearance|selected=false, expanded=true|default':
        't:inset.sm',
    'root.width|appearance|selected=false, expanded=true|default': 'k:FILL',
    'root.shadow|appearance|selected=false, expanded=true|focus':
        't:shadow.focus.default',
    'root.background|appearance|selected=false, expanded=true|focus': 'none',
    'root.background|appearance|selected=true, expanded=false|default':
        't:color.surface.active',
    'root.background|appearance|selected=true, expanded=false|hover':
        't:color.surface.active',
    'root.shadow|appearance|selected=true, expanded=false|focus':
        't:shadow.focus.default',
    'root.background|appearance|selected=true, expanded=true|default':
        't:color.surface.active',
    'root.gap|appearance|selected=true, expanded=true|default': 't:inset.sm',
    'root.paddingRight|appearance|selected=true, expanded=true|default':
        't:inset.sm',
    'root.paddingLeft|appearance|selected=true, expanded=true|default':
        't:inset.sm',
    'root.width|appearance|selected=true, expanded=true|default': 'k:FILL',
    'root.background|appearance|selected=true, expanded=true|hover':
        't:color.surface.active',
    'root.shadow|appearance|selected=true, expanded=true|focus':
        't:shadow.focus.default',
    'iconOutline.present|base': 'b:true',
    'iconOutline.component|base': 'k:Icon/Home',
    'iconOutline.variant.solid|base': 'k:false',
    'iconOutline.width|base': 't:icon.md',
    'iconOutline.height|base': 't:icon.md',
    'iconOutline.color|base': 't:color.icon.secondary',
    'iconOutline.variant.solid|appearance|selected=true, expanded=false|default':
        'k:true',
    'iconOutline.color|appearance|selected=true, expanded=false|default':
        't:color.icon.primary',
    'iconOutline.variant.solid|appearance|selected=true, expanded=false|hover':
        'k:true',
    'iconOutline.color|appearance|selected=true, expanded=false|hover':
        't:color.icon.primary',
    'iconOutline.variant.solid|appearance|selected=true, expanded=true|default':
        'k:true',
    'iconOutline.color|appearance|selected=true, expanded=true|default':
        't:color.icon.primary',
    'iconOutline.variant.solid|appearance|selected=true, expanded=true|hover':
        'k:true',
    'iconOutline.color|appearance|selected=true, expanded=true|hover':
        't:color.icon.primary',
    'iconOutline.color|appearance|selected=false, expanded=false|hover':
        't:color.icon.primary',
    'iconOutline.color|appearance|selected=false, expanded=false|focus':
        't:color.icon.secondary',
    'iconOutline.color|appearance|selected=false, expanded=true|hover':
        't:color.icon.primary',
    'iconOutline.color|appearance|selected=false, expanded=true|focus':
        't:color.icon.secondary',
    'label.present|base': 'b:false',
    'label.typography|base': 't:typography.label.md',
    'label.width|base': 'k:FILL',
    'label.present|appearance|selected=false, expanded=true|default': 'b:true',
    'label.color|appearance|selected=false, expanded=true|default':
        't:color.text.secondary',
    'label.present|appearance|selected=false, expanded=true|hover': 'b:true',
    'label.color|appearance|selected=false, expanded=true|hover':
        't:color.text.primary',
    'label.color|appearance|selected=false, expanded=true|focus':
        't:color.text.secondary',
    'label.present|appearance|selected=true, expanded=true|default': 'b:true',
    'label.color|appearance|selected=true, expanded=true|default':
        't:color.text.primary',
    'label.present|appearance|selected=true, expanded=true|hover': 'b:true',
    'label.color|appearance|selected=true, expanded=true|hover':
        't:color.text.primary',
  };

  /// Which state wins when several hold, highest first: the MUI recipe's cascade, read backwards.
  static const List<String> statePrecedence = ['focus', 'hover'];

  static bool _holds(String state, SolarNavItemProps p, Set<WidgetState> s) =>
      switch (state) {
        'focus' => s.contains(WidgetState.focused),
        'hover' => s.contains(WidgetState.hovered),
        _ => false,
      };

  /// The entry for one cell under these props and states, by the IR's precedence: a state that
  /// holds beats the resting value, the per-size-and-appearance entry beats the per-appearance
  /// one, and the resting value falls back through appearance, size and base.
  static String? lookup(String cell, SolarNavItemProps p, Set<WidgetState> s) {
    final combo = 'selected=${p.selected}, expanded=${p.expanded}';
    const size = '';
    for (final state in statePrecedence) {
      if (!_holds(state, p, s)) continue;
      final hit =
          cells['$cell|combined|$size|$combo|$state'] ??
          cells['$cell|appearance|$combo|$state'];
      if (hit != null) return hit;
    }
    return cells['$cell|combined|$size|$combo|default'] ??
        cells['$cell|appearance|$combo|default'] ??
        cells['$cell|size|$size'] ??
        cells['$cell|base'];
  }

  static Color color(
    SolarTheme t,
    String cell,
    SolarNavItemProps p,
    Set<WidgetState> s,
  ) {
    final c = t.colors;
    return switch (lookup(cell, p, s)) {
      'none' => Colors.transparent,
      't:color.icon.primary' => c.iconPrimary,
      't:color.icon.secondary' => c.iconSecondary,
      't:color.surface.active' => c.surfaceActive,
      't:color.surface.hover' => c.surfaceHover,
      't:color.text.primary' => c.textPrimary,
      't:color.text.secondary' => c.textSecondary,
      final v => throw StateError('$cell: no colour for $v'),
    };
  }

  static List<BoxShadow> shadow(
    SolarTheme t,
    String cell,
    SolarNavItemProps p,
    Set<WidgetState> s,
  ) => switch (lookup(cell, p, s)) {
    'none' || null => const <BoxShadow>[],
    't:shadow.focus.default' => t.shadows.focusDefault,
    final v => throw StateError('$cell: no shadow for $v'),
  };

  /// A length in logical pixels, or null where the IR says the layer hugs its content.
  static double? dimension(
    String cell,
    SolarNavItemProps p,
    Set<WidgetState> s,
  ) {
    final v = lookup(cell, p, s);
    if (v == null || v == 'k:HUG') return null;
    if (v == 'k:FILL') return double.infinity;
    if (v.startsWith('px:')) return double.parse(v.substring(3));
    return switch (v) {
      't:icon.md' => SolarIconSize.md,
      't:inset.none' => SolarInset.none,
      't:inset.sm' => SolarInset.sm,
      't:radius.control' => SolarRadius.control,
      _ => throw StateError('$cell: no length for $v'),
    };
  }

  static TextStyle? textStyle(
    SolarTheme t,
    String cell,
    SolarNavItemProps p,
    Set<WidgetState> s,
  ) => switch (lookup(cell, p, s)) {
    'none' || null => null,
    't:typography.label.md' => t.typography.labelMd,
    final v => throw StateError('$cell: no text style for $v'),
  };

  /// Whether a layer is drawn: the shell reads this, the style does not.
  static bool present(String layer, SolarNavItemProps p, Set<WidgetState> s) =>
      lookup('$layer.present', p, s) != 'b:false';
}
