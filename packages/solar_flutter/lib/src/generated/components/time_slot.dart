// SOLAR Time Slot recipe for Flutter. Generated by @bwp-web/codegen from spec/components/time slot.json. Do not edit.
//
// The recipe is data: token *names*, keyed by layer and cell and by the base, size, appearance
// and combined sections of the IR. Values resolve against the ambient SolarTheme, so Light and
// Dark switch with the theme and no colour here is a literal.

import 'package:flutter/foundation.dart' show immutable;
import 'package:flutter/material.dart'
    show BoxShadow, Color, Colors, TextStyle, WidgetState;

import '../tokens.dart';

enum SolarTimeSlotDensity { comfortable, compact }

/// The props a SOLAR Time Slot takes. Hover, pressed and focus are not here: they are
/// platform states, tracked by Flutter as [WidgetState]s.
@immutable
class SolarTimeSlotProps {
  const SolarTimeSlotProps({
    this.selected = false,
    this.density = SolarTimeSlotDensity.comfortable,
  });

  final bool selected;
  final SolarTimeSlotDensity density;
}

abstract final class SolarTimeSlotRecipe {
  /// Every IR entry, as `<layer>.<cell>|<section>…` to a token name (`t:`), a keyword (`k:`),
  /// `none`, a presence (`b:`) or a literal the overlay allowed (`px:`).
  static const Map<String, String> cells = {
    'root.present|base': 'b:true',
    'root.background|base': 't:color.surface.base',
    'root.borderColor|base': 't:color.border.subtle',
    'root.shadow|base': 'none',
    'root.radius|base': 'none',
    'root.borderTopWidth|base': 't:border.default',
    'root.borderRightWidth|base': 'none',
    'root.borderBottomWidth|base': 'none',
    'root.borderLeftWidth|base': 't:border.default',
    'root.width|base': 'k:FILL',
    'root.height|base': 'px:48',
    'root.background|appearance|density=comfortable|hover':
        't:color.surface.hover',
    'root.background|appearance|density=comfortable|selected':
        't:color.surface.feedback.info.subtle',
    'root.borderColor|appearance|density=comfortable|selected':
        't:color.border.feedback.focus.strong',
    'root.background|appearance|density=compact|hover': 't:color.surface.hover',
    'root.background|appearance|density=compact|selected':
        't:color.surface.feedback.info.subtle',
    'root.borderColor|appearance|density=compact|selected':
        't:color.border.feedback.focus.strong',
    'root.height|appearance|density=compact|default': 'px:32',
    'halfHourRule.present|base': 'b:true',
    'halfHourRule.background|base': 'none',
    'halfHourRule.borderColor|base': 't:color.border.subtle',
    'halfHourRule.shadow|base': 'none',
    'halfHourRule.radius|base': 'none',
    'halfHourRule.borderTopWidth|base': 't:border.default',
    'halfHourRule.borderRightWidth|base': 'none',
    'halfHourRule.borderBottomWidth|base': 'none',
    'halfHourRule.borderLeftWidth|base': 'none',
    'halfHourRule.borderDash|base': 'd:2,4',
    'halfHourRule.width|base': 'k:FILL',
    'halfHourRule.height|base': 't:border.default',
    'halfHourRule.x|base': 'px:0',
    'halfHourRule.y|base': 'px:24',
    'halfHourRule.y|appearance|density=compact|default': 'px:16',
    'halfHourRule.y|appearance|density=compact|hover': 'px:16',
    'halfHourRule.y|appearance|density=compact|selected': 'px:16',
  };

  /// Each layer's children, in Figma's order: the tree the widget draws (SolarLayers).
  static const Map<String, List<String>> tree = {
    'root': ['halfHourRule'],
  };

  /// Which state wins when several hold, highest first: the MUI recipe's cascade, read backwards.
  static const List<String> statePrecedence = ['selected', 'hover'];

  static bool _holds(String state, SolarTimeSlotProps p, Set<WidgetState> s) =>
      switch (state) {
        'selected' => p.selected,
        'hover' => s.contains(WidgetState.hovered),
        _ => false,
      };

  /// The entry for one cell under these props and states, by the IR's precedence: a state that
  /// holds beats the resting value, the per-size-and-appearance entry beats the per-appearance
  /// one, and the resting value falls back through appearance, size and base.
  static String? lookup(String cell, SolarTimeSlotProps p, Set<WidgetState> s) {
    final combo = 'density=${p.density.name}';
    const size = '';
    for (final state in statePrecedence) {
      if (!_holds(state, p, s)) continue;
      final hit =
          cells['$cell|combined|$size|$combo|$state'] ??
          cells['$cell|appearance|$combo|$state'];
      if (hit != null) return hit;
    }
    return cells['$cell|combined|$size|$combo|default'] ??
        cells['$cell|appearance|$combo|default'] ??
        cells['$cell|size|$size'] ??
        cells['$cell|base'];
  }

  static Color color(
    SolarTheme t,
    String cell,
    SolarTimeSlotProps p,
    Set<WidgetState> s,
  ) {
    final c = t.colors;
    return switch (lookup(cell, p, s)) {
      'none' => Colors.transparent,
      't:color.border.feedback.focus.strong' => c.borderFeedbackFocusStrong,
      't:color.border.subtle' => c.borderSubtle,
      't:color.surface.base' => c.surfaceBase,
      't:color.surface.feedback.info.subtle' => c.surfaceFeedbackInfoSubtle,
      't:color.surface.hover' => c.surfaceHover,
      final v => throw StateError('$cell: no colour for $v'),
    };
  }

  static List<BoxShadow> shadow(
    SolarTheme t,
    String cell,
    SolarTimeSlotProps p,
    Set<WidgetState> s,
  ) => switch (lookup(cell, p, s)) {
    'none' || null => const <BoxShadow>[],

    final v => throw StateError('$cell: no shadow for $v'),
  };

  /// A length in logical pixels, or null where the IR says the layer hugs its content.
  static double? dimension(
    String cell,
    SolarTimeSlotProps p,
    Set<WidgetState> s,
  ) {
    final v = lookup(cell, p, s);
    if (v == null || v == 'k:HUG') return null;
    if (v == 'k:FILL') return double.infinity;
    if (v.startsWith('px:')) return double.parse(v.substring(3));
    return switch (v) {
      't:border.default' => SolarBorder.$default,
      _ => throw StateError('$cell: no length for $v'),
    };
  }

  static TextStyle? textStyle(
    SolarTheme t,
    String cell,
    SolarTimeSlotProps p,
    Set<WidgetState> s,
  ) => switch (lookup(cell, p, s)) {
    'none' || null => null,

    final v => throw StateError('$cell: no text style for $v'),
  };

  /// Whether a layer is drawn: the shell reads this, the style does not.
  static bool present(String layer, SolarTimeSlotProps p, Set<WidgetState> s) =>
      lookup('$layer.present', p, s) != 'b:false';
}
