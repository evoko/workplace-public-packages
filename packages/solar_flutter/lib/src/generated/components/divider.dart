// SOLAR Divider recipe for Flutter. Generated by @bwp-web/codegen from spec/components/divider.json. Do not edit.
//
// The recipe is data: token *names*, keyed by layer and cell and by the base, size, appearance
// and combined sections of the IR. Values resolve against the ambient SolarTheme, so Light and
// Dark switch with the theme and no colour here is a literal.

import 'package:flutter/foundation.dart' show immutable;
import 'package:flutter/material.dart'
    show BoxShadow, Color, Colors, TextStyle, WidgetState;

import '../tokens.dart';

enum SolarDividerOrientation { horizontal, vertical }

enum SolarDividerType {
  full('full'),
  inset('inset'),
  withLabel('with-label');

  const SolarDividerType(this.figma);

  /// The value as Figma spells it, which the recipe is keyed by.
  final String figma;
}

/// The props a SOLAR Divider takes. Hover, pressed and focus are not here: they are
/// platform states, tracked by Flutter as [WidgetState]s.
@immutable
class SolarDividerProps {
  const SolarDividerProps({
    this.orientation = SolarDividerOrientation.horizontal,
    this.type = SolarDividerType.full,
  });

  final SolarDividerOrientation orientation;
  final SolarDividerType type;
}

abstract final class SolarDividerRecipe {
  /// Every IR entry, as `<layer>.<cell>|<section>…` to a token name (`t:`), a keyword (`k:`),
  /// `none`, a presence (`b:`) or a literal the overlay allowed (`px:`).
  static const Map<String, String> cells = {
    'root.present|base': 'b:true',
    'root.background|base': 'none',
    'root.borderColor|base': 'none',
    'root.shadow|base': 'none',
    'root.radius|base': 'none',
    'root.borderWidth|base': 'none',
    'root.direction|base': 'k:HORIZONTAL',
    'root.align|base': 'k:MIN/CENTER',
    'root.gap|base': 't:inset.none',
    'root.paddingTop|base': 't:inset.none',
    'root.paddingRight|base': 't:inset.none',
    'root.paddingBottom|base': 't:inset.none',
    'root.paddingLeft|base': 't:inset.none',
    'root.width|base': 'k:FILL',
    'root.height|base': 't:border.default',
    'root.direction|appearance|orientation=vertical, type=full|default':
        'k:VERTICAL',
    'root.width|appearance|orientation=vertical, type=full|default':
        't:border.default',
    'root.height|appearance|orientation=vertical, type=full|default': 'k:FILL',
    'root.direction|appearance|orientation=vertical, type=inset|default':
        'k:VERTICAL',
    'root.paddingLeft|appearance|orientation=vertical, type=inset|default':
        't:inset.md',
    'root.direction|appearance|orientation=vertical, type=with-label|default':
        'k:VERTICAL',
    'root.align|appearance|orientation=vertical, type=with-label|default':
        'k:CENTER/CENTER',
    'root.gap|appearance|orientation=vertical, type=with-label|default':
        't:stack.sm',
    'root.align|appearance|orientation=horizontal, type=with-label|default':
        'k:CENTER/CENTER',
    'root.gap|appearance|orientation=horizontal, type=with-label|default':
        't:stack.sm',
    'root.height|appearance|orientation=horizontal, type=with-label|default':
        'px:20',
    'root.paddingLeft|appearance|orientation=horizontal, type=inset|default':
        't:inset.md',
    'root.width|appearance|orientation=horizontal, type=inset|default':
        'k:FILL',
    'rule.present|base': 'b:true',
    'rule.background|base': 't:color.border.subtle',
    'rule.borderColor|base': 'none',
    'rule.shadow|base': 'none',
    'rule.radius|base': 'none',
    'rule.borderWidth|base': 'none',
    'rule.width|base': 'k:FILL',
    'rule.height|base': 't:border.default',
    'rule.width|appearance|orientation=vertical, type=full|default':
        't:border.default',
    'rule.height|appearance|orientation=vertical, type=full|default': 'k:FILL',
    'rule.width|appearance|orientation=vertical, type=inset|default':
        't:border.default',
    'rule.height|appearance|orientation=vertical, type=inset|default': 'k:FILL',
    'rule.width|appearance|orientation=vertical, type=with-label|default':
        't:border.default',
    'rule.height|appearance|orientation=vertical, type=with-label|default':
        'k:FILL',
    'label.present|base': 'b:false',
    'label.typography|base': 't:typography.label.sm',
    'label.present|appearance|orientation=horizontal, type=with-label|default':
        'b:true',
    'label.color|appearance|orientation=horizontal, type=with-label|default':
        't:color.text.tertiary',
    'rule2.present|base': 'b:false',
    'rule2.radius|base': 'none',
    'rule2.borderWidth|base': 'none',
    'rule2.width|base': 'k:FILL',
    'rule2.height|base': 't:border.default',
    'rule2.present|appearance|orientation=horizontal, type=with-label|default':
        'b:true',
    'rule2.background|appearance|orientation=horizontal, type=with-label|default':
        't:color.border.subtle',
    'rule2.borderColor|appearance|orientation=horizontal, type=with-label|default':
        'none',
    'rule2.shadow|appearance|orientation=horizontal, type=with-label|default':
        'none',
  };

  /// Which state wins when several hold, highest first: the MUI recipe's cascade, read backwards.
  static const List<String> statePrecedence = <String>[];

  static bool _holds(String state, SolarDividerProps p, Set<WidgetState> s) =>
      switch (state) {
        _ => false,
      };

  /// The entry for one cell under these props and states, by the IR's precedence: a state that
  /// holds beats the resting value, the per-size-and-appearance entry beats the per-appearance
  /// one, and the resting value falls back through appearance, size and base.
  static String? lookup(String cell, SolarDividerProps p, Set<WidgetState> s) {
    final combo = 'orientation=${p.orientation.name}, type=${p.type.figma}';
    const size = '';
    for (final state in statePrecedence) {
      if (!_holds(state, p, s)) continue;
      final hit =
          cells['$cell|combined|$size|$combo|$state'] ??
          cells['$cell|appearance|$combo|$state'];
      if (hit != null) return hit;
    }
    return cells['$cell|combined|$size|$combo|default'] ??
        cells['$cell|appearance|$combo|default'] ??
        cells['$cell|size|$size'] ??
        cells['$cell|base'];
  }

  static Color color(
    SolarTheme t,
    String cell,
    SolarDividerProps p,
    Set<WidgetState> s,
  ) {
    final c = t.colors;
    return switch (lookup(cell, p, s)) {
      'none' => Colors.transparent,
      't:color.border.subtle' => c.borderSubtle,
      't:color.text.tertiary' => c.textTertiary,
      final v => throw StateError('$cell: no colour for $v'),
    };
  }

  static List<BoxShadow> shadow(
    SolarTheme t,
    String cell,
    SolarDividerProps p,
    Set<WidgetState> s,
  ) => switch (lookup(cell, p, s)) {
    'none' || null => const <BoxShadow>[],

    final v => throw StateError('$cell: no shadow for $v'),
  };

  /// A length in logical pixels, or null where the IR says the layer hugs its content.
  static double? dimension(
    String cell,
    SolarDividerProps p,
    Set<WidgetState> s,
  ) {
    final v = lookup(cell, p, s);
    if (v == null || v == 'k:HUG') return null;
    if (v == 'k:FILL') return double.infinity;
    if (v.startsWith('px:')) return double.parse(v.substring(3));
    return switch (v) {
      't:border.default' => SolarBorder.$default,
      't:inset.md' => SolarInset.md,
      't:inset.none' => SolarInset.none,
      't:stack.sm' => SolarStack.sm,
      _ => throw StateError('$cell: no length for $v'),
    };
  }

  static TextStyle? textStyle(
    SolarTheme t,
    String cell,
    SolarDividerProps p,
    Set<WidgetState> s,
  ) => switch (lookup(cell, p, s)) {
    'none' || null => null,
    't:typography.label.sm' => t.typography.labelSm,
    final v => throw StateError('$cell: no text style for $v'),
  };

  /// Whether a layer is drawn: the shell reads this, the style does not.
  static bool present(String layer, SolarDividerProps p, Set<WidgetState> s) =>
      lookup('$layer.present', p, s) != 'b:false';
}
