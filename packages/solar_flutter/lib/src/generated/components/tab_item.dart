// SOLAR Tab Item recipe for Flutter. Generated by @bwp-web/codegen from spec/components/tab item.json. Do not edit.
//
// The recipe is data: token *names*, keyed by layer and cell and by the base, size, appearance
// and combined sections of the IR. Values resolve against the ambient SolarTheme, so Light and
// Dark switch with the theme and no colour here is a literal.

import 'package:flutter/foundation.dart' show immutable;
import 'package:flutter/material.dart'
    show BoxShadow, Color, Colors, TextStyle, WidgetState;

import '../tokens.dart';

enum SolarTabItemSize { sm, md }

/// The props a SOLAR Tab Item takes. Hover, pressed and focus are not here: they are
/// platform states, tracked by Flutter as [WidgetState]s.
@immutable
class SolarTabItemProps {
  const SolarTabItemProps({
    this.size = SolarTabItemSize.md,
    this.disabled = false,
    this.selected = false,
  });

  final SolarTabItemSize size;
  final bool disabled;
  final bool selected;
}

abstract final class SolarTabItemRecipe {
  /// Every IR entry, as `<layer>.<cell>|<section>…` to a token name (`t:`), a keyword (`k:`),
  /// `none`, a presence (`b:`) or a literal the overlay allowed (`px:`).
  static const Map<String, String> cells = {
    'root.present|base': 'b:true',
    'root.background|base': 'none',
    'root.borderColor|base': 'none',
    'root.shadow|base': 'none',
    'root.radius|base': 'none',
    'root.direction|base': 'k:HORIZONTAL',
    'root.align|base': 'k:CENTER/CENTER',
    'root.gap|base': 't:stack.xs',
    'root.paddingTop|base': 't:inset.sm',
    'root.paddingRight|base': 't:inset.md',
    'root.paddingBottom|base': 't:inset.sm',
    'root.paddingLeft|base': 't:inset.md',
    'root.width|base': 'k:HUG',
    'root.height|base': 'px:40',
    'root.borderTopWidth|base': 'none',
    'root.borderRightWidth|base': 'none',
    'root.borderBottomWidth|base': 'none',
    'root.borderLeftWidth|base': 'none',
    'root.paddingTop|size|sm': 't:inset.xs',
    'root.paddingRight|size|sm': 't:inset.sm',
    'root.paddingBottom|size|sm': 't:inset.xs',
    'root.paddingLeft|size|sm': 't:inset.sm',
    'root.height|size|sm': 'px:32',
    'root.borderColor|appearance|default|selected': 't:color.border.strong',
    'root.borderBottomWidth|appearance|default|selected': 't:border.strong',
    'root.borderColor|appearance|default|focus': 't:color.border.strong',
    'root.shadow|appearance|default|focus': 't:shadow.focus.default',
    'root.borderBottomWidth|appearance|default|focus': 't:border.strong',
    'leadingIcon.present|base': 'b:true',
    'leadingIcon.component|base': 'k:Icon/None',
    'leadingIcon.variant.solid|base': 'k:false',
    'leadingIcon.width|base': 't:icon.sm',
    'leadingIcon.height|base': 't:icon.sm',
    'leadingIcon.color|base': 't:color.icon.secondary',
    'leadingIcon.width|size|sm': 't:icon.xs',
    'leadingIcon.height|size|sm': 't:icon.xs',
    'leadingIcon.color|appearance|default|hover': 't:color.icon.primary',
    'leadingIcon.color|appearance|default|selected': 't:color.icon.primary',
    'leadingIcon.color|appearance|default|focus': 't:color.icon.primary',
    'leadingIcon.color|appearance|default|disabled': 't:color.icon.disabled',
    'label.present|base': 'b:true',
    'label.color|base': 't:color.text.secondary',
    'label.typography|base': 't:typography.body.md.medium',
    'label.typography|size|sm': 't:typography.body.sm.medium',
    'label.color|appearance|default|hover': 't:color.text.primary',
    'label.color|appearance|default|selected': 't:color.text.primary',
    'label.color|appearance|default|focus': 't:color.text.primary',
    'label.color|appearance|default|disabled': 't:color.text.disabled',
    'trailingIcon.present|base': 'b:true',
    'trailingIcon.component|base': 'k:Icon/None',
    'trailingIcon.variant.solid|base': 'k:false',
    'trailingIcon.width|base': 't:icon.sm',
    'trailingIcon.height|base': 't:icon.sm',
    'trailingIcon.color|base': 't:color.icon.secondary',
    'trailingIcon.width|size|sm': 't:icon.xs',
    'trailingIcon.height|size|sm': 't:icon.xs',
    'trailingIcon.color|appearance|default|hover': 't:color.icon.primary',
    'trailingIcon.color|appearance|default|selected': 't:color.icon.primary',
    'trailingIcon.color|appearance|default|focus': 't:color.icon.primary',
    'trailingIcon.color|appearance|default|disabled': 't:color.icon.disabled',
    'counter.present|base': 'b:true',
    'counter.component|base': 'k:Counter',
    'counter.variant.type|base': 'k:idle',
    'counter.variant.state|base': 'k:default',
    'counter.width|base': 'k:HUG',
    'counter.height|base': 'none',
    'counter.variant.type|appearance|default|selected': 'k:regular',
    'counter.variant.type|appearance|default|focus': 'k:regular',
    'counter.variant.type|appearance|default|disabled': 'k:regular',
  };

  /// Each layer's children, in Figma's order: the tree the widget draws (SolarLayers).
  static const Map<String, List<String>> tree = {
    'root': ['leadingIcon', 'label', 'trailingIcon', 'counter'],
  };

  /// Which state wins when several hold, highest first: the MUI recipe's cascade, read backwards.
  static const List<String> statePrecedence = [
    'disabled',
    'selected',
    'focus',
    'hover',
  ];

  static bool _holds(String state, SolarTabItemProps p, Set<WidgetState> s) =>
      switch (state) {
        'disabled' => p.disabled || s.contains(WidgetState.disabled),
        'selected' => p.selected,
        'focus' => s.contains(WidgetState.focused),
        'hover' => s.contains(WidgetState.hovered),
        _ => false,
      };

  /// The entry for one cell under these props and states, by the IR's precedence: a state that
  /// holds beats the resting value, the per-size-and-appearance entry beats the per-appearance
  /// one, and the resting value falls back through appearance, size and base.
  static String? lookup(String cell, SolarTabItemProps p, Set<WidgetState> s) {
    const combo = 'default';
    final size = p.size.name;
    for (final state in statePrecedence) {
      if (!_holds(state, p, s)) continue;
      final hit =
          cells['$cell|combined|$size|$combo|$state'] ??
          cells['$cell|appearance|$combo|$state'];
      if (hit != null) return hit;
    }
    return cells['$cell|combined|$size|$combo|default'] ??
        cells['$cell|appearance|$combo|default'] ??
        cells['$cell|size|$size'] ??
        cells['$cell|base'];
  }

  static Color color(
    SolarTheme t,
    String cell,
    SolarTabItemProps p,
    Set<WidgetState> s,
  ) {
    final c = t.colors;
    return switch (lookup(cell, p, s)) {
      'none' => Colors.transparent,
      't:color.border.strong' => c.borderStrong,
      't:color.icon.disabled' => c.iconDisabled,
      't:color.icon.primary' => c.iconPrimary,
      't:color.icon.secondary' => c.iconSecondary,
      't:color.text.disabled' => c.textDisabled,
      't:color.text.primary' => c.textPrimary,
      't:color.text.secondary' => c.textSecondary,
      final v => throw StateError('$cell: no colour for $v'),
    };
  }

  static List<BoxShadow> shadow(
    SolarTheme t,
    String cell,
    SolarTabItemProps p,
    Set<WidgetState> s,
  ) => switch (lookup(cell, p, s)) {
    'none' || null => const <BoxShadow>[],
    't:shadow.focus.default' => t.shadows.focusDefault,
    final v => throw StateError('$cell: no shadow for $v'),
  };

  /// A length in logical pixels, or null where the IR says the layer hugs its content.
  static double? dimension(
    String cell,
    SolarTabItemProps p,
    Set<WidgetState> s,
  ) {
    final v = lookup(cell, p, s);
    if (v == null || v == 'k:HUG') return null;
    if (v == 'k:FILL') return double.infinity;
    if (v.startsWith('px:')) return double.parse(v.substring(3));
    return switch (v) {
      't:border.strong' => SolarBorder.strong,
      't:icon.sm' => SolarIconSize.sm,
      't:icon.xs' => SolarIconSize.xs,
      't:inset.md' => SolarInset.md,
      't:inset.sm' => SolarInset.sm,
      't:inset.xs' => SolarInset.xs,
      't:stack.xs' => SolarStack.xs,
      _ => throw StateError('$cell: no length for $v'),
    };
  }

  static TextStyle? textStyle(
    SolarTheme t,
    String cell,
    SolarTabItemProps p,
    Set<WidgetState> s,
  ) => switch (lookup(cell, p, s)) {
    'none' || null => null,
    't:typography.body.md.medium' => t.typography.bodyMdMedium,
    't:typography.body.sm.medium' => t.typography.bodySmMedium,
    final v => throw StateError('$cell: no text style for $v'),
  };

  /// Whether a layer is drawn: the shell reads this, the style does not.
  static bool present(String layer, SolarTabItemProps p, Set<WidgetState> s) =>
      lookup('$layer.present', p, s) != 'b:false';
}
