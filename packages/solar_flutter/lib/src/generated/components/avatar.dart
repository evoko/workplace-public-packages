// SOLAR Avatar recipe for Flutter. Generated by @bwp-web/codegen from spec/components/avatar.json. Do not edit.
//
// The recipe is data: token *names*, keyed by layer and cell and by the base, size, appearance
// and combined sections of the IR. Values resolve against the ambient SolarTheme, so Light and
// Dark switch with the theme and no colour here is a literal.

import 'package:flutter/foundation.dart' show immutable;
import 'package:flutter/material.dart'
    show BoxShadow, Color, Colors, TextStyle, WidgetState;

import '../tokens.dart';

enum SolarAvatarSize { lg, md, sm, xs }

enum SolarAvatarType { text, photo, logo }

/// The props a SOLAR Avatar takes. Hover, pressed and focus are not here: they are
/// platform states, tracked by Flutter as [WidgetState]s.
@immutable
class SolarAvatarProps {
  const SolarAvatarProps({
    this.size = SolarAvatarSize.lg,
    this.type = SolarAvatarType.text,
    this.color,
  });

  final SolarAvatarSize size;
  final SolarAvatarType type;
  final Color? color;
}

abstract final class SolarAvatarRecipe {
  /// Every IR entry, as `<layer>.<cell>|<section>…` to a token name (`t:`), a keyword (`k:`),
  /// `none`, a presence (`b:`) or a literal the overlay allowed (`px:`).
  static const Map<String, String> cells = {
    'root.present|base': 'b:true',
    'root.background|base': 't:color.surface.feedback.neutral.subtle',
    'root.borderColor|base': 't:color.border.subtle',
    'root.shadow|base': 'none',
    'root.radius|base': 't:radius.pill',
    'root.borderWidth|base': 't:border.default',
    'root.direction|base': 'k:VERTICAL',
    'root.align|base': 'k:CENTER/CENTER',
    'root.gap|base': 't:stack.none',
    'root.paddingTop|base': 't:inset.none',
    'root.paddingRight|base': 't:inset.none',
    'root.paddingBottom|base': 't:inset.none',
    'root.paddingLeft|base': 't:inset.none',
    'root.width|base': 'px:44',
    'root.height|base': 'px:44',
    'root.width|size|md': 'px:32',
    'root.height|size|md': 'px:32',
    'root.width|size|sm': 'px:24',
    'root.height|size|sm': 'px:24',
    'root.width|size|xs': 'px:16',
    'root.height|size|xs': 'px:16',
    'root.background|appearance|type=photo|default': 'none',
    'root.borderColor|appearance|type=photo|default': 't:color.border.medium',
    'root.direction|appearance|type=photo|default': 'k:HORIZONTAL',
    'root.image|appearance|type=photo|default': 'b:true',
    'root.background|appearance|type=logo|default': 'none',
    'root.borderColor|appearance|type=logo|default': 'none',
    'root.radius|appearance|type=logo|default': 't:radius.container',
    'root.borderWidth|appearance|type=logo|default': 'none',
    'root.direction|appearance|type=logo|default': 'k:HORIZONTAL',
    'root.image|appearance|type=logo|default': 'b:true',
    'initials.present|base': 'b:true',
    'initials.color|base': 't:color.text.primary',
    'initials.typography|base': 't:typography.body.lg.medium',
    'initials.typography|size|md': 't:typography.body.sm.semibold',
    'initials.typography|size|sm': 't:typography.body.xs.semibold',
    'initials.typography|size|xs': 't:typography.body.2xs.semibold',
    'initials.present|appearance|type=photo|default': 'b:false',
    'initials.present|appearance|type=logo|default': 'b:false',
  };

  /// Each layer's children, in Figma's order: the tree the widget draws (SolarLayers).
  static const Map<String, List<String>> tree = {
    'root': ['initials'],
  };

  /// Which state wins when several hold, highest first: the MUI recipe's cascade, read backwards.
  static const List<String> statePrecedence = <String>[];

  static bool _holds(String state, SolarAvatarProps p, Set<WidgetState> s) =>
      switch (state) {
        _ => false,
      };

  /// The entry for one cell under these props and states, by the IR's precedence: a state that
  /// holds beats the resting value, the per-size-and-appearance entry beats the per-appearance
  /// one, and the resting value falls back through appearance, size and base.
  static String? lookup(String cell, SolarAvatarProps p, Set<WidgetState> s) {
    final combo = 'type=${p.type.name}';
    final size = p.size.name;
    for (final state in statePrecedence) {
      if (!_holds(state, p, s)) continue;
      final hit =
          cells['$cell|combined|$size|$combo|$state'] ??
          cells['$cell|appearance|$combo|$state'];
      if (hit != null) return hit;
    }
    return cells['$cell|combined|$size|$combo|default'] ??
        cells['$cell|appearance|$combo|default'] ??
        cells['$cell|size|$size'] ??
        cells['$cell|base'];
  }

  static Color color(
    SolarTheme t,
    String cell,
    SolarAvatarProps p,
    Set<WidgetState> s,
  ) {
    final c = t.colors;
    return switch (lookup(cell, p, s)) {
      'none' => Colors.transparent,
      't:color.border.medium' => c.borderMedium,
      't:color.border.subtle' => c.borderSubtle,
      't:color.surface.feedback.neutral.subtle' =>
        c.surfaceFeedbackNeutralSubtle,
      't:color.text.primary' => c.textPrimary,
      final v => throw StateError('$cell: no colour for $v'),
    };
  }

  static List<BoxShadow> shadow(
    SolarTheme t,
    String cell,
    SolarAvatarProps p,
    Set<WidgetState> s,
  ) => switch (lookup(cell, p, s)) {
    'none' || null => const <BoxShadow>[],

    final v => throw StateError('$cell: no shadow for $v'),
  };

  /// A length in logical pixels, or null where the IR says the layer hugs its content.
  static double? dimension(
    String cell,
    SolarAvatarProps p,
    Set<WidgetState> s,
  ) {
    final v = lookup(cell, p, s);
    if (v == null || v == 'k:HUG') return null;
    if (v == 'k:FILL') return double.infinity;
    if (v.startsWith('px:')) return double.parse(v.substring(3));
    return switch (v) {
      't:border.default' => SolarBorder.$default,
      't:inset.none' => SolarInset.none,
      't:radius.container' => SolarRadius.container,
      't:radius.pill' => SolarRadius.pill,
      't:stack.none' => SolarStack.none,
      _ => throw StateError('$cell: no length for $v'),
    };
  }

  static TextStyle? textStyle(
    SolarTheme t,
    String cell,
    SolarAvatarProps p,
    Set<WidgetState> s,
  ) => switch (lookup(cell, p, s)) {
    'none' || null => null,
    't:typography.body.2xs.semibold' => t.typography.body2xsSemibold,
    't:typography.body.lg.medium' => t.typography.bodyLgMedium,
    't:typography.body.sm.semibold' => t.typography.bodySmSemibold,
    't:typography.body.xs.semibold' => t.typography.bodyXsSemibold,
    final v => throw StateError('$cell: no text style for $v'),
  };

  /// Whether a layer is drawn: the shell reads this, the style does not.
  static bool present(String layer, SolarAvatarProps p, Set<WidgetState> s) =>
      lookup('$layer.present', p, s) != 'b:false';
}
