// SOLAR Split Dropdown recipe for Flutter. Generated by @bwp-web/codegen from spec/components/split dropdown.json. Do not edit.
//
// The recipe is data: token *names*, keyed by layer and cell and by the base, size, appearance
// and combined sections of the IR. Values resolve against the ambient SolarTheme, so Light and
// Dark switch with the theme and no colour here is a literal.

import 'package:flutter/foundation.dart' show immutable;
import 'package:flutter/material.dart'
    show BoxShadow, Color, Colors, TextStyle, WidgetState;

import '../tokens.dart';

/// The props a SOLAR Split Dropdown takes. Hover, pressed and focus are not here: they are
/// platform states, tracked by Flutter as [WidgetState]s.
@immutable
class SolarSplitDropdownProps {
  const SolarSplitDropdownProps();
}

abstract final class SolarSplitDropdownRecipe {
  /// Every IR entry, as `<layer>.<cell>|<section>…` to a token name (`t:`), a keyword (`k:`),
  /// `none`, a presence (`b:`) or a literal the overlay allowed (`px:`).
  static const Map<String, String> cells = {
    'root.present|base': 'b:true',
    'root.background|base': 'none',
    'root.borderColor|base': 't:color.border.subtle',
    'root.shadow|base': 'none',
    'root.radius|base': 't:radius.container',
    'root.borderWidth|base': 't:border.default',
    'root.direction|base': 'k:VERTICAL',
    'root.align|base': 'k:MIN/MIN',
    'root.gap|base': 't:stack.none',
    'root.paddingTop|base': 't:inset.none',
    'root.paddingRight|base': 't:inset.none',
    'root.paddingBottom|base': 't:inset.none',
    'root.paddingLeft|base': 't:inset.none',
    'root.width|base': 'k:FILL',
    'root.height|base': 'k:HUG',
    'top.present|base': 'b:true',
    'top.background|base': 't:color.surface.raised',
    'top.borderColor|base': 'none',
    'top.shadow|base': 'none',
    'top.radius|base': 'none',
    'top.borderWidth|base': 'none',
    'top.direction|base': 'k:VERTICAL',
    'top.align|base': 'k:MIN/MIN',
    'top.gap|base': 't:inset.xs',
    'top.paddingTop|base': 't:inset.md',
    'top.paddingRight|base': 't:inset.sm',
    'top.paddingBottom|base': 't:inset.md',
    'top.paddingLeft|base': 't:inset.sm',
    'top.width|base': 'k:FILL',
    'top.height|base': 'k:HUG',
    'topContent.present|base': 'b:true',
    'topContent.background|base': 'none',
    'topContent.borderColor|base': 'none',
    'topContent.shadow|base': 'none',
    'topContent.radius|base': 'none',
    'topContent.borderWidth|base': 'none',
    'topContent.direction|base': 'k:HORIZONTAL',
    'topContent.align|base': 'k:CENTER/CENTER',
    'topContent.gap|base': 't:inset.none',
    'topContent.paddingTop|base': 't:inset.none',
    'topContent.paddingRight|base': 't:inset.none',
    'topContent.paddingBottom|base': 't:inset.none',
    'topContent.paddingLeft|base': 't:inset.none',
    'topContent.width|base': 'k:FILL',
    'topContent.height|base': 'k:HUG',
    'lower.present|base': 'b:true',
    'lower.background|base': 't:color.surface.background',
    'lower.borderColor|base': 'none',
    'lower.shadow|base': 'none',
    'lower.radius|base': 'none',
    'lower.borderWidth|base': 'none',
    'lower.direction|base': 'k:VERTICAL',
    'lower.align|base': 'k:MIN/MIN',
    'lower.gap|base': 't:inset.xs',
    'lower.paddingTop|base': 't:inset.sm',
    'lower.paddingRight|base': 't:inset.sm',
    'lower.paddingBottom|base': 't:inset.sm',
    'lower.paddingLeft|base': 't:inset.sm',
    'lower.width|base': 'k:FILL',
    'lower.height|base': 'k:HUG',
    'lowerContent.present|base': 'b:true',
    'lowerContent.background|base': 'none',
    'lowerContent.borderColor|base': 'none',
    'lowerContent.shadow|base': 'none',
    'lowerContent.radius|base': 'none',
    'lowerContent.borderWidth|base': 'none',
    'lowerContent.direction|base': 'k:HORIZONTAL',
    'lowerContent.align|base': 'k:CENTER/CENTER',
    'lowerContent.gap|base': 't:inset.none',
    'lowerContent.paddingTop|base': 't:inset.none',
    'lowerContent.paddingRight|base': 't:inset.none',
    'lowerContent.paddingBottom|base': 't:inset.none',
    'lowerContent.paddingLeft|base': 't:inset.none',
    'lowerContent.width|base': 'k:FILL',
    'lowerContent.height|base': 'k:HUG',
  };

  /// Each layer's children, in Figma's order: the tree the widget draws (SolarLayers).
  static const Map<String, List<String>> tree = {
    'root': ['top', 'lower'],
    'top': ['topContent'],
    'lower': ['lowerContent'],
  };

  /// Which state wins when several hold, highest first: the MUI recipe's cascade, read backwards.
  static const List<String> statePrecedence = <String>[];

  static bool _holds(
    String state,
    SolarSplitDropdownProps p,
    Set<WidgetState> s,
  ) => switch (state) {
    _ => false,
  };

  /// The entry for one cell under these props and states, by the IR's precedence: a state that
  /// holds beats the resting value, the per-size-and-appearance entry beats the per-appearance
  /// one, and the resting value falls back through appearance, size and base.
  static String? lookup(
    String cell,
    SolarSplitDropdownProps p,
    Set<WidgetState> s,
  ) {
    const combo = 'default';
    const size = '';
    for (final state in statePrecedence) {
      if (!_holds(state, p, s)) continue;
      final hit =
          cells['$cell|combined|$size|$combo|$state'] ??
          cells['$cell|appearance|$combo|$state'];
      if (hit != null) return hit;
    }
    return cells['$cell|combined|$size|$combo|default'] ??
        cells['$cell|appearance|$combo|default'] ??
        cells['$cell|size|$size'] ??
        cells['$cell|base'];
  }

  static Color color(
    SolarTheme t,
    String cell,
    SolarSplitDropdownProps p,
    Set<WidgetState> s,
  ) {
    final c = t.colors;
    return switch (lookup(cell, p, s)) {
      'none' => Colors.transparent,
      't:color.border.subtle' => c.borderSubtle,
      't:color.surface.background' => c.surfaceBackground,
      't:color.surface.raised' => c.surfaceRaised,
      final v => throw StateError('$cell: no colour for $v'),
    };
  }

  static List<BoxShadow> shadow(
    SolarTheme t,
    String cell,
    SolarSplitDropdownProps p,
    Set<WidgetState> s,
  ) => switch (lookup(cell, p, s)) {
    'none' || null => const <BoxShadow>[],

    final v => throw StateError('$cell: no shadow for $v'),
  };

  /// A length in logical pixels, or null where the IR says the layer hugs its content.
  static double? dimension(
    String cell,
    SolarSplitDropdownProps p,
    Set<WidgetState> s,
  ) {
    final v = lookup(cell, p, s);
    if (v == null || v == 'k:HUG') return null;
    if (v == 'k:FILL') return double.infinity;
    if (v.startsWith('px:')) return double.parse(v.substring(3));
    return switch (v) {
      't:border.default' => SolarBorder.$default,
      't:inset.md' => SolarInset.md,
      't:inset.none' => SolarInset.none,
      't:inset.sm' => SolarInset.sm,
      't:inset.xs' => SolarInset.xs,
      't:radius.container' => SolarRadius.container,
      't:stack.none' => SolarStack.none,
      _ => throw StateError('$cell: no length for $v'),
    };
  }

  static TextStyle? textStyle(
    SolarTheme t,
    String cell,
    SolarSplitDropdownProps p,
    Set<WidgetState> s,
  ) => switch (lookup(cell, p, s)) {
    'none' || null => null,

    final v => throw StateError('$cell: no text style for $v'),
  };

  /// Whether a layer is drawn: the shell reads this, the style does not.
  static bool present(
    String layer,
    SolarSplitDropdownProps p,
    Set<WidgetState> s,
  ) => lookup('$layer.present', p, s) != 'b:false';
}
