// SOLAR Chart Tooltip recipe for Flutter. Generated by @bwp-web/codegen from spec/components/chart tooltip.json. Do not edit.
//
// The recipe is data: token *names*, keyed by layer and cell and by the base, size, appearance
// and combined sections of the IR. Values resolve against the ambient SolarTheme, so Light and
// Dark switch with the theme and no colour here is a literal.

import 'package:flutter/foundation.dart' show immutable;
import 'package:flutter/material.dart'
    show BoxShadow, Color, Colors, TextStyle, WidgetState;

import '../tokens.dart';

enum SolarChartTooltipSeries { single, multi }

/// The props a SOLAR Chart Tooltip takes. Hover, pressed and focus are not here: they are
/// platform states, tracked by Flutter as [WidgetState]s.
@immutable
class SolarChartTooltipProps {
  const SolarChartTooltipProps({this.series = SolarChartTooltipSeries.single});

  final SolarChartTooltipSeries series;
}

abstract final class SolarChartTooltipRecipe {
  /// Every IR entry, as `<layer>.<cell>|<section>…` to a token name (`t:`), a keyword (`k:`),
  /// `none`, a presence (`b:`) or a literal the overlay allowed (`px:`).
  static const Map<String, String> cells = {
    'root.present|base': 'b:true',
    'root.background|base': 't:color.surface.raised',
    'root.borderColor|base': 't:color.border.subtle',
    'root.shadow|base': 't:shadow.overlay',
    'root.radius|base': 't:radius.control',
    'root.borderWidth|base': 't:border.default',
    'root.direction|base': 'k:VERTICAL',
    'root.align|base': 'k:MIN/MIN',
    'root.gap|base': 't:stack.xs',
    'root.paddingTop|base': 't:inset.sm',
    'root.paddingRight|base': 't:inset.sm',
    'root.paddingBottom|base': 't:inset.sm',
    'root.paddingLeft|base': 't:inset.sm',
    'root.width|base': 'k:HUG',
    'root.height|base': 'k:HUG',
    'title.present|base': 'b:true',
    'title.color|base': 't:color.text.primary',
    'title.typography|base': 't:typography.body.sm.semibold',
    'frame.present|base': 'b:true',
    'frame.background|base': 'none',
    'frame.borderColor|base': 'none',
    'frame.shadow|base': 'none',
    'frame.radius|base': 'none',
    'frame.borderWidth|base': 'none',
    'frame.direction|base': 'k:HORIZONTAL',
    'frame.align|base': 'k:MIN/CENTER',
    'frame.gap|base': 't:inset.2xs',
    'frame.paddingTop|base': 't:inset.none',
    'frame.paddingRight|base': 't:inset.none',
    'frame.paddingBottom|base': 't:inset.none',
    'frame.paddingLeft|base': 't:inset.none',
    'frame.width|base': 'k:HUG',
    'frame.height|base': 'k:HUG',
    'frame.align|appearance|series=multi|default': 'k:SPACE_BETWEEN/CENTER',
    'frame.gap|appearance|series=multi|default': 't:inset.md',
    'frame.width|appearance|series=multi|default': 'px:132',
    'swatch.present|base': 'b:true',
    'swatch.component|base': 'k:StatusIndicator',
    'swatch.variant.type|base': 'k:success',
    'swatch.variant.size|base': 'k:xs',
    'swatch.width|base': 'none',
    'swatch.height|base': 'none',
    'swatch.present|appearance|series=multi|default': 'b:false',
    'series.present|base': 'b:false',
    'series.radius|base': 'none',
    'series.borderWidth|base': 'none',
    'series.direction|base': 'k:HORIZONTAL',
    'series.align|base': 'k:MIN/CENTER',
    'series.gap|base': 't:inset.2xs',
    'series.paddingTop|base': 't:inset.none',
    'series.paddingRight|base': 't:inset.none',
    'series.paddingBottom|base': 't:inset.none',
    'series.paddingLeft|base': 't:inset.none',
    'series.width|base': 'k:HUG',
    'series.height|base': 'k:HUG',
    'series.present|appearance|series=multi|default': 'b:true',
    'series.background|appearance|series=multi|default': 'none',
    'series.borderColor|appearance|series=multi|default': 'none',
    'series.shadow|appearance|series=multi|default': 'none',
    'frameValue.present|base': 'b:true',
    'frameValue.color|base': 't:color.text.primary',
    'frameValue.typography|base': 't:typography.body.sm.medium',
    'rowSwatch.present|base': 'b:false',
    'rowSwatch.component|base': 'k:StatusIndicator',
    'rowSwatch.variant.size|base': 'k:xs',
    'rowSwatch.width|base': 'none',
    'rowSwatch.height|base': 'none',
    'rowSwatch.present|appearance|series=multi|default': 'b:true',
    'rowSwatch.variant.type|appearance|series=multi|default': 'k:success',
    'frameFrameLabel.present|base': 'b:false',
    'frameFrameLabel.typography|base': 't:typography.label.sm',
    'frameFrameLabel.present|appearance|series=multi|default': 'b:true',
    'frameFrameLabel.color|appearance|series=multi|default':
        't:color.text.secondary',
  };

  /// Each layer's children, in Figma's order: the tree the widget draws (SolarLayers).
  static const Map<String, List<String>> tree = {
    'root': ['title', 'frame'],
    'frame': ['swatch', 'series', 'frameValue'],
    'series': ['rowSwatch', 'frameFrameLabel'],
  };

  /// Which state wins when several hold, highest first: the MUI recipe's cascade, read backwards.
  static const List<String> statePrecedence = <String>[];

  static bool _holds(
    String state,
    SolarChartTooltipProps p,
    Set<WidgetState> s,
  ) => switch (state) {
    _ => false,
  };

  /// The entry for one cell under these props and states, by the IR's precedence: a state that
  /// holds beats the resting value, the per-size-and-appearance entry beats the per-appearance
  /// one, and the resting value falls back through appearance, size and base.
  static String? lookup(
    String cell,
    SolarChartTooltipProps p,
    Set<WidgetState> s,
  ) {
    final combo = 'series=${p.series.name}';
    const size = '';
    for (final state in statePrecedence) {
      if (!_holds(state, p, s)) continue;
      final hit =
          cells['$cell|combined|$size|$combo|$state'] ??
          cells['$cell|appearance|$combo|$state'];
      if (hit != null) return hit;
    }
    return cells['$cell|combined|$size|$combo|default'] ??
        cells['$cell|appearance|$combo|default'] ??
        cells['$cell|size|$size'] ??
        cells['$cell|base'];
  }

  static Color color(
    SolarTheme t,
    String cell,
    SolarChartTooltipProps p,
    Set<WidgetState> s,
  ) {
    final c = t.colors;
    return switch (lookup(cell, p, s)) {
      'none' => Colors.transparent,
      't:color.border.subtle' => c.borderSubtle,
      't:color.surface.raised' => c.surfaceRaised,
      't:color.text.primary' => c.textPrimary,
      't:color.text.secondary' => c.textSecondary,
      final v => throw StateError('$cell: no colour for $v'),
    };
  }

  static List<BoxShadow> shadow(
    SolarTheme t,
    String cell,
    SolarChartTooltipProps p,
    Set<WidgetState> s,
  ) => switch (lookup(cell, p, s)) {
    'none' || null => const <BoxShadow>[],
    't:shadow.overlay' => t.shadows.overlay,
    final v => throw StateError('$cell: no shadow for $v'),
  };

  /// A length in logical pixels, or null where the IR says the layer hugs its content.
  static double? dimension(
    String cell,
    SolarChartTooltipProps p,
    Set<WidgetState> s,
  ) {
    final v = lookup(cell, p, s);
    if (v == null || v == 'k:HUG') return null;
    if (v == 'k:FILL') return double.infinity;
    if (v.startsWith('px:')) return double.parse(v.substring(3));
    return switch (v) {
      't:border.default' => SolarBorder.$default,
      't:inset.2xs' => SolarInset.$2xs,
      't:inset.md' => SolarInset.md,
      't:inset.none' => SolarInset.none,
      't:inset.sm' => SolarInset.sm,
      't:radius.control' => SolarRadius.control,
      't:stack.xs' => SolarStack.xs,
      _ => throw StateError('$cell: no length for $v'),
    };
  }

  static TextStyle? textStyle(
    SolarTheme t,
    String cell,
    SolarChartTooltipProps p,
    Set<WidgetState> s,
  ) => switch (lookup(cell, p, s)) {
    'none' || null => null,
    't:typography.body.sm.medium' => t.typography.bodySmMedium,
    't:typography.body.sm.semibold' => t.typography.bodySmSemibold,
    't:typography.label.sm' => t.typography.labelSm,
    final v => throw StateError('$cell: no text style for $v'),
  };

  /// Whether a layer is drawn: the shell reads this, the style does not.
  static bool present(
    String layer,
    SolarChartTooltipProps p,
    Set<WidgetState> s,
  ) => lookup('$layer.present', p, s) != 'b:false';
}
