// SOLAR Event Row recipe for Flutter. Generated by @bwp-web/codegen from spec/components/event row.json. Do not edit.
//
// The recipe is data: token *names*, keyed by layer and cell and by the base, size, appearance
// and combined sections of the IR. Values resolve against the ambient SolarTheme, so Light and
// Dark switch with the theme and no colour here is a literal.

import 'package:flutter/foundation.dart' show immutable;
import 'package:flutter/material.dart'
    show BoxShadow, Color, Colors, TextStyle, WidgetState;

import '../tokens.dart';

/// The props a SOLAR Event Row takes. Hover, pressed and focus are not here: they are
/// platform states, tracked by Flutter as [WidgetState]s.
@immutable
class SolarEventRowProps {
  const SolarEventRowProps();
}

abstract final class SolarEventRowRecipe {
  /// Every IR entry, as `<layer>.<cell>|<section>…` to a token name (`t:`), a keyword (`k:`),
  /// `none`, a presence (`b:`) or a literal the overlay allowed (`px:`).
  static const Map<String, String> cells = {
    'root.present|base': 'b:true',
    'root.background|base': 't:color.surface.base',
    'root.borderColor|base': 't:color.border.subtle',
    'root.shadow|base': 'none',
    'root.radius|base': 't:radius.container',
    'root.borderWidth|base': 't:border.default',
    'root.direction|base': 'k:HORIZONTAL',
    'root.align|base': 'k:MIN/CENTER',
    'root.gap|base': 't:stack.sm',
    'root.paddingTop|base': 't:inset.sm',
    'root.paddingRight|base': 't:inset.sm',
    'root.paddingBottom|base': 't:inset.sm',
    'root.paddingLeft|base': 't:inset.sm',
    'root.width|base': 'k:FILL',
    'root.height|base': 'k:HUG',
    'root.background|appearance|default|hover': 't:color.surface.hover',
    'root.shadow|appearance|default|focus': 't:shadow.focus.default',
    'root.background|appearance|default|focus': 't:color.surface.base',
    'leading.present|base': 'b:true',
    'leading.component|base': 'k:Avatar',
    'leading.variant.size|base': 'k:md',
    'leading.variant.type|base': 'k:text',
    'leading.variant.color|base': 'k:neutral',
    'leading.variant.shade|base': 'k:Light',
    'leading.width|base': 'none',
    'leading.height|base': 'none',
    'content.present|base': 'b:true',
    'content.background|base': 'none',
    'content.borderColor|base': 'none',
    'content.shadow|base': 'none',
    'content.radius|base': 'none',
    'content.borderWidth|base': 'none',
    'content.direction|base': 'k:VERTICAL',
    'content.align|base': 'k:MIN/MIN',
    'content.gap|base': 't:inset.xs',
    'content.paddingTop|base': 't:inset.none',
    'content.paddingRight|base': 't:inset.none',
    'content.paddingBottom|base': 't:inset.none',
    'content.paddingLeft|base': 't:inset.none',
    'content.width|base': 'k:FILL',
    'content.height|base': 'k:HUG',
    'title.present|base': 'b:true',
    'title.color|base': 't:color.text.primary',
    'title.typography|base': 't:typography.body.md.medium',
    'meta.present|base': 'b:true',
    'meta.background|base': 'none',
    'meta.borderColor|base': 'none',
    'meta.shadow|base': 'none',
    'meta.radius|base': 'none',
    'meta.borderWidth|base': 'none',
    'meta.direction|base': 'k:HORIZONTAL',
    'meta.align|base': 'k:MIN/CENTER',
    'meta.gap|base': 't:stack.xs',
    'meta.paddingTop|base': 't:inset.none',
    'meta.paddingRight|base': 't:inset.none',
    'meta.paddingBottom|base': 't:inset.none',
    'meta.paddingLeft|base': 't:inset.none',
    'meta.width|base': 'k:FILL',
    'meta.height|base': 'k:HUG',
    'meta.background|appearance|default|focus': 'none',
    'productTag.present|base': 'b:true',
    'productTag.color|base': 't:color.text.secondary',
    'productTag.typography|base': 't:typography.label.sm',
    'metaText.present|base': 'b:true',
    'metaText.color|base': 't:color.text.secondary',
    'metaText.typography|base': 't:typography.body.sm.regular',
    'timestamp.present|base': 'b:true',
    'timestamp.color|base': 't:color.text.tertiary',
    'timestamp.typography|base': 't:typography.body.sm.regular',
    'more.present|base': 'b:true',
    'more.component|base': 'k:Icon/More',
    'more.variant.solid|base': 'k:false',
    'more.width|base': 't:icon.md',
    'more.height|base': 't:icon.md',
    'more.color|base': 't:color.icon.secondary',
  };

  /// Which state wins when several hold, highest first: the MUI recipe's cascade, read backwards.
  static const List<String> statePrecedence = ['focus', 'hover'];

  static bool _holds(String state, SolarEventRowProps p, Set<WidgetState> s) =>
      switch (state) {
        'focus' => s.contains(WidgetState.focused),
        'hover' => s.contains(WidgetState.hovered),
        _ => false,
      };

  /// The entry for one cell under these props and states, by the IR's precedence: a state that
  /// holds beats the resting value, the per-size-and-appearance entry beats the per-appearance
  /// one, and the resting value falls back through appearance, size and base.
  static String? lookup(String cell, SolarEventRowProps p, Set<WidgetState> s) {
    const combo = 'default';
    const size = '';
    for (final state in statePrecedence) {
      if (!_holds(state, p, s)) continue;
      final hit =
          cells['$cell|combined|$size|$combo|$state'] ??
          cells['$cell|appearance|$combo|$state'];
      if (hit != null) return hit;
    }
    return cells['$cell|combined|$size|$combo|default'] ??
        cells['$cell|appearance|$combo|default'] ??
        cells['$cell|size|$size'] ??
        cells['$cell|base'];
  }

  static Color color(
    SolarTheme t,
    String cell,
    SolarEventRowProps p,
    Set<WidgetState> s,
  ) {
    final c = t.colors;
    return switch (lookup(cell, p, s)) {
      'none' => Colors.transparent,
      't:color.border.subtle' => c.borderSubtle,
      't:color.icon.secondary' => c.iconSecondary,
      't:color.surface.base' => c.surfaceBase,
      't:color.surface.hover' => c.surfaceHover,
      't:color.text.primary' => c.textPrimary,
      't:color.text.secondary' => c.textSecondary,
      't:color.text.tertiary' => c.textTertiary,
      final v => throw StateError('$cell: no colour for $v'),
    };
  }

  static List<BoxShadow> shadow(
    SolarTheme t,
    String cell,
    SolarEventRowProps p,
    Set<WidgetState> s,
  ) => switch (lookup(cell, p, s)) {
    'none' || null => const <BoxShadow>[],
    't:shadow.focus.default' => t.shadows.focusDefault,
    final v => throw StateError('$cell: no shadow for $v'),
  };

  /// A length in logical pixels, or null where the IR says the layer hugs its content.
  static double? dimension(
    String cell,
    SolarEventRowProps p,
    Set<WidgetState> s,
  ) {
    final v = lookup(cell, p, s);
    if (v == null || v == 'k:HUG') return null;
    if (v == 'k:FILL') return double.infinity;
    if (v.startsWith('px:')) return double.parse(v.substring(3));
    return switch (v) {
      't:border.default' => SolarBorder.$default,
      't:icon.md' => SolarIconSize.md,
      't:inset.none' => SolarInset.none,
      't:inset.sm' => SolarInset.sm,
      't:inset.xs' => SolarInset.xs,
      't:radius.container' => SolarRadius.container,
      't:stack.sm' => SolarStack.sm,
      't:stack.xs' => SolarStack.xs,
      _ => throw StateError('$cell: no length for $v'),
    };
  }

  static TextStyle? textStyle(
    SolarTheme t,
    String cell,
    SolarEventRowProps p,
    Set<WidgetState> s,
  ) => switch (lookup(cell, p, s)) {
    'none' || null => null,
    't:typography.body.md.medium' => t.typography.bodyMdMedium,
    't:typography.body.sm.regular' => t.typography.bodySmRegular,
    't:typography.label.sm' => t.typography.labelSm,
    final v => throw StateError('$cell: no text style for $v'),
  };

  /// Whether a layer is drawn: the shell reads this, the style does not.
  static bool present(String layer, SolarEventRowProps p, Set<WidgetState> s) =>
      lookup('$layer.present', p, s) != 'b:false';
}
