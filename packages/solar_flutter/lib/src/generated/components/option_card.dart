// SOLAR Option Card recipe for Flutter. Generated by @bwp-web/codegen from spec/components/option card.json. Do not edit.
//
// The recipe is data: token *names*, keyed by layer and cell and by the base, size, appearance
// and combined sections of the IR. Values resolve against the ambient SolarTheme, so Light and
// Dark switch with the theme and no colour here is a literal.

import 'package:flutter/foundation.dart' show immutable;
import 'package:flutter/material.dart'
    show BoxShadow, Color, Colors, TextStyle, WidgetState;

import '../tokens.dart';

/// The props a SOLAR Option Card takes. Hover, pressed and focus are not here: they are
/// platform states, tracked by Flutter as [WidgetState]s.
@immutable
class SolarOptionCardProps {
  const SolarOptionCardProps({this.selected = false});

  final bool selected;
}

abstract final class SolarOptionCardRecipe {
  /// Every IR entry, as `<layer>.<cell>|<section>…` to a token name (`t:`), a keyword (`k:`),
  /// `none`, a presence (`b:`) or a literal the overlay allowed (`px:`).
  static const Map<String, String> cells = {
    'root.present|base': 'b:true',
    'root.background|base': 't:color.surface.base',
    'root.borderColor|base': 't:color.border.medium',
    'root.shadow|base': 'none',
    'root.radius|base': 't:radius.container',
    'root.borderWidth|base': 't:border.default',
    'root.borderDash|base': 'd:4,4',
    'root.direction|base': 'k:VERTICAL',
    'root.align|base': 'k:CENTER/CENTER',
    'root.gap|base': 't:stack.lg',
    'root.paddingTop|base': 't:inset.none',
    'root.paddingRight|base': 't:inset.none',
    'root.paddingBottom|base': 't:inset.none',
    'root.paddingLeft|base': 't:inset.none',
    'root.width|base': 'k:FILL',
    'root.height|base': 'px:228',
    'root.borderColor|appearance|default|hover': 't:color.border.subtle',
    'root.shadow|appearance|default|hover': 'none',
    'root.borderDash|appearance|default|hover': 'd:4,4',
    'root.shadow|appearance|default|selected': 't:shadow.raised',
    'root.borderDash|appearance|default|selected': 'none',
    'root.shadow|appearance|default|focus': 't:shadow.focus.default',
    'root.borderColor|appearance|default|focus': 't:color.border.medium',
    'root.borderDash|appearance|default|focus': 'd:4,4',
    'container.present|base': 'b:true',
    'container.background|base': 't:color.surface.background',
    'container.borderColor|base': 'none',
    'container.shadow|base': 'none',
    'container.radius|base': 't:radius.pill',
    'container.borderWidth|base': 'none',
    'container.direction|base': 'k:HORIZONTAL',
    'container.align|base': 'k:CENTER/CENTER',
    'container.gap|base': 't:inset.none',
    'container.paddingTop|base': 't:inset.none',
    'container.paddingRight|base': 't:inset.none',
    'container.paddingBottom|base': 't:inset.none',
    'container.paddingLeft|base': 't:inset.none',
    'container.width|base': 'px:64',
    'container.height|base': 'px:64',
    'iconPlus.present|base': 'b:true',
    'iconPlus.component|base': 'k:Icon/Plus',
    'iconPlus.variant.solid|base': 'k:false',
    'iconPlus.width|base': 't:icon.lg',
    'iconPlus.height|base': 't:icon.lg',
    'iconPlus.color|base': 't:color.icon.secondary',
    'iconPlus.color|appearance|default|hover': 't:color.icon.primary',
    'iconPlus.color|appearance|default|selected': 't:color.icon.primary',
    'iconPlus.color|appearance|default|focus': 't:color.icon.secondary',
    'label.present|base': 'b:true',
    'label.color|base': 't:color.text.primary',
    'label.typography|base': 't:typography.body.md.medium',
  };

  /// Each layer's children, in Figma's order: the tree the widget draws (SolarLayers).
  static const Map<String, List<String>> tree = {
    'root': ['container', 'label'],
    'container': ['iconPlus'],
  };

  /// Which state wins when several hold, highest first: the MUI recipe's cascade, read backwards.
  static const List<String> statePrecedence = ['selected', 'focus', 'hover'];

  static bool _holds(
    String state,
    SolarOptionCardProps p,
    Set<WidgetState> s,
  ) => switch (state) {
    'selected' => p.selected,
    'focus' => s.contains(WidgetState.focused),
    'hover' => s.contains(WidgetState.hovered),
    _ => false,
  };

  /// The entry for one cell under these props and states, by the IR's precedence: a state that
  /// holds beats the resting value, the per-size-and-appearance entry beats the per-appearance
  /// one, and the resting value falls back through appearance, size and base.
  static String? lookup(
    String cell,
    SolarOptionCardProps p,
    Set<WidgetState> s,
  ) {
    const combo = 'default';
    const size = '';
    for (final state in statePrecedence) {
      if (!_holds(state, p, s)) continue;
      final hit =
          cells['$cell|combined|$size|$combo|$state'] ??
          cells['$cell|appearance|$combo|$state'];
      if (hit != null) return hit;
    }
    return cells['$cell|combined|$size|$combo|default'] ??
        cells['$cell|appearance|$combo|default'] ??
        cells['$cell|size|$size'] ??
        cells['$cell|base'];
  }

  static Color color(
    SolarTheme t,
    String cell,
    SolarOptionCardProps p,
    Set<WidgetState> s,
  ) {
    final c = t.colors;
    return switch (lookup(cell, p, s)) {
      'none' => Colors.transparent,
      't:color.border.medium' => c.borderMedium,
      't:color.border.subtle' => c.borderSubtle,
      't:color.icon.primary' => c.iconPrimary,
      't:color.icon.secondary' => c.iconSecondary,
      't:color.surface.background' => c.surfaceBackground,
      't:color.surface.base' => c.surfaceBase,
      't:color.text.primary' => c.textPrimary,
      final v => throw StateError('$cell: no colour for $v'),
    };
  }

  static List<BoxShadow> shadow(
    SolarTheme t,
    String cell,
    SolarOptionCardProps p,
    Set<WidgetState> s,
  ) => switch (lookup(cell, p, s)) {
    'none' || null => const <BoxShadow>[],
    't:shadow.focus.default' => t.shadows.focusDefault,
    't:shadow.raised' => t.shadows.raised,
    final v => throw StateError('$cell: no shadow for $v'),
  };

  /// A length in logical pixels, or null where the IR says the layer hugs its content.
  static double? dimension(
    String cell,
    SolarOptionCardProps p,
    Set<WidgetState> s,
  ) {
    final v = lookup(cell, p, s);
    if (v == null || v == 'k:HUG') return null;
    if (v == 'k:FILL') return double.infinity;
    if (v.startsWith('px:')) return double.parse(v.substring(3));
    return switch (v) {
      't:border.default' => SolarBorder.$default,
      't:icon.lg' => SolarIconSize.lg,
      't:inset.none' => SolarInset.none,
      't:radius.container' => SolarRadius.container,
      't:radius.pill' => SolarRadius.pill,
      't:stack.lg' => SolarStack.lg,
      _ => throw StateError('$cell: no length for $v'),
    };
  }

  static TextStyle? textStyle(
    SolarTheme t,
    String cell,
    SolarOptionCardProps p,
    Set<WidgetState> s,
  ) => switch (lookup(cell, p, s)) {
    'none' || null => null,
    't:typography.body.md.medium' => t.typography.bodyMdMedium,
    final v => throw StateError('$cell: no text style for $v'),
  };

  /// Whether a layer is drawn: the shell reads this, the style does not.
  static bool present(
    String layer,
    SolarOptionCardProps p,
    Set<WidgetState> s,
  ) => lookup('$layer.present', p, s) != 'b:false';
}
