// SOLAR TableFooter recipe for Flutter. Generated by @bwp-web/codegen from spec/components/tablefooter.json. Do not edit.
//
// The recipe is data: token *names*, keyed by layer and cell and by the base, size, appearance
// and combined sections of the IR. Values resolve against the ambient SolarTheme, so Light and
// Dark switch with the theme and no colour here is a literal.

import 'package:flutter/foundation.dart' show immutable;
import 'package:flutter/material.dart'
    show BoxShadow, Color, Colors, TextStyle, WidgetState;

import '../tokens.dart';

enum SolarTableFooterBreakpoint { desktop, mobile }

/// The props a SOLAR TableFooter takes. Hover, pressed and focus are not here: they are
/// platform states, tracked by Flutter as [WidgetState]s.
@immutable
class SolarTableFooterProps {
  const SolarTableFooterProps({
    this.breakpoint = SolarTableFooterBreakpoint.desktop,
  });

  final SolarTableFooterBreakpoint breakpoint;
}

abstract final class SolarTableFooterRecipe {
  /// Every IR entry, as `<layer>.<cell>|<section>…` to a token name (`t:`), a keyword (`k:`),
  /// `none`, a presence (`b:`) or a literal the overlay allowed (`px:`).
  static const Map<String, String> cells = {
    'root.present|base': 'b:true',
    'root.background|base': 'none',
    'root.borderColor|base': 'none',
    'root.shadow|base': 'none',
    'root.radius|base': 'none',
    'root.borderWidth|base': 'none',
    'root.direction|base': 'k:HORIZONTAL',
    'root.align|base': 'k:CENTER/CENTER',
    'root.gap|base': 't:inset.none',
    'root.paddingTop|base': 't:inset.xs',
    'root.paddingRight|base': 't:stack.none',
    'root.paddingBottom|base': 't:inset.xs',
    'root.paddingLeft|base': 't:stack.none',
    'root.width|base': 'k:FILL',
    'root.height|base': 'k:HUG',
    'root.align|appearance|breakpoint=mobile|default': 'k:SPACE_BETWEEN/CENTER',
    'root.gap|appearance|breakpoint=mobile|default': 't:inset.md',
    'root.width|appearance|breakpoint=mobile|default': 'k:FILL',
    'container.present|base': 'b:true',
    'container.background|base': 'none',
    'container.borderColor|base': 'none',
    'container.shadow|base': 'none',
    'container.radius|base': 'none',
    'container.borderWidth|base': 'none',
    'container.direction|base': 'k:HORIZONTAL',
    'container.align|base': 'k:SPACE_BETWEEN/CENTER',
    'container.gap|base': 't:inset.md',
    'container.paddingTop|base': 't:inset.none',
    'container.paddingRight|base': 't:inset.none',
    'container.paddingBottom|base': 't:inset.none',
    'container.paddingLeft|base': 't:inset.none',
    'container.width|base': 'k:FILL',
    'container.height|base': 'k:HUG',
    'container.present|appearance|breakpoint=mobile|default': 'b:false',
    'containerContainer.present|base': 'b:true',
    'containerContainer.background|base': 'none',
    'containerContainer.borderColor|base': 'none',
    'containerContainer.shadow|base': 'none',
    'containerContainer.radius|base': 'none',
    'containerContainer.borderWidth|base': 'none',
    'containerContainer.direction|base': 'k:HORIZONTAL',
    'containerContainer.align|base': 'k:MIN/CENTER',
    'containerContainer.gap|base': 't:inset.sm',
    'containerContainer.paddingTop|base': 't:inset.none',
    'containerContainer.paddingRight|base': 't:inset.none',
    'containerContainer.paddingBottom|base': 't:inset.none',
    'containerContainer.paddingLeft|base': 't:inset.none',
    'containerContainer.width|base': 'k:HUG',
    'containerContainer.height|base': 'k:HUG',
    'containerContainer.present|appearance|breakpoint=mobile|default':
        'b:false',
    'rowsPerPage.present|base': 'b:true',
    'rowsPerPage.component|base': 'k:Dropdown',
    'rowsPerPage.variant.size|base': 'k:md',
    'rowsPerPage.variant.state|base': 'k:default',
    'rowsPerPage.width|base': 'k:HUG',
    'rowsPerPage.height|base': 'k:HUG',
    'rowsPerPage.present|appearance|breakpoint=mobile|default': 'b:false',
    'rowsPerPageLabel.present|base': 'b:true',
    'rowsPerPageLabel.color|base': 't:color.text.secondary',
    'rowsPerPageLabel.typography|base': 't:typography.body.md.regular',
    'rowsPerPageLabel.present|appearance|breakpoint=mobile|default': 'b:false',
    'pagination.present|base': 'b:true',
    'pagination.component|base': 'k:Pagination',
    'pagination.width|base': 'k:HUG',
    'pagination.height|base': 'k:HUG',
    'pagination.present|appearance|breakpoint=mobile|default': 'b:false',
    'button.present|base': 'b:true',
    'button.component|base': 'k:Button',
    'button.variant.size|base': 'k:md',
    'button.variant.prio|base': 'k:primary',
    'button.variant.state|base': 'k:default',
    'button.variant.danger|base': 'k:false',
    'button.width|base': 'k:HUG',
    'button.height|base': 'none',
    'button.present|appearance|breakpoint=mobile|default': 'b:false',
    'rowsPerPageMobile.present|base': 'b:false',
    'rowsPerPageMobile.component|base': 'k:Dropdown',
    'rowsPerPageMobile.variant.size|base': 'k:md',
    'rowsPerPageMobile.width|base': 'k:HUG',
    'rowsPerPageMobile.height|base': 'k:HUG',
    'rowsPerPageMobile.present|appearance|breakpoint=mobile|default': 'b:true',
    'rowsPerPageMobile.variant.state|appearance|breakpoint=mobile|default':
        'k:default',
    'paginationMobile.present|base': 'b:false',
    'paginationMobile.component|base': 'k:Pagination',
    'paginationMobile.width|base': 'k:HUG',
    'paginationMobile.height|base': 'k:HUG',
    'paginationMobile.present|appearance|breakpoint=mobile|default': 'b:true',
    'iconButton.present|base': 'b:false',
    'iconButton.component|base': 'k:Icon Button',
    'iconButton.variant.size|base': 'k:md',
    'iconButton.width|base': 'none',
    'iconButton.height|base': 'none',
    'iconButton.present|appearance|breakpoint=mobile|default': 'b:true',
    'iconButton.variant.shape|appearance|breakpoint=mobile|default': 'k:square',
    'iconButton.variant.prio|appearance|breakpoint=mobile|default': 'k:primary',
    'iconButton.variant.state|appearance|breakpoint=mobile|default':
        'k:default',
  };

  /// Each layer's children, in Figma's order: the tree the widget draws (SolarLayers).
  static const Map<String, List<String>> tree = {
    'root': [
      'container',
      'rowsPerPageMobile',
      'paginationMobile',
      'iconButton',
    ],
    'container': ['containerContainer', 'pagination', 'button'],
    'containerContainer': ['rowsPerPage', 'rowsPerPageLabel'],
  };

  /// Which state wins when several hold, highest first: the MUI recipe's cascade, read backwards.
  static const List<String> statePrecedence = <String>[];

  static bool _holds(
    String state,
    SolarTableFooterProps p,
    Set<WidgetState> s,
  ) => switch (state) {
    _ => false,
  };

  /// The entry for one cell under these props and states, by the IR's precedence: a state that
  /// holds beats the resting value, the per-size-and-appearance entry beats the per-appearance
  /// one, and the resting value falls back through appearance, size and base.
  static String? lookup(
    String cell,
    SolarTableFooterProps p,
    Set<WidgetState> s,
  ) {
    final combo = 'breakpoint=${p.breakpoint.name}';
    const size = '';
    for (final state in statePrecedence) {
      if (!_holds(state, p, s)) continue;
      final hit =
          cells['$cell|combined|$size|$combo|$state'] ??
          cells['$cell|appearance|$combo|$state'];
      if (hit != null) return hit;
    }
    return cells['$cell|combined|$size|$combo|default'] ??
        cells['$cell|appearance|$combo|default'] ??
        cells['$cell|size|$size'] ??
        cells['$cell|base'];
  }

  static Color color(
    SolarTheme t,
    String cell,
    SolarTableFooterProps p,
    Set<WidgetState> s,
  ) {
    final c = t.colors;
    return switch (lookup(cell, p, s)) {
      'none' => Colors.transparent,
      't:color.text.secondary' => c.textSecondary,
      final v => throw StateError('$cell: no colour for $v'),
    };
  }

  static List<BoxShadow> shadow(
    SolarTheme t,
    String cell,
    SolarTableFooterProps p,
    Set<WidgetState> s,
  ) => switch (lookup(cell, p, s)) {
    'none' || null => const <BoxShadow>[],

    final v => throw StateError('$cell: no shadow for $v'),
  };

  /// A length in logical pixels, or null where the IR says the layer hugs its content.
  static double? dimension(
    String cell,
    SolarTableFooterProps p,
    Set<WidgetState> s,
  ) {
    final v = lookup(cell, p, s);
    if (v == null || v == 'k:HUG') return null;
    if (v == 'k:FILL') return double.infinity;
    if (v.startsWith('px:')) return double.parse(v.substring(3));
    return switch (v) {
      't:inset.md' => SolarInset.md,
      't:inset.none' => SolarInset.none,
      't:inset.sm' => SolarInset.sm,
      't:inset.xs' => SolarInset.xs,
      't:stack.none' => SolarStack.none,
      _ => throw StateError('$cell: no length for $v'),
    };
  }

  static TextStyle? textStyle(
    SolarTheme t,
    String cell,
    SolarTableFooterProps p,
    Set<WidgetState> s,
  ) => switch (lookup(cell, p, s)) {
    'none' || null => null,
    't:typography.body.md.regular' => t.typography.bodyMdRegular,
    final v => throw StateError('$cell: no text style for $v'),
  };

  /// Whether a layer is drawn: the shell reads this, the style does not.
  static bool present(
    String layer,
    SolarTableFooterProps p,
    Set<WidgetState> s,
  ) => lookup('$layer.present', p, s) != 'b:false';
}
