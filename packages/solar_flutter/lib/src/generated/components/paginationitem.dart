// SOLAR PaginationItem recipe for Flutter. Generated by @bwp-web/codegen from spec/components/paginationitem.json. Do not edit.
//
// The recipe is data: token *names*, keyed by layer and cell and by the base, size, appearance
// and combined sections of the IR. Values resolve against the ambient SolarTheme, so Light and
// Dark switch with the theme and no colour here is a literal.

import 'package:flutter/foundation.dart' show immutable;
import 'package:flutter/material.dart'
    show BoxShadow, Color, Colors, TextStyle, WidgetState;

import '../tokens.dart';

/// The props a SOLAR PaginationItem takes. Hover, pressed and focus are not here: they are
/// platform states, tracked by Flutter as [WidgetState]s.
@immutable
class SolarPaginationItemProps {
  const SolarPaginationItemProps({
    this.selected = false,
    this.disabled = false,
  });

  final bool selected;
  final bool disabled;
}

abstract final class SolarPaginationItemRecipe {
  /// Every IR entry, as `<layer>.<cell>|<section>…` to a token name (`t:`), a keyword (`k:`),
  /// `none`, a presence (`b:`) or a literal the overlay allowed (`px:`).
  static const Map<String, String> cells = {
    'root.present|base': 'b:true',
    'root.background|base': 'none',
    'root.borderColor|base': 'none',
    'root.shadow|base': 'none',
    'root.radius|base': 't:radius.container',
    'root.borderWidth|base': 'none',
    'root.direction|base': 'k:HORIZONTAL',
    'root.align|base': 'k:CENTER/CENTER',
    'root.gap|base': 't:inset.none',
    'root.paddingTop|base': 't:inset.none',
    'root.paddingRight|base': 't:inset.none',
    'root.paddingBottom|base': 't:inset.none',
    'root.paddingLeft|base': 't:inset.none',
    'root.width|base': 'px:24',
    'root.height|base': 'px:24',
    'root.background|appearance|selected=false|hover': 't:color.surface.hover',
    'root.background|appearance|selected=false|pressed':
        't:color.surface.active',
    'root.shadow|appearance|selected=false|focus': 't:shadow.focus.default',
    'root.background|appearance|selected=false|focus': 'none',
    'root.background|appearance|selected=true|default':
        't:color.action.primary.bg.default',
    'root.background|appearance|selected=true|hover':
        't:color.action.primary.bg.hover',
    'root.background|appearance|selected=true|pressed':
        't:color.action.primary.bg.active',
    'root.background|appearance|selected=true|focus':
        't:color.action.primary.bg.default',
    'root.shadow|appearance|selected=true|focus': 't:shadow.focus.default',
    'root.background|appearance|selected=true|disabled':
        't:color.action.primary.bg.disabled',
    'page.present|base': 'b:true',
    'page.color|base': 't:color.text.primary',
    'page.typography|base': 't:typography.label.md',
    'page.width|base': 'k:FILL',
    'page.color|appearance|selected=false|disabled': 't:color.text.disabled',
    'page.color|appearance|selected=true|default':
        't:color.action.primary.text.default',
    'page.color|appearance|selected=true|hover':
        't:color.action.primary.text.default',
    'page.color|appearance|selected=true|pressed':
        't:color.action.primary.text.default',
    'page.color|appearance|selected=true|focus':
        't:color.action.primary.text.default',
    'page.color|appearance|selected=true|disabled':
        't:color.action.primary.text.disabled',
  };

  /// Each layer's children, in Figma's order: the tree the widget draws (SolarLayers).
  static const Map<String, List<String>> tree = {
    'root': ['page'],
  };

  /// Which state wins when several hold, highest first: the MUI recipe's cascade, read backwards.
  static const List<String> statePrecedence = [
    'disabled',
    'focus',
    'pressed',
    'hover',
  ];

  static bool _holds(
    String state,
    SolarPaginationItemProps p,
    Set<WidgetState> s,
  ) => switch (state) {
    'disabled' => p.disabled || s.contains(WidgetState.disabled),
    'focus' => s.contains(WidgetState.focused),
    'pressed' => s.contains(WidgetState.pressed),
    'hover' => s.contains(WidgetState.hovered),
    _ => false,
  };

  /// The entry for one cell under these props and states, by the IR's precedence: a state that
  /// holds beats the resting value, the per-size-and-appearance entry beats the per-appearance
  /// one, and the resting value falls back through appearance, size and base.
  static String? lookup(
    String cell,
    SolarPaginationItemProps p,
    Set<WidgetState> s,
  ) {
    final combo = 'selected=${p.selected}';
    const size = '';
    for (final state in statePrecedence) {
      if (!_holds(state, p, s)) continue;
      final hit =
          cells['$cell|combined|$size|$combo|$state'] ??
          cells['$cell|appearance|$combo|$state'];
      if (hit != null) return hit;
    }
    return cells['$cell|combined|$size|$combo|default'] ??
        cells['$cell|appearance|$combo|default'] ??
        cells['$cell|size|$size'] ??
        cells['$cell|base'];
  }

  static Color color(
    SolarTheme t,
    String cell,
    SolarPaginationItemProps p,
    Set<WidgetState> s,
  ) {
    final c = t.colors;
    return switch (lookup(cell, p, s)) {
      'none' => Colors.transparent,
      't:color.action.primary.bg.active' => c.actionPrimaryBgActive,
      't:color.action.primary.bg.default' => c.actionPrimaryBgDefault,
      't:color.action.primary.bg.disabled' => c.actionPrimaryBgDisabled,
      't:color.action.primary.bg.hover' => c.actionPrimaryBgHover,
      't:color.action.primary.text.default' => c.actionPrimaryTextDefault,
      't:color.action.primary.text.disabled' => c.actionPrimaryTextDisabled,
      't:color.surface.active' => c.surfaceActive,
      't:color.surface.hover' => c.surfaceHover,
      't:color.text.disabled' => c.textDisabled,
      't:color.text.primary' => c.textPrimary,
      final v => throw StateError('$cell: no colour for $v'),
    };
  }

  static List<BoxShadow> shadow(
    SolarTheme t,
    String cell,
    SolarPaginationItemProps p,
    Set<WidgetState> s,
  ) => switch (lookup(cell, p, s)) {
    'none' || null => const <BoxShadow>[],
    't:shadow.focus.default' => t.shadows.focusDefault,
    final v => throw StateError('$cell: no shadow for $v'),
  };

  /// A length in logical pixels, or null where the IR says the layer hugs its content.
  static double? dimension(
    String cell,
    SolarPaginationItemProps p,
    Set<WidgetState> s,
  ) {
    final v = lookup(cell, p, s);
    if (v == null || v == 'k:HUG') return null;
    if (v == 'k:FILL') return double.infinity;
    if (v.startsWith('px:')) return double.parse(v.substring(3));
    return switch (v) {
      't:inset.none' => SolarInset.none,
      't:radius.container' => SolarRadius.container,
      _ => throw StateError('$cell: no length for $v'),
    };
  }

  static TextStyle? textStyle(
    SolarTheme t,
    String cell,
    SolarPaginationItemProps p,
    Set<WidgetState> s,
  ) => switch (lookup(cell, p, s)) {
    'none' || null => null,
    't:typography.label.md' => t.typography.labelMd,
    final v => throw StateError('$cell: no text style for $v'),
  };

  /// Whether a layer is drawn: the shell reads this, the style does not.
  static bool present(
    String layer,
    SolarPaginationItemProps p,
    Set<WidgetState> s,
  ) => lookup('$layer.present', p, s) != 'b:false';
}
