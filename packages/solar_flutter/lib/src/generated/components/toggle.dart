// SOLAR Toggle recipe for Flutter. Generated by @bwp-web/codegen from spec/components/toggle.json. Do not edit.
//
// The recipe is data: token *names*, keyed by layer and cell and by the base, size, appearance
// and combined sections of the IR. Values resolve against the ambient SolarTheme, so Light and
// Dark switch with the theme and no colour here is a literal.

import 'package:flutter/foundation.dart' show immutable;
import 'package:flutter/material.dart'
    show BoxShadow, Color, Colors, TextStyle, WidgetState;

import '../tokens.dart';

/// The props a SOLAR Toggle takes. Hover, pressed and focus are not here: they are
/// platform states, tracked by Flutter as [WidgetState]s.
@immutable
class SolarToggleProps {
  const SolarToggleProps({this.selected = false, this.disabled = false});

  final bool selected;
  final bool disabled;
}

abstract final class SolarToggleRecipe {
  /// Every IR entry, as `<layer>.<cell>|<section>…` to a token name (`t:`), a keyword (`k:`),
  /// `none`, a presence (`b:`) or a literal the overlay allowed (`px:`).
  static const Map<String, String> cells = {
    'root.present|base': 'b:true',
    'root.background|base': 't:color.surface.muted',
    'root.borderColor|base': 't:color.border.subtle',
    'root.shadow|base': 'none',
    'root.radius|base': 't:radius.pill',
    'root.borderWidth|base': 't:border.default',
    'root.width|base': 'px:32',
    'root.height|base': 'px:18',
    'root.background|appearance|selected=true|default':
        't:color.action.primary.bg.default',
    'root.background|appearance|selected=true|hover':
        't:color.action.primary.bg.hover',
    'root.borderColor|appearance|selected=true|hover': 't:color.border.medium',
    'root.background|appearance|selected=true|disabled':
        't:color.action.primary.bg.disabled',
    'root.shadow|appearance|selected=true|focus': 't:shadow.focus.default',
    'root.background|appearance|selected=true|focus':
        't:color.action.primary.bg.default',
    'root.borderColor|appearance|selected=true|focus': 't:color.border.subtle',
    'root.borderColor|appearance|selected=false|hover': 't:color.border.medium',
    'root.shadow|appearance|selected=false|focus': 't:shadow.focus.default',
    'root.borderColor|appearance|selected=false|focus': 't:color.border.subtle',
    'thumb.present|base': 'b:true',
    'thumb.background|base': 't:color.action.primary.icon.default',
    'thumb.borderColor|base': 't:color.border.subtle',
    'thumb.shadow|base': 't:shadow.control',
    'thumb.radius|base': 't:radius.pill',
    'thumb.borderWidth|base': 't:border.default',
    'thumb.width|base': 'px:12',
    'thumb.height|base': 'px:12',
    'thumb.x|base': 'px:3',
    'thumb.y|base': 'px:3',
    'thumb.background|appearance|selected=false|disabled':
        't:color.action.primary.icon.disabled',
    'thumb.borderColor|appearance|selected=false|disabled': 'none',
    'thumb.borderWidth|appearance|selected=false|disabled': 'none',
    'thumb.borderColor|appearance|selected=false|hover':
        't:color.border.medium',
    'thumb.borderColor|appearance|selected=false|focus':
        't:color.border.subtle',
    'thumb.background|appearance|selected=true|disabled':
        't:color.action.primary.icon.disabled',
    'thumb.borderColor|appearance|selected=true|disabled': 'none',
    'thumb.borderWidth|appearance|selected=true|disabled': 'none',
    'thumb.x|appearance|selected=true|disabled': 'px:17',
    'thumb.borderColor|appearance|selected=true|hover': 't:color.border.medium',
    'thumb.x|appearance|selected=true|hover': 'px:17',
    'thumb.x|appearance|selected=true|default': 'px:17',
    'thumb.borderColor|appearance|selected=true|focus': 't:color.border.subtle',
  };

  /// Each layer's children, in Figma's order: the tree the widget draws (SolarLayers).
  static const Map<String, List<String>> tree = {
    'root': ['thumb'],
  };

  /// Which state wins when several hold, highest first: the MUI recipe's cascade, read backwards.
  static const List<String> statePrecedence = ['disabled', 'focus', 'hover'];

  static bool _holds(String state, SolarToggleProps p, Set<WidgetState> s) =>
      switch (state) {
        'disabled' => p.disabled || s.contains(WidgetState.disabled),
        'focus' => s.contains(WidgetState.focused),
        'hover' => s.contains(WidgetState.hovered),
        _ => false,
      };

  /// The entry for one cell under these props and states, by the IR's precedence: a state that
  /// holds beats the resting value, the per-size-and-appearance entry beats the per-appearance
  /// one, and the resting value falls back through appearance, size and base.
  static String? lookup(String cell, SolarToggleProps p, Set<WidgetState> s) {
    final combo = 'selected=${p.selected}';
    const size = '';
    for (final state in statePrecedence) {
      if (!_holds(state, p, s)) continue;
      final hit =
          cells['$cell|combined|$size|$combo|$state'] ??
          cells['$cell|appearance|$combo|$state'];
      if (hit != null) return hit;
    }
    return cells['$cell|combined|$size|$combo|default'] ??
        cells['$cell|appearance|$combo|default'] ??
        cells['$cell|size|$size'] ??
        cells['$cell|base'];
  }

  static Color color(
    SolarTheme t,
    String cell,
    SolarToggleProps p,
    Set<WidgetState> s,
  ) {
    final c = t.colors;
    return switch (lookup(cell, p, s)) {
      'none' => Colors.transparent,
      't:color.action.primary.bg.default' => c.actionPrimaryBgDefault,
      't:color.action.primary.bg.disabled' => c.actionPrimaryBgDisabled,
      't:color.action.primary.bg.hover' => c.actionPrimaryBgHover,
      't:color.action.primary.icon.default' => c.actionPrimaryIconDefault,
      't:color.action.primary.icon.disabled' => c.actionPrimaryIconDisabled,
      't:color.border.medium' => c.borderMedium,
      't:color.border.subtle' => c.borderSubtle,
      't:color.surface.muted' => c.surfaceMuted,
      final v => throw StateError('$cell: no colour for $v'),
    };
  }

  static List<BoxShadow> shadow(
    SolarTheme t,
    String cell,
    SolarToggleProps p,
    Set<WidgetState> s,
  ) => switch (lookup(cell, p, s)) {
    'none' || null => const <BoxShadow>[],
    't:shadow.control' => t.shadows.control,
    't:shadow.focus.default' => t.shadows.focusDefault,
    final v => throw StateError('$cell: no shadow for $v'),
  };

  /// A length in logical pixels, or null where the IR says the layer hugs its content.
  static double? dimension(
    String cell,
    SolarToggleProps p,
    Set<WidgetState> s,
  ) {
    final v = lookup(cell, p, s);
    if (v == null || v == 'k:HUG') return null;
    if (v == 'k:FILL') return double.infinity;
    if (v.startsWith('px:')) return double.parse(v.substring(3));
    return switch (v) {
      't:border.default' => SolarBorder.$default,
      't:radius.pill' => SolarRadius.pill,
      _ => throw StateError('$cell: no length for $v'),
    };
  }

  static TextStyle? textStyle(
    SolarTheme t,
    String cell,
    SolarToggleProps p,
    Set<WidgetState> s,
  ) => switch (lookup(cell, p, s)) {
    'none' || null => null,

    final v => throw StateError('$cell: no text style for $v'),
  };

  /// Whether a layer is drawn: the shell reads this, the style does not.
  static bool present(String layer, SolarToggleProps p, Set<WidgetState> s) =>
      lookup('$layer.present', p, s) != 'b:false';
}
