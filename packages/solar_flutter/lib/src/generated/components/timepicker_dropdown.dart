// SOLAR TimePicker Dropdown recipe for Flutter. Generated by @bwp-web/codegen from spec/components/timepicker dropdown.json. Do not edit.
//
// The recipe is data: token *names*, keyed by layer and cell and by the base, size, appearance
// and combined sections of the IR. Values resolve against the ambient SolarTheme, so Light and
// Dark switch with the theme and no colour here is a literal.

import 'package:flutter/foundation.dart' show immutable;
import 'package:flutter/material.dart'
    show BoxShadow, Color, Colors, TextStyle, WidgetState;

import '../tokens.dart';

enum SolarTimePickerDropdownSize { sm, md }

/// The props a SOLAR TimePicker Dropdown takes. Hover, pressed and focus are not here: they are
/// platform states, tracked by Flutter as [WidgetState]s.
@immutable
class SolarTimePickerDropdownProps {
  const SolarTimePickerDropdownProps({
    this.size = SolarTimePickerDropdownSize.md,
  });

  final SolarTimePickerDropdownSize size;
}

abstract final class SolarTimePickerDropdownRecipe {
  /// Every IR entry, as `<layer>.<cell>|<section>…` to a token name (`t:`), a keyword (`k:`),
  /// `none`, a presence (`b:`) or a literal the overlay allowed (`px:`).
  static const Map<String, String> cells = {
    'root.present|base': 'b:true',
    'root.background|base': 't:color.surface.overlay',
    'root.borderColor|base': 't:color.border.medium',
    'root.shadow|base': 't:shadow.control',
    'root.radius|base': 't:radius.container',
    'root.borderWidth|base': 't:border.default',
    'root.direction|base': 'k:VERTICAL',
    'root.align|base': 'k:MIN/MIN',
    'root.gap|base': 't:stack.none',
    'root.paddingTop|base': 't:inset.none',
    'root.paddingRight|base': 't:inset.none',
    'root.paddingBottom|base': 't:inset.none',
    'root.paddingLeft|base': 't:inset.none',
    'root.width|base': 'k:HUG',
    'root.height|base': 'k:HUG',
    'content.present|base': 'b:true',
    'content.background|base': 'none',
    'content.borderColor|base': 'none',
    'content.shadow|base': 'none',
    'content.radius|base': 'none',
    'content.borderWidth|base': 'none',
    'content.direction|base': 'k:VERTICAL',
    'content.align|base': 'k:MIN/MIN',
    'content.gap|base': 't:inset.none',
    'content.paddingTop|base': 't:inset.none',
    'content.paddingRight|base': 't:inset.none',
    'content.paddingBottom|base': 't:inset.none',
    'content.paddingLeft|base': 't:inset.none',
    'content.width|base': 'k:FILL',
    'content.height|base': 'k:HUG',
    'dropdownItem.present|base': 'b:true',
    'dropdownItem.component|base': 'k:Dropdown Item',
    'dropdownItem.variant.size|base': 'k:md',
    'dropdownItem.variant.state|base': 'k:selected',
    'dropdownItem.width|base': 'k:FILL',
    'dropdownItem.height|base': 'k:HUG',
    'dropdownItem.variant.size|size|sm': 'k:sm',
    'dropdownItem2.present|base': 'b:true',
    'dropdownItem2.component|base': 'k:Dropdown Item',
    'dropdownItem2.variant.size|base': 'k:md',
    'dropdownItem2.variant.state|base': 'k:default',
    'dropdownItem2.width|base': 'k:FILL',
    'dropdownItem2.height|base': 'k:HUG',
    'dropdownItem2.variant.size|size|sm': 'k:sm',
    'dropdownItem3.present|base': 'b:true',
    'dropdownItem3.component|base': 'k:Dropdown Item',
    'dropdownItem3.variant.size|base': 'k:md',
    'dropdownItem3.variant.state|base': 'k:default',
    'dropdownItem3.width|base': 'k:FILL',
    'dropdownItem3.height|base': 'k:HUG',
    'dropdownItem3.variant.size|size|sm': 'k:sm',
    'dropdownItem4.present|base': 'b:true',
    'dropdownItem4.component|base': 'k:Dropdown Item',
    'dropdownItem4.variant.size|base': 'k:md',
    'dropdownItem4.variant.state|base': 'k:default',
    'dropdownItem4.width|base': 'k:FILL',
    'dropdownItem4.height|base': 'k:HUG',
    'dropdownItem4.variant.size|size|sm': 'k:sm',
    'dropdownItem5.present|base': 'b:true',
    'dropdownItem5.component|base': 'k:Dropdown Item',
    'dropdownItem5.variant.size|base': 'k:md',
    'dropdownItem5.variant.state|base': 'k:default',
    'dropdownItem5.width|base': 'k:FILL',
    'dropdownItem5.height|base': 'k:HUG',
    'dropdownItem5.variant.size|size|sm': 'k:sm',
    'dropdownItem6.present|base': 'b:true',
    'dropdownItem6.component|base': 'k:Dropdown Item',
    'dropdownItem6.variant.size|base': 'k:md',
    'dropdownItem6.variant.state|base': 'k:default',
    'dropdownItem6.width|base': 'k:FILL',
    'dropdownItem6.height|base': 'k:HUG',
    'dropdownItem6.variant.size|size|sm': 'k:sm',
  };

  /// Which state wins when several hold, highest first: the MUI recipe's cascade, read backwards.
  static const List<String> statePrecedence = <String>[];

  static bool _holds(
    String state,
    SolarTimePickerDropdownProps p,
    Set<WidgetState> s,
  ) => switch (state) {
    _ => false,
  };

  /// The entry for one cell under these props and states, by the IR's precedence: a state that
  /// holds beats the resting value, the per-size-and-appearance entry beats the per-appearance
  /// one, and the resting value falls back through appearance, size and base.
  static String? lookup(
    String cell,
    SolarTimePickerDropdownProps p,
    Set<WidgetState> s,
  ) {
    const combo = 'default';
    final size = p.size.name;
    for (final state in statePrecedence) {
      if (!_holds(state, p, s)) continue;
      final hit =
          cells['$cell|combined|$size|$combo|$state'] ??
          cells['$cell|appearance|$combo|$state'];
      if (hit != null) return hit;
    }
    return cells['$cell|combined|$size|$combo|default'] ??
        cells['$cell|appearance|$combo|default'] ??
        cells['$cell|size|$size'] ??
        cells['$cell|base'];
  }

  static Color color(
    SolarTheme t,
    String cell,
    SolarTimePickerDropdownProps p,
    Set<WidgetState> s,
  ) {
    final c = t.colors;
    return switch (lookup(cell, p, s)) {
      'none' => Colors.transparent,
      't:color.border.medium' => c.borderMedium,
      't:color.surface.overlay' => c.surfaceOverlay,
      final v => throw StateError('$cell: no colour for $v'),
    };
  }

  static List<BoxShadow> shadow(
    SolarTheme t,
    String cell,
    SolarTimePickerDropdownProps p,
    Set<WidgetState> s,
  ) => switch (lookup(cell, p, s)) {
    'none' || null => const <BoxShadow>[],
    't:shadow.control' => t.shadows.control,
    final v => throw StateError('$cell: no shadow for $v'),
  };

  /// A length in logical pixels, or null where the IR says the layer hugs its content.
  static double? dimension(
    String cell,
    SolarTimePickerDropdownProps p,
    Set<WidgetState> s,
  ) {
    final v = lookup(cell, p, s);
    if (v == null || v == 'k:HUG') return null;
    if (v == 'k:FILL') return double.infinity;
    if (v.startsWith('px:')) return double.parse(v.substring(3));
    return switch (v) {
      't:border.default' => SolarBorder.$default,
      't:inset.none' => SolarInset.none,
      't:radius.container' => SolarRadius.container,
      't:stack.none' => SolarStack.none,
      _ => throw StateError('$cell: no length for $v'),
    };
  }

  static TextStyle? textStyle(
    SolarTheme t,
    String cell,
    SolarTimePickerDropdownProps p,
    Set<WidgetState> s,
  ) => switch (lookup(cell, p, s)) {
    'none' || null => null,

    final v => throw StateError('$cell: no text style for $v'),
  };

  /// Whether a layer is drawn: the shell reads this, the style does not.
  static bool present(
    String layer,
    SolarTimePickerDropdownProps p,
    Set<WidgetState> s,
  ) => lookup('$layer.present', p, s) != 'b:false';
}
