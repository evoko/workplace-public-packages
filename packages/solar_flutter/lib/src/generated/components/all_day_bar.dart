// SOLAR All-Day Bar recipe for Flutter. Generated by @bwp-web/codegen from spec/components/all-day bar.json. Do not edit.
//
// The recipe is data: token *names*, keyed by layer and cell and by the base, size, appearance
// and combined sections of the IR. Values resolve against the ambient SolarTheme, so Light and
// Dark switch with the theme and no colour here is a literal.

import 'package:flutter/foundation.dart' show immutable;
import 'package:flutter/material.dart'
    show BoxShadow, Color, Colors, TextStyle, WidgetState;

import '../tokens.dart';

enum SolarAllDayBarVariant { subtle, solid }

enum SolarAllDayBarSpan { single, start, middle, end }

/// The props a SOLAR All-Day Bar takes. Hover, pressed and focus are not here: they are
/// platform states, tracked by Flutter as [WidgetState]s.
@immutable
class SolarAllDayBarProps {
  const SolarAllDayBarProps({
    this.variant = SolarAllDayBarVariant.subtle,
    this.span = SolarAllDayBarSpan.single,
  });

  final SolarAllDayBarVariant variant;
  final SolarAllDayBarSpan span;
}

abstract final class SolarAllDayBarRecipe {
  /// Every IR entry, as `<layer>.<cell>|<section>…` to a token name (`t:`), a keyword (`k:`),
  /// `none`, a presence (`b:`) or a literal the overlay allowed (`px:`).
  static const Map<String, String> cells = {
    'root.present|base': 'b:true',
    'root.background|base': 'none',
    'root.borderColor|base': 'none',
    'root.shadow|base': 'none',
    'root.radius|base': 't:radius.none',
    'root.borderWidth|base': 'none',
    'root.direction|base': 'k:HORIZONTAL',
    'root.align|base': 'k:MIN/CENTER',
    'root.gap|base': 't:inset.none',
    'root.paddingTop|base': 't:inset.none',
    'root.paddingRight|base': 't:inset.none',
    'root.paddingBottom|base': 't:inset.none',
    'root.paddingLeft|base': 't:inset.none',
    'root.width|base': 'k:FILL',
    'root.height|base': 'px:22',
    'root.background|appearance|variant=solid, span=single|default':
        't:color.data.category.06.strong',
    'root.radius|appearance|variant=solid, span=single|default':
        't:radius.control',
    'root.align|appearance|variant=solid, span=single|default': 'k:MIN/MIN',
    'root.background|appearance|variant=solid, span=start|default':
        't:color.data.category.06.strong',
    'root.radius|appearance|variant=solid, span=start|default':
        't:radius.control',
    'root.align|appearance|variant=solid, span=start|default': 'k:MIN/MIN',
    'root.background|appearance|variant=solid, span=middle|default':
        't:color.data.category.06.strong',
    'root.radius|appearance|variant=solid, span=middle|default':
        't:radius.control',
    'root.align|appearance|variant=solid, span=middle|default': 'k:MIN/MIN',
    'root.background|appearance|variant=solid, span=end|default':
        't:color.data.category.06.strong',
    'root.radius|appearance|variant=solid, span=end|default':
        't:radius.control',
    'root.align|appearance|variant=solid, span=end|default': 'k:MIN/MIN',
    'stripe.present|base': 'b:true',
    'stripe.background|base': 't:color.data.category.06.strong',
    'stripe.borderColor|base': 'none',
    'stripe.shadow|base': 'none',
    'stripe.radius|base': 't:radius.pill',
    'stripe.borderWidth|base': 'none',
    'stripe.width|base': 'px:3',
    'stripe.height|base': 'k:FILL',
    'stripe.present|appearance|variant=solid, span=single|default': 'b:false',
    'stripe.radius|appearance|variant=solid, span=single|default': 'none',
    'stripe.height|appearance|variant=solid, span=single|default': 'px:25',
    'stripe.present|appearance|variant=solid, span=start|default': 'b:false',
    'stripe.radius|appearance|variant=solid, span=start|default': 'none',
    'stripe.height|appearance|variant=solid, span=start|default': 'px:25',
    'stripe.present|appearance|variant=solid, span=middle|default': 'b:false',
    'stripe.radius|appearance|variant=solid, span=middle|default': 'none',
    'stripe.height|appearance|variant=solid, span=middle|default': 'px:25',
    'stripe.present|appearance|variant=solid, span=end|default': 'b:false',
    'stripe.radius|appearance|variant=solid, span=end|default': 'none',
    'stripe.height|appearance|variant=solid, span=end|default': 'px:25',
    'content.present|base': 'b:true',
    'content.background|base': 'none',
    'content.borderColor|base': 'none',
    'content.shadow|base': 'none',
    'content.radius|base': 'none',
    'content.borderWidth|base': 'none',
    'content.direction|base': 'k:HORIZONTAL',
    'content.align|base': 'k:MIN/CENTER',
    'content.gap|base': 't:stack.xs',
    'content.paddingTop|base': 't:inset.none',
    'content.paddingRight|base': 't:inset.sm',
    'content.paddingBottom|base': 't:inset.none',
    'content.paddingLeft|base': 't:inset.sm',
    'content.width|base': 'k:FILL',
    'content.height|base': 'k:FILL',
    'time.present|base': 'b:true',
    'time.color|base': 't:color.text.secondary',
    'time.typography|base': 't:typography.body.sm.medium',
    'time.color|appearance|variant=solid, span=single|default':
        't:color.text.inverse',
    'time.color|appearance|variant=solid, span=start|default':
        't:color.text.inverse',
    'time.color|appearance|variant=solid, span=middle|default':
        't:color.text.inverse',
    'time.color|appearance|variant=solid, span=end|default':
        't:color.text.inverse',
    'title.present|base': 'b:true',
    'title.color|base': 't:color.text.primary',
    'title.typography|base': 't:typography.body.sm.medium',
    'title.width|base': 'k:FILL',
    'title.color|appearance|variant=solid, span=single|default':
        't:color.text.inverse',
    'title.typography|appearance|variant=solid, span=single|default':
        't:typography.body.sm.semibold',
    'title.color|appearance|variant=solid, span=start|default':
        't:color.text.inverse',
    'title.typography|appearance|variant=solid, span=start|default':
        't:typography.body.sm.semibold',
    'title.color|appearance|variant=solid, span=middle|default':
        't:color.text.inverse',
    'title.typography|appearance|variant=solid, span=middle|default':
        't:typography.body.sm.semibold',
    'title.color|appearance|variant=solid, span=end|default':
        't:color.text.inverse',
    'title.typography|appearance|variant=solid, span=end|default':
        't:typography.body.sm.semibold',
  };

  /// Each layer's children, in Figma's order: the tree the widget draws (SolarLayers).
  static const Map<String, List<String>> tree = {
    'root': ['stripe', 'content'],
    'content': ['time', 'title'],
  };

  /// Which state wins when several hold, highest first: the MUI recipe's cascade, read backwards.
  static const List<String> statePrecedence = <String>[];

  static bool _holds(String state, SolarAllDayBarProps p, Set<WidgetState> s) =>
      switch (state) {
        _ => false,
      };

  /// The entry for one cell under these props and states, by the IR's precedence: a state that
  /// holds beats the resting value, the per-size-and-appearance entry beats the per-appearance
  /// one, and the resting value falls back through appearance, size and base.
  static String? lookup(
    String cell,
    SolarAllDayBarProps p,
    Set<WidgetState> s,
  ) {
    final combo = 'variant=${p.variant.name}, span=${p.span.name}';
    const size = '';
    for (final state in statePrecedence) {
      if (!_holds(state, p, s)) continue;
      final hit =
          cells['$cell|combined|$size|$combo|$state'] ??
          cells['$cell|appearance|$combo|$state'];
      if (hit != null) return hit;
    }
    return cells['$cell|combined|$size|$combo|default'] ??
        cells['$cell|appearance|$combo|default'] ??
        cells['$cell|size|$size'] ??
        cells['$cell|base'];
  }

  static Color color(
    SolarTheme t,
    String cell,
    SolarAllDayBarProps p,
    Set<WidgetState> s,
  ) {
    final c = t.colors;
    return switch (lookup(cell, p, s)) {
      'none' => Colors.transparent,
      't:color.data.category.06.strong' => c.dataCategory06Strong,
      't:color.text.inverse' => c.textInverse,
      't:color.text.primary' => c.textPrimary,
      't:color.text.secondary' => c.textSecondary,
      final v => throw StateError('$cell: no colour for $v'),
    };
  }

  static List<BoxShadow> shadow(
    SolarTheme t,
    String cell,
    SolarAllDayBarProps p,
    Set<WidgetState> s,
  ) => switch (lookup(cell, p, s)) {
    'none' || null => const <BoxShadow>[],

    final v => throw StateError('$cell: no shadow for $v'),
  };

  /// A length in logical pixels, or null where the IR says the layer hugs its content.
  static double? dimension(
    String cell,
    SolarAllDayBarProps p,
    Set<WidgetState> s,
  ) {
    final v = lookup(cell, p, s);
    if (v == null || v == 'k:HUG') return null;
    if (v == 'k:FILL') return double.infinity;
    if (v.startsWith('px:')) return double.parse(v.substring(3));
    return switch (v) {
      't:inset.none' => SolarInset.none,
      't:inset.sm' => SolarInset.sm,
      't:radius.control' => SolarRadius.control,
      't:radius.none' => SolarRadius.none,
      't:radius.pill' => SolarRadius.pill,
      't:stack.xs' => SolarStack.xs,
      _ => throw StateError('$cell: no length for $v'),
    };
  }

  static TextStyle? textStyle(
    SolarTheme t,
    String cell,
    SolarAllDayBarProps p,
    Set<WidgetState> s,
  ) => switch (lookup(cell, p, s)) {
    'none' || null => null,
    't:typography.body.sm.medium' => t.typography.bodySmMedium,
    't:typography.body.sm.semibold' => t.typography.bodySmSemibold,
    final v => throw StateError('$cell: no text style for $v'),
  };

  /// Whether a layer is drawn: the shell reads this, the style does not.
  static bool present(
    String layer,
    SolarAllDayBarProps p,
    Set<WidgetState> s,
  ) => lookup('$layer.present', p, s) != 'b:false';
}
