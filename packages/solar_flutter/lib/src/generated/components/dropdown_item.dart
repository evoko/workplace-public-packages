// SOLAR Dropdown Item recipe for Flutter. Generated by @bwp-web/codegen from spec/components/dropdown item.json. Do not edit.
//
// The recipe is data: token *names*, keyed by layer and cell and by the base, size, appearance
// and combined sections of the IR. Values resolve against the ambient SolarTheme, so Light and
// Dark switch with the theme and no colour here is a literal.

import 'package:flutter/foundation.dart' show immutable;
import 'package:flutter/material.dart'
    show BoxShadow, Color, Colors, TextStyle, WidgetState;

import '../tokens.dart';

enum SolarDropdownItemSize { md, sm }

/// The props a SOLAR Dropdown Item takes. Hover, pressed and focus are not here: they are
/// platform states, tracked by Flutter as [WidgetState]s.
@immutable
class SolarDropdownItemProps {
  const SolarDropdownItemProps({
    this.size = SolarDropdownItemSize.md,
    this.selected = false,
    this.disabled = false,
  });

  final SolarDropdownItemSize size;
  final bool selected;
  final bool disabled;
}

abstract final class SolarDropdownItemRecipe {
  /// Every IR entry, as `<layer>.<cell>|<section>…` to a token name (`t:`), a keyword (`k:`),
  /// `none`, a presence (`b:`) or a literal the overlay allowed (`px:`).
  static const Map<String, String> cells = {
    'root.present|base': 'b:true',
    'root.background|base': 'none',
    'root.borderColor|base': 'none',
    'root.shadow|base': 'none',
    'root.radius|base': 'none',
    'root.borderWidth|base': 'none',
    'root.direction|base': 'k:HORIZONTAL',
    'root.align|base': 'k:MIN/CENTER',
    'root.gap|base': 't:inset.xs',
    'root.paddingTop|base': 't:inset.sm',
    'root.paddingRight|base': 't:inset.sm',
    'root.paddingBottom|base': 't:inset.sm',
    'root.paddingLeft|base': 't:inset.sm',
    'root.width|base': 'k:FILL',
    'root.height|base': 'k:HUG',
    'root.paddingTop|size|sm': 't:inset.xs',
    'root.paddingBottom|size|sm': 't:inset.xs',
    'root.width|size|sm': 'k:FILL',
    'root.background|appearance|default|hover': 't:color.surface.hover',
    'root.background|appearance|default|selected': 't:color.surface.active',
    'checkbox.present|base': 'b:true',
    'checkbox.component|base': 'k:Checkbox',
    'checkbox.variant.checked|base': 'k:false',
    'checkbox.variant.disabled|base': 'k:false',
    'checkbox.variant.hover|base': 'k:false',
    'checkbox.variant.mixed|base': 'k:false',
    'checkbox.variant.focus|base': 'k:false',
    'checkbox.width|base': 'none',
    'checkbox.height|base': 'none',
    'checkbox.variant.checked|appearance|default|selected': 'k:true',
    'checkbox.variant.disabled|appearance|default|disabled': 'k:true',
    'checkbox.variant.hover|appearance|default|hover': 'k:true',
    'icon.present|base': 'b:true',
    'icon.component|base': 'k:Icon/None',
    'icon.variant.solid|base': 'k:false',
    'icon.width|base': 't:icon.md',
    'icon.height|base': 't:icon.md',
    'icon.color|base': 't:color.icon.primary',
    'icon.width|size|sm': 't:icon.sm',
    'icon.height|size|sm': 't:icon.sm',
    'icon.color|appearance|default|disabled': 't:color.icon.disabled',
    'content.present|base': 'b:true',
    'content.background|base': 'none',
    'content.borderColor|base': 'none',
    'content.shadow|base': 'none',
    'content.radius|base': 'none',
    'content.borderWidth|base': 'none',
    'content.direction|base': 'k:VERTICAL',
    'content.align|base': 'k:MIN/MIN',
    'content.gap|base': 't:inset.xs',
    'content.paddingTop|base': 't:inset.none',
    'content.paddingRight|base': 't:inset.none',
    'content.paddingBottom|base': 't:inset.none',
    'content.paddingLeft|base': 't:inset.none',
    'content.width|base': 'k:FILL',
    'content.height|base': 'k:HUG',
    'label.present|base': 'b:true',
    'label.color|base': 't:color.text.primary',
    'label.typography|base': 't:typography.body.md.medium',
    'label.typography|size|sm': 't:typography.body.sm.medium',
    'label.color|appearance|default|disabled': 't:color.text.disabled',
    'helper.present|base': 'b:true',
    'helper.color|base': 't:color.text.secondary',
    'helper.typography|base': 't:typography.body.sm.regular',
    'helper.color|appearance|default|disabled': 't:color.text.disabled',
  };

  /// Which state wins when several hold, highest first: the MUI recipe's cascade, read backwards.
  static const List<String> statePrecedence = ['disabled', 'selected', 'hover'];

  static bool _holds(
    String state,
    SolarDropdownItemProps p,
    Set<WidgetState> s,
  ) => switch (state) {
    'disabled' => p.disabled || s.contains(WidgetState.disabled),
    'selected' => p.selected,
    'hover' =>
      s.contains(WidgetState.hovered) || s.contains(WidgetState.focused),
    _ => false,
  };

  /// The entry for one cell under these props and states, by the IR's precedence: a state that
  /// holds beats the resting value, the per-size-and-appearance entry beats the per-appearance
  /// one, and the resting value falls back through appearance, size and base.
  static String? lookup(
    String cell,
    SolarDropdownItemProps p,
    Set<WidgetState> s,
  ) {
    const combo = 'default';
    final size = p.size.name;
    for (final state in statePrecedence) {
      if (!_holds(state, p, s)) continue;
      final hit =
          cells['$cell|combined|$size|$combo|$state'] ??
          cells['$cell|appearance|$combo|$state'];
      if (hit != null) return hit;
    }
    return cells['$cell|combined|$size|$combo|default'] ??
        cells['$cell|appearance|$combo|default'] ??
        cells['$cell|size|$size'] ??
        cells['$cell|base'];
  }

  static Color color(
    SolarTheme t,
    String cell,
    SolarDropdownItemProps p,
    Set<WidgetState> s,
  ) {
    final c = t.colors;
    return switch (lookup(cell, p, s)) {
      'none' => Colors.transparent,
      't:color.icon.disabled' => c.iconDisabled,
      't:color.icon.primary' => c.iconPrimary,
      't:color.surface.active' => c.surfaceActive,
      't:color.surface.hover' => c.surfaceHover,
      't:color.text.disabled' => c.textDisabled,
      't:color.text.primary' => c.textPrimary,
      't:color.text.secondary' => c.textSecondary,
      final v => throw StateError('$cell: no colour for $v'),
    };
  }

  static List<BoxShadow> shadow(
    SolarTheme t,
    String cell,
    SolarDropdownItemProps p,
    Set<WidgetState> s,
  ) => switch (lookup(cell, p, s)) {
    'none' || null => const <BoxShadow>[],

    final v => throw StateError('$cell: no shadow for $v'),
  };

  /// A length in logical pixels, or null where the IR says the layer hugs its content.
  static double? dimension(
    String cell,
    SolarDropdownItemProps p,
    Set<WidgetState> s,
  ) {
    final v = lookup(cell, p, s);
    if (v == null || v == 'k:HUG') return null;
    if (v == 'k:FILL') return double.infinity;
    if (v.startsWith('px:')) return double.parse(v.substring(3));
    return switch (v) {
      't:icon.md' => SolarIconSize.md,
      't:icon.sm' => SolarIconSize.sm,
      't:inset.none' => SolarInset.none,
      't:inset.sm' => SolarInset.sm,
      't:inset.xs' => SolarInset.xs,
      _ => throw StateError('$cell: no length for $v'),
    };
  }

  static TextStyle? textStyle(
    SolarTheme t,
    String cell,
    SolarDropdownItemProps p,
    Set<WidgetState> s,
  ) => switch (lookup(cell, p, s)) {
    'none' || null => null,
    't:typography.body.md.medium' => t.typography.bodyMdMedium,
    't:typography.body.sm.medium' => t.typography.bodySmMedium,
    't:typography.body.sm.regular' => t.typography.bodySmRegular,
    final v => throw StateError('$cell: no text style for $v'),
  };

  /// Whether a layer is drawn: the shell reads this, the style does not.
  static bool present(
    String layer,
    SolarDropdownItemProps p,
    Set<WidgetState> s,
  ) => lookup('$layer.present', p, s) != 'b:false';
}
