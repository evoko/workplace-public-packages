// SOLAR Option Row recipe for Flutter. Generated by @bwp-web/codegen from spec/components/option row.json. Do not edit.
//
// The recipe is data: token *names*, keyed by layer and cell and by the base, size, appearance
// and combined sections of the IR. Values resolve against the ambient SolarTheme, so Light and
// Dark switch with the theme and no colour here is a literal.

import 'package:flutter/foundation.dart' show immutable;
import 'package:flutter/material.dart'
    show BoxShadow, Color, Colors, TextStyle, WidgetState;

import '../tokens.dart';

enum SolarOptionRowControl { checkbox, radio, toggle }

/// The props a SOLAR Option Row takes. Hover, pressed and focus are not here: they are
/// platform states, tracked by Flutter as [WidgetState]s.
@immutable
class SolarOptionRowProps {
  const SolarOptionRowProps({this.control = SolarOptionRowControl.checkbox});

  final SolarOptionRowControl control;
}

abstract final class SolarOptionRowRecipe {
  /// Every IR entry, as `<layer>.<cell>|<section>…` to a token name (`t:`), a keyword (`k:`),
  /// `none`, a presence (`b:`) or a literal the overlay allowed (`px:`).
  static const Map<String, String> cells = {
    'root.present|base': 'b:true',
    'root.background|base': 'none',
    'root.borderColor|base': 'none',
    'root.shadow|base': 'none',
    'root.radius|base': 'none',
    'root.borderWidth|base': 'none',
    'root.direction|base': 'k:HORIZONTAL',
    'root.align|base': 'k:MIN/MIN',
    'root.gap|base': 't:inset.xs',
    'root.paddingTop|base': 't:inset.sm',
    'root.paddingRight|base': 't:inset.none',
    'root.paddingBottom|base': 't:inset.sm',
    'root.paddingLeft|base': 't:inset.none',
    'root.width|base': 'k:FILL',
    'root.height|base': 'k:HUG',
    'control.present|base': 'b:true',
    'control.component|base': 'k:Checkbox',
    'control.variant.checked|base': 'k:false',
    'control.variant.disabled|base': 'k:false',
    'control.variant.hover|base': 'k:false',
    'control.variant.mixed|base': 'k:false',
    'control.variant.focus|base': 'k:false',
    'control.width|base': 'none',
    'control.height|base': 'none',
    'control.present|appearance|control=toggle|default': 'b:false',
    'control.component|appearance|control=radio|default': 'k:Radio',
    'control.variant.state|appearance|control=radio|default': 'k:default',
    'text.present|base': 'b:true',
    'text.background|base': 'none',
    'text.borderColor|base': 'none',
    'text.shadow|base': 'none',
    'text.radius|base': 'none',
    'text.borderWidth|base': 'none',
    'text.direction|base': 'k:VERTICAL',
    'text.align|base': 'k:CENTER/MIN',
    'text.gap|base': 't:stack.xs',
    'text.paddingTop|base': 't:inset.none',
    'text.paddingRight|base': 't:inset.none',
    'text.paddingBottom|base': 't:inset.none',
    'text.paddingLeft|base': 't:inset.none',
    'text.width|base': 'k:FILL',
    'text.height|base': 'k:HUG',
    'label.present|base': 'b:true',
    'label.color|base': 't:color.text.primary',
    'label.typography|base': 't:typography.label.md',
    'label.width|base': 'k:FILL',
    'supportingText.present|base': 'b:true',
    'supportingText.color|base': 't:color.text.secondary',
    'supportingText.typography|base': 't:typography.body.sm.regular',
    'supportingText.width|base': 'k:FILL',
    'toggle.present|base': 'b:false',
    'toggle.component|base': 'k:Toggle',
    'toggle.width|base': 'none',
    'toggle.height|base': 'none',
    'toggle.present|appearance|control=toggle|default': 'b:true',
    'toggle.variant.selected|appearance|control=toggle|default': 'k:false',
    'toggle.variant.state|appearance|control=toggle|default': 'k:default',
  };

  /// Each layer's children, in Figma's order: the tree the widget draws (SolarLayers).
  static const Map<String, List<String>> tree = {
    'root': ['control', 'text', 'toggle'],
    'text': ['label', 'supportingText'],
  };

  /// Which state wins when several hold, highest first: the MUI recipe's cascade, read backwards.
  static const List<String> statePrecedence = <String>[];

  static bool _holds(String state, SolarOptionRowProps p, Set<WidgetState> s) =>
      switch (state) {
        _ => false,
      };

  /// The entry for one cell under these props and states, by the IR's precedence: a state that
  /// holds beats the resting value, the per-size-and-appearance entry beats the per-appearance
  /// one, and the resting value falls back through appearance, size and base.
  static String? lookup(
    String cell,
    SolarOptionRowProps p,
    Set<WidgetState> s,
  ) {
    final combo = 'control=${p.control.name}';
    const size = '';
    for (final state in statePrecedence) {
      if (!_holds(state, p, s)) continue;
      final hit =
          cells['$cell|combined|$size|$combo|$state'] ??
          cells['$cell|appearance|$combo|$state'];
      if (hit != null) return hit;
    }
    return cells['$cell|combined|$size|$combo|default'] ??
        cells['$cell|appearance|$combo|default'] ??
        cells['$cell|size|$size'] ??
        cells['$cell|base'];
  }

  static Color color(
    SolarTheme t,
    String cell,
    SolarOptionRowProps p,
    Set<WidgetState> s,
  ) {
    final c = t.colors;
    return switch (lookup(cell, p, s)) {
      'none' => Colors.transparent,
      't:color.text.primary' => c.textPrimary,
      't:color.text.secondary' => c.textSecondary,
      final v => throw StateError('$cell: no colour for $v'),
    };
  }

  static List<BoxShadow> shadow(
    SolarTheme t,
    String cell,
    SolarOptionRowProps p,
    Set<WidgetState> s,
  ) => switch (lookup(cell, p, s)) {
    'none' || null => const <BoxShadow>[],

    final v => throw StateError('$cell: no shadow for $v'),
  };

  /// A length in logical pixels, or null where the IR says the layer hugs its content.
  static double? dimension(
    String cell,
    SolarOptionRowProps p,
    Set<WidgetState> s,
  ) {
    final v = lookup(cell, p, s);
    if (v == null || v == 'k:HUG') return null;
    if (v == 'k:FILL') return double.infinity;
    if (v.startsWith('px:')) return double.parse(v.substring(3));
    return switch (v) {
      't:inset.none' => SolarInset.none,
      't:inset.sm' => SolarInset.sm,
      't:inset.xs' => SolarInset.xs,
      't:stack.xs' => SolarStack.xs,
      _ => throw StateError('$cell: no length for $v'),
    };
  }

  static TextStyle? textStyle(
    SolarTheme t,
    String cell,
    SolarOptionRowProps p,
    Set<WidgetState> s,
  ) => switch (lookup(cell, p, s)) {
    'none' || null => null,
    't:typography.body.sm.regular' => t.typography.bodySmRegular,
    't:typography.label.md' => t.typography.labelMd,
    final v => throw StateError('$cell: no text style for $v'),
  };

  /// Whether a layer is drawn: the shell reads this, the style does not.
  static bool present(
    String layer,
    SolarOptionRowProps p,
    Set<WidgetState> s,
  ) => lookup('$layer.present', p, s) != 'b:false';
}
