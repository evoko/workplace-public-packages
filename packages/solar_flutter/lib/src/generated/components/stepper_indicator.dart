// SOLAR Stepper Indicator recipe for Flutter. Generated by @bwp-web/codegen from spec/components/stepper indicator.json. Do not edit.
//
// The recipe is data: token *names*, keyed by layer and cell and by the base, size, appearance
// and combined sections of the IR. Values resolve against the ambient SolarTheme, so Light and
// Dark switch with the theme and no colour here is a literal.

import 'package:flutter/foundation.dart' show immutable;
import 'package:flutter/material.dart'
    show BoxShadow, Color, Colors, TextStyle, WidgetState;

import '../tokens.dart';

enum SolarStepperIndicatorStatus { completed, active, upcoming, error }

/// The props a SOLAR Stepper Indicator takes. Hover, pressed and focus are not here: they are
/// platform states, tracked by Flutter as [WidgetState]s.
@immutable
class SolarStepperIndicatorProps {
  const SolarStepperIndicatorProps({
    this.status = SolarStepperIndicatorStatus.completed,
  });

  final SolarStepperIndicatorStatus status;
}

abstract final class SolarStepperIndicatorRecipe {
  /// Every IR entry, as `<layer>.<cell>|<section>…` to a token name (`t:`), a keyword (`k:`),
  /// `none`, a presence (`b:`) or a literal the overlay allowed (`px:`).
  static const Map<String, String> cells = {
    'root.present|base': 'b:true',
    'root.background|base': 't:color.surface.inverse',
    'root.borderColor|base': 'none',
    'root.shadow|base': 'none',
    'root.radius|base': 't:radius.pill',
    'root.borderWidth|base': 'none',
    'root.direction|base': 'k:HORIZONTAL',
    'root.align|base': 'k:CENTER/CENTER',
    'root.gap|base': 't:inset.none',
    'root.paddingTop|base': 't:inset.none',
    'root.paddingRight|base': 't:inset.none',
    'root.paddingBottom|base': 't:inset.none',
    'root.paddingLeft|base': 't:inset.none',
    'root.width|base': 'px:24',
    'root.height|base': 'px:24',
    'root.background|appearance|status=upcoming|default':
        't:color.surface.base',
    'root.borderColor|appearance|status=upcoming|default':
        't:color.border.medium',
    'root.borderWidth|appearance|status=upcoming|default': 't:border.default',
    'root.background|appearance|status=error|default':
        't:color.surface.feedback.danger.strong',
    'root.borderColor|appearance|status=error|default': 't:color.border.medium',
    'root.borderWidth|appearance|status=error|default': 't:border.default',
    'iconCheck.present|base': 'b:true',
    'iconCheck.component|base': 'k:Icon/Check',
    'iconCheck.variant.solid|base': 'k:false',
    'iconCheck.width|base': 't:icon.sm',
    'iconCheck.height|base': 't:icon.sm',
    'iconCheck.color|base': 't:color.icon.inverse',
    'iconCheck.present|appearance|status=active|default': 'b:false',
    'iconCheck.present|appearance|status=upcoming|default': 'b:false',
    'iconCheck.present|appearance|status=error|default': 'b:false',
    'number.present|base': 'b:false',
    'number.typography|base': 't:typography.body.sm.semibold',
    'number.present|appearance|status=active|default': 'b:true',
    'number.color|appearance|status=active|default': 't:color.text.inverse',
    'number.present|appearance|status=upcoming|default': 'b:true',
    'number.color|appearance|status=upcoming|default': 't:color.text.secondary',
    'icon.present|base': 'b:false',
    'icon.typography|base': 't:typography.body.sm.semibold',
    'icon.present|appearance|status=error|default': 'b:true',
    'icon.color|appearance|status=error|default': 't:color.icon.inverse',
  };

  /// Each layer's children, in Figma's order: the tree the widget draws (SolarLayers).
  static const Map<String, List<String>> tree = {
    'root': ['iconCheck', 'number', 'icon'],
  };

  /// Which state wins when several hold, highest first: the MUI recipe's cascade, read backwards.
  static const List<String> statePrecedence = <String>[];

  static bool _holds(
    String state,
    SolarStepperIndicatorProps p,
    Set<WidgetState> s,
  ) => switch (state) {
    _ => false,
  };

  /// The entry for one cell under these props and states, by the IR's precedence: a state that
  /// holds beats the resting value, the per-size-and-appearance entry beats the per-appearance
  /// one, and the resting value falls back through appearance, size and base.
  static String? lookup(
    String cell,
    SolarStepperIndicatorProps p,
    Set<WidgetState> s,
  ) {
    final combo = 'status=${p.status.name}';
    const size = '';
    for (final state in statePrecedence) {
      if (!_holds(state, p, s)) continue;
      final hit =
          cells['$cell|combined|$size|$combo|$state'] ??
          cells['$cell|appearance|$combo|$state'];
      if (hit != null) return hit;
    }
    return cells['$cell|combined|$size|$combo|default'] ??
        cells['$cell|appearance|$combo|default'] ??
        cells['$cell|size|$size'] ??
        cells['$cell|base'];
  }

  static Color color(
    SolarTheme t,
    String cell,
    SolarStepperIndicatorProps p,
    Set<WidgetState> s,
  ) {
    final c = t.colors;
    return switch (lookup(cell, p, s)) {
      'none' => Colors.transparent,
      't:color.border.medium' => c.borderMedium,
      't:color.icon.inverse' => c.iconInverse,
      't:color.surface.base' => c.surfaceBase,
      't:color.surface.feedback.danger.strong' => c.surfaceFeedbackDangerStrong,
      't:color.surface.inverse' => c.surfaceInverse,
      't:color.text.inverse' => c.textInverse,
      't:color.text.secondary' => c.textSecondary,
      final v => throw StateError('$cell: no colour for $v'),
    };
  }

  static List<BoxShadow> shadow(
    SolarTheme t,
    String cell,
    SolarStepperIndicatorProps p,
    Set<WidgetState> s,
  ) => switch (lookup(cell, p, s)) {
    'none' || null => const <BoxShadow>[],

    final v => throw StateError('$cell: no shadow for $v'),
  };

  /// A length in logical pixels, or null where the IR says the layer hugs its content.
  static double? dimension(
    String cell,
    SolarStepperIndicatorProps p,
    Set<WidgetState> s,
  ) {
    final v = lookup(cell, p, s);
    if (v == null || v == 'k:HUG') return null;
    if (v == 'k:FILL') return double.infinity;
    if (v.startsWith('px:')) return double.parse(v.substring(3));
    return switch (v) {
      't:border.default' => SolarBorder.$default,
      't:icon.sm' => SolarIconSize.sm,
      't:inset.none' => SolarInset.none,
      't:radius.pill' => SolarRadius.pill,
      _ => throw StateError('$cell: no length for $v'),
    };
  }

  static TextStyle? textStyle(
    SolarTheme t,
    String cell,
    SolarStepperIndicatorProps p,
    Set<WidgetState> s,
  ) => switch (lookup(cell, p, s)) {
    'none' || null => null,
    't:typography.body.sm.semibold' => t.typography.bodySmSemibold,
    final v => throw StateError('$cell: no text style for $v'),
  };

  /// Whether a layer is drawn: the shell reads this, the style does not.
  static bool present(
    String layer,
    SolarStepperIndicatorProps p,
    Set<WidgetState> s,
  ) => lookup('$layer.present', p, s) != 'b:false';
}
