analyzer:
  exclude:
    - build/**
    # Packages of their own, each analysed with its own dependencies.
    - variants/**
    - widgetbook/**
include: package:flutter_lints/flutter.yaml

linter:
  rules:
    prefer_const_constructors: true
    prefer_const_declarations: true
