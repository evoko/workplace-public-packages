// The Flutter registry: a case per generated component, by its Figma name. The check requires one
// for every oracle under spec/verify/, so a new component cannot go unmeasured.

import '../harness.dart';
import 'button.dart';
import 'button_group.dart';
import 'icon_button.dart';
import 'spinner.dart';

final cases = <String, VisualCase>{
  'Button': buttonCase,
  'Button Group': buttonGroupCase,
  'Icon Button': iconButtonCase,
  'Spinner': spinnerCase,
};
