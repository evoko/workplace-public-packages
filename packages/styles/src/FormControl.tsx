import MuiFormControl from '@mui/material/FormControl';
import type { FormControlProps } from '@mui/material/FormControl';
import MuiFormControlLabel from '@mui/material/FormControlLabel';
import type { FormControlLabelProps } from '@mui/material/FormControlLabel';
import MuiFormGroup from '@mui/material/FormGroup';
import type { FormGroupProps } from '@mui/material/FormGroup';
import MuiFormHelperText from '@mui/material/FormHelperText';
import type { FormHelperTextProps } from '@mui/material/FormHelperText';
import MuiFormLabel from '@mui/material/FormLabel';
import type { FormLabelProps } from '@mui/material/FormLabel';

const FormControl = MuiFormControl;
const FormControlLabel = MuiFormControlLabel;
const FormGroup = MuiFormGroup;
const FormHelperText = MuiFormHelperText;
const FormLabel = MuiFormLabel;

export { FormControl, FormControlLabel, FormGroup, FormHelperText, FormLabel };
export type {
  FormControlProps,
  FormControlLabelProps,
  FormGroupProps,
  FormHelperTextProps,
  FormLabelProps,
};
