// SOLAR Inline Input recipe for MUI. Generated by @bwp-web/codegen from spec/components/inline input.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export interface SolarInlineInputProps {
  error?: boolean;
  disabled?: boolean;
}

/** The props and what the shell derives from its content (the recipe is keyed by both). */
export interface SolarInlineInputRecipeProps extends SolarInlineInputProps {
  filled?: boolean;
}

export const solarInlineInputDefaults = {
  error: false,
  disabled: false,
  filled: false,
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarInlineInputStyles = {
  reset: {
    display: 'inline-flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarInlineInput-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarInlineInput-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarInlineInput-glyph': {
      display: 'block',
      overflow: 'visible',
      flexShrink: '0',
    },
    '& .SolarInlineInput-drawnIcon': {
      flexShrink: '0',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
    cursor: 'text',
    '&.SolarInlineInput-editing, &.SolarInlineInput-disabled': {
      cursor: 'default',
    },
    '& .SolarInlineInput-value': {
      flex: '1 1 0%',
      minWidth: '0',
      overflow: 'hidden',
      textOverflow: 'ellipsis',
    },
    '& .SolarInlineInput-words': {
      display: 'contents',
    },
    '& .SolarInlineInput-value.MuiInputBase-input': {
      height: 'auto',
      padding: '0',
      WebkitTextFillColor: 'currentcolor',
    },
    '& .SolarInlineInput-iconButton': {
      visibility: 'hidden',
    },
    '&:hover .SolarInlineInput-iconButton, & .SolarInlineInput-iconButton:focus-within':
      {
        visibility: 'visible',
      },
  },
  root: {
    backgroundColor: 'transparent',
    borderColor: 'transparent',
    boxShadow: 'none',
    borderRadius: 'var(--solar-radius-none)',
    borderStyle: 'none',
    flexDirection: 'row',
    justifyContent: 'flex-start',
    alignItems: 'center',
    gap: 'var(--solar-inset-xs)',
    paddingTop: 'var(--solar-inset-none)',
    paddingRight: 'var(--solar-inset-sm)',
    paddingBottom: 'var(--solar-inset-none)',
    paddingLeft: 'var(--solar-inset-sm)',
    width: '100%',
    height: '40px',
    '& .SolarInlineInput-value': {
      color: 'var(--solar-color-text-primary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-400)',
      fontSize: 'var(--solar-type-size-body-md)',
      lineHeight: 'var(--solar-type-line-height-body-md)',
      letterSpacing: '-0.02286em',
      textDecoration: 'none',
      width: '100%',
    },
    '& .SolarInlineInput-frame1': {
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'row',
      justifyContent: 'flex-start',
      alignItems: 'center',
      gap: 'var(--solar-inset-none)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
    },
  },
  sizes: {},
  appearances: {
    default: {
      '&:hover': {
        backgroundColor: 'var(--solar-color-surface-hover)',
        borderRadius: 'var(--solar-radius-container)',
        paddingTop: 'var(--solar-inset-2xs)',
        paddingRight: 'var(--solar-inset-2xs)',
        paddingBottom: 'var(--solar-inset-2xs)',
      },
      '&:has(.SolarInlineInput-words.Mui-focused)': {
        backgroundColor: 'var(--solar-color-surface-base)',
        borderColor: 'var(--solar-color-border-feedback-focus-strong)',
        boxShadow: 'var(--solar-shadow-focus-default)',
        borderRadius: 'var(--solar-radius-control)',
        borderWidth: 'var(--solar-border-default)',
        borderStyle: 'solid',
        paddingTop: 'var(--solar-inset-2xs)',
        paddingRight: 'var(--solar-inset-2xs)',
        paddingBottom: 'var(--solar-inset-2xs)',
        '& .SolarInlineInput-frame1': {
          backgroundColor: 'transparent',
          borderColor: 'transparent',
          boxShadow: 'none',
        },
      },
      '&.SolarInlineInput-filled': {
        backgroundColor: 'var(--solar-color-surface-base)',
        borderColor: 'var(--solar-color-border-subtle)',
        boxShadow: 'var(--solar-shadow-control)',
        borderRadius: 'var(--solar-radius-control)',
        borderWidth: 'var(--solar-border-default)',
        borderStyle: 'solid',
        paddingTop: 'var(--solar-inset-2xs)',
        paddingRight: 'var(--solar-inset-2xs)',
        paddingBottom: 'var(--solar-inset-2xs)',
        '& .SolarInlineInput-frame1': {
          backgroundColor: 'transparent',
          borderColor: 'transparent',
          boxShadow: 'none',
        },
      },
      '&.SolarInlineInput-error': {
        backgroundColor: 'var(--solar-color-surface-base)',
        borderColor: 'var(--solar-color-border-feedback-danger-strong)',
        boxShadow: 'var(--solar-shadow-danger)',
        borderRadius: 'var(--solar-radius-control)',
        borderWidth: 'var(--solar-border-default)',
        borderStyle: 'solid',
        paddingTop: 'var(--solar-inset-2xs)',
        paddingRight: 'var(--solar-inset-2xs)',
        paddingBottom: 'var(--solar-inset-2xs)',
        '& .SolarInlineInput-frame1': {
          backgroundColor: 'transparent',
          borderColor: 'transparent',
          boxShadow: 'none',
        },
      },
      '&.SolarInlineInput-disabled': {
        '& .SolarInlineInput-value': {
          color: 'var(--solar-color-text-disabled)',
        },
      },
    },
  },
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarInlineInputComposition = {
  root: {
    base: {
      present: true,
    },
  },
  value: {
    base: {
      present: true,
    },
  },
  iconButton: {
    base: {
      present: false,
      component: 'Icon Button',
      'variant.size': 'sm',
    },
    appearance: {
      default: {
        hover: {
          present: true,
          'variant.shape': 'square',
          'variant.prio': 'tertiary',
          'variant.state': 'default',
        },
      },
    },
  },
  frame1: {
    base: {
      present: false,
    },
    appearance: {
      default: {
        focus: {
          present: true,
        },
        filled: {
          present: true,
        },
        error: {
          present: true,
        },
      },
    },
  },
  confirm: {
    base: {
      present: false,
      component: 'Icon Button',
      'variant.size': 'sm',
    },
    appearance: {
      default: {
        focus: {
          present: true,
          'variant.shape': 'square',
          'variant.prio': 'tertiary',
          'variant.state': 'default',
        },
        filled: {
          present: true,
          'variant.shape': 'square',
          'variant.prio': 'tertiary',
          'variant.state': 'default',
        },
        error: {
          present: true,
          'variant.shape': 'square',
          'variant.prio': 'tertiary',
          'variant.state': 'default',
        },
      },
    },
  },
  cancel: {
    base: {
      present: false,
      component: 'Icon Button',
      'variant.size': 'sm',
    },
    appearance: {
      default: {
        focus: {
          present: true,
          'variant.shape': 'square',
          'variant.prio': 'tertiary',
          'variant.state': 'default',
        },
        filled: {
          present: true,
          'variant.shape': 'square',
          'variant.prio': 'tertiary',
          'variant.state': 'default',
        },
        error: {
          present: true,
          'variant.shape': 'square',
          'variant.prio': 'tertiary',
          'variant.state': 'default',
        },
      },
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarInlineInputStyle(
  props: SolarInlineInputRecipeProps = {},
): Style {
  const p: Record<string, unknown> = { ...solarInlineInputDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `default`;
  const s = solarInlineInputStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  return merge(s.reset, s.root, s.appearances[key]);
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarInlineInputParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarInlineInputParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarInlineInputCompose(
  props: SolarInlineInputRecipeProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarInlineInputDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `default`;
  const size = '';
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarInlineInputComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}
