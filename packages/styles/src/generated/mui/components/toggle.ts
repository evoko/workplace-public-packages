// SOLAR Toggle recipe for MUI. Generated by @bwp-web/codegen from spec/components/toggle.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export interface SolarToggleProps {
  selected?: boolean;
  disabled?: boolean;
}

export const solarToggleDefaults = {
  selected: false,
  disabled: false,
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarToggleStyles = {
  reset: {
    display: 'inline-flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarToggle-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarToggle-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarToggle-glyph': {
      display: 'block',
      overflow: 'visible',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
    padding: '0',
    overflow: 'visible',
    '& .MuiSwitch-track': {
      display: 'none',
    },
    '& .MuiSwitch-switchBase, & .MuiSwitch-switchBase.Mui-checked': {
      position: 'absolute',
      inset: '0',
      padding: '0',
      transform: 'none',
      backgroundColor: 'transparent',
    },
    '& .MuiSwitch-switchBase .MuiSwitch-input': {
      top: '50%',
      left: '50%',
      width: 'max(100%, 44px)',
      height: 'max(100%, 44px)',
      transform: 'translate(-50%, -50%)',
    },
  },
  root: {
    backgroundColor: 'var(--solar-color-surface-muted)',
    borderColor: 'var(--solar-color-border-subtle)',
    boxShadow: 'none',
    borderRadius: 'var(--solar-radius-pill)',
    borderWidth: 'var(--solar-border-default)',
    borderStyle: 'solid',
    '--solar-placed-left': 'var(--solar-border-default)',
    '--solar-placed-top': 'var(--solar-border-default)',
    width: '32px',
    height: '18px',
    '& .SolarToggle-thumb': {
      backgroundColor: 'var(--solar-color-action-primary-icon-default)',
      borderColor: 'var(--solar-color-border-subtle)',
      boxShadow: 'var(--solar-shadow-control)',
      borderRadius: 'var(--solar-radius-pill)',
      borderWidth: 'var(--solar-border-default)',
      borderStyle: 'solid',
      width: '12px',
      height: '12px',
      position: 'absolute',
      left: 'calc(3px - var(--solar-placed-left, 0px))',
      top: 'calc(3px - var(--solar-placed-top, 0px))',
    },
    position: 'relative',
  },
  sizes: {},
  appearances: {
    'selected=false': {
      '&:hover, .SolarStatesScope:hover &': {
        borderColor: 'var(--solar-color-border-medium)',
        '& .SolarToggle-thumb': {
          borderColor: 'var(--solar-color-border-medium)',
        },
      },
      '&:has(.Mui-focusVisible)': {
        boxShadow: 'var(--solar-shadow-focus-default)',
        borderColor: 'var(--solar-color-border-subtle)',
        '& .SolarToggle-thumb': {
          borderColor: 'var(--solar-color-border-subtle)',
        },
      },
      '&.SolarToggle-disabled': {
        '& .SolarToggle-thumb': {
          backgroundColor: 'var(--solar-color-action-primary-icon-disabled)',
          borderColor: 'transparent',
          borderStyle: 'none',
        },
      },
    },
    'selected=true': {
      backgroundColor: 'var(--solar-color-action-primary-bg-default)',
      '&:hover, .SolarStatesScope:hover &': {
        backgroundColor: 'var(--solar-color-action-primary-bg-hover)',
        borderColor: 'var(--solar-color-border-medium)',
        '& .SolarToggle-thumb': {
          borderColor: 'var(--solar-color-border-medium)',
          position: 'absolute',
          left: 'calc(17px - var(--solar-placed-left, 0px))',
        },
      },
      '&:has(.Mui-focusVisible)': {
        boxShadow: 'var(--solar-shadow-focus-default)',
        backgroundColor: 'var(--solar-color-action-primary-bg-default)',
        borderColor: 'var(--solar-color-border-subtle)',
        '& .SolarToggle-thumb': {
          borderColor: 'var(--solar-color-border-subtle)',
        },
      },
      '&.SolarToggle-disabled': {
        backgroundColor: 'var(--solar-color-action-primary-bg-disabled)',
        '& .SolarToggle-thumb': {
          backgroundColor: 'var(--solar-color-action-primary-icon-disabled)',
          borderColor: 'transparent',
          borderStyle: 'none',
          position: 'absolute',
          left: 'calc(17px - var(--solar-placed-left, 0px))',
        },
      },
      '& .SolarToggle-thumb': {
        position: 'absolute',
        left: 'calc(17px - var(--solar-placed-left, 0px))',
      },
    },
  },
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarToggleComposition = {
  root: {
    base: {
      present: true,
    },
  },
  thumb: {
    base: {
      present: true,
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarToggleStyle(props: SolarToggleProps = {}): Style {
  const p: Record<string, unknown> = { ...solarToggleDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `selected=${p.selected}`;
  const s = solarToggleStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  return merge(s.reset, s.root, s.appearances[key]);
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarToggleParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarToggleParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarToggleCompose(
  props: SolarToggleProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarToggleDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `selected=${p.selected}`;
  const size = '';
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarToggleComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}
