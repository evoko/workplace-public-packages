// SOLAR FAB recipe for MUI. Generated by @bwp-web/codegen from spec/components/fab.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export type SolarFABSize = 'sm' | 'md';
export type SolarFABType = 'icon' | 'extended';

export interface SolarFABProps {
  size?: SolarFABSize;
  disabled?: boolean;
  loading?: boolean;
}

/** The props and what the shell derives from its content (the recipe is keyed by both). */
export interface SolarFABRecipeProps extends SolarFABProps {
  type?: SolarFABType;
}

export const solarFABDefaults = {
  size: 'sm',
  disabled: false,
  loading: false,
  type: 'icon',
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarFABStyles = {
  reset: {
    minWidth: 'auto',
    textTransform: 'none',
    '& .MuiButton-startIcon': {
      margin: '0',
      flexShrink: '0',
    },
    '& .MuiButton-startIcon > svg': {
      width: '100%',
      height: '100%',
    },
    '&': {
      position: 'relative',
    },
    '&::after': {
      content: '""',
      position: 'absolute',
      top: '50%',
      left: '50%',
      width: 'max(100%, var(--solar-size-target-min))',
      height: 'max(100%, var(--solar-size-target-min))',
      transform: 'translate(-50%, -50%)',
    },
  },
  root: {
    backgroundColor: 'var(--solar-color-action-primary-bg-default)',
    borderColor: 'var(--solar-color-border-medium)',
    boxShadow: 'var(--solar-shadow-overlay)',
    borderRadius: 'var(--solar-radius-pill)',
    borderWidth: 'var(--solar-border-default)',
    borderStyle: 'solid',
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'center',
    gap: 'var(--solar-inset-none)',
    paddingTop: 'var(--solar-inset-sm)',
    paddingRight: 'var(--solar-inset-sm)',
    paddingBottom: 'var(--solar-inset-sm)',
    paddingLeft: 'var(--solar-inset-sm)',
    width: '44px',
    height: '44px',
    '& .MuiButton-startIcon': {
      width: 'var(--solar-icon-md)',
      minWidth: 'var(--solar-icon-md)',
      height: 'var(--solar-icon-md)',
      minHeight: 'var(--solar-icon-md)',
      color: 'var(--solar-color-action-primary-icon-default)',
    },
    fontFamily: 'var(--solar-type-font-family-inter)',
    fontWeight: 'var(--solar-type-font-weight-500)',
    fontSize: 'var(--solar-type-size-label-sm)',
    lineHeight: 'var(--solar-type-line-height-label-sm)',
    letterSpacing: '-0.02em',
    textDecoration: 'none',
  },
  sizes: {
    md: {
      paddingTop: 'var(--solar-inset-md)',
      paddingBottom: 'var(--solar-inset-md)',
      paddingLeft: 'var(--solar-inset-md)',
      height: '56px',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-label-md)',
      lineHeight: 'var(--solar-type-line-height-label-md)',
      letterSpacing: '-0.02em',
      textDecoration: 'none',
    },
  },
  appearances: {
    'type=icon': {
      '&:hover': {
        backgroundColor: 'var(--solar-color-action-primary-bg-hover)',
        '& .MuiButton-startIcon': {
          color: 'var(--solar-color-action-primary-icon-hover)',
        },
      },
      '&:active': {
        backgroundColor: 'var(--solar-color-action-primary-bg-active)',
        '& .MuiButton-startIcon': {
          color: 'var(--solar-color-action-primary-icon-active)',
        },
      },
      '&.Mui-focusVisible': {
        boxShadow: 'var(--solar-shadow-focus-default)',
      },
      '&.MuiButton-loading': {},
      '&.Mui-disabled:not(.MuiButton-loading)': {
        backgroundColor: 'var(--solar-color-action-primary-bg-disabled)',
        '& .MuiButton-startIcon': {
          color: 'var(--solar-color-action-primary-icon-disabled)',
        },
      },
    },
    'type=extended': {
      '&:hover': {
        backgroundColor: 'var(--solar-color-action-primary-bg-hover)',
        '& .MuiButton-startIcon': {
          color: 'var(--solar-color-action-primary-icon-hover)',
        },
        color: 'var(--solar-color-action-primary-text-default)',
      },
      '&:active': {
        backgroundColor: 'var(--solar-color-action-primary-bg-active)',
        '& .MuiButton-startIcon': {
          color: 'var(--solar-color-action-primary-icon-active)',
        },
        color: 'var(--solar-color-action-primary-text-default)',
      },
      '&.Mui-focusVisible': {
        boxShadow: 'var(--solar-shadow-focus-default)',
        color: 'var(--solar-color-action-primary-text-default)',
      },
      '&.MuiButton-loading': {
        color: 'var(--solar-color-action-primary-text-default)',
      },
      '&.Mui-disabled:not(.MuiButton-loading)': {
        backgroundColor: 'var(--solar-color-action-primary-bg-disabled)',
        '& .MuiButton-startIcon': {
          color: 'var(--solar-color-action-primary-icon-disabled)',
        },
        color: 'var(--solar-color-action-primary-text-disabled)',
      },
      color: 'var(--solar-color-action-primary-text-default)',
    },
  },
  combined: {
    sm: {
      'type=icon': {
        '&.Mui-focusVisible': {
          backgroundColor: 'var(--solar-color-action-primary-bg-default)',
          '& .MuiButton-startIcon': {
            color: 'var(--solar-color-action-primary-icon-default)',
          },
        },
      },
      'type=extended': {
        gap: 'var(--solar-stack-xs)',
        paddingRight: 'var(--solar-inset-lg)',
        width: 'auto',
        '&.Mui-focusVisible': {
          backgroundColor: 'var(--solar-color-action-primary-bg-default)',
          '& .MuiButton-startIcon': {
            color: 'var(--solar-color-action-primary-icon-default)',
          },
        },
      },
    },
    md: {
      'type=icon': {
        paddingRight: 'var(--solar-inset-md)',
        width: '56px',
        '&.Mui-focusVisible': {
          backgroundColor: 'var(--solar-color-action-primary-bg-default)',
          '& .MuiButton-startIcon': {
            color: 'var(--solar-color-action-primary-icon-default)',
          },
        },
      },
      'type=extended': {
        gap: 'var(--solar-stack-sm)',
        paddingRight: 'var(--solar-inset-xl)',
        width: 'auto',
        '&.Mui-focusVisible': {
          backgroundColor: 'var(--solar-color-action-primary-bg-default)',
          '& .MuiButton-startIcon': {
            color: 'var(--solar-color-action-primary-icon-default)',
          },
        },
        '& .MuiButton-startIcon': {
          width: 'var(--solar-icon-lg)',
          minWidth: 'var(--solar-icon-lg)',
          height: 'var(--solar-icon-lg)',
          minHeight: 'var(--solar-icon-lg)',
        },
      },
    },
  },
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarFABComposition = {
  root: {
    base: {
      present: true,
    },
  },
  icon: {
    base: {
      present: true,
      component: 'Icon/None',
      'variant.solid': 'false',
    },
    appearance: {
      'type=icon': {
        loading: {
          present: false,
        },
      },
      'type=extended': {
        loading: {
          present: false,
        },
      },
    },
  },
  spinner: {
    base: {
      present: false,
      component: 'Spinner',
      'variant.size': 'sm',
    },
    appearance: {
      'type=icon': {
        loading: {
          present: true,
          'variant.style': 'inverse',
        },
      },
      'type=extended': {
        loading: {
          present: true,
          'variant.style': 'inverse',
        },
      },
    },
    size: {
      md: {
        'variant.size': 'md',
      },
    },
  },
  label: {
    base: {
      present: false,
    },
    appearance: {
      'type=extended': {
        default: {
          present: true,
        },
        hover: {
          present: true,
        },
        pressed: {
          present: true,
        },
        disabled: {
          present: true,
        },
        focus: {
          present: true,
        },
        loading: {
          present: false,
        },
      },
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarFABStyle(props: SolarFABRecipeProps = {}): Style {
  const p: Record<string, unknown> = { ...solarFABDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `type=${p.type}`;
  const s = solarFABStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  const size = p.size as string;
  return merge(
    s.reset,
    s.root,
    s.sizes[size],
    s.appearances[key],
    s.combined[size]?.[key],
  );
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarFABParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarFABParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarFABCompose(
  props: SolarFABRecipeProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarFABDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `type=${p.type}`;
  const size = p.size as string;
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarFABComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}

/** Each layer's children, in Figma's order: the tree the shell draws (`drawChildren`). */
export const solarFABTree: Record<string, string[]> = {
  root: ['icon', 'spinner', 'label'],
};

/** Each slot's layer, to the slot it is: its class is `SolarFAB-<slot>`, public. */
export const solarFABSlots: Record<string, string> = {
  label: 'label',
  icon: 'icon',
};
