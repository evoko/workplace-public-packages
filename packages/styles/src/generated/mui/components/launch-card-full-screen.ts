// SOLAR Launch Card Full Screen recipe for MUI. Generated by @bwp-web/codegen from spec/components/launch card full screen.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export type SolarLaunchCardFullScreenProps = Record<never, never>;

export const solarLaunchCardFullScreenDefaults = {} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarLaunchCardFullScreenStyles = {
  reset: {
    display: 'flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarLaunchCardFullScreen-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarLaunchCardFullScreen--text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarLaunchCardFullScreen-glyph': {
      display: 'block',
      overflow: 'visible',
      flexShrink: '0',
    },
    '& .SolarLaunchCardFullScreen-drawnIcon': {
      flexShrink: '0',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
    '& .SolarLaunchCardFullScreen-name, & .SolarLaunchCardFullScreen-intro, & .SolarLaunchCardFullScreen--feature, & .SolarLaunchCardFullScreen--feature2, & .SolarLaunchCardFullScreen--feature3':
      {
        whiteSpace: 'normal',
        minWidth: '0',
      },
    '& .SolarLaunchCardFullScreen--image': {
      overflow: 'hidden',
      flexShrink: '0',
    },
    '& .SolarLaunchCardFullScreen--image > img': {
      width: '100%',
      height: '100%',
      objectFit: 'cover',
    },
    '& .SolarLaunchCardFullScreen-appIcon > img': {
      display: 'block',
      width: '100%',
      height: '100%',
    },
  },
  root: {
    backgroundColor: 'var(--solar-color-surface-raised)',
    borderColor: 'transparent',
    boxShadow: 'none',
    borderRadius: 'var(--solar-radius-dialog)',
    borderStyle: 'none',
    flexDirection: 'row',
    justifyContent: 'flex-start',
    alignItems: 'flex-start',
    gap: 'var(--solar-inset-2xl)',
    paddingTop: 'var(--solar-inset-lg)',
    paddingRight: 'var(--solar-inset-lg)',
    paddingBottom: 'var(--solar-inset-lg)',
    paddingLeft: 'var(--solar-inset-lg)',
    width: '100%',
    '& .SolarLaunchCardFullScreen--image': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-container)',
      borderStyle: 'none',
      width: '520px',
      height: '420px',
    },
    '& .SolarLaunchCardFullScreen--text': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'column',
      justifyContent: 'space-between',
      alignItems: 'flex-start',
      gap: 'var(--solar-inset-md)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
      width: '100%',
      height: '420px',
    },
    '& .SolarLaunchCardFullScreen--content': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'column',
      justifyContent: 'flex-start',
      alignItems: 'flex-start',
      gap: 'var(--solar-inset-md)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
      width: '100%',
    },
    '& .SolarLaunchCardFullScreen--headline': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'row',
      justifyContent: 'space-between',
      alignItems: 'center',
      gap: 'var(--solar-inset-md)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
      width: '100%',
    },
    '& .SolarLaunchCardFullScreen-appIcon': {
      width: '40px',
      height: '40px',
    },
    '& .SolarLaunchCardFullScreen-name': {
      color: 'var(--solar-color-text-primary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-title-sm)',
      lineHeight: 'var(--solar-type-line-height-title-sm)',
      letterSpacing: '-0.03em',
      textDecoration: 'none',
    },
    '& .SolarLaunchCardFullScreen--bodyCopy': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'column',
      justifyContent: 'flex-start',
      alignItems: 'flex-start',
      gap: 'var(--solar-inset-md)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
      width: '100%',
    },
    '& .SolarLaunchCardFullScreen-intro': {
      color: 'var(--solar-color-text-primary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-400)',
      fontSize: 'var(--solar-type-size-body-sm)',
      lineHeight: 'var(--solar-type-line-height-body-sm)',
      letterSpacing: '-0.02em',
      textDecoration: 'none',
      width: '100%',
    },
    '& .SolarLaunchCardFullScreen--feature': {
      color: 'var(--solar-color-text-primary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-400)',
      fontSize: 'var(--solar-type-size-body-sm)',
      lineHeight: 'var(--solar-type-line-height-body-sm)',
      letterSpacing: '-0.02em',
      textDecoration: 'none',
      width: '100%',
    },
    '& .SolarLaunchCardFullScreen--feature2': {
      color: 'var(--solar-color-text-primary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-400)',
      fontSize: 'var(--solar-type-size-body-sm)',
      lineHeight: 'var(--solar-type-line-height-body-sm)',
      letterSpacing: '-0.02em',
      textDecoration: 'none',
      width: '100%',
    },
    '& .SolarLaunchCardFullScreen--feature3': {
      color: 'var(--solar-color-text-primary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-400)',
      fontSize: 'var(--solar-type-size-body-sm)',
      lineHeight: 'var(--solar-type-line-height-body-sm)',
      letterSpacing: '-0.02em',
      textDecoration: 'none',
      width: '100%',
    },
  },
  sizes: {},
  appearances: {},
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarLaunchCardFullScreenComposition = {
  root: {
    base: {
      present: true,
    },
  },
  image: {
    base: {
      present: true,
      image: true,
    },
  },
  text: {
    base: {
      present: true,
    },
  },
  content: {
    base: {
      present: true,
    },
  },
  headline: {
    base: {
      present: true,
    },
  },
  appIcon: {
    base: {
      present: true,
      component: 'App Icon',
      'variant.App': 'Workplace',
    },
  },
  favourite: {
    base: {
      present: true,
      component: 'Icon Button',
      'variant.size': 'md',
      'variant.shape': 'round',
      'variant.prio': 'tertiary',
      'variant.state': 'default',
    },
  },
  name: {
    base: {
      present: true,
    },
  },
  bodyCopy: {
    base: {
      present: true,
    },
  },
  intro: {
    base: {
      present: true,
    },
  },
  feature: {
    base: {
      present: true,
    },
  },
  feature2: {
    base: {
      present: true,
    },
  },
  feature3: {
    base: {
      present: true,
    },
  },
  action: {
    base: {
      present: true,
      component: 'Button',
      'variant.size': 'md',
      'variant.prio': 'primary',
      'variant.state': 'default',
      'variant.danger': 'false',
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarLaunchCardFullScreenStyle(
  props: SolarLaunchCardFullScreenProps = {},
): Style {
  const p: Record<string, unknown> = { ...solarLaunchCardFullScreenDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `default`;
  const s = solarLaunchCardFullScreenStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  return merge(s.reset, s.root, s.appearances[key]);
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarLaunchCardFullScreenParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarLaunchCardFullScreenParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarLaunchCardFullScreenCompose(
  props: SolarLaunchCardFullScreenProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarLaunchCardFullScreenDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `default`;
  const size = '';
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarLaunchCardFullScreenComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}
