// SOLAR TableFooter recipe for MUI. Generated by @bwp-web/codegen from spec/components/tablefooter.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export type SolarTableFooterBreakpoint = 'desktop' | 'mobile';

export interface SolarTableFooterProps {
  breakpoint?: SolarTableFooterBreakpoint;
}

export const solarTableFooterDefaults = {
  breakpoint: 'desktop',
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarTableFooterStyles = {
  reset: {
    display: 'flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarTableFooter-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarTableFooter-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarTableFooter-glyph': {
      display: 'block',
      overflow: 'visible',
      flexShrink: '0',
    },
    '& .SolarTableFooter-drawnIcon': {
      flexShrink: '0',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
  },
  root: {
    backgroundColor: 'transparent',
    borderColor: 'transparent',
    boxShadow: 'none',
    borderRadius: 'var(--solar-radius-none)',
    borderStyle: 'none',
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'center',
    gap: 'var(--solar-inset-none)',
    paddingTop: 'var(--solar-inset-xs)',
    paddingRight: 'var(--solar-stack-none)',
    paddingBottom: 'var(--solar-inset-xs)',
    paddingLeft: 'var(--solar-stack-none)',
    width: '100%',
    '& .SolarTableFooter--container': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'row',
      justifyContent: 'space-between',
      alignItems: 'center',
      gap: 'var(--solar-inset-md)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
      width: '100%',
      minWidth: 0,
    },
    '& .SolarTableFooter--containerContainer': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'row',
      justifyContent: 'flex-start',
      alignItems: 'center',
      gap: 'var(--solar-inset-sm)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
    },
    '& .SolarTableFooter-rowsPerPageLabel': {
      color: 'var(--solar-color-text-secondary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-400)',
      fontSize: 'var(--solar-type-size-body-md)',
      lineHeight: 'var(--solar-type-line-height-body-md)',
      letterSpacing: '-0.02286em',
      textDecoration: 'none',
    },
  },
  sizes: {},
  appearances: {
    'breakpoint=mobile': {
      justifyContent: 'space-between',
      alignItems: 'center',
      gap: 'var(--solar-inset-md)',
      width: '100%',
    },
  },
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarTableFooterComposition = {
  root: {
    base: {
      present: true,
    },
  },
  container: {
    base: {
      present: true,
    },
    appearance: {
      'breakpoint=mobile': {
        default: {
          present: false,
        },
      },
    },
  },
  containerContainer: {
    base: {
      present: true,
    },
    appearance: {
      'breakpoint=mobile': {
        default: {
          present: false,
        },
      },
    },
  },
  rowsPerPage: {
    base: {
      present: true,
      component: 'Dropdown',
      'variant.size': 'md',
      'variant.state': 'default',
    },
    appearance: {
      'breakpoint=mobile': {
        default: {
          present: false,
        },
      },
    },
  },
  rowsPerPageLabel: {
    base: {
      present: true,
    },
    appearance: {
      'breakpoint=mobile': {
        default: {
          present: false,
        },
      },
    },
  },
  pagination: {
    base: {
      present: true,
      component: 'Pagination',
    },
    appearance: {
      'breakpoint=mobile': {
        default: {
          present: false,
        },
      },
    },
  },
  button: {
    base: {
      present: true,
      component: 'Button',
      'variant.size': 'md',
      'variant.prio': 'primary',
      'variant.state': 'default',
      'variant.danger': 'false',
    },
    appearance: {
      'breakpoint=mobile': {
        default: {
          present: false,
        },
      },
    },
  },
  rowsPerPageMobile: {
    base: {
      present: false,
      component: 'Dropdown',
      'variant.size': 'md',
    },
    appearance: {
      'breakpoint=mobile': {
        default: {
          present: true,
          'variant.state': 'default',
        },
      },
    },
  },
  paginationMobile: {
    base: {
      present: false,
      component: 'Pagination',
    },
    appearance: {
      'breakpoint=mobile': {
        default: {
          present: true,
        },
      },
    },
  },
  iconButton: {
    base: {
      present: false,
      component: 'Icon Button',
      'variant.size': 'md',
    },
    appearance: {
      'breakpoint=mobile': {
        default: {
          present: true,
          'variant.shape': 'square',
          'variant.prio': 'primary',
          'variant.state': 'default',
        },
      },
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarTableFooterStyle(
  props: SolarTableFooterProps = {},
): Style {
  const p: Record<string, unknown> = { ...solarTableFooterDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `breakpoint=${p.breakpoint}`;
  const s = solarTableFooterStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  return merge(s.reset, s.root, s.appearances[key]);
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarTableFooterParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarTableFooterParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarTableFooterCompose(
  props: SolarTableFooterProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarTableFooterDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `breakpoint=${p.breakpoint}`;
  const size = '';
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarTableFooterComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}

/** Each layer's children, in Figma's order: the tree the shell draws (`drawChildren`). */
export const solarTableFooterTree: Record<string, string[]> = {
  root: ['container', 'rowsPerPageMobile', 'paginationMobile', 'iconButton'],
  container: ['containerContainer', 'pagination', 'button'],
  containerContainer: ['rowsPerPage', 'rowsPerPageLabel'],
};

/** Each slot's layer, to the slot it is: its class is `SolarTableFooter-<slot>`, public. */
export const solarTableFooterSlots: Record<string, string> = {
  button: 'button',
  rowsPerPage: 'rowsPerPage',
  rowsPerPageLabel: 'rowsPerPageLabel',
  rowsPerPageMobile: 'rowsPerPageMobile',
  paginationMobile: 'paginationMobile',
  pagination: 'pagination',
  iconButton: 'iconButton',
};
