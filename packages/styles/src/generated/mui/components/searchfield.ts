// SOLAR SearchField recipe for MUI. Generated by @bwp-web/codegen from spec/components/searchfield.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export type SolarSearchFieldSize = 'md' | 'sm';

export interface SolarSearchFieldProps {
  error?: boolean;
  disabled?: boolean;
  size?: SolarSearchFieldSize;
}

/** The props and what the shell derives from its content (the recipe is keyed by both). */
export interface SolarSearchFieldRecipeProps extends SolarSearchFieldProps {
  filled?: boolean;
}

export const solarSearchFieldDefaults = {
  error: false,
  disabled: false,
  size: 'md',
  filled: false,
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarSearchFieldStyles = {
  reset: {
    display: 'inline-flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarSearchField-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarSearchField-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarSearchField-glyph': {
      display: 'block',
      overflow: 'visible',
      flexShrink: '0',
    },
    '& .SolarSearchField-drawnIcon': {
      flexShrink: '0',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
    '& .SolarSearchField--search.MuiInputBase-input': {
      flex: '1 1 0%',
      minWidth: '0',
      width: 'auto',
      height: 'auto',
      padding: '0',
      boxSizing: 'border-box',
      WebkitTextFillColor: 'currentcolor',
      '&::placeholder': {
        color: 'inherit',
        opacity: '1',
      },
    },
    '& .SolarSearchField-filter > svg': {
      display: 'block',
      width: '100%',
      height: '100%',
    },
    '&': {
      position: 'relative',
      isolation: 'isolate',
    },
    '&::after': {
      content: '""',
      position: 'absolute',
      top: '50%',
      left: '50%',
      width: 'max(100%, var(--solar-size-target-min))',
      height: 'max(100%, var(--solar-size-target-min))',
      transform: 'translate(-50%, -50%)',
      zIndex: '-1',
    },
    '& .SolarSearchField--search::-webkit-search-cancel-button, & .SolarSearchField--search::-webkit-search-decoration':
      {
        WebkitAppearance: 'none',
        appearance: 'none',
      },
  },
  root: {
    backgroundColor: 'var(--solar-color-surface-base)',
    borderColor: 'var(--solar-color-border-subtle)',
    boxShadow: 'var(--solar-shadow-control)',
    borderRadius: 'var(--solar-radius-control)',
    borderWidth: 'var(--solar-border-default)',
    borderStyle: 'solid',
    flexDirection: 'row',
    justifyContent: 'flex-start',
    alignItems: 'center',
    gap: 'var(--solar-inset-sm)',
    paddingTop: 'var(--solar-inset-none)',
    paddingRight: 'var(--solar-inset-sm)',
    paddingBottom: 'var(--solar-inset-none)',
    paddingLeft: 'var(--solar-inset-sm)',
    width: '100%',
    height: 'var(--solar-size-control-md)',
    '& .SolarSearchField--iconSearch': {
      width: 'var(--solar-icon-sm)',
      height: 'var(--solar-icon-sm)',
      color: 'var(--solar-color-icon-secondary)',
    },
    '& .SolarSearchField--search': {
      color: 'var(--solar-color-text-secondary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-400)',
      fontSize: 'var(--solar-type-size-body-md)',
      lineHeight: 'var(--solar-type-line-height-body-md)',
      letterSpacing: '-0.02286em',
      textDecoration: 'none',
      width: '100%',
    },
    '& .SolarSearchField-filter': {
      width: 'var(--solar-icon-sm)',
      height: 'var(--solar-icon-sm)',
      color: 'var(--solar-color-icon-secondary)',
    },
  },
  sizes: {
    sm: {
      gap: 'var(--solar-inset-xs)',
      width: '100%',
      height: 'var(--solar-size-control-sm)',
      '& .SolarSearchField--iconSearch': {
        width: 'var(--solar-icon-xs)',
        height: 'var(--solar-icon-xs)',
      },
      '& .SolarSearchField--search': {
        fontFamily: 'var(--solar-type-font-family-inter)',
        fontWeight: 'var(--solar-type-font-weight-400)',
        fontSize: 'var(--solar-type-size-body-sm)',
        lineHeight: 'var(--solar-type-line-height-body-sm)',
        letterSpacing: '-0.02em',
        textDecoration: 'none',
      },
      '& .SolarSearchField-filter': {
        width: 'var(--solar-icon-xs)',
        height: 'var(--solar-icon-xs)',
      },
    },
  },
  appearances: {
    default: {
      '&:hover': {
        borderColor: 'var(--solar-color-border-medium)',
        '& .SolarSearchField-filter': {
          color: 'var(--solar-color-icon-primary)',
        },
      },
      '&.Mui-focused': {
        borderColor: 'var(--solar-color-border-feedback-focus-strong)',
        '& .SolarSearchField--iconSearch': {
          color: 'var(--solar-color-icon-primary)',
        },
        '& .SolarSearchField--search': {
          color: 'var(--solar-color-text-primary)',
        },
        '& .SolarSearchField-filter': {
          color: 'var(--solar-color-icon-primary)',
        },
      },
      '&.SolarSearchField-filled': {
        '& .SolarSearchField--iconSearch': {
          color: 'var(--solar-color-icon-primary)',
        },
        '& .SolarSearchField--search': {
          color: 'var(--solar-color-text-primary)',
        },
        '& .SolarSearchField-filter': {
          color: 'var(--solar-color-icon-primary)',
        },
      },
      '&.SolarSearchField-error': {
        borderColor: 'var(--solar-color-border-feedback-danger-strong)',
        '& .SolarSearchField--iconSearch': {
          color: 'var(--solar-color-icon-primary)',
        },
        '& .SolarSearchField--search': {
          color: 'var(--solar-color-text-primary)',
        },
        '& .SolarSearchField-filter': {
          color: 'var(--solar-color-icon-primary)',
        },
      },
      '&.SolarSearchField-disabled': {
        '& .SolarSearchField--iconSearch': {
          color: 'var(--solar-color-icon-disabled)',
        },
        '& .SolarSearchField--search': {
          color: 'var(--solar-color-text-disabled)',
        },
        '& .SolarSearchField-filter': {
          color: 'var(--solar-color-icon-disabled)',
        },
      },
    },
  },
  combined: {
    md: {
      default: {
        '&.Mui-focused': {
          boxShadow: 'var(--solar-shadow-focus-default)',
        },
        '&.SolarSearchField-error': {
          boxShadow: 'var(--solar-shadow-danger)',
        },
      },
    },
    sm: {
      default: {
        boxShadow: 'none',
        '&:hover': {
          boxShadow: 'none',
        },
        '&.Mui-focused': {
          boxShadow: 'var(--solar-shadow-focus-default)',
        },
        '&.SolarSearchField-filled': {
          boxShadow: 'none',
        },
        '&.SolarSearchField-error': {
          boxShadow: 'var(--solar-shadow-danger)',
        },
        '&.SolarSearchField-disabled': {
          boxShadow: 'none',
        },
      },
    },
  },
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarSearchFieldComposition = {
  root: {
    base: {
      present: true,
    },
  },
  iconSearch: {
    base: {
      present: true,
      component: 'Icon/Search',
      'variant.solid': 'false',
    },
  },
  search: {
    base: {
      present: true,
    },
  },
  filter: {
    base: {
      present: true,
      component: 'Icon/Filter',
      'variant.solid': 'false',
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarSearchFieldStyle(
  props: SolarSearchFieldRecipeProps = {},
): Style {
  const p: Record<string, unknown> = { ...solarSearchFieldDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `default`;
  const s = solarSearchFieldStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  const size = p.size as string;
  return merge(
    s.reset,
    s.root,
    s.sizes[size],
    s.appearances[key],
    s.combined[size]?.[key],
  );
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarSearchFieldParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarSearchFieldParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarSearchFieldCompose(
  props: SolarSearchFieldRecipeProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarSearchFieldDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `default`;
  const size = p.size as string;
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarSearchFieldComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}

/** Each layer's children, in Figma's order: the tree the shell draws (`drawChildren`). */
export const solarSearchFieldTree: Record<string, string[]> = {
  root: ['iconSearch', 'search', 'filter'],
};

/** Each slot's layer, to the slot it is: its class is `SolarSearchField-<slot>`, public. */
export const solarSearchFieldSlots: Record<string, string> = {
  filter: 'filter',
};
