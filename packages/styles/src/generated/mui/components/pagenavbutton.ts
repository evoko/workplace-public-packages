// SOLAR PageNavButton recipe for MUI. Generated by @bwp-web/codegen from spec/components/pagenavbutton.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export type SolarPageNavButtonDirection = 'prev' | 'next';

export interface SolarPageNavButtonProps {
  direction?: SolarPageNavButtonDirection;
  disabled?: boolean;
}

export const solarPageNavButtonDefaults = {
  direction: 'prev',
  disabled: false,
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarPageNavButtonStyles = {
  reset: {
    display: 'flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarPageNavButton-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarPageNavButton-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarPageNavButton-glyph': {
      display: 'block',
      overflow: 'visible',
      flexShrink: '0',
    },
    '& .SolarPageNavButton-drawnIcon': {
      flexShrink: '0',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
    borderStyle: 'solid',
    '& .SolarPageNavButton--iconArrowLeft, & .SolarPageNavButton--iconArrowRight':
      {
        flexShrink: '0',
      },
    '&:dir(rtl) .SolarPageNavButton--iconArrowLeft, &:dir(rtl) .SolarPageNavButton--iconArrowRight':
      {
        transform: 'scaleX(-1)',
      },
    '&': {
      position: 'relative',
    },
    '&::after': {
      content: '""',
      position: 'absolute',
      top: '50%',
      left: '50%',
      width: 'max(100%, var(--solar-size-target-min))',
      height: 'max(100%, var(--solar-size-target-min))',
      transform: 'translate(-50%, -50%)',
    },
  },
  root: {
    backgroundColor: 'var(--solar-color-action-secondary-bg-default)',
    borderColor: 'var(--solar-color-border-medium)',
    boxShadow: 'var(--solar-shadow-control)',
    borderRadius: 'var(--solar-radius-control)',
    borderWidth: 'var(--solar-border-default)',
    borderStyle: 'solid',
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    gap: 'var(--solar-stack-xs)',
    paddingTop: 'var(--solar-inset-none)',
    paddingRight: 'var(--solar-inset-sm)',
    paddingBottom: 'var(--solar-inset-none)',
    paddingLeft: 'var(--solar-inset-sm)',
    height: '36px',
    '& .SolarPageNavButton--iconArrowLeft': {
      width: 'var(--solar-icon-md)',
      height: 'var(--solar-icon-md)',
      color: 'var(--solar-color-action-secondary-icon-default)',
    },
    '& .SolarPageNavButton--label': {
      color: 'var(--solar-color-action-secondary-text-default)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-label-md)',
      lineHeight: 'var(--solar-type-line-height-label-md)',
      letterSpacing: '-0.02em',
      textDecoration: 'none',
    },
    '& .SolarPageNavButton--iconArrowRight': {
      width: 'var(--solar-icon-md)',
      height: 'var(--solar-icon-md)',
    },
  },
  sizes: {},
  appearances: {
    'direction=prev': {
      '&:hover': {
        backgroundColor: 'var(--solar-color-action-secondary-bg-hover)',
        '& .SolarPageNavButton--iconArrowLeft': {
          color: 'var(--solar-color-action-secondary-icon-hover)',
        },
      },
      '&:active': {
        backgroundColor: 'var(--solar-color-action-secondary-bg-active)',
        boxShadow: 'none',
        '& .SolarPageNavButton--iconArrowLeft': {
          color: 'var(--solar-color-action-secondary-icon-active)',
        },
      },
      '&.Mui-focusVisible': {
        boxShadow: 'var(--solar-shadow-focus-default)',
        backgroundColor: 'var(--solar-color-action-secondary-bg-default)',
        '& .SolarPageNavButton--iconArrowLeft': {
          color: 'var(--solar-color-action-secondary-icon-default)',
        },
      },
      '&.Mui-disabled': {
        backgroundColor: 'var(--solar-color-action-secondary-bg-disabled)',
        borderColor: 'var(--solar-color-border-disabled)',
        boxShadow: 'none',
        '& .SolarPageNavButton--iconArrowLeft': {
          color: 'var(--solar-color-action-secondary-icon-disabled)',
        },
        '& .SolarPageNavButton--label': {
          color: 'var(--solar-color-action-secondary-text-disabled)',
        },
      },
    },
    'direction=next': {
      '&:hover': {
        backgroundColor: 'var(--solar-color-action-secondary-bg-hover)',
        '& .SolarPageNavButton--iconArrowRight': {
          color: 'var(--solar-color-action-secondary-icon-hover)',
        },
      },
      '&:active': {
        backgroundColor: 'var(--solar-color-action-secondary-bg-active)',
        boxShadow: 'none',
        '& .SolarPageNavButton--iconArrowRight': {
          color: 'var(--solar-color-action-secondary-icon-active)',
        },
      },
      '&.Mui-focusVisible': {
        boxShadow: 'var(--solar-shadow-focus-default)',
        backgroundColor: 'var(--solar-color-action-secondary-bg-default)',
        '& .SolarPageNavButton--iconArrowRight': {
          color: 'var(--solar-color-action-secondary-icon-default)',
        },
      },
      '&.Mui-disabled': {
        backgroundColor: 'var(--solar-color-action-secondary-bg-disabled)',
        borderColor: 'var(--solar-color-border-disabled)',
        boxShadow: 'none',
        '& .SolarPageNavButton--label': {
          color: 'var(--solar-color-action-secondary-text-disabled)',
        },
        '& .SolarPageNavButton--iconArrowRight': {
          color: 'var(--solar-color-action-secondary-icon-disabled)',
        },
      },
      '& .SolarPageNavButton--iconArrowRight': {
        color: 'var(--solar-color-action-secondary-icon-default)',
      },
    },
  },
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarPageNavButtonComposition = {
  root: {
    base: {
      present: true,
    },
  },
  iconArrowLeft: {
    base: {
      present: true,
      component: 'Icon/ArrowLeft',
      'variant.solid': 'false',
    },
    appearance: {
      'direction=next': {
        default: {
          present: false,
        },
        hover: {
          present: false,
        },
        pressed: {
          present: false,
        },
        focus: {
          present: false,
        },
        disabled: {
          present: false,
        },
      },
    },
  },
  label: {
    base: {
      present: true,
    },
  },
  iconArrowRight: {
    base: {
      present: false,
      component: 'Icon/ArrowRight',
    },
    appearance: {
      'direction=next': {
        default: {
          present: true,
          'variant.solid': 'false',
        },
        hover: {
          present: true,
          'variant.solid': 'false',
        },
        pressed: {
          present: true,
          'variant.solid': 'false',
        },
        focus: {
          present: true,
          'variant.solid': 'false',
        },
        disabled: {
          present: true,
          'variant.solid': 'false',
        },
      },
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarPageNavButtonStyle(
  props: SolarPageNavButtonProps = {},
): Style {
  const p: Record<string, unknown> = { ...solarPageNavButtonDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `direction=${p.direction}`;
  const s = solarPageNavButtonStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  return merge(s.reset, s.root, s.appearances[key]);
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarPageNavButtonParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarPageNavButtonParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarPageNavButtonCompose(
  props: SolarPageNavButtonProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarPageNavButtonDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `direction=${p.direction}`;
  const size = '';
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarPageNavButtonComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}

/** Each layer's children, in Figma's order: the tree the shell draws (`drawChildren`). */
export const solarPageNavButtonTree: Record<string, string[]> = {
  root: ['iconArrowLeft', 'label', 'iconArrowRight'],
};

/** Each slot's layer, to the slot it is: its class is `SolarPageNavButton-<slot>`, public. */
export const solarPageNavButtonSlots: Record<string, string> = {};
