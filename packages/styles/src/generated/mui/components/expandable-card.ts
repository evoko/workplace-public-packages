// SOLAR Expandable Card recipe for MUI. Generated by @bwp-web/codegen from spec/components/expandable card.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export interface SolarExpandableCardProps {
  expanded?: boolean;
}

export const solarExpandableCardDefaults = {
  expanded: false,
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarExpandableCardStyles = {
  reset: {
    display: 'flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarExpandableCard-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarExpandableCard-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarExpandableCard-glyph': {
      display: 'block',
      overflow: 'visible',
      flexShrink: '0',
    },
    '& .SolarExpandableCard-drawnIcon': {
      flexShrink: '0',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
    '& .SolarExpandableCard-title, & .SolarExpandableCard-description': {
      whiteSpace: 'normal',
      minWidth: '0',
      textAlign: 'start',
    },
    '& .SolarExpandableCard--header': {
      padding: '0',
      margin: '0',
      font: 'inherit',
      color: 'inherit',
    },
  },
  root: {
    backgroundColor: 'var(--solar-color-surface-base)',
    borderColor: 'var(--solar-color-border-subtle)',
    boxShadow: 'var(--solar-shadow-raised)',
    borderRadius: 'var(--solar-radius-container)',
    borderWidth: 'var(--solar-border-default)',
    borderStyle: 'solid',
    flexDirection: 'row',
    justifyContent: 'flex-start',
    alignItems: 'flex-start',
    gap: 'var(--solar-stack-sm)',
    paddingTop: 'var(--solar-inset-md)',
    paddingRight: 'var(--solar-inset-md)',
    paddingBottom: 'var(--solar-inset-md)',
    paddingLeft: 'var(--solar-inset-md)',
    width: '100%',
    '& .SolarExpandableCard--header': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'row',
      justifyContent: 'flex-start',
      alignItems: 'center',
      gap: 'var(--solar-stack-sm)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
      width: '100%',
    },
    '& .SolarExpandableCard-title': {
      color: 'var(--solar-color-text-primary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-body-lg)',
      lineHeight: 'var(--solar-type-line-height-body-lg)',
      letterSpacing: '-0.02em',
      textDecoration: 'none',
      width: '100%',
    },
    '& .SolarExpandableCard--iconChevronDown': {
      width: 'var(--solar-icon-sm)',
      height: 'var(--solar-icon-sm)',
      color: 'var(--solar-color-icon-primary)',
    },
    '& .SolarExpandableCard-content': {
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'column',
      justifyContent: 'flex-start',
      alignItems: 'flex-start',
      gap: 'var(--solar-stack-none)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
      width: '100%',
    },
    '& .SolarExpandableCard--iconChevronUp': {
      width: 'var(--solar-icon-sm)',
      height: 'var(--solar-icon-sm)',
    },
    '& .SolarExpandableCard-description': {
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-400)',
      fontSize: 'var(--solar-type-size-body-md)',
      lineHeight: 'var(--solar-type-line-height-body-md)',
      letterSpacing: '-0.02286em',
      textDecoration: 'none',
      width: '100%',
    },
  },
  sizes: {},
  appearances: {
    'expanded=false': {
      '&:has(.SolarExpandableCard--header:hover)': {
        borderColor: 'var(--solar-color-border-medium)',
      },
      '&:has(.SolarExpandableCard--header.Mui-focusVisible)': {
        boxShadow: 'var(--solar-shadow-focus-default)',
        borderColor: 'var(--solar-color-border-subtle)',
      },
    },
    'expanded=true': {
      flexDirection: 'column',
      '&:has(.SolarExpandableCard--header:hover)': {
        borderColor: 'var(--solar-color-border-medium)',
        '& .SolarExpandableCard-content': {
          backgroundColor: 'transparent',
          borderColor: 'transparent',
          boxShadow: 'none',
        },
        '& .SolarExpandableCard--iconChevronUp': {
          color: 'var(--solar-color-icon-primary)',
        },
        '& .SolarExpandableCard-description': {
          color: 'var(--solar-color-text-secondary)',
        },
      },
      '&:has(.SolarExpandableCard--header.Mui-focusVisible)': {
        boxShadow: 'var(--solar-shadow-focus-default)',
        borderColor: 'var(--solar-color-border-subtle)',
      },
      '& .SolarExpandableCard-content': {
        backgroundColor: 'transparent',
        borderColor: 'transparent',
        boxShadow: 'none',
      },
      '& .SolarExpandableCard--iconChevronUp': {
        color: 'var(--solar-color-icon-primary)',
      },
      '& .SolarExpandableCard-description': {
        color: 'var(--solar-color-text-secondary)',
      },
    },
  },
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarExpandableCardComposition = {
  root: {
    base: {
      present: true,
    },
  },
  header: {
    base: {
      present: true,
    },
  },
  title: {
    base: {
      present: true,
    },
  },
  iconChevronDown: {
    base: {
      present: true,
      component: 'Icon/ChevronDown',
      'variant.solid': 'false',
    },
    appearance: {
      'expanded=true': {
        default: {
          present: false,
        },
        hover: {
          present: false,
        },
      },
    },
  },
  content: {
    base: {
      present: false,
    },
    appearance: {
      'expanded=true': {
        default: {
          present: true,
        },
        hover: {
          present: true,
        },
      },
    },
  },
  iconChevronUp: {
    base: {
      present: false,
      component: 'Icon/ChevronUp',
    },
    appearance: {
      'expanded=true': {
        default: {
          present: true,
          'variant.solid': 'false',
        },
        hover: {
          present: true,
          'variant.solid': 'false',
        },
      },
    },
  },
  description: {
    base: {
      present: false,
    },
    appearance: {
      'expanded=true': {
        default: {
          present: true,
        },
        hover: {
          present: true,
        },
      },
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarExpandableCardStyle(
  props: SolarExpandableCardProps = {},
): Style {
  const p: Record<string, unknown> = { ...solarExpandableCardDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `expanded=${p.expanded}`;
  const s = solarExpandableCardStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  return merge(s.reset, s.root, s.appearances[key]);
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarExpandableCardParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarExpandableCardParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarExpandableCardCompose(
  props: SolarExpandableCardProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarExpandableCardDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `expanded=${p.expanded}`;
  const size = '';
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarExpandableCardComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}

/** Each layer's children, in Figma's order: the tree the shell draws (`drawChildren`). */
export const solarExpandableCardTree: Record<string, string[]> = {
  root: ['header', 'content'],
  header: ['title', 'iconChevronDown', 'iconChevronUp'],
  content: ['description'],
};

/** Each slot's layer, to the slot it is: its class is `SolarExpandableCard-<slot>`, public. */
export const solarExpandableCardSlots: Record<string, string> = {
  content: 'content',
  title: 'title',
  description: 'description',
};
