// SOLAR Token Input recipe for MUI. Generated by @bwp-web/codegen from spec/components/token input.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export type SolarTokenInputSize = 'md' | 'sm';

export interface SolarTokenInputProps {
  size?: SolarTokenInputSize;
  disabled?: boolean;
  error?: boolean;
  readonly?: boolean;
}

/** The props and what the shell derives from its content (the recipe is keyed by both). */
export interface SolarTokenInputRecipeProps extends SolarTokenInputProps {
  active?: boolean;
  filled?: boolean;
}

export const solarTokenInputDefaults = {
  size: 'md',
  disabled: false,
  error: false,
  readonly: false,
  active: false,
  filled: false,
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarTokenInputStyles = {
  reset: {
    display: 'inline-flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarTokenInput-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarTokenInput-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarTokenInput-glyph': {
      display: 'block',
      overflow: 'visible',
      flexShrink: '0',
    },
    '& .SolarTokenInput-drawnIcon': {
      flexShrink: '0',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
    '& .SolarTokenInput--addItems.MuiInputBase-input': {
      minWidth: '4ch',
    },
    '& .SolarTokenInput-helper': {
      whiteSpace: 'normal',
    },
    '& .SolarTokenInput--field': {
      position: 'relative',
      isolation: 'isolate',
    },
    '& .SolarTokenInput--field::after': {
      content: '""',
      position: 'absolute',
      top: '50%',
      left: '50%',
      width: 'max(100%, var(--solar-size-target-min))',
      height: 'max(100%, var(--solar-size-target-min))',
      transform: 'translate(-50%, -50%)',
      zIndex: '-1',
    },
    '& .SolarTokenInput-words': {
      display: 'contents',
    },
    '& .SolarTokenInput--tags': {
      minWidth: '0',
      overflow: 'hidden',
    },
    '& .SolarTokenInput--tags > *': {
      flexShrink: '0',
    },
  },
  root: {
    backgroundColor: 'transparent',
    borderColor: 'transparent',
    boxShadow: 'none',
    borderRadius: 'var(--solar-radius-none)',
    borderStyle: 'none',
    flexDirection: 'column',
    justifyContent: 'flex-start',
    alignItems: 'flex-start',
    gap: 'var(--solar-stack-xs)',
    paddingTop: 'var(--solar-inset-none)',
    paddingRight: 'var(--solar-inset-none)',
    paddingBottom: 'var(--solar-inset-none)',
    paddingLeft: 'var(--solar-inset-none)',
    width: '100%',
    '& .SolarTokenInput-label': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'row',
      justifyContent: 'flex-start',
      alignItems: 'center',
      gap: 'var(--solar-inset-2xs)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
      width: '100%',
    },
    '& .SolarTokenInput--labelLabel': {
      color: 'var(--solar-color-text-primary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-label-md)',
      lineHeight: 'var(--solar-type-line-height-label-md)',
      letterSpacing: '-0.02em',
      textDecoration: 'none',
    },
    '& .SolarTokenInput-mandatory': {
      color: 'var(--solar-color-text-feedback-info)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-label-md)',
      lineHeight: 'var(--solar-type-line-height-label-md)',
      letterSpacing: '-0.02em',
      textDecoration: 'none',
    },
    '& .SolarTokenInput--field': {
      backgroundColor: 'var(--solar-color-surface-base)',
      borderColor: 'var(--solar-color-border-subtle)',
      boxShadow: 'var(--solar-shadow-control)',
      borderRadius: 'var(--solar-radius-control)',
      borderWidth: 'var(--solar-border-default)',
      borderStyle: 'solid',
      flexDirection: 'row',
      justifyContent: 'flex-start',
      alignItems: 'center',
      gap: 'var(--solar-inset-xs)',
      paddingTop: 'var(--solar-inset-xs)',
      paddingRight: 'var(--solar-inset-sm)',
      paddingBottom: 'var(--solar-inset-xs)',
      paddingLeft: 'var(--solar-inset-sm)',
      width: '100%',
    },
    '& .SolarTokenInput--tags': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'row',
      justifyContent: 'flex-start',
      alignItems: 'center',
      gap: 'var(--solar-inset-2xs)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
      width: '100%',
    },
    '& .SolarTokenInput--addItems': {
      color: 'var(--solar-color-text-tertiary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-400)',
      fontSize: 'var(--solar-type-size-body-md)',
      lineHeight: 'var(--solar-type-line-height-body-md)',
      letterSpacing: '-0.02286em',
      textDecoration: 'none',
      width: '100%',
    },
    '& .SolarTokenInput-helper': {
      color: 'var(--solar-color-text-secondary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-400)',
      fontSize: 'var(--solar-type-size-helper-md)',
      lineHeight: 'var(--solar-type-line-height-helper-md)',
      letterSpacing: '-0.02em',
      textDecoration: 'none',
      width: '100%',
    },
  },
  sizes: {
    sm: {
      width: '100%',
      '& .SolarTokenInput--labelLabel': {
        fontFamily: 'var(--solar-type-font-family-inter)',
        fontWeight: 'var(--solar-type-font-weight-500)',
        fontSize: 'var(--solar-type-size-label-sm)',
        lineHeight: 'var(--solar-type-line-height-label-sm)',
        letterSpacing: '-0.02em',
        textDecoration: 'none',
      },
      '& .SolarTokenInput-mandatory': {
        fontFamily: 'var(--solar-type-font-family-inter)',
        fontWeight: 'var(--solar-type-font-weight-500)',
        fontSize: 'var(--solar-type-size-label-sm)',
        lineHeight: 'var(--solar-type-line-height-label-sm)',
        letterSpacing: '-0.02em',
        textDecoration: 'none',
      },
      '& .SolarTokenInput--field': {
        paddingTop: 'var(--solar-inset-2xs)',
        paddingRight: 'var(--solar-inset-xs)',
        paddingBottom: 'var(--solar-inset-2xs)',
        paddingLeft: 'var(--solar-inset-xs)',
      },
      '& .SolarTokenInput--addItems': {
        fontFamily: 'var(--solar-type-font-family-inter)',
        fontWeight: 'var(--solar-type-font-weight-400)',
        fontSize: 'var(--solar-type-size-body-sm)',
        lineHeight: 'var(--solar-type-line-height-body-sm)',
        letterSpacing: '-0.02em',
        textDecoration: 'none',
      },
      '& .SolarTokenInput-helper': {
        fontFamily: 'var(--solar-type-font-family-inter)',
        fontWeight: 'var(--solar-type-font-weight-400)',
        fontSize: 'var(--solar-type-size-helper-sm)',
        lineHeight: 'var(--solar-type-line-height-helper-sm)',
        letterSpacing: '-0.02em',
        textDecoration: 'none',
      },
    },
  },
  appearances: {
    default: {
      '&:has(.SolarTokenInput--field:hover)': {
        '& .SolarTokenInput--field': {
          borderColor: 'var(--solar-color-border-medium)',
        },
      },
      '&:has(.SolarTokenInput--field .Mui-focused)': {
        '& .SolarTokenInput--field': {
          borderColor: 'var(--solar-color-border-feedback-focus-strong)',
          boxShadow: 'var(--solar-shadow-focus-default)',
        },
      },
      '&.SolarTokenInput-active': {
        '& .SolarTokenInput--field': {
          borderColor: 'var(--solar-color-border-feedback-focus-strong)',
          boxShadow: 'var(--solar-shadow-focus-default)',
        },
        '& .SolarTokenInput--addItems': {
          color: 'var(--solar-color-text-primary)',
        },
      },
      '&.SolarTokenInput-filled': {},
      '&.SolarTokenInput-readonly': {
        '& .SolarTokenInput--field': {
          backgroundColor: 'var(--solar-color-surface-background)',
        },
      },
      '&.SolarTokenInput-error': {
        '& .SolarTokenInput--field': {
          borderColor: 'var(--solar-color-border-feedback-danger-strong)',
          boxShadow: 'var(--solar-shadow-danger)',
        },
        '& .SolarTokenInput-helper': {
          color: 'var(--solar-color-text-feedback-danger)',
        },
      },
      '&.SolarTokenInput-disabled': {
        '& .SolarTokenInput--labelLabel': {
          color: 'var(--solar-color-text-disabled)',
        },
        '& .SolarTokenInput--field': {
          backgroundColor: 'var(--solar-color-surface-background)',
          borderColor: 'var(--solar-color-border-disabled)',
        },
        '& .SolarTokenInput--addItems': {
          color: 'var(--solar-color-text-disabled)',
        },
        '& .SolarTokenInput-helper': {
          color: 'var(--solar-color-text-disabled)',
        },
      },
    },
  },
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarTokenInputComposition = {
  root: {
    base: {
      present: true,
    },
  },
  label: {
    base: {
      present: true,
    },
  },
  labelLabel: {
    base: {
      present: true,
    },
  },
  mandatory: {
    base: {
      present: true,
    },
  },
  field: {
    base: {
      present: true,
    },
  },
  tags: {
    base: {
      present: true,
    },
  },
  addItems: {
    base: {
      present: true,
    },
  },
  helper: {
    base: {
      present: true,
    },
  },
  tag: {
    base: {
      present: false,
      component: 'Tag',
    },
    appearance: {
      default: {
        focus: {
          present: true,
          'variant.status': 'neutral',
          'variant.type': 'closable',
          'variant.invert': 'false',
        },
        filled: {
          present: true,
          'variant.status': 'neutral',
          'variant.type': 'closable',
          'variant.invert': 'false',
        },
        disabled: {
          present: true,
          'variant.status': 'neutral',
          'variant.type': 'text-only',
          'variant.invert': 'false',
        },
        error: {
          present: true,
          'variant.status': 'neutral',
          'variant.type': 'closable',
          'variant.invert': 'false',
        },
        readonly: {
          present: true,
          'variant.status': 'neutral',
          'variant.type': 'text-only',
          'variant.invert': 'false',
        },
      },
    },
  },
  counter: {
    base: {
      present: false,
      component: 'Counter',
    },
    appearance: {
      default: {
        filled: {
          present: true,
          'variant.type': 'regular',
          'variant.state': 'default',
        },
      },
    },
  },
  tag2: {
    base: {
      present: false,
      component: 'Tag',
    },
    appearance: {
      default: {
        filled: {
          present: true,
          'variant.status': 'neutral',
          'variant.type': 'closable',
          'variant.invert': 'false',
        },
        error: {
          present: true,
          'variant.status': 'neutral',
          'variant.type': 'closable',
          'variant.invert': 'false',
        },
        readonly: {
          present: true,
          'variant.status': 'neutral',
          'variant.type': 'text-only',
          'variant.invert': 'false',
        },
      },
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarTokenInputStyle(
  props: SolarTokenInputRecipeProps = {},
): Style {
  const p: Record<string, unknown> = { ...solarTokenInputDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `default`;
  const s = solarTokenInputStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  const size = p.size as string;
  return merge(
    s.reset,
    s.root,
    s.sizes[size],
    s.appearances[key],
    s.combined[size]?.[key],
  );
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarTokenInputParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarTokenInputParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarTokenInputCompose(
  props: SolarTokenInputRecipeProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarTokenInputDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `default`;
  const size = p.size as string;
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarTokenInputComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}

/** Each layer's children, in Figma's order: the tree the shell draws (`drawChildren`). */
export const solarTokenInputTree: Record<string, string[]> = {
  root: ['label', 'field', 'helper'],
  label: ['labelLabel', 'mandatory'],
  field: ['tags', 'counter'],
  tags: ['addItems', 'tag', 'tag2'],
};

/** Each slot's layer, to the slot it is: its class is `SolarTokenInput-<slot>`, public. */
export const solarTokenInputSlots: Record<string, string> = {
  label: 'label',
  mandatory: 'mandatory',
  helper: 'helper',
};
