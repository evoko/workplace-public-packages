// SOLAR Launch Card recipe for MUI. Generated by @bwp-web/codegen from spec/components/launch card.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export type SolarLaunchCardProps = Record<never, never>;

export const solarLaunchCardDefaults = {} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarLaunchCardStyles = {
  reset: {
    display: 'flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarLaunchCard-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarLaunchCard-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarLaunchCard-glyph': {
      display: 'block',
      overflow: 'visible',
      flexShrink: '0',
    },
    '& .SolarLaunchCard-drawnIcon': {
      flexShrink: '0',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
    '& .SolarLaunchCard-bodyText': {
      whiteSpace: 'normal',
      minWidth: '0',
    },
    '& .SolarLaunchCard-press': {
      position: 'static',
      display: 'inline',
      padding: '0',
      margin: '0',
      font: 'inherit',
      color: 'inherit',
      textAlign: 'start',
      textDecoration: 'none',
      verticalAlign: 'baseline',
    },
    '& .SolarLaunchCard-press::after': {
      content: '""',
      position: 'absolute',
      inset: '0',
      borderRadius: 'inherit',
    },
    '& .SolarLaunchCard-box :is(a, button, input, select, textarea, [tabindex]):not(.SolarLaunchCard-press)':
      {
        position: 'relative',
      },
    '& .SolarLaunchCard-name': {
      position: 'absolute',
      width: '1px',
      height: '1px',
      overflow: 'hidden',
      clipPath: 'inset(50%)',
      whiteSpace: 'nowrap',
    },
    '& .SolarLaunchCard-appIcon, & .SolarLaunchCard-tag, & .SolarLaunchCard-favouriteNoImage':
      {
        flexShrink: '0',
      },
    overflow: 'hidden',
    '& .SolarLaunchCard-image': {
      position: 'relative',
    },
    '& .SolarLaunchCard-image > img': {
      position: 'absolute',
      inset: '0',
      width: '100%',
      height: '100%',
      objectFit: 'cover',
    },
    '& .SolarLaunchCard-appIcon > img': {
      display: 'block',
      width: '100%',
      height: '100%',
    },
    '& .SolarLaunchCard-actions > *': {
      width: '100%',
    },
  },
  root: {
    backgroundColor: 'var(--solar-color-surface-dialog)',
    borderColor: 'var(--solar-color-border-subtle)',
    boxShadow: 'none',
    borderRadius: 'var(--solar-radius-dialog)',
    borderWidth: 'var(--solar-border-default)',
    borderStyle: 'solid',
    flexDirection: 'column',
    justifyContent: 'flex-start',
    alignItems: 'flex-start',
    gap: 'var(--solar-stack-none)',
    paddingTop: 'var(--solar-inset-none)',
    paddingRight: 'var(--solar-inset-none)',
    paddingBottom: 'var(--solar-inset-none)',
    paddingLeft: 'var(--solar-inset-none)',
    width: '100%',
    '& .SolarLaunchCard-image': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      '--solar-placed-right': '0px',
      flexDirection: 'row',
      justifyContent: 'flex-end',
      alignItems: 'flex-start',
      gap: 'var(--solar-stack-lg)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
      width: '100%',
      height: '160px',
      position: 'relative',
    },
    '& .SolarLaunchCard-favourite': {
      position: 'absolute',
      right: 'calc(16px - var(--solar-placed-right, 0px))',
      top: 'calc(16px - var(--solar-placed-top, 0px))',
    },
    '& .SolarLaunchCard-content': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'column',
      justifyContent: 'center',
      alignItems: 'flex-start',
      gap: 'var(--solar-stack-xl)',
      paddingTop: 'var(--solar-stack-md)',
      paddingRight: 'var(--solar-stack-md)',
      paddingBottom: 'var(--solar-stack-md)',
      paddingLeft: 'var(--solar-stack-md)',
      width: '100%',
    },
    '& .SolarLaunchCard-text': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'column',
      justifyContent: 'flex-start',
      alignItems: 'flex-start',
      gap: 'var(--solar-inset-sm)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
      width: '100%',
    },
    '& .SolarLaunchCard-headline': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'row',
      justifyContent: 'flex-start',
      alignItems: 'center',
      gap: 'var(--solar-inset-xs)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
      width: '100%',
    },
    '& .SolarLaunchCard-appIcon': {
      width: '24px',
      height: '24px',
    },
    '& .SolarLaunchCard-name': {
      color: 'var(--solar-color-text-primary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-title-xs)',
      lineHeight: 'var(--solar-type-line-height-title-xs)',
      letterSpacing: '-0.03em',
      textDecoration: 'none',
      width: '100%',
    },
    '& .SolarLaunchCard-bodyText': {
      color: 'var(--solar-color-text-secondary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-400)',
      fontSize: 'var(--solar-type-size-body-sm)',
      lineHeight: 'var(--solar-type-line-height-body-sm)',
      letterSpacing: '-0.02em',
      textDecoration: 'none',
      width: '100%',
    },
    '& .SolarLaunchCard-actions': {
      width: '100%',
    },
  },
  sizes: {},
  appearances: {
    default: {
      '&:has(.SolarLaunchCard-press.Mui-focusVisible)': {
        boxShadow: 'var(--solar-shadow-focus-default)',
      },
    },
  },
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarLaunchCardComposition = {
  root: {
    base: {
      present: true,
    },
  },
  image: {
    base: {
      present: true,
      image: true,
    },
  },
  favourite: {
    base: {
      present: true,
      component: 'Icon Button',
      'variant.size': 'sm',
      'variant.shape': 'round',
      'variant.prio': 'tertiary',
      'variant.state': 'default',
    },
  },
  content: {
    base: {
      present: true,
    },
  },
  text: {
    base: {
      present: true,
    },
  },
  headline: {
    base: {
      present: true,
    },
  },
  appIcon: {
    base: {
      present: true,
      component: 'App Icon',
      'variant.App': 'Workplace',
    },
  },
  name: {
    base: {
      present: true,
    },
  },
  tag: {
    base: {
      present: false,
      component: 'Tag',
      'variant.status': 'success',
      'variant.type': 'text-only',
      'variant.invert': 'false',
    },
  },
  favouriteNoImage: {
    base: {
      present: false,
      component: 'Icon Button',
      'variant.size': 'sm',
      'variant.shape': 'round',
      'variant.prio': 'tertiary',
      'variant.state': 'default',
    },
  },
  bodyText: {
    base: {
      present: true,
    },
  },
  actions: {
    base: {
      present: true,
      component: 'Button Group',
      'variant.orientation': 'horizontal',
      'variant.type': 'regular',
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarLaunchCardStyle(props: SolarLaunchCardProps = {}): Style {
  const p: Record<string, unknown> = { ...solarLaunchCardDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `default`;
  const s = solarLaunchCardStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  return merge(s.reset, s.root, s.appearances[key]);
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarLaunchCardParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarLaunchCardParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarLaunchCardCompose(
  props: SolarLaunchCardProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarLaunchCardDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `default`;
  const size = '';
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarLaunchCardComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}
