// SOLAR List recipe for MUI. Generated by @bwp-web/codegen from spec/components/list.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export interface SolarListProps {
  inCard?: boolean;
}

export const solarListDefaults = {
  inCard: true,
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarListStyles = {
  reset: {
    display: 'flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarList-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarList-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarList-glyph': {
      display: 'block',
      overflow: 'visible',
      flexShrink: '0',
    },
    '& .SolarList-drawnIcon': {
      flexShrink: '0',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
    flexDirection: 'column',
    '& .SolarList-items': {
      margin: '0',
      padding: '0',
      listStyle: 'none',
    },
    '& .SolarList-items > li': {
      display: 'flex',
      flexDirection: 'column',
    },
  },
  root: {
    backgroundColor: 'transparent',
    borderColor: 'var(--solar-color-border-subtle)',
    boxShadow: 'var(--solar-shadow-raised)',
    borderRadius: 'var(--solar-radius-container)',
    borderWidth: 'var(--solar-border-default)',
    borderStyle: 'solid',
    flexDirection: 'column',
    justifyContent: 'flex-start',
    alignItems: 'flex-start',
    gap: 'var(--solar-stack-none)',
    paddingTop: 'var(--solar-inset-none)',
    paddingRight: 'var(--solar-inset-none)',
    paddingBottom: 'var(--solar-inset-none)',
    paddingLeft: 'var(--solar-inset-none)',
    width: '100%',
    '& .SolarList-items': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'column',
      justifyContent: 'flex-start',
      alignItems: 'flex-start',
      gap: 'var(--solar-stack-none)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
      width: '100%',
    },
    '& .SolarList--listItem': {
      width: '100%',
    },
    '& .SolarList--divider': {
      width: '100%',
    },
    '& .SolarList--listItem2': {
      width: '100%',
    },
    '& .SolarList--divider2': {
      width: '100%',
    },
    '& .SolarList--listItem3': {
      width: '100%',
    },
    '& .SolarList--divider3': {
      width: '100%',
    },
    '& .SolarList--listItem4': {
      width: '100%',
    },
    '& .SolarList--divider4': {
      width: '100%',
    },
    '& .SolarList--listItem5': {
      width: '100%',
    },
  },
  sizes: {},
  appearances: {
    'inCard=false': {
      borderColor: 'transparent',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      '& .SolarList-items': {
        width: '100%',
      },
      '& .SolarList--listItem': {
        width: '100%',
      },
      '& .SolarList--listItem2': {
        width: '100%',
      },
      '& .SolarList--listItem3': {
        width: '100%',
      },
      '& .SolarList--listItem4': {
        width: '100%',
      },
      '& .SolarList--listItem5': {
        width: '100%',
      },
    },
  },
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarListComposition = {
  root: {
    base: {
      present: true,
    },
  },
  items: {
    base: {
      present: true,
    },
  },
  listItem: {
    base: {
      present: true,
      component: 'ListItem',
      'variant.type': 'icon',
      'variant.state': 'default',
      'variant.compact': 'false',
    },
    appearance: {
      'inCard=false': {
        default: {
          'variant.compact': 'true',
        },
      },
    },
  },
  divider: {
    base: {
      present: true,
      component: 'Divider',
      'variant.orientation': 'horizontal',
      'variant.type': 'full',
    },
  },
  listItem2: {
    base: {
      present: true,
      component: 'ListItem',
      'variant.type': 'icon',
      'variant.state': 'default',
      'variant.compact': 'false',
    },
    appearance: {
      'inCard=false': {
        default: {
          'variant.compact': 'true',
        },
      },
    },
  },
  divider2: {
    base: {
      present: true,
      component: 'Divider',
      'variant.orientation': 'horizontal',
      'variant.type': 'full',
    },
  },
  listItem3: {
    base: {
      present: true,
      component: 'ListItem',
      'variant.type': 'icon',
      'variant.state': 'default',
      'variant.compact': 'false',
    },
    appearance: {
      'inCard=false': {
        default: {
          'variant.compact': 'true',
        },
      },
    },
  },
  divider3: {
    base: {
      present: true,
      component: 'Divider',
      'variant.orientation': 'horizontal',
      'variant.type': 'full',
    },
  },
  listItem4: {
    base: {
      present: true,
      component: 'ListItem',
      'variant.type': 'icon',
      'variant.state': 'default',
      'variant.compact': 'false',
    },
    appearance: {
      'inCard=false': {
        default: {
          'variant.compact': 'true',
        },
      },
    },
  },
  divider4: {
    base: {
      present: true,
      component: 'Divider',
      'variant.orientation': 'horizontal',
      'variant.type': 'full',
    },
  },
  listItem5: {
    base: {
      present: true,
      component: 'ListItem',
      'variant.type': 'icon',
      'variant.state': 'default',
      'variant.compact': 'false',
    },
    appearance: {
      'inCard=false': {
        default: {
          'variant.compact': 'true',
        },
      },
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarListStyle(props: SolarListProps = {}): Style {
  const p: Record<string, unknown> = { ...solarListDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `inCard=${p.inCard}`;
  const s = solarListStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  return merge(s.reset, s.root, s.appearances[key]);
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarListParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarListParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarListCompose(
  props: SolarListProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarListDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `inCard=${p.inCard}`;
  const size = '';
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarListComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}
