// SOLAR Toast recipe for MUI. Generated by @bwp-web/codegen from spec/components/toast.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export type SolarToastStatus =
  'neutral' | 'success' | 'warning' | 'danger' | 'info';

export interface SolarToastProps {
  status?: SolarToastStatus;
}

export const solarToastDefaults = {
  status: 'success',
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarToastStyles = {
  reset: {
    display: 'inline-flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarToast-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarToast-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarToast-glyph': {
      display: 'block',
      overflow: 'visible',
      flexShrink: '0',
    },
    '& .SolarToast-drawnIcon': {
      flexShrink: '0',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
    '& button.SolarToast-action': {
      position: 'relative',
    },
    '& button.SolarToast-action::after': {
      content: '""',
      position: 'absolute',
      top: '50%',
      left: '50%',
      width: 'max(100%, 44px)',
      height: 'max(100%, 44px)',
      transform: 'translate(-50%, -50%)',
    },
    '& .SolarToast-chevron > svg': {
      display: 'block',
      width: '100%',
      height: '100%',
    },
  },
  root: {
    backgroundColor: 'var(--solar-color-surface-feedback-success-subtle)',
    borderColor: 'var(--solar-color-border-feedback-success-subtle)',
    boxShadow: 'var(--solar-shadow-raised)',
    borderRadius: 'var(--solar-radius-pill)',
    borderWidth: 'var(--solar-border-default)',
    borderStyle: 'solid',
    flexDirection: 'row',
    justifyContent: 'flex-start',
    alignItems: 'center',
    gap: 'var(--solar-inset-xs)',
    paddingTop: 'var(--solar-inset-2xs)',
    paddingRight: 'var(--solar-inset-sm)',
    paddingBottom: 'var(--solar-inset-2xs)',
    paddingLeft: 'var(--solar-inset-2xs)',
    '& .SolarToast-tag': {
      '& > *': {
        backgroundColor: 'var(--solar-color-surface-overlay)',
        borderColor: 'var(--solar-color-border-feedback-success-subtle)',
      },
    },
    '& .SolarToast-message': {
      color: 'var(--solar-color-text-primary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-body-sm)',
      lineHeight: 'var(--solar-type-line-height-body-sm)',
      letterSpacing: '-0.02em',
      textDecoration: 'none',
    },
    '& .SolarToast-action': {
      color: 'var(--solar-color-text-feedback-success)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-body-sm)',
      lineHeight: 'var(--solar-type-line-height-body-sm)',
      letterSpacing: '-0.02em',
      textDecoration: 'none',
    },
    '& .SolarToast-chevron': {
      width: 'var(--solar-icon-xs)',
      height: 'var(--solar-icon-xs)',
      color: 'var(--solar-color-icon-feedback-success)',
    },
  },
  sizes: {},
  appearances: {
    'status=neutral': {
      backgroundColor: 'var(--solar-color-surface-feedback-neutral-subtle)',
      borderColor: 'var(--solar-color-border-subtle)',
      '& .SolarToast-tag': {
        '& > *': {
          borderColor: 'var(--solar-color-border-subtle)',
        },
      },
      '& .SolarToast-action': {
        color: 'var(--solar-color-text-feedback-neutral)',
      },
      '& .SolarToast-chevron': {
        color: 'var(--solar-color-icon-feedback-neutral)',
      },
    },
    'status=warning': {
      backgroundColor: 'var(--solar-color-surface-feedback-warning-subtle)',
      borderColor: 'var(--solar-color-border-feedback-warning-subtle)',
      '& .SolarToast-tag': {
        '& > *': {
          borderColor: 'var(--solar-color-border-feedback-warning-subtle)',
        },
      },
      '& .SolarToast-action': {
        color: 'var(--solar-color-text-feedback-warning)',
      },
      '& .SolarToast-chevron': {
        color: 'var(--solar-color-icon-feedback-warning)',
      },
    },
    'status=danger': {
      backgroundColor: 'var(--solar-color-surface-feedback-danger-subtle)',
      borderColor: 'var(--solar-color-border-feedback-danger-subtle)',
      '& .SolarToast-tag': {
        '& > *': {
          borderColor: 'var(--solar-color-border-feedback-warning-subtle)',
        },
      },
      '& .SolarToast-action': {
        color: 'var(--solar-color-text-feedback-danger)',
      },
      '& .SolarToast-chevron': {
        color: 'var(--solar-color-icon-feedback-danger)',
      },
    },
    'status=info': {
      backgroundColor: 'var(--solar-color-surface-feedback-info-subtle)',
      borderColor: 'var(--solar-color-border-feedback-info-subtle)',
      '& .SolarToast-tag': {
        '& > *': {
          borderColor: 'var(--solar-color-border-feedback-info-subtle)',
        },
      },
      '& .SolarToast-action': {
        color: 'var(--solar-color-text-feedback-info)',
      },
      '& .SolarToast-chevron': {
        color: 'var(--solar-color-icon-feedback-info)',
      },
    },
  },
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarToastComposition = {
  root: {
    base: {
      present: true,
    },
  },
  tag: {
    base: {
      present: true,
      component: 'Tag',
      'variant.status': 'neutral',
      'variant.type': 'status',
    },
  },
  message: {
    base: {
      present: true,
    },
  },
  action: {
    base: {
      present: true,
    },
  },
  chevron: {
    base: {
      present: true,
      component: 'Icon/ChevronRight',
      'variant.solid': 'false',
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarToastStyle(props: SolarToastProps = {}): Style {
  const p: Record<string, unknown> = { ...solarToastDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `status=${p.status}`;
  const s = solarToastStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  return merge(s.reset, s.root, s.appearances[key]);
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarToastParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarToastParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarToastCompose(
  props: SolarToastProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarToastDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `status=${p.status}`;
  const size = '';
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarToastComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}
