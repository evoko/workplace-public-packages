// SOLAR Date Picker Day Cell recipe for MUI. Generated by @bwp-web/codegen from spec/components/date picker day cell.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export type SolarDatePickerDayCellRangeRole =
  'none' | 'start' | 'middle' | 'end' | 'preview-middle' | 'preview-end';

export interface SolarDatePickerDayCellProps {
  selected?: boolean;
  today?: boolean;
  disabled?: boolean;
  filled?: boolean;
  error?: boolean;
  rangeRole?: SolarDatePickerDayCellRangeRole;
}

export const solarDatePickerDayCellDefaults = {
  selected: false,
  today: false,
  disabled: false,
  filled: false,
  error: false,
  rangeRole: 'none',
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarDatePickerDayCellStyles = {
  reset: {
    display: 'inline-flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarDatePickerDayCell-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarDatePickerDayCell-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarDatePickerDayCell-glyph': {
      display: 'block',
      overflow: 'visible',
      flexShrink: '0',
    },
    '& .SolarDatePickerDayCell-drawnIcon': {
      flexShrink: '0',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
    cursor: 'pointer',
    outline: 'none',
    '&.SolarDatePickerDayCell-disabled': {
      cursor: 'default',
    },
  },
  root: {
    backgroundColor: 'transparent',
    borderColor: 'transparent',
    boxShadow: 'none',
    borderStyle: 'none',
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'center',
    gap: 'var(--solar-inset-none)',
    paddingTop: 'var(--solar-inset-none)',
    paddingRight: 'var(--solar-inset-none)',
    paddingBottom: 'var(--solar-inset-none)',
    paddingLeft: 'var(--solar-inset-none)',
    width: '36px',
    height: '36px',
    borderTopLeftRadius: 'var(--solar-radius-container)',
    borderTopRightRadius: 'var(--solar-radius-container)',
    borderBottomRightRadius: 'var(--solar-radius-container)',
    borderBottomLeftRadius: 'var(--solar-radius-container)',
    '--solar-border-style': 'solid',
    '& .SolarDatePickerDayCell-day': {
      color: 'var(--solar-color-text-primary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-body-md)',
      lineHeight: 'var(--solar-type-line-height-body-md)',
      letterSpacing: '-0.02286em',
      textDecoration: 'none',
    },
  },
  sizes: {},
  appearances: {
    'rangeRole=none': {
      '&:hover': {
        backgroundColor: 'var(--solar-color-surface-hover)',
      },
      '&:focus-visible': {
        backgroundColor: 'var(--solar-color-surface-hover)',
        borderColor: 'var(--solar-color-border-feedback-focus-strong)',
        boxShadow: 'var(--solar-shadow-focus-default)',
        borderWidth: 'var(--solar-border-default)',
        borderStyle: 'var(--solar-border-style, solid)',
      },
      '&.SolarDatePickerDayCell-today': {
        borderColor: 'var(--solar-color-border-medium)',
        borderWidth: 'var(--solar-border-default)',
        borderStyle: 'var(--solar-border-style, solid)',
        '& .SolarDatePickerDayCell-day': {
          color: 'var(--solar-color-action-primary-bg-default)',
        },
      },
      '&.SolarDatePickerDayCell-selected': {
        backgroundColor: 'var(--solar-color-action-primary-bg-default)',
        '& .SolarDatePickerDayCell-day': {
          color: 'var(--solar-color-action-primary-text-default)',
        },
      },
      '&.SolarDatePickerDayCell-disabled': {
        '& .SolarDatePickerDayCell-day': {
          color: 'var(--solar-color-text-tertiary)',
        },
      },
    },
    'rangeRole=start': {
      backgroundColor: 'var(--solar-color-action-primary-bg-default)',
      borderColor: 'var(--solar-color-action-primary-border-default)',
      borderWidth: 'var(--solar-border-default)',
      borderStyle: 'var(--solar-border-style, solid)',
      borderTopRightRadius: 'var(--solar-radius-none)',
      borderBottomRightRadius: 'var(--solar-radius-none)',
      '& .SolarDatePickerDayCell-day': {
        color: 'var(--solar-color-action-primary-text-default)',
      },
    },
    'rangeRole=middle': {
      backgroundColor: 'var(--solar-color-surface-feedback-neutral-subtle)',
      borderTopLeftRadius: 'var(--solar-radius-none)',
      borderTopRightRadius: 'var(--solar-radius-none)',
      borderBottomRightRadius: 'var(--solar-radius-none)',
      borderBottomLeftRadius: 'var(--solar-radius-none)',
    },
    'rangeRole=end': {
      backgroundColor: 'var(--solar-color-action-primary-bg-default)',
      borderColor: 'var(--solar-color-action-primary-border-default)',
      borderWidth: 'var(--solar-border-default)',
      borderStyle: 'var(--solar-border-style, solid)',
      borderTopLeftRadius: 'var(--solar-radius-none)',
      borderBottomLeftRadius: 'var(--solar-radius-none)',
      '& .SolarDatePickerDayCell-day': {
        color: 'var(--solar-color-action-primary-text-default)',
      },
    },
    'rangeRole=preview-middle': {
      backgroundColor: 'var(--solar-color-surface-hover)',
      borderTopLeftRadius: 'var(--solar-radius-none)',
      borderTopRightRadius: 'var(--solar-radius-none)',
      borderBottomRightRadius: 'var(--solar-radius-none)',
      borderBottomLeftRadius: 'var(--solar-radius-none)',
    },
    'rangeRole=preview-end': {
      backgroundColor: 'var(--solar-color-surface-hover)',
      borderColor: 'var(--solar-color-border-subtle)',
      borderWidth: 'var(--solar-border-default)',
      borderStyle: 'var(--solar-border-style, solid)',
      borderTopLeftRadius: 'var(--solar-radius-none)',
      borderTopRightRadius: 'var(--solar-radius-pill)',
      borderBottomRightRadius: 'var(--solar-radius-pill)',
      borderBottomLeftRadius: 'var(--solar-radius-none)',
      '--solar-border-style': 'dashed',
    },
  },
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarDatePickerDayCellComposition = {
  root: {
    base: {
      present: true,
    },
  },
  day: {
    base: {
      present: true,
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarDatePickerDayCellStyle(
  props: SolarDatePickerDayCellProps = {},
): Style {
  const p: Record<string, unknown> = { ...solarDatePickerDayCellDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `rangeRole=${p.rangeRole}`;
  const s = solarDatePickerDayCellStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  return merge(s.reset, s.root, s.appearances[key]);
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarDatePickerDayCellParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarDatePickerDayCellParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarDatePickerDayCellCompose(
  props: SolarDatePickerDayCellProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarDatePickerDayCellDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `rangeRole=${p.rangeRole}`;
  const size = '';
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarDatePickerDayCellComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}

/** Each layer's children, in Figma's order: the tree the shell draws (`drawChildren`). */
export const solarDatePickerDayCellTree: Record<string, string[]> = {
  root: ['day'],
};

/** Each slot's layer, to the slot it is: its class is `SolarDatePickerDayCell-<slot>`, public. */
export const solarDatePickerDayCellSlots: Record<string, string> = {
  day: 'day',
};
