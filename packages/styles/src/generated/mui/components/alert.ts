// SOLAR Alert recipe for MUI. Generated by @bwp-web/codegen from spec/components/alert.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export type SolarAlertType =
  'default' | 'info' | 'success' | 'warning' | 'danger';
export type SolarAlertVariant = 'filled' | 'outlined';

export interface SolarAlertProps {
  type?: SolarAlertType;
  variant?: SolarAlertVariant;
}

export const solarAlertDefaults = {
  type: 'success',
  variant: 'filled',
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarAlertStyles = {
  reset: {
    display: 'inline-flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarAlert-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarAlert-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarAlert-glyph': {
      display: 'block',
      overflow: 'visible',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
    '& button.SolarAlert-action': {
      position: 'relative',
    },
    '& button.SolarAlert-action::after': {
      content: '""',
      position: 'absolute',
      top: '50%',
      left: '50%',
      width: 'max(100%, 44px)',
      height: 'max(100%, 44px)',
      transform: 'translate(-50%, -50%)',
    },
  },
  root: {
    backgroundColor: 'var(--solar-color-surface-feedback-success-subtle)',
    borderColor: 'var(--solar-color-border-feedback-success-subtle)',
    boxShadow: 'var(--solar-shadow-dialog)',
    borderRadius: 'var(--solar-radius-container)',
    borderWidth: 'var(--solar-border-default)',
    borderStyle: 'solid',
    flexDirection: 'row',
    justifyContent: 'flex-start',
    alignItems: 'flex-start',
    gap: 'var(--solar-inset-sm)',
    paddingTop: 'var(--solar-inset-md)',
    paddingRight: 'var(--solar-inset-md)',
    paddingBottom: 'var(--solar-inset-md)',
    paddingLeft: 'var(--solar-inset-md)',
    width: '100%',
    '& .SolarAlert-content': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'column',
      justifyContent: 'flex-start',
      alignItems: 'flex-start',
      gap: 'var(--solar-inset-sm)',
      paddingTop: 'var(--solar-stack-2xs)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-stack-2xs)',
      paddingLeft: 'var(--solar-inset-none)',
      width: '100%',
    },
    '& .SolarAlert-title': {
      color: 'var(--solar-color-text-feedback-success)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-body-lg)',
      lineHeight: 'var(--solar-type-line-height-body-lg)',
      letterSpacing: '-0.02em',
      textDecoration: 'none',
    },
    '& .SolarAlert-description': {
      color: 'var(--solar-color-text-primary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-400)',
      fontSize: 'var(--solar-type-size-body-md)',
      lineHeight: 'var(--solar-type-line-height-body-md)',
      letterSpacing: '-0.02286em',
      textDecoration: 'none',
    },
    '& .SolarAlert-action': {
      color: 'var(--solar-color-text-feedback-success)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-body-md)',
      lineHeight: 'var(--solar-type-line-height-body-md)',
      letterSpacing: '-0.02286em',
      textDecoration: 'none',
    },
  },
  sizes: {},
  appearances: {
    'type=default, variant=filled': {
      backgroundColor: 'var(--solar-color-surface-feedback-neutral-subtle)',
      borderColor: 'var(--solar-color-border-subtle)',
      '& .SolarAlert-title': {
        color: 'var(--solar-color-text-primary)',
      },
      '& .SolarAlert-action': {
        color: 'var(--solar-color-text-primary)',
      },
    },
    'type=default, variant=outlined': {
      backgroundColor: 'var(--solar-color-surface-base)',
      borderColor: 'var(--solar-color-border-subtle)',
      '& .SolarAlert-title': {
        color: 'var(--solar-color-text-primary)',
      },
      '& .SolarAlert-action': {
        color: 'var(--solar-color-text-primary)',
      },
    },
    'type=info, variant=filled': {
      backgroundColor: 'var(--solar-color-surface-feedback-info-subtle)',
      borderColor: 'var(--solar-color-border-feedback-info-subtle)',
      '& .SolarAlert-title': {
        color: 'var(--solar-color-text-feedback-info)',
      },
      '& .SolarAlert-action': {
        color: 'var(--solar-color-text-feedback-info)',
      },
    },
    'type=info, variant=outlined': {
      backgroundColor: 'var(--solar-color-surface-base)',
      borderColor: 'var(--solar-color-border-subtle)',
      '& .SolarAlert-title': {
        color: 'var(--solar-color-text-feedback-info)',
      },
      '& .SolarAlert-action': {
        color: 'var(--solar-color-text-feedback-info)',
      },
    },
    'type=success, variant=outlined': {
      backgroundColor: 'var(--solar-color-surface-base)',
      borderColor: 'var(--solar-color-border-subtle)',
    },
    'type=warning, variant=filled': {
      backgroundColor: 'var(--solar-color-surface-feedback-warning-subtle)',
      borderColor: 'var(--solar-color-border-feedback-warning-subtle)',
      '& .SolarAlert-title': {
        color: 'var(--solar-color-text-feedback-warning)',
      },
      '& .SolarAlert-action': {
        color: 'var(--solar-color-text-feedback-warning)',
      },
    },
    'type=warning, variant=outlined': {
      backgroundColor: 'var(--solar-color-surface-base)',
      borderColor: 'var(--solar-color-border-subtle)',
      '& .SolarAlert-title': {
        color: 'var(--solar-color-text-feedback-warning)',
      },
      '& .SolarAlert-action': {
        color: 'var(--solar-color-text-feedback-warning)',
      },
    },
    'type=danger, variant=filled': {
      backgroundColor: 'var(--solar-color-surface-feedback-danger-subtle)',
      borderColor: 'var(--solar-color-border-feedback-danger-subtle)',
      '& .SolarAlert-title': {
        color: 'var(--solar-color-text-feedback-danger)',
      },
      '& .SolarAlert-action': {
        color: 'var(--solar-color-text-feedback-danger)',
      },
    },
    'type=danger, variant=outlined': {
      backgroundColor: 'var(--solar-color-surface-base)',
      borderColor: 'var(--solar-color-border-subtle)',
      '& .SolarAlert-title': {
        color: 'var(--solar-color-text-feedback-danger)',
      },
      '& .SolarAlert-action': {
        color: 'var(--solar-color-text-feedback-danger)',
      },
    },
  },
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarAlertComposition = {
  root: {
    base: {
      present: true,
    },
  },
  statusIndicator: {
    base: {
      present: true,
      component: 'StatusIndicator',
      'variant.type': 'success',
      'variant.size': 'md',
    },
    appearance: {
      'type=warning, variant=filled': {
        default: {
          'variant.type': 'warning',
        },
      },
      'type=warning, variant=outlined': {
        default: {
          'variant.type': 'warning',
        },
      },
      'type=danger, variant=filled': {
        default: {
          'variant.type': 'danger',
        },
      },
      'type=danger, variant=outlined': {
        default: {
          'variant.type': 'danger',
        },
      },
      'type=info, variant=filled': {
        default: {
          'variant.type': 'info',
        },
      },
      'type=info, variant=outlined': {
        default: {
          'variant.type': 'info',
        },
      },
      'type=default, variant=filled': {
        default: {
          'variant.type': 'neutral',
        },
      },
      'type=default, variant=outlined': {
        default: {
          'variant.type': 'neutral',
        },
      },
    },
  },
  content: {
    base: {
      present: true,
    },
  },
  title: {
    base: {
      present: true,
    },
  },
  description: {
    base: {
      present: true,
    },
  },
  action: {
    base: {
      present: true,
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarAlertStyle(props: SolarAlertProps = {}): Style {
  const p: Record<string, unknown> = { ...solarAlertDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `type=${p.type}, variant=${p.variant}`;
  const s = solarAlertStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  return merge(s.reset, s.root, s.appearances[key]);
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarAlertParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarAlertParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarAlertCompose(
  props: SolarAlertProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarAlertDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `type=${p.type}, variant=${p.variant}`;
  const size = '';
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarAlertComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}
