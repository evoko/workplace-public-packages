// SOLAR All-Day Bar recipe for MUI. Generated by @bwp-web/codegen from spec/components/all-day bar.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export type SolarAllDayBarVariant = 'subtle' | 'solid';
export type SolarAllDayBarSpan = 'single' | 'start' | 'middle' | 'end';
export type SolarAllDayBarCategory =
  | 'red'
  | 'orange'
  | 'yellow'
  | 'green'
  | 'turquoise'
  | 'blue'
  | 'purple'
  | 'pink';

export interface SolarAllDayBarProps {
  variant?: SolarAllDayBarVariant;
  span?: SolarAllDayBarSpan;
  category?: SolarAllDayBarCategory;
}

export const solarAllDayBarDefaults = {
  variant: 'subtle',
  span: 'single',
  category: 'blue',
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarAllDayBarStyles = {
  reset: {
    display: 'inline-flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarAllDayBar-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarAllDayBar-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarAllDayBar-glyph': {
      display: 'block',
      overflow: 'visible',
      flexShrink: '0',
    },
    '& .SolarAllDayBar-drawnIcon': {
      flexShrink: '0',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
    '& .SolarAllDayBar--stripe, & .SolarAllDayBar-time': {
      flexShrink: 0,
      whiteSpace: 'nowrap',
    },
    '& .SolarAllDayBar-title': {
      minWidth: 0,
      overflow: 'hidden',
      textOverflow: 'ellipsis',
      whiteSpace: 'nowrap',
    },
  },
  root: {
    backgroundColor: 'transparent',
    borderColor: 'transparent',
    boxShadow: 'none',
    borderRadius: 'var(--solar-radius-none)',
    borderStyle: 'none',
    flexDirection: 'row',
    justifyContent: 'flex-start',
    alignItems: 'center',
    gap: 'var(--solar-inset-none)',
    paddingTop: 'var(--solar-inset-none)',
    paddingRight: 'var(--solar-inset-none)',
    paddingBottom: 'var(--solar-inset-none)',
    paddingLeft: 'var(--solar-inset-none)',
    width: '100%',
    height: '22px',
    '& .SolarAllDayBar--stripe': {
      backgroundColor: 'var(--solar-color-data-category-06-strong)',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-pill)',
      borderStyle: 'none',
      width: '3px',
      minWidth: '3px',
      height: '100%',
      minHeight: 0,
    },
    '& .SolarAllDayBar--content': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'row',
      justifyContent: 'flex-start',
      alignItems: 'center',
      gap: 'var(--solar-stack-xs)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-sm)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-sm)',
      width: '100%',
      minWidth: 0,
      height: '100%',
      minHeight: 0,
    },
    '& .SolarAllDayBar-time': {
      color: 'var(--solar-color-text-secondary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-body-sm)',
      lineHeight: 'var(--solar-type-line-height-body-sm)',
      letterSpacing: '-0.02em',
      textDecoration: 'none',
    },
    '& .SolarAllDayBar-title': {
      color: 'var(--solar-color-text-primary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-body-sm)',
      lineHeight: 'var(--solar-type-line-height-body-sm)',
      letterSpacing: '-0.02em',
      textDecoration: 'none',
      width: '100%',
      minWidth: 0,
    },
  },
  sizes: {},
  appearances: {
    'variant=solid, span=single': {
      backgroundColor: 'var(--solar-color-data-category-06-strong)',
      borderRadius: 'var(--solar-radius-control)',
      justifyContent: 'flex-start',
      alignItems: 'flex-start',
      '& .SolarAllDayBar--stripe': {
        borderRadius: 'var(--solar-radius-none)',
        height: '25px',
        minHeight: '25px',
      },
      '& .SolarAllDayBar-time': {
        color: 'var(--solar-color-text-inverse)',
      },
      '& .SolarAllDayBar-title': {
        color: 'var(--solar-color-text-inverse)',
        fontFamily: 'var(--solar-type-font-family-inter)',
        fontWeight: 'var(--solar-type-font-weight-600)',
        fontSize: 'var(--solar-type-size-body-sm)',
        lineHeight: 'var(--solar-type-line-height-body-sm)',
        letterSpacing: '-0.02em',
        textDecoration: 'none',
      },
    },
    'variant=solid, span=start': {
      backgroundColor: 'var(--solar-color-data-category-06-strong)',
      borderRadius: 'var(--solar-radius-control)',
      justifyContent: 'flex-start',
      alignItems: 'flex-start',
      '& .SolarAllDayBar--stripe': {
        borderRadius: 'var(--solar-radius-none)',
        height: '25px',
        minHeight: '25px',
      },
      '& .SolarAllDayBar-time': {
        color: 'var(--solar-color-text-inverse)',
      },
      '& .SolarAllDayBar-title': {
        color: 'var(--solar-color-text-inverse)',
        fontFamily: 'var(--solar-type-font-family-inter)',
        fontWeight: 'var(--solar-type-font-weight-600)',
        fontSize: 'var(--solar-type-size-body-sm)',
        lineHeight: 'var(--solar-type-line-height-body-sm)',
        letterSpacing: '-0.02em',
        textDecoration: 'none',
      },
    },
    'variant=solid, span=middle': {
      backgroundColor: 'var(--solar-color-data-category-06-strong)',
      borderRadius: 'var(--solar-radius-control)',
      justifyContent: 'flex-start',
      alignItems: 'flex-start',
      '& .SolarAllDayBar--stripe': {
        borderRadius: 'var(--solar-radius-none)',
        height: '25px',
        minHeight: '25px',
      },
      '& .SolarAllDayBar-time': {
        color: 'var(--solar-color-text-inverse)',
      },
      '& .SolarAllDayBar-title': {
        color: 'var(--solar-color-text-inverse)',
        fontFamily: 'var(--solar-type-font-family-inter)',
        fontWeight: 'var(--solar-type-font-weight-600)',
        fontSize: 'var(--solar-type-size-body-sm)',
        lineHeight: 'var(--solar-type-line-height-body-sm)',
        letterSpacing: '-0.02em',
        textDecoration: 'none',
      },
    },
    'variant=solid, span=end': {
      backgroundColor: 'var(--solar-color-data-category-06-strong)',
      borderRadius: 'var(--solar-radius-control)',
      justifyContent: 'flex-start',
      alignItems: 'flex-start',
      '& .SolarAllDayBar--stripe': {
        borderRadius: 'var(--solar-radius-none)',
        height: '25px',
        minHeight: '25px',
      },
      '& .SolarAllDayBar-time': {
        color: 'var(--solar-color-text-inverse)',
      },
      '& .SolarAllDayBar-title': {
        color: 'var(--solar-color-text-inverse)',
        fontFamily: 'var(--solar-type-font-family-inter)',
        fontWeight: 'var(--solar-type-font-weight-600)',
        fontSize: 'var(--solar-type-size-body-sm)',
        lineHeight: 'var(--solar-type-line-height-body-sm)',
        letterSpacing: '-0.02em',
        textDecoration: 'none',
      },
    },
  },
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarAllDayBarComposition = {
  root: {
    base: {
      present: true,
    },
  },
  stripe: {
    base: {
      present: true,
    },
    appearance: {
      'variant=solid, span=single': {
        default: {
          present: false,
        },
      },
      'variant=solid, span=start': {
        default: {
          present: false,
        },
      },
      'variant=solid, span=middle': {
        default: {
          present: false,
        },
      },
      'variant=solid, span=end': {
        default: {
          present: false,
        },
      },
    },
  },
  content: {
    base: {
      present: true,
    },
  },
  time: {
    base: {
      present: true,
    },
  },
  title: {
    base: {
      present: true,
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarAllDayBarStyle(props: SolarAllDayBarProps = {}): Style {
  const p: Record<string, unknown> = { ...solarAllDayBarDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `variant=${p.variant}, span=${p.span}`;
  const s = solarAllDayBarStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  return tint(merge(s.reset, s.root, s.appearances[key]), p);
}

/** Each tinting axis: the default's colour family, and each value's (the overlay's tint). */
export const solarAllDayBarTints = {
  category: {
    from: '--solar-color-data-category-06-',
    values: {
      red: '--solar-color-data-category-01-',
      orange: '--solar-color-data-category-02-',
      yellow: '--solar-color-data-category-03-',
      green: '--solar-color-data-category-04-',
      turquoise: '--solar-color-data-category-05-',
      blue: '--solar-color-data-category-06-',
      purple: '--solar-color-data-category-07-',
      pink: '--solar-color-data-category-08-',
    },
  },
} as const;

function swap(style: Style, from: string, to: string): Style {
  const out: Style = {};
  for (const [k, v] of Object.entries(style))
    out[k] = typeof v === 'string' ? v.replaceAll(from, to) : swap(v, from, to);
  return out;
}

/** The style in the colours the caller's tinting values pick. */
function tint(style: Style, p: Record<string, unknown>): Style {
  let out = style;
  for (const [axis, t] of Object.entries(solarAllDayBarTints)) {
    const to = (t.values as Record<string, string>)[p[axis] as string];
    if (to && to !== t.from) out = swap(out, t.from, to);
  }
  return out;
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarAllDayBarParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarAllDayBarParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarAllDayBarCompose(
  props: SolarAllDayBarProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarAllDayBarDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `variant=${p.variant}, span=${p.span}`;
  const size = '';
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarAllDayBarComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}

/** Each layer's children, in Figma's order: the tree the shell draws (`drawChildren`). */
export const solarAllDayBarTree: Record<string, string[]> = {
  root: ['stripe', 'content'],
  content: ['time', 'title'],
};

/** Each slot's layer, to the slot it is: its class is `SolarAllDayBar-<slot>`, public. */
export const solarAllDayBarSlots: Record<string, string> = {
  time: 'time',
  title: 'title',
};
