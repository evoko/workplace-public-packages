// SOLAR Segmented Control recipe for MUI. Generated by @bwp-web/codegen from spec/components/segmented control.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export type SolarSegmentedControlSize = 'md' | 'sm';

export interface SolarSegmentedControlProps {
  size?: SolarSegmentedControlSize;
}

export const solarSegmentedControlDefaults = {
  size: 'md',
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarSegmentedControlStyles = {
  reset: {
    display: 'inline-flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarSegmentedControl-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarSegmentedControl-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarSegmentedControl-glyph': {
      display: 'block',
      overflow: 'visible',
      flexShrink: '0',
    },
    '& .SolarSegmentedControl-drawnIcon': {
      flexShrink: '0',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
  },
  root: {
    backgroundColor: 'transparent',
    borderColor: 'transparent',
    boxShadow: 'none',
    borderRadius: 'var(--solar-radius-none)',
    borderStyle: 'none',
    flexDirection: 'column',
    justifyContent: 'flex-start',
    alignItems: 'flex-start',
    gap: 'var(--solar-inset-xs)',
    paddingTop: 'var(--solar-inset-none)',
    paddingRight: 'var(--solar-inset-none)',
    paddingBottom: 'var(--solar-inset-none)',
    paddingLeft: 'var(--solar-inset-none)',
    '& .SolarSegmentedControl-label': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'row',
      justifyContent: 'flex-start',
      alignItems: 'flex-start',
      gap: 'var(--solar-inset-2xs)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
    },
    '& .SolarSegmentedControl--labelLabel': {
      color: 'var(--solar-color-text-primary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-label-md)',
      lineHeight: 'var(--solar-type-line-height-label-md)',
      letterSpacing: '-0.02em',
      textDecoration: 'none',
    },
    '& .SolarSegmentedControl-mandatory': {
      color: 'var(--solar-color-text-feedback-info)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-label-md)',
      lineHeight: 'var(--solar-type-line-height-label-md)',
      letterSpacing: '-0.02em',
      textDecoration: 'none',
    },
    '& .SolarSegmentedControl-track': {
      backgroundColor: 'var(--solar-color-surface-background)',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-container)',
      borderStyle: 'none',
      flexDirection: 'row',
      justifyContent: 'flex-start',
      alignItems: 'flex-start',
      gap: 'var(--solar-inset-none)',
      paddingTop: 'var(--solar-inset-2xs)',
      paddingRight: 'var(--solar-inset-2xs)',
      paddingBottom: 'var(--solar-inset-2xs)',
      paddingLeft: 'var(--solar-inset-2xs)',
      width: '100%',
      minWidth: 0,
    },
    '& .SolarSegmentedControl-helper': {
      color: 'var(--solar-color-text-secondary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-400)',
      fontSize: 'var(--solar-type-size-helper-md)',
      lineHeight: 'var(--solar-type-line-height-helper-md)',
      letterSpacing: '-0.02em',
      textDecoration: 'none',
    },
  },
  sizes: {
    sm: {
      '& .SolarSegmentedControl--labelLabel': {
        fontFamily: 'var(--solar-type-font-family-inter)',
        fontWeight: 'var(--solar-type-font-weight-500)',
        fontSize: 'var(--solar-type-size-label-sm)',
        lineHeight: 'var(--solar-type-line-height-label-sm)',
        letterSpacing: '-0.02em',
        textDecoration: 'none',
      },
      '& .SolarSegmentedControl-mandatory': {
        fontFamily: 'var(--solar-type-font-family-inter)',
        fontWeight: 'var(--solar-type-font-weight-500)',
        fontSize: 'var(--solar-type-size-label-sm)',
        lineHeight: 'var(--solar-type-line-height-label-sm)',
        letterSpacing: '-0.02em',
        textDecoration: 'none',
      },
      '& .SolarSegmentedControl-track': {
        width: 'auto',
        minWidth: 'auto',
      },
      '& .SolarSegmentedControl-helper': {
        fontFamily: 'var(--solar-type-font-family-inter)',
        fontWeight: 'var(--solar-type-font-weight-400)',
        fontSize: 'var(--solar-type-size-helper-sm)',
        lineHeight: 'var(--solar-type-line-height-helper-sm)',
        letterSpacing: '-0.02em',
        textDecoration: 'none',
      },
    },
  },
  appearances: {},
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarSegmentedControlComposition = {
  root: {
    base: {
      present: true,
    },
  },
  label: {
    base: {
      present: false,
    },
  },
  labelLabel: {
    base: {
      present: false,
    },
  },
  mandatory: {
    base: {
      present: true,
    },
  },
  track: {
    base: {
      present: true,
    },
  },
  segmentedControlItem: {
    base: {
      present: true,
      component: 'Segmented Control Item',
      'variant.selected': 'true',
      'variant.state': 'default',
      'variant.size': 'md',
    },
    size: {
      sm: {
        'variant.size': 'sm',
      },
    },
  },
  segmentedControlItem2: {
    base: {
      present: true,
      component: 'Segmented Control Item',
      'variant.selected': 'false',
      'variant.state': 'default',
      'variant.size': 'md',
    },
    size: {
      sm: {
        'variant.size': 'sm',
      },
    },
  },
  segmentedControlItem3: {
    base: {
      present: true,
      component: 'Segmented Control Item',
      'variant.selected': 'false',
      'variant.state': 'default',
      'variant.size': 'md',
    },
    size: {
      sm: {
        'variant.size': 'sm',
      },
    },
  },
  segmentedControlItem4: {
    base: {
      present: true,
      component: 'Segmented Control Item',
      'variant.selected': 'false',
      'variant.state': 'default',
      'variant.size': 'md',
    },
    size: {
      sm: {
        'variant.size': 'sm',
      },
    },
  },
  segmentedControlItem5: {
    base: {
      present: true,
      component: 'Segmented Control Item',
      'variant.selected': 'false',
      'variant.state': 'default',
      'variant.size': 'md',
    },
    size: {
      sm: {
        'variant.size': 'sm',
      },
    },
  },
  segmentedControlItem6: {
    base: {
      present: true,
      component: 'Segmented Control Item',
      'variant.selected': 'false',
      'variant.state': 'default',
      'variant.size': 'md',
    },
    size: {
      sm: {
        'variant.size': 'sm',
      },
    },
  },
  helper: {
    base: {
      present: false,
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarSegmentedControlStyle(
  props: SolarSegmentedControlProps = {},
): Style {
  const p: Record<string, unknown> = { ...solarSegmentedControlDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `default`;
  const s = solarSegmentedControlStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  const size = p.size as string;
  return merge(
    s.reset,
    s.root,
    s.sizes[size],
    s.appearances[key],
    s.combined[size]?.[key],
  );
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarSegmentedControlParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarSegmentedControlParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarSegmentedControlCompose(
  props: SolarSegmentedControlProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarSegmentedControlDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `default`;
  const size = p.size as string;
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarSegmentedControlComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}

/** Each layer's children, in Figma's order: the tree the shell draws (`drawChildren`). */
export const solarSegmentedControlTree: Record<string, string[]> = {
  root: ['label', 'track', 'helper'],
  label: ['labelLabel', 'mandatory'],
  track: [
    'segmentedControlItem',
    'segmentedControlItem2',
    'segmentedControlItem3',
    'segmentedControlItem4',
    'segmentedControlItem5',
    'segmentedControlItem6',
  ],
};

/** Each slot's layer, to the slot it is: its class is `SolarSegmentedControl-<slot>`, public. */
export const solarSegmentedControlSlots: Record<string, string> = {
  label: 'label',
  mandatory: 'mandatory',
  track: 'track',
  helper: 'helper',
};
