// SOLAR Coachmark recipe for MUI. Generated by @bwp-web/codegen from spec/components/coachmark.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export type SolarCoachmarkSide = 'left' | 'right';

export interface SolarCoachmarkProps {
  side?: SolarCoachmarkSide;
}

export const solarCoachmarkDefaults = {
  side: 'right',
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarCoachmarkStyles = {
  reset: {
    display: 'inline-flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarCoachmark-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarCoachmark-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarCoachmark-glyph': {
      display: 'block',
      overflow: 'visible',
      flexShrink: '0',
    },
    '& .SolarCoachmark-drawnIcon': {
      flexShrink: '0',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
    '& .SolarCoachmark-title, & .SolarCoachmark-body, & .SolarCoachmark-counter':
      {
        whiteSpace: 'normal',
      },
    '& .SolarCoachmark--close > svg': {
      display: 'block',
      width: '100%',
      height: '100%',
    },
    '& button.SolarCoachmark--close': {
      appearance: 'none',
      border: '0',
      padding: '0',
      margin: '0',
      background: 'none',
      cursor: 'pointer',
    },
    '& button.SolarCoachmark--close::after': {
      content: '""',
      position: 'absolute',
      top: '50%',
      left: '50%',
      width: 'max(100%, var(--solar-size-target-min))',
      height: 'max(100%, var(--solar-size-target-min))',
      transform: 'translate(-50%, -50%)',
    },
    '& .SolarCoachmark--connector': {
      pointerEvents: 'none',
    },
  },
  root: {
    backgroundColor: 'transparent',
    borderColor: 'transparent',
    boxShadow: 'none',
    borderRadius: 'var(--solar-radius-none)',
    borderStyle: 'none',
    '--solar-placed-left': '0px',
    '--solar-placed-top': '0px',
    '--solar-placed-right': '0px',
    flexDirection: 'row',
    justifyContent: 'flex-start',
    alignItems: 'center',
    gap: 'var(--solar-inset-none)',
    paddingTop: 'var(--solar-inset-none)',
    paddingRight: 'var(--solar-inset-none)',
    paddingBottom: 'var(--solar-inset-none)',
    paddingLeft: 'var(--solar-inset-none)',
    '& .SolarCoachmark--card': {
      backgroundColor: 'var(--solar-color-surface-inverse)',
      borderColor: 'transparent',
      boxShadow: 'var(--solar-shadow-overlay)',
      borderRadius: 'var(--solar-radius-container)',
      borderStyle: 'none',
      flexDirection: 'column',
      justifyContent: 'flex-start',
      alignItems: 'flex-start',
      gap: 'var(--solar-stack-lg)',
      paddingTop: 'var(--solar-inset-md)',
      paddingRight: 'var(--solar-inset-md)',
      paddingBottom: 'var(--solar-inset-md)',
      paddingLeft: 'var(--solar-inset-md)',
      width: '320px',
    },
    '& .SolarCoachmark--header': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'row',
      justifyContent: 'flex-end',
      alignItems: 'center',
      gap: 'var(--solar-inset-xs)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
      width: '100%',
      position: 'relative',
    },
    '& .SolarCoachmark--close': {
      width: 'var(--solar-icon-md)',
      height: 'var(--solar-icon-md)',
      color: 'var(--solar-color-icon-inverse)',
    },
    '& .SolarCoachmark-title': {
      color: 'var(--solar-color-text-inverse)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-title-sm)',
      lineHeight: 'var(--solar-type-line-height-title-sm)',
      letterSpacing: '-0.03em',
      textDecoration: 'none',
      position: 'absolute',
      left: 'calc(50% + 0.5px)',
      '--solar-placed-cx': '-50%',
      translate: 'var(--solar-placed-cx, 0px) var(--solar-placed-cy, 0px)',
      top: 'calc(50% + 0px)',
      '--solar-placed-cy': '-50%',
    },
    '& .SolarCoachmark-body': {
      color: 'var(--solar-color-text-inverse)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-400)',
      fontSize: 'var(--solar-type-size-body-sm)',
      lineHeight: 'var(--solar-type-line-height-body-sm)',
      letterSpacing: '-0.02em',
      textDecoration: 'none',
      width: '100%',
    },
    '& .SolarCoachmark-counter': {
      color: 'var(--solar-color-text-tertiary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-body-md)',
      lineHeight: 'var(--solar-type-line-height-body-md)',
      letterSpacing: '-0.02286em',
      textDecoration: 'none',
      width: '100%',
    },
    '& .SolarCoachmark-actions': {
      width: '100%',
    },
    '& .SolarCoachmark--connector': {
      left: 'auto',
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      '--solar-placed-left': '0px',
      '--solar-placed-top': '0px',
      '--solar-placed-right': '0px',
      flexDirection: 'row',
      justifyContent: 'flex-start',
      alignItems: 'center',
      gap: 'var(--solar-inset-md)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
      width: '100px',
      height: '6px',
      position: 'absolute',
      right: 'calc(-100px - var(--solar-placed-right, 0px))',
      top: 'calc(50% + 0.5px)',
      '--solar-placed-cy': '-50%',
      translate: 'var(--solar-placed-cx, 0px) var(--solar-placed-cy, 0px)',
    },
    '& .SolarCoachmark--line': {
      fill: 'var(--solar-color-surface-feedback-info-strong)',
      stroke: 'none',
      '& .SolarGlyph-stroke': {
        fill: 'none',
      },
      strokeWidth: '0',
      width: '100%',
      height: 'var(--solar-border-default)',
    },
    '& .SolarCoachmark--originDot': {
      backgroundColor: 'var(--solar-color-surface-feedback-info-strong)',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-pill)',
      borderStyle: 'none',
      width: '6px',
      height: '6px',
      position: 'absolute',
      left: 'calc(-3px - var(--solar-placed-left, 0px))',
      top: 'calc(0px - var(--solar-placed-top, 0px))',
    },
    '& .SolarCoachmark--nodeEnd': {
      position: 'absolute',
      right: 'calc(-6px - var(--solar-placed-right, 0px))',
      top: 'calc(-3px - var(--solar-placed-top, 0px))',
    },
    position: 'relative',
  },
  sizes: {},
  appearances: {
    'side=left': {
      '& .SolarCoachmark--connector': {
        right: 'auto',
        height: '7px',
        position: 'absolute',
        top: 'calc(50% + 0px)',
        '--solar-placed-cy': '-50%',
        translate: 'var(--solar-placed-cx, 0px) var(--solar-placed-cy, 0px)',
        left: 'calc(-100px - var(--solar-placed-left, 0px))',
      },
      '& .SolarCoachmark--originDot': {
        position: 'absolute',
        left: 'calc(97px - var(--solar-placed-left, 0px))',
        top: 'calc(1px - var(--solar-placed-top, 0px))',
      },
      '& .SolarCoachmark--nodeEnd': {
        position: 'absolute',
        right: 'calc(94px - var(--solar-placed-right, 0px))',
        top: 'calc(-2px - var(--solar-placed-top, 0px))',
      },
    },
  },
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarCoachmarkComposition = {
  root: {
    base: {
      present: true,
    },
  },
  card: {
    base: {
      present: true,
    },
  },
  header: {
    base: {
      present: true,
    },
  },
  close: {
    base: {
      present: true,
      component: 'Icon/Close',
      'variant.solid': 'false',
    },
  },
  title: {
    base: {
      present: true,
    },
  },
  body: {
    base: {
      present: true,
    },
  },
  counter: {
    base: {
      present: true,
    },
  },
  actions: {
    base: {
      present: true,
      component: 'Button Group',
      'variant.orientation': 'horizontal',
      'variant.type': 'regular',
    },
  },
  connector: {
    base: {
      present: true,
    },
  },
  line: {
    base: {
      present: true,
      glyph: {
        width: 100,
        height: 1,
        fill: [
          {
            d: 'M100 0L100 1L0 1L0 0L100 0Z',
            evenOdd: false,
          },
        ],
        stroke: [],
      },
    },
  },
  originDot: {
    base: {
      present: true,
    },
  },
  nodeEnd: {
    base: {
      present: true,
      component: 'Node End',
      'variant.halo': 'true',
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarCoachmarkStyle(props: SolarCoachmarkProps = {}): Style {
  const p: Record<string, unknown> = { ...solarCoachmarkDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `side=${p.side}`;
  const s = solarCoachmarkStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  return merge(s.reset, s.root, s.appearances[key]);
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarCoachmarkParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarCoachmarkParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarCoachmarkCompose(
  props: SolarCoachmarkProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarCoachmarkDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `side=${p.side}`;
  const size = '';
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarCoachmarkComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}

/** Each layer's children, in Figma's order: the tree the shell draws (`drawChildren`). */
export const solarCoachmarkTree: Record<string, string[]> = {
  root: ['card', 'connector'],
  card: ['header', 'body', 'counter', 'actions'],
  header: ['close', 'title'],
  connector: ['line', 'originDot', 'nodeEnd'],
};

/** Each slot's layer, to the slot it is: its class is `SolarCoachmark-<slot>`, public. */
export const solarCoachmarkSlots: Record<string, string> = {
  title: 'title',
  body: 'body',
  counter: 'counter',
  actions: 'actions',
};
