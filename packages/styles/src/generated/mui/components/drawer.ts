// SOLAR Drawer recipe for MUI. Generated by @bwp-web/codegen from spec/components/drawer.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export type SolarDrawerProps = Record<never, never>;

export const solarDrawerDefaults = {} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarDrawerStyles = {
  reset: {
    display: 'flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarDrawer-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarDrawer-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarDrawer-glyph': {
      display: 'block',
      overflow: 'visible',
      flexShrink: '0',
    },
    '& .SolarDrawer-drawnIcon': {
      flexShrink: '0',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
    backgroundImage: 'none',
    maxWidth: '100%',
    '& .SolarDrawer-title': {
      whiteSpace: 'normal',
      minWidth: '0',
    },
    '& .SolarDrawer-content': {
      overflowY: 'auto',
      minHeight: '0',
    },
    '& .SolarDrawer--close': {
      flexShrink: '0',
    },
  },
  root: {
    backgroundColor: 'var(--solar-color-surface-raised)',
    borderColor: 'var(--solar-color-border-surface)',
    boxShadow: 'var(--solar-shadow-overlay)',
    borderTopLeftRadius: 'var(--solar-radius-dialog)',
    borderTopRightRadius: 'var(--solar-radius-none)',
    borderBottomRightRadius: 'var(--solar-radius-none)',
    borderBottomLeftRadius: 'var(--solar-radius-dialog)',
    borderTopStyle: 'none',
    borderRightStyle: 'none',
    borderBottomStyle: 'none',
    borderLeftWidth: 'var(--solar-border-default)',
    borderLeftStyle: 'solid',
    flexDirection: 'column',
    justifyContent: 'flex-start',
    alignItems: 'flex-start',
    gap: 'var(--solar-stack-none)',
    paddingTop: 'var(--solar-inset-none)',
    paddingRight: 'var(--solar-inset-none)',
    paddingBottom: 'var(--solar-inset-none)',
    paddingLeft: 'var(--solar-inset-none)',
    width: '348px',
    height: '100%',
    '& .SolarDrawer--header': {
      backgroundColor: 'transparent',
      borderColor: 'var(--solar-color-border-surface)',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderTopStyle: 'none',
      borderRightStyle: 'none',
      borderBottomWidth: 'var(--solar-border-default)',
      borderBottomStyle: 'solid',
      borderLeftStyle: 'none',
      flexDirection: 'row',
      justifyContent: 'center',
      alignItems: 'center',
      gap: 'var(--solar-inset-none)',
      paddingTop: 'var(--solar-inset-xs)',
      paddingRight: 'var(--solar-inset-lg)',
      paddingBottom: 'var(--solar-inset-xs)',
      paddingLeft: 'var(--solar-inset-lg)',
      width: '100%',
    },
    '& .SolarDrawer-title': {
      color: 'var(--solar-color-text-primary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-body-lg)',
      lineHeight: 'var(--solar-type-line-height-body-lg)',
      letterSpacing: '-0.02em',
      textDecoration: 'none',
      width: '100%',
    },
    '& .SolarDrawer-content': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'column',
      justifyContent: 'flex-start',
      alignItems: 'flex-start',
      gap: 'var(--solar-stack-sm)',
      paddingTop: 'var(--solar-inset-md)',
      paddingRight: 'var(--solar-inset-lg)',
      paddingBottom: 'var(--solar-inset-md)',
      paddingLeft: 'var(--solar-inset-lg)',
      width: '100%',
      height: '100%',
    },
    '& .SolarDrawer-cta': {
      width: '100%',
    },
  },
  sizes: {},
  appearances: {},
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarDrawerComposition = {
  root: {
    base: {
      present: true,
    },
  },
  header: {
    base: {
      present: true,
    },
  },
  title: {
    base: {
      present: true,
    },
  },
  close: {
    base: {
      present: true,
      component: 'Icon Button',
      'variant.size': 'md',
      'variant.shape': 'square',
      'variant.prio': 'tertiary',
      'variant.state': 'default',
    },
  },
  content: {
    base: {
      present: true,
    },
  },
  cta: {
    base: {
      present: true,
      component: 'Button Group',
      'variant.orientation': 'horizontal',
      'variant.type': 'full-width',
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarDrawerStyle(props: SolarDrawerProps = {}): Style {
  const p: Record<string, unknown> = { ...solarDrawerDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `default`;
  const s = solarDrawerStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  return merge(s.reset, s.root, s.appearances[key]);
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarDrawerParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarDrawerParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarDrawerCompose(
  props: SolarDrawerProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarDrawerDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `default`;
  const size = '';
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarDrawerComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}

/** Each layer's children, in Figma's order: the tree the shell draws (`drawChildren`). */
export const solarDrawerTree: Record<string, string[]> = {
  root: ['header', 'content', 'cta'],
  header: ['title', 'close'],
};

/** Each slot's layer, to the slot it is: its class is `SolarDrawer-<slot>`, public. */
export const solarDrawerSlots: Record<string, string> = {
  title: 'title',
  content: 'content',
  cta: 'cta',
};
