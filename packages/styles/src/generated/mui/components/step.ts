// SOLAR Step recipe for MUI. Generated by @bwp-web/codegen from spec/components/step.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export type SolarStepStatus = 'error' | 'complete' | 'active' | 'upcoming';
export type SolarStepType = 'round' | 'horizontal';

export interface SolarStepProps {
  status?: SolarStepStatus;
  type?: SolarStepType;
}

export const solarStepDefaults = {
  status: 'complete',
  type: 'round',
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarStepStyles = {
  reset: {
    display: 'flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarStep-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarStep-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarStep-glyph': {
      display: 'block',
      overflow: 'visible',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
    '&:is(button)': {
      position: 'relative',
    },
    '&:is(button)::after': {
      content: '""',
      position: 'absolute',
      top: '50%',
      left: '50%',
      width: 'max(100%, 44px)',
      height: 'max(100%, 44px)',
      transform: 'translate(-50%, -50%)',
    },
  },
  root: {
    backgroundColor: 'transparent',
    borderColor: 'transparent',
    boxShadow: 'none',
    borderRadius: 'var(--solar-radius-none)',
    borderStyle: 'none',
    flexDirection: 'column',
    justifyContent: 'flex-start',
    alignItems: 'center',
    gap: 'var(--solar-inset-xs)',
    paddingTop: 'var(--solar-inset-none)',
    paddingRight: 'var(--solar-inset-none)',
    paddingBottom: 'var(--solar-inset-none)',
    paddingLeft: 'var(--solar-inset-none)',
    '& .SolarStep-step': {
      color: 'var(--solar-color-text-primary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-body-sm)',
      lineHeight: 'var(--solar-type-line-height-body-sm)',
      letterSpacing: '-0.02em',
      textDecoration: 'none',
    },
    '& .SolarStep-label': {
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-label-sm)',
      lineHeight: 'var(--solar-type-line-height-label-sm)',
      letterSpacing: '-0.02em',
      textDecoration: 'none',
    },
    '& .SolarStep-line': {
      borderRadius: 'var(--solar-radius-control)',
      borderStyle: 'none',
      width: '100%',
      height: '3px',
    },
  },
  sizes: {},
  appearances: {
    'status=error, type=round': {
      '& .SolarStep-step': {
        color: 'var(--solar-color-text-feedback-danger)',
      },
    },
    'status=error, type=horizontal': {
      justifyContent: 'flex-start',
      alignItems: 'flex-start',
      gap: 'var(--solar-stack-xs)',
      width: '100%',
      '& .SolarStep-label': {
        color: 'var(--solar-color-text-feedback-danger)',
      },
      '& .SolarStep-line': {
        backgroundColor: 'var(--solar-color-surface-feedback-danger-strong)',
        borderColor: 'transparent',
        boxShadow: 'none',
      },
    },
    'status=complete, type=horizontal': {
      justifyContent: 'flex-start',
      alignItems: 'flex-start',
      gap: 'var(--solar-stack-xs)',
      width: '100%',
      '& .SolarStep-label': {
        color: 'var(--solar-color-text-primary)',
      },
      '& .SolarStep-line': {
        backgroundColor: 'var(--solar-color-surface-inverse)',
        borderColor: 'transparent',
        boxShadow: 'none',
      },
    },
    'status=active, type=round': {},
    'status=active, type=horizontal': {
      justifyContent: 'flex-start',
      alignItems: 'flex-start',
      gap: 'var(--solar-stack-xs)',
      width: '100%',
      '& .SolarStep-label': {
        color: 'var(--solar-color-text-primary)',
      },
      '& .SolarStep-line': {
        backgroundColor: 'var(--solar-color-surface-inverse)',
        borderColor: 'transparent',
        boxShadow: 'none',
      },
    },
    'status=upcoming, type=round': {},
    'status=upcoming, type=horizontal': {
      justifyContent: 'flex-start',
      alignItems: 'flex-start',
      gap: 'var(--solar-stack-xs)',
      width: '100%',
      '& .SolarStep-label': {
        color: 'var(--solar-color-text-tertiary)',
      },
      '& .SolarStep-line': {
        backgroundColor: 'var(--solar-color-surface-muted)',
        borderColor: 'transparent',
        boxShadow: 'none',
      },
    },
  },
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarStepComposition = {
  root: {
    base: {
      present: true,
    },
  },
  stepperIndicator: {
    base: {
      present: true,
      component: 'Stepper Indicator',
      'variant.status': 'completed',
    },
    appearance: {
      'status=complete, type=horizontal': {
        default: {
          present: false,
        },
      },
      'status=active, type=horizontal': {
        default: {
          present: false,
        },
      },
      'status=upcoming, type=horizontal': {
        default: {
          present: false,
        },
      },
      'status=error, type=horizontal': {
        default: {
          present: false,
        },
      },
      'status=active, type=round': {
        default: {
          'variant.status': 'active',
        },
      },
      'status=upcoming, type=round': {
        default: {
          'variant.status': 'upcoming',
        },
      },
      'status=error, type=round': {
        default: {
          'variant.status': 'error',
        },
      },
    },
  },
  step: {
    base: {
      present: true,
    },
    appearance: {
      'status=complete, type=horizontal': {
        default: {
          present: false,
        },
      },
      'status=active, type=horizontal': {
        default: {
          present: false,
        },
      },
      'status=upcoming, type=horizontal': {
        default: {
          present: false,
        },
      },
      'status=error, type=horizontal': {
        default: {
          present: false,
        },
      },
    },
  },
  label: {
    base: {
      present: false,
    },
    appearance: {
      'status=complete, type=horizontal': {
        default: {
          present: true,
        },
      },
      'status=active, type=horizontal': {
        default: {
          present: true,
        },
      },
      'status=upcoming, type=horizontal': {
        default: {
          present: true,
        },
      },
      'status=error, type=horizontal': {
        default: {
          present: true,
        },
      },
    },
  },
  line: {
    base: {
      present: false,
    },
    appearance: {
      'status=complete, type=horizontal': {
        default: {
          present: true,
        },
      },
      'status=active, type=horizontal': {
        default: {
          present: true,
        },
      },
      'status=upcoming, type=horizontal': {
        default: {
          present: true,
        },
      },
      'status=error, type=horizontal': {
        default: {
          present: true,
        },
      },
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarStepStyle(props: SolarStepProps = {}): Style {
  const p: Record<string, unknown> = { ...solarStepDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `status=${p.status}, type=${p.type}`;
  const s = solarStepStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  return merge(s.reset, s.root, s.appearances[key]);
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarStepParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarStepParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarStepCompose(
  props: SolarStepProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarStepDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `status=${p.status}, type=${p.type}`;
  const size = '';
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarStepComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}
