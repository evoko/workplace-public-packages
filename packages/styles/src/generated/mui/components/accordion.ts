// SOLAR Accordion recipe for MUI. Generated by @bwp-web/codegen from spec/components/accordion.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export interface SolarAccordionProps {
  disabled?: boolean;
  expanded?: boolean;
}

export const solarAccordionDefaults = {
  disabled: false,
  expanded: false,
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarAccordionStyles = {
  reset: {
    display: 'flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarAccordion-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarAccordion-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarAccordion-glyph': {
      display: 'block',
      overflow: 'visible',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
    padding: '0',
    margin: '0',
    font: 'inherit',
    color: 'inherit',
    textAlign: 'start',
    '& .SolarAccordion-title, & .SolarAccordion-description': {
      whiteSpace: 'normal',
      minWidth: '0',
    },
    '& .SolarAccordion-drawnIcon': {
      flexShrink: '0',
    },
    '&.SolarAccordion-expanded > .SolarAccordion-accordion .SolarAccordion-iconChevronDown':
      {
        transform: 'rotate(180deg)',
      },
  },
  root: {
    backgroundColor: 'transparent',
    borderColor: 'var(--solar-color-border-subtle)',
    boxShadow: 'none',
    borderRadius: 'var(--solar-radius-none)',
    borderTopWidth: 'var(--solar-border-default)',
    borderTopStyle: 'solid',
    borderRightStyle: 'none',
    borderBottomStyle: 'none',
    borderLeftStyle: 'none',
    flexDirection: 'row',
    justifyContent: 'flex-start',
    alignItems: 'center',
    gap: 'var(--solar-inset-xs)',
    paddingTop: 'var(--solar-inset-md)',
    paddingRight: 'var(--solar-inset-md)',
    paddingBottom: 'var(--solar-inset-md)',
    paddingLeft: 'var(--solar-inset-md)',
    width: '100%',
    '& .SolarAccordion-title': {
      color: 'var(--solar-color-text-primary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-body-md)',
      lineHeight: 'var(--solar-type-line-height-body-md)',
      letterSpacing: '-0.02286em',
      textDecoration: 'none',
      width: '100%',
    },
    '& .SolarAccordion-iconChevronDown': {
      width: 'var(--solar-icon-sm)',
      height: 'var(--solar-icon-sm)',
      color: 'var(--solar-color-icon-primary)',
    },
    '& .SolarAccordion-accordion': {
      width: '100%',
    },
    '& .SolarAccordion-content': {
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'column',
      justifyContent: 'flex-start',
      alignItems: 'flex-start',
      gap: 'var(--solar-stack-xs)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-md)',
      paddingBottom: 'var(--solar-inset-md)',
      paddingLeft: 'var(--solar-inset-md)',
      width: '100%',
    },
    '& .SolarAccordion-description': {
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-400)',
      fontSize: 'var(--solar-type-size-body-md)',
      lineHeight: 'var(--solar-type-line-height-body-md)',
      letterSpacing: '-0.02286em',
      textDecoration: 'none',
    },
  },
  sizes: {},
  appearances: {
    'expanded=false': {
      '&:hover, .SolarAccordion-expanded:hover > &': {
        backgroundColor: 'var(--solar-color-surface-hover)',
      },
      '&.Mui-focusVisible': {
        boxShadow: 'var(--solar-shadow-focus-default)',
        backgroundColor: 'transparent',
      },
      '&.SolarAccordion-disabled': {
        '& .SolarAccordion-title': {
          color: 'var(--solar-color-text-disabled)',
        },
      },
    },
    'expanded=true': {
      borderColor: 'transparent',
      borderTopStyle: 'none',
      flexDirection: 'column',
      justifyContent: 'flex-start',
      alignItems: 'flex-start',
      gap: 'var(--solar-stack-none)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
      '&:hover, .SolarAccordion-expanded:hover > &': {
        borderColor: 'transparent',
        '& .SolarAccordion-content': {
          backgroundColor: 'var(--solar-color-surface-hover)',
          borderColor: 'transparent',
          boxShadow: 'none',
        },
        '& .SolarAccordion-description': {
          color: 'var(--solar-color-text-secondary)',
        },
      },
      '&.Mui-focusVisible': {
        '& .SolarAccordion-content': {
          backgroundColor: 'transparent',
        },
      },
      '& .SolarAccordion-content': {
        backgroundColor: 'transparent',
        borderColor: 'transparent',
        boxShadow: 'none',
      },
      '&.SolarAccordion-disabled': {
        borderColor: 'transparent',
        '& .SolarAccordion-content': {
          backgroundColor: 'transparent',
          borderColor: 'transparent',
          boxShadow: 'none',
        },
        '& .SolarAccordion-description': {
          color: 'var(--solar-color-text-disabled)',
        },
      },
      '& .SolarAccordion-description': {
        color: 'var(--solar-color-text-secondary)',
      },
    },
  },
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarAccordionComposition = {
  root: {
    base: {
      present: true,
    },
  },
  title: {
    base: {
      present: true,
    },
    appearance: {
      'expanded=true': {
        default: {
          present: false,
        },
        hover: {
          present: false,
        },
        disabled: {
          present: false,
        },
      },
    },
  },
  iconChevronDown: {
    base: {
      present: true,
      component: 'Icon/ChevronDown',
      'variant.solid': 'false',
    },
    appearance: {
      'expanded=true': {
        default: {
          present: false,
        },
        hover: {
          present: false,
        },
        disabled: {
          present: false,
        },
      },
    },
  },
  accordion: {
    base: {
      present: false,
      component: 'Accordion',
    },
    appearance: {
      'expanded=true': {
        default: {
          present: true,
          'variant.state': 'default',
          'variant.expanded': 'false',
        },
        hover: {
          present: true,
          'variant.state': 'hover',
          'variant.expanded': 'false',
        },
        disabled: {
          present: true,
          'variant.state': 'disabled',
          'variant.expanded': 'false',
        },
      },
    },
  },
  content: {
    base: {
      present: false,
    },
    appearance: {
      'expanded=true': {
        default: {
          present: true,
        },
        hover: {
          present: true,
        },
        disabled: {
          present: true,
        },
      },
    },
  },
  description: {
    base: {
      present: false,
    },
    appearance: {
      'expanded=true': {
        default: {
          present: true,
        },
        hover: {
          present: true,
        },
        disabled: {
          present: true,
        },
      },
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarAccordionStyle(props: SolarAccordionProps = {}): Style {
  const p: Record<string, unknown> = { ...solarAccordionDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `expanded=${p.expanded}`;
  const s = solarAccordionStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  return merge(s.reset, s.root, s.appearances[key]);
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarAccordionParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarAccordionParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarAccordionCompose(
  props: SolarAccordionProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarAccordionDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `expanded=${p.expanded}`;
  const size = '';
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarAccordionComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}
