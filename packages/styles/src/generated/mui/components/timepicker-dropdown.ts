// SOLAR TimePicker Dropdown recipe for MUI. Generated by @bwp-web/codegen from spec/components/timepicker dropdown.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export type SolarTimePickerDropdownSize = 'sm' | 'md';

export interface SolarTimePickerDropdownProps {
  size?: SolarTimePickerDropdownSize;
}

export const solarTimePickerDropdownDefaults = {
  size: 'md',
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarTimePickerDropdownStyles = {
  reset: {
    display: 'inline-flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarTimePickerDropdown-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarTimePickerDropdown-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarTimePickerDropdown-glyph': {
      display: 'block',
      overflow: 'visible',
      flexShrink: '0',
    },
    '& .SolarTimePickerDropdown-drawnIcon': {
      flexShrink: '0',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
    flexDirection: 'column',
    maxHeight: '300px',
    '& .SolarTimePickerDropdown-content': {
      margin: '0',
      padding: '0',
      listStyle: 'none',
      outline: 'none',
      overflowY: 'auto',
      minHeight: '0',
    },
    '& .SolarTimePickerDropdown-content > *': {
      width: '100%',
    },
  },
  root: {
    backgroundColor: 'var(--solar-color-surface-overlay)',
    borderColor: 'var(--solar-color-border-medium)',
    boxShadow: 'var(--solar-shadow-control)',
    borderRadius: 'var(--solar-radius-container)',
    borderWidth: 'var(--solar-border-default)',
    borderStyle: 'solid',
    flexDirection: 'column',
    justifyContent: 'flex-start',
    alignItems: 'flex-start',
    gap: 'var(--solar-stack-none)',
    paddingTop: 'var(--solar-inset-none)',
    paddingRight: 'var(--solar-inset-none)',
    paddingBottom: 'var(--solar-inset-none)',
    paddingLeft: 'var(--solar-inset-none)',
    '& .SolarTimePickerDropdown-content': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'column',
      justifyContent: 'flex-start',
      alignItems: 'flex-start',
      gap: 'var(--solar-inset-none)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
      width: '100%',
    },
    '& .SolarTimePickerDropdown-dropdownItem': {
      width: '100%',
    },
    '& .SolarTimePickerDropdown-dropdownItem2': {
      width: '100%',
    },
    '& .SolarTimePickerDropdown-dropdownItem3': {
      width: '100%',
    },
    '& .SolarTimePickerDropdown-dropdownItem4': {
      width: '100%',
    },
    '& .SolarTimePickerDropdown-dropdownItem5': {
      width: '100%',
    },
    '& .SolarTimePickerDropdown-dropdownItem6': {
      width: '100%',
    },
  },
  sizes: {
    sm: {},
  },
  appearances: {},
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarTimePickerDropdownComposition = {
  root: {
    base: {
      present: true,
    },
  },
  content: {
    base: {
      present: true,
    },
  },
  dropdownItem: {
    base: {
      present: true,
      component: 'Dropdown Item',
      'variant.size': 'md',
      'variant.state': 'selected',
    },
    size: {
      sm: {
        'variant.size': 'sm',
      },
    },
  },
  dropdownItem2: {
    base: {
      present: true,
      component: 'Dropdown Item',
      'variant.size': 'md',
      'variant.state': 'default',
    },
    size: {
      sm: {
        'variant.size': 'sm',
      },
    },
  },
  dropdownItem3: {
    base: {
      present: true,
      component: 'Dropdown Item',
      'variant.size': 'md',
      'variant.state': 'default',
    },
    size: {
      sm: {
        'variant.size': 'sm',
      },
    },
  },
  dropdownItem4: {
    base: {
      present: true,
      component: 'Dropdown Item',
      'variant.size': 'md',
      'variant.state': 'default',
    },
    size: {
      sm: {
        'variant.size': 'sm',
      },
    },
  },
  dropdownItem5: {
    base: {
      present: true,
      component: 'Dropdown Item',
      'variant.size': 'md',
      'variant.state': 'default',
    },
    size: {
      sm: {
        'variant.size': 'sm',
      },
    },
  },
  dropdownItem6: {
    base: {
      present: true,
      component: 'Dropdown Item',
      'variant.size': 'md',
      'variant.state': 'default',
    },
    size: {
      sm: {
        'variant.size': 'sm',
      },
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarTimePickerDropdownStyle(
  props: SolarTimePickerDropdownProps = {},
): Style {
  const p: Record<string, unknown> = { ...solarTimePickerDropdownDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `default`;
  const s = solarTimePickerDropdownStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  const size = p.size as string;
  return merge(
    s.reset,
    s.root,
    s.sizes[size],
    s.appearances[key],
    s.combined[size]?.[key],
  );
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarTimePickerDropdownParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarTimePickerDropdownParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarTimePickerDropdownCompose(
  props: SolarTimePickerDropdownProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarTimePickerDropdownDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `default`;
  const size = p.size as string;
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarTimePickerDropdownComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}
