// SOLAR Stepper Indicator recipe for MUI. Generated by @bwp-web/codegen from spec/components/stepper indicator.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export type SolarStepperIndicatorStatus =
  'completed' | 'active' | 'upcoming' | 'error';

export interface SolarStepperIndicatorProps {
  status?: SolarStepperIndicatorStatus;
}

export const solarStepperIndicatorDefaults = {
  status: 'completed',
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarStepperIndicatorStyles = {
  reset: {
    display: 'flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarStepperIndicator-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarStepperIndicator-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarStepperIndicator-glyph': {
      display: 'block',
      overflow: 'visible',
      flexShrink: '0',
    },
    '& .SolarStepperIndicator-drawnIcon': {
      flexShrink: '0',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
    borderStyle: 'solid',
  },
  root: {
    backgroundColor: 'var(--solar-color-surface-inverse)',
    borderColor: 'transparent',
    boxShadow: 'none',
    borderRadius: 'var(--solar-radius-pill)',
    borderStyle: 'none',
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'center',
    gap: 'var(--solar-inset-none)',
    paddingTop: 'var(--solar-inset-none)',
    paddingRight: 'var(--solar-inset-none)',
    paddingBottom: 'var(--solar-inset-none)',
    paddingLeft: 'var(--solar-inset-none)',
    width: '24px',
    height: '24px',
    '& .SolarStepperIndicator--iconCheck': {
      width: 'var(--solar-icon-sm)',
      minWidth: 'var(--solar-icon-sm)',
      height: 'var(--solar-icon-sm)',
      minHeight: 'var(--solar-icon-sm)',
      color: 'var(--solar-color-icon-inverse)',
    },
    '& .SolarStepperIndicator--number': {
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-600)',
      fontSize: 'var(--solar-type-size-body-sm)',
      lineHeight: 'var(--solar-type-line-height-body-sm)',
      letterSpacing: '-0.02em',
      textDecoration: 'none',
    },
    '& .SolarStepperIndicator--icon': {
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-600)',
      fontSize: 'var(--solar-type-size-body-sm)',
      lineHeight: 'var(--solar-type-line-height-body-sm)',
      letterSpacing: '-0.02em',
      textDecoration: 'none',
    },
  },
  sizes: {},
  appearances: {
    'status=active': {
      '& .SolarStepperIndicator--number': {
        color: 'var(--solar-color-text-inverse)',
      },
    },
    'status=upcoming': {
      backgroundColor: 'var(--solar-color-surface-base)',
      borderColor: 'var(--solar-color-border-medium)',
      borderWidth: 'var(--solar-border-default)',
      borderStyle: 'solid',
      '& .SolarStepperIndicator--number': {
        color: 'var(--solar-color-text-secondary)',
      },
    },
    'status=error': {
      backgroundColor: 'var(--solar-color-surface-feedback-danger-strong)',
      borderColor: 'var(--solar-color-border-medium)',
      borderWidth: 'var(--solar-border-default)',
      borderStyle: 'solid',
      '& .SolarStepperIndicator--icon': {
        color: 'var(--solar-color-icon-inverse)',
      },
    },
  },
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarStepperIndicatorComposition = {
  root: {
    base: {
      present: true,
    },
  },
  iconCheck: {
    base: {
      present: true,
      component: 'Icon/Check',
      'variant.solid': 'false',
    },
    appearance: {
      'status=active': {
        default: {
          present: false,
        },
      },
      'status=upcoming': {
        default: {
          present: false,
        },
      },
      'status=error': {
        default: {
          present: false,
        },
      },
    },
  },
  number: {
    base: {
      present: false,
    },
    appearance: {
      'status=active': {
        default: {
          present: true,
        },
      },
      'status=upcoming': {
        default: {
          present: true,
        },
      },
    },
  },
  icon: {
    base: {
      present: false,
    },
    appearance: {
      'status=error': {
        default: {
          present: true,
        },
      },
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarStepperIndicatorStyle(
  props: SolarStepperIndicatorProps = {},
): Style {
  const p: Record<string, unknown> = { ...solarStepperIndicatorDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `status=${p.status}`;
  const s = solarStepperIndicatorStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  return merge(s.reset, s.root, s.appearances[key]);
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarStepperIndicatorParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarStepperIndicatorParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarStepperIndicatorCompose(
  props: SolarStepperIndicatorProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarStepperIndicatorDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `status=${p.status}`;
  const size = '';
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarStepperIndicatorComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}

/** Each layer's children, in Figma's order: the tree the shell draws (`drawChildren`). */
export const solarStepperIndicatorTree: Record<string, string[]> = {
  root: ['iconCheck', 'number', 'icon'],
};

/** Each slot's layer, to the slot it is: its class is `SolarStepperIndicator-<slot>`, public. */
export const solarStepperIndicatorSlots: Record<string, string> = {};
