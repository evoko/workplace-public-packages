// SOLAR File Card recipe for MUI. Generated by @bwp-web/codegen from spec/components/file card.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export type SolarFileCardType = 'file' | 'create';

export interface SolarFileCardProps {
  type?: SolarFileCardType;
}

export const solarFileCardDefaults = {
  type: 'file',
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarFileCardStyles = {
  reset: {
    display: 'flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarFileCard-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarFileCard--text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarFileCard-glyph': {
      display: 'block',
      overflow: 'visible',
      flexShrink: '0',
    },
    '& .SolarFileCard-drawnIcon': {
      flexShrink: '0',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
    '& .SolarFileCard-title, & .SolarFileCard-meta, & .SolarFileCard-label': {
      whiteSpace: 'normal',
      minWidth: '0',
    },
    '& .SolarFileCard-fileIcon': {
      flexShrink: '0',
    },
    '& .SolarFileCard-fileIcon > svg': {
      display: 'block',
      width: '100%',
      height: '100%',
    },
    '& .SolarFileCard-press': {
      position: 'static',
      display: 'inline',
      padding: '0',
      margin: '0',
      font: 'inherit',
      color: 'inherit',
      textAlign: 'start',
      textDecoration: 'none',
      verticalAlign: 'baseline',
    },
    '& .SolarFileCard-press::after': {
      content: '""',
      position: 'absolute',
      inset: '0',
      borderRadius: 'inherit',
    },
    '& .SolarFileCard-box :is(a, button, input, select, textarea, [tabindex]):not(.SolarFileCard-press)':
      {
        position: 'relative',
      },
    '& .SolarFileCard-name': {
      position: 'absolute',
      width: '1px',
      height: '1px',
      overflow: 'hidden',
      clipPath: 'inset(50%)',
      whiteSpace: 'nowrap',
    },
    '& .SolarFileCard--iconMore': {
      position: 'relative',
      flexShrink: '0',
      padding: '0',
      color: 'inherit',
    },
    '& .SolarFileCard--iconMore::after': {
      content: '""',
      position: 'absolute',
      top: '50%',
      left: '50%',
      width: 'max(100%, var(--solar-size-target-min))',
      height: 'max(100%, var(--solar-size-target-min))',
      transform: 'translate(-50%, -50%)',
    },
    '& .SolarFileCard--iconMore > svg': {
      display: 'block',
      width: '100%',
      height: '100%',
    },
    '& .SolarFileCard--thumbnail': {
      overflow: 'hidden',
    },
    '& .SolarFileCard--thumbnail > img': {
      width: '100%',
      height: '100%',
      objectFit: 'cover',
    },
  },
  root: {
    backgroundColor: 'var(--solar-color-surface-base)',
    borderColor: 'var(--solar-color-border-subtle)',
    boxShadow: 'var(--solar-shadow-raised)',
    borderRadius: 'var(--solar-radius-container)',
    borderWidth: 'var(--solar-border-default)',
    borderStyle: 'solid',
    flexDirection: 'column',
    justifyContent: 'flex-start',
    alignItems: 'flex-start',
    gap: 'var(--solar-stack-none)',
    paddingTop: 'var(--solar-inset-none)',
    paddingRight: 'var(--solar-inset-none)',
    paddingBottom: 'var(--solar-inset-none)',
    paddingLeft: 'var(--solar-inset-none)',
    width: '100%',
    height: '222px',
    '& .SolarFileCard--thumbnail': {
      backgroundColor: 'var(--solar-color-surface-background)',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderTopLeftRadius: 'var(--solar-radius-container)',
      borderTopRightRadius: 'var(--solar-radius-container)',
      borderBottomRightRadius: 'var(--solar-radius-none)',
      borderBottomLeftRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'row',
      justifyContent: 'center',
      alignItems: 'center',
      gap: 'var(--solar-inset-none)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
      width: '100%',
      height: '100%',
    },
    '& .SolarFileCard-fileIcon': {
      width: 'var(--solar-icon-lg)',
      height: 'var(--solar-icon-lg)',
      color: 'var(--solar-color-icon-tertiary)',
    },
    '& .SolarFileCard--footer': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'row',
      justifyContent: 'flex-start',
      alignItems: 'center',
      gap: 'var(--solar-inset-xs)',
      paddingTop: 'var(--solar-inset-md)',
      paddingRight: 'var(--solar-inset-md)',
      paddingBottom: 'var(--solar-inset-md)',
      paddingLeft: 'var(--solar-inset-md)',
      width: '100%',
    },
    '& .SolarFileCard--text': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'column',
      justifyContent: 'flex-start',
      alignItems: 'flex-start',
      gap: 'var(--solar-stack-xs)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
      width: '100%',
    },
    '& .SolarFileCard-title': {
      color: 'var(--solar-color-text-primary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-body-md)',
      lineHeight: 'var(--solar-type-line-height-body-md)',
      letterSpacing: '-0.02286em',
      textDecoration: 'none',
      width: '100%',
    },
    '& .SolarFileCard-meta': {
      color: 'var(--solar-color-text-secondary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-400)',
      fontSize: 'var(--solar-type-size-body-sm)',
      lineHeight: 'var(--solar-type-line-height-body-sm)',
      letterSpacing: '-0.02em',
      textDecoration: 'none',
      width: '100%',
    },
    '& .SolarFileCard--iconMore': {
      width: 'var(--solar-icon-md)',
      height: 'var(--solar-icon-md)',
      color: 'var(--solar-color-icon-primary)',
    },
    '& .SolarFileCard--frame1': {
      borderRadius: 'var(--solar-radius-pill)',
      borderStyle: 'none',
      flexDirection: 'row',
      justifyContent: 'flex-start',
      alignItems: 'center',
      gap: 'var(--solar-inset-xs)',
      paddingTop: 'var(--solar-inset-md)',
      paddingRight: 'var(--solar-inset-md)',
      paddingBottom: 'var(--solar-inset-md)',
      paddingLeft: 'var(--solar-inset-md)',
    },
    '& .SolarFileCard-label': {
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-body-md)',
      lineHeight: 'var(--solar-type-line-height-body-md)',
      letterSpacing: '-0.02286em',
      textDecoration: 'none',
    },
    '& .SolarFileCard--iconPlus': {
      width: 'var(--solar-icon-lg)',
      height: 'var(--solar-icon-lg)',
    },
  },
  sizes: {},
  appearances: {
    'type=file': {
      '&:has(.SolarFileCard-press.Mui-focusVisible)': {
        boxShadow: 'var(--solar-shadow-focus-default)',
      },
    },
    'type=create': {
      justifyContent: 'center',
      alignItems: 'center',
      gap: 'var(--solar-stack-sm)',
      '&:has(.SolarFileCard-press.Mui-focusVisible)': {
        boxShadow: 'var(--solar-shadow-focus-default)',
      },
      '& .SolarFileCard--frame1': {
        backgroundColor: 'var(--solar-color-surface-background)',
        borderColor: 'transparent',
        boxShadow: 'none',
      },
      '& .SolarFileCard-label': {
        color: 'var(--solar-color-text-primary)',
      },
      '& .SolarFileCard--iconPlus': {
        color: 'var(--solar-color-icon-tertiary)',
      },
    },
  },
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarFileCardComposition = {
  root: {
    base: {
      present: true,
    },
  },
  thumbnail: {
    base: {
      present: true,
    },
    appearance: {
      'type=create': {
        default: {
          present: false,
        },
      },
    },
  },
  fileIcon: {
    base: {
      present: true,
      component: 'Icon/Image',
      'variant.solid': 'false',
    },
    appearance: {
      'type=create': {
        default: {
          present: false,
        },
      },
    },
  },
  footer: {
    base: {
      present: true,
    },
    appearance: {
      'type=create': {
        default: {
          present: false,
        },
      },
    },
  },
  text: {
    base: {
      present: true,
    },
    appearance: {
      'type=create': {
        default: {
          present: false,
        },
      },
    },
  },
  title: {
    base: {
      present: true,
    },
    appearance: {
      'type=create': {
        default: {
          present: false,
        },
      },
    },
  },
  meta: {
    base: {
      present: true,
    },
    appearance: {
      'type=create': {
        default: {
          present: false,
        },
      },
    },
  },
  iconMore: {
    base: {
      present: true,
      component: 'Icon/More',
      'variant.solid': 'false',
    },
    appearance: {
      'type=create': {
        default: {
          present: false,
        },
      },
    },
  },
  frame1: {
    base: {
      present: false,
    },
    appearance: {
      'type=create': {
        default: {
          present: true,
        },
      },
    },
  },
  label: {
    base: {
      present: false,
    },
    appearance: {
      'type=create': {
        default: {
          present: true,
        },
      },
    },
  },
  iconPlus: {
    base: {
      present: false,
      component: 'Icon/Plus',
    },
    appearance: {
      'type=create': {
        default: {
          present: true,
          'variant.solid': 'false',
        },
      },
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarFileCardStyle(props: SolarFileCardProps = {}): Style {
  const p: Record<string, unknown> = { ...solarFileCardDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `type=${p.type}`;
  const s = solarFileCardStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  return merge(s.reset, s.root, s.appearances[key]);
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarFileCardParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarFileCardParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarFileCardCompose(
  props: SolarFileCardProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarFileCardDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `type=${p.type}`;
  const size = '';
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarFileCardComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}

/** Each layer's children, in Figma's order: the tree the shell draws (`drawChildren`). */
export const solarFileCardTree: Record<string, string[]> = {
  root: ['thumbnail', 'footer', 'frame1', 'label'],
  thumbnail: ['fileIcon'],
  footer: ['text', 'iconMore'],
  text: ['title', 'meta'],
  frame1: ['iconPlus'],
};

/** Each slot's layer, to the slot it is: its class is `SolarFileCard-<slot>`, public. */
export const solarFileCardSlots: Record<string, string> = {
  fileIcon: 'fileIcon',
  title: 'title',
  meta: 'meta',
  label: 'label',
};
