// SOLAR Split Dialog recipe for MUI. Generated by @bwp-web/codegen from spec/components/split dialog.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export type SolarSplitDialogCta = 'regular' | 'full-width';

export interface SolarSplitDialogProps {
  cta?: SolarSplitDialogCta;
}

export const solarSplitDialogDefaults = {
  cta: 'full-width',
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarSplitDialogStyles = {
  reset: {
    display: 'flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarSplitDialog-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarSplitDialog--text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarSplitDialog-glyph': {
      display: 'block',
      overflow: 'visible',
      flexShrink: '0',
    },
    '& .SolarSplitDialog-drawnIcon': {
      flexShrink: '0',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
    backgroundImage: 'none',
    maxWidth: 'calc(100% - 2 * var(--solar-inset-xl))',
    '& .SolarSplitDialog-title': {
      whiteSpace: 'normal',
      minWidth: '0',
    },
    '& .SolarSplitDialog-icon > svg': {
      display: 'block',
      width: '100%',
      height: '100%',
    },
    '& .SolarSplitDialog--leading, & .SolarSplitDialog--close': {
      flexShrink: '0',
    },
  },
  root: {
    backgroundColor: 'var(--solar-color-surface-dialog)',
    borderColor: 'transparent',
    boxShadow: 'var(--solar-shadow-dialog)',
    borderRadius: 'var(--solar-radius-dialog)',
    borderStyle: 'none',
    flexDirection: 'column',
    justifyContent: 'flex-start',
    alignItems: 'flex-start',
    gap: 'var(--solar-stack-none)',
    paddingTop: 'var(--solar-inset-none)',
    paddingRight: 'var(--solar-inset-none)',
    paddingBottom: 'var(--solar-inset-none)',
    paddingLeft: 'var(--solar-inset-none)',
    width: '640px',
    '& .SolarSplitDialog--header': {
      backgroundColor: 'transparent',
      borderColor: 'var(--solar-color-border-subtle)',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderTopStyle: 'none',
      borderRightStyle: 'none',
      borderBottomWidth: 'var(--solar-border-default)',
      borderBottomStyle: 'solid',
      borderLeftStyle: 'none',
      flexDirection: 'column',
      justifyContent: 'flex-start',
      alignItems: 'flex-start',
      gap: 'var(--solar-inset-xs)',
      paddingTop: 'var(--solar-inset-xs)',
      paddingRight: 'var(--solar-inset-xs)',
      paddingBottom: 'var(--solar-inset-xs)',
      paddingLeft: 'var(--solar-inset-xs)',
      width: '100%',
      minWidth: 0,
    },
    '& .SolarSplitDialog--text': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'row',
      justifyContent: 'space-between',
      alignItems: 'center',
      gap: 'var(--solar-inset-md)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
      width: '100%',
      minWidth: 0,
    },
    '& .SolarSplitDialog--leading': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'row',
      justifyContent: 'center',
      alignItems: 'center',
      gap: 'var(--solar-stack-xs)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
      width: '36px',
      minWidth: '36px',
      height: '36px',
      minHeight: '36px',
    },
    '& .SolarSplitDialog-icon': {
      width: 'var(--solar-icon-xs)',
      minWidth: 'var(--solar-icon-xs)',
      height: 'var(--solar-icon-xs)',
      minHeight: 'var(--solar-icon-xs)',
      color: 'var(--solar-color-icon-primary)',
    },
    '& .SolarSplitDialog-title': {
      color: 'var(--solar-color-text-primary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-title-sm)',
      lineHeight: 'var(--solar-type-line-height-title-sm)',
      letterSpacing: '-0.03em',
      textDecoration: 'none',
      width: '100%',
      minWidth: 0,
    },
    '& .SolarSplitDialog--body': {
      backgroundColor: 'var(--solar-color-surface-base)',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'row',
      justifyContent: 'flex-start',
      alignItems: 'flex-start',
      gap: 'var(--solar-inset-none)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
      width: '100%',
      minWidth: 0,
      height: '100%',
      minHeight: 0,
    },
    '& .SolarSplitDialog-left': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'column',
      justifyContent: 'flex-start',
      alignItems: 'flex-start',
      gap: 'var(--solar-stack-md)',
      paddingTop: 'var(--solar-inset-lg)',
      paddingRight: 'var(--solar-inset-lg)',
      paddingBottom: 'var(--solar-inset-lg)',
      paddingLeft: 'var(--solar-inset-lg)',
      width: '100%',
      minWidth: 0,
      height: '100%',
      minHeight: 0,
    },
    '& .SolarSplitDialog--container': {
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'column',
      justifyContent: 'flex-start',
      alignItems: 'flex-start',
      gap: 'var(--solar-stack-none)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
      width: '100%',
      minWidth: 0,
      height: '100%',
      minHeight: 0,
    },
    '& .SolarSplitDialog-right': {
      backgroundColor: 'transparent',
      borderColor: 'var(--solar-color-border-subtle)',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderTopStyle: 'none',
      borderRightStyle: 'none',
      borderBottomStyle: 'none',
      borderLeftWidth: 'var(--solar-border-default)',
      borderLeftStyle: 'solid',
      flexDirection: 'column',
      justifyContent: 'flex-start',
      alignItems: 'flex-start',
      gap: 'var(--solar-stack-md)',
      paddingTop: 'var(--solar-inset-lg)',
      paddingRight: 'var(--solar-inset-lg)',
      paddingBottom: 'var(--solar-inset-lg)',
      paddingLeft: 'var(--solar-inset-lg)',
      width: '100%',
      minWidth: 0,
      height: '100%',
      minHeight: 0,
    },
    '& .SolarSplitDialog-actions': {
      width: '100%',
      minWidth: 0,
    },
    '& .SolarSplitDialog-leftRegular': {
      borderRadius: 'var(--solar-radius-none)',
      borderTopStyle: 'none',
      borderRightStyle: 'none',
      borderBottomStyle: 'none',
      borderLeftWidth: 'var(--solar-border-default)',
      borderLeftStyle: 'solid',
      flexDirection: 'column',
      justifyContent: 'flex-start',
      alignItems: 'flex-start',
      gap: 'var(--solar-stack-md)',
      paddingTop: 'var(--solar-inset-lg)',
      paddingRight: 'var(--solar-inset-lg)',
      paddingBottom: 'var(--solar-inset-lg)',
      paddingLeft: 'var(--solar-inset-lg)',
      width: '100%',
      minWidth: 0,
      height: '100%',
      minHeight: 0,
    },
    '& .SolarSplitDialog-actionsRegular': {
      width: '100%',
      minWidth: 0,
    },
  },
  sizes: {},
  appearances: {
    'cta=regular': {
      '& .SolarSplitDialog--container': {
        backgroundColor: 'transparent',
        borderColor: 'transparent',
        boxShadow: 'none',
      },
      '& .SolarSplitDialog-leftRegular': {
        backgroundColor: 'transparent',
        borderColor: 'var(--solar-color-border-subtle)',
        boxShadow: 'none',
      },
    },
  },
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarSplitDialogComposition = {
  root: {
    base: {
      present: true,
    },
  },
  header: {
    base: {
      present: true,
    },
  },
  text: {
    base: {
      present: true,
    },
  },
  leading: {
    base: {
      present: true,
    },
  },
  icon: {
    base: {
      present: true,
      component: 'Icon/Empty',
      'variant.solid': 'false',
    },
  },
  title: {
    base: {
      present: true,
    },
  },
  close: {
    base: {
      present: true,
      component: 'Icon Button',
      'variant.size': 'md',
      'variant.shape': 'round',
      'variant.prio': 'tertiary',
      'variant.state': 'default',
    },
  },
  body: {
    base: {
      present: true,
    },
  },
  left: {
    base: {
      present: true,
    },
    appearance: {
      'cta=regular': {
        default: {
          present: false,
        },
      },
    },
  },
  container: {
    base: {
      present: false,
    },
    appearance: {
      'cta=regular': {
        default: {
          present: true,
        },
      },
    },
  },
  right: {
    base: {
      present: true,
    },
  },
  actions: {
    base: {
      present: true,
      component: 'Button Group',
      'variant.orientation': 'horizontal',
      'variant.type': 'full-width',
    },
    appearance: {
      'cta=regular': {
        default: {
          present: false,
        },
      },
    },
  },
  leftRegular: {
    base: {
      present: false,
    },
    appearance: {
      'cta=regular': {
        default: {
          present: true,
        },
      },
    },
  },
  actionsRegular: {
    base: {
      present: false,
      component: 'Button Group',
    },
    appearance: {
      'cta=regular': {
        default: {
          present: true,
          'variant.orientation': 'horizontal',
          'variant.type': 'regular',
        },
      },
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarSplitDialogStyle(
  props: SolarSplitDialogProps = {},
): Style {
  const p: Record<string, unknown> = { ...solarSplitDialogDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `cta=${p.cta}`;
  const s = solarSplitDialogStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  return merge(s.reset, s.root, s.appearances[key]);
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarSplitDialogParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarSplitDialogParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarSplitDialogCompose(
  props: SolarSplitDialogProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarSplitDialogDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `cta=${p.cta}`;
  const size = '';
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarSplitDialogComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}

/** Each layer's children, in Figma's order: the tree the shell draws (`drawChildren`). */
export const solarSplitDialogTree: Record<string, string[]> = {
  root: ['header', 'body', 'actions'],
  header: ['text'],
  text: ['leading', 'title', 'close'],
  leading: ['icon'],
  body: ['left', 'container', 'right'],
  container: ['leftRegular', 'actionsRegular'],
};

/** Each slot's layer, to the slot it is: its class is `SolarSplitDialog-<slot>`, public. */
export const solarSplitDialogSlots: Record<string, string> = {
  left: 'left',
  right: 'right',
  icon: 'icon',
  title: 'title',
  leftRegular: 'leftRegular',
  actions: 'actions',
  actionsRegular: 'actionsRegular',
};
