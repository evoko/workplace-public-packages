// SOLAR BackButton recipe for MUI. Generated by @bwp-web/codegen from spec/components/backbutton.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export type SolarBackButtonSize = 'sm' | 'md';

export interface SolarBackButtonProps {
  size?: SolarBackButtonSize;
  disabled?: boolean;
  loading?: boolean;
}

export const solarBackButtonDefaults = {
  size: 'md',
  disabled: false,
  loading: false,
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarBackButtonStyles = {
  reset: {
    minWidth: 'auto',
    textTransform: 'none',
    '& .MuiButton-startIcon': {
      margin: '0',
      flexShrink: '0',
    },
    '& .MuiButton-startIcon > svg': {
      width: '100%',
      height: '100%',
    },
    '&': {
      position: 'relative',
    },
    '&::after': {
      content: '""',
      position: 'absolute',
      top: '50%',
      left: '50%',
      width: 'max(100%, 44px)',
      height: 'max(100%, 44px)',
      transform: 'translate(-50%, -50%)',
    },
  },
  root: {
    backgroundColor: 'var(--solar-color-action-tertiary-bg-default)',
    borderColor: 'var(--solar-color-border-medium)',
    boxShadow: 'var(--solar-shadow-control)',
    borderRadius: 'var(--solar-radius-control)',
    borderWidth: 'var(--solar-border-default)',
    borderStyle: 'solid',
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'center',
    gap: 'var(--solar-stack-sm)',
    paddingTop: 'var(--solar-inset-none)',
    paddingRight: 'var(--solar-inset-sm)',
    paddingBottom: 'var(--solar-inset-none)',
    paddingLeft: 'var(--solar-inset-sm)',
    height: '40px',
    '& .MuiButton-startIcon': {
      width: 'var(--solar-icon-md)',
      height: 'var(--solar-icon-md)',
      color: 'var(--solar-color-action-tertiary-icon-default)',
    },
    color: 'var(--solar-color-action-tertiary-text-default)',
    fontFamily: 'var(--solar-type-font-family-inter)',
    fontWeight: 'var(--solar-type-font-weight-500)',
    fontSize: 'var(--solar-type-size-label-md)',
    lineHeight: 'var(--solar-type-line-height-label-md)',
    letterSpacing: '-0.02em',
    textDecoration: 'none',
  },
  sizes: {
    sm: {
      gap: 'var(--solar-stack-xs)',
      paddingRight: 'var(--solar-inset-xs)',
      paddingLeft: 'var(--solar-inset-xs)',
      height: '32px',
      '& .MuiButton-startIcon': {
        width: 'var(--solar-icon-sm)',
        height: 'var(--solar-icon-sm)',
      },
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-label-sm)',
      lineHeight: 'var(--solar-type-line-height-label-sm)',
      letterSpacing: '-0.02em',
      textDecoration: 'none',
    },
  },
  appearances: {
    default: {
      '&:hover': {
        backgroundColor: 'var(--solar-color-action-tertiary-bg-hover)',
        '& .MuiButton-startIcon': {
          color: 'var(--solar-color-action-tertiary-icon-hover)',
        },
        color: 'var(--solar-color-action-tertiary-text-hover)',
      },
      '&:active': {
        backgroundColor: 'var(--solar-color-action-tertiary-bg-active)',
        '& .MuiButton-startIcon': {
          color: 'var(--solar-color-action-tertiary-icon-active)',
        },
        color: 'var(--solar-color-action-tertiary-text-active)',
      },
      '&.Mui-focusVisible': {
        boxShadow: 'var(--solar-shadow-focus-default)',
      },
      '&.Mui-disabled:not(.MuiButton-loading)': {
        backgroundColor: 'var(--solar-color-action-tertiary-bg-disabled)',
        '& .MuiButton-startIcon': {
          color: 'var(--solar-color-action-tertiary-icon-disabled)',
        },
        color: 'var(--solar-color-action-tertiary-text-disabled)',
      },
      '&.MuiButton-loading': {},
    },
  },
  combined: {
    sm: {
      default: {
        '&.Mui-focusVisible': {
          backgroundColor: 'var(--solar-color-action-tertiary-bg-default)',
          '& .MuiButton-startIcon': {
            color: 'var(--solar-color-action-tertiary-icon-default)',
          },
          color: 'var(--solar-color-action-tertiary-text-default)',
        },
      },
    },
    md: {
      default: {
        '&.Mui-focusVisible': {
          backgroundColor: 'var(--solar-color-action-tertiary-bg-default)',
          '& .MuiButton-startIcon': {
            color: 'var(--solar-color-action-tertiary-icon-default)',
          },
          color: 'var(--solar-color-action-tertiary-text-default)',
        },
      },
    },
  },
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarBackButtonComposition = {
  root: {
    base: {
      present: true,
    },
  },
  iconArrowLeft: {
    base: {
      present: true,
      component: 'Icon/ArrowLeft',
      'variant.solid': 'false',
    },
    appearance: {
      default: {
        loading: {
          present: false,
        },
      },
    },
  },
  label: {
    base: {
      present: true,
    },
    appearance: {
      default: {
        loading: {
          present: false,
        },
      },
    },
  },
  spinner: {
    base: {
      present: false,
      component: 'Spinner',
      'variant.size': 'md',
    },
    appearance: {
      default: {
        loading: {
          present: true,
          'variant.style': 'default',
        },
      },
    },
    size: {
      sm: {
        'variant.size': 'sm',
      },
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarBackButtonStyle(props: SolarBackButtonProps = {}): Style {
  const p: Record<string, unknown> = { ...solarBackButtonDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `default`;
  const s = solarBackButtonStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  const size = p.size as string;
  return merge(
    s.reset,
    s.root,
    s.sizes[size],
    s.appearances[key],
    s.combined[size]?.[key],
  );
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarBackButtonParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarBackButtonParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarBackButtonCompose(
  props: SolarBackButtonProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarBackButtonDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `default`;
  const size = p.size as string;
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarBackButtonComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}
