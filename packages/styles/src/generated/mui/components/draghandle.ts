// SOLAR DragHandle recipe for MUI. Generated by @bwp-web/codegen from spec/components/draghandle.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export type SolarDragHandleSize = 'sm' | 'md';

export interface SolarDragHandleProps {
  size?: SolarDragHandleSize;
  disabled?: boolean;
}

export const solarDragHandleDefaults = {
  size: 'sm',
  disabled: false,
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarDragHandleStyles = {
  reset: {
    display: 'inline-flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarDragHandle-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarDragHandle-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarDragHandle-glyph': {
      display: 'block',
      overflow: 'visible',
      flexShrink: '0',
    },
    '& .SolarDragHandle-drawnIcon': {
      flexShrink: '0',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
    cursor: 'grab',
    '&:active': {
      cursor: 'grabbing',
    },
    '&.SolarDragHandle-disabled': {
      cursor: 'default',
    },
    '&:focus-visible': {
      outline: 'none',
    },
    '&': {
      position: 'relative',
    },
    '&::after': {
      content: '""',
      position: 'absolute',
      top: '50%',
      left: '50%',
      width: 'max(100%, var(--solar-size-target-min))',
      height: 'max(100%, var(--solar-size-target-min))',
      transform: 'translate(-50%, -50%)',
    },
  },
  root: {
    backgroundColor: 'transparent',
    borderColor: 'transparent',
    boxShadow: 'none',
    borderRadius: 'var(--solar-radius-control)',
    borderStyle: 'none',
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'center',
    gap: 'var(--solar-stack-2xs)',
    paddingTop: 'var(--solar-inset-xs)',
    paddingRight: 'var(--solar-inset-xs)',
    paddingBottom: 'var(--solar-inset-xs)',
    paddingLeft: 'var(--solar-inset-xs)',
    '& .SolarDragHandle--col1': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'column',
      justifyContent: 'flex-start',
      alignItems: 'flex-start',
      gap: 'var(--solar-stack-2xs)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
    },
    '& .SolarDragHandle--col1Dot': {
      backgroundColor: 'var(--solar-color-icon-secondary)',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-pill)',
      borderStyle: 'none',
      width: '3px',
      minWidth: '3px',
      height: '3px',
      minHeight: '3px',
    },
    '& .SolarDragHandle--col1Dot2': {
      backgroundColor: 'var(--solar-color-icon-secondary)',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-pill)',
      borderStyle: 'none',
      width: '3px',
      minWidth: '3px',
      height: '3px',
      minHeight: '3px',
    },
    '& .SolarDragHandle--col1Dot3': {
      backgroundColor: 'var(--solar-color-icon-secondary)',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-pill)',
      borderStyle: 'none',
      width: '3px',
      minWidth: '3px',
      height: '3px',
      minHeight: '3px',
    },
    '& .SolarDragHandle--col2': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'column',
      justifyContent: 'flex-start',
      alignItems: 'flex-start',
      gap: 'var(--solar-stack-2xs)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
    },
    '& .SolarDragHandle--col2Dot': {
      backgroundColor: 'var(--solar-color-icon-secondary)',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-pill)',
      borderStyle: 'none',
      width: '3px',
      minWidth: '3px',
      height: '3px',
      minHeight: '3px',
    },
    '& .SolarDragHandle--col2Dot2': {
      backgroundColor: 'var(--solar-color-icon-secondary)',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-pill)',
      borderStyle: 'none',
      width: '3px',
      minWidth: '3px',
      height: '3px',
      minHeight: '3px',
    },
    '& .SolarDragHandle--col2Dot3': {
      backgroundColor: 'var(--solar-color-icon-secondary)',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-pill)',
      borderStyle: 'none',
      width: '3px',
      minWidth: '3px',
      height: '3px',
      minHeight: '3px',
    },
  },
  sizes: {
    md: {
      '& .SolarDragHandle--col1Dot': {
        width: '4px',
        minWidth: '4px',
        height: '4px',
        minHeight: '4px',
      },
      '& .SolarDragHandle--col1Dot2': {
        width: '4px',
        minWidth: '4px',
        height: '4px',
        minHeight: '4px',
      },
      '& .SolarDragHandle--col1Dot3': {
        width: '4px',
        minWidth: '4px',
        height: '4px',
        minHeight: '4px',
      },
      '& .SolarDragHandle--col2Dot': {
        width: '4px',
        minWidth: '4px',
        height: '4px',
        minHeight: '4px',
      },
      '& .SolarDragHandle--col2Dot2': {
        width: '4px',
        minWidth: '4px',
        height: '4px',
        minHeight: '4px',
      },
      '& .SolarDragHandle--col2Dot3': {
        width: '4px',
        minWidth: '4px',
        height: '4px',
        minHeight: '4px',
      },
    },
  },
  appearances: {
    default: {
      '&:hover': {
        '& .SolarDragHandle--col1Dot': {
          backgroundColor: 'var(--solar-color-icon-primary)',
        },
        '& .SolarDragHandle--col1Dot2': {
          backgroundColor: 'var(--solar-color-icon-primary)',
        },
        '& .SolarDragHandle--col1Dot3': {
          backgroundColor: 'var(--solar-color-icon-primary)',
        },
        '& .SolarDragHandle--col2Dot': {
          backgroundColor: 'var(--solar-color-icon-primary)',
        },
        '& .SolarDragHandle--col2Dot2': {
          backgroundColor: 'var(--solar-color-icon-primary)',
        },
        '& .SolarDragHandle--col2Dot3': {
          backgroundColor: 'var(--solar-color-icon-primary)',
        },
      },
      '&:active, &[aria-pressed="true"]': {
        '& .SolarDragHandle--col1Dot': {
          backgroundColor: 'var(--solar-color-icon-primary)',
        },
        '& .SolarDragHandle--col1Dot2': {
          backgroundColor: 'var(--solar-color-icon-primary)',
        },
        '& .SolarDragHandle--col1Dot3': {
          backgroundColor: 'var(--solar-color-icon-primary)',
        },
        '& .SolarDragHandle--col2Dot': {
          backgroundColor: 'var(--solar-color-icon-primary)',
        },
        '& .SolarDragHandle--col2Dot2': {
          backgroundColor: 'var(--solar-color-icon-primary)',
        },
        '& .SolarDragHandle--col2Dot3': {
          backgroundColor: 'var(--solar-color-icon-primary)',
        },
      },
      '&:focus-visible': {
        boxShadow: 'var(--solar-shadow-focus-default)',
      },
      '&.SolarDragHandle-disabled': {
        '& .SolarDragHandle--col1Dot': {
          backgroundColor: 'var(--solar-color-icon-disabled)',
        },
        '& .SolarDragHandle--col1Dot2': {
          backgroundColor: 'var(--solar-color-icon-disabled)',
        },
        '& .SolarDragHandle--col1Dot3': {
          backgroundColor: 'var(--solar-color-icon-disabled)',
        },
        '& .SolarDragHandle--col2Dot': {
          backgroundColor: 'var(--solar-color-icon-disabled)',
        },
        '& .SolarDragHandle--col2Dot2': {
          backgroundColor: 'var(--solar-color-icon-disabled)',
        },
        '& .SolarDragHandle--col2Dot3': {
          backgroundColor: 'var(--solar-color-icon-disabled)',
        },
      },
    },
  },
  combined: {
    sm: {
      default: {
        '&:focus-visible': {
          '& .SolarDragHandle--col1Dot': {
            backgroundColor: 'var(--solar-color-icon-secondary)',
          },
          '& .SolarDragHandle--col1Dot2': {
            backgroundColor: 'var(--solar-color-icon-secondary)',
          },
          '& .SolarDragHandle--col1Dot3': {
            backgroundColor: 'var(--solar-color-icon-secondary)',
          },
          '& .SolarDragHandle--col2Dot': {
            backgroundColor: 'var(--solar-color-icon-secondary)',
          },
          '& .SolarDragHandle--col2Dot2': {
            backgroundColor: 'var(--solar-color-icon-secondary)',
          },
          '& .SolarDragHandle--col2Dot3': {
            backgroundColor: 'var(--solar-color-icon-secondary)',
          },
        },
      },
    },
    md: {
      default: {
        '&:focus-visible': {
          '& .SolarDragHandle--col1Dot': {
            backgroundColor: 'var(--solar-color-icon-secondary)',
          },
          '& .SolarDragHandle--col1Dot2': {
            backgroundColor: 'var(--solar-color-icon-secondary)',
          },
          '& .SolarDragHandle--col1Dot3': {
            backgroundColor: 'var(--solar-color-icon-secondary)',
          },
          '& .SolarDragHandle--col2Dot': {
            backgroundColor: 'var(--solar-color-icon-secondary)',
          },
          '& .SolarDragHandle--col2Dot2': {
            backgroundColor: 'var(--solar-color-icon-secondary)',
          },
          '& .SolarDragHandle--col2Dot3': {
            backgroundColor: 'var(--solar-color-icon-secondary)',
          },
        },
      },
    },
  },
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarDragHandleComposition = {
  root: {
    base: {
      present: true,
    },
  },
  col1: {
    base: {
      present: true,
    },
  },
  col1Dot: {
    base: {
      present: true,
    },
  },
  col1Dot2: {
    base: {
      present: true,
    },
  },
  col1Dot3: {
    base: {
      present: true,
    },
  },
  col2: {
    base: {
      present: true,
    },
  },
  col2Dot: {
    base: {
      present: true,
    },
  },
  col2Dot2: {
    base: {
      present: true,
    },
  },
  col2Dot3: {
    base: {
      present: true,
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarDragHandleStyle(props: SolarDragHandleProps = {}): Style {
  const p: Record<string, unknown> = { ...solarDragHandleDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `default`;
  const s = solarDragHandleStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  const size = p.size as string;
  return merge(
    s.reset,
    s.root,
    s.sizes[size],
    s.appearances[key],
    s.combined[size]?.[key],
  );
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarDragHandleParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarDragHandleParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarDragHandleCompose(
  props: SolarDragHandleProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarDragHandleDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `default`;
  const size = p.size as string;
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarDragHandleComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}

/** Each layer's children, in Figma's order: the tree the shell draws (`drawChildren`). */
export const solarDragHandleTree: Record<string, string[]> = {
  root: ['col1', 'col2'],
  col1: ['col1Dot', 'col1Dot2', 'col1Dot3'],
  col2: ['col2Dot', 'col2Dot2', 'col2Dot3'],
};

/** Each slot's layer, to the slot it is: its class is `SolarDragHandle-<slot>`, public. */
export const solarDragHandleSlots: Record<string, string> = {};
