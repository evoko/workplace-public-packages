// SOLAR Option Card recipe for MUI. Generated by @bwp-web/codegen from spec/components/option card.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export interface SolarOptionCardProps {
  selected?: boolean;
}

export const solarOptionCardDefaults = {
  selected: false,
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarOptionCardStyles = {
  reset: {
    display: 'flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarOptionCard-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarOptionCard-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarOptionCard-glyph': {
      display: 'block',
      overflow: 'visible',
      flexShrink: '0',
    },
    '& .SolarOptionCard-drawnIcon': {
      flexShrink: '0',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
    '& .SolarOptionCard-label': {
      textAlign: 'center',
    },
    '& .SolarOptionCard--iconPlus': {
      flexShrink: '0',
    },
    '& .SolarOptionCard--iconPlus > svg': {
      display: 'block',
      width: '100%',
      height: '100%',
    },
    '& .SolarOptionCard-press': {
      position: 'static',
      display: 'inline',
      padding: '0',
      margin: '0',
      font: 'inherit',
      color: 'inherit',
      textAlign: 'start',
      textDecoration: 'none',
      verticalAlign: 'baseline',
    },
    '& .SolarOptionCard-press::after': {
      content: '""',
      position: 'absolute',
      inset: '0',
      borderRadius: 'inherit',
    },
    '& .SolarOptionCard-box :is(a, button, input, select, textarea, [tabindex]):not(.SolarOptionCard-press)':
      {
        position: 'relative',
      },
    '& .SolarOptionCard-name': {
      position: 'absolute',
      width: '1px',
      height: '1px',
      overflow: 'hidden',
      clipPath: 'inset(50%)',
      whiteSpace: 'nowrap',
    },
  },
  root: {
    backgroundColor: 'var(--solar-color-surface-base)',
    borderColor: 'var(--solar-color-border-medium)',
    boxShadow: 'none',
    borderRadius: 'var(--solar-radius-container)',
    borderWidth: 'var(--solar-border-default)',
    borderStyle: 'var(--solar-border-style, solid)',
    '--solar-border-style': 'dashed',
    flexDirection: 'column',
    justifyContent: 'center',
    alignItems: 'center',
    gap: 'var(--solar-stack-lg)',
    paddingTop: 'var(--solar-inset-none)',
    paddingRight: 'var(--solar-inset-none)',
    paddingBottom: 'var(--solar-inset-none)',
    paddingLeft: 'var(--solar-inset-none)',
    width: '100%',
    height: '228px',
    '& .SolarOptionCard--container': {
      backgroundColor: 'var(--solar-color-surface-background)',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-pill)',
      borderStyle: 'none',
      flexDirection: 'row',
      justifyContent: 'center',
      alignItems: 'center',
      gap: 'var(--solar-inset-none)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
      width: '64px',
      minWidth: '64px',
      height: '64px',
      minHeight: '64px',
    },
    '& .SolarOptionCard--iconPlus': {
      width: 'var(--solar-icon-lg)',
      minWidth: 'var(--solar-icon-lg)',
      height: 'var(--solar-icon-lg)',
      minHeight: 'var(--solar-icon-lg)',
      color: 'var(--solar-color-icon-secondary)',
    },
    '& .SolarOptionCard-label': {
      color: 'var(--solar-color-text-primary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-body-md)',
      lineHeight: 'var(--solar-type-line-height-body-md)',
      letterSpacing: '-0.02286em',
      textDecoration: 'none',
    },
  },
  sizes: {},
  appearances: {
    default: {
      '&.SolarOptionCard-pressable:hover': {
        borderColor: 'var(--solar-color-border-subtle)',
        boxShadow: 'none',
        '--solar-border-style': 'dashed',
        '& .SolarOptionCard--iconPlus': {
          color: 'var(--solar-color-icon-primary)',
        },
      },
      '&:has(.SolarOptionCard-press.Mui-focusVisible)': {
        boxShadow: 'var(--solar-shadow-focus-default)',
        borderColor: 'var(--solar-color-border-medium)',
        '--solar-border-style': 'dashed',
        '& .SolarOptionCard--iconPlus': {
          color: 'var(--solar-color-icon-secondary)',
        },
      },
      '&.SolarOptionCard-selected': {
        boxShadow: 'var(--solar-shadow-raised)',
        '--solar-border-style': 'solid',
        '& .SolarOptionCard--iconPlus': {
          color: 'var(--solar-color-icon-primary)',
        },
      },
    },
  },
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarOptionCardComposition = {
  root: {
    base: {
      present: true,
    },
  },
  container: {
    base: {
      present: true,
    },
  },
  iconPlus: {
    base: {
      present: true,
      component: 'Icon/Plus',
      'variant.solid': 'false',
    },
  },
  label: {
    base: {
      present: true,
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarOptionCardStyle(props: SolarOptionCardProps = {}): Style {
  const p: Record<string, unknown> = { ...solarOptionCardDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `default`;
  const s = solarOptionCardStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  return merge(s.reset, s.root, s.appearances[key]);
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarOptionCardParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarOptionCardParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarOptionCardCompose(
  props: SolarOptionCardProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarOptionCardDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `default`;
  const size = '';
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarOptionCardComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}

/** Each layer's children, in Figma's order: the tree the shell draws (`drawChildren`). */
export const solarOptionCardTree: Record<string, string[]> = {
  root: ['container', 'label'],
  container: ['iconPlus'],
};

/** Each slot's layer, to the slot it is: its class is `SolarOptionCard-<slot>`, public. */
export const solarOptionCardSlots: Record<string, string> = { label: 'label' };
