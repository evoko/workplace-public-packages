// SOLAR Chart Tooltip recipe for MUI. Generated by @bwp-web/codegen from spec/components/chart tooltip.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export type SolarChartTooltipSeries = 'single' | 'multi';

export type SolarChartTooltipProps = Record<never, never>;

/** The props and what the shell derives from its content (the recipe is keyed by both). */
export interface SolarChartTooltipRecipeProps extends SolarChartTooltipProps {
  series?: SolarChartTooltipSeries;
}

export const solarChartTooltipDefaults = {
  series: 'single',
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarChartTooltipStyles = {
  reset: {
    display: 'inline-flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarChartTooltip-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarChartTooltip-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarChartTooltip-glyph': {
      display: 'block',
      overflow: 'visible',
      flexShrink: '0',
    },
    '& .SolarChartTooltip-drawnIcon': {
      flexShrink: '0',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
  },
  root: {
    backgroundColor: 'var(--solar-color-surface-raised)',
    borderColor: 'var(--solar-color-border-subtle)',
    boxShadow: 'var(--solar-shadow-overlay)',
    borderRadius: 'var(--solar-radius-control)',
    borderWidth: 'var(--solar-border-default)',
    borderStyle: 'solid',
    flexDirection: 'column',
    justifyContent: 'flex-start',
    alignItems: 'flex-start',
    gap: 'var(--solar-stack-xs)',
    paddingTop: 'var(--solar-inset-sm)',
    paddingRight: 'var(--solar-inset-sm)',
    paddingBottom: 'var(--solar-inset-sm)',
    paddingLeft: 'var(--solar-inset-sm)',
    '& .SolarChartTooltip-title': {
      color: 'var(--solar-color-text-primary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-600)',
      fontSize: 'var(--solar-type-size-body-sm)',
      lineHeight: 'var(--solar-type-line-height-body-sm)',
      letterSpacing: '-0.02em',
      textDecoration: 'none',
    },
    '& .SolarChartTooltip--frame': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'row',
      justifyContent: 'flex-start',
      alignItems: 'center',
      gap: 'var(--solar-inset-2xs)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
    },
    '& .SolarChartTooltip--series': {
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'row',
      justifyContent: 'flex-start',
      alignItems: 'center',
      gap: 'var(--solar-inset-2xs)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
    },
    '& .SolarChartTooltip-value': {
      color: 'var(--solar-color-text-primary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-body-sm)',
      lineHeight: 'var(--solar-type-line-height-body-sm)',
      letterSpacing: '-0.02em',
      textDecoration: 'none',
    },
    '& .SolarChartTooltip-label': {
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-label-sm)',
      lineHeight: 'var(--solar-type-line-height-label-sm)',
      letterSpacing: '-0.02em',
      textDecoration: 'none',
    },
  },
  sizes: {},
  appearances: {
    'series=multi': {
      '& .SolarChartTooltip--frame': {
        justifyContent: 'space-between',
        alignItems: 'center',
        gap: 'var(--solar-inset-md)',
        width: '132px',
        minWidth: '132px',
      },
      '& .SolarChartTooltip--series': {
        backgroundColor: 'transparent',
        borderColor: 'transparent',
        boxShadow: 'none',
      },
      '& .SolarChartTooltip-label': {
        color: 'var(--solar-color-text-secondary)',
      },
    },
  },
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarChartTooltipComposition = {
  root: {
    base: {
      present: true,
    },
  },
  title: {
    base: {
      present: true,
    },
  },
  frame: {
    base: {
      present: true,
    },
  },
  swatch: {
    base: {
      present: true,
      component: 'StatusIndicator',
      'variant.type': 'success',
      'variant.size': 'xs',
    },
    appearance: {
      'series=multi': {
        default: {
          present: false,
        },
      },
    },
  },
  series: {
    base: {
      present: false,
    },
    appearance: {
      'series=multi': {
        default: {
          present: true,
        },
      },
    },
  },
  frameValue: {
    base: {
      present: true,
    },
  },
  rowSwatch: {
    base: {
      present: false,
      component: 'StatusIndicator',
      'variant.size': 'xs',
    },
    appearance: {
      'series=multi': {
        default: {
          present: true,
          'variant.type': 'success',
        },
      },
    },
  },
  frameFrameLabel: {
    base: {
      present: false,
    },
    appearance: {
      'series=multi': {
        default: {
          present: true,
        },
      },
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarChartTooltipStyle(
  props: SolarChartTooltipRecipeProps = {},
): Style {
  const p: Record<string, unknown> = { ...solarChartTooltipDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `series=${p.series}`;
  const s = solarChartTooltipStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  return merge(s.reset, s.root, s.appearances[key]);
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarChartTooltipParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarChartTooltipParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarChartTooltipCompose(
  props: SolarChartTooltipRecipeProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarChartTooltipDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `series=${p.series}`;
  const size = '';
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarChartTooltipComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}

/** Each layer's children, in Figma's order: the tree the shell draws (`drawChildren`). */
export const solarChartTooltipTree: Record<string, string[]> = {
  root: ['title', 'frame'],
  frame: ['swatch', 'series', 'frameValue'],
  series: ['rowSwatch', 'frameFrameLabel'],
};

/** Each slot's layer, to the slot it is: its class is `SolarChartTooltip-<slot>`, public. */
export const solarChartTooltipSlots: Record<string, string> = {
  title: 'title',
  frameFrameLabel: 'label',
  frameValue: 'value',
};
