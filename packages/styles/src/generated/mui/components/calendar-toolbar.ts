// SOLAR Calendar Toolbar recipe for MUI. Generated by @bwp-web/codegen from spec/components/calendar toolbar.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export type SolarCalendarToolbarProps = Record<never, never>;

export const solarCalendarToolbarDefaults = {} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarCalendarToolbarStyles = {
  reset: {
    display: 'inline-flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarCalendarToolbar-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarCalendarToolbar-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarCalendarToolbar-glyph': {
      display: 'block',
      overflow: 'visible',
      flexShrink: '0',
    },
    '& .SolarCalendarToolbar-drawnIcon': {
      flexShrink: '0',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
  },
  root: {
    backgroundColor: 'var(--solar-color-surface-base)',
    borderColor: 'transparent',
    boxShadow: 'none',
    borderRadius: 'var(--solar-radius-none)',
    borderStyle: 'none',
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    gap: 'var(--solar-inset-md)',
    paddingTop: 'var(--solar-inset-sm)',
    paddingRight: 'var(--solar-inset-md)',
    paddingBottom: 'var(--solar-inset-sm)',
    paddingLeft: 'var(--solar-inset-md)',
    width: '100%',
    '& .SolarCalendarToolbar--left': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'row',
      justifyContent: 'flex-start',
      alignItems: 'center',
      gap: 'var(--solar-inset-sm)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
      height: 'var(--solar-size-control-sm)',
      minHeight: 'var(--solar-size-control-sm)',
    },
    '& .SolarCalendarToolbar--nav': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'row',
      justifyContent: 'flex-start',
      alignItems: 'center',
      gap: 'var(--solar-inset-2xs)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
    },
    '& .SolarCalendarToolbar-range': {
      color: 'var(--solar-color-text-primary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-title-xs)',
      lineHeight: 'var(--solar-type-line-height-title-xs)',
      letterSpacing: '-0.03em',
      textDecoration: 'none',
    },
    '& .SolarCalendarToolbar--right': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'row',
      justifyContent: 'flex-start',
      alignItems: 'center',
      gap: 'var(--solar-inset-sm)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
    },
  },
  sizes: {},
  appearances: {},
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarCalendarToolbarComposition = {
  root: {
    base: {
      present: true,
    },
  },
  left: {
    base: {
      present: true,
    },
  },
  nav: {
    base: {
      present: true,
    },
  },
  prev: {
    base: {
      present: true,
      component: 'Icon Button',
      'variant.size': 'md',
      'variant.shape': 'square',
      'variant.prio': 'tertiary',
      'variant.state': 'default',
    },
  },
  next: {
    base: {
      present: true,
      component: 'Icon Button',
      'variant.size': 'md',
      'variant.shape': 'square',
      'variant.prio': 'tertiary',
      'variant.state': 'default',
    },
  },
  todayButton: {
    base: {
      present: true,
      component: 'Button',
      'variant.size': 'sm',
      'variant.prio': 'secondary',
      'variant.state': 'default',
      'variant.danger': 'false',
    },
  },
  range: {
    base: {
      present: true,
    },
  },
  right: {
    base: {
      present: true,
    },
  },
  views: {
    base: {
      present: true,
      component: 'Segmented Control',
      'variant.size': 'sm',
    },
  },
  action: {
    base: {
      present: true,
      component: 'Button',
      'variant.size': 'sm',
      'variant.prio': 'secondary',
      'variant.state': 'default',
      'variant.danger': 'false',
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarCalendarToolbarStyle(
  props: SolarCalendarToolbarProps = {},
): Style {
  const p: Record<string, unknown> = { ...solarCalendarToolbarDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `default`;
  const s = solarCalendarToolbarStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  return merge(s.reset, s.root, s.appearances[key]);
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarCalendarToolbarParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarCalendarToolbarParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarCalendarToolbarCompose(
  props: SolarCalendarToolbarProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarCalendarToolbarDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `default`;
  const size = '';
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarCalendarToolbarComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}

/** Each layer's children, in Figma's order: the tree the shell draws (`drawChildren`). */
export const solarCalendarToolbarTree: Record<string, string[]> = {
  root: ['left', 'right'],
  left: ['nav', 'todayButton', 'range'],
  nav: ['prev', 'next'],
  right: ['views', 'action'],
};

/** Each slot's layer, to the slot it is: its class is `SolarCalendarToolbar-<slot>`, public. */
export const solarCalendarToolbarSlots: Record<string, string> = {
  range: 'range',
  views: 'views',
  action: 'action',
};
