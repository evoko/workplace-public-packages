// SOLAR Time Slot recipe for MUI. Generated by @bwp-web/codegen from spec/components/time slot.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export type SolarTimeSlotDensity = 'comfortable' | 'compact';

export interface SolarTimeSlotProps {
  selected?: boolean;
  density?: SolarTimeSlotDensity;
}

export const solarTimeSlotDefaults = {
  selected: false,
  density: 'comfortable',
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarTimeSlotStyles = {
  reset: {
    display: 'inline-flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarTimeSlot-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarTimeSlot-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarTimeSlot-glyph': {
      display: 'block',
      overflow: 'visible',
      flexShrink: '0',
    },
    '& .SolarTimeSlot-drawnIcon': {
      flexShrink: '0',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
    cursor: 'pointer',
    outline: 'none',
  },
  root: {
    backgroundColor: 'var(--solar-color-surface-base)',
    borderColor: 'var(--solar-color-border-subtle)',
    boxShadow: 'none',
    borderRadius: 'var(--solar-radius-none)',
    borderTopWidth: 'var(--solar-border-default)',
    borderTopStyle: 'solid',
    '--solar-placed-top': 'var(--solar-border-default)',
    borderRightStyle: 'none',
    borderBottomStyle: 'none',
    borderLeftWidth: 'var(--solar-border-default)',
    borderLeftStyle: 'solid',
    '--solar-placed-left': 'var(--solar-border-default)',
    width: '100%',
    height: '48px',
    '& .SolarTimeSlot--halfHourRule': {
      backgroundColor: 'transparent',
      borderColor: 'var(--solar-color-border-subtle)',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderTopWidth: 'var(--solar-border-default)',
      borderTopStyle: 'var(--solar-border-style, solid)',
      borderRightStyle: 'none',
      borderBottomStyle: 'none',
      borderLeftStyle: 'none',
      '--solar-border-style': 'dashed',
      width: '100%',
      minWidth: 0,
      height: 'var(--solar-border-default)',
      minHeight: 'var(--solar-border-default)',
      position: 'absolute',
      left: 'calc(0px - var(--solar-placed-left, 0px))',
      top: 'calc(24px - var(--solar-placed-top, 0px))',
    },
    position: 'relative',
  },
  sizes: {},
  appearances: {
    'density=comfortable': {
      '&:hover': {
        backgroundColor: 'var(--solar-color-surface-hover)',
      },
      '&.SolarTimeSlot-selected': {
        backgroundColor: 'var(--solar-color-surface-feedback-info-subtle)',
        borderColor: 'var(--solar-color-border-feedback-focus-strong)',
      },
    },
    'density=compact': {
      height: '32px',
      '&:hover': {
        backgroundColor: 'var(--solar-color-surface-hover)',
        '& .SolarTimeSlot--halfHourRule': {
          top: 'calc(16px - var(--solar-placed-top, 0px))',
        },
      },
      '&.SolarTimeSlot-selected': {
        backgroundColor: 'var(--solar-color-surface-feedback-info-subtle)',
        borderColor: 'var(--solar-color-border-feedback-focus-strong)',
        '& .SolarTimeSlot--halfHourRule': {
          top: 'calc(16px - var(--solar-placed-top, 0px))',
        },
      },
      '& .SolarTimeSlot--halfHourRule': {
        top: 'calc(16px - var(--solar-placed-top, 0px))',
      },
    },
  },
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarTimeSlotComposition = {
  root: {
    base: {
      present: true,
    },
  },
  halfHourRule: {
    base: {
      present: true,
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarTimeSlotStyle(props: SolarTimeSlotProps = {}): Style {
  const p: Record<string, unknown> = { ...solarTimeSlotDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `density=${p.density}`;
  const s = solarTimeSlotStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  return merge(s.reset, s.root, s.appearances[key]);
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarTimeSlotParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarTimeSlotParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarTimeSlotCompose(
  props: SolarTimeSlotProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarTimeSlotDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `density=${p.density}`;
  const size = '';
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarTimeSlotComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}

/** Each layer's children, in Figma's order: the tree the shell draws (`drawChildren`). */
export const solarTimeSlotTree: Record<string, string[]> = {
  root: ['halfHourRule'],
};

/** Each slot's layer, to the slot it is: its class is `SolarTimeSlot-<slot>`, public. */
export const solarTimeSlotSlots: Record<string, string> = {};
