// SOLAR Table recipe for MUI. Generated by @bwp-web/codegen from spec/components/table.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export type SolarTableBreakpoint = 'desktop' | 'mobile';

export interface SolarTableProps {
  breakpoint?: SolarTableBreakpoint;
  expandable?: boolean;
  selectable?: boolean;
}

export const solarTableDefaults = {
  breakpoint: 'desktop',
  expandable: false,
  selectable: false,
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarTableStyles = {
  reset: {
    display: 'flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarTable-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarTable-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarTable-glyph': {
      display: 'block',
      overflow: 'visible',
      flexShrink: '0',
    },
    '& .SolarTable-drawnIcon': {
      flexShrink: '0',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
    borderCollapse: 'initial',
    '& .SolarTable--dimming': {
      pointerEvents: 'none',
    },
  },
  root: {
    backgroundColor: 'transparent',
    borderColor: 'var(--solar-color-border-subtle)',
    boxShadow: 'none',
    borderRadius: 'var(--solar-radius-none)',
    borderTopStyle: 'none',
    borderRightStyle: 'none',
    '--solar-placed-right': '0px',
    borderBottomWidth: 'var(--solar-border-default)',
    borderBottomStyle: 'solid',
    borderLeftStyle: 'none',
    flexDirection: 'column',
    justifyContent: 'flex-start',
    alignItems: 'center',
    gap: 'var(--solar-stack-none)',
    paddingTop: 'var(--solar-inset-none)',
    paddingRight: 'var(--solar-stack-none)',
    paddingBottom: 'var(--solar-inset-none)',
    paddingLeft: 'var(--solar-stack-none)',
    width: '100%',
    '& .SolarTable-header': {
      width: '100%',
    },
    '& .SolarTable-rows': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'column',
      justifyContent: 'flex-start',
      alignItems: 'center',
      gap: 'var(--solar-stack-none)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
      width: '100%',
    },
    '& .SolarTable--row': {
      width: '100%',
    },
    '& .SolarTable--dimming': {
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      width: '32px',
      height: '100%',
    },
    position: 'relative',
  },
  sizes: {},
  appearances: {
    'breakpoint=desktop, expandable=false, selectable=false': {
      height: 'auto',
      width: '100%',
      '& .SolarTable-rows': {
        height: 'auto',
      },
    },
    'breakpoint=desktop, expandable=false, selectable=true': {
      height: 'auto',
      width: '100%',
      '& .SolarTable-rows': {
        height: 'auto',
      },
    },
    'breakpoint=desktop, expandable=true, selectable=true': {
      height: 'auto',
      width: '100%',
      '& .SolarTable-rows': {
        height: 'auto',
      },
    },
    'breakpoint=mobile, expandable=false, selectable=false': {
      justifyContent: 'center',
      alignItems: 'center',
      height: 'auto',
      width: '100%',
      '& .SolarTable-rows': {
        justifyContent: 'flex-start',
        alignItems: 'flex-start',
        height: 'auto',
      },
      '& .SolarTable--dimming': {
        backgroundColor: 'transparent',
        backgroundImage:
          'linear-gradient(to right, transparent 0%, var(--solar-color-surface-base) 100%)',
        borderColor: 'transparent',
        boxShadow: 'none',
        position: 'absolute',
        right: 'calc(0px - var(--solar-placed-right, 0px))',
        top: 'calc(0px - var(--solar-placed-top, 0px))',
      },
    },
    'breakpoint=mobile, expandable=false, selectable=true': {
      justifyContent: 'center',
      alignItems: 'center',
      height: 'auto',
      width: '100%',
      '& .SolarTable-rows': {
        justifyContent: 'flex-start',
        alignItems: 'flex-start',
        height: 'auto',
      },
      '& .SolarTable--dimming': {
        backgroundColor: 'transparent',
        backgroundImage:
          'linear-gradient(to right, transparent 0%, var(--solar-color-surface-base) 100%)',
        borderColor: 'transparent',
        boxShadow: 'none',
        position: 'absolute',
        right: 'calc(0px - var(--solar-placed-right, 0px))',
        top: 'calc(0px - var(--solar-placed-top, 0px))',
      },
    },
    'breakpoint=mobile, expandable=true, selectable=false': {
      justifyContent: 'center',
      alignItems: 'center',
      '& .SolarTable-rows': {
        justifyContent: 'flex-start',
        alignItems: 'flex-start',
      },
    },
    'breakpoint=mobile, expandable=true, selectable=true': {
      justifyContent: 'center',
      alignItems: 'center',
      height: 'auto',
      width: '100%',
      '& .SolarTable-rows': {
        justifyContent: 'flex-start',
        alignItems: 'flex-start',
        height: 'auto',
      },
      '& .SolarTable--dimming': {
        backgroundColor: 'transparent',
        backgroundImage:
          'linear-gradient(to right, transparent 0%, var(--solar-color-surface-base) 100%)',
        borderColor: 'transparent',
        boxShadow: 'none',
        position: 'absolute',
        right: 'calc(0px - var(--solar-placed-right, 0px))',
        top: 'calc(0px - var(--solar-placed-top, 0px))',
      },
    },
  },
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarTableComposition = {
  root: {
    base: {
      present: true,
    },
  },
  header: {
    base: {
      present: true,
      component: 'Row',
      'variant.type': 'title',
      'variant.state': 'default',
    },
  },
  rows: {
    base: {
      present: true,
    },
  },
  row: {
    base: {
      present: true,
      component: 'Row',
      'variant.type': 'top',
      'variant.state': 'default',
    },
  },
  dimming: {
    base: {
      present: false,
    },
    appearance: {
      'breakpoint=mobile, expandable=true, selectable=true': {
        default: {
          present: true,
        },
      },
      'breakpoint=mobile, expandable=false, selectable=true': {
        default: {
          present: true,
        },
      },
      'breakpoint=mobile, expandable=false, selectable=false': {
        default: {
          present: true,
        },
      },
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarTableStyle(props: SolarTableProps = {}): Style {
  const p: Record<string, unknown> = { ...solarTableDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `breakpoint=${p.breakpoint}, expandable=${p.expandable}, selectable=${p.selectable}`;
  const s = solarTableStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  return merge(s.reset, s.root, s.appearances[key]);
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarTableParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarTableParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarTableCompose(
  props: SolarTableProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarTableDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `breakpoint=${p.breakpoint}, expandable=${p.expandable}, selectable=${p.selectable}`;
  const size = '';
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarTableComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}

/** Each layer's children, in Figma's order: the tree the shell draws (`drawChildren`). */
export const solarTableTree: Record<string, string[]> = {
  root: ['header', 'rows', 'dimming'],
  rows: ['row'],
};

/** Each slot's layer, to the slot it is: its class is `SolarTable-<slot>`, public. */
export const solarTableSlots: Record<string, string> = {
  rows: 'rows',
  header: 'header',
};
