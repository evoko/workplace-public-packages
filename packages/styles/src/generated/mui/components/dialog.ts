// SOLAR Dialog recipe for MUI. Generated by @bwp-web/codegen from spec/components/dialog.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export type SolarDialogType = 'wizard' | 'image' | 'default';

export type SolarDialogProps = Record<never, never>;

/** The props and what the shell derives from its content (the recipe is keyed by both). */
export interface SolarDialogRecipeProps extends SolarDialogProps {
  type?: SolarDialogType;
}

export const solarDialogDefaults = {
  type: 'default',
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarDialogStyles = {
  reset: {
    display: 'flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarDialog-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarDialog--text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarDialog-glyph': {
      display: 'block',
      overflow: 'visible',
      flexShrink: '0',
    },
    '& .SolarDialog-drawnIcon': {
      flexShrink: '0',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
    backgroundImage: 'none',
    maxWidth: 'calc(100% - 2 * var(--solar-inset-xl))',
    '& .SolarDialog-title, & .SolarDialog-imageTitle, & .SolarDialog-description':
      {
        whiteSpace: 'normal',
        minWidth: '0',
      },
    '& .SolarDialog-icon > svg': {
      display: 'block',
      width: '100%',
      height: '100%',
    },
    '& .SolarDialog-modalImage': {
      position: 'relative',
      overflow: 'hidden',
    },
    '& .SolarDialog-modalImage > img, & .SolarDialog-modalImage > video': {
      position: 'absolute',
      inset: '0',
      width: '100%',
      height: '100%',
      objectFit: 'cover',
    },
    '& .SolarDialog--imageClose': {
      zIndex: '1',
    },
    '& .SolarDialog--leading, & .SolarDialog--close': {
      flexShrink: '0',
    },
  },
  root: {
    backgroundColor: 'var(--solar-color-surface-dialog)',
    borderColor: 'transparent',
    boxShadow: 'var(--solar-shadow-dialog)',
    borderRadius: 'var(--solar-radius-dialog)',
    borderStyle: 'none',
    flexDirection: 'column',
    justifyContent: 'flex-start',
    alignItems: 'flex-start',
    gap: 'var(--solar-stack-none)',
    paddingTop: 'var(--solar-inset-none)',
    paddingRight: 'var(--solar-inset-none)',
    paddingBottom: 'var(--solar-inset-none)',
    paddingLeft: 'var(--solar-inset-none)',
    width: '480px',
    '& .SolarDialog--header': {
      backgroundColor: 'transparent',
      borderColor: 'var(--solar-color-border-subtle)',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderTopStyle: 'none',
      borderRightStyle: 'none',
      borderBottomWidth: 'var(--solar-border-default)',
      borderBottomStyle: 'solid',
      borderLeftStyle: 'none',
      flexDirection: 'column',
      justifyContent: 'flex-start',
      alignItems: 'flex-start',
      gap: 'var(--solar-inset-xs)',
      paddingTop: 'var(--solar-inset-xs)',
      paddingRight: 'var(--solar-inset-xs)',
      paddingBottom: 'var(--solar-inset-xs)',
      paddingLeft: 'var(--solar-inset-xs)',
      width: '100%',
    },
    '& .SolarDialog--text': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'row',
      justifyContent: 'space-between',
      alignItems: 'center',
      gap: 'var(--solar-inset-md)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
      width: '100%',
    },
    '& .SolarDialog--leading': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'row',
      justifyContent: 'center',
      alignItems: 'center',
      gap: 'var(--solar-stack-xs)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
      width: '36px',
      height: '36px',
    },
    '& .SolarDialog-icon': {
      width: 'var(--solar-icon-xs)',
      height: 'var(--solar-icon-xs)',
      color: 'var(--solar-color-icon-primary)',
    },
    '& .SolarDialog-title': {
      color: 'var(--solar-color-text-primary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-title-sm)',
      lineHeight: 'var(--solar-type-line-height-title-sm)',
      letterSpacing: '-0.03em',
      textDecoration: 'none',
      width: '100%',
    },
    '& .SolarDialog-modalImage': {
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      '--solar-placed-right': '0px',
      flexDirection: 'row',
      justifyContent: 'flex-end',
      alignItems: 'flex-start',
      gap: 'var(--solar-stack-lg)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
      width: '100%',
      height: '279px',
      position: 'relative',
    },
    '& .SolarDialog-content': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'column',
      justifyContent: 'flex-start',
      alignItems: 'flex-start',
      gap: 'var(--solar-stack-md)',
      paddingTop: 'var(--solar-inset-lg)',
      paddingRight: 'var(--solar-inset-lg)',
      paddingBottom: 'var(--solar-inset-lg)',
      paddingLeft: 'var(--solar-inset-lg)',
      width: '100%',
    },
    '& .SolarDialog-actions': {
      width: '100%',
    },
    '& .SolarDialog-imageTitle': {
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-title-sm)',
      lineHeight: 'var(--solar-type-line-height-title-sm)',
      letterSpacing: '-0.03em',
      textDecoration: 'none',
    },
    '& .SolarDialog-description': {
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-400)',
      fontSize: 'var(--solar-type-size-body-md)',
      lineHeight: 'var(--solar-type-line-height-body-md)',
      letterSpacing: '-0.02286em',
      textDecoration: 'none',
      width: '100%',
    },
    '& .SolarDialog-stepper': {
      width: '100%',
    },
  },
  sizes: {},
  appearances: {
    'type=wizard': {
      '& .SolarDialog--text': {
        justifyContent: 'flex-start',
        alignItems: 'center',
        gap: 'var(--solar-inset-xs)',
      },
      '& .SolarDialog-content': {
        justifyContent: 'flex-start',
        alignItems: 'center',
      },
    },
    'type=image': {
      '& .SolarDialog-modalImage': {
        backgroundColor: 'var(--solar-color-surface-muted)',
        borderColor: 'transparent',
        boxShadow: 'none',
      },
      '& .SolarDialog-content': {
        justifyContent: 'center',
        alignItems: 'center',
        gap: 'var(--solar-stack-lg)',
        paddingTop: 'var(--solar-stack-lg)',
        paddingRight: 'var(--solar-stack-lg)',
        paddingBottom: 'var(--solar-stack-lg)',
        paddingLeft: 'var(--solar-stack-lg)',
      },
      '& .SolarDialog--imageClose': {
        position: 'absolute',
        right: 'calc(16px - var(--solar-placed-right, 0px))',
        top: 'calc(16px - var(--solar-placed-top, 0px))',
      },
      '& .SolarDialog-imageTitle': {
        color: 'var(--solar-color-text-primary)',
      },
      '& .SolarDialog-description': {
        color: 'var(--solar-color-text-primary)',
      },
    },
  },
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarDialogComposition = {
  root: {
    base: {
      present: true,
    },
  },
  header: {
    base: {
      present: true,
    },
    appearance: {
      'type=image': {
        default: {
          present: false,
        },
      },
    },
  },
  text: {
    base: {
      present: true,
    },
    appearance: {
      'type=image': {
        default: {
          present: false,
        },
      },
    },
  },
  leading: {
    base: {
      present: true,
    },
    appearance: {
      'type=image': {
        default: {
          present: false,
        },
      },
    },
  },
  icon: {
    base: {
      present: true,
      component: 'Icon/Empty',
      'variant.solid': 'false',
    },
    appearance: {
      'type=image': {
        default: {
          present: false,
        },
      },
    },
  },
  title: {
    base: {
      present: true,
    },
    appearance: {
      'type=image': {
        default: {
          present: false,
        },
      },
    },
  },
  close: {
    base: {
      present: true,
      component: 'Icon Button',
      'variant.size': 'md',
      'variant.shape': 'round',
      'variant.prio': 'tertiary',
      'variant.state': 'default',
    },
    appearance: {
      'type=image': {
        default: {
          present: false,
        },
      },
    },
  },
  modalImage: {
    base: {
      present: false,
    },
    appearance: {
      'type=image': {
        default: {
          present: true,
          image: true,
        },
      },
    },
  },
  content: {
    base: {
      present: true,
    },
  },
  actions: {
    base: {
      present: true,
      component: 'Button Group',
      'variant.orientation': 'horizontal',
      'variant.type': 'full-width',
    },
  },
  imageClose: {
    base: {
      present: false,
      component: 'Icon Button',
      'variant.size': 'md',
    },
    appearance: {
      'type=image': {
        default: {
          present: true,
          'variant.shape': 'round',
          'variant.prio': 'tertiary',
          'variant.state': 'default',
        },
      },
    },
  },
  imageTitle: {
    base: {
      present: false,
    },
    appearance: {
      'type=image': {
        default: {
          present: true,
        },
      },
    },
  },
  description: {
    base: {
      present: false,
    },
    appearance: {
      'type=image': {
        default: {
          present: true,
        },
      },
    },
  },
  stepper: {
    base: {
      present: false,
      component: 'Stepper',
    },
    appearance: {
      'type=wizard': {
        default: {
          present: true,
          'variant.type': 'line+text',
        },
      },
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarDialogStyle(props: SolarDialogRecipeProps = {}): Style {
  const p: Record<string, unknown> = { ...solarDialogDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `type=${p.type}`;
  const s = solarDialogStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  return merge(s.reset, s.root, s.appearances[key]);
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarDialogParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarDialogParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarDialogCompose(
  props: SolarDialogRecipeProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarDialogDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `type=${p.type}`;
  const size = '';
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarDialogComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}

/** Each layer's children, in Figma's order: the tree the shell draws (`drawChildren`). */
export const solarDialogTree: Record<string, string[]> = {
  root: ['header', 'modalImage', 'content', 'actions'],
  header: ['text', 'stepper'],
  text: ['leading', 'title', 'close'],
  leading: ['icon'],
  modalImage: ['imageClose'],
  content: ['imageTitle', 'description'],
};

/** Each slot's layer, to the slot it is: its class is `SolarDialog-<slot>`, public. */
export const solarDialogSlots: Record<string, string> = {
  title: 'title',
  content: 'content',
  modalImage: 'modalImage',
  icon: 'icon',
  stepper: 'stepper',
  imageTitle: 'imageTitle',
  description: 'description',
  actions: 'actions',
};
