// SOLAR Dropdown Item recipe for MUI. Generated by @bwp-web/codegen from spec/components/dropdown item.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export type SolarDropdownItemSize = 'md' | 'sm';

export interface SolarDropdownItemProps {
  size?: SolarDropdownItemSize;
  selected?: boolean;
  disabled?: boolean;
}

export const solarDropdownItemDefaults = {
  size: 'md',
  selected: false,
  disabled: false,
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarDropdownItemStyles = {
  reset: {
    display: 'flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarDropdownItem-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarDropdownItem-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarDropdownItem-glyph': {
      display: 'block',
      overflow: 'visible',
      flexShrink: '0',
    },
    '& .SolarDropdownItem-drawnIcon': {
      flexShrink: '0',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
    minHeight: '0',
    '&.Mui-disabled': {
      opacity: '1',
    },
    '& .SolarDropdownItem-checkbox, & .SolarDropdownItem-icon': {
      flexShrink: '0',
    },
    '& .SolarDropdownItem-icon > svg': {
      display: 'block',
      width: '100%',
      height: '100%',
    },
    '& .SolarDropdownItem-helper': {
      whiteSpace: 'normal',
    },
  },
  root: {
    backgroundColor: 'transparent',
    borderColor: 'transparent',
    boxShadow: 'none',
    borderRadius: 'var(--solar-radius-none)',
    borderStyle: 'none',
    flexDirection: 'row',
    justifyContent: 'flex-start',
    alignItems: 'center',
    gap: 'var(--solar-inset-xs)',
    paddingTop: 'var(--solar-inset-sm)',
    paddingRight: 'var(--solar-inset-sm)',
    paddingBottom: 'var(--solar-inset-sm)',
    paddingLeft: 'var(--solar-inset-sm)',
    width: '100%',
    '& .SolarDropdownItem-icon': {
      width: 'var(--solar-icon-md)',
      height: 'var(--solar-icon-md)',
      color: 'var(--solar-color-icon-primary)',
    },
    '& .SolarDropdownItem-content': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'column',
      justifyContent: 'flex-start',
      alignItems: 'flex-start',
      gap: 'var(--solar-inset-xs)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
      width: '100%',
    },
    '& .SolarDropdownItem-label': {
      color: 'var(--solar-color-text-primary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-body-md)',
      lineHeight: 'var(--solar-type-line-height-body-md)',
      letterSpacing: '-0.02286em',
      textDecoration: 'none',
      width: '100%',
    },
    '& .SolarDropdownItem-helper': {
      color: 'var(--solar-color-text-secondary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-400)',
      fontSize: 'var(--solar-type-size-body-sm)',
      lineHeight: 'var(--solar-type-line-height-body-sm)',
      letterSpacing: '-0.02em',
      textDecoration: 'none',
      width: '100%',
    },
  },
  sizes: {
    sm: {
      paddingTop: 'var(--solar-inset-xs)',
      paddingBottom: 'var(--solar-inset-xs)',
      width: '100%',
      '& .SolarDropdownItem-icon': {
        width: 'var(--solar-icon-sm)',
        height: 'var(--solar-icon-sm)',
      },
      '& .SolarDropdownItem-label': {
        fontFamily: 'var(--solar-type-font-family-inter)',
        fontWeight: 'var(--solar-type-font-weight-500)',
        fontSize: 'var(--solar-type-size-body-sm)',
        lineHeight: 'var(--solar-type-line-height-body-sm)',
        letterSpacing: '-0.02em',
        textDecoration: 'none',
      },
    },
  },
  appearances: {
    default: {
      '&:hover, &.Mui-focusVisible, &.Mui-focused': {
        backgroundColor: 'var(--solar-color-surface-hover)',
      },
      '&.SolarDropdownItem-selected': {
        backgroundColor: 'var(--solar-color-surface-active)',
      },
      '&.Mui-disabled': {
        '& .SolarDropdownItem-icon': {
          color: 'var(--solar-color-icon-disabled)',
        },
        '& .SolarDropdownItem-label': {
          color: 'var(--solar-color-text-disabled)',
        },
        '& .SolarDropdownItem-helper': {
          color: 'var(--solar-color-text-disabled)',
        },
      },
    },
  },
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarDropdownItemComposition = {
  root: {
    base: {
      present: true,
    },
  },
  checkbox: {
    base: {
      present: true,
      component: 'Checkbox',
      'variant.checked': 'false',
      'variant.disabled': 'false',
      'variant.hover': 'false',
      'variant.mixed': 'false',
      'variant.focus': 'false',
    },
    appearance: {
      default: {
        selected: {
          'variant.checked': 'true',
        },
        disabled: {
          'variant.disabled': 'true',
        },
        hover: {
          'variant.hover': 'true',
        },
      },
    },
  },
  icon: {
    base: {
      present: true,
      component: 'Icon/None',
      'variant.solid': 'false',
    },
  },
  content: {
    base: {
      present: true,
    },
  },
  label: {
    base: {
      present: true,
    },
  },
  helper: {
    base: {
      present: true,
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarDropdownItemStyle(
  props: SolarDropdownItemProps = {},
): Style {
  const p: Record<string, unknown> = { ...solarDropdownItemDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `default`;
  const s = solarDropdownItemStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  const size = p.size as string;
  return merge(
    s.reset,
    s.root,
    s.sizes[size],
    s.appearances[key],
    s.combined[size]?.[key],
  );
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarDropdownItemParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarDropdownItemParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarDropdownItemCompose(
  props: SolarDropdownItemProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarDropdownItemDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `default`;
  const size = p.size as string;
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarDropdownItemComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}
