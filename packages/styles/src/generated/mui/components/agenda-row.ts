// SOLAR Agenda Row recipe for MUI. Generated by @bwp-web/codegen from spec/components/agenda row.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export type SolarAgendaRowDensity = 'comfortable' | 'compact';

export interface SolarAgendaRowProps {
  selected?: boolean;
  density?: SolarAgendaRowDensity;
  color?: string;
}

export const solarAgendaRowDefaults = {
  selected: false,
  density: 'comfortable',
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarAgendaRowStyles = {
  reset: {
    display: 'inline-flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarAgendaRow-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarAgendaRow-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarAgendaRow-glyph': {
      display: 'block',
      overflow: 'visible',
      flexShrink: '0',
    },
    '& .SolarAgendaRow-drawnIcon': {
      flexShrink: '0',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
    cursor: 'pointer',
    outline: 'none',
    '& .SolarAgendaRow-title, & .SolarAgendaRow-meta': {
      minWidth: 0,
      overflow: 'hidden',
      textOverflow: 'ellipsis',
      whiteSpace: 'nowrap',
    },
  },
  root: {
    backgroundColor: 'var(--solar-color-surface-base)',
    borderColor: 'var(--solar-color-border-subtle)',
    boxShadow: 'none',
    borderRadius: 'var(--solar-radius-none)',
    borderTopStyle: 'none',
    borderRightStyle: 'none',
    borderBottomWidth: 'var(--solar-border-default)',
    borderBottomStyle: 'solid',
    borderLeftStyle: 'none',
    flexDirection: 'row',
    justifyContent: 'flex-start',
    alignItems: 'center',
    gap: 'var(--solar-stack-md)',
    paddingTop: 'var(--solar-inset-md)',
    paddingRight: 'var(--solar-inset-md)',
    paddingBottom: 'var(--solar-inset-md)',
    paddingLeft: 'var(--solar-inset-md)',
    width: '100%',
    '& .SolarAgendaRow--time': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'column',
      justifyContent: 'center',
      alignItems: 'flex-start',
      gap: 'var(--solar-stack-xs)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
      width: '36px',
      minWidth: '36px',
    },
    '& .SolarAgendaRow-start': {
      color: 'var(--solar-color-text-primary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-600)',
      fontSize: 'var(--solar-type-size-body-md)',
      lineHeight: 'var(--solar-type-line-height-body-md)',
      letterSpacing: '-0.02286em',
      textDecoration: 'none',
    },
    '& .SolarAgendaRow-end': {
      color: 'var(--solar-color-text-tertiary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-400)',
      fontSize: 'var(--solar-type-size-body-sm)',
      lineHeight: 'var(--solar-type-line-height-body-sm)',
      letterSpacing: '-0.02em',
      textDecoration: 'none',
    },
    '& .SolarAgendaRow--dot': {
      backgroundColor: 'var(--solar-color-data-category-06-strong)',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-pill)',
      borderStyle: 'none',
      width: '8px',
      minWidth: '8px',
      height: '8px',
      minHeight: '8px',
    },
    '& .SolarAgendaRow--body': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'column',
      justifyContent: 'center',
      alignItems: 'flex-start',
      gap: 'var(--solar-stack-xs)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
      width: '100%',
      minWidth: 0,
    },
    '& .SolarAgendaRow-title': {
      color: 'var(--solar-color-text-primary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-600)',
      fontSize: 'var(--solar-type-size-body-md)',
      lineHeight: 'var(--solar-type-line-height-body-md)',
      letterSpacing: '-0.02286em',
      textDecoration: 'none',
      width: '100%',
      minWidth: 0,
    },
    '& .SolarAgendaRow-meta': {
      color: 'var(--solar-color-text-secondary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-400)',
      fontSize: 'var(--solar-type-size-body-sm)',
      lineHeight: 'var(--solar-type-line-height-body-sm)',
      letterSpacing: '-0.02em',
      textDecoration: 'none',
      width: '100%',
      minWidth: 0,
    },
    '& .SolarAgendaRow-range': {
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-body-sm)',
      lineHeight: 'var(--solar-type-line-height-body-sm)',
      letterSpacing: '-0.02em',
      textDecoration: 'none',
    },
  },
  sizes: {},
  appearances: {
    'density=comfortable': {
      '&:hover': {
        backgroundColor: 'var(--solar-color-surface-hover)',
      },
      '&.SolarAgendaRow-selected': {
        backgroundColor: 'var(--solar-color-surface-active)',
      },
    },
    'density=compact': {
      paddingTop: 'var(--solar-inset-sm)',
      paddingBottom: 'var(--solar-inset-sm)',
      '&:hover': {
        backgroundColor: 'var(--solar-color-surface-hover)',
        '& .SolarAgendaRow-range': {
          color: 'var(--solar-color-text-primary)',
        },
      },
      '&.SolarAgendaRow-selected': {
        backgroundColor: 'var(--solar-color-surface-active)',
        '& .SolarAgendaRow-range': {
          color: 'var(--solar-color-text-primary)',
        },
      },
      '& .SolarAgendaRow--time': {
        width: '76px',
        minWidth: '76px',
      },
      '& .SolarAgendaRow-range': {
        color: 'var(--solar-color-text-primary)',
      },
    },
  },
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarAgendaRowComposition = {
  root: {
    base: {
      present: true,
    },
  },
  time: {
    base: {
      present: true,
    },
  },
  start: {
    base: {
      present: true,
    },
    appearance: {
      'density=compact': {
        default: {
          present: false,
        },
        hover: {
          present: false,
        },
        selected: {
          present: false,
        },
      },
    },
  },
  end: {
    base: {
      present: true,
    },
    appearance: {
      'density=compact': {
        default: {
          present: false,
        },
        hover: {
          present: false,
        },
        selected: {
          present: false,
        },
      },
    },
  },
  dot: {
    base: {
      present: true,
    },
  },
  body: {
    base: {
      present: true,
    },
  },
  title: {
    base: {
      present: true,
    },
  },
  meta: {
    base: {
      present: true,
    },
    appearance: {
      'density=compact': {
        default: {
          present: false,
        },
        hover: {
          present: false,
        },
        selected: {
          present: false,
        },
      },
    },
  },
  attendee: {
    base: {
      present: true,
      component: 'Avatar',
      'variant.size': 'md',
      'variant.type': 'text',
      'variant.color': 'purple',
      'variant.shade': 'Light',
    },
    appearance: {
      'density=compact': {
        default: {
          present: false,
        },
        hover: {
          present: false,
        },
        selected: {
          present: false,
        },
      },
    },
  },
  range: {
    base: {
      present: false,
    },
    appearance: {
      'density=compact': {
        default: {
          present: true,
        },
        hover: {
          present: true,
        },
        selected: {
          present: true,
        },
      },
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarAgendaRowStyle(props: SolarAgendaRowProps = {}): Style {
  const p: Record<string, unknown> = { ...solarAgendaRowDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `density=${p.density}`;
  const s = solarAgendaRowStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  return merge(s.reset, s.root, s.appearances[key]);
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarAgendaRowParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarAgendaRowParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarAgendaRowCompose(
  props: SolarAgendaRowProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarAgendaRowDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `density=${p.density}`;
  const size = '';
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarAgendaRowComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}

/** Each layer's children, in Figma's order: the tree the shell draws (`drawChildren`). */
export const solarAgendaRowTree: Record<string, string[]> = {
  root: ['time', 'dot', 'body', 'attendee'],
  time: ['start', 'end', 'range'],
  body: ['title', 'meta'],
};

/** Each slot's layer, to the slot it is: its class is `SolarAgendaRow-<slot>`, public. */
export const solarAgendaRowSlots: Record<string, string> = {
  title: 'title',
  start: 'start',
  end: 'end',
  range: 'range',
  meta: 'meta',
  attendee: 'attendee',
};
