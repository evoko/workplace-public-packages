// SOLAR Context Menu Item recipe for MUI. Generated by @bwp-web/codegen from spec/components/context menu item.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export interface SolarContextMenuItemProps {
  disabled?: boolean;
  destructive?: boolean;
}

export const solarContextMenuItemDefaults = {
  disabled: false,
  destructive: false,
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarContextMenuItemStyles = {
  reset: {
    display: 'flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarContextMenuItem-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarContextMenuItem-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarContextMenuItem-glyph': {
      display: 'block',
      overflow: 'visible',
      flexShrink: '0',
    },
    '& .SolarContextMenuItem-drawnIcon': {
      flexShrink: '0',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
    minHeight: '0',
    '&.Mui-disabled': {
      opacity: '1',
    },
    '& .SolarContextMenuItem-leadingIcon, & .SolarContextMenuItem-trailingIcon':
      {
        flexShrink: '0',
      },
    '& .SolarContextMenuItem-leadingIcon > svg, & .SolarContextMenuItem-trailingIcon > svg':
      {
        display: 'block',
        width: '100%',
        height: '100%',
      },
  },
  root: {
    backgroundColor: 'transparent',
    borderColor: 'transparent',
    boxShadow: 'none',
    borderRadius: 'var(--solar-radius-none)',
    borderStyle: 'none',
    flexDirection: 'row',
    justifyContent: 'flex-start',
    alignItems: 'center',
    gap: 'var(--solar-stack-sm)',
    paddingTop: 'var(--solar-inset-none)',
    paddingRight: 'var(--solar-inset-sm)',
    paddingBottom: 'var(--solar-inset-none)',
    paddingLeft: 'var(--solar-inset-sm)',
    width: '100%',
    height: '36px',
    '& .SolarContextMenuItem-leadingIcon': {
      width: 'var(--solar-icon-sm)',
      minWidth: 'var(--solar-icon-sm)',
      height: 'var(--solar-icon-sm)',
      minHeight: 'var(--solar-icon-sm)',
      color: 'var(--solar-color-icon-primary)',
    },
    '& .SolarContextMenuItem-label': {
      color: 'var(--solar-color-text-primary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-label-md)',
      lineHeight: 'var(--solar-type-line-height-label-md)',
      letterSpacing: '-0.02em',
      textDecoration: 'none',
      width: '100%',
      minWidth: 0,
    },
    '& .SolarContextMenuItem-shortcut': {
      color: 'var(--solar-color-text-tertiary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-body-md)',
      lineHeight: 'var(--solar-type-line-height-body-md)',
      letterSpacing: '-0.02286em',
      textDecoration: 'none',
    },
    '& .SolarContextMenuItem-trailingIcon': {
      width: 'var(--solar-icon-sm)',
      minWidth: 'var(--solar-icon-sm)',
      height: 'var(--solar-icon-sm)',
      minHeight: 'var(--solar-icon-sm)',
      color: 'var(--solar-color-icon-primary)',
    },
  },
  sizes: {},
  appearances: {
    'destructive=false': {
      '&:hover': {
        backgroundColor: 'var(--solar-color-surface-hover)',
      },
      '&.Mui-focusVisible': {
        backgroundColor: 'var(--solar-color-surface-hover)',
        borderColor: 'var(--solar-color-border-feedback-focus-strong)',
        boxShadow: 'var(--solar-shadow-focus-default)',
        borderWidth: 'var(--solar-border-default)',
        borderStyle: 'solid',
      },
      '&.Mui-disabled': {
        '& .SolarContextMenuItem-leadingIcon': {
          color: 'var(--solar-color-icon-disabled)',
        },
        '& .SolarContextMenuItem-label': {
          color: 'var(--solar-color-text-disabled)',
        },
        '& .SolarContextMenuItem-shortcut': {
          color: 'var(--solar-color-text-disabled)',
        },
        '& .SolarContextMenuItem-trailingIcon': {
          color: 'var(--solar-color-icon-disabled)',
        },
      },
    },
    'destructive=true': {
      '&:hover': {
        backgroundColor: 'var(--solar-color-surface-hover)',
        '& .SolarContextMenuItem-leadingIcon': {
          color: 'var(--solar-color-icon-feedback-danger)',
        },
        '& .SolarContextMenuItem-label': {
          color: 'var(--solar-color-text-feedback-danger)',
        },
        '& .SolarContextMenuItem-shortcut': {
          color: 'var(--solar-color-text-tertiary)',
        },
        '& .SolarContextMenuItem-trailingIcon': {
          color: 'var(--solar-color-icon-primary)',
        },
      },
      '&.Mui-focusVisible': {
        backgroundColor: 'var(--solar-color-surface-hover)',
        borderColor: 'var(--solar-color-border-feedback-focus-strong)',
        boxShadow: 'var(--solar-shadow-focus-default)',
        borderWidth: 'var(--solar-border-default)',
        borderStyle: 'solid',
        '& .SolarContextMenuItem-leadingIcon': {
          color: 'var(--solar-color-icon-feedback-danger)',
        },
        '& .SolarContextMenuItem-label': {
          color: 'var(--solar-color-text-feedback-danger)',
        },
        '& .SolarContextMenuItem-shortcut': {
          color: 'var(--solar-color-text-tertiary)',
        },
        '& .SolarContextMenuItem-trailingIcon': {
          color: 'var(--solar-color-icon-primary)',
        },
      },
      '& .SolarContextMenuItem-leadingIcon': {
        color: 'var(--solar-color-icon-feedback-danger)',
      },
      '&.Mui-disabled': {
        '& .SolarContextMenuItem-leadingIcon': {
          color: 'var(--solar-color-icon-disabled)',
        },
        '& .SolarContextMenuItem-label': {
          color: 'var(--solar-color-text-disabled)',
        },
        '& .SolarContextMenuItem-shortcut': {
          color: 'var(--solar-color-text-disabled)',
        },
        '& .SolarContextMenuItem-trailingIcon': {
          color: 'var(--solar-color-icon-disabled)',
        },
      },
      '& .SolarContextMenuItem-label': {
        color: 'var(--solar-color-text-feedback-danger)',
      },
      '& .SolarContextMenuItem-shortcut': {
        color: 'var(--solar-color-text-feedback-danger)',
      },
      '& .SolarContextMenuItem-trailingIcon': {
        color: 'var(--solar-color-icon-feedback-danger)',
      },
    },
  },
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarContextMenuItemComposition = {
  root: {
    base: {
      present: true,
    },
  },
  leadingIcon: {
    base: {
      present: true,
      component: 'Icon/None',
      'variant.solid': 'false',
    },
  },
  label: {
    base: {
      present: true,
    },
  },
  shortcut: {
    base: {
      present: true,
    },
  },
  trailingIcon: {
    base: {
      present: false,
      component: 'Icon/None',
      'variant.solid': 'false',
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarContextMenuItemStyle(
  props: SolarContextMenuItemProps = {},
): Style {
  const p: Record<string, unknown> = { ...solarContextMenuItemDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `destructive=${p.destructive}`;
  const s = solarContextMenuItemStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  return merge(s.reset, s.root, s.appearances[key]);
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarContextMenuItemParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarContextMenuItemParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarContextMenuItemCompose(
  props: SolarContextMenuItemProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarContextMenuItemDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `destructive=${p.destructive}`;
  const size = '';
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarContextMenuItemComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}

/** Each layer's children, in Figma's order: the tree the shell draws (`drawChildren`). */
export const solarContextMenuItemTree: Record<string, string[]> = {
  root: ['leadingIcon', 'label', 'shortcut', 'trailingIcon'],
};

/** Each slot's layer, to the slot it is: its class is `SolarContextMenuItem-<slot>`, public. */
export const solarContextMenuItemSlots: Record<string, string> = {
  leadingIcon: 'leadingIcon',
  label: 'label',
  shortcut: 'shortcut',
  trailingIcon: 'trailingIcon',
};
