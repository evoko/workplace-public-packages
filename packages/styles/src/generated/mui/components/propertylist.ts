// SOLAR PropertyList recipe for MUI. Generated by @bwp-web/codegen from spec/components/propertylist.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export interface SolarPropertyListProps {
  inCard?: boolean;
}

export const solarPropertyListDefaults = {
  inCard: true,
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarPropertyListStyles = {
  reset: {
    display: 'flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarPropertyList-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarPropertyList-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarPropertyList-glyph': {
      display: 'block',
      overflow: 'visible',
      flexShrink: '0',
    },
    '& .SolarPropertyList-drawnIcon': {
      flexShrink: '0',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
    '& .SolarPropertyList-items': {
      margin: '0',
    },
  },
  root: {
    backgroundColor: 'var(--solar-color-surface-raised)',
    borderColor: 'var(--solar-color-border-subtle)',
    boxShadow: 'none',
    borderRadius: 'var(--solar-radius-container)',
    borderWidth: 'var(--solar-border-default)',
    borderStyle: 'solid',
    flexDirection: 'column',
    justifyContent: 'flex-start',
    alignItems: 'flex-start',
    gap: 'var(--solar-stack-none)',
    paddingTop: 'var(--solar-inset-none)',
    paddingRight: 'var(--solar-inset-none)',
    paddingBottom: 'var(--solar-inset-none)',
    paddingLeft: 'var(--solar-inset-none)',
    width: '100%',
    '& .SolarPropertyList-items': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'column',
      justifyContent: 'flex-start',
      alignItems: 'flex-start',
      gap: 'var(--solar-stack-none)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
      width: '100%',
      minWidth: 0,
    },
    '& .SolarPropertyList--propertyRow': {
      width: '100%',
      minWidth: 0,
    },
    '& .SolarPropertyList--divider': {
      width: '100%',
      minWidth: 0,
    },
    '& .SolarPropertyList--propertyRow2': {
      width: '100%',
      minWidth: 0,
    },
    '& .SolarPropertyList--divider2': {
      width: '100%',
      minWidth: 0,
    },
    '& .SolarPropertyList--propertyRow3': {
      width: '100%',
      minWidth: 0,
    },
    '& .SolarPropertyList--divider3': {
      width: '100%',
      minWidth: 0,
    },
    '& .SolarPropertyList--propertyRow4': {
      width: '100%',
      minWidth: 0,
    },
  },
  sizes: {},
  appearances: {
    'inCard=false': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
    },
  },
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarPropertyListComposition = {
  root: {
    base: {
      present: true,
    },
  },
  items: {
    base: {
      present: true,
    },
  },
  propertyRow: {
    base: {
      present: true,
      component: 'PropertyRow',
      'variant.in-card': 'true',
      'variant.trailing': 'action',
    },
    appearance: {
      'inCard=false': {
        default: {
          'variant.in-card': 'false',
        },
      },
    },
  },
  divider: {
    base: {
      present: true,
      component: 'Divider',
      'variant.orientation': 'horizontal',
      'variant.type': 'full',
    },
  },
  propertyRow2: {
    base: {
      present: true,
      component: 'PropertyRow',
      'variant.in-card': 'true',
      'variant.trailing': 'toggle',
    },
    appearance: {
      'inCard=false': {
        default: {
          'variant.in-card': 'false',
        },
      },
    },
  },
  divider2: {
    base: {
      present: true,
      component: 'Divider',
      'variant.orientation': 'horizontal',
      'variant.type': 'full',
    },
  },
  propertyRow3: {
    base: {
      present: true,
      component: 'PropertyRow',
      'variant.in-card': 'true',
      'variant.trailing': 'select',
    },
    appearance: {
      'inCard=false': {
        default: {
          'variant.in-card': 'false',
        },
      },
    },
  },
  divider3: {
    base: {
      present: true,
      component: 'Divider',
      'variant.orientation': 'horizontal',
      'variant.type': 'full',
    },
  },
  propertyRow4: {
    base: {
      present: true,
      component: 'PropertyRow',
      'variant.in-card': 'true',
      'variant.trailing': 'icon-button',
    },
    appearance: {
      'inCard=false': {
        default: {
          'variant.in-card': 'false',
        },
      },
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarPropertyListStyle(
  props: SolarPropertyListProps = {},
): Style {
  const p: Record<string, unknown> = { ...solarPropertyListDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `inCard=${p.inCard}`;
  const s = solarPropertyListStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  return merge(s.reset, s.root, s.appearances[key]);
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarPropertyListParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarPropertyListParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarPropertyListCompose(
  props: SolarPropertyListProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarPropertyListDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `inCard=${p.inCard}`;
  const size = '';
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarPropertyListComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}

/** Each layer's children, in Figma's order: the tree the shell draws (`drawChildren`). */
export const solarPropertyListTree: Record<string, string[]> = {
  root: ['items'],
  items: [
    'propertyRow',
    'divider',
    'propertyRow2',
    'divider2',
    'propertyRow3',
    'divider3',
    'propertyRow4',
  ],
};

/** Each slot's layer, to the slot it is: its class is `SolarPropertyList-<slot>`, public. */
export const solarPropertyListSlots: Record<string, string> = {
  items: 'items',
};
