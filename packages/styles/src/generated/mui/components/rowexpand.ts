// SOLAR RowExpand recipe for MUI. Generated by @bwp-web/codegen from spec/components/rowexpand.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export type SolarRowExpandType =
  | 'bottom-row'
  | 'title-row'
  | 'collapsed'
  | 'expanded'
  | 'middle-row'
  | 'vertical-only';

export interface SolarRowExpandProps {
  type?: SolarRowExpandType;
}

export const solarRowExpandDefaults = {
  type: 'title-row',
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarRowExpandStyles = {
  reset: {
    display: 'inline-flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarRowExpand-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarRowExpand-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarRowExpand-glyph': {
      display: 'block',
      overflow: 'visible',
      flexShrink: '0',
    },
    '& .SolarRowExpand-drawnIcon': {
      display: 'block',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
  },
  root: {
    backgroundColor: 'var(--solar-color-surface-background)',
    borderColor: 'transparent',
    boxShadow: 'none',
    borderRadius: 'var(--solar-radius-none)',
    borderStyle: 'none',
    '--solar-placed-left': '0px',
    '--solar-placed-top': '0px',
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'center',
    gap: 'var(--solar-inset-sm)',
    paddingTop: 'var(--solar-inset-none)',
    paddingRight: 'var(--solar-inset-none)',
    paddingBottom: 'var(--solar-inset-none)',
    paddingLeft: 'var(--solar-inset-none)',
    width: 'var(--solar-icon-sm)',
    height: '40px',
  },
  sizes: {},
  appearances: {
    'type=bottom-row': {
      backgroundColor: 'transparent',
      gap: 'var(--solar-inset-none)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
      '& .SolarRowExpand--container': {
        fill: 'none',
        stroke: 'var(--solar-color-border-medium)',
        '& .SolarGlyph-stroke': {
          fill: 'var(--solar-color-border-medium)',
        },
        strokeWidth: 'var(--solar-border-default)',
      },
    },
    'type=collapsed': {
      backgroundColor: 'transparent',
      '& .SolarRowExpand--iconChevronRight': {
        width: 'var(--solar-icon-sm)',
        height: 'var(--solar-icon-sm)',
        color: 'var(--solar-color-icon-primary)',
      },
    },
    'type=expanded': {
      backgroundColor: 'transparent',
      '& .SolarRowExpand--iconChevronDown': {
        width: 'var(--solar-icon-sm)',
        height: 'var(--solar-icon-sm)',
        color: 'var(--solar-color-icon-primary)',
      },
    },
    'type=middle-row': {
      backgroundColor: 'transparent',
      gap: 'var(--solar-inset-none)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
      '& .SolarRowExpand--container': {
        fill: 'none',
        stroke: 'var(--solar-color-border-medium)',
        '& .SolarGlyph-stroke': {
          fill: 'var(--solar-color-border-medium)',
        },
        strokeWidth: 'var(--solar-border-default)',
      },
      '& .SolarRowExpand--container2': {
        fill: 'none',
        stroke: 'var(--solar-color-border-medium)',
        '& .SolarGlyph-stroke': {
          fill: 'var(--solar-color-border-medium)',
        },
        strokeWidth: 'var(--solar-border-default)',
      },
    },
    'type=vertical-only': {
      backgroundColor: 'transparent',
      gap: 'var(--solar-inset-none)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
      '& .SolarRowExpand--container': {
        fill: 'none',
        stroke: 'var(--solar-color-border-medium)',
        '& .SolarGlyph-stroke': {
          fill: 'var(--solar-color-border-medium)',
        },
        strokeWidth: 'var(--solar-border-default)',
      },
    },
  },
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarRowExpandComposition = {
  root: {
    base: {
      present: true,
    },
  },
  iconChevronRight: {
    base: {
      present: false,
    },
    appearance: {
      'type=collapsed': {
        default: {
          present: true,
          component: 'Icon/ChevronRight',
          'variant.solid': 'false',
        },
      },
    },
  },
  iconChevronDown: {
    base: {
      present: false,
    },
    appearance: {
      'type=expanded': {
        default: {
          present: true,
          component: 'Icon/ChevronDown',
          'variant.solid': 'false',
        },
      },
    },
  },
  container: {
    base: {
      present: false,
    },
    appearance: {
      'type=bottom-row': {
        default: {
          present: true,
          glyph: {
            width: 8,
            height: 20,
            fill: [],
            stroke: [
              {
                d: 'M0 0L-0.5 0L-0.5 16L0 16L0.5 16L0.5 0L0 0ZM4 20L4 20.5L8 20.5L8 20L8 19.5L4 19.5L4 20ZM0 16L-0.5 16C-0.5 18.4853 1.51472 20.5 4 20.5L4 20L4 19.5C2.067 19.5 0.5 17.933 0.5 16L0 16Z',
                evenOdd: false,
              },
            ],
          },
          x: 8,
          y: 0,
        },
      },
      'type=middle-row': {
        default: {
          present: true,
          glyph: {
            width: 8,
            height: 20,
            fill: [],
            stroke: [
              {
                d: 'M0 20L-0.5 20L-0.5 20.5L0 20.5L0 20ZM0 0L-0.5 0L-0.5 20L0 20L0.5 20L0.5 0L0 0ZM0 20L0 20.5L8 20.5L8 20L8 19.5L0 19.5L0 20Z',
                evenOdd: false,
              },
            ],
          },
          x: 8.25,
          y: 0,
        },
      },
      'type=vertical-only': {
        default: {
          present: true,
          glyph: {
            width: 0,
            height: 40,
            fill: [],
            stroke: [
              {
                d: 'M0 0L-0.5 0L-0.5 40L0 40L0.5 40L0.5 0L0 0Z',
                evenOdd: false,
              },
            ],
          },
          x: 8.25,
          y: 0,
        },
      },
    },
  },
  container2: {
    base: {
      present: false,
    },
    appearance: {
      'type=middle-row': {
        default: {
          present: true,
          glyph: {
            width: 0,
            height: 20,
            fill: [],
            stroke: [
              {
                d: 'M0 0L-0.5 0L-0.5 20L0 20L0.5 20L0.5 0L0 0Z',
                evenOdd: false,
              },
            ],
          },
          x: 8.25,
          y: 20,
        },
      },
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarRowExpandStyle(props: SolarRowExpandProps = {}): Style {
  const p: Record<string, unknown> = { ...solarRowExpandDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `type=${p.type}`;
  const s = solarRowExpandStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  return merge(s.reset, s.root, s.appearances[key]);
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarRowExpandParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarRowExpandParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarRowExpandCompose(
  props: SolarRowExpandProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarRowExpandDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `type=${p.type}`;
  const size = '';
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarRowExpandComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}

/** Each layer's children, in Figma's order: the tree the shell draws (`drawChildren`). */
export const solarRowExpandTree: Record<string, string[]> = {
  root: ['iconChevronRight', 'iconChevronDown', 'container', 'container2'],
};

/** Each slot's layer, to the slot it is: its class is `SolarRowExpand-<slot>`, public. */
export const solarRowExpandSlots: Record<string, string> = {};
