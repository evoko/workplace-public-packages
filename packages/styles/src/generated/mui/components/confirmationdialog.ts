// SOLAR ConfirmationDialog recipe for MUI. Generated by @bwp-web/codegen from spec/components/confirmationdialog.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export type SolarConfirmationDialogIntent = 'default' | 'danger';

export interface SolarConfirmationDialogProps {
  intent?: SolarConfirmationDialogIntent;
}

export const solarConfirmationDialogDefaults = {
  intent: 'default',
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarConfirmationDialogStyles = {
  reset: {
    display: 'flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarConfirmationDialog-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarConfirmationDialog-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarConfirmationDialog-glyph': {
      display: 'block',
      overflow: 'visible',
      flexShrink: '0',
    },
    '& .SolarConfirmationDialog-drawnIcon': {
      flexShrink: '0',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
    backgroundImage: 'none',
    maxWidth: 'calc(100% - 2 * var(--solar-inset-xl))',
    '& .SolarConfirmationDialog-title, & .SolarConfirmationDialog-description':
      {
        whiteSpace: 'normal',
        minWidth: '0',
      },
  },
  root: {
    backgroundColor: 'var(--solar-color-surface-dialog)',
    borderColor: 'transparent',
    boxShadow: 'var(--solar-shadow-dialog)',
    borderRadius: 'var(--solar-radius-dialog)',
    borderStyle: 'none',
    flexDirection: 'column',
    justifyContent: 'flex-start',
    alignItems: 'flex-start',
    gap: 'var(--solar-stack-none)',
    paddingTop: 'var(--solar-inset-none)',
    paddingRight: 'var(--solar-inset-none)',
    paddingBottom: 'var(--solar-inset-none)',
    paddingLeft: 'var(--solar-inset-none)',
    width: '400px',
    '& .SolarConfirmationDialog--content': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'column',
      justifyContent: 'flex-start',
      alignItems: 'center',
      gap: 'var(--solar-stack-md)',
      paddingTop: 'var(--solar-inset-xl)',
      paddingRight: 'var(--solar-inset-lg)',
      paddingBottom: 'var(--solar-inset-xl)',
      paddingLeft: 'var(--solar-inset-lg)',
      width: '100%',
    },
    '& .SolarConfirmationDialog-title': {
      color: 'var(--solar-color-text-primary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-title-sm)',
      lineHeight: 'var(--solar-type-line-height-title-sm)',
      letterSpacing: '-0.03em',
      textDecoration: 'none',
      width: '100%',
    },
    '& .SolarConfirmationDialog-description': {
      color: 'var(--solar-color-text-secondary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-400)',
      fontSize: 'var(--solar-type-size-body-md)',
      lineHeight: 'var(--solar-type-line-height-body-md)',
      letterSpacing: '-0.02286em',
      textDecoration: 'none',
      width: '100%',
    },
    '& .SolarConfirmationDialog--buttonGroup': {
      width: '100%',
    },
  },
  sizes: {},
  appearances: {},
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarConfirmationDialogComposition = {
  root: {
    base: {
      present: true,
    },
  },
  content: {
    base: {
      present: true,
    },
  },
  title: {
    base: {
      present: true,
    },
  },
  description: {
    base: {
      present: true,
    },
  },
  buttonGroup: {
    base: {
      present: true,
      component: 'Button Group',
      'variant.orientation': 'horizontal',
      'variant.type': 'full-width',
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarConfirmationDialogStyle(
  props: SolarConfirmationDialogProps = {},
): Style {
  const p: Record<string, unknown> = { ...solarConfirmationDialogDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `default`;
  const s = solarConfirmationDialogStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  return merge(s.reset, s.root, s.appearances[key]);
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarConfirmationDialogParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarConfirmationDialogParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarConfirmationDialogCompose(
  props: SolarConfirmationDialogProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarConfirmationDialogDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `default`;
  const size = '';
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarConfirmationDialogComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}

/** Each layer's children, in Figma's order: the tree the shell draws (`drawChildren`). */
export const solarConfirmationDialogTree: Record<string, string[]> = {
  root: ['content', 'buttonGroup'],
  content: ['title', 'description'],
};

/** Each slot's layer, to the slot it is: its class is `SolarConfirmationDialog-<slot>`, public. */
export const solarConfirmationDialogSlots: Record<string, string> = {
  title: 'title',
  description: 'description',
};
