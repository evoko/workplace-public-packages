// SOLAR TableHeader recipe for MUI. Generated by @bwp-web/codegen from spec/components/tableheader.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export type SolarTableHeaderBreakpoint = 'desktop' | 'mobile';

export interface SolarTableHeaderProps {
  breakpoint?: SolarTableHeaderBreakpoint;
}

export const solarTableHeaderDefaults = {
  breakpoint: 'desktop',
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarTableHeaderStyles = {
  reset: {
    display: 'flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarTableHeader-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarTableHeader-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarTableHeader-glyph': {
      display: 'block',
      overflow: 'visible',
      flexShrink: '0',
    },
    '& .SolarTableHeader-drawnIcon': {
      flexShrink: '0',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
    '& .SolarTableHeader-search, & .SolarTableHeader-segmentedControl, & .SolarTableHeader-segmentedControlMobile':
      {
        flexShrink: '0',
      },
  },
  root: {
    backgroundColor: 'transparent',
    borderColor: 'transparent',
    boxShadow: 'none',
    borderRadius: 'var(--solar-radius-none)',
    borderStyle: 'none',
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    gap: 'var(--solar-inset-md)',
    paddingTop: 'var(--solar-inset-xs)',
    paddingRight: 'var(--solar-stack-none)',
    paddingBottom: 'var(--solar-inset-xs)',
    paddingLeft: 'var(--solar-stack-none)',
    width: '100%',
    '& .SolarTableHeader--container': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'row',
      justifyContent: 'space-between',
      alignItems: 'center',
      gap: 'var(--solar-inset-md)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
      width: '100%',
      minWidth: 0,
    },
    '& .SolarTableHeader-search': {
      width: '240px',
      minWidth: '240px',
    },
    '& .SolarTableHeader-actions': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'row',
      justifyContent: 'flex-end',
      alignItems: 'center',
      gap: 'var(--solar-stack-xs)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
    },
    '& .SolarTableHeader-actionsMobile': {
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'row',
      justifyContent: 'flex-start',
      alignItems: 'center',
      gap: 'var(--solar-stack-xs)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
    },
  },
  sizes: {},
  appearances: {
    'breakpoint=mobile': {
      width: '100%',
      '& .SolarTableHeader-actionsMobile': {
        backgroundColor: 'transparent',
        borderColor: 'transparent',
        boxShadow: 'none',
      },
    },
  },
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarTableHeaderComposition = {
  root: {
    base: {
      present: true,
    },
  },
  container: {
    base: {
      present: true,
    },
    appearance: {
      'breakpoint=mobile': {
        default: {
          present: false,
        },
      },
    },
  },
  search: {
    base: {
      present: true,
      component: 'SearchField',
      'variant.state': 'default',
      'variant.size': 'md',
    },
    appearance: {
      'breakpoint=mobile': {
        default: {
          present: false,
        },
      },
    },
  },
  segmentedControl: {
    base: {
      present: true,
      component: 'Segmented Control',
      'variant.size': 'md',
    },
    appearance: {
      'breakpoint=mobile': {
        default: {
          present: false,
        },
      },
    },
  },
  actions: {
    base: {
      present: true,
    },
    appearance: {
      'breakpoint=mobile': {
        default: {
          present: false,
        },
      },
    },
  },
  containerActionsIconButton: {
    base: {
      present: true,
      component: 'Icon Button',
      'variant.size': 'md',
      'variant.shape': 'square',
      'variant.prio': 'secondary',
      'variant.state': 'default',
    },
    appearance: {
      'breakpoint=mobile': {
        default: {
          present: false,
        },
      },
    },
  },
  containerActionsIconButton2: {
    base: {
      present: true,
      component: 'Icon Button',
      'variant.size': 'md',
      'variant.shape': 'square',
      'variant.prio': 'secondary',
      'variant.state': 'default',
    },
    appearance: {
      'breakpoint=mobile': {
        default: {
          present: false,
        },
      },
    },
  },
  containerActionsIconButton3: {
    base: {
      present: true,
      component: 'Icon Button',
      'variant.size': 'md',
      'variant.shape': 'square',
      'variant.prio': 'secondary',
      'variant.state': 'default',
    },
    appearance: {
      'breakpoint=mobile': {
        default: {
          present: false,
        },
      },
    },
  },
  segmentedControlMobile: {
    base: {
      present: false,
      component: 'Segmented Control',
      'variant.size': 'md',
    },
    appearance: {
      'breakpoint=mobile': {
        default: {
          present: true,
        },
      },
    },
  },
  actionsMobile: {
    base: {
      present: false,
    },
    appearance: {
      'breakpoint=mobile': {
        default: {
          present: true,
        },
      },
    },
  },
  actionsIconButton: {
    base: {
      present: false,
      component: 'Icon Button',
      'variant.size': 'md',
    },
    appearance: {
      'breakpoint=mobile': {
        default: {
          present: true,
          'variant.shape': 'square',
          'variant.prio': 'secondary',
          'variant.state': 'default',
        },
      },
    },
  },
  actionsIconButton2: {
    base: {
      present: false,
      component: 'Icon Button',
      'variant.size': 'md',
    },
    appearance: {
      'breakpoint=mobile': {
        default: {
          present: true,
          'variant.shape': 'square',
          'variant.prio': 'secondary',
          'variant.state': 'default',
        },
      },
    },
  },
  actionsIconButton3: {
    base: {
      present: false,
      component: 'Icon Button',
      'variant.size': 'md',
    },
    appearance: {
      'breakpoint=mobile': {
        default: {
          present: true,
          'variant.shape': 'square',
          'variant.prio': 'secondary',
          'variant.state': 'default',
        },
      },
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarTableHeaderStyle(
  props: SolarTableHeaderProps = {},
): Style {
  const p: Record<string, unknown> = { ...solarTableHeaderDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `breakpoint=${p.breakpoint}`;
  const s = solarTableHeaderStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  return merge(s.reset, s.root, s.appearances[key]);
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarTableHeaderParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarTableHeaderParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarTableHeaderCompose(
  props: SolarTableHeaderProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarTableHeaderDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `breakpoint=${p.breakpoint}`;
  const size = '';
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarTableHeaderComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}

/** Each layer's children, in Figma's order: the tree the shell draws (`drawChildren`). */
export const solarTableHeaderTree: Record<string, string[]> = {
  root: ['container', 'segmentedControlMobile', 'actionsMobile'],
  container: ['search', 'segmentedControl', 'actions'],
  actions: [
    'containerActionsIconButton',
    'containerActionsIconButton2',
    'containerActionsIconButton3',
  ],
  actionsMobile: [
    'actionsIconButton',
    'actionsIconButton2',
    'actionsIconButton3',
  ],
};

/** Each slot's layer, to the slot it is: its class is `SolarTableHeader-<slot>`, public. */
export const solarTableHeaderSlots: Record<string, string> = {
  actions: 'actions',
  search: 'search',
  segmentedControl: 'segmentedControl',
  segmentedControlMobile: 'segmentedControlMobile',
  actionsMobile: 'actionsMobile',
};
