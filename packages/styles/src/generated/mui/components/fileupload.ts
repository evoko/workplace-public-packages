// SOLAR FileUpload recipe for MUI. Generated by @bwp-web/codegen from spec/components/fileupload.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export interface SolarFileUploadProps {
  error?: boolean;
  disabled?: boolean;
}

/** The props and what the shell derives from its content (the recipe is keyed by both). */
export interface SolarFileUploadRecipeProps extends SolarFileUploadProps {
  filled?: boolean;
}

export const solarFileUploadDefaults = {
  error: false,
  disabled: false,
  filled: false,
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarFileUploadStyles = {
  reset: {
    display: 'inline-flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarFileUpload-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarFileUpload-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarFileUpload-glyph': {
      display: 'block',
      overflow: 'visible',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
    '& .SolarFileUpload-fileName': {
      flex: '1 1 0%',
      minWidth: '0',
      overflow: 'hidden',
      textOverflow: 'ellipsis',
    },
    '& .SolarFileUpload-helper': {
      whiteSpace: 'normal',
    },
  },
  root: {
    backgroundColor: 'transparent',
    borderColor: 'transparent',
    boxShadow: 'none',
    borderRadius: 'var(--solar-radius-none)',
    borderStyle: 'none',
    flexDirection: 'column',
    justifyContent: 'flex-start',
    alignItems: 'flex-start',
    gap: 'var(--solar-stack-sm)',
    paddingTop: 'var(--solar-inset-none)',
    paddingRight: 'var(--solar-inset-none)',
    paddingBottom: 'var(--solar-inset-none)',
    paddingLeft: 'var(--solar-inset-none)',
    width: '100%',
    '& .SolarFileUpload-label': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'row',
      justifyContent: 'flex-start',
      alignItems: 'flex-start',
      gap: 'var(--solar-inset-2xs)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
    },
    '& .SolarFileUpload-uploadAFile': {
      color: 'var(--solar-color-text-primary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-body-md)',
      lineHeight: 'var(--solar-type-line-height-body-md)',
      letterSpacing: '-0.02286em',
      textDecoration: 'none',
    },
    '& .SolarFileUpload-mandatory': {
      color: 'var(--solar-color-text-feedback-info)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-label-md)',
      lineHeight: 'var(--solar-type-line-height-label-md)',
      letterSpacing: '-0.02em',
      textDecoration: 'none',
    },
    '& .SolarFileUpload-field': {
      backgroundColor: 'var(--solar-color-surface-base)',
      borderColor: 'var(--solar-color-border-subtle)',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-control)',
      borderWidth: 'var(--solar-border-default)',
      borderStyle: 'solid',
      flexDirection: 'row',
      justifyContent: 'flex-start',
      alignItems: 'center',
      gap: 'var(--solar-stack-sm)',
      paddingTop: 'var(--solar-inset-sm)',
      paddingRight: 'var(--solar-inset-sm)',
      paddingBottom: 'var(--solar-inset-sm)',
      paddingLeft: 'var(--solar-inset-md)',
      width: '100%',
    },
    '& .SolarFileUpload-iconFile': {
      width: 'var(--solar-icon-md)',
      height: 'var(--solar-icon-md)',
      color: 'var(--solar-color-icon-tertiary)',
    },
    '& .SolarFileUpload-fileName': {
      color: 'var(--solar-color-text-tertiary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-400)',
      fontSize: 'var(--solar-type-size-body-md)',
      lineHeight: 'var(--solar-type-line-height-body-md)',
      letterSpacing: '-0.02286em',
      textDecoration: 'none',
    },
    '& .SolarFileUpload-helper': {
      color: 'var(--solar-color-text-tertiary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-400)',
      fontSize: 'var(--solar-type-size-helper-sm)',
      lineHeight: 'var(--solar-type-line-height-helper-sm)',
      letterSpacing: '-0.02em',
      textDecoration: 'none',
    },
  },
  sizes: {},
  appearances: {
    default: {
      '&:has(.SolarFileUpload-field:hover), &.SolarFileUpload-dragging': {
        '& .SolarFileUpload-field': {
          borderColor: 'var(--solar-color-border-medium)',
        },
      },
      '&:has(.SolarFileUpload-field :focus-visible)': {
        boxShadow: 'none',
        '& .SolarFileUpload-field': {
          boxShadow: 'var(--solar-shadow-focus-default)',
          borderColor: 'var(--solar-color-border-subtle)',
        },
      },
      '&.SolarFileUpload-filled': {
        '& .SolarFileUpload-iconFile': {
          color: 'var(--solar-color-icon-primary)',
        },
        '& .SolarFileUpload-fileName': {
          color: 'var(--solar-color-text-primary)',
        },
      },
      '&.SolarFileUpload-error': {
        '& .SolarFileUpload-field': {
          backgroundColor: 'var(--solar-color-surface-feedback-danger-subtle)',
          borderColor: 'var(--solar-color-border-feedback-danger-subtle)',
        },
        '& .SolarFileUpload-iconFile': {
          color: 'var(--solar-color-icon-feedback-danger)',
        },
        '& .SolarFileUpload-fileName': {
          color: 'var(--solar-color-text-feedback-danger)',
        },
        '& .SolarFileUpload-helper': {
          color: 'var(--solar-color-text-feedback-danger)',
        },
      },
      '&.SolarFileUpload-disabled': {
        '& .SolarFileUpload-field': {
          backgroundColor: 'var(--solar-color-surface-muted)',
          borderColor: 'transparent',
          borderStyle: 'none',
        },
        '& .SolarFileUpload-iconFile': {
          color: 'var(--solar-color-icon-disabled)',
        },
        '& .SolarFileUpload-fileName': {
          color: 'var(--solar-color-text-disabled)',
        },
        '& .SolarFileUpload-helper': {
          color: 'var(--solar-color-text-disabled)',
        },
      },
    },
  },
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarFileUploadComposition = {
  root: {
    base: {
      present: true,
    },
  },
  label: {
    base: {
      present: true,
    },
  },
  uploadAFile: {
    base: {
      present: true,
    },
  },
  mandatory: {
    base: {
      present: true,
    },
  },
  field: {
    base: {
      present: true,
    },
  },
  iconFile: {
    base: {
      present: true,
      component: 'Icon/File',
      'variant.solid': 'false',
    },
  },
  fileName: {
    base: {
      present: true,
    },
  },
  button: {
    base: {
      present: true,
      component: 'Button',
      'variant.size': 'md',
      'variant.prio': 'secondary',
      'variant.state': 'default',
      'variant.danger': 'false',
    },
    appearance: {
      default: {
        filled: {
          present: false,
        },
        disabled: {
          'variant.state': 'disabled',
        },
      },
    },
  },
  helper: {
    base: {
      present: true,
    },
  },
  iconButton: {
    base: {
      present: false,
      component: 'Icon Button',
      'variant.size': 'md',
    },
    appearance: {
      default: {
        filled: {
          present: true,
          'variant.shape': 'square',
          'variant.prio': 'secondary',
          'variant.state': 'default',
        },
      },
    },
  },
  iconButton2: {
    base: {
      present: false,
      component: 'Icon Button',
      'variant.size': 'md',
    },
    appearance: {
      default: {
        filled: {
          present: true,
          'variant.shape': 'square',
          'variant.prio': 'secondary',
          'variant.state': 'default',
        },
      },
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarFileUploadStyle(
  props: SolarFileUploadRecipeProps = {},
): Style {
  const p: Record<string, unknown> = { ...solarFileUploadDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `default`;
  const s = solarFileUploadStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  return merge(s.reset, s.root, s.appearances[key]);
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarFileUploadParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarFileUploadParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarFileUploadCompose(
  props: SolarFileUploadRecipeProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarFileUploadDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `default`;
  const size = '';
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarFileUploadComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}
