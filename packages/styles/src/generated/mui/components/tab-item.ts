// SOLAR Tab Item recipe for MUI. Generated by @bwp-web/codegen from spec/components/tab item.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export type SolarTabItemSize = 'sm' | 'md';

export interface SolarTabItemProps {
  size?: SolarTabItemSize;
  disabled?: boolean;
  selected?: boolean;
}

export const solarTabItemDefaults = {
  size: 'md',
  disabled: false,
  selected: false,
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarTabItemStyles = {
  reset: {
    display: 'flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarTabItem-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarTabItem-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarTabItem-glyph': {
      display: 'block',
      overflow: 'visible',
      flexShrink: '0',
    },
    '& .SolarTabItem-drawnIcon': {
      flexShrink: '0',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
    flexDirection: 'row',
    minHeight: '0',
    minWidth: '0',
    maxWidth: 'none',
    textTransform: 'none',
    opacity: '1',
    overflow: 'visible',
    borderStyle: 'solid',
    '& .SolarTabItem-leadingIcon, & .SolarTabItem-trailingIcon, & .SolarTabItem-counter':
      {
        flexShrink: '0',
      },
    '& .SolarTabItem-leadingIcon > svg, & .SolarTabItem-trailingIcon > svg': {
      display: 'block',
      width: '100%',
      height: '100%',
    },
    '&': {
      position: 'relative',
    },
    '&::after': {
      content: '""',
      position: 'absolute',
      top: '50%',
      left: '50%',
      width: 'max(100%, 44px)',
      height: 'max(100%, 44px)',
      transform: 'translate(-50%, -50%)',
    },
  },
  root: {
    backgroundColor: 'transparent',
    borderColor: 'transparent',
    boxShadow: 'none',
    borderRadius: 'var(--solar-radius-none)',
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'center',
    gap: 'var(--solar-stack-xs)',
    paddingTop: 'var(--solar-inset-sm)',
    paddingRight: 'var(--solar-inset-md)',
    paddingBottom: 'var(--solar-inset-sm)',
    paddingLeft: 'var(--solar-inset-md)',
    height: '40px',
    borderTopStyle: 'none',
    borderRightStyle: 'none',
    borderBottomStyle: 'none',
    borderLeftStyle: 'none',
    '& .SolarTabItem-leadingIcon': {
      width: 'var(--solar-icon-sm)',
      height: 'var(--solar-icon-sm)',
      color: 'var(--solar-color-icon-secondary)',
    },
    '& .SolarTabItem-label': {
      color: 'var(--solar-color-text-secondary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-body-md)',
      lineHeight: 'var(--solar-type-line-height-body-md)',
      letterSpacing: '-0.02286em',
      textDecoration: 'none',
    },
    '& .SolarTabItem-trailingIcon': {
      width: 'var(--solar-icon-sm)',
      height: 'var(--solar-icon-sm)',
      color: 'var(--solar-color-icon-secondary)',
    },
  },
  sizes: {
    sm: {
      paddingTop: 'var(--solar-inset-xs)',
      paddingRight: 'var(--solar-inset-sm)',
      paddingBottom: 'var(--solar-inset-xs)',
      paddingLeft: 'var(--solar-inset-sm)',
      height: '32px',
      '& .SolarTabItem-leadingIcon': {
        width: 'var(--solar-icon-xs)',
        height: 'var(--solar-icon-xs)',
      },
      '& .SolarTabItem-label': {
        fontFamily: 'var(--solar-type-font-family-inter)',
        fontWeight: 'var(--solar-type-font-weight-500)',
        fontSize: 'var(--solar-type-size-body-sm)',
        lineHeight: 'var(--solar-type-line-height-body-sm)',
        letterSpacing: '-0.02em',
        textDecoration: 'none',
      },
      '& .SolarTabItem-trailingIcon': {
        width: 'var(--solar-icon-xs)',
        height: 'var(--solar-icon-xs)',
      },
    },
  },
  appearances: {
    default: {
      '&:hover': {
        '& .SolarTabItem-leadingIcon': {
          color: 'var(--solar-color-icon-primary)',
        },
        '& .SolarTabItem-label': {
          color: 'var(--solar-color-text-primary)',
        },
        '& .SolarTabItem-trailingIcon': {
          color: 'var(--solar-color-icon-primary)',
        },
      },
      '&.Mui-focusVisible': {
        borderColor: 'var(--solar-color-border-strong)',
        boxShadow: 'var(--solar-shadow-focus-default)',
        borderBottomWidth: 'var(--solar-border-strong)',
        borderBottomStyle: 'solid',
        '& .SolarTabItem-leadingIcon': {
          color: 'var(--solar-color-icon-primary)',
        },
        '& .SolarTabItem-label': {
          color: 'var(--solar-color-text-primary)',
        },
        '& .SolarTabItem-trailingIcon': {
          color: 'var(--solar-color-icon-primary)',
        },
      },
      '&.Mui-selected': {
        borderColor: 'var(--solar-color-border-strong)',
        borderBottomWidth: 'var(--solar-border-strong)',
        borderBottomStyle: 'solid',
        '& .SolarTabItem-leadingIcon': {
          color: 'var(--solar-color-icon-primary)',
        },
        '& .SolarTabItem-label': {
          color: 'var(--solar-color-text-primary)',
        },
        '& .SolarTabItem-trailingIcon': {
          color: 'var(--solar-color-icon-primary)',
        },
      },
      '&.Mui-disabled': {
        '& .SolarTabItem-leadingIcon': {
          color: 'var(--solar-color-icon-disabled)',
        },
        '& .SolarTabItem-label': {
          color: 'var(--solar-color-text-disabled)',
        },
        '& .SolarTabItem-trailingIcon': {
          color: 'var(--solar-color-icon-disabled)',
        },
      },
    },
  },
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarTabItemComposition = {
  root: {
    base: {
      present: true,
    },
  },
  leadingIcon: {
    base: {
      present: true,
      component: 'Icon/None',
      'variant.solid': 'false',
    },
  },
  label: {
    base: {
      present: true,
    },
  },
  trailingIcon: {
    base: {
      present: true,
      component: 'Icon/None',
      'variant.solid': 'false',
    },
  },
  counter: {
    base: {
      present: true,
      component: 'Counter',
      'variant.type': 'idle',
      'variant.state': 'default',
    },
    appearance: {
      default: {
        selected: {
          'variant.type': 'regular',
        },
        focus: {
          'variant.type': 'regular',
        },
        disabled: {
          'variant.type': 'regular',
        },
      },
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarTabItemStyle(props: SolarTabItemProps = {}): Style {
  const p: Record<string, unknown> = { ...solarTabItemDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `default`;
  const s = solarTabItemStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  const size = p.size as string;
  return merge(
    s.reset,
    s.root,
    s.sizes[size],
    s.appearances[key],
    s.combined[size]?.[key],
  );
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarTabItemParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarTabItemParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarTabItemCompose(
  props: SolarTabItemProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarTabItemDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `default`;
  const size = p.size as string;
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarTabItemComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}
