// SOLAR Counter recipe for MUI. Generated by @bwp-web/codegen from spec/components/counter.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export type SolarCounterType = 'regular' | 'danger' | 'inverted' | 'idle';

export interface SolarCounterProps {
  type?: SolarCounterType;
  disabled?: boolean;
}

export const solarCounterDefaults = {
  type: 'regular',
  disabled: false,
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarCounterStyles = {
  reset: {
    display: 'inline-flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarCounter-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarCounter-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarCounter-glyph': {
      display: 'block',
      overflow: 'visible',
      flexShrink: '0',
    },
    '& .SolarCounter-drawnIcon': {
      flexShrink: '0',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
    '&:is(button)': {
      position: 'relative',
    },
    '&:is(button):disabled': {
      cursor: 'default',
    },
    '&:is(button)::after': {
      content: '""',
      position: 'absolute',
      top: '50%',
      left: '50%',
      width: 'max(100%, 44px)',
      height: 'max(100%, 44px)',
      transform: 'translate(-50%, -50%)',
    },
  },
  root: {
    backgroundColor: 'var(--solar-color-action-primary-bg-default)',
    borderColor: 'var(--solar-color-border-medium)',
    boxShadow: 'none',
    borderRadius: 'var(--solar-radius-pill)',
    borderWidth: 'var(--solar-border-default)',
    borderStyle: 'solid',
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'center',
    gap: 'var(--solar-inset-none)',
    paddingTop: 'var(--solar-inset-none)',
    paddingRight: 'var(--solar-inset-xs)',
    paddingBottom: 'var(--solar-inset-none)',
    paddingLeft: 'var(--solar-inset-xs)',
    height: '20px',
    '& .SolarCounter--value': {
      color: 'var(--solar-color-action-primary-text-default)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-600)',
      fontSize: 'var(--solar-type-size-body-sm)',
      lineHeight: 'var(--solar-type-line-height-body-sm)',
      letterSpacing: '-0.02em',
      textDecoration: 'none',
    },
  },
  sizes: {},
  appearances: {
    'type=regular': {
      '&:is(button):not(:disabled):hover, button:not(:disabled):not([role="tab"]):hover &':
        {
          backgroundColor: 'var(--solar-color-action-primary-bg-hover)',
          '& .SolarCounter--value': {
            color: 'var(--solar-color-action-primary-text-hover)',
          },
        },
      '&:is(button):not(:disabled):active, button:not(:disabled):not([role="tab"]):active &':
        {
          backgroundColor: 'var(--solar-color-action-primary-bg-active)',
          '& .SolarCounter--value': {
            color: 'var(--solar-color-action-primary-text-active)',
          },
        },
      '&.SolarCounter-disabled, button:not([role="tab"]):disabled &, .Mui-disabled:not([role="tab"]) &':
        {
          backgroundColor: 'var(--solar-color-action-primary-bg-disabled)',
          '& .SolarCounter--value': {
            color: 'var(--solar-color-action-primary-text-disabled)',
          },
        },
    },
    'type=danger': {
      backgroundColor: 'var(--solar-color-action-primary-bg-danger-default)',
      '&:is(button):not(:disabled):hover, button:not(:disabled):not([role="tab"]):hover &':
        {
          backgroundColor: 'var(--solar-color-action-primary-bg-danger-hover)',
          '& .SolarCounter--value': {
            color: 'var(--solar-color-action-primary-text-danger-hover)',
          },
        },
      '&:is(button):not(:disabled):active, button:not(:disabled):not([role="tab"]):active &':
        {
          backgroundColor: 'var(--solar-color-action-primary-bg-danger-active)',
          '& .SolarCounter--value': {
            color: 'var(--solar-color-action-primary-text-danger-active)',
          },
        },
      '&.SolarCounter-disabled, button:not([role="tab"]):disabled &, .Mui-disabled:not([role="tab"]) &':
        {
          backgroundColor:
            'var(--solar-color-action-primary-bg-danger-disabled)',
          '& .SolarCounter--value': {
            color: 'var(--solar-color-action-primary-text-danger-disabled)',
          },
        },
      '& .SolarCounter--value': {
        color: 'var(--solar-color-action-primary-text-danger-default)',
      },
    },
    'type=inverted': {
      backgroundColor: 'var(--solar-color-action-secondary-bg-default)',
      borderWidth: 'var(--solar-border-none)',
      borderStyle: 'solid',
      '&:is(button):not(:disabled):hover, button:not(:disabled):not([role="tab"]):hover &':
        {
          backgroundColor: 'var(--solar-color-action-secondary-bg-hover)',
          '& .SolarCounter--value': {
            color: 'var(--solar-color-action-secondary-text-hover)',
          },
        },
      '&:is(button):not(:disabled):active, button:not(:disabled):not([role="tab"]):active &':
        {
          backgroundColor: 'var(--solar-color-action-secondary-bg-active)',
          '& .SolarCounter--value': {
            color: 'var(--solar-color-action-secondary-text-active)',
          },
        },
      '&.SolarCounter-disabled, button:not([role="tab"]):disabled &, .Mui-disabled:not([role="tab"]) &':
        {
          backgroundColor: 'var(--solar-color-action-secondary-bg-disabled)',
          '& .SolarCounter--value': {
            color: 'var(--solar-color-action-secondary-text-disabled)',
          },
        },
      '& .SolarCounter--value': {
        color: 'var(--solar-color-action-secondary-text-default)',
      },
    },
    'type=idle': {
      backgroundColor: 'var(--solar-color-action-secondary-bg-default)',
      borderColor: 'var(--solar-color-border-subtle)',
      '&:is(button):not(:disabled):hover, button:not(:disabled):not([role="tab"]):hover &':
        {
          backgroundColor: 'var(--solar-color-action-secondary-bg-default)',
          borderColor: 'var(--solar-color-border-subtle)',
          '& .SolarCounter--value': {
            color: 'var(--solar-color-text-secondary)',
          },
        },
      '&:is(button):not(:disabled):active, button:not(:disabled):not([role="tab"]):active &':
        {
          backgroundColor: 'var(--solar-color-action-secondary-bg-default)',
          borderColor: 'var(--solar-color-border-subtle)',
          '& .SolarCounter--value': {
            color: 'var(--solar-color-text-tertiary)',
          },
        },
      '&.SolarCounter-disabled, button:not([role="tab"]):disabled &, .Mui-disabled:not([role="tab"]) &':
        {
          backgroundColor: 'var(--solar-color-action-primary-bg-disabled)',
          borderColor: 'var(--solar-color-border-medium)',
          '& .SolarCounter--value': {
            color: 'var(--solar-color-action-primary-text-disabled)',
          },
        },
      '& .SolarCounter--value': {
        color: 'var(--solar-color-text-tertiary)',
      },
    },
  },
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarCounterComposition = {
  root: {
    base: {
      present: true,
    },
  },
  value: {
    base: {
      present: true,
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarCounterStyle(props: SolarCounterProps = {}): Style {
  const p: Record<string, unknown> = { ...solarCounterDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `type=${p.type}`;
  const s = solarCounterStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  return merge(s.reset, s.root, s.appearances[key]);
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarCounterParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarCounterParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarCounterCompose(
  props: SolarCounterProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarCounterDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `type=${p.type}`;
  const size = '';
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarCounterComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}
