// SOLAR Section Nav Item recipe for MUI. Generated by @bwp-web/codegen from spec/components/section nav item.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export interface SolarSectionNavItemProps {
  selected?: boolean;
  disabled?: boolean;
}

export const solarSectionNavItemDefaults = {
  selected: false,
  disabled: false,
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarSectionNavItemStyles = {
  reset: {
    display: 'flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarSectionNavItem-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarSectionNavItem-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarSectionNavItem-glyph': {
      display: 'block',
      overflow: 'visible',
      flexShrink: '0',
    },
    '& .SolarSectionNavItem-drawnIcon': {
      flexShrink: '0',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
    textDecoration: 'none',
    '& .SolarSectionNavItem-icon': {
      flexShrink: '0',
    },
    '& .SolarSectionNavItem-icon > svg': {
      display: 'block',
      width: '100%',
      height: '100%',
    },
  },
  root: {
    backgroundColor: 'transparent',
    borderColor: 'transparent',
    boxShadow: 'none',
    borderRadius: 'var(--solar-radius-control)',
    borderStyle: 'none',
    flexDirection: 'row',
    justifyContent: 'flex-start',
    alignItems: 'center',
    gap: 'var(--solar-inset-xs)',
    paddingTop: 'var(--solar-inset-none)',
    paddingRight: 'var(--solar-inset-xs)',
    paddingBottom: 'var(--solar-inset-none)',
    paddingLeft: 'var(--solar-inset-xs)',
    width: '100%',
    height: 'var(--solar-size-control-sm)',
    '& .SolarSectionNavItem-icon': {
      width: 'var(--solar-icon-sm)',
      height: 'var(--solar-icon-sm)',
      color: 'var(--solar-color-icon-secondary)',
    },
    '& .SolarSectionNavItem-label': {
      color: 'var(--solar-color-text-secondary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-label-md)',
      lineHeight: 'var(--solar-type-line-height-label-md)',
      letterSpacing: '-0.02em',
      textDecoration: 'none',
      width: '100%',
    },
  },
  sizes: {},
  appearances: {
    default: {
      '&:hover': {
        backgroundColor: 'var(--solar-color-surface-hover)',
        '& .SolarSectionNavItem-icon': {
          color: 'var(--solar-color-icon-primary)',
        },
        '& .SolarSectionNavItem-label': {
          color: 'var(--solar-color-text-primary)',
        },
      },
      '&.Mui-focusVisible': {
        boxShadow: 'var(--solar-shadow-focus-default)',
        backgroundColor: 'transparent',
        '& .SolarSectionNavItem-icon': {
          color: 'var(--solar-color-icon-secondary)',
        },
        '& .SolarSectionNavItem-label': {
          color: 'var(--solar-color-text-secondary)',
        },
      },
      '&.SolarSectionNavItem-selected': {
        backgroundColor: 'var(--solar-color-surface-active)',
        '& .SolarSectionNavItem-icon': {
          color: 'var(--solar-color-icon-primary)',
        },
        '& .SolarSectionNavItem-label': {
          color: 'var(--solar-color-text-primary)',
        },
      },
      '&.Mui-disabled': {
        opacity: '0.4',
      },
    },
  },
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarSectionNavItemComposition = {
  root: {
    base: {
      present: true,
    },
  },
  icon: {
    base: {
      present: true,
      component: 'Icon/None',
      'variant.solid': 'false',
    },
  },
  label: {
    base: {
      present: true,
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarSectionNavItemStyle(
  props: SolarSectionNavItemProps = {},
): Style {
  const p: Record<string, unknown> = { ...solarSectionNavItemDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `default`;
  const s = solarSectionNavItemStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  return merge(s.reset, s.root, s.appearances[key]);
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarSectionNavItemParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarSectionNavItemParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarSectionNavItemCompose(
  props: SolarSectionNavItemProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarSectionNavItemDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `default`;
  const size = '';
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarSectionNavItemComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}

/** Each layer's children, in Figma's order: the tree the shell draws (`drawChildren`). */
export const solarSectionNavItemTree: Record<string, string[]> = {
  root: ['icon', 'label'],
};

/** Each slot's layer, to the slot it is: its class is `SolarSectionNavItem-<slot>`, public. */
export const solarSectionNavItemSlots: Record<string, string> = {
  icon: 'icon',
  label: 'label',
};
