// SOLAR Breadcrumbs recipe for MUI. Generated by @bwp-web/codegen from spec/components/breadcrumbs.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export type SolarBreadcrumbsProps = Record<never, never>;

export const solarBreadcrumbsDefaults = {} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarBreadcrumbsStyles = {
  reset: {
    display: 'flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarBreadcrumbs-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarBreadcrumbs-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarBreadcrumbs-glyph': {
      display: 'block',
      overflow: 'visible',
      flexShrink: '0',
    },
    '& .SolarBreadcrumbs-drawnIcon': {
      flexShrink: '0',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
    '& .SolarBreadcrumbs-list': {
      display: 'flex',
      flexWrap: 'wrap',
      gap: 'inherit',
      alignItems: 'inherit',
      margin: '0',
      padding: '0',
      listStyle: 'none',
    },
    '& .SolarBreadcrumbs-list > li': {
      display: 'flex',
    },
    '& .SolarBreadcrumbs--iconChevronRight, & .SolarBreadcrumbs--iconChevronRight2, & .SolarBreadcrumbs--iconChevronRight3, & .SolarBreadcrumbs--iconChevronRight4':
      {
        display: 'block',
        flexShrink: '0',
      },
  },
  root: {
    backgroundColor: 'transparent',
    borderColor: 'transparent',
    boxShadow: 'none',
    borderRadius: 'var(--solar-radius-none)',
    borderStyle: 'none',
    flexDirection: 'row',
    justifyContent: 'flex-start',
    alignItems: 'center',
    gap: 'var(--solar-stack-sm)',
    paddingTop: 'var(--solar-inset-none)',
    paddingRight: 'var(--solar-inset-none)',
    paddingBottom: 'var(--solar-inset-none)',
    paddingLeft: 'var(--solar-inset-none)',
    '& .SolarBreadcrumbs--iconChevronRight': {
      width: 'var(--solar-icon-xs)',
      height: 'var(--solar-icon-xs)',
      color: 'var(--solar-color-icon-secondary)',
    },
    '& .SolarBreadcrumbs--iconChevronRight2': {
      width: 'var(--solar-icon-xs)',
      height: 'var(--solar-icon-xs)',
      color: 'var(--solar-color-icon-secondary)',
    },
    '& .SolarBreadcrumbs--iconChevronRight3': {
      width: 'var(--solar-icon-xs)',
      height: 'var(--solar-icon-xs)',
      color: 'var(--solar-color-icon-secondary)',
    },
    '& .SolarBreadcrumbs--iconChevronRight4': {
      width: 'var(--solar-icon-xs)',
      height: 'var(--solar-icon-xs)',
      color: 'var(--solar-color-icon-secondary)',
    },
  },
  sizes: {},
  appearances: {},
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarBreadcrumbsComposition = {
  root: {
    base: {
      present: true,
    },
  },
  item1: {
    base: {
      present: true,
      component: 'Breadcrumb Item',
      'variant.type': 'link',
      'variant.state': 'default',
    },
  },
  iconChevronRight: {
    base: {
      present: true,
      component: 'Icon/ChevronRight',
      'variant.solid': 'false',
    },
  },
  item2: {
    base: {
      present: true,
      component: 'Breadcrumb Item',
      'variant.type': 'link',
      'variant.state': 'default',
    },
  },
  iconChevronRight2: {
    base: {
      present: true,
      component: 'Icon/ChevronRight',
      'variant.solid': 'false',
    },
  },
  current: {
    base: {
      present: true,
      component: 'Breadcrumb Item',
      'variant.type': 'current',
      'variant.state': 'default',
    },
  },
  item3: {
    base: {
      present: true,
      component: 'Breadcrumb Item',
      'variant.type': 'link',
      'variant.state': 'default',
    },
  },
  iconChevronRight3: {
    base: {
      present: true,
      component: 'Icon/ChevronRight',
      'variant.solid': 'false',
    },
  },
  item4: {
    base: {
      present: true,
      component: 'Breadcrumb Item',
      'variant.type': 'link',
      'variant.state': 'default',
    },
  },
  iconChevronRight4: {
    base: {
      present: true,
      component: 'Icon/ChevronRight',
      'variant.solid': 'false',
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarBreadcrumbsStyle(
  props: SolarBreadcrumbsProps = {},
): Style {
  const p: Record<string, unknown> = { ...solarBreadcrumbsDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `default`;
  const s = solarBreadcrumbsStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  return merge(s.reset, s.root, s.appearances[key]);
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarBreadcrumbsParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarBreadcrumbsParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarBreadcrumbsCompose(
  props: SolarBreadcrumbsProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarBreadcrumbsDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `default`;
  const size = '';
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarBreadcrumbsComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}
