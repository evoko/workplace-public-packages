// SOLAR Context Menu recipe for MUI. Generated by @bwp-web/codegen from spec/components/context menu.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export type SolarContextMenuProps = Record<never, never>;

export const solarContextMenuDefaults = {} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarContextMenuStyles = {
  reset: {
    display: 'inline-flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarContextMenu-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarContextMenu-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarContextMenu-glyph': {
      display: 'block',
      overflow: 'visible',
      flexShrink: '0',
    },
    '& .SolarContextMenu-drawnIcon': {
      flexShrink: '0',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
    flexDirection: 'column',
    maxHeight: '300px',
    '& .SolarContextMenu-content': {
      margin: '0',
      padding: '0',
      listStyle: 'none',
      outline: 'none',
      overflowY: 'auto',
      minHeight: '0',
    },
    '& .SolarContextMenu-content > *': {
      width: '100%',
    },
  },
  root: {
    backgroundColor: 'var(--solar-color-surface-dialog)',
    borderColor: 'var(--solar-color-border-subtle)',
    boxShadow: 'var(--solar-shadow-dialog)',
    borderRadius: 'var(--solar-radius-container)',
    borderWidth: 'var(--solar-border-default)',
    borderStyle: 'solid',
    flexDirection: 'column',
    justifyContent: 'flex-start',
    alignItems: 'flex-start',
    gap: 'var(--solar-stack-none)',
    paddingTop: 'var(--solar-inset-none)',
    paddingRight: 'var(--solar-inset-none)',
    paddingBottom: 'var(--solar-inset-none)',
    paddingLeft: 'var(--solar-inset-none)',
    '& .SolarContextMenu-content': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'column',
      justifyContent: 'flex-start',
      alignItems: 'flex-start',
      gap: 'var(--solar-stack-none)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
    },
    '& .SolarContextMenu-contextMenuItem': {
      width: '100%',
    },
    '& .SolarContextMenu-contextMenuItem2': {
      width: '100%',
    },
    '& .SolarContextMenu-contextMenuItem3': {
      width: '100%',
    },
    '& .SolarContextMenu-contextMenuItem4': {
      width: '100%',
    },
    '& .SolarContextMenu-contextMenuItem5': {
      width: '100%',
    },
    '& .SolarContextMenu-contextMenuItem6': {
      width: '100%',
    },
    '& .SolarContextMenu-divider': {
      width: '100%',
    },
    '& .SolarContextMenu-contextMenuItem7': {
      width: '100%',
    },
  },
  sizes: {},
  appearances: {},
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarContextMenuComposition = {
  root: {
    base: {
      present: true,
    },
  },
  content: {
    base: {
      present: true,
    },
  },
  contextMenuItem: {
    base: {
      present: true,
      component: 'Context Menu Item',
      'variant.state': 'default',
      'variant.destructive': 'false',
    },
  },
  contextMenuItem2: {
    base: {
      present: true,
      component: 'Context Menu Item',
      'variant.state': 'default',
      'variant.destructive': 'false',
    },
  },
  contextMenuItem3: {
    base: {
      present: true,
      component: 'Context Menu Item',
      'variant.state': 'default',
      'variant.destructive': 'false',
    },
  },
  contextMenuItem4: {
    base: {
      present: true,
      component: 'Context Menu Item',
      'variant.state': 'default',
      'variant.destructive': 'false',
    },
  },
  contextMenuItem5: {
    base: {
      present: true,
      component: 'Context Menu Item',
      'variant.state': 'default',
      'variant.destructive': 'false',
    },
  },
  contextMenuItem6: {
    base: {
      present: true,
      component: 'Context Menu Item',
      'variant.state': 'default',
      'variant.destructive': 'false',
    },
  },
  divider: {
    base: {
      present: true,
      component: 'Divider',
      'variant.orientation': 'horizontal',
      'variant.type': 'full',
    },
  },
  contextMenuItem7: {
    base: {
      present: true,
      component: 'Context Menu Item',
      'variant.state': 'default',
      'variant.destructive': 'true',
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarContextMenuStyle(
  props: SolarContextMenuProps = {},
): Style {
  const p: Record<string, unknown> = { ...solarContextMenuDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `default`;
  const s = solarContextMenuStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  return merge(s.reset, s.root, s.appearances[key]);
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarContextMenuParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarContextMenuParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarContextMenuCompose(
  props: SolarContextMenuProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarContextMenuDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `default`;
  const size = '';
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarContextMenuComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}
