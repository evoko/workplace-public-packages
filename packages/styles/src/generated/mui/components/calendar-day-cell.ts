// SOLAR Calendar Day Cell recipe for MUI. Generated by @bwp-web/codegen from spec/components/calendar day cell.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export interface SolarCalendarDayCellProps {
  otherMonth?: boolean;
  selected?: boolean;
  today?: boolean;
  todayColumn?: boolean;
}

export const solarCalendarDayCellDefaults = {
  otherMonth: false,
  selected: false,
  today: false,
  todayColumn: false,
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarCalendarDayCellStyles = {
  reset: {
    display: 'inline-flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarCalendarDayCell-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarCalendarDayCell-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarCalendarDayCell-glyph': {
      display: 'block',
      overflow: 'visible',
      flexShrink: '0',
    },
    '& .SolarCalendarDayCell-drawnIcon': {
      flexShrink: '0',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
    overflow: 'hidden',
  },
  root: {
    backgroundColor: 'var(--solar-color-surface-base)',
    borderColor: 'var(--solar-color-border-surface)',
    boxShadow: 'none',
    borderRadius: 'var(--solar-radius-none)',
    borderTopWidth: 'var(--solar-border-default)',
    borderTopStyle: 'solid',
    borderRightStyle: 'none',
    borderBottomStyle: 'none',
    borderLeftWidth: 'var(--solar-border-default)',
    borderLeftStyle: 'solid',
    flexDirection: 'column',
    justifyContent: 'flex-start',
    alignItems: 'flex-start',
    gap: 'var(--solar-stack-xs)',
    paddingTop: 'var(--solar-inset-xs)',
    paddingRight: 'var(--solar-inset-xs)',
    paddingBottom: 'var(--solar-inset-xs)',
    paddingLeft: 'var(--solar-inset-xs)',
    width: '100%',
    height: '120px',
    '& .SolarCalendarDayCell--dayNumRow': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'row',
      justifyContent: 'flex-start',
      alignItems: 'center',
      gap: 'var(--solar-inset-none)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-2xs)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-2xs)',
      width: '100%',
      minWidth: 0,
    },
    '& .SolarCalendarDayCell--dayNumPill': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-pill)',
      borderStyle: 'none',
      flexDirection: 'row',
      justifyContent: 'center',
      alignItems: 'center',
      gap: 'var(--solar-inset-none)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-2xs)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-2xs)',
      width: '24px',
      minWidth: '24px',
      height: '24px',
      minHeight: '24px',
    },
    '& .SolarCalendarDayCell-day': {
      color: 'var(--solar-color-text-primary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-body-sm)',
      lineHeight: 'var(--solar-type-line-height-body-sm)',
      letterSpacing: '-0.02em',
      textDecoration: 'none',
    },
    '& .SolarCalendarDayCell-events': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'column',
      justifyContent: 'flex-start',
      alignItems: 'flex-start',
      gap: 'var(--solar-inset-2xs)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
      width: '100%',
      minWidth: 0,
    },
  },
  sizes: {},
  appearances: {
    default: {
      '&.SolarCalendarDayCell-todayColumn': {
        backgroundColor: 'var(--solar-color-surface-feedback-info-subtle)',
      },
      '&.SolarCalendarDayCell-today': {
        '& .SolarCalendarDayCell--dayNumPill': {
          backgroundColor: 'var(--solar-color-surface-inverse)',
          borderColor: 'var(--solar-color-border-medium)',
          borderWidth: 'var(--solar-border-default)',
          borderStyle: 'solid',
        },
        '& .SolarCalendarDayCell-day': {
          color: 'var(--solar-color-text-inverse)',
          fontFamily: 'var(--solar-type-font-family-inter)',
          fontWeight: 'var(--solar-type-font-weight-600)',
          fontSize: 'var(--solar-type-size-body-sm)',
          lineHeight: 'var(--solar-type-line-height-body-sm)',
          letterSpacing: '-0.02em',
          textDecoration: 'none',
        },
      },
      '&.SolarCalendarDayCell-selected': {
        backgroundColor: 'var(--solar-color-surface-active)',
      },
      '&.SolarCalendarDayCell-otherMonth': {
        backgroundColor: 'var(--solar-color-surface-background)',
        '& .SolarCalendarDayCell-day': {
          color: 'var(--solar-color-text-tertiary)',
        },
        '& .SolarCalendarDayCell-events': {
          opacity: '0.5',
        },
      },
    },
  },
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarCalendarDayCellComposition = {
  root: {
    base: {
      present: true,
    },
  },
  dayNumRow: {
    base: {
      present: true,
    },
  },
  dayNumPill: {
    base: {
      present: true,
    },
  },
  day: {
    base: {
      present: true,
    },
  },
  events: {
    base: {
      present: true,
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarCalendarDayCellStyle(
  props: SolarCalendarDayCellProps = {},
): Style {
  const p: Record<string, unknown> = { ...solarCalendarDayCellDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `default`;
  const s = solarCalendarDayCellStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  return merge(s.reset, s.root, s.appearances[key]);
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarCalendarDayCellParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarCalendarDayCellParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarCalendarDayCellCompose(
  props: SolarCalendarDayCellProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarCalendarDayCellDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `default`;
  const size = '';
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarCalendarDayCellComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}

/** Each layer's children, in Figma's order: the tree the shell draws (`drawChildren`). */
export const solarCalendarDayCellTree: Record<string, string[]> = {
  root: ['dayNumRow', 'events'],
  dayNumRow: ['dayNumPill'],
  dayNumPill: ['day'],
};

/** Each slot's layer, to the slot it is: its class is `SolarCalendarDayCell-<slot>`, public. */
export const solarCalendarDayCellSlots: Record<string, string> = {
  day: 'day',
  events: 'events',
};
