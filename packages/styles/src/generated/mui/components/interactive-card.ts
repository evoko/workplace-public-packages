// SOLAR Interactive Card recipe for MUI. Generated by @bwp-web/codegen from spec/components/interactive card.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export type SolarInteractiveCardControl =
  'none' | 'checkbox' | 'radio' | 'toggle';

export interface SolarInteractiveCardProps {
  selected?: boolean;
  dragging?: boolean;
  control?: SolarInteractiveCardControl;
}

export const solarInteractiveCardDefaults = {
  selected: false,
  dragging: false,
  control: 'none',
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarInteractiveCardStyles = {
  reset: {
    display: 'flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarInteractiveCard-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarInteractiveCard-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarInteractiveCard-glyph': {
      display: 'block',
      overflow: 'visible',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
    '& .SolarInteractiveCard-title, & .SolarInteractiveCard-description': {
      whiteSpace: 'normal',
      minWidth: '0',
    },
    '& .SolarInteractiveCard-icon': {
      flexShrink: '0',
    },
    '& .SolarInteractiveCard-icon > svg': {
      display: 'block',
      width: '100%',
      height: '100%',
    },
    '& .SolarInteractiveCard-press': {
      position: 'static',
      display: 'inline',
      padding: '0',
      margin: '0',
      font: 'inherit',
      color: 'inherit',
      textAlign: 'start',
      textDecoration: 'none',
      verticalAlign: 'baseline',
    },
    '& .SolarInteractiveCard-press::after': {
      content: '""',
      position: 'absolute',
      inset: '0',
      borderRadius: 'inherit',
    },
    '& .SolarInteractiveCard-box :is(a, button, input, select, textarea, [tabindex]):not(.SolarInteractiveCard-press)':
      {
        position: 'relative',
      },
    '& .SolarInteractiveCard-name': {
      position: 'absolute',
      width: '1px',
      height: '1px',
      overflow: 'hidden',
      clipPath: 'inset(50%)',
      whiteSpace: 'nowrap',
    },
    '& .SolarInteractiveCard-dragHandle, & .SolarInteractiveCard-toggle, & .SolarInteractiveCard-radioButton, & .SolarInteractiveCard-checkbox, & .SolarInteractiveCard-actions':
      {
        flexShrink: '0',
      },
  },
  root: {
    backgroundColor: 'var(--solar-color-surface-background)',
    borderColor: 'var(--solar-color-border-subtle)',
    boxShadow: 'none',
    borderRadius: 'var(--solar-radius-dialog)',
    borderWidth: 'var(--solar-border-default)',
    borderStyle: 'solid',
    flexDirection: 'row',
    justifyContent: 'flex-start',
    alignItems: 'center',
    gap: 'var(--solar-stack-md)',
    paddingTop: 'var(--solar-inset-md)',
    paddingRight: 'var(--solar-inset-lg)',
    paddingBottom: 'var(--solar-inset-md)',
    paddingLeft: 'var(--solar-inset-md)',
    width: '100%',
    '& .SolarInteractiveCard-text': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'column',
      justifyContent: 'flex-start',
      alignItems: 'flex-start',
      gap: 'var(--solar-stack-sm)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
      width: '100%',
    },
    '& .SolarInteractiveCard-textIcon': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'row',
      justifyContent: 'flex-start',
      alignItems: 'center',
      gap: 'var(--solar-inset-xs)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
      width: '100%',
    },
    '& .SolarInteractiveCard-icon': {
      width: 'var(--solar-icon-sm)',
      height: 'var(--solar-icon-sm)',
      color: 'var(--solar-color-icon-primary)',
    },
    '& .SolarInteractiveCard-title': {
      color: 'var(--solar-color-text-primary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-600)',
      fontSize: 'var(--solar-type-size-body-md)',
      lineHeight: 'var(--solar-type-line-height-body-md)',
      letterSpacing: '-0.02286em',
      textDecoration: 'none',
    },
    '& .SolarInteractiveCard-description': {
      color: 'var(--solar-color-text-secondary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-400)',
      fontSize: 'var(--solar-type-size-body-md)',
      lineHeight: 'var(--solar-type-line-height-body-md)',
      letterSpacing: '-0.02286em',
      textDecoration: 'none',
    },
    '& .SolarInteractiveCard-actions': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'row',
      justifyContent: 'flex-start',
      alignItems: 'center',
      gap: 'var(--solar-inset-xs)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
    },
  },
  sizes: {},
  appearances: {
    'selected=false, dragging=false': {
      '&:has(.SolarInteractiveCard-press.Mui-focusVisible)': {
        boxShadow: 'var(--solar-shadow-focus-default)',
      },
    },
    'selected=false, dragging=true': {
      borderColor: 'var(--solar-color-border-feedback-focus-strong)',
      boxShadow: 'var(--solar-shadow-focus-default)',
      borderWidth: 'var(--solar-border-strong)',
      borderStyle: 'solid',
    },
    'selected=true, dragging=false': {
      backgroundColor: 'var(--solar-color-surface-feedback-info-subtle)',
      '&:has(.SolarInteractiveCard-press.Mui-focusVisible)': {
        boxShadow: 'var(--solar-shadow-focus-default)',
      },
      '& .SolarInteractiveCard-description': {
        color: 'var(--solar-color-text-primary)',
      },
    },
    'selected=true, dragging=true': {
      borderWidth: 'var(--solar-border-strong)',
      borderStyle: 'solid',
    },
  },
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarInteractiveCardComposition = {
  root: {
    base: {
      present: true,
    },
  },
  dragHandle: {
    base: {
      present: true,
      component: 'DragHandle',
      'variant.size': 'sm',
      'variant.state': 'default',
    },
  },
  toggle: {
    base: {
      present: true,
      component: 'Toggle',
      'variant.selected': 'false',
      'variant.state': 'default',
    },
    appearance: {
      'selected=true, dragging=false': {
        default: {
          'variant.selected': 'true',
        },
      },
    },
  },
  radioButton: {
    base: {
      present: true,
      component: 'Radio',
      'variant.state': 'default',
      'variant.checked': 'false',
    },
    appearance: {
      'selected=true, dragging=false': {
        default: {
          'variant.checked': 'true',
        },
      },
    },
  },
  checkbox: {
    base: {
      present: true,
      component: 'Checkbox',
      'variant.checked': 'false',
      'variant.disabled': 'false',
      'variant.hover': 'false',
      'variant.mixed': 'false',
      'variant.focus': 'false',
    },
    appearance: {
      'selected=true, dragging=false': {
        default: {
          'variant.checked': 'true',
        },
      },
    },
  },
  text: {
    base: {
      present: true,
    },
  },
  textIcon: {
    base: {
      present: true,
    },
  },
  icon: {
    base: {
      present: false,
      component: 'Icon/None',
      'variant.solid': 'false',
    },
  },
  title: {
    base: {
      present: true,
    },
  },
  description: {
    base: {
      present: true,
    },
  },
  actions: {
    base: {
      present: false,
    },
  },
  iconButton: {
    base: {
      present: true,
      component: 'Icon Button',
      'variant.size': 'sm',
      'variant.shape': 'square',
      'variant.prio': 'secondary',
      'variant.state': 'default',
    },
  },
  iconButton2: {
    base: {
      present: true,
      component: 'Icon Button',
      'variant.size': 'sm',
      'variant.shape': 'square',
      'variant.prio': 'secondary',
      'variant.state': 'default',
    },
  },
  iconButton3: {
    base: {
      present: true,
      component: 'Icon Button',
      'variant.size': 'sm',
      'variant.shape': 'square',
      'variant.prio': 'secondary',
      'variant.state': 'default',
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarInteractiveCardStyle(
  props: SolarInteractiveCardProps = {},
): Style {
  const p: Record<string, unknown> = { ...solarInteractiveCardDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `selected=${p.selected}, dragging=${p.dragging}`;
  const s = solarInteractiveCardStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  return merge(s.reset, s.root, s.appearances[key]);
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarInteractiveCardParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarInteractiveCardParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarInteractiveCardCompose(
  props: SolarInteractiveCardProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarInteractiveCardDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `selected=${p.selected}, dragging=${p.dragging}`;
  const size = '';
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarInteractiveCardComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}
