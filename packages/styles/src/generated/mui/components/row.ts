// SOLAR Row recipe for MUI. Generated by @bwp-web/codegen from spec/components/row.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export type SolarRowType =
  'top' | 'middle' | 'title' | 'bottom' | 'non-expandable';

export interface SolarRowProps {
  type?: SolarRowType;
  selected?: boolean;
}

export const solarRowDefaults = {
  type: 'non-expandable',
  selected: false,
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarRowStyles = {
  reset: {
    display: 'flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarRow-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarRow-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarRow-glyph': {
      display: 'block',
      overflow: 'visible',
      flexShrink: '0',
    },
    '& .SolarRow-drawnIcon': {
      flexShrink: '0',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
    verticalAlign: 'initial',
    '& .SolarRow-cells': {
      minWidth: '0',
    },
    '& .SolarRow-expandButton': {
      border: '0',
      margin: '0',
      font: 'inherit',
      color: 'inherit',
      cursor: 'pointer',
    },
    '& .SolarRow-expandButton:focus-visible': {
      outline: 'none',
      boxShadow: 'var(--solar-shadow-focus-default)',
    },
    '&.SolarRow-pressable': {
      cursor: 'pointer',
    },
  },
  root: {
    backgroundColor: 'transparent',
    borderColor: 'transparent',
    boxShadow: 'none',
    borderRadius: 'var(--solar-radius-container)',
    borderStyle: 'none',
    flexDirection: 'row',
    justifyContent: 'flex-start',
    alignItems: 'center',
    gap: 'var(--solar-inset-none)',
    paddingTop: 'var(--solar-inset-none)',
    paddingRight: 'var(--solar-inset-none)',
    paddingBottom: 'var(--solar-inset-none)',
    paddingLeft: 'var(--solar-inset-none)',
    width: '100%',
    '& .SolarRow-titleRowContent': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'row',
      justifyContent: 'flex-start',
      alignItems: 'center',
      gap: 'var(--solar-inset-none)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
      width: '100%',
      minWidth: 0,
      height: '100%',
      minHeight: 0,
    },
    '& .SolarRow--columnItem': {
      width: '100%',
      minWidth: 0,
      height: '100%',
      minHeight: 0,
    },
  },
  sizes: {},
  appearances: {
    'type=top': {
      borderRadius: 'var(--solar-radius-none)',
      height: '44px',
      '&.SolarRow-pressable:hover': {
        backgroundColor: 'var(--solar-color-surface-hover)',
      },
      '&.SolarRow-selected': {
        backgroundColor: 'var(--solar-color-surface-active)',
      },
    },
    'type=middle': {
      borderRadius: 'var(--solar-radius-none)',
      height: '44px',
      '&.SolarRow-pressable:hover': {
        backgroundColor: 'var(--solar-color-surface-hover)',
      },
      '&.SolarRow-selected': {
        backgroundColor: 'var(--solar-color-surface-active)',
      },
    },
    'type=bottom': {
      borderRadius: 'var(--solar-radius-none)',
      height: '44px',
      '&.SolarRow-pressable:hover': {
        backgroundColor: 'var(--solar-color-surface-hover)',
      },
      '&.SolarRow-selected': {
        backgroundColor: 'var(--solar-color-surface-active)',
      },
    },
    'type=non-expandable': {
      borderRadius: 'var(--solar-radius-none)',
      height: '44px',
      '&.SolarRow-pressable:hover': {
        backgroundColor: 'var(--solar-color-surface-hover)',
      },
      '&.SolarRow-selected': {
        backgroundColor: 'var(--solar-color-surface-active)',
      },
    },
  },
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarRowComposition = {
  root: {
    base: {
      present: true,
    },
  },
  checkBox: {
    base: {
      present: true,
      component: 'RowSelect',
      'variant.title-row': 'true',
    },
    appearance: {
      'type=non-expandable': {
        default: {
          'variant.title-row': 'false',
        },
        selected: {
          'variant.title-row': 'false',
        },
      },
      'type=top': {
        default: {
          'variant.title-row': 'false',
        },
        selected: {
          'variant.title-row': 'false',
        },
      },
      'type=middle': {
        default: {
          'variant.title-row': 'false',
        },
        selected: {
          'variant.title-row': 'false',
        },
      },
      'type=bottom': {
        default: {
          'variant.title-row': 'false',
        },
        selected: {
          'variant.title-row': 'false',
        },
      },
    },
  },
  expand: {
    base: {
      present: true,
      component: 'RowExpand',
      'variant.type': 'title-row',
    },
    appearance: {
      'type=non-expandable': {
        default: {
          'variant.type': 'collapsed',
        },
        selected: {
          'variant.type': 'collapsed',
        },
      },
      'type=top': {
        default: {
          'variant.type': 'collapsed',
        },
        selected: {
          'variant.type': 'collapsed',
        },
      },
      'type=middle': {
        default: {
          'variant.type': 'middle-row',
        },
        selected: {
          'variant.type': 'middle-row',
        },
      },
      'type=bottom': {
        default: {
          'variant.type': 'bottom-row',
        },
        selected: {
          'variant.type': 'bottom-row',
        },
      },
    },
  },
  titleRowContent: {
    base: {
      present: true,
    },
  },
  columnItem: {
    base: {
      present: true,
      component: 'Column Item',
      'variant.title': 'true',
      'variant.type': 'text',
    },
    appearance: {
      'type=non-expandable': {
        default: {
          'variant.title': 'false',
        },
        selected: {
          'variant.title': 'false',
        },
      },
      'type=top': {
        default: {
          'variant.title': 'false',
        },
        selected: {
          'variant.title': 'false',
        },
      },
      'type=middle': {
        default: {
          'variant.title': 'false',
        },
        selected: {
          'variant.title': 'false',
        },
      },
      'type=bottom': {
        default: {
          'variant.title': 'false',
        },
        selected: {
          'variant.title': 'false',
        },
      },
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarRowStyle(props: SolarRowProps = {}): Style {
  const p: Record<string, unknown> = { ...solarRowDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `type=${p.type}`;
  const s = solarRowStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  return merge(s.reset, s.root, s.appearances[key]);
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarRowParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarRowParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarRowCompose(
  props: SolarRowProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarRowDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `type=${p.type}`;
  const size = '';
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarRowComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}

/** Each layer's children, in Figma's order: the tree the shell draws (`drawChildren`). */
export const solarRowTree: Record<string, string[]> = {
  root: ['checkBox', 'expand', 'titleRowContent'],
  titleRowContent: ['columnItem'],
};

/** Each slot's layer, to the slot it is: its class is `SolarRow-<slot>`, public. */
export const solarRowSlots: Record<string, string> = {
  checkBox: 'checkBox',
  expand: 'expand',
  titleRowContent: 'titleRowContent',
};
