// SOLAR Segmented Control Item recipe for MUI. Generated by @bwp-web/codegen from spec/components/segmented control item.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export type SolarSegmentedControlItemSize = 'md' | 'sm';

export interface SolarSegmentedControlItemProps {
  selected?: boolean;
  size?: SolarSegmentedControlItemSize;
}

export const solarSegmentedControlItemDefaults = {
  selected: true,
  size: 'md',
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarSegmentedControlItemStyles = {
  reset: {
    display: 'inline-flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarSegmentedControlItem-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarSegmentedControlItem-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarSegmentedControlItem-glyph': {
      display: 'block',
      overflow: 'visible',
      flexShrink: '0',
    },
    '& .SolarSegmentedControlItem-drawnIcon': {
      flexShrink: '0',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
    cursor: 'pointer',
    '&': {
      position: 'relative',
    },
    '&::after': {
      content: '""',
      position: 'absolute',
      top: '50%',
      left: '50%',
      width: 'max(100%, var(--solar-size-target-min))',
      height: 'max(100%, var(--solar-size-target-min))',
      transform: 'translate(-50%, -50%)',
    },
    '& .SolarSegmentedControlItem-input': {
      position: 'absolute',
      opacity: '0',
      width: '1px',
      height: '1px',
      margin: '0',
      pointerEvents: 'none',
    },
    '& .SolarSegmentedControlItem-iconLeading > svg, & .SolarSegmentedControlItem-iconTrailing > svg':
      {
        display: 'block',
        width: '100%',
        height: '100%',
      },
  },
  root: {
    backgroundColor: 'var(--solar-color-surface-overlay)',
    borderColor: 'var(--solar-color-border-subtle)',
    boxShadow: 'var(--solar-shadow-control)',
    borderRadius: 'var(--solar-radius-control)',
    borderWidth: 'var(--solar-border-default)',
    borderStyle: 'solid',
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'center',
    gap: 'var(--solar-inset-xs)',
    paddingTop: 'var(--solar-inset-none)',
    paddingRight: 'var(--solar-inset-sm)',
    paddingBottom: 'var(--solar-inset-none)',
    paddingLeft: 'var(--solar-inset-sm)',
    height: '32px',
    '& .SolarSegmentedControlItem-iconLeading': {
      width: 'var(--solar-icon-sm)',
      minWidth: 'var(--solar-icon-sm)',
      height: 'var(--solar-icon-sm)',
      minHeight: 'var(--solar-icon-sm)',
      color: 'var(--solar-color-icon-primary)',
    },
    '& .SolarSegmentedControlItem--label': {
      color: 'var(--solar-color-text-primary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-label-md)',
      lineHeight: 'var(--solar-type-line-height-label-md)',
      letterSpacing: '-0.02em',
      textDecoration: 'none',
    },
    '& .SolarSegmentedControlItem-iconTrailing': {
      width: 'var(--solar-icon-sm)',
      minWidth: 'var(--solar-icon-sm)',
      height: 'var(--solar-icon-sm)',
      minHeight: 'var(--solar-icon-sm)',
      color: 'var(--solar-color-icon-primary)',
    },
  },
  sizes: {
    sm: {
      paddingRight: 'var(--solar-inset-xs)',
      paddingLeft: 'var(--solar-inset-xs)',
      height: '24px',
      '& .SolarSegmentedControlItem-iconLeading': {
        width: 'var(--solar-icon-xs)',
        minWidth: 'var(--solar-icon-xs)',
        height: 'var(--solar-icon-xs)',
        minHeight: 'var(--solar-icon-xs)',
      },
      '& .SolarSegmentedControlItem--label': {
        fontFamily: 'var(--solar-type-font-family-inter)',
        fontWeight: 'var(--solar-type-font-weight-500)',
        fontSize: 'var(--solar-type-size-label-sm)',
        lineHeight: 'var(--solar-type-line-height-label-sm)',
        letterSpacing: '-0.02em',
        textDecoration: 'none',
      },
      '& .SolarSegmentedControlItem-iconTrailing': {
        width: 'var(--solar-icon-xs)',
        minWidth: 'var(--solar-icon-xs)',
        height: 'var(--solar-icon-xs)',
        minHeight: 'var(--solar-icon-xs)',
      },
    },
  },
  appearances: {
    'selected=false': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      '&:hover': {
        backgroundColor: 'transparent',
        borderColor: 'transparent',
        boxShadow: 'none',
        '& .SolarSegmentedControlItem-iconLeading': {
          color: 'var(--solar-color-icon-primary)',
        },
        '& .SolarSegmentedControlItem--label': {
          color: 'var(--solar-color-text-primary)',
        },
        '& .SolarSegmentedControlItem-iconTrailing': {
          color: 'var(--solar-color-icon-primary)',
        },
      },
      '&:has(.SolarSegmentedControlItem-input:focus-visible)': {
        backgroundColor: 'transparent',
        borderColor: 'transparent',
        boxShadow: 'var(--solar-shadow-focus-default)',
        '& .SolarSegmentedControlItem-iconLeading': {
          color: 'var(--solar-color-icon-secondary)',
        },
        '& .SolarSegmentedControlItem--label': {
          color: 'var(--solar-color-text-secondary)',
        },
        '& .SolarSegmentedControlItem-iconTrailing': {
          color: 'var(--solar-color-icon-secondary)',
        },
      },
      '& .SolarSegmentedControlItem-iconLeading': {
        color: 'var(--solar-color-icon-secondary)',
      },
      '& .SolarSegmentedControlItem--label': {
        color: 'var(--solar-color-text-secondary)',
      },
      '& .SolarSegmentedControlItem-iconTrailing': {
        color: 'var(--solar-color-icon-secondary)',
      },
    },
    'selected=true': {
      '&:hover': {
        borderColor: 'var(--solar-color-border-medium)',
      },
      '&:has(.SolarSegmentedControlItem-input:focus-visible)': {
        boxShadow: 'var(--solar-shadow-focus-default)',
      },
    },
  },
  combined: {
    md: {
      'selected=true': {
        '&:has(.SolarSegmentedControlItem-input:focus-visible)': {
          borderColor: 'var(--solar-color-border-subtle)',
        },
      },
    },
    sm: {
      'selected=true': {
        '&:has(.SolarSegmentedControlItem-input:focus-visible)': {
          borderColor: 'var(--solar-color-border-subtle)',
        },
      },
    },
  },
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarSegmentedControlItemComposition = {
  root: {
    base: {
      present: true,
    },
  },
  iconLeading: {
    base: {
      present: true,
      component: 'Icon/None',
      'variant.solid': 'false',
    },
  },
  label: {
    base: {
      present: true,
    },
  },
  iconTrailing: {
    base: {
      present: true,
      component: 'Icon/None',
      'variant.solid': 'false',
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarSegmentedControlItemStyle(
  props: SolarSegmentedControlItemProps = {},
): Style {
  const p: Record<string, unknown> = { ...solarSegmentedControlItemDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `selected=${p.selected}`;
  const s = solarSegmentedControlItemStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  const size = p.size as string;
  return merge(
    s.reset,
    s.root,
    s.sizes[size],
    s.appearances[key],
    s.combined[size]?.[key],
  );
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarSegmentedControlItemParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarSegmentedControlItemParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarSegmentedControlItemCompose(
  props: SolarSegmentedControlItemProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarSegmentedControlItemDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `selected=${p.selected}`;
  const size = p.size as string;
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarSegmentedControlItemComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}

/** Each layer's children, in Figma's order: the tree the shell draws (`drawChildren`). */
export const solarSegmentedControlItemTree: Record<string, string[]> = {
  root: ['iconLeading', 'label', 'iconTrailing'],
};

/** Each slot's layer, to the slot it is: its class is `SolarSegmentedControlItem-<slot>`, public. */
export const solarSegmentedControlItemSlots: Record<string, string> = {
  iconLeading: 'iconLeading',
  iconTrailing: 'iconTrailing',
};
