// SOLAR Nav Item recipe for MUI. Generated by @bwp-web/codegen from spec/components/nav item.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export interface SolarNavItemProps {
  selected?: boolean;
  expanded?: boolean;
}

export const solarNavItemDefaults = {
  selected: false,
  expanded: false,
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarNavItemStyles = {
  reset: {
    display: 'flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarNavItem-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarNavItem-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarNavItem-glyph': {
      display: 'block',
      overflow: 'visible',
      flexShrink: '0',
    },
    '& .SolarNavItem-drawnIcon': {
      flexShrink: '0',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
    textDecoration: 'none',
    '& .SolarNavItem-iconOutline': {
      flexShrink: '0',
    },
    '& .SolarNavItem-iconOutline > svg': {
      display: 'block',
      width: '100%',
      height: '100%',
    },
    '&': {
      position: 'relative',
    },
    '&::after': {
      content: '""',
      position: 'absolute',
      top: '50%',
      left: '50%',
      width: 'max(100%, var(--solar-size-target-min))',
      height: 'max(100%, var(--solar-size-target-min))',
      transform: 'translate(-50%, -50%)',
    },
  },
  root: {
    backgroundColor: 'transparent',
    borderColor: 'transparent',
    boxShadow: 'none',
    borderRadius: 'var(--solar-radius-control)',
    borderStyle: 'none',
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'center',
    gap: 'var(--solar-inset-none)',
    paddingTop: 'var(--solar-inset-none)',
    paddingRight: 'var(--solar-inset-none)',
    paddingBottom: 'var(--solar-inset-none)',
    paddingLeft: 'var(--solar-inset-none)',
    width: '40px',
    height: 'var(--solar-size-control-md)',
    '& .SolarNavItem-iconOutline': {
      width: 'var(--solar-icon-md)',
      height: 'var(--solar-icon-md)',
      color: 'var(--solar-color-icon-secondary)',
    },
    '& .SolarNavItem-label': {
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-label-md)',
      lineHeight: 'var(--solar-type-line-height-label-md)',
      letterSpacing: '-0.02em',
      textDecoration: 'none',
      width: '100%',
    },
  },
  sizes: {},
  appearances: {
    'selected=false, expanded=false': {
      '&:hover': {
        backgroundColor: 'var(--solar-color-surface-hover)',
        '& .SolarNavItem-iconOutline': {
          color: 'var(--solar-color-icon-primary)',
        },
      },
      '&.Mui-focusVisible': {
        boxShadow: 'var(--solar-shadow-focus-default)',
        backgroundColor: 'transparent',
        '& .SolarNavItem-iconOutline': {
          color: 'var(--solar-color-icon-secondary)',
        },
      },
    },
    'selected=false, expanded=true': {
      gap: 'var(--solar-inset-sm)',
      paddingRight: 'var(--solar-inset-sm)',
      paddingLeft: 'var(--solar-inset-sm)',
      width: '100%',
      '&:hover': {
        backgroundColor: 'var(--solar-color-surface-hover)',
        '& .SolarNavItem-iconOutline': {
          color: 'var(--solar-color-icon-primary)',
        },
        '& .SolarNavItem-label': {
          color: 'var(--solar-color-text-primary)',
        },
      },
      '&.Mui-focusVisible': {
        boxShadow: 'var(--solar-shadow-focus-default)',
        backgroundColor: 'transparent',
        '& .SolarNavItem-iconOutline': {
          color: 'var(--solar-color-icon-secondary)',
        },
        '& .SolarNavItem-label': {
          color: 'var(--solar-color-text-secondary)',
        },
      },
      '& .SolarNavItem-label': {
        color: 'var(--solar-color-text-secondary)',
      },
    },
    'selected=true, expanded=false': {
      backgroundColor: 'var(--solar-color-surface-active)',
      '&:hover': {
        backgroundColor: 'var(--solar-color-surface-active)',
        '& .SolarNavItem-iconOutline': {
          color: 'var(--solar-color-icon-primary)',
        },
      },
      '&.Mui-focusVisible': {
        backgroundColor: 'var(--solar-color-surface-active)',
        boxShadow: 'var(--solar-shadow-focus-default)',
        '& .SolarNavItem-iconOutline': {
          color: 'var(--solar-color-icon-primary)',
        },
      },
      '& .SolarNavItem-iconOutline': {
        color: 'var(--solar-color-icon-primary)',
      },
    },
    'selected=true, expanded=true': {
      backgroundColor: 'var(--solar-color-surface-active)',
      gap: 'var(--solar-inset-sm)',
      paddingRight: 'var(--solar-inset-sm)',
      paddingLeft: 'var(--solar-inset-sm)',
      width: '100%',
      '&:hover': {
        backgroundColor: 'var(--solar-color-surface-active)',
        '& .SolarNavItem-iconOutline': {
          color: 'var(--solar-color-icon-primary)',
        },
        '& .SolarNavItem-label': {
          color: 'var(--solar-color-text-primary)',
        },
      },
      '&.Mui-focusVisible': {
        backgroundColor: 'var(--solar-color-surface-active)',
        boxShadow: 'var(--solar-shadow-focus-default)',
        '& .SolarNavItem-iconOutline': {
          color: 'var(--solar-color-icon-primary)',
        },
        '& .SolarNavItem-label': {
          color: 'var(--solar-color-text-primary)',
        },
      },
      '& .SolarNavItem-iconOutline': {
        color: 'var(--solar-color-icon-primary)',
      },
      '& .SolarNavItem-label': {
        color: 'var(--solar-color-text-primary)',
      },
    },
  },
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarNavItemComposition = {
  root: {
    base: {
      present: true,
    },
  },
  iconOutline: {
    base: {
      present: true,
      component: 'Icon/Home',
      'variant.solid': 'false',
    },
    appearance: {
      'selected=true, expanded=false': {
        default: {
          'variant.solid': 'true',
        },
        hover: {
          'variant.solid': 'true',
        },
        focus: {
          'variant.solid': 'true',
        },
      },
      'selected=true, expanded=true': {
        default: {
          'variant.solid': 'true',
        },
        hover: {
          'variant.solid': 'true',
        },
        focus: {
          'variant.solid': 'true',
        },
      },
    },
  },
  label: {
    base: {
      present: false,
    },
    appearance: {
      'selected=false, expanded=true': {
        default: {
          present: true,
        },
        hover: {
          present: true,
        },
        focus: {
          present: true,
        },
      },
      'selected=true, expanded=true': {
        default: {
          present: true,
        },
        hover: {
          present: true,
        },
        focus: {
          present: true,
        },
      },
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarNavItemStyle(props: SolarNavItemProps = {}): Style {
  const p: Record<string, unknown> = { ...solarNavItemDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `selected=${p.selected}, expanded=${p.expanded}`;
  const s = solarNavItemStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  return merge(s.reset, s.root, s.appearances[key]);
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarNavItemParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarNavItemParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarNavItemCompose(
  props: SolarNavItemProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarNavItemDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `selected=${p.selected}, expanded=${p.expanded}`;
  const size = '';
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarNavItemComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}

/** Each layer's children, in Figma's order: the tree the shell draws (`drawChildren`). */
export const solarNavItemTree: Record<string, string[]> = {
  root: ['iconOutline', 'label'],
};

/** Each slot's layer, to the slot it is: its class is `SolarNavItem-<slot>`, public. */
export const solarNavItemSlots: Record<string, string> = {
  iconOutline: 'iconOutline',
  label: 'label',
};
