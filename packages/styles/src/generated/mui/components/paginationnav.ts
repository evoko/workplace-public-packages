// SOLAR PaginationNav recipe for MUI. Generated by @bwp-web/codegen from spec/components/paginationnav.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export type SolarPaginationNavDirection = 'previous' | 'next';

export interface SolarPaginationNavProps {
  direction?: SolarPaginationNavDirection;
  disabled?: boolean;
}

export const solarPaginationNavDefaults = {
  direction: 'previous',
  disabled: false,
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarPaginationNavStyles = {
  reset: {
    display: 'flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarPaginationNav-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarPaginationNav-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarPaginationNav-glyph': {
      display: 'block',
      overflow: 'visible',
      flexShrink: '0',
    },
    '& .SolarPaginationNav-drawnIcon': {
      flexShrink: '0',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
    '& .SolarPaginationNav--icon > svg': {
      display: 'block',
      width: '100%',
      height: '100%',
    },
    '&:dir(rtl) .SolarPaginationNav--icon': {
      transform: 'scaleX(-1)',
    },
  },
  root: {
    backgroundColor: 'transparent',
    borderColor: 'transparent',
    boxShadow: 'none',
    borderRadius: 'var(--solar-radius-container)',
    borderStyle: 'none',
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'center',
    gap: 'var(--solar-inset-none)',
    paddingTop: 'var(--solar-inset-none)',
    paddingRight: 'var(--solar-inset-none)',
    paddingBottom: 'var(--solar-inset-none)',
    paddingLeft: 'var(--solar-inset-none)',
    width: '24px',
    height: '24px',
    '& .SolarPaginationNav--icon': {
      width: 'var(--solar-icon-sm)',
      minWidth: 'var(--solar-icon-sm)',
      height: 'var(--solar-icon-sm)',
      minHeight: 'var(--solar-icon-sm)',
      color: 'var(--solar-color-icon-primary)',
    },
  },
  sizes: {},
  appearances: {
    'direction=previous': {
      '&:hover': {
        backgroundColor: 'var(--solar-color-surface-hover)',
      },
      '&:active': {
        backgroundColor: 'var(--solar-color-surface-active)',
      },
      '&.Mui-focusVisible': {
        boxShadow: 'var(--solar-shadow-focus-default)',
        backgroundColor: 'transparent',
      },
      '&.Mui-disabled': {
        '& .SolarPaginationNav--icon': {
          color: 'var(--solar-color-icon-disabled)',
        },
      },
    },
    'direction=next': {
      '&:hover': {
        backgroundColor: 'var(--solar-color-surface-hover)',
      },
      '&:active': {
        backgroundColor: 'var(--solar-color-surface-active)',
      },
      '&.Mui-focusVisible': {
        boxShadow: 'var(--solar-shadow-focus-default)',
        backgroundColor: 'transparent',
      },
      '&.Mui-disabled': {
        '& .SolarPaginationNav--icon': {
          color: 'var(--solar-color-icon-disabled)',
        },
      },
    },
  },
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarPaginationNavComposition = {
  root: {
    base: {
      present: true,
    },
  },
  icon: {
    base: {
      present: true,
      component: 'Icon/ChevronLeft',
      'variant.solid': 'false',
    },
    appearance: {
      'direction=next': {
        default: {
          component: 'Icon/ChevronRight',
        },
      },
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarPaginationNavStyle(
  props: SolarPaginationNavProps = {},
): Style {
  const p: Record<string, unknown> = { ...solarPaginationNavDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `direction=${p.direction}`;
  const s = solarPaginationNavStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  return merge(s.reset, s.root, s.appearances[key]);
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarPaginationNavParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarPaginationNavParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarPaginationNavCompose(
  props: SolarPaginationNavProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarPaginationNavDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `direction=${p.direction}`;
  const size = '';
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarPaginationNavComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}

/** Each layer's children, in Figma's order: the tree the shell draws (`drawChildren`). */
export const solarPaginationNavTree: Record<string, string[]> = {
  root: ['icon'],
};

/** Each slot's layer, to the slot it is: its class is `SolarPaginationNav-<slot>`, public. */
export const solarPaginationNavSlots: Record<string, string> = {};
