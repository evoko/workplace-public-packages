// SOLAR Button Group recipe for MUI. Generated by @bwp-web/codegen from spec/components/button group.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export type SolarButtonGroupOrientation = 'horizontal' | 'vertical';
export type SolarButtonGroupType = 'regular' | 'full-width';

export interface SolarButtonGroupProps {
  orientation?: SolarButtonGroupOrientation;
  type?: SolarButtonGroupType;
}

export const solarButtonGroupDefaults = {
  orientation: 'horizontal',
  type: 'regular',
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarButtonGroupStyles = {
  reset: {
    display: 'flex',
  },
  root: {
    backgroundColor: 'transparent',
    borderColor: 'transparent',
    boxShadow: 'none',
    borderRadius: 'var(--solar-radius-none)',
    flexDirection: 'row',
    justifyContent: 'flex-start',
    alignItems: 'center',
    gap: 'var(--solar-inset-xs)',
    paddingTop: 'var(--solar-inset-sm)',
    paddingRight: 'var(--solar-inset-sm)',
    paddingBottom: 'var(--solar-inset-sm)',
    paddingLeft: 'var(--solar-inset-sm)',
    width: '100%',
    borderTopStyle: 'none',
    borderRightStyle: 'none',
    borderBottomStyle: 'none',
    borderLeftStyle: 'none',
    '& > *': {
      width: '100%',
      minWidth: 0,
    },
  },
  sizes: {},
  appearances: {
    'orientation=horizontal, type=full-width': {
      borderColor: 'var(--solar-color-border-subtle)',
      gap: 'var(--solar-inset-none)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
      borderTopWidth: 'var(--solar-border-default)',
      borderTopStyle: 'solid',
      '& > *': {
        width: '100%',
        minWidth: 0,
      },
    },
    'orientation=vertical, type=regular': {
      flexDirection: 'column',
      justifyContent: 'center',
      alignItems: 'flex-start',
      '& > *': {
        width: '100%',
        minWidth: 0,
      },
    },
  },
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarButtonGroupComposition = {
  root: {
    base: {
      present: true,
    },
  },
  tertiaryCTA: {
    base: {
      present: false,
      component: 'Button',
      'variant.size': 'md',
      'variant.prio': 'tertiary',
      'variant.state': 'default',
      'variant.danger': 'false',
    },
    appearance: {
      'orientation=vertical, type=regular': {
        default: {
          present: true,
          'variant.prio': 'primary',
        },
      },
      'orientation=horizontal, type=full-width': {
        default: {
          present: true,
          'variant.prio': 'secondary',
        },
      },
    },
  },
  secondaryCTA: {
    base: {
      present: true,
      component: 'Button',
      'variant.size': 'md',
      'variant.prio': 'secondary',
      'variant.state': 'default',
      'variant.danger': 'false',
    },
    appearance: {
      'orientation=horizontal, type=full-width': {
        default: {
          'variant.prio': 'primary',
        },
      },
    },
  },
  button3: {
    base: {
      present: true,
      component: 'Button',
      'variant.size': 'md',
      'variant.prio': 'primary',
      'variant.state': 'default',
      'variant.danger': 'false',
    },
    appearance: {
      'orientation=vertical, type=regular': {
        default: {
          present: false,
          'variant.prio': 'tertiary',
        },
      },
      'orientation=horizontal, type=full-width': {
        default: {
          present: false,
        },
      },
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarButtonGroupStyle(
  props: SolarButtonGroupProps = {},
): Style {
  const p: Record<string, unknown> = { ...solarButtonGroupDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `orientation=${p.orientation}, type=${p.type}`;
  const s = solarButtonGroupStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  return merge(s.reset, s.root, s.appearances[key]);
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarButtonGroupParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarButtonGroupParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarButtonGroupCompose(
  props: SolarButtonGroupProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarButtonGroupDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `orientation=${p.orientation}, type=${p.type}`;
  const size = '';
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarButtonGroupComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}

/** Each layer's children, in Figma's order: the tree the shell draws (`drawChildren`). */
export const solarButtonGroupTree: Record<string, string[]> = {
  root: ['tertiaryCTA', 'secondaryCTA', 'button3'],
};

/** Each slot's layer, to the slot it is: its class is `SolarButtonGroup-<slot>`, public. */
export const solarButtonGroupSlots: Record<string, string> = {
  tertiaryCTA: 'tertiaryCTA',
  secondaryCTA: 'secondaryCTA',
};
