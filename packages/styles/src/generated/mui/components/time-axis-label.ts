// SOLAR Time Axis Label recipe for MUI. Generated by @bwp-web/codegen from spec/components/time axis label.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export type SolarTimeAxisLabelEmphasis = 'default' | 'now';
export type SolarTimeAxisLabelDensity = 'comfortable' | 'compact';

export interface SolarTimeAxisLabelProps {
  emphasis?: SolarTimeAxisLabelEmphasis;
  density?: SolarTimeAxisLabelDensity;
}

export const solarTimeAxisLabelDefaults = {
  emphasis: 'default',
  density: 'comfortable',
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarTimeAxisLabelStyles = {
  reset: {
    display: 'inline-flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarTimeAxisLabel-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarTimeAxisLabel-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarTimeAxisLabel-glyph': {
      display: 'block',
      overflow: 'visible',
      flexShrink: '0',
    },
    '& .SolarTimeAxisLabel-drawnIcon': {
      flexShrink: '0',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
  },
  root: {
    backgroundColor: 'var(--solar-color-surface-base)',
    borderColor: 'transparent',
    boxShadow: 'none',
    borderRadius: 'var(--solar-radius-none)',
    borderStyle: 'none',
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'flex-start',
    gap: 'var(--solar-inset-none)',
    paddingTop: 'var(--solar-inset-none)',
    paddingRight: 'var(--solar-inset-sm)',
    paddingBottom: 'var(--solar-inset-none)',
    paddingLeft: 'var(--solar-inset-sm)',
    width: '56px',
    height: '48px',
    '& .SolarTimeAxisLabel-label': {
      color: 'var(--solar-color-text-tertiary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-body-xs)',
      lineHeight: 'var(--solar-type-line-height-body-xs)',
      letterSpacing: '-0.02em',
      textDecoration: 'none',
    },
  },
  sizes: {},
  appearances: {
    'emphasis=default, density=compact': {
      height: '32px',
    },
    'emphasis=now, density=comfortable': {
      '& .SolarTimeAxisLabel-label': {
        color: 'var(--solar-color-text-feedback-info)',
        fontFamily: 'var(--solar-type-font-family-inter)',
        fontWeight: 'var(--solar-type-font-weight-600)',
        fontSize: 'var(--solar-type-size-body-xs)',
        lineHeight: 'var(--solar-type-line-height-body-xs)',
        letterSpacing: '-0.02em',
        textDecoration: 'none',
      },
    },
    'emphasis=now, density=compact': {
      height: '32px',
      '& .SolarTimeAxisLabel-label': {
        color: 'var(--solar-color-text-feedback-info)',
        fontFamily: 'var(--solar-type-font-family-inter)',
        fontWeight: 'var(--solar-type-font-weight-600)',
        fontSize: 'var(--solar-type-size-body-xs)',
        lineHeight: 'var(--solar-type-line-height-body-xs)',
        letterSpacing: '-0.02em',
        textDecoration: 'none',
      },
    },
  },
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarTimeAxisLabelComposition = {
  root: {
    base: {
      present: true,
    },
  },
  label: {
    base: {
      present: true,
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarTimeAxisLabelStyle(
  props: SolarTimeAxisLabelProps = {},
): Style {
  const p: Record<string, unknown> = { ...solarTimeAxisLabelDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `emphasis=${p.emphasis}, density=${p.density}`;
  const s = solarTimeAxisLabelStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  return merge(s.reset, s.root, s.appearances[key]);
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarTimeAxisLabelParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarTimeAxisLabelParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarTimeAxisLabelCompose(
  props: SolarTimeAxisLabelProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarTimeAxisLabelDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `emphasis=${p.emphasis}, density=${p.density}`;
  const size = '';
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarTimeAxisLabelComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}

/** Each layer's children, in Figma's order: the tree the shell draws (`drawChildren`). */
export const solarTimeAxisLabelTree: Record<string, string[]> = {
  root: ['label'],
};

/** Each slot's layer, to the slot it is: its class is `SolarTimeAxisLabel-<slot>`, public. */
export const solarTimeAxisLabelSlots: Record<string, string> = {
  label: 'label',
};
