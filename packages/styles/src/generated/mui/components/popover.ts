// SOLAR Popover recipe for MUI. Generated by @bwp-web/codegen from spec/components/popover.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export type SolarPopoverPlacement = 'top' | 'bottom' | 'left' | 'right';
export type SolarPopoverSize = 'sm' | 'md';

export interface SolarPopoverProps {
  placement?: SolarPopoverPlacement;
  size?: SolarPopoverSize;
}

export const solarPopoverDefaults = {
  placement: 'top',
  size: 'md',
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarPopoverStyles = {
  reset: {
    display: 'inline-flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarPopover-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarPopover-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarPopover-glyph': {
      display: 'block',
      overflow: 'visible',
      flexShrink: '0',
    },
    '& .SolarPopover-drawnIcon': {
      flexShrink: '0',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
    '& .SolarPopover--title, & .SolarPopover--body': {
      whiteSpace: 'normal',
    },
    '& .SolarPopover--tip': {
      flexShrink: 0,
    },
  },
  root: {
    backgroundColor: 'transparent',
    borderColor: 'transparent',
    boxShadow: 'none',
    borderRadius: 'var(--solar-radius-none)',
    borderStyle: 'none',
    flexDirection: 'column',
    justifyContent: 'flex-start',
    alignItems: 'flex-start',
    gap: 'var(--solar-stack-none)',
    paddingTop: 'var(--solar-inset-none)',
    paddingRight: 'var(--solar-inset-none)',
    paddingBottom: 'var(--solar-inset-none)',
    paddingLeft: 'var(--solar-inset-none)',
    '& .SolarPopover--content': {
      backgroundColor: 'var(--solar-color-surface-raised)',
      borderColor: 'transparent',
      boxShadow: 'var(--solar-shadow-overlay)',
      borderTopLeftRadius: 'var(--solar-radius-container)',
      borderTopRightRadius: 'var(--solar-radius-container)',
      borderBottomRightRadius: 'var(--solar-radius-container)',
      borderBottomLeftRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'column',
      justifyContent: 'flex-start',
      alignItems: 'flex-start',
      gap: 'var(--solar-stack-sm)',
      paddingTop: 'var(--solar-inset-md)',
      paddingRight: 'var(--solar-inset-md)',
      paddingBottom: 'var(--solar-inset-md)',
      paddingLeft: 'var(--solar-inset-md)',
      width: '320px',
      minWidth: '320px',
      order: 0,
    },
    '& .SolarPopover--title': {
      color: 'var(--solar-color-text-primary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-body-md)',
      lineHeight: 'var(--solar-type-line-height-body-md)',
      letterSpacing: '-0.02286em',
      textDecoration: 'none',
      width: '100%',
      minWidth: 0,
    },
    '& .SolarPopover--body': {
      color: 'var(--solar-color-text-secondary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-400)',
      fontSize: 'var(--solar-type-size-body-lg)',
      lineHeight: 'var(--solar-type-line-height-body-lg)',
      letterSpacing: '-0.02em',
      textDecoration: 'none',
      width: '100%',
      minWidth: 0,
    },
    '& .SolarPopover--tip': {
      fill: 'var(--solar-color-surface-raised)',
      stroke: 'none',
      '& .SolarGlyph-stroke': {
        fill: 'none',
      },
      strokeWidth: '0',
      width: '10px',
      minWidth: '10px',
      height: '10px',
      minHeight: '10px',
      order: 1,
    },
  },
  sizes: {
    sm: {
      '& .SolarPopover--content': {
        gap: 'var(--solar-stack-xs)',
        paddingTop: 'var(--solar-inset-sm)',
        paddingRight: 'var(--solar-inset-sm)',
        paddingBottom: 'var(--solar-inset-sm)',
        paddingLeft: 'var(--solar-inset-sm)',
        width: '240px',
        minWidth: '240px',
      },
      '& .SolarPopover--title': {
        fontFamily: 'var(--solar-type-font-family-inter)',
        fontWeight: 'var(--solar-type-font-weight-500)',
        fontSize: 'var(--solar-type-size-body-sm)',
        lineHeight: 'var(--solar-type-line-height-body-sm)',
        letterSpacing: '-0.02em',
        textDecoration: 'none',
      },
      '& .SolarPopover--body': {
        fontFamily: 'var(--solar-type-font-family-inter)',
        fontWeight: 'var(--solar-type-font-weight-400)',
        fontSize: 'var(--solar-type-size-body-sm)',
        lineHeight: 'var(--solar-type-line-height-body-sm)',
        letterSpacing: '-0.02em',
        textDecoration: 'none',
      },
    },
  },
  appearances: {
    'placement=bottom': {
      '& .SolarPopover--content': {
        borderTopLeftRadius: 'var(--solar-radius-none)',
        borderBottomLeftRadius: 'var(--solar-radius-container)',
        order: 1,
      },
      '& .SolarPopover--tip': {
        order: 0,
      },
    },
    'placement=left': {
      flexDirection: 'row',
      '& .SolarPopover--content': {
        borderTopRightRadius: 'var(--solar-radius-none)',
        borderBottomLeftRadius: 'var(--solar-radius-container)',
      },
    },
    'placement=right': {
      flexDirection: 'row',
      '& .SolarPopover--content': {
        borderTopLeftRadius: 'var(--solar-radius-none)',
        borderBottomLeftRadius: 'var(--solar-radius-container)',
        order: 1,
      },
      '& .SolarPopover--tip': {
        order: 0,
      },
    },
  },
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarPopoverComposition = {
  root: {
    base: {
      present: true,
    },
  },
  content: {
    base: {
      present: true,
    },
  },
  title: {
    base: {
      present: true,
    },
  },
  body: {
    base: {
      present: true,
    },
  },
  tip: {
    base: {
      present: true,
      glyph: {
        width: 10,
        height: 10,
        fill: [
          {
            d: 'M0 0L10 0L0 10L0 0Z',
            evenOdd: false,
          },
        ],
        stroke: [],
      },
    },
    appearance: {
      'placement=bottom': {
        default: {
          glyph: {
            width: 10,
            height: 10,
            fill: [
              {
                d: 'M0 10L10 10L0 0L0 10Z',
                evenOdd: false,
              },
            ],
            stroke: [],
          },
        },
      },
      'placement=left': {
        default: {
          glyph: {
            width: 10,
            height: 10,
            fill: [
              {
                d: 'M0 0L0 10L10 0L0 0Z',
                evenOdd: false,
              },
            ],
            stroke: [],
          },
        },
      },
      'placement=right': {
        default: {
          glyph: {
            width: 10,
            height: 10,
            fill: [
              {
                d: 'M10 0L10 10L0 0L10 0Z',
                evenOdd: false,
              },
            ],
            stroke: [],
          },
        },
      },
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarPopoverStyle(props: SolarPopoverProps = {}): Style {
  const p: Record<string, unknown> = { ...solarPopoverDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `placement=${p.placement}`;
  const s = solarPopoverStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  const size = p.size as string;
  return merge(
    s.reset,
    s.root,
    s.sizes[size],
    s.appearances[key],
    s.combined[size]?.[key],
  );
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarPopoverParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarPopoverParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarPopoverCompose(
  props: SolarPopoverProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarPopoverDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `placement=${p.placement}`;
  const size = p.size as string;
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarPopoverComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}

/** Each layer's children, in Figma's order: the tree the shell draws (`drawChildren`). */
export const solarPopoverTree: Record<string, string[]> = {
  root: ['content', 'tip'],
  content: ['title', 'body'],
};

/** Each slot's layer, to the slot it is: its class is `SolarPopover-<slot>`, public. */
export const solarPopoverSlots: Record<string, string> = {};
