// SOLAR Banner recipe for MUI. Generated by @bwp-web/codegen from spec/components/banner.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export type SolarBannerType =
  'neutral' | 'info' | 'success' | 'warning' | 'danger';

export interface SolarBannerProps {
  type?: SolarBannerType;
}

export const solarBannerDefaults = {
  type: 'neutral',
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarBannerStyles = {
  reset: {
    display: 'inline-flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarBanner-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarBanner-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarBanner-glyph': {
      display: 'block',
      overflow: 'visible',
      flexShrink: '0',
    },
    '& .SolarBanner-drawnIcon': {
      flexShrink: '0',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
    '& .SolarBanner-description': {
      minWidth: '0',
      overflow: 'hidden',
      textOverflow: 'ellipsis',
    },
    '& button.SolarBanner-action, & button.SolarBanner-close': {
      appearance: 'none',
      border: '0',
      padding: '0',
      margin: '0',
      background: 'none',
      cursor: 'pointer',
    },
    '& button.SolarBanner-action': {
      position: 'relative',
    },
    '& button.SolarBanner-action::after': {
      content: '""',
      position: 'absolute',
      top: '50%',
      left: '50%',
      width: 'max(100%, 44px)',
      height: 'max(100%, 44px)',
      transform: 'translate(-50%, -50%)',
    },
    '& button.SolarBanner-close': {
      position: 'relative',
    },
    '& button.SolarBanner-close::after': {
      content: '""',
      position: 'absolute',
      top: '50%',
      left: '50%',
      width: 'max(100%, 44px)',
      height: 'max(100%, 44px)',
      transform: 'translate(-50%, -50%)',
    },
    '& .SolarBanner-close > svg': {
      display: 'block',
      width: '100%',
      height: '100%',
    },
  },
  root: {
    backgroundColor: 'var(--solar-color-surface-feedback-neutral-medium)',
    borderColor: 'transparent',
    boxShadow: 'none',
    borderRadius: 'var(--solar-radius-none)',
    borderStyle: 'none',
    flexDirection: 'row',
    justifyContent: 'flex-start',
    alignItems: 'center',
    gap: 'var(--solar-inset-sm)',
    paddingTop: 'var(--solar-inset-sm)',
    paddingRight: 'var(--solar-inset-sm)',
    paddingBottom: 'var(--solar-inset-sm)',
    paddingLeft: 'var(--solar-inset-sm)',
    width: '100%',
    height: '44px',
    '& .SolarBanner--iconInfo': {
      width: 'var(--solar-icon-md)',
      height: 'var(--solar-icon-md)',
      color: 'var(--solar-color-icon-primary)',
    },
    '& .SolarBanner-description': {
      color: 'var(--solar-color-text-primary)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-body-md)',
      lineHeight: 'var(--solar-type-line-height-body-md)',
      letterSpacing: '-0.02286em',
      textDecoration: 'none',
      width: '100%',
    },
    '& .SolarBanner--actionGroup': {
      backgroundColor: 'transparent',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      flexDirection: 'row',
      justifyContent: 'flex-start',
      alignItems: 'center',
      gap: 'var(--solar-inset-xs)',
      paddingTop: 'var(--solar-inset-none)',
      paddingRight: 'var(--solar-inset-none)',
      paddingBottom: 'var(--solar-inset-none)',
      paddingLeft: 'var(--solar-inset-none)',
    },
    '& .SolarBanner-action': {
      color: 'var(--solar-color-action-secondary-text-default)',
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-body-sm)',
      lineHeight: 'var(--solar-type-line-height-body-sm)',
      letterSpacing: '-0.02em',
      textDecoration: 'underline',
    },
    '& .SolarBanner-close': {
      width: 'var(--solar-icon-md)',
      height: 'var(--solar-icon-md)',
      color: 'var(--solar-color-icon-primary)',
    },
    '& .SolarBanner--iconSuccess': {
      width: 'var(--solar-icon-md)',
      height: 'var(--solar-icon-md)',
    },
    '& .SolarBanner--iconWarning': {
      width: 'var(--solar-icon-md)',
      height: 'var(--solar-icon-md)',
    },
    '& .SolarBanner--iconDanger': {
      width: 'var(--solar-icon-md)',
      height: 'var(--solar-icon-md)',
    },
  },
  sizes: {},
  appearances: {
    'type=info': {
      backgroundColor: 'var(--solar-color-surface-feedback-info-medium)',
    },
    'type=success': {
      backgroundColor: 'var(--solar-color-surface-feedback-success-medium)',
      '& .SolarBanner--iconSuccess': {
        color: 'var(--solar-color-icon-primary)',
      },
    },
    'type=warning': {
      backgroundColor: 'var(--solar-color-surface-feedback-warning-medium)',
      '& .SolarBanner--iconWarning': {
        color: 'var(--solar-color-icon-primary)',
      },
    },
    'type=danger': {
      backgroundColor: 'var(--solar-color-surface-feedback-danger-medium)',
      '& .SolarBanner--iconDanger': {
        color: 'var(--solar-color-icon-primary)',
      },
    },
  },
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarBannerComposition = {
  root: {
    base: {
      present: true,
    },
  },
  iconInfo: {
    base: {
      present: true,
      component: 'Icon/Info',
      'variant.solid': 'false',
    },
    appearance: {
      'type=success': {
        default: {
          present: false,
        },
      },
      'type=warning': {
        default: {
          present: false,
        },
      },
      'type=danger': {
        default: {
          present: false,
        },
      },
    },
  },
  description: {
    base: {
      present: true,
    },
  },
  actionGroup: {
    base: {
      present: true,
    },
  },
  primaryButton: {
    base: {
      present: false,
      component: 'Button',
      'variant.size': 'sm',
      'variant.prio': 'primary',
      'variant.state': 'default',
      'variant.danger': 'false',
    },
  },
  secondaryButton: {
    base: {
      present: false,
      component: 'Button',
      'variant.size': 'sm',
      'variant.prio': 'secondary',
      'variant.state': 'default',
      'variant.danger': 'false',
    },
  },
  action: {
    base: {
      present: true,
    },
  },
  close: {
    base: {
      present: true,
      component: 'Icon/Close',
      'variant.solid': 'false',
    },
  },
  iconSuccess: {
    base: {
      present: false,
      component: 'Icon/Success',
    },
    appearance: {
      'type=success': {
        default: {
          present: true,
          'variant.solid': 'false',
        },
      },
    },
  },
  iconWarning: {
    base: {
      present: false,
      component: 'Icon/Warning',
    },
    appearance: {
      'type=warning': {
        default: {
          present: true,
          'variant.solid': 'false',
        },
      },
    },
  },
  iconDanger: {
    base: {
      present: false,
      component: 'Icon/Danger',
    },
    appearance: {
      'type=danger': {
        default: {
          present: true,
          'variant.solid': 'false',
        },
      },
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarBannerStyle(props: SolarBannerProps = {}): Style {
  const p: Record<string, unknown> = { ...solarBannerDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `type=${p.type}`;
  const s = solarBannerStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  return merge(s.reset, s.root, s.appearances[key]);
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarBannerParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarBannerParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarBannerCompose(
  props: SolarBannerProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarBannerDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `type=${p.type}`;
  const size = '';
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarBannerComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}

/** Each layer's children, in Figma's order: the tree the shell draws (`drawChildren`). */
export const solarBannerTree: Record<string, string[]> = {
  root: [
    'iconInfo',
    'description',
    'actionGroup',
    'close',
    'iconSuccess',
    'iconWarning',
    'iconDanger',
  ],
  actionGroup: ['primaryButton', 'secondaryButton', 'action'],
};

/** Each slot's layer, to the slot it is: its class is `SolarBanner-<slot>`, public. */
export const solarBannerSlots: Record<string, string> = {
  description: 'description',
  primaryButton: 'primaryButton',
  secondaryButton: 'secondaryButton',
  action: 'action',
  close: 'close',
};
