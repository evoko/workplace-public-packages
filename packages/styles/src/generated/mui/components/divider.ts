// SOLAR Divider recipe for MUI. Generated by @bwp-web/codegen from spec/components/divider.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export type SolarDividerOrientation = 'horizontal' | 'vertical';
export type SolarDividerType = 'full' | 'inset' | 'with-label';

export interface SolarDividerProps {
  orientation?: SolarDividerOrientation;
  type?: SolarDividerType;
}

export const solarDividerDefaults = {
  orientation: 'horizontal',
  type: 'full',
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarDividerStyles = {
  reset: {
    display: 'flex',
    position: 'relative',
    boxSizing: 'border-box',
    flexShrink: '0',
    '& .SolarDivider-box': {
      display: 'flex',
      boxSizing: 'border-box',
    },
    '& .SolarDivider-text': {
      whiteSpace: 'nowrap',
    },
    '& .SolarDivider-glyph': {
      display: 'block',
      overflow: 'visible',
      flexShrink: '0',
    },
    '& .SolarDivider-drawnIcon': {
      flexShrink: '0',
    },
    '& .SolarGlyph-fill, & .SolarGlyph-stroke': {
      stroke: 'none',
    },
  },
  root: {
    backgroundColor: 'transparent',
    borderColor: 'transparent',
    boxShadow: 'none',
    borderRadius: 'var(--solar-radius-none)',
    borderStyle: 'none',
    flexDirection: 'row',
    justifyContent: 'flex-start',
    alignItems: 'center',
    gap: 'var(--solar-inset-none)',
    paddingTop: 'var(--solar-inset-none)',
    paddingRight: 'var(--solar-inset-none)',
    paddingBottom: 'var(--solar-inset-none)',
    paddingLeft: 'var(--solar-inset-none)',
    width: '100%',
    height: 'var(--solar-border-default)',
    '& .SolarDivider-rule': {
      backgroundColor: 'var(--solar-color-border-subtle)',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      width: '100%',
      height: 'var(--solar-border-default)',
    },
    '& .SolarDivider-label': {
      fontFamily: 'var(--solar-type-font-family-inter)',
      fontWeight: 'var(--solar-type-font-weight-500)',
      fontSize: 'var(--solar-type-size-label-sm)',
      lineHeight: 'var(--solar-type-line-height-label-sm)',
      letterSpacing: '-0.02em',
      textDecoration: 'none',
    },
    '& .SolarDivider-rule2': {
      borderRadius: 'var(--solar-radius-none)',
      borderStyle: 'none',
      width: '100%',
      height: 'var(--solar-border-default)',
    },
  },
  sizes: {},
  appearances: {
    'orientation=horizontal, type=inset': {
      paddingLeft: 'var(--solar-inset-md)',
      width: '100%',
    },
    'orientation=horizontal, type=with-label': {
      justifyContent: 'center',
      alignItems: 'center',
      gap: 'var(--solar-stack-sm)',
      height: '20px',
      '& .SolarDivider-label': {
        color: 'var(--solar-color-text-tertiary)',
      },
      '& .SolarDivider-rule2': {
        backgroundColor: 'var(--solar-color-border-subtle)',
        borderColor: 'transparent',
        boxShadow: 'none',
      },
    },
    'orientation=vertical, type=full': {
      flexDirection: 'column',
      width: 'var(--solar-border-default)',
      height: '100%',
      '& .SolarDivider-rule': {
        width: 'var(--solar-border-default)',
        height: '100%',
      },
    },
    'orientation=vertical, type=inset': {
      flexDirection: 'column',
      paddingLeft: 'var(--solar-inset-md)',
      '& .SolarDivider-rule': {
        width: 'var(--solar-border-default)',
        height: '100%',
      },
    },
    'orientation=vertical, type=with-label': {
      flexDirection: 'column',
      justifyContent: 'center',
      alignItems: 'center',
      gap: 'var(--solar-stack-sm)',
      '& .SolarDivider-rule': {
        width: 'var(--solar-border-default)',
        height: '100%',
      },
    },
  },
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarDividerComposition = {
  root: {
    base: {
      present: true,
    },
  },
  rule: {
    base: {
      present: true,
    },
  },
  label: {
    base: {
      present: false,
    },
    appearance: {
      'orientation=horizontal, type=with-label': {
        default: {
          present: true,
        },
      },
    },
  },
  rule2: {
    base: {
      present: false,
    },
    appearance: {
      'orientation=horizontal, type=with-label': {
        default: {
          present: true,
        },
      },
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarDividerStyle(props: SolarDividerProps = {}): Style {
  const p: Record<string, unknown> = { ...solarDividerDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `orientation=${p.orientation}, type=${p.type}`;
  const s = solarDividerStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  return merge(s.reset, s.root, s.appearances[key]);
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarDividerParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarDividerParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarDividerCompose(
  props: SolarDividerProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarDividerDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `orientation=${p.orientation}, type=${p.type}`;
  const size = '';
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarDividerComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}
