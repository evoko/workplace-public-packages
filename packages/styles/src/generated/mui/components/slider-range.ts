// SOLAR Slider Range recipe for MUI. Generated by @bwp-web/codegen from spec/components/slider range.json. Do not edit.
// Plain data on purpose: this module imports nothing, so @bwp-web/styles stays dependency free.
// Every value is a var(--solar-*) reference into tokens.css, which must be loaded.

export interface SolarSliderRangeProps {
  disabled?: boolean;
}

export const solarSliderRangeDefaults = {
  disabled: false,
} as const;

/** Style by layer and state: `root` is the base, then per size, per appearance, and per size and appearance together. */
export const solarSliderRangeStyles = {
  reset: {
    display: 'block',
    padding: '0',
    boxSizing: 'border-box',
    '& .MuiSlider-rail': {
      opacity: '1',
      transform: 'none',
    },
    '& .MuiSlider-track': {
      border: 'none',
      transform: 'none',
    },
    '& .MuiSlider-thumb': {
      transform: 'translateX(-50%)',
      boxSizing: 'border-box',
      '&::before': {
        display: 'none',
      },
      '&::after': {
        width: 'var(--solar-size-target-min)',
        height: 'var(--solar-size-target-min)',
      },
    },
    '&': {
      position: 'relative',
    },
    '&::after': {
      content: '""',
      position: 'absolute',
      top: '50%',
      left: '50%',
      width: 'max(100%, var(--solar-size-target-min))',
      height: 'max(100%, var(--solar-size-target-min))',
      transform: 'translate(-50%, -50%)',
    },
  },
  root: {
    backgroundColor: 'transparent',
    borderColor: 'transparent',
    boxShadow: 'none',
    borderRadius: 'var(--solar-radius-none)',
    borderStyle: 'none',
    '--solar-placed-left': '0px',
    '--solar-placed-top': '0px',
    width: '100%',
    height: '20px',
    '& .MuiSlider-rail': {
      backgroundColor: 'var(--solar-color-surface-muted)',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-pill)',
      borderStyle: 'none',
      width: '100%',
      minWidth: 0,
      height: '4px',
      minHeight: '4px',
      position: 'absolute',
      left: 'calc(0px - var(--solar-placed-left, 0px))',
      top: 'calc(8px - var(--solar-placed-top, 0px))',
    },
    '& .MuiSlider-track': {
      backgroundColor: 'var(--solar-color-action-primary-bg-default)',
      borderColor: 'transparent',
      boxShadow: 'none',
      borderRadius: 'var(--solar-radius-pill)',
      borderStyle: 'none',
      height: '4px',
      minHeight: '4px',
      top: 'calc(8px - var(--solar-placed-top, 0px))',
    },
    '& .MuiSlider-thumb[data-index="0"]': {
      backgroundColor: 'var(--solar-color-surface-base)',
      borderColor: 'var(--solar-color-action-primary-border-default)',
      boxShadow: 'var(--solar-shadow-control)',
      borderRadius: 'var(--solar-radius-pill)',
      borderWidth: 'var(--solar-border-strong)',
      borderStyle: 'solid',
      width: '16px',
      minWidth: '16px',
      height: '16px',
      minHeight: '16px',
      top: 'calc(2px - var(--solar-placed-top, 0px))',
    },
    '& .MuiSlider-thumb[data-index="1"]': {
      backgroundColor: 'var(--solar-color-surface-base)',
      borderColor: 'var(--solar-color-action-primary-border-default)',
      boxShadow: 'var(--solar-shadow-control)',
      borderRadius: 'var(--solar-radius-pill)',
      borderWidth: 'var(--solar-border-strong)',
      borderStyle: 'solid',
      width: '16px',
      minWidth: '16px',
      height: '16px',
      minHeight: '16px',
      top: 'calc(2px - var(--solar-placed-top, 0px))',
    },
    position: 'relative',
  },
  sizes: {},
  appearances: {
    default: {
      '&:hover': {
        '& .MuiSlider-track': {
          backgroundColor: 'var(--solar-color-action-primary-bg-hover)',
        },
        '& .MuiSlider-thumb[data-index="0"]': {
          borderColor: 'var(--solar-color-action-primary-border-hover)',
          boxShadow: 'var(--solar-shadow-raised)',
        },
        '& .MuiSlider-thumb[data-index="1"]': {
          borderColor: 'var(--solar-color-action-primary-border-hover)',
          boxShadow: 'var(--solar-shadow-raised)',
        },
      },
      '&:has(.Mui-active)': {
        '& .MuiSlider-track': {
          backgroundColor: 'var(--solar-color-action-primary-bg-active)',
        },
        '& .MuiSlider-thumb[data-index="0"]': {
          borderColor: 'var(--solar-color-action-primary-border-active)',
          boxShadow: 'var(--solar-shadow-control)',
        },
        '& .MuiSlider-thumb[data-index="1"]': {
          borderColor: 'var(--solar-color-action-primary-border-active)',
          boxShadow: 'var(--solar-shadow-control)',
        },
      },
      '&:has(.Mui-focusVisible)': {
        '& .MuiSlider-track': {
          backgroundColor: 'var(--solar-color-action-primary-bg-default)',
        },
        '& .MuiSlider-thumb[data-index="0"]': {
          boxShadow: 'var(--solar-shadow-focus-default)',
          borderColor: 'var(--solar-color-action-primary-border-default)',
        },
        '& .MuiSlider-thumb[data-index="1"]': {
          boxShadow: 'var(--solar-shadow-focus-default)',
          borderColor: 'var(--solar-color-action-primary-border-default)',
        },
      },
      '&.Mui-disabled': {
        '& .MuiSlider-track': {
          backgroundColor: 'var(--solar-color-action-primary-bg-disabled)',
        },
        '& .MuiSlider-thumb[data-index="0"]': {
          backgroundColor: 'var(--solar-color-action-primary-bg-disabled)',
          borderColor: 'var(--solar-color-border-subtle)',
          boxShadow: 'none',
          borderWidth: 'var(--solar-border-default)',
          borderStyle: 'solid',
          width: '14px',
          minWidth: '14px',
          height: '14px',
          minHeight: '14px',
          top: 'calc(3px - var(--solar-placed-top, 0px))',
        },
        '& .MuiSlider-thumb[data-index="1"]': {
          backgroundColor: 'var(--solar-color-action-primary-bg-disabled)',
          borderColor: 'var(--solar-color-border-subtle)',
          boxShadow: 'none',
          borderWidth: 'var(--solar-border-default)',
          borderStyle: 'solid',
          width: '14px',
          minWidth: '14px',
          height: '14px',
          minHeight: '14px',
          top: 'calc(3px - var(--solar-placed-top, 0px))',
        },
      },
    },
  },
  combined: {},
} as const;

/** What the shell renders rather than styles: which layers show, and which variant each child takes. */
export const solarSliderRangeComposition = {
  root: {
    base: {
      present: true,
    },
  },
  track: {
    base: {
      present: true,
    },
  },
  fill: {
    base: {
      present: true,
    },
  },
  handle: {
    base: {
      present: true,
    },
  },
  handle2: {
    base: {
      present: true,
    },
  },
} as const;

type Style = { [key: string]: string | Style };

function merge(...styles: (Style | undefined)[]): Style {
  const out: Style = {};
  for (const style of styles)
    for (const [k, v] of Object.entries(style ?? {}))
      out[k] =
        typeof v === 'object' && typeof out[k] === 'object'
          ? merge(out[k] as Style, v)
          : v;
  return out;
}

/** The complete style for one set of props, for `sx` or `styleOverrides.root`. */
export function solarSliderRangeStyle(
  props: SolarSliderRangeProps = {},
): Style {
  const p: Record<string, unknown> = { ...solarSliderRangeDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `default`;
  const s = solarSliderRangeStyles as unknown as {
    reset: Style;
    root: Style;
    sizes: Record<string, Style>;
    appearances: Record<string, Style>;
    combined: Record<string, Record<string, Style>>;
  };
  return merge(s.reset, s.root, s.appearances[key]);
}

/** A drawn layer's outline: its box, and the fill's and the stroke's paths (Figma's geometry). */
type Glyph = {
  width: number;
  height: number;
  fill: { d: string; evenOdd: boolean }[];
  stroke: { d: string; evenOdd: boolean }[];
};
/** One layer's composition: shown or not, its composed child, its glyph, its position. */
export type SolarSliderRangeParts = Record<
  string,
  string | number | boolean | Glyph | null
>;
type Parts = SolarSliderRangeParts;
type Layered = {
  base: Parts;
  size?: Record<string, Parts>;
  appearance?: Record<string, Record<string, Parts>>;
  combined?: Record<string, Record<string, Record<string, Parts>>>;
};

/**
 * What the shell renders for one set of props in one state, by layer: whether it shows, and which
 * component and variant a composed child takes. The same precedence as the style: base, size,
 * appearance, then size and appearance together, each at rest and then in the state.
 */
export function solarSliderRangeCompose(
  props: SolarSliderRangeProps = {},
  state: string = 'default',
): Record<string, Parts> {
  const p: Record<string, unknown> = { ...solarSliderRangeDefaults };
  for (const [k, v] of Object.entries(props)) if (v !== undefined) p[k] = v;
  const key = `default`;
  const size = '';
  const out: Record<string, Parts> = {};
  for (const [layer, c] of Object.entries(
    solarSliderRangeComposition as unknown as Record<string, Layered>,
  )) {
    const states = state === 'default' ? ['default'] : ['default', state];
    out[layer] = Object.assign(
      {},
      c.base,
      c.size?.[size],
      ...states.flatMap((st) => [
        c.appearance?.[key]?.[st],
        c.combined?.[size]?.[key]?.[st],
      ]),
    );
  }
  return out;
}

/** Each layer's children, in Figma's order: the tree the shell draws (`drawChildren`). */
export const solarSliderRangeTree: Record<string, string[]> = {
  root: ['track', 'fill', 'handle', 'handle2'],
};

/** Each slot's layer, to the slot it is: its class is `SolarSliderRange-<slot>`, public. */
export const solarSliderRangeSlots: Record<string, string> = {};
