// Generated by @bwp-web/codegen from spec/overlay/mui-theme.yaml and the component recipes. Do not edit.
// The stock MUI components the SOLAR MUI theme styles, each from a SOLAR component's recipe
// (createSolarThemeOptions' components). Every value is a var(--solar-*).

import { solarButtonStyle } from './components/button.js';
import { solarIconButtonStyle } from './components/icon-button.js';

type Style = Record<string, unknown>;
type Props = Record<string, unknown>;

/** A stock MUI component, not one a SOLAR shell draws its own recipe on (it carries data-solar). */
const stock = (p: Props) => p['data-solar'] === undefined;

/**
 * A recipe's style with each of `cells` stated in every state of the root (`&:hover`,
 * `&.Mui-disabled`…) at its resting value, where the recipe leaves it as at rest: MUI's own
 * variant changes it there, which the recipe for SOLAR's shell, on MUI's plain text variant, does
 * not need to undo.
 */
function restated(style: Style, cells: string[]): Style {
  const out: Style = { ...style };
  for (const [key, value] of Object.entries(style)) {
    if (!/^&[:.](?!:)/.test(key) || !value || typeof value !== 'object')
      continue;
    const block = { ...(value as Style) };
    for (const cell of cells)
      if (!(cell in block) && cell in style) block[cell] = style[cell];
    out[key] = block;
  }
  return out;
}

/** A recipe's style with each layer's selector moved to where MUI's own markup has it. */
function moved(style: Style, moves: [string, string][]): Style {
  const out: Style = {};
  for (const [key, value] of Object.entries(style)) {
    const to = moves.find(([from]) => from === key)?.[1] ?? key;
    const v =
      value && typeof value === 'object' ? moved(value as Style, moves) : value;
    out[to] =
      to in out && typeof out[to] === 'object' && typeof v === 'object'
        ? { ...(out[to] as Style), ...(v as Style) }
        : v;
  }
  return out;
}

/** One MUI component's theme entry: its default props, and a recipe's style per prop combination. */
export interface SolarMuiComponent {
  defaultProps: Record<string, unknown>;
  variants: { props: (p: Props) => boolean; style: Style }[];
}

export const solarMuiComponents: Record<string, SolarMuiComponent> = {
  // Button's recipe (spec/overlay/mui-theme.yaml).
  MuiButton: {
    defaultProps: { disableRipple: true },
    variants: [
      {
        props: (p) =>
          stock(p) &&
          p.variant === 'contained' &&
          p.color === 'primary' &&
          p.size === 'small',
        style: restated(
          solarButtonStyle({ variant: 'primary', danger: false, size: 'sm' }),
          ['boxShadow', 'borderWidth', 'borderStyle', 'borderColor'],
        ),
      },
      {
        props: (p) =>
          stock(p) &&
          p.variant === 'contained' &&
          p.color === 'primary' &&
          p.size === 'medium',
        style: restated(
          solarButtonStyle({ variant: 'primary', danger: false, size: 'md' }),
          ['boxShadow', 'borderWidth', 'borderStyle', 'borderColor'],
        ),
      },
      {
        props: (p) =>
          stock(p) &&
          p.variant === 'contained' &&
          p.color === 'primary' &&
          p.size === 'large',
        style: restated(
          solarButtonStyle({ variant: 'primary', danger: false, size: 'lg' }),
          ['boxShadow', 'borderWidth', 'borderStyle', 'borderColor'],
        ),
      },
      {
        props: (p) =>
          stock(p) &&
          p.variant === 'contained' &&
          p.color === 'error' &&
          p.size === 'small',
        style: restated(
          solarButtonStyle({ variant: 'primary', danger: true, size: 'sm' }),
          ['boxShadow', 'borderWidth', 'borderStyle', 'borderColor'],
        ),
      },
      {
        props: (p) =>
          stock(p) &&
          p.variant === 'contained' &&
          p.color === 'error' &&
          p.size === 'medium',
        style: restated(
          solarButtonStyle({ variant: 'primary', danger: true, size: 'md' }),
          ['boxShadow', 'borderWidth', 'borderStyle', 'borderColor'],
        ),
      },
      {
        props: (p) =>
          stock(p) &&
          p.variant === 'contained' &&
          p.color === 'error' &&
          p.size === 'large',
        style: restated(
          solarButtonStyle({ variant: 'primary', danger: true, size: 'lg' }),
          ['boxShadow', 'borderWidth', 'borderStyle', 'borderColor'],
        ),
      },
      {
        props: (p) =>
          stock(p) &&
          p.variant === 'outlined' &&
          p.color === 'primary' &&
          p.size === 'small',
        style: restated(
          solarButtonStyle({ variant: 'secondary', danger: false, size: 'sm' }),
          ['boxShadow', 'borderWidth', 'borderStyle', 'borderColor'],
        ),
      },
      {
        props: (p) =>
          stock(p) &&
          p.variant === 'outlined' &&
          p.color === 'primary' &&
          p.size === 'medium',
        style: restated(
          solarButtonStyle({ variant: 'secondary', danger: false, size: 'md' }),
          ['boxShadow', 'borderWidth', 'borderStyle', 'borderColor'],
        ),
      },
      {
        props: (p) =>
          stock(p) &&
          p.variant === 'outlined' &&
          p.color === 'primary' &&
          p.size === 'large',
        style: restated(
          solarButtonStyle({ variant: 'secondary', danger: false, size: 'lg' }),
          ['boxShadow', 'borderWidth', 'borderStyle', 'borderColor'],
        ),
      },
      {
        props: (p) =>
          stock(p) &&
          p.variant === 'outlined' &&
          p.color === 'error' &&
          p.size === 'small',
        style: restated(
          solarButtonStyle({ variant: 'secondary', danger: true, size: 'sm' }),
          ['boxShadow', 'borderWidth', 'borderStyle', 'borderColor'],
        ),
      },
      {
        props: (p) =>
          stock(p) &&
          p.variant === 'outlined' &&
          p.color === 'error' &&
          p.size === 'medium',
        style: restated(
          solarButtonStyle({ variant: 'secondary', danger: true, size: 'md' }),
          ['boxShadow', 'borderWidth', 'borderStyle', 'borderColor'],
        ),
      },
      {
        props: (p) =>
          stock(p) &&
          p.variant === 'outlined' &&
          p.color === 'error' &&
          p.size === 'large',
        style: restated(
          solarButtonStyle({ variant: 'secondary', danger: true, size: 'lg' }),
          ['boxShadow', 'borderWidth', 'borderStyle', 'borderColor'],
        ),
      },
      {
        props: (p) =>
          stock(p) &&
          p.variant === 'text' &&
          p.color === 'primary' &&
          p.size === 'small',
        style: restated(
          solarButtonStyle({ variant: 'tertiary', danger: false, size: 'sm' }),
          ['boxShadow', 'borderWidth', 'borderStyle', 'borderColor'],
        ),
      },
      {
        props: (p) =>
          stock(p) &&
          p.variant === 'text' &&
          p.color === 'primary' &&
          p.size === 'medium',
        style: restated(
          solarButtonStyle({ variant: 'tertiary', danger: false, size: 'md' }),
          ['boxShadow', 'borderWidth', 'borderStyle', 'borderColor'],
        ),
      },
      {
        props: (p) =>
          stock(p) &&
          p.variant === 'text' &&
          p.color === 'primary' &&
          p.size === 'large',
        style: restated(
          solarButtonStyle({ variant: 'tertiary', danger: false, size: 'lg' }),
          ['boxShadow', 'borderWidth', 'borderStyle', 'borderColor'],
        ),
      },
      {
        props: (p) =>
          stock(p) &&
          p.variant === 'text' &&
          p.color === 'error' &&
          p.size === 'small',
        style: restated(
          solarButtonStyle({ variant: 'tertiary', danger: true, size: 'sm' }),
          ['boxShadow', 'borderWidth', 'borderStyle', 'borderColor'],
        ),
      },
      {
        props: (p) =>
          stock(p) &&
          p.variant === 'text' &&
          p.color === 'error' &&
          p.size === 'medium',
        style: restated(
          solarButtonStyle({ variant: 'tertiary', danger: true, size: 'md' }),
          ['boxShadow', 'borderWidth', 'borderStyle', 'borderColor'],
        ),
      },
      {
        props: (p) =>
          stock(p) &&
          p.variant === 'text' &&
          p.color === 'error' &&
          p.size === 'large',
        style: restated(
          solarButtonStyle({ variant: 'tertiary', danger: true, size: 'lg' }),
          ['boxShadow', 'borderWidth', 'borderStyle', 'borderColor'],
        ),
      },
    ],
  },
  // Icon Button's recipe (spec/overlay/mui-theme.yaml).
  MuiIconButton: {
    defaultProps: { disableRipple: true },
    variants: [
      {
        props: (p) => stock(p) && p.color === 'primary' && p.size === 'small',
        style: moved(
          solarIconButtonStyle({
            shape: 'square',
            variant: 'primary',
            size: 'sm',
          }),
          [['& .SolarIconButton-icon', '& > svg']],
        ),
      },
      {
        props: (p) => stock(p) && p.color === 'primary' && p.size === 'medium',
        style: moved(
          solarIconButtonStyle({
            shape: 'square',
            variant: 'primary',
            size: 'md',
          }),
          [['& .SolarIconButton-icon', '& > svg']],
        ),
      },
      {
        props: (p) => stock(p) && p.color === 'primary' && p.size === 'large',
        style: moved(
          solarIconButtonStyle({
            shape: 'square',
            variant: 'primary',
            size: 'lg',
          }),
          [['& .SolarIconButton-icon', '& > svg']],
        ),
      },
      {
        props: (p) => stock(p) && p.color === 'secondary' && p.size === 'small',
        style: moved(
          solarIconButtonStyle({
            shape: 'square',
            variant: 'secondary',
            size: 'sm',
          }),
          [['& .SolarIconButton-icon', '& > svg']],
        ),
      },
      {
        props: (p) =>
          stock(p) && p.color === 'secondary' && p.size === 'medium',
        style: moved(
          solarIconButtonStyle({
            shape: 'square',
            variant: 'secondary',
            size: 'md',
          }),
          [['& .SolarIconButton-icon', '& > svg']],
        ),
      },
      {
        props: (p) => stock(p) && p.color === 'secondary' && p.size === 'large',
        style: moved(
          solarIconButtonStyle({
            shape: 'square',
            variant: 'secondary',
            size: 'lg',
          }),
          [['& .SolarIconButton-icon', '& > svg']],
        ),
      },
      {
        props: (p) => stock(p) && p.color === 'default' && p.size === 'small',
        style: moved(
          solarIconButtonStyle({
            shape: 'square',
            variant: 'tertiary',
            size: 'sm',
          }),
          [['& .SolarIconButton-icon', '& > svg']],
        ),
      },
      {
        props: (p) => stock(p) && p.color === 'default' && p.size === 'medium',
        style: moved(
          solarIconButtonStyle({
            shape: 'square',
            variant: 'tertiary',
            size: 'md',
          }),
          [['& .SolarIconButton-icon', '& > svg']],
        ),
      },
      {
        props: (p) => stock(p) && p.color === 'default' && p.size === 'large',
        style: moved(
          solarIconButtonStyle({
            shape: 'square',
            variant: 'tertiary',
            size: 'lg',
          }),
          [['& .SolarIconButton-icon', '& > svg']],
        ),
      },
    ],
  },
};

/**
 * Which SOLAR component each themed MUI component draws, and how its MUI props map onto the SOLAR
 * axes: the decisions, as data, for an app or a check that goes from one to the other.
 */
export const solarMuiThemeDecisions = {
  MuiButton: {
    component: 'Button',
    props: {
      variant: {
        axis: 'variant',
        values: {
          contained: 'primary',
          outlined: 'secondary',
          text: 'tertiary',
        },
      },
      color: {
        axis: 'danger',
        values: {
          primary: false,
          error: true,
        },
      },
      size: {
        axis: 'size',
        values: {
          small: 'sm',
          medium: 'md',
          large: 'lg',
        },
      },
    },
    fixed: {},
    slots: {},
  },
  MuiIconButton: {
    component: 'Icon Button',
    props: {
      color: {
        axis: 'variant',
        values: {
          primary: 'primary',
          secondary: 'secondary',
          default: 'tertiary',
        },
      },
      size: {
        axis: 'size',
        values: {
          small: 'sm',
          medium: 'md',
          large: 'lg',
        },
      },
    },
    fixed: {
      shape: 'square',
    },
    slots: {
      icon: '& > svg',
    },
  },
} as const;
