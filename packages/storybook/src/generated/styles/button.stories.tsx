// Generated by @bwp-web/ds-compiler 2.0.0-alpha.0 for target stories from design.ir.json (source hash 9655bdf5b3a6db78f453f2e180e15b3befa6781e0d7cd6468cfd1934f95dea43). Do not edit; run bwp-ds generate.

import type { Meta, StoryObj } from '@storybook/react-vite';
import { Button, createBwpTheme } from '@bwp-web/styles-mui';
import { CompareGrid, parityPlay, type CompareRow, type CompareSpec } from '../../harness';
import { storiesConfig } from '../config';

const spec: CompareSpec = {
  name: 'button',
  displayName: 'Button',
  rootElement: 'button',
  axes: [
    {
      name: 'variant',
      values: [
        'filled',
        'ghost',
      ],
      default: 'filled',
    },
    {
      name: 'size',
      values: [
        'sm',
        'md',
      ],
      default: 'md',
    },
  ],
  states: [
    { name: 'hover', kind: 'hover' },
    { name: 'focus-visible', kind: 'focus-visible' },
    { name: 'active', kind: 'active' },
    { name: 'disabled', kind: 'attribute', attributes: { disabled: '' }, muiProp: 'disabled' },
  ],
  slots: [{ name: 'icon', element: 'span', content: 'plus' }],
  label: 'Button',
  labelSlot: null,
  labelElement: 'span',
  tailwind: true,
  mui: {
    rootClass: 'MuiButton-root',
    slotClasses: { icon: 'MuiButton-startIcon' },
    theme: createBwpTheme(),
    render: (row: CompareRow) => (
      <Button variant={row.axes.variant as 'filled' | 'ghost'} size={row.axes.size as 'sm' | 'md'} disabled={row.state === 'disabled'} icon={'plus'}>
        {'Button'}
      </Button>
    ),
  },
};

const meta: Meta = {
  title: 'Styles/Button',
  parameters: { layout: 'padded' },
};

export default meta;

export const Compare: StoryObj = {
  render: (_args, context) => <CompareGrid spec={spec} config={storiesConfig} targets={context.globals.dsTargets} />,
  play: (context) => parityPlay(context, spec, storiesConfig),
};
