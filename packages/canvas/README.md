# @bwp-web/canvas

The interactive canvas editor and viewer. Not started in V2: the package is an empty skeleton, since
it needs the SOLAR Spatial library, which has not been extracted from Figma yet
([open work](../../docs/engineering/open-work.md)). For the V1 release line see the `v1` branch
and the `v1-latest` npm dist-tag.
