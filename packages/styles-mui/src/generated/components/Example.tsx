// Generated by @bwp-web/ds-compiler 2.0.0-alpha.0 for target mui from design.ir.json (source hash 776d802d307e00c3c842964b88b64ca0171a616bbf262b8d42a7a5beb2149f73). Do not edit; run bwp-ds generate.

import * as React from 'react';
import { styled, useThemeProps } from '@mui/material/styles';

export type ExampleTone = 'neutral' | 'accent';
export type ExampleSize = 'sm' | 'md';

export interface ExampleOwnerState {
  tone: ExampleTone;
  size: ExampleSize;
}

export interface ExampleProps
  extends Omit<React.ComponentPropsWithoutRef<'button'>, 'children' | 'disabled' | 'icon' | 'size' | 'tone'> {
  /** Axis `tone`; default `neutral`. */
  tone?: ExampleTone;
  /** Axis `size`; default `md`. */
  size?: ExampleSize;
  /** State `disabled`; rendered as the `disabled` attribute. */
  disabled?: boolean;
  /** Slot `label`. */
  children?: React.ReactNode;
  /** Slot `icon` (optional). */
  icon?: React.ReactNode;
}

export const exampleClasses = {
  root: 'BwpExample-root',
  label: 'BwpExample-label',
  icon: 'BwpExample-icon',
} as const;

const ExampleRoot = styled('button', {
  name: 'BwpExample',
  slot: 'Root',
  overridesResolver: (_props, styles) => styles.root,
})<{ ownerState: ExampleOwnerState }>({});

export const Example = React.forwardRef<React.ComponentRef<'button'>, ExampleProps>(
  function Example(inProps, ref) {
    const props = useThemeProps({ props: inProps, name: 'BwpExample' });
    const { tone = 'neutral', size = 'md', disabled = false, children, icon, className, ...other } = props;
    const ownerState: ExampleOwnerState = { tone, size };
    return (
      <ExampleRoot
        ref={ref}
        ownerState={ownerState}
        className={className ? `${exampleClasses.root} ${className}` : exampleClasses.root}
        {...other}
        disabled={disabled}
      >
        <span className={exampleClasses.label}>{children}</span>
        {icon === undefined ? null : <span className={exampleClasses.icon}>{icon}</span>}
      </ExampleRoot>
    );
  },
);
