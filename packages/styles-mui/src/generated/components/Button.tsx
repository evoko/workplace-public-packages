// Generated by @bwp-web/ds-compiler 2.0.0-alpha.0 for target mui from design.ir.json (source hash 776d802d307e00c3c842964b88b64ca0171a616bbf262b8d42a7a5beb2149f73). Do not edit; run bwp-ds generate.

import * as React from 'react';
import { Button as MuiButton } from '@mui/material';
import '../augmentation.js';

export type ButtonVariant = 'filled' | 'ghost';
export type ButtonSize = 'sm' | 'md';

export interface ButtonProps
  extends Omit<React.ComponentPropsWithoutRef<'button'>, 'LinkComponent' | 'TouchRippleProps' | 'action' | 'centerRipple' | 'children' | 'classes' | 'color' | 'disableElevation' | 'disableFocusRipple' | 'disableRipple' | 'disableTouchRipple' | 'disabled' | 'endIcon' | 'focusRipple' | 'focusVisibleClassName' | 'fullWidth' | 'href' | 'icon' | 'loading' | 'loadingIndicator' | 'loadingPosition' | 'nativeButton' | 'onFocusVisible' | 'size' | 'startIcon' | 'sx' | 'touchRippleRef' | 'variant'> {
  /** Axis `variant`; default `filled`. MUI prop `variant`. */
  variant?: ButtonVariant;
  /** Axis `size`; default `md`. MUI prop `size`. */
  size?: ButtonSize;
  /** State `disabled`; MUI's `disabled` prop. */
  disabled?: boolean;
  /** Content of the root element. */
  children?: React.ReactNode;
  /** Slot `icon` (optional); MUI slot `startIcon`. */
  icon?: React.ReactNode;
}

export const buttonClasses = {
  root: 'MuiButton-root',
  icon: 'MuiButton-startIcon',
} as const;

/** MUI's `Button` with the design system's props; `theme.components.MuiButton` carries the styles. */
export const Button = React.forwardRef<React.ComponentRef<'button'>, ButtonProps>(
  function Button(props, ref) {
    const { variant = 'filled', size = 'md', disabled = false, children, icon, ...other } = props;
    return (
      <MuiButton
        ref={ref}
        variant={variant}
        size={size}
        startIcon={icon}
        {...other}
        disabled={disabled}
      >
        {children}
      </MuiButton>
    );
  },
);
